
(()=>{const input=document.querySelector('#search'),sections=[...document.querySelectorAll('main>section')];function filter(){const q=input.value.trim().toLowerCase();let n=0;for(const s of sections){const rows=[...s.querySelectorAll('tbody tr')];let hits=0;for(const r of rows){r.hidden=!!q&&!r.textContent.toLowerCase().includes(q);if(!r.hidden)hits++;}const hasProse=[...s.querySelectorAll('p,li,h2,h3')].some(x=>!x.closest('table')&&x.textContent.toLowerCase().includes(q));const visible=!q||hits>0||hasProse;s.classList.toggle('filter-miss',!visible);if(visible)n++;for(const t of s.querySelectorAll('table'))t.closest('.table-wrap')?.toggleAttribute('hidden',!!q&&![...t.querySelectorAll('tbody tr')].some(r=>!r.hidden));}document.querySelector('#found').textContent=q?n+' matching sections':'';document.querySelector('#empty').hidden=n>0;}input.addEventListener('input',filter);document.querySelector('#clear').onclick=()=>{input.value='';filter();input.focus();};document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>{input.value='';filter();}));})();