{
  "_comment": "Lintel session-digest SessionStart hook (ADR-0002). NOTE: since v5.0 the plugin auto-registers this via hooks/hooks.json — plugin installs need NO manual merge. This snippet remains ONLY for non-plugin installs (bare ~/.lintel checkout without the Claude Code plugin). Merge into ~/.claude/settings.json (or a repo's .claude/settings.json). Verify with: li-doctor.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.lintel/hooks/shared/session-digest/run.sh\""
          }
        ]
      }
    ]
  }
}
