{
  "_comment": "Lintel session-digest SessionStart hook (ADR-0002). Merge this into ~/.claude/settings.json (or a repo's .claude/settings.json). li-scaffold wires it per-repo by default; for a global install, merge by hand. Verify with: li-doctor.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.lintel/hooks/session-digest/run.sh\""
          }
        ]
      }
    ]
  }
}
