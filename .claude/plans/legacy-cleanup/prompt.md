# Cold-executor prompt: Removing legacy skills and agents

## Context

Read spec.md, plan.md and swarm/charter.md in this directory. This is the explicitly
authorized cleanup released at 2026-09-25 09:08 CEST. The parent coordinator is session
`f4584b03-22c2-468c-abe8-cb1bc3016a6d`; integration host/routing identity is
`e9c20b62-f877-4242-82cd-b5002d452da8` (workspace project-session identifier:
`e9d7470d-278c-4d7a-8a73-e1fd7a108ece`). Starting HEAD is
`d285a33283ec90341b79f3d093d1e9ca6a1504ce`, not the obsolete audit baseline.
Use the six existing isolated lanes, never a replacement parallel backlog.

## Constraints

The exact frozen paths in spec.md override ordinary lane ownership until PR #93
merges. Do not edit another checkout, install refused dependencies, restore the
removed PDF reader, mutate production, touch presentation publication or bypass CI.
Keep real browser/document/install/context/evidence behavior. Preserve legal notices.
Do not repeat the retired upstream identity in new prose or artifact names.

## Acceptance and deliverables

Complete every selected original leaf with actual evidence, attributable scope
and independent spec then quality review. Regenerate only after fan-in and verified
upstream-main reconciliation. One aggregate PR titled "Removing legacy skills and
agents" is created by the integration session using the supported PR tool against main.
Normal merge requires green hosted checks and completed review. Verify remote main
contains the merged changes, or report the exact remaining external blocker.

## How to resume

Validate work.json and swarm/coordination.json with the trusted repository helpers.
Recover from actual branches, scoped changes and reports. An idle session is not a
completed package. Keep every worker worktree until its work is accounted for.
Use normal host/model defaults. Workers never write shared state or generated outputs.
The coordinator owns commits and serial integration.
