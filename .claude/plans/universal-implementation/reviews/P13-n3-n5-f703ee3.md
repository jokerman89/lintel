# P13 N3/N5 follow-up recheck

**Verdict: scoped SPEC PASS, then eligible QUALITY PASS.** Findings: P1 0 / P2 0 / P3 0.
N3 and N5 from the [fan-in review](P13-fanin-f581b07.md) are closed. This recheck covers
only the optional test and comment follow-up. The A19.3, A19.4 and A20.4 closures at
`4a7d7456` do not depend on it. It is not a merge, version or delivery decision. The
reviewer changed no repository file, branch or commit; per coordinator88 this file is
delivered outside the repository for preservation.

## Frozen scope

Reviewer: `30146eab-ddeb-467e-8f1b-8da575256ba2` (`claude-opus-5.5`, max reasoning, long
context). Request: the P13 owner's recheck message of 16:49, with the handoff below.

| Identity | Exact value |
|---|---|
| Base (recovery at intake) | `1fda8ee0242ffe5651241f78656325da35f2ef07` |
| N3 commit | `03604b2f497a0f73657e3abf98686cdf33eebfbf` |
| N5 commit | `f703ee3f086e8ac86f8a06d064211c5dfd7397fd` |
| N3 file Git/LF SHA-256 | `7e0c7ceec3a77a4d3afd0e321db86af05d09a2d7cf3442675e64c850037af425` |
| N3 file CRLF checkout SHA-256 | `1b73b09f95c5f17d3423436573ac400076d00fb1114cb0068cdd764db22001a6` |
| N5 file Git/LF SHA-256 | `927f3e07237199003be08013ceb7248ba5137e9addbe4b4b0b050c1f60a278cc` |
| N5 file CRLF checkout SHA-256 | `84e5355ead5ddef197e758057ddd964d8c3593ff7780d29295763ee420b3d5a3` |
| Owner handoff SHA-256 | `d11f8429370c75f7f421116dbdca17c3f14fb9ffa1ba7dea017dc51a4ba8934d` |
| Owner packet manifest SHA-256 (479 entries) | `be322478e7253df586023fecdbb7b14c05248ece9e3cbdef36d4dd157d8d60cd` |

Independently verified identity:

- `03604b2f` has sole parent `1fda8ee0` and changes only `tests/integration/catalog-installed.py`.
  `f703ee3f` has sole parent `03604b2f` and changes only `install/upstream-sources.yaml`. From
  the base to `f703ee3f` exactly these two paths change. Both commits are unsigned and carry
  only the Copilot trailer.
- All four stated file hashes match, both as Git blobs and as the CRLF checkout of a fresh
  clone. The base test file equals the accepted N2 product `e922515c` (`ece581d9…`), and the
  base registry blob hash matches the handoff (`0167c2b5…`).
- The handoff and manifest hashes match. 132 manifest entries for the N3, N2-RED, N5,
  final-verify and recovery-drift receipts all rehash exactly.
- Recovery has since advanced to `2799f99d` (20 commits, 190 paths). Neither of the two paths
  is among them, so the follow-up applies unchanged. That delta includes reconciliation 7
  (`ac216479`), which adds `lib/memory.sh` to `ADAPTER_RESOURCES` as the fan-in review's N4
  recommended. It was observed here, not reviewed.

## SPEC

| Obligation | Result and concrete evidence |
|---|---|
| N3: the junction child gets synthetic roots and the telemetry opt-out | PASS. `child_environment` (`catalog-installed.py:313-330`) starts from the same system allowlist as the installed runs, creates `pwsh/{h,a,l,tmp}` inside the test's own resolved temporary root, checks each is an ordinary resolved directory, and sets HOME, USERPROFILE, APPDATA, LOCALAPPDATA, TEMP, TMP, TMPDIR, HOMEDRIVE, HOMEPATH and `POWERSHELL_TELEMETRY_OPTOUT=1` (`:329`). The child reports its environment and `[IO.Path]::GetTempPath()`, and the test asserts both (`:356-358`). |
| N3: fail-closed PowerShell 7 requirement and the N2 guard unchanged | PASS. `shutil.which("pwsh")` with its assertion (`:341-342`) and the in-child major-7 check remain. The guard at `:44-47` is byte-identical to the accepted N2 product. |
| N3: discriminating evidence | PASS, reproduced independently. In the reviewer launcher, the base file's run rewrote the launcher's synthetic `h\AppData\Local\Microsoft\PowerShell\StartupProfileData-NonInteractive` (size 68,996 → 69,308, new mtime and hash). With the old guard order restored in a scratch copy, the run exited 1: only the linked-root method failed, a `p13i-*` root was admitted inside `owned-target`, and the launcher roots were unchanged. The frozen file passed 2/2 with no admitted root. The child's observed environment was entirely under the test root's `pwsh\`, with opt-out `1`. Its only state file was that test-local `StartupProfileData`, the launcher roots were again unchanged, and the temporary root was cleaned up. |
| N5: the registry comment no longer points to a missing bundled path | PASS. Line 7 became lines 7-8: "See docs/provenance.md in the Lintel source repository." Every non-comment line is identical. The shared reader and PyYAML both parse the new file equal to the base; the parsed-JSON SHA-256 `2a0463bbc76594ffbf94414176641092868da1df1dcb116bd4ce850dcabc736b` matches the owner's. A fresh installation of `f703ee3f` bundles the new registry content-equal to the source, and its inventory digest matches. A `design-knowledge` selection over the installed bundle still declares both provenance records. |

## Independent execution

Every test, installation and analysis process ran through the reviewer launcher
`launch3.ps1` (SHA-256 `fa7b729ccc24db5cf219db817d9635fd8bf28c91b5f83adb3d0e79f840de5cc6`;
the fan-in launcher with receipts in `o3`). It supplies synthetic HOME, USERPROFILE,
APPDATA, LOCALAPPDATA, TEMP, TMP, TMPDIR and XDG roots under `%LOCALAPPDATA%\Temp\p13v`,
the CI-style `python3` wrapper, a Git ceiling, `GIT_CONFIG_NOSYSTEM=1`,
`GIT_CONFIG_GLOBAL=NUL`, no `LINTEL_*`, PowerShell 7 only (appended to PATH for the N3
runs) and no jq. The source is a fresh `git clone --shared` checked out detached at
`f703ee3f` with per-command `core.autocrlf=true` (no configuration write). After all runs
its HEAD was unchanged and `git status --ignored --untracked-files=all` was empty.

| Check | Outcome |
|---|---|
| `FixtureRootAdmission` on the base file | exit 0, 2/2; launcher `h` state rewritten |
| `FixtureRootAdmission`, old guard order on the N3 file (scratch copy) | exit 1; linked-root method only; launcher roots unchanged |
| `FixtureRootAdmission` on the frozen file | exit 0, 2/2; child state test-local; launcher roots unchanged |
| `li-lifecycle.py scaffold init --client copilot-cli` into a synthetic target | exit 0; 463 managed files; installed registry verified as in N5 |
| `bin/li-catalog.py --check` | exit 0, "Catalog matches skill frontmatter." |
| `tests/unit/catalog-metadata.sh`, `catalog-selection.sh` | 31 and 29 ok; zero skips |
| `bin/li-copilot.py check --target . --source .` (CI step) | exit 0 |
| `bash -n tests/integration/catalog-installed.sh` | exit 0 |

Git Bash printed no warning. `/tmp` is mounted on this reviewer's `p13v\t`, which
coordinator88 reports is now pinned host-wide (L-049); it stays in place until released.

## QUALITY (eligible after SPEC PASS)

The change is minimal and targeted: it reuses the class's own isolation pattern, asserts
the observed child environment rather than assuming it, and keeps the fail-closed
PowerShell 7 requirement and the accepted guard. Resolving the temporary base
(`catalog-installed.py:334`) is needed for the `path.resolve() == path` check on hosts with
short-name temp paths. The comment change keeps the declaration-only file's data intact and
makes a true statement for both the source and the bundled copy.

Observations, not findings:

- `TMPDIR` is set but absent from `CHILD_KEYS` (`:311`), so it is not asserted. Windows
  PowerShell 7 does not use it for `GetTempPath`, so this has no effect.
- PowerShell writes `StartupProfileData` under `USERPROFILE\AppData\Local`, not under
  LOCALAPPDATA. The test's `pwsh\l` therefore stays empty, and the state still lands inside
  the test root.
- The opt-out appears effective: without it, the first launcher run created
  `telemetry.uuid` in a fresh home, while the opted-out child homes contain only
  `StartupProfileData`. Whether any telemetry was transmitted was not observed.
- N5 fixes the dangling reference by rewording. It does not bundle the contribution
  procedure, which remains in the source repository only, and the handoff states this
  honestly.

## Evidence and limits

Private evidence is in `%LOCALAPPDATA%\Temp\p13v`: `o3\` (launcher, scripts, receipts and
`g-n3-snapshots.json`), `g\` (clone) and `i\` (installed target). The N3 run orchestrator
(`n3_runs.ps1`) and the clone setup ran in the reviewer's PowerShell 7 tool shell. They
performed Git reads, file hashing, launcher-root snapshots, and the writing and removal of
the two scratch test copies; every Python and PowerShell product process ran through the
launcher. The scratch copies were verified removed and the frozen file restored
byte-exactly (`1b73b09f…`). Keep `p13v` and especially `p13v\t` until coordinator88
releases them.

Limits: Windows with Python 3.11.9 only; no Linux, macOS or hosted CI; the non-Windows
symlink branch did not run; no full installed-consumer suite rerun (only the changed class,
one installation and the registry readers); no network or telemetry observation. The
recheck is on base `1fda8ee0`, not the current recovery tip, although no overlapping path
changed.

Retained reviewer failure: the first check of this file failed because it stated 135
spot-checked manifest entries instead of the actual 132; the count was corrected before the
passing recheck.
