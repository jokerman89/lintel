# P11 browser-only independent review

**Date:** 2026-09-22. **Result:** scoped SPEC passed first; separately staged
QUALITY failed with **P1 0 / P2 1 / P3 0**. Hold A16 integration for the numeric
extraction correction below. This does not close A14, all P11, or release acceptance.

**Reviewer:** app session `d823e773-d8dc-4d02-9716-77506a7a260e`, CLI context
`1f655ca9-eb2b-4325-9996-72ed8539c13b`; distinct from builder app session
`f413bdcb-e081-45e0-9524-7274b4391665` and its recorded execution context
`0517db27-55b8-43a3-a31f-39d0d6d24d74`. No worker/reviewer was spawned and no
product was repaired. Recovery coordinator: `88aecc43-40f9-41d4-8947-6c2fb0a55481`.

## Exact selection and authority

| Input | Pinned identity |
|---|---|
| Original base | `56df03d1757b483048e68ac846a3863ff558bb6b` |
| Frozen product | `311579071100bfaaa0f1762d4672939c643d7129` |
| Builder report / review parent | `043019ace7916170de9376c8bd8726c1cd5d4744` |
| Complete 717-line builder report, Git-byte SHA-256 | `1b7829bd1b784e265af9979cadb0ce6c3b21479d65392436d33e073ba826339e` |
| Final readiness authority | `810ae686adf5e2bbbc072e7c4efeeff533744ea4` |

Reviewed the complete `56df03d -> d389905 -> 84f0d38 -> f4cd1f3 -> a49e7b8 ->
3115790 -> 043019a` unit, not only the last three-file change. The current P11
card read at recovery `0ad183a` is byte-identical to the card at `810ae686`.
Read startup/Copilot/Universal contracts, neutral policy, relevant memory/ADRs,
mapped spec/plan and original A16 action, complete source/report, and all four
pre-change workflows. Original headless acceptance controls the review; the
later authority does not turn this into all-platform or document-layout certification.

All eleven original product paths were selected, read and sealed:

```text
skills\browse\SKILL.md
skills\browse\references\browser-operations.md
skills\browse\scripts\chromium.mjs
skills\open-managed-browser\SKILL.md
skills\setup-browser-cookies\SKILL.md
skills\scrape\SKILL.md
skills\scrape\scripts\extract.mjs
tests\integration\browser-operations.test.mjs
tests\integration\browser-live.mjs
tests\integration\design-browser-pipeline.py
tests\integration\design-browser-pipeline.sh
```

The builder report is the only additional changed path in that history; its
commit is report-only and all eleven product blobs equal `3115790`. No A14,
agents, document renderers, registry/profile/control/schema, P08/P10 or generated
output changes were found. This review adds only this report.

## Stage 1 - Complete scoped SPEC

This checkpoint was recorded and sent to the coordinator **before** QUALITY.
It verifies the original required scenarios; it does not pre-clear untested
correctness edges or waive the later P2 finding.

| Leaf | SPEC | Evidence and preservation |
|---|---|---|
| A16.1 | PASS | Four entry points share real open/read/act/screenshot/print/ownership methods. Retains action data, viewport/DOM/optional console, operator debugging/check, chosen login/revalidation, parsed selector/trim/numeric/multi/error/diff methods, and pacing/concurrency/robots instructions. YAML loading and batch scheduling remain honestly caller-owned; the concrete provider is serial. |
| A16.2 | PASS | Actual owned Chrome/CDP schema, paused exact-target selection and request/response interception precede navigation. Accepted P03 exact/wildcard/raw-Location rules and explicit origins remain shared, not copied. Final real origin/redirect/loop/script/resource/popup refusals and empty destination trap corroborate the source. No daemon/default personal session or OS-wide egress claim. |
| A16.3 | PASS | Exact-source retained run `a16-awm92xgk` completed 4 shared + 23 Node + 8 real headless scenarios. Mouse/Tab/Enter produced count 2; extraction changed 99.5 to 101.5; reduced motion was true with animation `none`. Independent artifact inspection verified both PNGs' actual pixels and two actual PDF pages/text/origins, not size proxies. |
| A16.4 | PASS | The documented method keeps surface choice/credential entry with the user and requires pausing capture during authentication; that manual flow was not executed. No credential/cookie/storage transfer or persistence promise. Actual password-fill/HTTP-auth refusals, profile-only/unavailable-engine negatives, ownership and cleanup support the headless boundary. Headed/manual login remains unobserved. |

Readiness correction is exactly the owned `DevToolsActivePort` ENOENT/EBUSY
predicate, retaining 100 reads/100-ms waits, malformed-endpoint refusal,
child-exit handling and immediate other-error/permission failures
(`chromium.mjs:186-196`). The five injected readiness cases are fault injection,
not proof that EBUSY occurred in the final native run or identification of its cause.

Retained actual execution is **builder-attributed**, not reviewer execution.
The browser-free reviewer verified 47 retained input hashes and reconciled all
18 live source seals with the pinned Git/working representations. Eight distinct
contexts have matching disposal, recorded exit 0, no cleanup errors and currently
absent owned profiles. Servers recorded successful health checks and shutdown;
119 proxy refusals were 403/non-forwarding; trap/credential observations were empty.
These facts do not establish all-descendant termination or OS-wide confinement.

## Stage 2 - Independent QUALITY

Reviewed correctness/error paths, ownership/authentication, shared-contract use,
boundedness, workflow preservation and regression coverage across the full unit.
Seven additional browser-free tests exercised the actual modules with explicitly
injected page/transport data: **6 passed, 1 failed, 0 skipped**.

| ID | Severity | File:lines | Finding and required correction | Confidence |
|---|---|---|---|---|
| Q1 | P2 - must fix | `skills\scrape\scripts\extract.mjs:29-33` | `number_extract` matches an internal digit substring instead of validating the numeric form, silently losing leading decimal points/signs. Preserve the complete amount/sign for supported forms, or emit an explicit field error for unsupported/ambiguous forms. Add regressions without broadening this into locale/currency infrastructure. | 10/10 |

Actual `extractPage` results with the same parsed numeric schema:

```text
page text   actual amount   actual record             acceptable result
$.50        50              ok:true, errors:[]         0.5 or explicit field error
-.50        50              ok:true, errors:[]        -0.5 or explicit field error
-$9.50      9.5             ok:true, errors:[]        -9.5 or explicit field error
```

This is reproducible silent data corruption in a retained scraping method, not
a request for every numeric locale. Ordinary `$99.50`, `-9.50`, `0.50 USD`,
`-0.50`, explicit multi-number/locale errors, trim/text/multi/diff and prototype-like
field keys passed. Page-change refusal and the real guard logic's five-hop/sixth-hop,
foreign/raw/duplicate/missing Location cases also passed with injected transport.
Those tests are not additional browser observations. A16.1's final acceptance is
held by Q1 even though its original SPEC scenarios passed.

## Commands, policy and evidence

Every product import/call, including the observer's P05 evaluation and outer source
comparisons, ran with inspected per-process allowlisted environment, explicit
synthetic HOME/USERPROFILE/AppData/LocalAppData/temp/XDG/LINTEL and derived paths,
PATHEXT, fixture Git configuration/ceilings and source seals before/after.
No personal startup, install, OS/global policy change, real-home incident inquiry,
new browser launch or denied-route workaround occurred. Logs retain true exits.

| Reviewer command (inside the sealed wrapper) | Actual outcome |
|---|---|
| `bash --noprofile --norc tests\integration\design-browser-pipeline.sh --pdf-reader pypdf` | Exit 0; 4 shared + 23 Node, no failures/skips; explicitly `browser:not_run`. Own run `a16-3zj0jrzo`. |
| `bash --noprofile --norc tests\unit\url-policy.sh` | Exit 0; 5 unchanged P03 regressions. |
| `bash --noprofile --norc tests\shape\frontmatter-lint-all.sh` | Exit 0; existing check, 127 skills/69 agents. No full suite. |
| `python -I -B <private>\observe_evidence.py` | First exit 1: reviewer compared extracted LF text with retained CRLF bytes. Retained hash plus eight CRLF pairs established the private oracle error. Corrected comparison preserves raw hashes and explicitly normalizes CRLF; second exit 0. Both records retained; no product changed. |
| `node --test --test-reporter=tap <private>\quality.test.mjs` | Exit 1; 7 tests, 6 pass/1 Q1 failure, no skips. No browser/network. |

Pillow 12.3.0 and explicitly selected pypdf 6.13.2 decoded the existing artifacts;
no package was installed. Actual PDF text and all 12 transformed text origins on
two pages match the retained records. The unchanged P05 evaluator independently
reproduced browser `pass` and **3.498154318627802:1 mandatory contrast fail/blocked**.
Passing this negative regression does not certify that deliberately inaccessible
fixture. Original P07 references remain distinct and unrebound; the reviewer static
run has its own `_default` 1.0.0 context. No private/company policy was activated.
Formal Stage 3 and strict v2 release clearance are not claimed after QUALITY failed.

Private reviewer evidence is in CLI session
`1f655ca9-eb2b-4325-9996-72ed8539c13b`, `files\p11-review\`:
`pin.json`, `spec-stage.json`, each command/log pair, `independent-evidence.json`,
`independent-printed-pages.json`, and the reproducible `quality.test.mjs`.
Decisive SHA-256 bindings:

```text
independent-evidence.json  7c9bc5634acaf287957e7e9622457c43a1a15a3ad3dfb75f3225e1e85d67d1c7
quality.test.mjs           3892ea61ae6a33e8664e7cf42c9a8b7aa7f7a8cdbdd2143f70c97a3002271449
quality.log                51b5dbc9154748227256c892532c4c39f70e2915e3d0f19e59763fdbce41bdfd
```

Builder evidence remains read-only in worktree `jokerman-microsoft-crispy-bassoon`,
`.claude\runtime\p11-a16\`: final run `a16-awm92xgk`, observer `a16-83x7usho`.
No current process mutation/query was needed; recorded exits and scoped profile
absence are the reviewed cleanup evidence.

## Limits and next action

The final **screen PNG was directly visible** to this reviewer: count 2,
$101.50, the deliberate low-contrast row and the stopped-motion square. The
print-media image hit the host's one-image limit. Both were pixel-inspected, but
**direct two-image visual clearance remains unverified**. PDF raster/full glyph
bounds, physical print, headed/login/persistence, other OS/clients, minimum
runtimes and installed-consumer execution remain unobserved. A print-media
screenshot is not a PDF raster.

Earlier startup/native failures, unknown background effects, the pdftotext crash,
policy-denied raster route, EBUSY/unknown lock holder and historical negative
contexts remain honest history, not superseded success. No consumed DONT_VERIFY
diagnostic or prohibited investigation was repeated.

**No original required headless observation is missing; the current block is Q1,
not a newly invented platform/layout gate.** The coordinator should release only
the existing builder's numeric-transform/test correction, then obtain the new
immutable source/report and affected independent re-review. Keep the builder
otherwise frozen; do not release A14 or call all P11 complete. This report is not
a fabricated independent actor receipt, strict v2 decision, or final integration
review.
