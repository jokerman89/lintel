# P10 final-candidate review: blocked positive observation

**Full SPEC: BLOCKED. First whole-P10 QUALITY: NOT STARTED.**
**F01, F02, F03 and F04: scoped CLOSED.**
**New attributable SPEC findings: P1 0 / P2 0 / P3 0.** **B01 is one unresolved
acceptance blocker**, not an invented handle, permission or compatibility diagnosis.

The supported linked-worktree initializer again failed while replacing its
transaction journal. The expressly authorized final-candidate attempt is complete;
it is not repeated. Other independent controls remain valid. This report does not
waive the missing positive observation, turn failed aggregates green, or release A13.

Same distinct independent reviewer; no product implementation or nested reviewer.
Only this report is committed. No product fix, policy change, global/home
investigation, permission probe, compatibility experiment or remote action occurred.
The WinError5 incidents were not recovered or rolled back.

## Exact scope and identities

| Identity | Value |
|---|---|
| Reviewed report HEAD / parent of this report | `4be5e0966937755ad19d7a2f6560c8fdbb27d479` |
| Product | `6e1b1e0047befae8fc45fd0858d337b117e2ff97` |
| Direct product parent | `50e654afcf7fae0548e4d16a4bf2d52b725523bc` |
| Original owned base | `fb96f71342f7994c42bdfc7cd61e6d452e6acb54` |
| Builder report Git SHA-256 | `87280a7bb620b3970301c09c70ffaa93ff3765399eed3a280483cf61738edacf` |
| F04 and remaining-selector authority | `b8f0cf7b3e827a90f5afc0c2eacc3a809adbd804` |
| Previous independent report | `131d3db4b61cdbb1792169913710522a15ecfeb6` |

The 17+1 original-selector continuation finished on its unchanged, separately
sealed `036e903` / `50e654af` export before any new F04 product execution.
Then clean detached `131d3db4` was retained at
`refs/heads/review-p10-036-frozen-131d3db`, and the worktree switched to exact
`4be5e096`. Earlier refs/reports, including `c1f6bad1`, remain preserved.
No reset, discard or amendment occurred.

The new product changes exactly three paths (+165/-11):
`bin/li-lifecycle.py`, `tests/integration/universal-lifecycle.py`, and
`docs/lifecycle.md`. Only production `layout_observation` changes; all other
production functions/mappings, the complete Copilot fixture and all 61 prior
lifecycle test-method ASTs remain unchanged. Four F04 test methods were added;
two private fixture helpers gained default-compatible options. The report-only
child changes only the builder report. A fresh 967-file raw-Git export was
verified against object IDs; 963 entries match the original-selector export.

| Current contract / changed test | Git-byte SHA-256 |
|---|---|
| `bin/li-lifecycle.py` | `99295ac651a07e6a8a59d03034534a731b8ff22fb9391e047e8794d6d2c621bf` |
| `docs/lifecycle.md` | `9f5b868728a03bb8ce7210701347af8871864eea6cca3cf63099d7f73b5b6070` |
| `tests/integration/universal-lifecycle.py` | `c32c6a97a85491638a71a7dc3fd976675b9cc7e179956300145cd50ecc4d2c79` |
| `bin/li-copilot.py` | `f43ac1d9ebe1a4f961754014d33fae2738d240d78bf35a02947597f7ef3e18fc` |
| `lib/managed_transaction.py` | `acbf84ef94a9fc4afdb8f66766d376653549ee82f4cf063c6a5721b1fdc08cae` |
| `docs/native-installation.md` | `e2cf6bf3417e10ad12112002e0719a581f1632b0db2881459cfaad20208e1ae0` |
| `install/directories.txt` | `c8e0e825586f309c43bbcb5dc6644eb270a233ee638c7244696193db8f52d7c3` |
| `install/native.sh` | `1425c05888b24b413a45ec33f70a98fe432f73774634197cae4cbdc9d1c9e187` |
| `install/native.ps1` | `57173b99e0c809d3fe45a6c9b894ddab0c07b3961a2d73b6fd61af25047483f7` |

Scope remains the original 48 owned paths plus the authorized copied validation
fixture. Original `a73cf9ee`, fixture `acf97f1`, F01 `4407827`, accepted-core
imports `bc0863a`/`8097ba2`, native-consumer `a31c5eb`, Git refusal `a559c9f`,
F02 `a797b2d`, F03 `036e903`, and F04 `6e1b1e0` retain their separate attribution.
Coordinator mode/runner/footer repairs and accepted P03/P07/provider bytes are
not reassigned to P10. No source was imported into the coordinator.

## F04 independent closure

The exact original contrast was independently rerun through the unchanged real
migrations skill and trusted dispatcher, followed by the actual source-owned
`li-migrate-claude-home --dry-run`. The separate plain targets remain length153,
marker180, redirect170 and canonical destination179. Both retain exact LF seeds:

```python
b"layout_version: 5\n"  # .claude\lintel-layout.yaml: 18 bytes
b"> Moved to .claude/memory/lessons.md (v5 .claude/ home layout, ADR-0005). Retained for explicit historical recovery.\n"
# tasks\lessons.md: 117 bytes
```

The marker/redirect SHA-256 values remain
`a8e022980d1bc17474f4199f6a22fd6d8eba27f145375854a5e4b65cb19f92eb` and
`819912e23957536ae225c7d220728c2564cde1af9f50b0398309dfc1ad6b0332`.

| Actual call | Independent observed result |
|---|---|
| Real reader, retained destination | 0, empty stderr; `current`, version5, empty legacy, retained lessons stub |
| Real reader, missing destination | 0, empty stderr; **`incomplete`, version5, legacy lessons, no stub** |
| Actual migration preview, same stranded target | 2, empty stdout; unchanged `Stranded redirect without its destination: tasks/lessons.md` |

All source/target/caller/home maps remain unchanged; no receipt, store, pin or
profile was created. These three independent calls are separate from the fixture
suite; their exact directories and evidence are retained, without cleanup/recovery.

The affected four-method canonical class then passed **4/4**, 269.286s, exit0:
**223 actual subprocess calls**, with full before/after map equality at short153
and plain256 and complete fixture cleanup. It covers all four knowledge mappings,
historical README-to-directory, generated README-to-file, ordinary/root and nested
hidden ADR redirects; valid/missing/unmarked/wrong-kind, near-match prose, actual
symlinks and Windows exclusive file-read denial. The actual producer preview is
compared on the same fixture, not replaced with a second parser.

Recorded calls are 112 readers (66 valid / 46 explicit errors) and 111 migration
previews (32 previews / 79 explicit refusals). A preview is not a mutation or
completion. The new local classifier uses existing checked/native observations,
preserves the producer's mapping and historical-directory precedence, and does not
infer destinations from arbitrary prose. No shared API, producer guard, F01 parser
or F02 ownership contract changes. **F04 is CLOSED for this finite class.**

## Independent original-selector outcomes

Commands used the private isolation runner, explicit Python3.11.9 `-I -B`,
the relevant immutable source test file and `-v`; no fail-fast stopped independent
methods. The lifecycle group also supplied explicit `--root` and Git Bash.
Full argv, exits, per-process roots and raw output are sealed.

| Actual run | Source | Result | Preservation |
|---|---|---|---|
| `remaining-copilot-seventeen` | `036e903` / `50e654af` | **FAILED: 17 methods, 15 pass / 2 fail**, 2855.701s, 112 subprocesses, exit1 | Exact cleanup, outer equality and 967 unchanged source files |
| `remaining-copied-validation` | Same frozen source | **1/1**, 18.743s, 14 subprocesses, exit0 | Exact cleanup and unchanged source/outer roots |
| `f04-canonical-class` | `6e1b1e0` / `4be5e096` | **4/4**, 269.286s, 223 subprocesses, exit0 | Full no-write maps, exact cleanup and source/outer equality |
| `final-two-original` | Same final candidate | **FAILED: 2 methods, 1 pass / 1 fail**, 619.667s, 46 subprocesses, exit1 | Exact cleanup and source/outer equality |

The final two-method run was **one expressly authorized final-candidate
verification**, after F04. No source, Git argv/flags, store or fixture dimensions
were changed to obtain a pass, and no product retry was added. It does not
establish a fix for an earlier WinError5. No further attempt follows its failure.

For `CopilotKit` selectors below, `PASS (036)` and `PASS (6e)` identify actual
independent observations, not a synthesized passing aggregate:

| Original selector suffix | Outcome |
|---|---|
| `test_canonical_required_caller_policy_refuses_missing_drifted_and_conflicting_context` | PASS (036) |
| `test_long_git_metadata_runs_real_effective_ignore_validation` | PASS (036) |
| `test_git_verification_directory_linked_and_plain_folder_controls` | **FAIL (036 and 6e), B01** |
| `test_missing_runtime_ignore_is_detected_and_repaired` | PASS (036) |
| `test_interrupted_adapter_publication_requires_explicit_owned_recovery` | PASS (036) |
| `test_full_protocol_is_portable_and_project_prose_survives_update` | PASS (036) |
| `test_modified_managed_file_refuses_entire_update` | PASS (036) |
| `test_unmanaged_collision_refuses_before_foundation_writes` | PASS (036) |
| `test_crlf_source_and_autocrlf_clone` | PASS (036) |
| `test_source_update_is_reviewable_and_deterministic` | PASS (036) |
| `test_symlink_parent_refused` | PASS (036) |
| `test_long_protocol_metadata_preserves_prose_eol_and_modified_block_refusal` | PASS (036) |
| `test_long_generated_link_metadata_checks_real_missing_and_linked_targets` | PASS (036) |
| `test_long_public_directory_index_is_selected_from_the_real_source` | PASS (036) |
| `test_incomplete_protocol_marker_refuses_without_changes` | PASS (036) |
| `test_invalid_inventory_root_types_are_clean_errors` | PASS (036) |
| `test_inventory_traversal_and_windows_paths_refused` | Initial bootstrap FAIL (036); full assertions PASS (6e) |

The eighteenth original selector,
`ProfileLifecycle.test_validation_skill_executes_shared_contract_without_activation`
in `universal-profile-context.py`, passed (036). It ran the actual copied one-block
skill, structured effective/compatibility output, nonactivation, existing-pin
verification, same-mtime drift/missing-pin refusal and malformed required policy
including explicit strict/default names. Cleanup completed. This supplies the
previously missing scoped D03 evidence; the old D03 ERROR remains historical.

## B01: supported linked initialization remains unobserved

**Acceptance blocker; observed failure confidence 10/10, cause unestablished.**
The exact failing expectation is
`tests/integration/copilot-kit.py:461`, via `_git_operation` at365:
supported Git exit1 must permit initial addition of the missing ignore rule.
Git succeeded in identifying/verifying the linked fixture; its exit1 means
not ignored, not unavailable/failed verification.

The initializer then returned **1 with empty success stdout**, reporting a
Windows journal-replacement failure. Final fixture dimensions remain
base109/source116, linked target128/`.git`133 and default store143.
The actual adapter argv is the selected Python executable, `-B`, the immutable
copied `bin/li-copilot.py`, `init --target <linked> --source <source>`.
The actual Git command remains:

```text
git -c core.fsmonitor=false -C <logical-linked-target>
  check-ignore --no-index --quiet .claude/runtime/.lintel-ignore-check
```

Final raw failure, personal prefix redacted:

```text
ERROR: [WinError 5] Access is denied:
'\\?\<fixture109>\.lintel-recovery-8954d3127379d1bb\transactions\
transaction-4a08b14f2fcb43f28585361caef4fd9d\.lintel-write-ytr9qtjp'
-> '<same transaction>\journal.json'
```

This is **after successful Git verification**, not the documented unsupported
long-root Git128 boundary. The latter's original target256/`.git`261 refusal passed
independently. No Git compatibility option, alias, redirect or retry was attempted.

The final Git selector recorded 14 operations: 10 state-preserving refusals,
3 successes and this one failed publication. The supported directory controls
completed; linked Git identity and its fresh missing-rule check completed.
**A successful linked init/check and the later linked negation/invalid-HEAD/
unavailable-Git checks and plain-folder/no-Git positive were not reached.**
The earlier 036 group recorded22 operations including the eight long-root refusals;
neither run is the builder's34-operation passing matrix.

The 036 run additionally failed initial setup of the inventory-traversal method
before any malicious inventory vectors, at test line1368. Its transaction was
`transaction-c05e9d43c930439cb64d7239c160b509`, temporary
`.lintel-write-4y96qgxc`, store `.lintel-recovery-ec6775d0728dae29`.
The later final-candidate method passed; that supplies new negative-case evidence,
not a diagnosis or retrospective pass for the earlier failure.

### Actual error and recovery evidence, without a cause claim

Before the 036 Git fixture's normal teardown, a read-only content-addressed copy
retained its failed linked target, controller and root-bound stores. The copied
failed transaction `transaction-13d982d3ac0a4386aaf14ea292f1acb7` has:

- `state: applying`; 326 applied / 115 pending paths; all441 copied current states
  match the corresponding planned-after/original-before phase, with zero mismatches.
- Matching root owner, plan digest and snapshot digest; final inventory pending
  and absent; no retained operation lock; recorded changes confined to target/store.
- Readable captured journal: 27,947 bytes, mode0666, readonly false. Its temporary
  `.lintel-write-naegi1g1` was already absent at capture.

There is no numeric generation field in this schema. ID, plan digest, file phases
and the current journal hash are retained. Pre-syscall expected journal hash/stat,
temporary mode/handle state and ACLs were not sampled. Neither the copied evidence
nor the owner's bounded static assessment establishes an open-handle, readonly,
expected-state, antivirus or external-sharing cause.

These are **copied-state observations, not an executed recovery of that incident**.
The later failures' live trees were not separately captured before successful
normal teardown; their raw errors/argv are retained, not invented state facts.
No actual-home/process/global/permission inspection or rollback was undertaken.

Separately, the actual original adapter interruption selector **passed**: it made
a real write before its explicit synthetic failure, emitted no success, refused
another init while incomplete, explicitly recovered exact original bytes, and
then completed init/check. Earlier independent runtime9/native/lifecycle and
default218->260 recovery/replay evidence remains applicable to unchanged code.
Thus the original error/owned-recovery mechanisms have real verification; the
unperformed recovery of this specific denied operation and B01's missing positive
must not be represented by those other passes.

## Original-leaf SPEC and QUALITY eligibility

`PASS` below is scoped to the original component requirement and named evidence,
not final integration/host approval. No mandatory result is averaged away.
**Every whole-P10 QUALITY entry is NOT STARTED** because A12.2.a remains blocked.

| Original control | SPEC | Evidence / limit |
|---|---|---|
| A12 | **BLOCKED** | B01 prevents complete acceptance |
| A12.1 | PASS | Real owned helpers and source/target/profile boundaries, scoped below |
| A12.1.a | PASS | Retained actual installed/default caller-child bridge; required-policy selector and diagnostic rechecks |
| A12.1.b | PASS | Retained structured switch/pin cases plus actual copied validation with completed cleanup |
| A12.1.c | PASS | Unchanged prior independent role/persona/private-boundary cases; no real private profile loaded |
| A12.1.d | PASS | F02 paired original ownership and long migration cases; actual migration previews preserve producer guards |
| A12.2 | **BLOCKED** | One required supported-positive observation remains missing |
| A12.2.a | **BLOCKED** | Native/default/classic positives retained; linked initialization B01 and cut-off subcases not cleared |
| A12.2.b | PASS | F02 preservation class, actual prose/conflict/EOL/update/link cases and final hostile-inventory refusal |
| A12.3 | PASS | Verified failure/owned recovery mechanisms; specific denied-operation recovery not claimed |
| A12.3.a | PASS | Explicit nonzero/no success; captured coherent incomplete state and real interruption case |
| A12.3.b | PASS | Real explicit adapter recovery plus unchanged prior foreign/corrupt/edit/replay/default controls |
| A12.4 | PASS | Truthful host/uninstall limits and F01/F03/F04 diagnosis; broader host acceptance remains separate |
| A12.4.a | PASS | Unsupported activation is explicit; no broad uninstall; actual long Git failure refused accurately |
| A12.4.b | PASS | F01 parser, F03 native observation and F04 canonical class independently closed; aliases retained |

Retained independent sources: `00a0bef` F01 (40 candidate +2 baseline cases and
seven tests); `c1a38a03` lifecycle53/runtime9; `c1f6bad1` installed F02 three,
long lifecycle four and its individual completed classic/default/canonical cases;
`131d3db4` F03 exact pair/six readers and the completed dependency selector.
Original classic109/116/182, atomic256->263, default113/store128 and218->260,
and canonical retained304 history were not replaced with shorter roots or an
explicit-store substitute. Earlier aggregate/cleanup limits remain attached.

The new completed selected runs settle previously missing preservation/validation
controls but do not rewrite any earlier failed/stopped run. No full17/53/9 suite
was restarted on the final candidate. Builder17/303-subprocess F04 and earlier
27/22/34-operation positives are builder evidence, not these independent results.

## Isolation, historical limits and handoff

Fresh owned fixtures were separate from all preserved old fixtures. Every product
call/import and outer comparison used verified allowlisted synthetic HOME,
USERPROFILE, application/XDG/cache/temp and effective Lintel/Git/derived roots.
Explicit PATHEXT/interpreters and discovery ceilings were retained. Where the
original Git fixture reconstructs its own environment, the private launcher adds
only missing approved system/global/discovery isolation fields; it does not alter
argv, PATH choices, Git options, product outcomes or assertions. Additions are logged.
Real-worktree EOL settings remain separate from fixture Git settings.

All completed new runs have exact cleanup, unchanged source and outer-map equality;
no unexplained cache change was ignored. Normal fixture teardown is not cleanup of
old incident evidence. Original exports, failed logs, refs, sealed copies and both
independent three-call fixtures are retained.

Actual runtime: Windows, Python3.11.9, Git Bash5.3.15, Git2.55.0.windows.3 for the
real Git controls. Approved earlier PS7.6.6/native no-Python evidence is reused,
not rerun. PS5.1 remains denied/unverified; actual3.9, stock Bash3.2/POSIX, other
OSs and host activation/enforcement remain unverified. No new runtime or policy change.

Historical120/128 **FAILED**, PATHEXT/cache diagnostics, REDs, failed15/7/3 groups,
all WinError5 observations, stopped Git experiments and prior interrupted review
aggregates remain distinct. D01's old failed setup, normalized260 correction,
original overcount/log and explicit-store control remain preserved. D03's old
cleanup ERROR is not erased by the new completed validation. Unsupported long
linked-Git positive support remains UNVERIFIED, not inferred from shorter controls.

Private evidence roots are `<session-files>\p10-remaining-036e903-independent`
and `<session-files>\p10-f04-6e1b1e0-independent`. Personal prefixes remain private.

| Durable evidence | SHA-256 |
|---|---|
| Original-selector `remaining-selector-outcomes.json` | `1f965785b13b641c22f038f4dfece8f4da78a46ec3af1e20ef9d97dc20ba01ef` |
| Original-selector index,572 evidence files | `36121f71d86c2d7accbe8c870f5dbd5cd8c7a054db461c8809088aeddbcbc4cf` |
| Frozen failed Git-control `capture.json` | `4ce5e777bfd685009303012616ae49a0768eb30e2c40adc1f59b73799bd6e5fc` |
| `copied-journal-facts.json` | `976721e56b333ff9414fe6595ca6a969a5a8a7b07b13df3837ae482944174ab5` |
| Final-candidate `fresh-results.json` | `41e484b0e53a7c326d5acb7af0c9a62024eb745371cc6f3e2977a0038218c344` |
| Final-candidate index,102 evidence files | `5b352367c0687c47ad4edf41ef338d3f51f2683bb0f9936df78080d47d24533d` |

The report is a sole local child of `4be5e096`, with required host trailer and
exact hash/clean-state receipt in the handoff. Actor labels/hashes do not
authenticate independence or enforce policy. No A13/P08 interface release,
source import, final CI/integration, human approval, push or PR is implied.

**Cycle position:** REVIEW / SPEC BLOCKED on B01 -> coordinator-held decision.
The authorized final observation is exhausted; no further loop or investigation.
First whole-P10 QUALITY remains NOT STARTED. Remain read-only.
