# A23.5 independent final review (Phase 2): the frozen delivery head

**Reviewer:** session `8df81ac8-0043-4564-97bd-e2ed12c94b24` ("A23.5 final review"), assigned by
coordinator88 (`88aecc43-40f9-41d4-8947-6c2fb0a55481`). It had no role in any package. Its
`session.start` records `claude-opus-5.5`, reasoning `max`, context `long_context`.
**Date:** 2026-09-24.

**Verdict: SPEC PASS, then whole QUALITY PASS with advisories.** Findings: P0 0 / P1 0 / P2 0 /
P3 6 (A5-19 to A5-24). All eighteen Phase 1 and Phase 1b findings (A5-01 to A5-18) are resolved.
This review satisfies A23.5 for the exact content `ad529ebc` below. It does not close A23.4,
which rests on the delivery PR's strict CI run of exactly this content; Phase 3 records that. It
is not a compliance, live-host, model-quality or release verdict. Compliance stays UNVERIFIED
without a resolved required-policy source; that is not a finding.

## 1. Exact merged content

| Item | Value |
|---|---|
| Delivery branch and frozen head | `jokerman-microsoft-mastersession-recovery` = `ad529ebc3c99da47ea7e31f8bc2f1841e421b483` |
| Base | `main` `9575aaacc4f32faeb4b0468a779d44b30464024a` (PRs #88-#92); equal to the remote `main` and to `merge-base(head, origin/main)` |
| History | `main` is an ancestor; the head is 724 commits ahead (436 first-parent, 61 merges), 0 behind |
| Delivery diff | `git diff 9575aaac ad529ebc`: 755 files, +147,187 / -15,118; 400 added, 355 modified, **0 deleted**, 0 renamed or type-changed |
| Split | 539 product paths and 216 record paths under `.claude/` |
| Presentation site | `presentations/` tree `6ab5f2bd9bd73df178195626328d6150162a73dc` and `.github/workflows/presentation-pages.yml` blob `a3fdd73ded881a56ee58f0c19f291ca2c4c52b51` are identical at `9575aaac`, `d6d4d6a8` and the head |
| Publication state | Nothing pushed: the branch is absent on the remote and no PR exists. No tag or release; version `0.11.0` on all nine manifest fields |
| After the freeze | The branch tip moved to `e5e96b1e7d3795d280ef02e0acc9ad40082af80a`, which adds only records: `.claude/memory/lessons.md` (L-053) and `handoff.md` (publishing steps), +32 lines, no secrets. Its product is identical to `ad529ebc`; this verdict is on `ad529ebc` |

Product paths by area (A/M): `skills` 147 (27/120), `agents` 66 (0/66), `tests/integration` 54
(48/6), `tests/unit` 53 (31/22), `docs` 38 (6/32), `lib` 36 (22/14), `bin` 33 (12/21),
`tests/fixtures` 28 (28/0), `hooks` 22 (0/22), `tests/shape` 12 (3/9), `scaffolding` 11 (5/6),
`install` 7 (3/4), `.github` 6 (2/4, including two of the seven manifests), `shims` 5 (1/4) and
`tests/behavior` 4 (2/2). There are 17 further single files:

- the other five manifests;
- the root entry files `AGENTS.md`, `AGENT-INSTRUCTIONS.md`, `CLAUDE.md`, `GEMINI.md`,
  `CONTRIBUTING.md` and `README.md`;
- `CHANGELOG.md`, `.gitattributes` and `packs/_default/pack.yaml`;
- `tests/README.md`, `tests/runner/run-all.sh` and `tests/e2e/harness-critical-path.sh`.

In all, 539 product paths.

## 2. Method and isolation

Phase 1's method applies (`reviews/A23.5-phase1-0d802cb.md`), and so do its tools. The additions
are:

- File-, line- and merge-level traceability of the whole delivery against base `9575aaac`.
- A secrets and signed-URL scan of every added line.
- Two detached checkouts of the exact head, one CRLF like the Windows CI job and one LF, under
  `%LOCALAPPDATA%\Temp\a235\w` and `\wl`, in which CI's once-per-system checks and the
  non-sharded tiers were run.

Every Python and Bash process ran through the launcher `tools/syn.ps1`, with a synthetic
environment from process birth. `LINTEL_POWERSHELL` names PowerShell 7, as in CI, and no other
`LINTEL_*` variable is present. There is no jq; `GIT_CONFIG_NOSYSTEM=1` and `GIT_CONFIG_GLOBAL=NUL`.

The shared repository was only read: no branch, commit, stash, fetch, push or configuration
write. The shared `/tmp` mount stayed at `%LOCALAPPDATA%\Temp\p13v\t` throughout, and nothing was
deleted.

The operator's A14.5 instruction was confirmed read-only in the local session store (section 4,
S7).

## 3. Deltas since Phase 1b (`9c6f65a4..ad529ebc`, 42 commits)

| Commit(s) | Change | Result |
|---|---|---|
| `37fc477b`, `f799b53d`, `05e6cffa`, `9c861da5`, `f3db688f`, `81f76773`, `14ab496e`, `6a6d95d0`, `ded66b0d`, `0cbacdc1`, `ad529ebc`, and the final.md part of `510bb1ca` | Records: plan, cards, memory, handoff, final report | Read; accuracy notes in A5-19 to A5-23 |
| `cad01026` | `ci.yml` behavior tier `!cancelled()`; `tests/README.md` apostrophe rule | PASS (verified at `edd2f556`; `ci.yml` blob `72bd94c1` is unchanged at the head) |
| `edd2f556`, `fb17f69f`, `0a6df5f6`, `562f0386`, `8a74d548`, `9d23f2ff`, `f724834c` | Review preservation | PASS: each file equals its reviewer's stated SHA-256 (table in S3) |
| `7ec02092` merges `1e362b3c` | F-INT-6: `catalog-installed.py` guard measures `generate()` output | PASS: automatic merge; test bytes equal the reviewed commit at the merge and the head |
| `8f418e68` merges `d13455de` (product `88c92377`) | P14 A23 unit, 8 product paths | PASS: automatic merge; all 8 equal the reviewed commits at the merge and the head |
| `4998f265` merges `9919e236` | A14.5 framework-app report (records only) | PASS: automatic merge; report equals the reviewed commit |
| `d6d4d6a8` merges `9575aaac` | `main` PRs #88-#92 | PASS: equals `git merge-tree` exactly; 44 paths, all under `presentations/` |
| `510bb1ca` (CHANGELOG part) | CHANGELOG: A14.5 added, and removed from the open list | PASS: accurate after A14.5's closure |

## 4. SPEC

**S1. Identity: PASS.** See section 1. `ad529ebc` was the branch tip at the freeze, and the
remote `main` equals the base.

**S2. Integration traceability: PASS.** The delivery's 436 first-parent commits give 162
final-byte owners across 755 paths.

- Product final bytes come from:
  - 484 paths of merged unit branches;
  - 53 paths of direct commits;
  - 2 merge results: `README.md` (`fe9e6284`, Phase 1) and `docs/client-adapters.md` (automatic
    union at `68d32240`, covered by SAME's P10 integration review).
- No path is unowned, and no owner's bytes differ from the head.
- Every direct owner is covered by one of:
  - a reviewed package cherry-pick with an equal patch ID;
  - an independently reviewed integration;
  - my Phase 1 review, or my Phase 1b review (`917d7125`, `89131723`, `b3620e5c`, `6d12fd91`);
  - this pass (`cad01026`, `510bb1ca`).
- All 49 mainline merges since the fork from `main` were reproduced with `git merge-tree`.
  - Product hand content appears in only three of them: `68d32240` (covered by SAME's P10
    integration review), `490a0f4e` (which takes the reviewed P04 side byte for byte) and
    `fe9e6284` (`README.md`, Phase 1).
  - Five others differ from the automatic merge only in `.claude/` records. The rest are exactly
    automatic.
  - Inside the P08 integration branch, the hand content of `3c7ce67e` is covered by SAME9db,
    which reproduced its 14 conflicts.

**S3. Preserved review files: PASS.** Each file at the head is byte-equal to its reviewer's
stated SHA-256. The eight new or newly checked files:

| Review file (under `reviews/`) | SHA-256 | Stated by |
|---|---|---|
| `P13-fint6-1e362b3.md` | `ad7a63d13acb6ea1841aefb36f8f9fd86132a61ca51dc6a568212ff0b2cbf75d` | 30146eab |
| `P14-a23-96bd5ba.md` (SPEC FAIL, historical) | `1034a9dc15436022f2e103d4e6acdde6c0ffd1518e85432af10b97bb2c7437a3` | 6310f7ad |
| `P14-a23-recheck-88c9237.md` | `9a245439837e64d87dc67e4fc2a5d5214699677cb393b4d46ff725f47e91c52e` | 6310f7ad |
| `P11-a145-static-v1.md` | `0602056a30228070bcc34dafbdd7828b45fe1e4a2fb1f6e134846c27388266ee` | aba328fd |
| `P11-a145-app.md` | `9ce3c88e6c57462c1d8230d16f27d55f1b01ec311ae33e3c748e6d25badcdf31` | aba328fd |
| `P08-integration-3ff5998.md` | `6a76db8c390fb6c18a2413d0cf9478233b6fb60e59bdff1d864d8ba60ce78b23` | SAME9db |
| `A23.5-phase1-0d802cb.md` | `98919898af816c6449cbdb19b1742e26c6830588272c55477df4a2f4512b858d` | this reviewer |
| `A23.5-phase1b-9c6f65a.md` | `2a6d79dfe684d0b97648b147e48dbf75d663cf0c4dccfe7e74913494965db01f` | this reviewer |

The eight files that Phase 1 checked (four P10, one P09, three P13) are unchanged at the head.
Stated values were taken from each reviewer's delivery message in the coordinator's session record
where the cards give none (the A23 recheck and SAME9db).

**S4. Acceptance claims: PASS.** `plan.md` has 113 top-level items: 107 checked, and 6 open
(A15.1-A15.4, A23.4, A23.5). That matches `reports/final.md` row by row. Every closure since
Phase 1 rests on an independent review of exact content and holds at the head:

- **A08, A10, A13:** SAME9db's whole-P08 integration review.
  - SPEC PASS with carried limits; QUALITY PASS, P3 3.
  - It reproduced the preview's 14 conflicts, verified losing-side line survival,
    reconciliations 1-7, catalog removals and merge purity.
  - It was made on `3ff59984` and stated for `6a6d95d0`. The product delta from `6a6d95d0` to the
    head is `CHANGELOG.md` alone, so the verdict applies.
- **A14.5:** aba328fd's static review (SPEC 8/8, QUALITY PASS) and app review (SPEC 10/10,
  QUALITY PASS).
  - A blind pixel control proves image visibility. V1 was observed directly, the restore boundary
    was checked, and no signed values were read or copied.
  - At the owner's execution checkout `683db6ab`, the design, browser and profile source is
    byte-identical to the head (`skills/design-dna`, `design-html`, `generate-web`,
    `frontend-design`, `browse`, `scrape`, `lib/url_policy.py`, `lib/profile_context.py`). O2
    covers the P05 files.
- **A23.1 and A23.2:** 6310f7ad's recheck of the repaired unit: complete SPEC, then first whole
  QUALITY, P3 2. It ran independently with no selector (10/10 in 596 s) and closes F1 and F2.
- **A23.3.s3:** the coordinator's reconciliation (`plan.md:677-694`).
  - The matrix has 20 non-executed rows: 4 skipped (X2, P13-6, C-5, C-7), 9 unverified (X5, P07-3,
    P08-3, P08-9, P08-12, C-3, P10-4, P10-9, C-10) and 7 host-unavailable (X1, X3, X4, P08-5,
    P09-5, P11-5, P12-2).
  - The reconciliation assigns each of the 20 exactly once, and none counts as executed evidence.
  - Its accuracy notes are A5-19 and A5-20.
- **A23.4.g3 and A23.4.p2:** `fin2-kit` passes 50/50, with no skip, on `3d7f1284`.
  - Its data-driven refusals cover every declared resource, reconciliation 7 included.
  - After `3d7f1284` the kit's inputs changed only in bundled text: the CHANGELOG wording and N5's
    registry comment.
  - At the exact head, `generate()` builds all 463 files and 11 seeds, 444 of them in the bundle,
    longest key 83. The CHANGELOG is bundled, and the target is never touched.

Open items are correctly open. A15.1-A15.4 are blocked by denied routes and the missing Visio
writer, and none is waived. The operator's recorded A14.5 message does not address A15, so no
authorization is assumed.

**S5. Preservation: PASS.**

- **No file lost.** The delivery deletes and renames nothing relative to `main`.
- **Swarm.** All 76 Swarm delta paths exist at the head, row 2 at its ADR-0027 destination.
- **Skills and roles.** The 126-skill map keeps every canonical name and path, all 46 aliases and
  all 35 module sub-capabilities. The 69-role map is retained (A17).
- **Tests.** No test was removed.
- **The P03/P07 residue that SAME9db routed here (`P08-integration-3ff5998.md:118`).**
  - The historical p8r5 "14/18" and "22/23" failures were checkpoint reservations at 260-290
    characters and a 283-character bisect ref-lock.
  - P03's accepted joined-path repair reproduces those exact lengths as RED, then GREEN
    (`reviews/P03-joined-path-final.md:120-136,183,197`).
  - What remains is an evidence gap, not a known failure: the original P08 18-wrapper aggregate
    has not been rerun at its original deep root. It is listed in section 6.

**S6. Evidence currency at the exact head: PASS.**

| Tree | Run | Coverage | Result |
|---|---|---|---|
| `3d7f1284` (fin2; P08 content equals `3ff59984`) | `fin2-kit`, `fin2-rest-a`, `fin2-rest-b`, `fin2-dic-short` | the kit plus the 148 other entries at that tree | kit 50/50; 72/73 (budget refusal at a long root, then 1/1 from a short root); 74/75 (F-INT-6, since fixed) |
| `14ab496e` (fin3) | `fin3-targeted`, 16 jobs | every test or runner file changed after `3d7f1284`: the runner contract, the five reason-string entries, `universal-a23`, `catalog-installed` (N3 and F-INT-6); also the light checks, the whole shape tier and `domain-installed-consumers` from a short root | 16/16 exit 0 |
| `ad529ebc` (this review, CRLF and LF checkouts) | `head-checks-crlf`, `head-checks-lf` | the five once-per-system checks; the behavior, e2e and shape tiers; installed-bundle `generate()` | all exit 0 in both; behavior 3/3, e2e 1/1, shape 41/41 (1 partial: the jq-denied SKIP); byte-identical shape logs |

- All five stated `results.json` hashes match.
- Every run's preflight records a clean source, the PowerShell 7 selector only and `/tmp` on
  `p13v\t`. The preflights do not record the launcher. `final.md` names launcher `d288e74b`, and
  that file exists in the coordinator's files with full SHA-256
  `d288e74b7d456508027ca7abfcb1bf070190a7bcdb72f114aea26eb9d71212f8`.
- The only skip evidence in any run is the jq-denied partial coverage, `manifest-identity` and
  `hook-input-adapter` (L-046). No unittest skip occurred anywhere.
- The product delta from `14ab496e` to the head is the CHANGELOG wording plus `presentations/`
  equal to `main`, and no delivery test scans `presentations/`.

**S7. Security and authority: PASS.**

- No private key, token, credential assignment, bearer header or signed query appears in any
  added line. The only URLs with a query are one public documentation link (`?tab=ide`).
- Feed hosts appear only as hostnames. No lockfile, `package.json`, `.npmrc`, `node_modules`,
  npm cache or `.claude/runtime/` file is tracked at the head.
- The A14.5 authority is the operator's message in the coordinator's session record (turn 520,
  2026-09-24T17:30:41Z): "By using Microsoft NPM router/registry since they blocked NPM
  publicly, so we have MS repo for it." Classifying the feed's first-party CDN redirects as part of
  that restore stays within the instruction. The narrower host list was the coordinator's own
  bound, the owner stopped when the redirects surfaced, and no credential, TLS change or global
  install occurred.

**SPEC: PASS.** A23's acceptance ("evidence category and limitations are explicit") is met
through the delivered 83-row matrix and this report's section 6. The final report's omissions
are record-accuracy findings (A5-19), not missing evidence.

## 5. QUALITY (whole integration; eligible after SPEC PASS)

Strengths:

- Every accepted unit is integrated by an automatic merge or an independently reviewed
  resolution.
- Reviews are preserved byte-exact with verifiable identities.
- The CI change keeps strictness per shard, and each tier and once-per-system check reports
  independently.
- The platform N/A rule is narrow and fail-closed: canonical reason only, off Windows only, every
  skip marked and at least one test run.
- The A23 unit reuses accepted producers and fails closed on odd inputs.
- The F-INT-6 guard now measures what the adapter writes.
- Evidence is labelled honestly throughout, and so are the synthetic actors.

Findings (all P3, advisory):

| ID | Location | Finding and suggestion |
|---|---|---|
| A5-19 | `reports/final.md:111-145`; `plan.md:685-689` | The reconciliation says its explicit limits are "stated in `reports/final.md`", but final.md omits five: P07-3 (live UNC I/O), P08-5 (case-sensitive positive), P09-5 (path budget at a long root), P10-4 (long linked-worktree positive, printed as UNVERIFIED at `copilot-kit.py:693`) and P13-6 (the `verify.sh` upstream listing without yq). It also omits SAME9db's carried limits (`P08-integration-3ff5998.md:117-119`) and the A14.5 notes in O2. All are listed in section 6. Add them to final.md, or point final.md to this report. |
| A5-20 | `plan.md:690` | The reconciliation's "open or blocked" group lists P11-4, an executed row, among the non-executed rows. It also calls P11-4 and P11-5 "under its review", but A14.5 has since closed (`6a6d95d0`). All 20 non-executed rows are still covered exactly once. |
| A5-21 | `reports/final.md:163,173` | "covering everything that changed since `3d7f1284`" omits N5's `install/upstream-sources.yaml` comment and the CHANGELOG corrections, which fin3 did not exercise; its own recheck and this review's bundle generation cover them. "Independent verdicts integrated in this delivery" lists only the recovery-era verdicts; the earlier package verdicts are reached through the area links. |
| A5-22 | `packages/P11.md:414`; `working-state.md:196` | The operator's authority is quoted in quotation marks, but the text is a paraphrase, and it is dated 19:25. The recorded message is 19:30:41 +02:00, with the wording in S7. The meaning is preserved; mark it as a paraphrase, or quote it verbatim. |
| A5-23 | `packages/P13.md:633` | "118 covered and 8 unmapped" contradicts the map's definition, where C/U is byte identity: 118 changed, 8 unchanged (`reports/P13-skills-preservation.md:415-417`). The same card's line 654 is correct. |
| A5-24 | `.claude/decisions/0032-ci-suite-sharding.md:69-70,92` | "About 2.9 hours" for the kit's shard is superseded. At the head, integration shard 3/4 holds 7 entries measuring 13,583 s (3.77 h) locally, under concurrent load; the kit alone took 12,578 s. The other shards measure 2.15 h, 1.22 h and 0.29 h against 300 minutes; the unit shards 0.24 h and 0.54 h against 180; `other` about 12 minutes against 60. It is still within budget, but with about 1.2 h of local margin; the first hosted run decides. Record the measured figure. |

Observations (no severity):

- **O1.** SAME9db's preserved review is CRLF throughout (156 CR, 156 LF), although its delivery
  message says LF. Its bytes equal the stated hash, so preservation is exact. It is the only file
  in the delivery diff with CR bytes. No action.
- **O2. A14.5's evidence is retained outside the repository.** Its artifacts, images and logs are
  in the P11 owner's gitignored runtime directory, bound by hash in `reports/P11.md` and the two
  reviews.
  - Its native checks ran at `683db6ab`, whose P05 review files predate the accepted
    native-observation repair `b22443f` and A13's `li-review-read` line. That repair is path I/O,
    so the typed control outcomes are unaffected in kind.
  - Of 43 lock entries, 24 carry SHA-1 integrity only. The fixture's build toolchain carries
    MPL-2.0, Apache-2.0, ISC and BSD-3-Clause licenses; the bundled runtime is MIT.
  - None of this is in the delivered product.
- **O3.** The kit's `long_linked_worktree_positive` line declares an out-of-scope scenario rather
  than skipping an assertion, so strict CI counts that test as passed. The matrix and section 6
  carry the limit (P10-4).
- **O4. Residual Windows CI risk.** On the hosted Windows runner, the capability-dependent
  `skipTest`s would refuse strict mode if the capability is missing: per-directory case
  sensitivity (`profile-path-identity.py:399-403`), symlinks (`:424`, `copilot-kit.py:1733`,
  `domain-result-handoff.py:769`, `adapter-navigation.py:429`) and directory aliases
  (`swarm-contract.py:188`). None skipped locally. Canonical N/A does not apply on Windows by
  design. The PR's CI decides, and Phase 3 records any occurrence.
- **O5.** CI's `actions/checkout` is shallow. No delivery test resolves a historical object:
  `universal-a23` records `BASE` and `AUTHORITY` as provenance strings only, and
  `universal-profile-scenarios` reads `HEAD`. No test needs the network.

**QUALITY: PASS with advisories** (P3 6). None of the findings affects the product or any
acceptance outcome. Optional records-only corrections may follow the one preserving commit, and
Phase 3 will list any record delta against `ad529ebc`.

## 6. Acceptance and preservation gaps (complete)

**Open acceptance items (6 of 113):**

- **A15.1-A15.4, blocked.** Their open sub-leaves are A15.3.word, .ppt and .shared, and A15.4.pdf,
  .xlsx and .visio. The causes are denied Word, Excel and PDF-raster routes, denied P12 record
  persistence, and no Visio writer (a template-only slot). None is waived.
- **A23.4, pending.** It closes on the delivery PR's strict `--require-all` CI on ubuntu, macOS
  and Windows, for exactly this content.
- **A23.5.** It is satisfied by this review for `ad529ebc`. The coordinator records it when the
  preserving commit lands.

**Limits on closed items** (explicit, not waived, not counted as executed):

- **Platforms.**
  - Linux, macOS, stock Bash 3.2 and hosted CI are unobserved until the PR's CI (X3).
  - Python 3.9 (X1), Python 3.12 and Windows PowerShell 5.1 (X4) are not verified.
  - The local evidence is native Windows with Python 3.11.9 and PowerShell 7.6.6.
- **jq-dependent assertions** (X2, C-5, C-7) remain unrun locally until CI (L-046).
- **Compliance.** Required-policy enforcement is UNVERIFIED.
- **Host.**
  - Live host and client coverage and native role registration (X5) are not established, and
    neither are host activation and hooks (P08-12).
  - The IC-F01 timestamp actor is unidentified, and the WinError 5 cause is unestablished (P10-9).
- **Unverified positives.**
  - Live UNC I/O (P07-3).
  - A case-sensitive filesystem positive (P08-5).
  - The installed path budget at a long root (P09-5, refused by the environment).
  - A long linked-worktree positive (P10-4).
  - The `verify.sh` upstream listing without yq (P13-6).
- **P08.**
  - The five advisories from recheck `624`.
  - The 704/aa5 selected-reader seam is revision-qualified.
  - The two cycle authority and confirmation sentences are reviewed as instruction text only.
  - The P03/P07 original 18-wrapper aggregate has not been rerun at its original deep root (S5).
- **Historical evidence.** `shell27` and `q02` stay invalid, with unknown actual-home effects.
- **Review advisories, P3.**
  - SAME9db: QI-1 (`li-lessons.py` template read without `OSError` handling), QI-2 (the
    `_audit.sh:53` `pack-lifecycle` residue) and QI-3 (the node-absent fallback of
    `hooks-registration-safe.sh`).
  - A23: Q1 (the cross-module `runpy`) and Q2 (one report sentence).
  - A23 recorded limits: N5 (duration), N6 (inventories compare regular-file content only), and
    a 120-character budget exercised only at 31 and 59 characters.
  - P13: N1 (the CI budget share).
  - A14.5: Q1, the cosmetic 390-pixel heading wrap.
- **Unrouted defect F-HOOK-MKDIR.** 25 hook files run an unguarded
  `mkdir -p "$LINTEL_HOME/audit"`, for example `hooks/shared/customer-data-block/run.sh:25`.
- **A14.5 evidence.** It is retained outside the repository, with SHA-1-only lock entries and
  pre-`b22443f` P05 control code at its execution checkout (O2).
- **CI feasibility.** Hosted durations are unknown; the kit's shard measures 3.77 h locally
  against 300 minutes (A5-24). The residual Windows capability skips are in O4.

**Preservation gaps:** none found. No path, alias, skill, role, module capability, test or Swarm
path is lost. The one preservation-evidence residue is the P03/P07 deep-root aggregate above.

## 7. Phase 3 (after the PR's CI)

When the coordinator reports the PR, I will verify the following:

1. Every commit from `ad529ebc` to the pushed head changes only `.claude/` records. Today that is
   `e5e96b1e`, plus the commit that preserves this file under
   `.claude/plans/universal-implementation/reviews/`. The pushed product must equal `ad529ebc`, and
   each record change is listed.
2. CI's `head_sha` equals that commit, and the tested merge ref's tree equals the head's tree, or
   differs only by later presentation-only `main` commits, which are named.
3. For each of the 21 suite jobs and the syntax job:
   - the conclusion, and whether strict mode passed;
   - the shard selections;
   - the `N/A` lines on ubuntu and macOS, expected as 4 (context-safety), 14
     (profile-path-identity), 2 (snapshot-ownership), 1 (review-evidence) and 1
     (universal-profile-context);
   - any Windows skip from O4;
   - durations against the timeouts.
4. I will deliver the short addendum file.

## 8. Evidence index

Tools (`files/tools/`, SHA-256):

| File | SHA-256 |
|---|---|
| `syn.ps1` | `e17d2a4a1321f435df6f616f70d85b943f3618480355edf80740afb0b04ce57f` |
| `trace2.py` | `0e57d3b3fd5606cda93433d0a879b3693cb4a8d26486fd246b0e18161f539f12` |
| `secret_scan.py` | `c796a9cf3d5bbb88919ae8f07fa9b99d72860a6b3cdda16b61784bb8e9526d7b` |
| `shard_budget.py` | `0da9126741a9325b9dc2a2d3a50dcc6519736d6c8f73a601c83b1ce73577d303` |
| `head_checks.sh` | `e11c10cac2d76b6cf91ece89e1ad1a23a63db1ba7d0737591f0ecc921c96c8a4` |
| `fin_analyze.py` | `8da2fda75ff01dcda116bae6dcf8b2d5bd70545e4d1ca804b3d8606cf98182c9` |

Outputs (`%LOCALAPPDATA%\Temp\a235\`, SHA-256):

| File | SHA-256 |
|---|---|
| `runs\trace-ad529ebc.json` | `5c569708198f3bd963e799de442110d0982a28474080a1d6ffebdcdb115d8e14` |
| `runs\trace-ad529ebc.out.txt` | `a94893706b723669166c6b5989bfe4ecb98f8958a261ca5bfb06cca4264251b0` |
| `runs\secret-scan-delivery.out.txt` | `6bd1207bdee2ec280c0e541b450326ed27f56050128415f9c510cc910eabd4ea` |
| `runs\shard-budget-ad529ebc.out.txt` | `6f885614a05d2683b9f33a4f2b41a651d5120b7e0b8220c2ab3ef58a333a1c76` |
| `runs\fin-analysis-final.out.txt` | `53cc19c04889e347920d9bc902f0506e42b3053b396b1f7057f99f0b49cb01c3` |
| `hc-crlf\steps.tsv` | `e0a587d768af1f68ecccf78ac2468769effa5e2822b4011f65c0fb4feee753a3` |
| `hc-lf\steps.tsv` | `b9d71eeddf84e9a88d429353de6ffc57871c401c5f14976df85ff6ba6760b6af` |
| `hc-crlf\tier-shape.log`, `hc-lf\tier-shape.log` | `40e606a92b46a2120109b3740c4f5bdaec3fa506dc1b2888276d174595f12905` (identical) |
| `runs\head-checks-crlf.env.txt`, `runs\head-checks-lf.env.txt` | `7e2af0e5675a52ce7339d1674cda3840a71b21f44e0f14035818524bdb489a85` (identical) |

The coordinator's evidence is under
`...88aecc43-40f9-41d4-8947-6c2fb0a55481\files\verification\` (fin2-*, fin3-targeted).
`%LOCALAPPDATA%\Temp\a235` is retained until Phase 3.

## 9. Limits of this review

- **Platform.** Native Windows only, with Python 3.11.9, Git Bash and PowerShell 7.6.6. The LF
  checkout is an end-of-line proxy for Linux, not a Linux run. POSIX behavior was simulated
  through `uname` (Phase 1b), not observed.
- **Scope of my own runs.** I did not run the unit tier, the integration tier or the kit. For
  those, the coordinator's fin2 and fin3 evidence was verified read-only, not reproduced.
- **Sampling.** The accepted package reviews were checked for identity, scope, verdict and
  applicability at the head, not re-derived. The 83-row matrix was checked for categories and
  row coverage, not per-row citations.
- **Durations.** They come from a shared, loaded host and are not CI predictions.
- **Compliance.** UNVERIFIED.
