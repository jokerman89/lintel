# A23.5 Phase 3: CI-repair delta review and CI binding

**Reviewer:** session `8df81ac8-0043-4564-97bd-e2ed12c94b24` ("A23.5 final review"), for coordinator88.
**Date:** 2026-09-25. It follows `reviews/A23.5-final-ad529eb.md` (Phase 2).

## Verdict

- **Delta `2ab1f25d..ed91ef4f` (10 commits): SPEC PASS, then QUALITY PASS with advisories.**
  - Each product and test change answers a failure observed in hosted CI, and none weakens an
    assertion or a product refusal.
  - Findings: P0 0 / P1 0 / P2 0 / P3 5 (A5-26 to A5-30). A5-25 is a P2 correction of my own
    Phase 2, now resolved.
- **CI binding: run `36065850657`**, event `pull_request`, attempt 1, head `ed91ef4ff9b7e2f79ca9747a0ec0dd1641c5b68b`.
  - All 22 jobs checked out the test merge `4382b3c85925dd5e29082191cbc2401db06e510d`, whose
    parents are `9575aaac` and `ed91ef4f`. Its tree `0b226533078f96d971d042d87bb081210fca2acc`
    equals `ed91ef4f`'s tree, so CI tested exactly the reviewed content.
  - 19 of 22 jobs pass: the syntax job plus unit-1, unit-2, integration-1, -2, -4 and other on each
    system. The only failing entry, in each of the three integration-3 jobs, is `document-pdf`.
- **A23.4 is not met.**
  - Result per system: 149 of 150 entries pass, with Skip 0 and Partial 0. `document-pdf` fails
    with `ModuleNotFoundError: No module named 'pypdf'` (27 errors in 27 tests).
  - pypdf is undeclared, and the operator refused to add it (L-054). This review does not treat
    it as acceptance or as a waiver.
  - A23.4 stays open pending that operator decision, and the count stays 108/113.
- **A23.5** stays satisfied: the product content is the Phase 2 content plus this reviewed delta.

## CI outcome per job (run 36065850657)

The durations come from the Actions API. Suite counts come from my own parse of the complete run log
archive (SHA-256 `5af71b02b1aa34531c4678c4618426e4ac22448dc14c41272adcfb250f04bee1`). Every job
shows Skip 0 and Partial 0.

| Job | Linux | macOS | Windows | Timeout |
|---|---|---|---|---|
| unit-1 | 38/38, N/A 18, 0.9 min | 38/38, N/A 18, 1.6 min | 38/38, N/A 0, 6.8 min | 180 |
| unit-2 | 37/37, N/A 4, 4.2 min | 37/37, N/A 4, 5.8 min | 37/37, N/A 0, 17.0 min | 180 |
| integration-1 | 8/8, 6.4 min | 8/8, 10.5 min | 8/8, 58.0 min | 300 |
| integration-2 | 8/8, 16.0 min | 8/8, 29.5 min | 8/8, 32.0 min | 300 |
| integration-3 | **6/7** (`document-pdf`), N/A 1, 24.2 min | **6/7** (`document-pdf`), N/A 1, 23.0 min | **6/7** (`document-pdf`), 93.6 min | 300 |
| integration-4 | 7/7, 2.6 min | 7/7, 4.3 min | 7/7, 9.2 min | 300 |
| other | behavior 3/3, e2e 1/1, shape 41/41; 1.3 min | same; 4.7 min | same; 6.4 min | 60 |

- **Syntax job:** success in 0.1 minutes.
- **`other` jobs:** every once-per-system step succeeded on all three systems. That is structure
  verification, the three tiers, the catalog, instructions, adapter and wiki checks, the stock
  Bash 3.2 install (macOS) and `check-install.ps1` (Windows).
- **N/A composition:** exactly the 23 canonical `platform: windows-only` skips, on both POSIX systems:
  - unit-1: `context-safety` 4 and `profile-path-identity` 14;
  - unit-2: `managed-transaction` 1, `review-evidence` 1 and `snapshot-ownership` 2;
  - integration-3: `universal-profile-context` 1.

  Windows reports none, so every one of those methods ran strictly there, and no Windows
  capability `skipTest` occurred (Phase 2 O4).
- **Environment:**
  - Python 3.12.14 on Linux and 3.12.10 on macOS and Windows; PyYAML 6.0.3; git 2.55.0.
  - jq 1.7, 1.8.2 and 1.8.1 on Linux, macOS and Windows. The jq-dependent assertions (matrix
    rows X2, C-5 and C-7) therefore ran strictly on all three systems.
  - Synthetic HOME and TEMP under `<RUNNER_TEMP>/s`.
- **Headroom:** the longest job is Windows integration-3, at 93.6 of 300 minutes, holding the
  kit. Phase 2 A5-24's local 3.77 h estimate did not carry over to hosted runners.

## Delta review (`2ab1f25d..ed91ef4f`)

I established the failure baseline from the complete logs of run `36054668106` on `2ab1f25d`: 37
distinct failing entries. `gh run view --log` truncates long jobs, so I used the API log archive.
Each change below matches a failure that I observed in those logs.

| Commit | Change | Failure it addresses (run 1 evidence) | Result |
|---|---|---|---|
| `261efeae` | Plan closure bullets reworded | `universal-profile-scenarios` on all systems. The product reader refuses the live map: "multiple definitions of the same kind: A23.2 / A23.1" | PASS. See A5-25 and A5-27 |
| `d739d479` | CI installs the declared `lib/envelope-requirements.txt` (`ci.yml:57-59`) and exports synthetic `HOME`, `USERPROFILE` and `TEMP` on a physical path (`:60-86`); `tests/README.md:32-40` | PyYAML refusals in the catalog, envelope and brief-forge tests and in the `catalog-installed`/`copilot-kit` methods; `KeyError: 'USERPROFILE'` (Linux `copilot-kit`); HOME not under the fixture parent (Windows); "Symlink/reparse root refused: /var" (macOS) | PASS. Consistent with L-054 and recorded in ADR-0032 |
| `d677efaa` | Product: Bash resolved through PATH (`li-review-evidence.py:125`, `li-swarm.py:133`, `swarm_evidence.py:106`); `dont_write_bytecode` set in six entries | "Windows Subsystem for Linux has no installed distributions"; `bin/__pycache__/li-copilot.cpython-312.pyc` in the kit's installed snapshot | PASS. Independent RED/GREEN below |
| `3dc8a01b` | Product: pack and extension staging roots resolved (`li-lifecycle.py:211,1039`) | macOS "Path escapes root: _default/pack.yaml" | PASS. These are the only two product sites that use a temp directory as a checked root; `install/native.sh:541-542` already resolves |
| `8c9893f9` | Product: `parser.close()` on an unterminated raw region (`markdown_source.py:280-282`) | Python 3.12 `adapter-navigation` lost `docs/after.md` | PASS. The closing offset still comes from `getpos()`; the fix is verified by the Linux 3.12.14 unit-1 green |
| `497863de` | Test portability: PATH Bash, a Python-free PATH through shadow links, an executable hook, logger `.get()`, a tolerant mount probe, XDG pop, hex*3 padding, and a windows-only retry test (`managed-transaction.py:469`) | Each named failure | PASS. POSIX journal propagation stays covered (`:511-546`) |
| `9d23accc`, `53383406` | Physical `mktemp -d` roots in eight bash entries, with an empty-result guard; `managed-transaction` verbose (`:944`) | macOS `/var` refusals in run 2; unit-2 partial on POSIX | PASS. The verbose output is required for N/A (Phase 1b A5-18) |
| `c5667a7e`, `ed91ef4f` | L-054 and L-055 | none (records) | Read |

Independent evidence for the delta. Every process ran in my synthetic launcher, with no `LINTEL_*`
variable except the PowerShell 7 selector.

- **Bytecode.**
  - An installed kit from `2ab1f25d` wrote `.github/lintel/bin/__pycache__` and
    `.github/lintel/lib/__pycache__` into the consumer; a kit from `c5667a7e` wrote none.
  - An AST check shows that at `2ab1f25d` exactly the six named entries lacked the flag, and at
    the head every entry sets it before its first local load.
- **Plan reader.** The product `work_context` over the live map:
  - refuses at `ad529ebc` and `2ab1f25d`;
  - parses at `3d7f1284`, `c5667a7e`, `ed91ef4f` and `6815ca46`.
- **Windows, CRLF checkouts.**
  - At `c5667a7e`, 16 of 16 delta-affected entries pass, including `universal-profile-scenarios`,
    `review-evidence`, `universal-a23` and `universal-work-lifecycle`.
  - At `ed91ef4f`, all nine changed entries pass, and `managed-transaction` runs 20 tests with no
    skip.
  - Both checkouts were clean afterwards, and `/tmp` stayed on `p13v\t`.
- **Undeclared dependencies.**
  - The only other pypdf import (`design-browser-pipeline.py:392-394`) is opt-in (`--pdf-reader pypdf`).
  - All Node imports are `node:` built-ins.
  - So `document-pdf` is the only undeclared-dependency gap in the default suite.

## Findings

| ID | Sev | Location | Finding |
|---|---|---|---|
| A5-25 | P2, reviewer self-correction, resolved | `.claude/plans/universal-implementation/plan.md` at `ad529ebc`; `tests/integration/universal-profile-scenarios.py:26-32,447-454` | My Phase 2 treated `.claude/` records as inert and passed S6 evidence currency. They are not inert: `test_c03` reads the live work map and plan, and the frozen head's closure bullets made the product reader refuse the initiative's own map. `universal-profile-scenarios` failed on all systems. `261efeae` resolves it, and integration-4 is green on all systems. |
| A5-26 | P3 | `tests/integration/universal-profile-context.py:256-291` | The Windows-only default-home test needs `len(temp base) + 224 > 260`. Run 1 failed at 258 under `D:\a\_temp\`. It now passes by 2 characters (262) under `D:\a\_temp\s\t\`, an incidental and unlisted fix. Pad from the base length, as `context-safety` does. |
| A5-27 | P3 | `lib/swarm_contract.py:480,493-497`; the same test | Records are test inputs: a list item whose first token contains a digit, or that repeats a task ID, is a task definition. So a records-only head is not automatically product-equivalent for CI, and it needs its own run, or at least the `work_context` check above. |
| A5-28 | P3 | `plan.md:651-656` | A23.4 is open, but the product reader's remaining-work view omits it at `c5667a7e`, `ed91ef4f` and `6815ca46`, because all its leaves are checked. The view lists only the A15 leaves. This is the parent-disappearance risk that A23.3.s3 existed for. An open leaf for the strict-CI and pypdf gate would restore visibility. |
| A5-29 | P3 | `lib/envelope-requirements.txt:1` | "legacy YAML envelope/content input only" understates the scope. Catalog metadata queries, selections and installed discovery need PyYAML (`skills/catalog/references/metadata.md:30-33`), and CI now installs it for them. |
| A5-30 | P3, advisory | `tests/integration/copilot-kit.py` (default-caller test); `tests/README.md:32-40` | The test requires `USERPROFILE` on POSIX and HOME under TEMP's parent. CI supplies both and the README documents them, but a plain POSIX run still raises `KeyError`. Deriving these values inside the test would remove the environment contract. |

Observations (no severity):

- **Bash resolution.** Resolving Bash through PATH is a strict improvement over CreateProcess
  order. A native Windows shell that lists System32 before Git can still resolve the WSL launcher.
  `li-lessons` and `li-lifecycle` already behave the same way.
- **macOS temp roots.** macOS system temp roots (`/var`, `/tmp`) stay refused by the product's
  linked-root policy, by design. Hosted CI now runs on physical temp roots, so it does not exercise
  that refusal on macOS defaults. `tests/README.md` and ADR-0032 document this.

## Corrections to Phase 2

- **A5-25 above.** My S6 claim that `fin3` covered every relevant change since `3d7f1284` was wrong
  for `universal-profile-scenarios`.
- **O5, "no test needs the network".** That was correct, but I did not check for third-party
  packages. The local host had PyYAML and pypdf; CI had neither until `d739d479` (PyYAML) and
  still has no pypdf.
- **The `b3620e5c` PASS.** I did not see that `TEMP=RUNNER_TEMP` put the default-home test at 258
  characters, under its 260 threshold (A5-26).
- **Confirmed prediction.** Phase 2's expected N/A counts were confirmed: 4, 14, 2, 1 and 1, now
  with `managed-transaction`'s 1.

## Which later head this binding permits

The verdict and CI binding carry to a descendant H of `ed91ef4f` when both conditions hold:

- `git diff --name-only ed91ef4f H` lists only `.claude/` paths;
- the CI run on H reproduces this outcome: the same 19 green jobs (syntax plus unit-1, unit-2,
  integration-1, -2, -4 and other on each system), each integration-3 failing only
  on `document-pdf`, and integration-4 green on all systems (A5-27).

The local `6815ca46` qualifies on content. It is records-only, the product reader parses its map,
and it states A23.4 correctly as open. Until its CI run completes, however, its CI binding is
unverified. Preserving this file under `reviews/` does not affect the parsed map.

## Evidence and limits

Run log archives, all SHA-256:

| Run | Archive | Parse |
|---|---|---|
| `36054668106` | `8d8edb8ffdb72102cf3597ffd7c34d29f42e4924885d3b4a231867e17feeaa36` | `6f7a764dc4b6eafad3b4bb65e0fc239558b978f2deb02e235aab94022f5a1333` |
| `36063322462` | `4ab51497b80ca1f358415bf469e9d80aa2ba39ad2f1883cf41e537e637e4a410` | `ead31a544a601c3b82c02074881ad03033922748311e1258fa4fddd7d167c022` |
| `36065850657` | `5af71b02b1aa34531c4678c4618426e4ac22448dc14c41272adcfb250f04bee1` | `e0cec6bd7f220635e9af624f6378b254fc15be0cb8b8a21224ae6a36654332ad` |

Other outputs:

| Output | SHA-256 |
|---|---|
| Windows `steps.tsv` at `c5667a7e` | `c3be54be9000f880ce946dd1d3d2519e4d7b553b12e803a213c5329c3083744c` |
| Windows `steps.tsv` at `ed91ef4f` | `41db1cf79132b5dd079c2cd08a8917a565d27fe95d6213f4fd618a7a9157e923` |
| Bytecode probe, old | `a91cb52ac777463bc53457d4b6cb0dcd85c06ea1dacdbe42ab920765a39b9342` |
| Bytecode probe, new | `645a67da1978e84bada2225d0516a1d0b323e39e3efec2ddad73576f6b102761` |
| Tool `ci_logs.py` | `735b0ba6b5f74c8b16b81386b3ef71662b0c7d438f58dcf3e0f251aba3c29da4` |
| Tool `pycache_probe.sh` | `02b08ea8f76c1d886bd21b4919f99c70d59e6e40f829141db12d294cf151b52a` |
| Tool `work_probe.py` | `f38a67de6971ad177b20f2eb5a3ef80807ba3ab2c811fe360bdf6582212048f3` |
| Tool `entries.sh` | `b23b57922cbf5eb54182a5ed691f29ab851f08e77ab5e8c873d7a9801bf7a600` |
| Launcher `syn.ps1` (unchanged) | `e17d2a4a1321f435df6f616f70d85b943f3618480355edf80740afb0b04ce57f` |

The outputs are under `%LOCALAPPDATA%\Temp\a235\` and the tools under this session's `files/tools/`.
Everything was read-only on the repository and through the GitHub API. There was no push,
configuration write or credential probe, and `/tmp` stayed on `p13v\t`.

- **Platform.** My own runs were native Windows only, with Python 3.11.9. Linux and macOS
  evidence is the hosted CI.
- **Log visibility.** The runner prints only failing entries' output, so the inner counts of
  passing entries come from the runner's classification, not from printed test lines.
- **Compliance.** It stays UNVERIFIED without a resolved required-policy source.
