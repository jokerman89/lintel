# P13 optional source families independent review

**Complete released-unit SPEC: PASS. Subsequent whole four-path QUALITY: PASS.**
**P1: 0 / P2: 0 / P3: 0.** Same distinct reviewer; no implementation, source repair
or nested reviewer. This accepts source-family declarations, closure and preservation
at the frozen revision, not installed distribution, new-family model utility, native
formats, P10, original parent outcomes or initiative/SHIP clearance.

## Exact authority and source

| Item | Pinned identity |
|---|---|
| Complete family release | `a4262426b83b0eae93cf9695c3c1d8cb5303d478` |
| Accepted dependency / sole product parent | `42abfcba39ac80a271ff57e838ffccb1d7f6ed7c` |
| Frozen product | `20493b74005d349f6fd311503fe179de992a3506` |
| Report-only checkpoint, sole parent product; required review parent | `e797bd48c31257e79c7ccc3eb369d584bfd7db8b` |
| Builder `reports/P13.md` Git-byte SHA-256 | `87777ff464519b9685ea5639f624c3505a88258f791a7a814c39e0439e742f79` |
| Retained consumer PASS | `9f58aa80c1dd9ce55b16b73db48ffad89c4951ea` |

Read the complete released P13 card, selected work/spec/plan and original acceptance,
the full four-path diff, descriptor/reference/test changes and preservation delta.
Startup, Copilot/Universal and accepted ADR-0028/0029 inputs are unchanged from the
preceding review; the changed working-state context was read. Independently verified
both sole-parent links and the report-only scope. The complete 1,053-line builder report
has an exact already-read 867-line prefix; all new lines 868-1053 were read.

| Only selected product path | Verified Git-byte SHA-256 |
|---|---|
| `lib/capability-selections.json` | `975cf134a8124000624d0599a3b88a1c8eb6e5c0e8bdf0a75f3790fbdd86752a` |
| `skills/catalog/references/selections.md` | `4859d15dfc00ee5a4d933dfdb98185e2c42990d16b2a8e65fb2900396442e801` |
| `tests/unit/catalog-selection.py` | `26278a1ab9dcdc7bf6d896e70bc892ea0cdc271111308d405dddbdcb7140d3e2` |
| `.claude/plans/universal-implementation/reports/P13-skills-preservation.md` | `052a7136edb5b232d77ed0f7c9f364568dcb4bf6c2b6aae762b4abd9df534918` |

All other Git blobs/modes remain identical to the named dependency, including all
196 canonical skills/roles, aliases, generator, parsers/providers, installer, notices,
versions and generated outputs. Earlier PASS/FAIL histories are preserved. The reviewer
writes only this report and private evidence, not shared plans, memory or runtime records.

## SPEC completed before QUALITY

Complete SPEC PASS was frozen **2026-09-23 00:26:17 UTC**, with QUALITY not started.

| Control | Verdict and substantive evidence |
|---|---|
| S01 - Exact release/ownership | PASS: chain, four-path scope, report hash/prefix and clean pinned source verified; no moving coordinator or P12 repair source imported. |
| S02 - Existing implementation/defaults | PASS: schema 1, shared core, core/demo records and all stage records equal the base. Same generator/strict readers remain unchanged; core/demo/default metadata comparisons match, ordinary stdlib catalog check preserves its exact message. |
| S03 - Ten real source families | PASS: descriptor `:79-496`; every canonical member/path and declared file resource resolves. Design resources include all 35 actual CSV files, six scripts and contract/token references. CostAnalyzer and CapacityPlanner resolve to their actual engineering paths; no invented NetworkArchitect. |
| S04 - Minimal and shared dependencies | PASS: Word/PDF/XLSX select only core plus their own family, not common pipeline, other formats or frontend. PPT retains real Design DNA slide retrieval; customer drafts do not require demo narration. Regulatory expertise cannot remove core/required-policy obligations. |
| S05 - Deterministic literal projection | PASS: actual union queries in reverse order are byte-identical; members/resources and inclusion reasons deduplicate. Agent/no-match filters preserve full closure; literal aliases remain valid in ordinary discovery without becoming invented family members. |
| S06 - Actual provenance | PASS: design-bearing closure returns exactly `design-dna-corpus` and `design-dna-example-profile`, both real MIT/Apache notices and attribution. Null historical imports remain null; registry v2.5.0 remains a recorded release, not an import commit or licensing clearance. No notice/registry bytes changed. |
| S07 - Examples and readiness | PASS: reference `:204-434`; each exact heading has useful selected inputs, method/output, concrete negative and source/execution limit. PDF/XLSX retain unknown/null despite implemented source; Visio retains its exact template digest. Rejected P12 B01 source is absent and not advertised as accepted. |
| S08 - Refusal boundaries | PASS: retained malformed/member/alias/cycle/path/provenance cases plus family-specific faults. Independent actual CLI cases reject unselected bad members, missing CSV, empty notices, cycles, unsafe paths, omitted provenance and stale stage proof before JSON; zero matches remain successful. |
| S09 - Read-only/body/trust boundary | PASS: independent opening-header guard covers all 196 records and prevents reads after closing delimiters or resource-body opens. Data-only fixture headers with invalid body bytes and target/helper decoys remain usable without executing decoys. Query file/directory snapshots and source seals are unchanged. |
| S10 - Exact preservation identities | PASS: map `:81-322`; 127 historical rows remain verbatim. The current delta is exactly 43 originals (33 old-U changes and ten old-C changes) plus Swarm. Comparison with the original is 73 changed/53 identical. Exactly five frontend roles changed since the historical pin; 69-role map, all 76 Swarm destinations and 17 original artifacts remain preserved. |
| S11 - Accepted evidence boundaries | PASS: map `:225-247,347-402`; J04/J09/J11/J12/J13 pointers retain their actual scoped reviews and native limitations. The old accepted-P06 alias/notice pilot does not establish new-family distribution or new-P10 acceptance. P12 `d4e9188` remains rejected in this frozen source; later repair packets are outside this review. |
| S12 - Actual checks and honest counts | PASS: 109 current methods rerun, eight independent probe methods completed with the retained oracle correction below, focused syntax/alias/header checks and Git identity comparisons. No builder rerun or structural assertion is labeled native/model evidence. |

Actual canonical closure counts below include core/dependency members and deduplicated
declared resources. These are source-query results, **not installed dependency closure**.

| New family | Declared dependency | Closure members / resources |
|---|---|---|
| design-knowledge | core | 14 / 79 |
| frontend-design | design-knowledge | 36 / 106 |
| document-content | design-knowledge | 23 / 91 |
| document-word | core | 14 / 31 |
| document-ppt | design-knowledge | 17 / 85 |
| document-pdf | core | 16 / 37 |
| document-xlsx | core | 15 / 34 |
| document-visio | core | 14 / 22 |
| customer-communication | core | 21 / 29 |
| regulatory-review | core | 15 / 22 |

The useful methods and negatives were reconciled with relevant accepted source, not
just member counts: PPT's actual retrieval step, standalone template/output/fidelity
boundaries, implemented PDF/workbook limits, genuine cost/capacity roles and optional
legal-framework analysis remain distinct. No incidental Markdown link was promoted
into a new runtime dependency graph, and no resource list requests all bodies in context.

Preservation is also more than counts: original recommendation/method/argument/alias
rows are byte-for-byte retained, while the exact current delta identifies changed
methods, responsible evidence and remaining gaps. The five frontend role pointers sit
beside the unchanged 69-role map. The 17 source-identical Swarm charter/topology/brief/
report/review entries include its two directory placeholders. U/C is a Git-byte fact,
not fresh semantic acceptance or a new whole-corpus audit.

## Subsequent whole four-path QUALITY

QUALITY was performed only after SPEC PASS and frozen **2026-09-23 00:27:58 UTC**.

| Control | Verdict |
|---|---|
| Q01 - Design/contract reuse | PASS: ordinary records in the existing schema and one inventory/parser; explicit unions rather than a new conditional-dependency language, scheduler or policy engine. |
| Q02 - Closure correctness | PASS: canonical member identities, explicit regular-file resources, minimal standalone selections and actual design dependencies/provenance agree with the released source methods. |
| Q03 - Regression/test meaning | PASS: all 20 prior selection methods remain; 19 are AST-identical and the index expectation is intentionally extended. Nine new methods plus independent actual CLI negatives cover the additions without weakening prior refusals. |
| Q04 - Documentation and claims | PASS: inputs, outputs, worked decisions, failure cases and host/manual limits are concrete. Unknown maturity and staged-slot discovery do not become native capability, licensing or model-utility claims. |
| Q05 - Historical/current preservation | PASS: four-path changes preserve historical rows and original roles/artifacts, adding a pinned current delta rather than rewriting old results. Rejected P12 serialization and remaining native gates stay visible. |
| Q06 - Scope and maintainability | PASS: no new dependencies, canonical bodies, providers, installer, aliases, notices or generated writes; explicit resource repetition fits the existing schema. Whole selected diff passes whitespace checking. |

**Open findings: P1 0, P2 0, P3 0.** No product repair requested. Earlier consumer
and compact-discovery failures remain historical evidence with their separately recorded
closures; this family PASS does not relabel another package's pending acceptance.

## Checks actually run and retained failures

| Independent execution on this candidate | Outcome |
|---|---|
| Selection / metadata / consumer / registry suites | **29 + 31 + 33 + 16 = 109 distinct methods PASS**, zero failures/errors/skips |
| Independent family probes | Eight distinct methods: seven initial PASS; one failed only on the reviewer's mistaken CostAnalyzer directory expectation, then passed the targeted recheck with the inspected canonical path |
| Canonical/data-fixture CLI and API | All ten real closures; union determinism/reasons, literal aliases, no-match closure, actual missing/invalid inputs, dependency failure, no-write and bounded body reads verified |
| Original/current Git comparison | Exact four paths, 196 unchanged canonical blobs/modes, historical 127-row equality, exact 43+Swarm delta, 73C/53U, five role pointers, 76 destinations and 17 source-identical historical artifacts |
| Defaults/dependencies | Core/demo/all-metadata objects unchanged; malformed/absent descriptor ignored by ordinary metadata; missing PyYAML visibly refuses; actual `-I -S -B --check` succeeds with exact Windows CRLF message and no writes |
| Focused checks | Alias and frontmatter shapes PASS (127 skills/69 roles); all 12 reference Bash blocks pass syntax; selection tests parse as Python 3.9 grammar; product diff whitespace PASS |

The 109 methods were executed here through the inspected outer Python harness. The
builder's committed Bash-entry run remains builder evidence, not another reviewer run.
The base had 20 selection methods, including the separately accepted unknown-stage
regression; this unit adds nine, not ten. Repeated executions do not add method coverage.

Failed evidence is retained exactly. Builder `families-red` ran two methods with one
failure and four error entries; `families-first-closure` ran seven methods, six passed,
with ten missing-file oracle error entries. Its later correction expects the existing
reader's specific FileNotFoundError rather than ValueError; the provider was unchanged.
Those are builder-attributed historical results, not reviewer executions.

Two reviewer-harness corrections are also explicit: the initial Git/map collector
stopped decoding UTF-8 with Windows' default codec; a private explicit-UTF-8 wrapper
completed it without source normalization. The first independent run had seven passes
and one incorrect `agents/devops/CostAnalyzer.md` oracle. Actual source and metadata
correctly name `agents/engineering/CostAnalyzer.md`; only that private expectation was
corrected, and only the affected method was rerun. Original scripts/exits/logs remain.
Neither was a product repair or a hidden passing aggregate.

All product imports/calls, including outer comparisons, used inspected allowlisted
synthetic HOME/USERPROFILE/AppData/temp/XDG/Lintel/source/target/derived roots, explicit
PATH/PATHEXT and fixture Git ceilings. Source Git EOL identity was separate. Logs opened
before execution preserve actual exits; query fixtures include complete file/directory
snapshots and immutable source seals. Synthetic resource/body/negative fixtures are
labeled data-only, not installed/native execution. No private/global scan, activation,
profile change, audit suppression or record-denial workaround occurred.

Private `p13-families-review/` retains the exact selection/diff/report, stage decisions,
all successful and failed receipts, source seals, fixtures and preservation records.
Its 2,559-file manifest SHA-256 is
`0569c56a0c271b4e6fad863941d07d8c79d6f7ac87271936bffe14d86fcb564a`.
Report delivery receipts are separate. Actual host: Windows build 26200, Python 3.11.9,
existing PyYAML 6.0.3 and Git Bash. Python 3.9 is grammar-only evidence. No full suite,
native/browser/Office/raster/model, Linux/macOS, network, install, jq, P10 or CI action.

## Next action and boundary

Recovery `88aecc43-40f9-41d4-8947-6c2fb0a55481` may integrate this exact source-family
unit and retain its original owner/history. New-family installed distribution and
model utility, required Word/Excel/PDF/static observations, P10 and later P12 pipeline
review remain separate. No original parent, final version/generated, initiative or
SHIP clearance follows from this report or its test counts.
