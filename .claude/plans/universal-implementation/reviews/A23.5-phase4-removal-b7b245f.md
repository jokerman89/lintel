# A23.5 Phase 4: the operator-ordered removal, post-merge review and CI binding

**Reviewer:** session `8df81ac8-0043-4564-97bd-e2ed12c94b24` ("A23.5 final review"), for coordinator88.
**Date:** 2026-09-25. It follows `reviews/A23.5-phase3-ci-ed91ef4.md` (Phase 3). This is a
post-merge review: PR #93 was merged before this file was delivered.

## Verdict

- **Delta `d285a332..b7b245fa` (2 commits, 17 files, +165/-476): SPEC PASS, then QUALITY PASS with
  advisories.**
  - `9ee452c7` removes exactly the pypdf blocker: the reader, its three pypdf-dependent test
    classes, and the reader's adapter, selection and documentation entries. No remaining
    assertion, product refusal or declared-resource refusal is weakened.
  - `b7b245fa` is records only. The product reader parses the map: 235 tasks, and the only
    incomplete one is `A23.4.ci`.
  - Findings: P0 0 / P1 0 / P2 1 / P3 4 (A5-31 to A5-35). None needs a revert. The P2, A5-35, is
    about how the merge was recorded, not about content.
- **Scope interpretation:**
  - For the reader, it is sound.
  - For A15 it is defensible and conservative, but it is coordinator88's reading of the operator's
    words, and ADR-0033 should say so (A5-33).
- **Merged content:**
  - `main` is `80002ed4aaa8697ff658f50902a469c80e14856d`, a merge commit with parents `9575aaac`
    and `b7b245fa`, merged by `jokerman89` at 08:03:07Z.
  - Its tree, `9a0aa484fb7bc84f8e91587cd12fac01b99d1218`, equals `b7b245fa`'s tree. So
    `git diff 9575aaac 80002ed4` equals `git diff 9575aaac b7b245fa`, and `main` carries exactly
    the reviewed content.
- **CI binding: run `36108366549`, which passes.**
  - Event `pull_request`, attempt 1, head `b7b245fac0c84fb86d9558c4f523d6a6218640c4`, conclusion
    success at 09:06:14Z.
  - All 22 jobs fetched and checked out the test merge
    `5120bc20714310d36bc3291cd6e78094fdc70a8b` (parents `9575aaac` and `b7b245fa`). Its tree
    equals `9a0aa484`, which is the tree of both `b7b245fa` and `main` `80002ed4`.
  - **22 of 22 jobs pass.** Every system passes all 150 entries, with Skip 0 and Partial 0.
  - `document-pdf` runs and passes on all three systems. CI installs only PyYAML; no pypdf is
    present.
- **A23.4 closes.** The condition for `A23.4.ci`, the strict CI run of the removal head, is met by
  this run. It was met at 09:06:13Z, when the last job finished, which was after the merge (A5-35).
  - The count becomes 109 of 109 in-scope items, with A15's four removed from scope: never
    accepted, and not waived.
  - A23.5 stays satisfied. The product content is the Phase 2 content plus the Phase 3 delta plus
    this delta, all reviewed.
- **Merge:** on content, I see no reason against the merge, and nothing on `main` needs a revert.
  - Had I been asked before it, I would have said to wait for the three Windows jobs. They were
    the only evidence for Windows integration-1 to -3, which hold the kit and the
    installed-consumer entries. They passed.
  - A5-31 to A5-34 are fix-forward documentation and records items.

## CI outcome per job (run 36108366549)

The durations come from the Actions API. The suite counts come from my own parse of the complete
run log archive. Every job shows Skip 0 and Partial 0.

| Job | Linux | macOS | Windows | Timeout |
|---|---|---|---|---|
| unit-1 | 38/38, N/A 18, 1.3 min | 38/38, N/A 18, 2.0 min | 38/38, N/A 0, 7.5 min | 180 |
| unit-2 | 37/37, N/A 4, 3.4 min | 37/37, N/A 4, 5.8 min | 37/37, N/A 0, 16.5 min | 180 |
| integration-1 | 8/8, 11.0 min | 8/8, 10.3 min | 8/8, 61.2 min | 300 |
| integration-2 | 8/8, 18.3 min | 8/8, 26.1 min | 8/8, 34.3 min | 300 |
| integration-3 | **7/7** (`document-pdf` passes), N/A 1, 24.1 min | **7/7**, N/A 1, 19.3 min | **7/7**, 91.2 min | 300 |
| integration-4 | 7/7, 2.3 min | 7/7, 3.8 min | 7/7, 9.5 min | 300 |
| other | behavior 3/3, e2e 1/1, shape 41/41; 1.5 min | same; 3.3 min | same; 4.4 min | 60 |

- **Strictness.** Each suite job ran `tests/runner/run-all.sh ... --require-all` with its scope and
  shard, and the shard lines show 38+37 of 75 unit files and 8+8+7+7 of 30 integration files.
- **Syntax job:** success in 0.1 minutes.
- **`other` jobs:** every once-per-system step succeeded, according to the step conclusions in the
  Actions API:
  - structure verification and the three tiers;
  - the catalog, instructions, adapter and wiki checks;
  - the stock Bash 3.2 install on macOS, and `check-install.ps1` on Windows.
- **N/A:** exactly the 23 canonical `platform: windows-only` skips on each POSIX system, the same
  composition as Phase 3:
  - unit-1: `context-safety` 4 and `profile-path-identity` 14;
  - unit-2: `managed-transaction` 1, `review-evidence` 1 and `snapshot-ownership` 2;
  - integration-3: `universal-profile-context` 1.

  Windows reports none.
- **Environment:** Python 3.12.14 on Linux, and 3.12.10 on macOS and Windows. `Successfully
  installed PyYAML-6.0.3` is the only package installation.
- **Headroom:** the longest job is Windows integration-3, at 91.2 of 300 minutes.
- **`main`'s push run `36110845426`** tests `80002ed4` itself. When this file was written
  (09:13Z), 21 of its 22 jobs had completed, all green, and Windows integration-3 was still
  running. The binding above does not depend on it, because the tree it tests is identical. A
  failure there would be new information and should be reported against `main`.

## Delta review (`d285a332..b7b245fa`)

| Commit / area | Change | Judgment |
|---|---|---|
| `9ee452c7` reader | Deletes `skills/generate-pdf/scripts/check_pdf.py` (152 lines) | PASS. It was the only product importer of pypdf. |
| `9ee452c7` declarations | Removes the reader from `bin/li-copilot.py` `ADAPTER_RESOURCES`, `lib/capability-selections.json` `document-pdf` (resources, inputs, outputs and limitations) and `skills/CATALOG.md` (regenerated, one row) | PASS. The generated bundle goes from 444 to 443 files, and the `check_pdf.py` entry disappears. |
| `9ee452c7` skill | `skills/generate-pdf/SKILL.md`: states that Lintel has no PDF reader (`:59-62`, `:96-99`), and removes the reader's step, command and expectations prose | PASS, with A5-32 |
| `9ee452c7` tests | `document-pdf.py` drops `readable_pdf` and the `PhysicalGeometry`, `Reader` and `NativePdf` classes, plus `--native-root`. It keeps `Preparation`, `TextContexts` and the P05 raster-blocked control, now `VisualInspection` | PASS. Every removed test called `CHECK.inspect_pdf` or the `check_pdf.py` CLI. `NativePdf` only read PDFs that were already printed; it never invoked a print. |
| `9ee452c7` expectations | `copilot-kit.py:44,251`, `catalog-selection.py:577-582` and `generate-skills-present.sh:87-96` drop the reader from their expected lists | PASS. The kit's missing-resource refusal before writes still iterates every entry of `ADAPTER_RESOURCES` (`copilot-kit.py:1436-1440`), and the installed-check loop now uses `prepare_html.py` as the PDF representative (`:248-268`). |
| `9ee452c7` docs | `tests/README.md:40`, the `CHANGELOG.md` "Removed" section, ADR-0033 | PASS, with A5-33 |
| `b7b245fa` records | plan, final, handoff, working-state | PASS on parse: 235 tasks, incomplete `A23.4.ci` only. See A5-34. |

**Refusals and checks kept.**
- The P05 blocked-raster control still runs.
- Preparation's refusals still run: duplicate heads, a style-element escape, unknown header keys,
  a missing converter, empty or fragment HTML, and preservation of existing output.
- The `print_pdf.mjs` request validation still runs: `validateRequest`, in 6 node tests of
  `document-pdf.test.mjs`.
- The data-driven kit refusals still run.

What is lost is automated verification of text retention, page size and text origins. ADR-0033
declares that as its negative consequence, and the skill, selection and CHANGELOG say so.

**Leftover sweep.** Excluding `.claude/`, the only remaining `check_pdf` or `pypdf` text in the
product tree is:
- the CHANGELOG's "Removed" entry;
- the opt-in `--pdf-reader pypdf` of `tests/integration/design-browser-pipeline.py:36,393-449`,
  documented at `skills/browse/references/browser-operations.md:204-208`. It is kept on purpose
  (ADR-0033, Neutral), CI does not run it, and it is test tooling, not a Lintel reader.

Reader claims that survive in other words are listed under A5-31.

**Scope judgement.**
- **The operator's message.** Turn 547 of the coordinator's session, at 07:09:45Z, is "Ta bort
  detta ur Lintel." It is prefixed to the coordinator's own two-item summary: pypdf in CI, and
  A15.1-A15.4 blocked by denied routes.
- **The reader.** Removing `check_pdf.py` together with its tests is the narrowest change that
  removes the undeclared dependency. Keeping the reader and dropping only its tests would ship
  untested code with an undeclared import. Removing only the test would weaken a check.
- **A15.** "Ur Lintel" ("out of Lintel") can also be read as removing the document capabilities
  themselves. The records-only reading keeps working, reviewed code (L-031). It avoids colliding
  with the separate legacy-removal initiative, and it is reversible. It is therefore the safer
  reading. The final report states that the items were never accepted and are not waived, so
  the 109/109 count does not overclaim.

## Findings

| ID | Sev | Location | Finding |
|---|---|---|---|
| A5-31 | P3 | `skills/catalog/references/selections.md:332-341`; `docs/client-adapters.md:128`; `skills/make-pdf/SKILL.md:4,62-68` | Reader claims survive outside the edited files. The first is the `document-pdf` selection's own example (`lib/capability-selections.json:364-367`). It still asks for an "exact source/page oracle" and "reader tools", and it says to "inspect text retention and physical page boundaries with the existing checker", a checker that no longer exists. `client-adapters.md` still lists "PDF preparation/inspection/print adapters", but no inspection adapter remains. `make-pdf`, a member of the same selection, still makes reader inspection (UserUnit, MediaBox/CropBox) a numbered step. That is acceptable only if it is read as an external, separately authorized reader. Reword the first two, and add one sentence to `make-pdf`. |
| A5-32 | P3 | `skills/generate-pdf/SKILL.md:75-76,96-99,142-145`; `lib/capability-selections.json:372` | Step 2 no longer declares text and page requirements, and **DONE** ("all requested writer/fidelity/visual and policy controls met") no longer names them. A run can therefore reach DONE while searchable text, page material and physical dimensions stay unverified, although the selection calls those gaps "explicit gates". State what status an unverified text or page requirement produces. For example, a caller-required text or page check that no authorized reader observed is BLOCKED, and it is never DONE. |
| A5-33 | P3 | `.claude/decisions/0033-remove-document-format-acceptance-and-pdf-reader.md:5,34-37,46-49` | The Deciders line names the operator, and the ADR states the narrower A15 reading, and the rejection of removing the whole family, as the decision. Those are coordinator88's interpretation of a one-line instruction, which the coordinator called "open to operator override" in its message to me, but the ADR does not say so. Add that sentence and record whether the operator was told. |
| A5-34 | P3 | `handoff.md:736-741`; `reports/final.md:23,99,250-258` | The records are stale in four places. `handoff.md` still says "Current original acceptance count is 107/113 ... Still open: A15.1-.4 ..., A23.4 ... and A23.5" under the current delivery status. `final.md:99` still credits `b3620e5c`'s Windows `TEMP` step, which `d739d479` replaced. The Delivery CI table has no row for `36076065427` (`d285a332`, 19/22, which reproduced the Phase 3 binding) or for `36108366549` (`b7b245fa`, 22/22). "108 of 109 original top-level items" would be clearer as "108 of the 109 items still in scope (113 original, minus A15's four)". |
| A5-35 | P2, process and records, open until recorded | PR #93; `handoff.md:730-733`; `plan.md` A23.4 (`A23.4.ci`) | The merge came before both gates that the delivery records named: this addendum, and the removal head's strict CI. At 08:03:07Z, three Windows jobs had not finished: integration-1 finished at 08:36:09Z, -2 at 08:09:15Z and -3 at 09:06:13Z. The coordinator attributes the early merge to the operator's rush order. The local session store records the operator's "Hurry" at 07:14:25Z (turn 549), and "... skip whatever you can skip we must RUSH" at 08:04:52Z (turn 552), 1 min 45 s *after* the merge. The store may timestamp a queued message when it is delivered rather than when it is typed, so I cannot establish the order. The content outcome is clean (22/22 on the merged tree). The follow-up records PR should state three things: that the merge preceded three jobs and this addendum; which operator instruction authorized skipping those gates, and when; and that `A23.4.ci` closed retroactively on run `36108366549` at 09:06:13Z. |

Observations (no severity):

- The historical P12 reports and reviews, `packages/P12.md:230,240` and the P13 preservation map
  still describe the reader. ADR-0033's Neutral clause keeps them by design.
- `skills/generate-qa/SKILL.md:191` ("PDF needs a real page renderer and text/page-fidelity
  check") and `skills/generate/SKILL.md:77-78` state gates, not a Lintel reader. They are
  consistent with the removal.
- `generate-pdf`'s "Custom paper needs ... observed PDF dimensions" (`:121`) now needs an external
  reader or viewer. Step 6 says so.

## Local evidence (Windows, CRLF checkout of `b7b245fa`)

Every process ran in my synthetic launcher, with no `LINTEL_*` variable except the PowerShell 7
selector, and without jq (L-046). The checkout was clean afterwards, and `/tmp` stayed on
`p13v\t`.

- **Changed entries, all exit 0:**
  - `document-pdf`: 16 unittest tests OK and 6 node tests, no skip;
  - `generate-skills-present`, `catalog-selection` (96 s), `catalog-metadata` and
    `catalog-consumers`;
  - `universal-profile-scenarios` (the live map);
  - `design-browser-pipeline` in its static default mode.
- **Once-per-system checks and tiers, all exit 0:**
  - `li-catalog --check`, `li-instructions check`, `li-copilot check`, `li-wiki-gen --check` and
    `install/verify.sh --all`;
  - behavior 3/3 and e2e 1/1;
  - shape 41/41. Its one partial is the local jq absence, and CI ran it strictly.
- **Bundle:** `generate()` produces 443 bundle files at `b7b245fa` and 444 at `ed91ef4f` (product
  equal to `d285a332`). `check_pdf.py` is absent at the head, and the remaining `pypdf`/`check_pdf`
  mentions are only the CHANGELOG entry and the opt-in browser test option. `generate()` did not
  touch its target.
- **Work map:** the product reader parses 235 tasks at `b7b245fa`, and the only incomplete task is
  `A23.4.ci`.
- **Earlier binding:** run `36076065427` on `d285a332` reproduced the Phase 3 outcome exactly: 19/22
  jobs, each integration-3 failing only on `document-pdf`, with test merge `43dacb8`. Its parse is
  byte-identical to that of run `36065850657`.

## Which later head this binding permits

The binding carries to a descendant H of `80002ed4` (`main`) when both conditions hold:

- `git diff --name-only 80002ed4 H` lists only `.claude/` paths;
- the CI run on H is 22/22 green. Records are test inputs (A5-27), so H needs its own run.

The follow-up records PR that preserves this file qualifies on those terms. Product changes, for
example from "Removing legacy skills and agents", are not covered, and they need their own review.

## Evidence and limits

| Output | SHA-256 |
|---|---|
| Run `36108366549` log archive | `c869a38d92b897eb8258500417624b9631b8f78addac32d9c92bdd1cb27baf18` |
| Its parse (`ci_logs.py`) | `43ded7561664def5301debce3b93318725b56d3f06e16f86e625a11a34d5a5e1` |
| Run `36076065427` log archive | `76114321d4f4f3e243b92a015fcc0a6e3f053543d103f7672f298aff27fd973f` |
| Its parse (identical to run `36065850657`'s) | `e0cec6bd7f220635e9af624f6378b254fc15be0cb8b8a21224ae6a36654332ad` |
| Windows entries `steps.tsv` at `b7b245fa` | `2bd50f74fada2d4928128bc733aab55c307f87090d3aa8e4a2d178c92ef3526e` |
| Head checks `steps.tsv` at `b7b245fa` | `9e71839177349fd27179151fab4dbd986ddccb08723b9ce6e88ba33ae1a8ef18` |
| Bundle probe at `b7b245fa` | `e172ecb5079c38b813e58259dbcf745b88b0bf02f06e29c0a7aeafbcbb3d503e` |
| Bundle probe at `ed91ef4f` | `60aa1d2e7a200590c745b87791b9dc0cafc52d424a6e3a4edbcb39aa1f82df67` |
| Work probe at `b7b245fa` | `35b5d50ac92e7790bd6cfc8fa1014e48673a7b5fbe7bf91d78949e6f6cdb0807` |
| Tool `bundle_pdf_probe.py` | `140a30b09615c8c81919c111fce79c8ead89a3865d3292cceedc906e858afefc` |
| Tool `head_checks.sh` | `e11c10cac2d76b6cf91ece89e1ad1a23a63db1ba7d0737591f0ecc921c96c8a4` |
| Tool `ci_logs.py` (unchanged) | `735b0ba6b5f74c8b16b81386b3ef71662b0c7d438f58dcf3e0f251aba3c29da4` |
| Tool `work_probe.py` (unchanged) | `f38a67de6971ad177b20f2eb5a3ef80807ba3ab2c811fe360bdf6582212048f3` |
| Tool `entries.sh` (unchanged) | `b23b57922cbf5eb54182a5ed691f29ab851f08e77ab5e8c873d7a9801bf7a600` |
| Launcher `syn.ps1` (unchanged) | `e17d2a4a1321f435df6f616f70d85b943f3618480355edf80740afb0b04ce57f` |

The outputs are under `%LOCALAPPDATA%\Temp\a235\`, and the tools are under this session's
`files/tools/`. Everything was read-only, on the repository and through the GitHub API. There was
no fetch, push, branch, commit or configuration write, and no credential probe.

- **Platform.** My own runs were native Windows only, with Python 3.11.9. The Linux and macOS
  evidence is the hosted CI.
- **Log visibility.** The runner prints only failing entries' output, so the inner counts of
  passing entries come from the runner's classification.
- **Authority timing.** The session-store timestamps for A5-35 may record delivery rather than
  composition.
- **Compliance.** It stays UNVERIFIED without a resolved required-policy source.
