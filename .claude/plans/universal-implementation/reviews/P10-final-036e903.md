# P10 final-candidate specification stop

**SPEC: FAIL. First whole-P10 QUALITY: NOT STARTED.**
**Open findings: P1 0 / P2 1 / P3 0.**
F03's native-observation repair passes its independent recheck; F01 and bounded
F02 closure remain valid. New **F04** reports a stranded canonical redirect as
`current`, although the actual migration preview correctly refuses that same
target. Product testing stopped. This filename is not a final acceptance claim.

Same independent reviewer; no implementation or nested reviewer. Coordinator
`88aecc43-40f9-41d4-8947-6c2fb0a55481` acknowledged F04 and requested this freeze,
with no further tests, reader expansion or QUALITY. This is the sole repository
output; no repair, recovery, post-stop fixture cleanup, shared-ledger change or import.

## Immutable scope

| Identity | Exact value |
|---|---|
| Reviewed report HEAD / parent required for this report | `50e654afcf7fae0548e4d16a4bf2d52b725523bc` |
| Product | `036e90330dd96fa27dd1b1574e7d39a12af2eaf2` |
| Direct product parent | `7bf5a31526cc39951978945df86dea85796fd01b` |
| Original owned base | `fb96f71342f7994c42bdfc7cd61e6d452e6acb54` |
| Builder report Git SHA-256 | `fb58454bd7304c1630b48aa220d188df3734a974f6477afe1354e38def949741` |
| F03 authority | `0ad183a7464288e016febc0523d34e57f8e88ea3` |
| Preserved prior review | `c1f6bad1a366bcb9088c118e2118a5ea3686a8b8` |

Initial HEAD was exactly the prior review and clean. A local preservation ref,
`refs/heads/review-p10-a797-frozen-c1f6bad`, retains it. The worktree then switched
cleanly to detached `50e654af`, without reset, discard or amendment. All 967 source
files were exported from immutable Git blobs and checked before/after execution.

The new product has exactly three paths, +240/-13: `bin/li-lifecycle.py`,
`tests/integration/universal-lifecycle.py`, and `docs/lifecycle.md`. Independent AST
comparison confirms only production `layout_observation` changed, all 57 prior
lifecycle test methods remain, and four new methods were added. Nineteen inspected
contract/core/caller identities, including the complete Copilot test file, match
`7bf5a315`; F01's parser and F02's producer functions are unchanged.

| Current producer / changed test | Git-byte SHA-256 |
|---|---|
| `bin/li-lifecycle.py` | `11bcaa9ac61a4352b4126a13c2f43c6f13de26121425341f4578141f783ad109` |
| `docs/lifecycle.md` | `1a460d4d93f4d1397c7d35ea4c5dde566fdbb1051d3ee0bd60426734b918ff34` |
| `tests/integration/universal-lifecycle.py` | `cf9ba0383897eb5ced5b25e441c360f19e5da2dd3a41d9b5369ef126b7d709df` |
| `lib/managed_transaction.py` | `acbf84ef94a9fc4afdb8f66766d376653549ee82f4cf063c6a5721b1fdc08cae` |
| `docs/native-installation.md` | `e2cf6bf3417e10ad12112002e0719a581f1632b0db2881459cfaad20208e1ae0` |
| `install/directories.txt` | `c8e0e825586f309c43bbcb5dc6644eb270a233ee638c7244696193db8f52d7c3` |
| `install/native.sh` | `1425c05888b24b413a45ec33f70a98fe432f73774634197cae4cbdc9d1c9e187` |
| `install/native.ps1` | `57173b99e0c809d3fe45a6c9b894ddab0c07b3961a2d73b6fd61af25047483f7` |

Original 48 paths plus the authorized copied validation fixture remain the P10
surface. Original implementation `a73cf9ee`, fixture `acf97f1`, F01 `4407827`,
accepted-core imports `bc0863a`/`8097ba2`, native-consumer `a31c5eb`, Git refusal
`a559c9f`, F02 `a797b2d`, and this F03 repair retain their separate attribution.
Coordinator mode/diagnostic/footer changes and accepted P03/P07/provider bytes
are not reassigned to P10. No unaccepted product was imported.

## F04: a canonical redirect hides its missing destination

**P2, confidence 10/10. Original A12.4.b / A12.4 / R01 / CP-16.**
Expected: a missing canonical redirect destination must remain an explicit
incomplete/error observation, not a clear `current` target. The defect is in the
read-only observation, not the correctly refusing migration guard.

`bin/li-lifecycle.py:490-497` recognizes a known redirect by its text prefix and
adds it to `stubs` without checking the canonical destination.
Lines 527-529 then report `current` when the version is 5 and `legacy` is empty.
In contrast, the same source's `migration_changes.move` at 777-790 observes the
destination and refuses its absence. That refusal precedes `runtime_publication`
at 1202; no receipt is involved.

Both independently constructed targets are plain directories of length **153**,
without Git metadata. Marker/redirect/destination lengths are **180/170/179**.
They have identical exact LF seeds, mode **0666** (438), readonly **false**:

```python
# .claude\lintel-layout.yaml: 18 bytes
b"layout_version: 5\n"

# tasks\lessons.md: 117 bytes, exactly the canonical producer's redirect
b"> Moved to .claude/memory/lessons.md (v5 .claude/ home layout, ADR-0005). Retained for explicit historical recovery.\n"
```

| Seed | SHA-256 before = after |
|---|---|
| Marker | `a8e022980d1bc17474f4199f6a22fd6d8eba27f145375854a5e4b65cb19f92eb` |
| Redirect | `819912e23957536ae225c7d220728c2564cde1af9f50b0398309dfc1ad6b0332` |

The positive control additionally contains
`.claude\memory\lessons.md = b"Retained migrated consumer lessons.\r\n"`.
The stranded fixture never contained that destination; no file was deleted to
manufacture a race or change a running target.

Exactly **three real product calls**, from unrelated owned fixture directories:

```bash
# Actual unchanged migrations skill block, once for each target:
bash "$LINTEL_SOURCE_ROOT/bin/li-lifecycle" \
  --source "$LINTEL_SOURCE_ROOT" --repo "$LINTEL_REPO_ROOT" migrations

# Actual recommended read-only migration preview, stranded target only:
bash "$LINTEL_SOURCE_ROOT/bin/li-migrate-claude-home" \
  --dry-run --repo "$LINTEL_REPO_ROOT"
```

Each outer argv was the existing Git Bash executable with
`--noprofile --norc -c <exact block>`. The source is the sealed full Git export;
the actual catalog, skill, dispatcher and migration wrapper were used unchanged.
No helper, parser, subprocess result or catalog was substituted.

| Actual call | Exit / output |
|---|---|
| Retained destination, migrations | 0; empty stderr; `current`, version 5, `legacy: []`, `stubs: ["tasks/lessons.md"]` |
| **Missing destination**, migrations | **0; empty stderr; the same `current` / empty legacy result** |
| Missing destination, actual migration preview | **2; empty stdout; `li-lifecycle: Stranded redirect without its destination: tasks/lessons.md`** |

Both catalog rows remain `overdue`; the error is not a dropped row or changed
schedule. No migration/transaction completion was claimed: the false claim is
the target observation `current`. The private discriminating probe returned **2**.

Complete before/after fixture and source maps match, including bytes, sizes,
modes, directories and synthetic home/config roots. No target profile, pin,
registry, audit, receipt, lock, snapshot, transaction or recovery store was created.
Both fixtures and raw output remain intact. Personal prefixes are redacted here;
exact normalized paths and all maps are in private evidence.

The unchecked known-redirect branch is AST-identical to `7bf5a315`; F03 did not
introduce this semantic defect. Source inspection shows the same branch covers
the four `KNOWLEDGE_MOVES` and the special `docs/adr/README.md` redirect. Only
the lessons mapping was executed for F04. Nested ADR redirects already have a
separate destination check at 518-525. The owner should reconcile this existing
reader/guard discrepancy as one class, not add a filename exception or weaken the
guard. No new schema, Git compatibility project or P03/P07 change is implied.

## F03 closure and actual continuation

The prior exact 66-byte marker / 63-byte unmigrated-lessons inputs were rerun
through the real skill at **short153 and plain256**, retaining marker180/283 and
lessons170/273. Both now return exit0, empty stderr, `incomplete`, version5 and
`legacy: ["tasks/lessons.md"]`. Source/fixture maps are unchanged; both fixtures
are retained. This closes the original native false-absence counterexample,
not all possible migration observations.

Six focused existing methods then passed: **6/6, 41.629s, exit0, 35 observed
subprocesses**, no skips, complete exact fixture cleanup and unchanged outer/source
maps. They cover real absence/current/incomplete, hidden/nested legacy files,
retained/missing nested redirects, malformed/non-file markers, real Windows
exclusive-handle read denial, the catalog caller and doctor observation.

```text
python.exe -I -B <files>\p10_combined_test_runner.py f03-readers
  <sealed-source>\tests\integration\universal-lifecycle.py
  --root <sealed-source> --bash <Git Bash> -v -f
  MigrationInventory.test_layout_observation_real_skill_matches_short_and_long_review_fixture
  MigrationInventory.test_layout_observation_distinguishes_absent_current_and_unmigrated_targets
  MigrationInventory.test_layout_observation_retains_nested_hidden_legacy_and_redirects
  MigrationInventory.test_layout_observation_refuses_malformed_nonfile_and_unreadable_inputs
  ProfileLifecycle.test_migrations_use_source_catalog_and_keep_overdue_unknown_states
  ProfileLifecycle.test_doctor_reports_bytes_not_hook_counts_or_historic_host_activity
```

The separate `required-original` Copilot command used the same isolated runner,
sealed `tests\integration\copilot-kit.py`, `-v -f`, and these ordered selectors:

```text
CopilotKit.test_joined_installed_dependencies_cannot_be_hidden_by_inventory_removal
CopilotKit.test_original_default_113_128_paths_init_check_and_owned_recovery
CopilotKit.test_canonical_default_caller_child_keeps_verified_parent_and_unbound_child
CopilotKit.test_canonical_required_caller_policy_refuses_missing_drifted_and_conflicting_context
CopilotKit.test_interrupted_adapter_publication_requires_explicit_owned_recovery
```

The first reported `ok`; the second was in progress when F04 was confirmed.
The known attached process was stopped immediately. **No aggregate exit or normal
cleanup result exists**; the last three methods did not run. Eleven subprocess
starts were recorded. Read-only sealing found two stable snapshots of **2,863
entries / 2,282 files**; journals include the first completed transaction and the
interrupted second transaction in `applying`. No recovery or cleanup was attempted.
This deliberate review stop is not a product interruption test or a cleanup error.

The newly completed dependency selector adds classic source-closure evidence;
it does not diagnose or erase the previous journal `WinError 5`. No retry,
permission change, changed root or transaction repair was used.

## Original acceptance and remaining execution

All whole-P10 QUALITY rows are **NOT STARTED**. `BLOCKED` retains scoped positive
evidence but does not manufacture complete acceptance from a stopped aggregate.

| Original control | SPEC | Evidence / remaining gate |
|---|---|---|
| A12 | **FAIL** | F04; full acceptance incomplete |
| A12.1 | BLOCKED | Existing lifecycle evidence retained; remaining caller/validation proof |
| A12.1.a | BLOCKED | F03 diagnostic recheck passes; canonical caller evidence retained, remaining policy selector |
| A12.1.b | BLOCKED | Profile/switch evidence retained; actual copied validation cleanup and required caller outstanding |
| A12.1.c | BLOCKED for complete gate | Unchanged role/persona/private-boundary evidence retained, not rerun |
| A12.1.d | BLOCKED for complete gate | F02 planning/ownership remains closed; migration guard refuses F04 correctly |
| A12.2 | BLOCKED | Remaining adapter preservation selectors |
| A12.2.a | BLOCKED for complete gate | Native no-Python and original default/classic evidence retained; current aggregate stopped |
| A12.2.b | BLOCKED for complete gate | F02 class closure retained; remaining protected-file/EOL/namespace selectors |
| A12.3 | BLOCKED | Remaining actual adapter interruption; prior error history unresolved |
| A12.3.a | BLOCKED | No success inferred from interrupted current transaction |
| A12.3.b | BLOCKED for complete gate | Owned recovery/replay evidence retained; no recovery run after this stop |
| A12.4 | **FAIL** | False `current` hides a missing canonical destination |
| A12.4.a | BLOCKED | Actual approved Git-refusal selectors not reached |
| A12.4.b | **FAIL** | F01/F03 scoped closure retained; F04 remains open |

Carry forward valid unchanged independent evidence, at its actual revision:
F01 `00a0bef` (40 candidate + 2 baseline cases, seven tests); `c1a38a03`
(53 lifecycle and nine runtime methods); `c1f6bad1` (three installed F02 methods,
four long lifecycle methods, and its individual completed classic/default/canonical
selectors). The latter includes default113/store128 and actual218->260 recovery,
classic109/source116/target182 and atomic256->263, plus the real installed caller
with retained304 history. Their aggregate/cleanup limitations remain recorded.
Do not restart all those unchanged suites after each repair.

The following deduplicated original controls remain uncompleted here. They are
a continuation inventory, not permission for more tests before a new candidate:

```text
# tests\integration\copilot-kit.py
CopilotKit.test_canonical_required_caller_policy_refuses_missing_drifted_and_conflicting_context
CopilotKit.test_long_git_metadata_runs_real_effective_ignore_validation
CopilotKit.test_git_verification_directory_linked_and_plain_folder_controls
CopilotKit.test_missing_runtime_ignore_is_detected_and_repaired
CopilotKit.test_interrupted_adapter_publication_requires_explicit_owned_recovery
CopilotKit.test_full_protocol_is_portable_and_project_prose_survives_update
CopilotKit.test_modified_managed_file_refuses_entire_update
CopilotKit.test_unmanaged_collision_refuses_before_foundation_writes
CopilotKit.test_crlf_source_and_autocrlf_clone
CopilotKit.test_source_update_is_reviewable_and_deterministic
CopilotKit.test_symlink_parent_refused
CopilotKit.test_long_protocol_metadata_preserves_prose_eol_and_modified_block_refusal
CopilotKit.test_long_generated_link_metadata_checks_real_missing_and_linked_targets
CopilotKit.test_long_public_directory_index_is_selected_from_the_real_source
CopilotKit.test_incomplete_protocol_marker_refuses_without_changes
CopilotKit.test_invalid_inventory_root_types_are_clean_errors
CopilotKit.test_inventory_traversal_and_windows_paths_refused
# tests\integration\universal-profile-context.py
ProfileLifecycle.test_validation_skill_executes_shared_contract_without_activation
```

The final three Copilot entries retain the original frozen preservation selection;
they were not part of this stopped five-selector command. Default/canonical
positives can be reused where the unchanged-source evidence suffices; any still
missing default-preservation/cleanup proof must remain explicit, not waived by
that reuse. Unsupported long linked-Git positive acceptance remains UNVERIFIED.
No stopped Git compatibility/setup variant should be retried.

## Isolation, retained limits and seals

Every command/product import used reconstructed synthetic HOME/USERPROFILE,
application/XDG/cache/temp, source/target/home/packs/pointer/audit/jobs/private-role
and derived profile/history/registry/state roots. Explicit PATHEXT and interpreter
binding, disabled global/system Git configuration, discovery ceilings and empty
hooks were recorded. Source-fixture Git configuration remained separate from
checkout-equivalent autocrlf used for real-worktree status. Output was redirected
before execution. The F04 probe used separate fixture/runtime roots, not the
running Copilot fixture. No unexplained cache difference was ignored.

Actual new runtime is Python3.11.9 and existing Git Bash5.3.15 on Windows.
Prior approved PS7.6.6/no-Python native evidence is retained, not freshly rerun.
Denied PS5.1, actual Python3.9, stock Bash3.2/POSIX, other OSs and host activation
remain unverified. No policy override, real-home inspection, external access,
global installation or new dependency occurred.

Historical strict **120/128 FAILED**, the failed native/PATHEXT/cache diagnostics,
F01/F02/F03 REDs, failed 15-/7-/3-case groups, journal `WinError 5`, stopped Git
experiments and both earlier independent stopped/failed aggregates remain as
recorded. Original D01's failed default setup, corrected normalized260 length,
original overcount/log and explicit-store control are not rewritten. D03's cleanup
**ERROR** remains separate; its actual copied validation method was not run here.
No synthetic aggregate pass, A13 release, P08 interface acceptance, source import,
final CI/integration or human/host approval follows.

Private evidence remains under `<session-files>\p10-final-036e903-independent`.
It includes exact argv, environments, raw exits/stdout/stderr, full before/after
maps, source manifest, scope proof and retained partial transaction state.

| Seal | SHA-256 |
|---|---|
| `final-spec-stop-seal.json` | `f1b3b1771abe615a2b3d3d03db3d21ea4e9b6de7a6f9511661c3f85d85d476a0` |
| `final-spec-evidence-index.json` (208 evidence files) | `b051c985c38a452eca6a626077bb75aeee6838a7e769f80dd6a3cf2c5c226130` |

Read-only sealing returned0 and reverified all967 source files, unchanged F04
fixtures/outer state and clean detached50e654af before this report. Hashes and
declared actor identity bind evidence; they do not authenticate independence or
enforce a policy. The sole report is delivered locally with the required trailer;
the exact commit/hash/clean result is supplied in the handoff.

**Cycle position:** REVIEW / SPEC FAIL -> original owner F04 repair and remaining
original SPEC continuation. Whole QUALITY has never started. Remain read-only.
