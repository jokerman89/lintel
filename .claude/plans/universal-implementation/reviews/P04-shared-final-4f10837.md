# P04 repaired shared-evidence consumer review at 4f10837

**Date:** 2026-09-22. **Independent reviewer:** session
`ed672f58-2e85-42e2-b1b2-0635ba5b2325`; no product implementation by this reviewer.
**Coordinator:** `88aecc43-40f9-41d4-8947-6c2fb0a55481`.

**Released-unit SPEC: PASS. First eligible full bounded QUALITY: PASS with one
advisory P3. Unit verdict: ACCEPT WITH ADVISORY.** Open findings:
**P1: 0; P2: 0; P3: 1**. F06/P1 and F07/P2 are closed on the exact repair below.
No product fix was made. The full specification stage passed before quality
review began; quality covered the whole released unit, not only the repair.

This is component-unit acceptance, **not final A22.7, package-parent, newer P08
selected-work/resume, strict-v2 integration, host/client or release acceptance**.
Synthetic actor records do not establish independent people or live-host
corroboration. Earlier historical merge/component acceptances and rejected
checkpoints remain separate, unchanged evidence.

## Immutable inputs and scope

| Role | Exact ref |
|---|---|
| Full released-unit delta base | `0516a4a05708e06f86cd51ae1d642399737e58b3` |
| Released dependency/card tree | `bdfd1274b47fb54687076ef89082fddf2ceda3b5` |
| Original shared product | `f02e994a5a8c131bac862c3c866289f49c05a212` |
| Repair parent / earlier report-only tip | `c2a9455a0c87db5900f93d73afa62e2e5f635ac4` |
| Repair authority | `71af12afb0d63fef779de43bc67004920270e8e6` |
| Reviewed repaired product | `4f108374a712818733f95df0b78b7c42e619444c` |
| Product tree | `6f98908b635135916d43e52a8c6d768a0b0a6d47` |
| Frozen report-only tip / this report's intended parent | `fcfed13b7345d539b3c9b2cce41dfe8cd1878711` |
| Preserved previous independent rejection | `63cf1bb30dd629f794a77a14bf09e8f264019e50` |

The clean detached snapshot was pinned to `fcfed13`; its parent is `4f10837`,
whose parent is `c2a9455`. The six repair paths are the two Swarm libraries,
shared-binding test, Swarm skill, concept documentation and P04 builder report.
No schema, CLI, provider, snapshot or P08 repair was smuggled into that delta.

The complete `0516a4a..4f10837` unit remains exactly 17 paths:
`bin/li-swarm.py`; `lib/swarm_contract.py`, `lib/swarm_evidence.py`,
`lib/swarm-schema.json`; `skills/swarm/SKILL.md`;
`docs/concepts/swarming-work.md`; the five Swarm brief/report/review/charter/
coordination templates; `tests/integration/swarm-shared-binding.py` and `.sh`;
`tests/integration/swarm-workflow.py`; `tests/shape/swarm-contract.sh`;
`tests/unit/swarm-contract.py`; and
`.claude/plans/universal-implementation/reports/P04.md`.

The builder report has 736 lines, retains the exact earlier 565-line prefix,
and hashes to
`4842fbcd7bff1ca7866d6ed2461d06ab6f0a5d7b902c50a76079e1565ad2672b`.
Its builder results and committed selected rechecks are not counted as this
reviewer's executions.

## Original finding dispositions

| Finding | Disposition | Repaired location and actual evidence |
|---|---|---|
| F06 / P1, coordinator runtime alias ownership | CLOSED | `lib/swarm_evidence.py:28-50,198-211`; `lib/swarm_contract.py:399-409,697-711,862-867`. The shared P03-backed metadata guard is used at topology, actual reviewer-path admission and consumption. All six physical hardlink/symlink cases against state/audit/jobs now fail both public gates; sentinels stay unchanged. |
| F07 / P2, valid containing P05 selection rejected | CLOSED | `lib/swarm_evidence.py:54-78,154-157`. Actual accepted P05 parent selection now passes status/wave/resume/verify. Future files revoke stale evidence; renewed evidence passes. Bound tracked-directory deletion passes. Partial child, sibling, textual prefix, missing parent and file-as-parent negatives remain blocking. |
| F01-F05, earlier component repairs | RETAINED, not reopened | Original authority/reducer/cross-lane hardlinks, root boundaries, package prerequisites, mode/type/exact-link-target controls remain in the passing local suite. Unchanged Brief Forge/YAML protections retain their earlier acceptance; that separate suite was not rerun. |

Additional F06 cases passed: all metadata-role directory leaves, linked/junction
or file ancestors, safe missing paths, reverse aliases, exact ordinary review
slots, coordinator references without actor grants, consumption-time physical
checks and explicit inspection refusal. The stricter metadata rule does not
change supported product-data symlink semantics.

## Complete released-unit specification

These are the same 12 review controls used in the prior rejection, not new task
IDs. All required selected controls pass; the path-boundary limitation below is
explicit, not an assertion of general Windows compatibility.

| Control | SPEC | Subsequent QUALITY | Evidence on the repaired unit |
|---|---|---|---|
| C01 Accepted providers and existing map/package APIs | PASS | PASS, F08 advisory | Owned-delta/caller inspection and real P05/P07/P09 calls; unchanged providers; no replacement snapshot hash, policy engine, latest selector, scheduler or P08 import. F08 concerns preflight diagnostics, not an alternate accepting provider. |
| C02 Work, leaves, package, attempt, actors and raw result/report identity | PASS | PASS | Shared identity negatives; all 57 local cases; independent raw-versus-normalized report/product probes. |
| C03 Complete caller-selected P05 coverage | PASS | PASS | Original F07 counterexample now passes all four commands; containing/deleted parent positives and finite negative boundaries pass. New scoped files still revoke evidence. |
| C04 Coordinator/reviewer ownership and physical aliases | PASS | PASS | Original six F06 public-gate cases plus new parent/leaf/reverse-alias/inspection/coordinator-reference cases; retained authority/reducer/cross-lane protections. |
| C05 One gate, latest rejection and unavailable evidence revoke | PASS | PASS | Status/wave/resume/verify positives and missing corroboration/later rejection; malformed log, newer valid recovery and missing explicit profile checks. |
| C06 Immutable mandatory QA obligations | PASS | PASS | Missing/retyped/downgraded/N-A/zero-execution negatives; independent multi-obligation omission blocks all four commands, exact restoration recovers. |
| C07 Explicit live P07 pin and required policy before/after | PASS | PASS | Missing pin, generation/policy/target drift and no-rebind checks; independent post-QA drift is caught by the final live check. |
| C08 Fresh non-clearing P09 and ordinary no-domain QA | PASS | PASS within stated limits | Actual domain positive, missing start/result and failed observation after reprepare all pass on the shorter private fixture path; QA equality and non-clearing fields retained. Original long-path failures are disclosed below. |
| C09 Verification-only and mechanical/manual distinctions | PASS | PASS | Actual unchanged product purpose; changed verification-only result blocks; explicit mechanical opt-out and separately supplied fixture corroboration remain distinct. |
| C10 Local/history never auto-upgraded | PASS | PASS | Inspect/check-complete remain non-clearing; missing shared evidence cannot close; valid historical P05 v1 artifacts remain rejected and byte-unchanged. |
| C11 Git attribution, fan-in, interruption and fallback | PASS | PASS | Fresh real-Git isolation/fan-in/conflict/recovery plus shared two-worktree integration/reprepare; native/sequenced/manual local observations and unrelated-commit reuse retained. |
| C12 Scope, preservation and meaningful entrypoints | PASS | PASS | Exact 17 paths; 968 baseline paths unchanged; all 76 destinations, 23 historical plan paths/17 Swarm artifacts and 69 roles preserved; shape and retained rich-brief behavior. |

### Leaf and stage mapping

| Authorized leaf/slice | SPEC disposition | QUALITY disposition |
|---|---|---|
| A22.1.a-d; A22.2 | Earlier historical acceptance preserved by exact ancestry/blob/mode evidence; not re-audited | Earlier decision retained; no new historical quality claim |
| A22.3.a-b | PASS, including F06 and retained physical/reviewer/reducer protection | PASS for owned caller/consumer integration |
| A22.4.a-b | PASS, retained short-leaf/package/Spec Kit and prerequisite behavior | PASS for released original API use |
| A21.1-4 | Earlier Brief Forge acceptance retained by unchanged source identity, not a fresh 15-case run | Earlier bounded quality decision retained |
| A22.5 | PASS, exact local/shared identities, no-change purpose and revocation | PASS |
| A22.6 | PASS, actual Git/recovery and useful non-clearing local capabilities | PASS |
| Released A22.7 shared-consumer unit | PASS, all C01-C12 | PASS with F08/P3 advisory |
| Final A22.7 parent, newer P08 selected-work/resume and other package parents | OPEN / outside this release | NOT ACCEPTED by this report |

## First eligible full bounded quality review

Reviewed the complete 17-path unit after SPEC passed: CLI/library call paths,
provider boundaries, error/fail-closed behavior, repeated metadata/profile checks,
selection edge cases, raw identity, retained test assertions, schema/templates,
documentation and report claims. No P1/P2 quality finding remains. The common
consumer and reused P03/P05/P07/P09 helpers avoid an alternate acceptance system;
local inspection is explicitly separated rather than silently weakening it.
No unbounded filesystem experiment or unrelated provider audit was undertaken.

### F08 - P3 advisory: review-skill preflight accepts unusable P05 names

**Locations:** `lib/swarm_contract.py:396-398` and
`lib/swarm-schema.json:126`. **Confidence: 10/10.**

The new pointer uses the generic alphanumeric `IDENTIFIER` preflight, and its
JSON schema has a different, still wider rule than accepted P05's
`^[a-z][a-z0-9-]*$` skill contract (`lib/review-schema.json:12`).
Actual `li-swarm.py validate` returned exit 0, `ok: true`, no diagnostics for
`1-review`, `Review` and `review_skill`. The real P05 shape validator rejected
each corresponding otherwise-valid prepared decision with
`review.skill: invalid string`; the original `review` decision passed.
The leading-digit case also contradicts P04's own schema.

This is a nonblocking preflight/diagnostic inconsistency: it admits a
configuration that cannot obtain matching P05 evidence, not false shared
clearance. **Recommendation to the original P04 owner through the coordinator:**
align the P04 declaration/admission rule with the accepted P05 skill contract
and add a focused negative test; do not change the provider or introduce a
third grammar. No fix or provider modification was made here.

## Checks actually executed

All product imports, comparisons and subprocesses used the verified private
runner and frozen source, not the ambient environment. Tests were loaded
without changing product code or assertions.

| Reviewer execution | Actual outcome |
|---|---|
| Original independent repair probes | 3/3 PASS, 43.613 s; six F06 physical aliases and actual F07 provider-positive contrast are subcases, not inflated counts |
| Supplementary QA/raw/v1/profile probes | 4/4 PASS, 100.366 s |
| `tests/unit/swarm-contract.py` | 57/57 PASS, 149.822 s; all 56 earlier method names retained, no unittest skips |
| Complete `tests/integration/swarm-shared-binding.py` attempt | 28 executed, **26 PASS, 1 FAIL, 1 ERROR**, 480.528 s; the two domain fixtures hit the private path boundary described below |
| Same two unchanged domain tests, shorter private temp root | 2/2 PASS, 65.560 s; no skips or product/assertion changes |
| `tests/integration/swarm-workflow.py` | PASS, 46.000 s; genuine Git isolation, interruption, serial/manual observations, conflict quarantine and ancestry-preserving fan-in |
| `tests/shape/swarm-contract.sh` | PASS; structural wiring evidence only |
| F08 quality probe | Three actual CLI/P05 contrasts confirmed in one bounded probe |
| Six changed Python files, Python 3.9 AST grammar | PASS grammar only; actual interpreter Python 3.11.9 |
| Immutable refs, scope, preserved blobs/modes/report prefix, whitespace and post-run clean snapshot | PASS |

The independent process used Git 2.55.0.windows.3. Builder 28/57/15 and committed
10+4 results remain builder evidence; they are not added to these counts.
There was **no single uninterrupted passing 28/28 reviewer run**.

### Domain fixture boundary and limitations

The original failed cases were
`test_actual_domain_data_is_reverified_and_never_becomes_review` at
`tests/integration/swarm-shared-binding.py:435`, and
`test_missing_domain_start_or_result_and_failed_observation_block_even_after_reprepare`
at `:447`. A finite unchanged-product diagnostic confirmed `start.json` at
259 characters was visible, while `result.json` at 260 characters existed and
was readable through the accepted native I/O path but not ordinary pathlib.
The P05 snapshot consequently lacked that result entry and the consumer
correctly blocked it; the other test failed at ordinary `read_bytes()`.

Only private TEMP/TMP/TMPDIR suffix `temp` was shortened to `t`, with the same
verified synthetic boundaries. Both original tests then passed unchanged.
This is a demonstrated fixture/host path boundary involving unchanged provider
discovery, not a repaired P04 defect or proof of arbitrary-length path support.
The initial failures and native/ordinary observations remain in private evidence.
No provider, OS setting or product test was repaired to obtain a pass.

The runner cleared inherited environment and verified synthetic HOME,
USERPROFILE/HOMEDRIVE/HOMEPATH, AppData/XDG/temp/Lintel and derived paths,
allowlisted PATH/PATHEXT and Git ceilings. It disabled system/global Git config,
prompts, credentials, signing, hooks and remote protocols, and rehashed the
approved jq. Python used `-B -S`; child calls were checked too. Existing runner
adaptations enforce subprocess isolation and bounded Windows fixture cleanup.
No target-supplied command, dependency installation, private-home read,
global activation, nested agent, network/auth or production operation occurred.

No full repository suite, actual Python 3.9 runtime, POSIX executable-bit host,
installed-client/model matrix, real people corroboration or race-proof
filesystem authorization was established. The post-QA drift probe is
deterministic injection after a real verifier, not proof of a live race.
Enterprise/compliance activation and final joined acceptance were not selected.

## Preservation and delivery

Accepted providers/helpers and their hashes are unchanged; so are the 76
preservation destinations, all historical evidence and 69 roles. Prior reviewer
heads `4bf5315`, `6bf86c3`, `3b2ab123`, `279dfc9` and `63cf1bb` remain clean and
unchanged, as does source branch `275a3544`. No builder/master/source branch,
shared plan, memory, reducer or historical report was edited.

Private reproducer sources, exact command/environments, failed and successful
observations and preservation checks are indexed in
`.claude/runtime/p04-shared-final-private/evidence-index.json` in the reviewer
workspace. The index records 52 evidence files; SHA-256:
`70a520b8bc702a7e8fafbf507dcb799a31f64a6872ee61d8862d351309851556`.
It also binds the previous private evidence index. This is review evidence,
not a P05 decision record or actor corroboration.

Commit this report alone atop `fcfed13`, naming product `4f10837` so the
report-only child is not mistaken for a product revision. The coordinator may
retain this bounded acceptance and F08 advisory. Final P08 selected-work/resume
binding, coordinator outputs, joined enterprise/hybrid/Swarm review and A22.7
remain separate open gates.
