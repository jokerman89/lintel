# A23.5 final review: pre-freeze delta check (Phase 1b)

**Reviewer:** session `8df81ac8-0043-4564-97bd-e2ed12c94b24` ("A23.5 final review"), for coordinator88.
**Date:** 2026-09-24. **Status:** interim. It checks the commits made since Phase 1 in response to
the Phase 1 notes, so that the P3 items below can land before the freeze. It is not the A23.5
verdict, which Phase 2 gives on the frozen head.

## Scope

`0d802cb4241fb9f7cb740e09caed57c480c20519..9c6f65a4f31808de44288328ccfe2def10eb1c10`, nine commits:
`4748838d` and `b7e296f4` (memory records only), `b3620e5c`, `917d7125`, `89131723`, `6d12fd91`,
`3ea42d70`, `322452e5` and `9c6f65a4`. Every product path in the range is changed only by the
commit named for it below. The environment and method are those of Phase 1, plus a `python3`
wrapper on the synthetic PATH, as CI provides. There was no repository mutation, and the shared
`/tmp` mount stayed at `p13v\t`.

Since then, and by the time this file was sent: the delivery branch is at `37fc477b`, which adds
only a P13 card record (the F-INT-6 fix intake); `origin/main` is at `c61d8e72` (PR #90), whose 14
changed paths are all under `presentations/`. Neither changes any conclusion below.

## Verdicts

| Commit | Change | Result |
|---|---|---|
| `9c6f65a4` | Preserves the Phase 1 notes as `reviews/A23.5-phase1-0d802cb.md` | PASS: the blob is byte-equal to the delivered file (SHA-256 `98919898af816c6449cbdb19b1742e26c6830588272c55477df4a2f4512b858d`, 35,060 bytes) |
| `917d7125` | Runner: windows-only unittest skips count as N/A off Windows; runner contract; README | PASS, with the P3 advisory A5-18 |
| `89131723` | The 22 canonical skip reasons | PASS |
| `b3620e5c` | Windows `TEMP`/`TMP` set to `RUNNER_TEMP`; ADR-0032 note | PASS |
| `6d12fd91` | Tier steps split, later checks run with `!cancelled()`; ADR-0032 consequences | PASS, with the P3 finding A5-15 |
| `3ea42d70` | CHANGELOG corrections | PASS: A5-09, A5-10 and A5-11 are resolved |
| `322452e5` | `final.md` and P13 card corrections | PASS for A5-02 to A5-05, A5-07 and A5-14, with the P3 findings A5-16 and A5-17 |

## Evidence

**Runner, `917d7125`** (`tests/runner/run-all.sh:66-71,120-139,169-171`).

- The runner contract at the new head passes in the synthetic environment: exit 0, five PASS
  lines, and no "python3 absent" skip (`contract-green-9c6f65a4`, 59.8 s).
- The same contract with the old runner (`0d802cb4`, blob `7566e50c`) fails exactly as stated:
  `FAIL: host Linux --require-all: expected rc 0 with [N/A: 2 windows-only unittest assertions],
  got rc 1` (`contract-red-oldrunner`, exit 1). That is an independent RED.
- Real unittest output (Python 3.11.9) rather than echo fixtures: five entries use the 22 actual
  reason strings, extracted by AST from the new head. Hosts are simulated through an exported
  `uname`, as in the contract.

  | Entry | Linux, Darwin | MINGW64_NT |
  |---|---|---|
  | `unittest.main(verbosity=2)`: one running test with a docstring that prints `noise ... skipped`, plus 22 skips, one with a docstring | `N/A: 22`, passes | partial |
  | the same through an explicit `TextTestRunner(verbosity=2)` | `N/A: 22`, passes | partial |
  | `unittest.main()`, verbosity 1, so no reasons are printed | partial | partial |
  | one canonical reason plus one containing an apostrophe | partial | partial |
  | `SkipTest` raised in `setUpClass`, plus one running test | skipped | skipped |

  Every refusal is fail-closed, and strict mode exits 1 wherever any partial or skipped entry
  remains. The five real entries print at verbosity 2, and none of the 22 reasons contains a
  quote character, so on POSIX all five become N/A.
- Accounting: an N/A entry counts as passed only when the canonical count equals `OK (skipped=N)`
  and `Ran` exceeds `N`. It is reported per entry and in the summary. The rule leaves shell
  `SKIP:` lines unchanged. On a Windows host every canonical skip stays partial, so the Windows
  jobs must run those methods.

**Skip reasons, `89131723`.** The AST check (`tools/ast_reasons.py`) finds all 22 reasons equal
to `"platform: windows-only; "` followed by the old reason, and every file's AST identical once
that prefix is removed (context-safety 4, profile-path-identity 14, snapshot-ownership 2,
review_evidence 1, universal-profile-context 1). Windows behavior is therefore unchanged. The
only other skips in `tests/**/*.py` are the six conditional `skipTest` calls for symlinks,
directory aliases and case sensitivity (`copilot-kit.py:1733`, `domain-result-handoff.py:769`,
`adapter-navigation.py:429`, `profile-path-identity.py:403,424`, `swarm-contract.py:188`). No
shell test skips by platform.

**Windows temp root, `b3620e5c`.** The pwsh step appends `LINTEL_POWERSHELL`, `TEMP` and `TMP`
to `GITHUB_ENV` in the same way as before, and only on Windows. `TEMP` and `TMP` are not
protected `GITHUB_*` or `RUNNER_*` names. The path budgets of `domain-installed-consumers` apply
only when `os.name == "nt"` (`domain-installed-consumers.py:237,274,305`); its `assertLess(...,
100)` at `:121` applies everywhere and fits short POSIX roots.
`catalog-installed.py:101-110` applies its 220-character worst case on every platform. At this
head the longest tracked path is 108 characters, a presentations file from `main`. On macOS the
resolved temp root is about `/private/var/folders/<2>/<30>/T/p13i-XXXXXXXX/1`, 72 characters, so
the guard measures about 72 + 27 + 108 = 207 < 220. That is an estimate with a margin of about
13, not a run. After the F-INT-6 fix only bundled files count, and the longest of those is 68
characters.

**Steps, `6d12fd91`** (`ci.yml:71-105`). PyYAML 6.0.3 parses the workflow. An evaluation of each
`if:` over the 21 jobs gives: structure verification, the tier steps and the four drift checks
run exactly once per system; the Bash 3.2 install runs only on macOS `other`; the PowerShell
check runs only on Windows `other`. After an earlier failure, the e2e and shape tiers, both
install checks and all four drift checks still run. The ADR-0032 consequences now record the
N/A rule and the trade-offs (a) to (d) of A5-13.

## Findings

| ID | Sev | Location | Finding |
|---|---|---|---|
| A5-15 | P3 | `.github/workflows/ci.yml:77-79` | The behavior-tier step keeps the default `success()` condition, so a failure of `install/verify.sh --all` (`:71-73`) still hides the behavior tier. Adding `${{ !cancelled() && matrix.part == 'other' }}` there completes A5-12. |
| A5-16 | P3 | `reports/final.md` A14 and A15 table rows | The rows still give only the TLS reason (A14) and the denied routes (A15). The corrected bullets add V1 and the missing Visio writer; the rows should agree with them. |
| A5-17 | P3 | `reports/final.md`, "Earlier coordinator product commits" | "that no package review names" is not literal: reviews list `935b640c` and `aa56a672` as non-authored dependencies (`reviews/P08-partial.md:56-57`, `reviews/P10-component.md:42-43`), list `0eab731c` without replaying it (`reviews/P07-long-path-final.md:30`), and assess `9f49e261`'s registry through P13's reviews. "that no package review covers as its own content" would be accurate. |
| A5-18 | P3, advisory | `tests/runner/run-all.sh:125`; `tests/README.md:20-24` | The N/A grep recognizes only a single-quoted reason. Python prints a reason that contains an apostrophe in double quotes, so such a canonical skip refuses on POSIX (verified fail-closed above). Either document "no single quote in the reason" or accept both quote forms. Optional hardening: a shape check that the canonical reason appears only in `skipUnless(os.name == "nt", ...)`. Misuse is already caught on Windows, where canonical skips stay partial. |

## Status of the Phase 1 findings

- **A5-01 resolved at the runner level.** On POSIX the five entries become N/A rather than
  partial. The lower-confidence Windows skips (symlinks, per-directory case sensitivity) remain,
  and the PR's CI decides them.
- **Resolved:** A5-02, A5-03, A5-04, A5-05, A5-07, A5-09, A5-10, A5-11, A5-12 (except A5-15),
  A5-13 and A5-14.
- **At the freeze:**
  - A5-06: the base, now `origin/main` `c61d8e72`.
  - A5-08: the CHANGELOG lines for A08 and A13.
  - The CHANGELOG statement "CI adds Linux and macOS with Python 3.12" becomes evidence only once
    the PR's CI passes; Phase 3 records it.

## Evidence index (SHA-256)

| File | SHA-256 |
|---|---|
| `files/tools/na_fixture.py` | `31c1bff2b627509b5d15941dc729fcdf262968d736e8f19c5666bbc169d5017e` |
| `files/tools/ast_reasons.py` | `847dbd30e5a6c76847e752282e27af56d6f63d22d2b5cda31c5dbb2fb37ce073` |
| `files/tools/ci_steps.py` | `2f9d2194a8fb09e2cab327d87e16714ee7933934d0dab991dcb283dbaf1f7928` |
| `files/tools/syn.ps1` (unchanged) | `e17d2a4a1321f435df6f616f70d85b943f3618480355edf80740afb0b04ce57f` |
| `runs/contract-green-9c6f65a4.out.txt` | `c84ed12f1d80c739108d81800f907fd76b00e7ec236bacdf07e7e7a7d99a29fd` |
| `runs/contract-red-oldrunner.out.txt` | `9a26ccc40aa0de97665db2cd2c13dcaebea31fcd1bd19f14515f0dfa7044df4a` |
| `runs/ast-reasons.out.txt` | `d4d84d6c23c6ddcf2fbdf5238e1f8f8004be19bf522374cb6615f0ec4d35ad77` |
| `runs/ci-steps-9c6f65a4.out.txt` | `3b14230cf07b1f45235697fa6d86d59caa97da94e65a8ef829567f6187a1242b` |
| `na/na-Linux.txt`, `na/na-Darwin.txt` | `842146576937d04431c368882149cdfb88f9abd0e686ab33373131e78420ce74` |
| `na/na-MINGW64_NT-10.0.txt` | `f123550a7243c4886689da1aeaa29b1465210f92bd7d04d2e1ab0ed0ece83013` |

`runs/` and `na/` are under `%LOCALAPPDATA%\Temp\a235\`. Every run's environment record contains
no `LINTEL_*` variable.

Limits: native Windows with Python 3.11.9 and Git Bash; Linux and macOS behavior is simulated
through `uname`, not observed, and Python 3.12 output is inferred from the unchanged unittest
status format. No product suite ran. Compliance stays UNVERIFIED without a resolved
required-policy source; that is not a finding.
