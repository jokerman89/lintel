# P04 selected-work and cold-resume join at 08879e9

**Date:** 2026-09-23. **Independent reviewer:**
`ed672f58-2e85-42e2-b1b2-0635ba5b2325`.
**Coordinator:** `88aecc43-40f9-41d4-8947-6c2fb0a55481`.

**Selected final-join SPEC: PASS. Subsequent bounded QUALITY: PASS.**
Open findings: **P1: 0; P2: 0; P3: 0**. F08 is closed; F06/F07 remain closed.
Accept the exact released source join for coordinator integration. This is **not
self-closure of A22.7**, whole P08/Universal, installed-client, native-actor,
enterprise-policy or live-domain acceptance.

The reviewer did not implement or repair the product. Only this report and private
review evidence were authored. SPEC was recorded before QUALITY; no new reviewer,
remote/auth call, dependency, hook/global activation or jq operation was used.

## Immutable review boundary

| Role | Exact identity |
|---|---|
| Product | `08879e9e3deaa61f7f47282c1068226814e3dd25` |
| Sole product parent / diff base | `8e0ac4f52eaa9956cd75dcc1442635d95f158d06` |
| Product tree | `9e01162f94d750ba5a77377a0f4ff78776e08da0` |
| Frozen report-only checkout / this report's parent | `7a453b0f18a325d7d81adbebf21576bda4392178` |
| Final-join authority | `74308387fbc5c662c6abaec16d07e68936bf17a8` |
| Read-only diagnostic authority | `27b43e3066160f9b555a10b267edeafd43bc97bd` |
| Accepted named-ID provider | `aa5cf0e408604dbe8f5b47df8b453622a0db5f13` |
| Earlier accepted shared product / independent report | `4f108374a712818733f95df0b78b7c42e619444c` / `bb939e55b478e363b3d886781b026e64cc0822bd` |

The pinned checkout was clean and detached. Parent `8e0ac4f` has parents
`dcba002b91ea226d0bf4697d7e0f9688ae86e5bc` and `aa5cf0e`; its tree exactly
equals the accepted provider tree. `7a453b0` changes only the builder report.
The 1,030-line report retains the complete 736-line predecessor prefix; Git-byte
SHA-256 is `3e8ec82fe1f68995dcd7148e9c1b3f2b8c790ce080ccf29f445b5f340232469c`.

The entire reviewed owner delta is exactly eight paths: `bin/li-swarm.py`,
`lib/swarm_contract.py`, `lib/swarm-schema.json`, `skills/swarm/SKILL.md`,
`docs/concepts/swarming-work.md`, `tests/integration/swarm-shared-binding.py`,
`tests/shape/swarm-contract.sh`, and the P04 builder report. Incoming unrelated
work was not re-audited. The owner did not alter `swarm_evidence.py` or shared
P03/P05/P07/P08/P09 providers.

Read the complete accepted P08 named-ID report from its separate `78e3c09`
review history and verified SHA-256
`1cf94a0309da2dab1340c16125a79ae671db3b8f27cdc945e9e3c9a7b7739727`.
The current reader is exactly blob `281cd8f4ce6c5652b3739fd8c54d474b79a73421`,
SHA-256 `0b39ef9f3519c14ee7d33d91ce9d11741ee2f75721a51f5a6e5647fa24b686b0`.
Separate `78e3c09`/`e98e7da8` review ancestry was not imported as a product fix.

## Stage 1: released final-join specification

Read the final P04 card, specification/interfaces, relevant accepted decisions,
complete owned delta, actual provider/caller implementations and discriminating
tests. The following are review controls, not a new task ledger.

| Control | SPEC | Subsequent QUALITY | Source and observed evidence |
|---|---|---|---|
| J01 Trusted provider composition, no duplicate parser/backlog | PASS | PASS | `bin/li-swarm.py:75-88,219-228`; actual `work_context`, original P04 package API, CLI-only composition. Target reader/workflow decoys never execute. |
| J02 Explicit map and coordination identity | PASS | PASS | CLI `:78-88`; all four commands match the actual provider; existing wrong map/backpointer and two-initiative decoys refuse. No ambient map or recency selection. |
| J03 Original native/grouped/tree/Spec Kit/named IDs | PASS | PASS | Original plan/tasks alias, numeric/tree leaves, T011/T027 and named `core` retain their actual provider/package identities. Source progress remains non-clearing; actual P05 binding survives progress but not acceptance drift. |
| J04 Bounded selected inputs and explicit failure | PASS | PASS | CLI `:80,225-228`; actual provider refuses missing/empty/over-bound selection before the shared gate, without choosing the decoy. Exact provider file/byte-bound evidence is retained from its unchanged accepted review. |
| J05 F08 canonical grammar | PASS | PASS | `lib/swarm_contract.py:396-400`; `lib/swarm-schema.json:126`. Actual P05 validator and canonical schema reference, not copied regex/length rules. Invalid names refuse; a 103-character valid name passes real logging and all four consumers. |
| J06 Explicit cold cycle and faithful metadata | PASS | PASS | CLI `:104-149,229-246`; actual original cycle wins over newer decoy. Phase, operation, map/artifacts/profile/policy equal direct provider output. Missing/wrong/injection-shaped IDs refuse with true exits. |
| J07 Profile, required policy and state continuity | PASS | PASS | CLI `:144-149,237-248`; lost/drifted pin, generation/policy mismatch and another target refuse; another valid pin cannot replace the cycle. Explicit state and nondefault existing profile file work without fallback or writes. |
| J08 Read-only diagnostics, not a durable receipt | PASS | PASS | CLI `:92-101,129-138`; all arguments are escaped to visible stderr, adapter is not exported, no audit directory/bootstrap occurs, provider exit 2 remains 2. Independent missing/unverified mandatory-receipt controls still block despite valid recovery. |
| J09 Same latest-P05/QA/live-P07/P09 gate | PASS | PASS | CLI `:248-258`, unchanged `swarm_evidence.py:88-219`; later rejection, immutable QA failures, missing and freshly re-prepared unknown domain results block. Domain and recovery data remain non-clearing. |
| J10 Honest artifact-only/native/serial/manual recovery | PASS | PASS | CLI `:230,248-249`; no cycle flag means artifact-only, not persisted recovery. Ambient cycle ignored; native/sequenced/none candidates remain useful, independent corroboration remains required where applicable. |
| J11 Result and recovery preservation | PASS | PASS | Verification-only work needs no fictitious edit; truncated DONE remains BUILD despite complete lane evidence. Actual Git fan-in requires fresh shared preparation; conflict/recovery evidence from the unchanged earlier workflow stays attributed. |
| J12 Ownership, history and freeze | PASS | PASS | Six physical alias public-gate cases, repeated metadata checks and parent/future-file selection pass. All 28 earlier shared method ASTs, history and out-of-scope source remain preserved. |

### Earlier findings and original leaves

F08/P3 is **CLOSED**, including the original `1-review`, `Review`,
`review_skill` counterexamples and the long-name positive. F06/P1 and F07/P2
remain **CLOSED**, with fresh selected ownership/containing-parent/consumption
checks. F01-F05 and A21's earlier Brief Forge acceptance remain historical
accepted evidence, not new fixes or freshly rerun suites.

A22.1/A22.2 preservation is retained without repeating the historical merge
audit. A22.3-A22.6 behavior remains supported by the named retained checks and
exact earlier evidence. The **released A22.7 final-consumer source join** passes
this review; the coordinator still owns joined integration evidence, generated
outputs and the final A22.7/parent decision.

## Stage 2: bounded quality

QUALITY began only after private `spec-stage.json` recorded PASS. Reviewed all
eight changed paths for correctness, error handling, source/data trust, process
isolation, maintainability, test meaning and documentation consistency. No
actionable P1/P2/P3 finding was found in this boundary.

The work reader remains outside libraries that it calls back into. It supplies
data rather than a second acceptance identity. Resume's fixed quoted argument
vector removes ambient selectors/startup overrides and uses trusted provider
code; errors do not become a new cycle or artifact-only success. The saved live
profile constrains the subsequent unchanged shared gate. F08 delegates validation
and schema ownership to P05. Documentation distinguishes observed metadata from
permission, persisted audit, live specialist work and client acceptance.

A further actual nondefault-profile-file probe removed the default pin, used an
existing selected file containing the real P07 record, and compared cold CLI
recovery to the direct trusted provider. It passed with unchanged filesystem
state. This proves the explicit supported profile-file route, not every possible
ambient or custom runtime arrangement.

## Actual reviewer executions

Every product import, direct comparison and subprocess ran inside the verified
allowlisted synthetic environment. Test code/assertions and product files were
not patched. All listed product-test runs exited 0 with no unittest skips.

| Reviewer run | Actual result |
|---|---|
| All 19 added `SharedBinding` join methods | **19/19 PASS**, 460.222 s; exact selectors in private runner/log |
| Six retained shared cases | **6/6 PASS**, 150.701 s: six physical aliases at both public gates; containing parent/future file; consumption-time metadata; real Git fan-in/reprepare; immutable QA; substantive/manual versus explicit mechanical review |
| Three independent seam probes | **3/3 PASS**, 92.265 s: required profile plus mandatory receipt refusal; exact callback escaping/non-export/no-write; absent state and newly prepared unknown domain refusal |
| Subsequent custom-profile QUALITY probe | **1/1 PASS**, 29.234 s; actual default pin absent, explicit nondefault file retained |
| Swarm shape plus original work-artifact wrapper | PASS, 1.406 s; structural evidence and original unsafe/missing-map/no-ledger checks |
| Actual Git discovery boundary | Uninitialized nested fixture exits **128** with no selected root; initialized fixture exits **0** and selects its own root |
| Immutable Git/AST/whitespace checks | PASS; modified Python files also pass Python 3.9 grammar on actual Python 3.11.9 |

Builder **47/47**, provider **25/25** and committed **6/6** remain builder
executions, not reviewer totals. The accepted P08 provider review and earlier
reviewer local **57**, real-Git workflow and Brief Forge evidence retain their
exact original attribution. This reviewer did not rerun those full suites.
The old 46/47 pre-provider run, earlier fixture/shape failures, previous rejected
checkpoints and prior 260-character domain-path failure remain historical facts.

A private evidence collector initially assumed LF stdout and exited 1 on valid
CRLF test logs. Its parser was corrected to use line boundaries, then sealing
passed. The failed collector log remains indexed; no product/test change or
test rerun was used to turn it green.

## Preservation, isolation and limits

Git-object comparisons verify **1,016 outside paths unchanged** from the exact
join base; all **76 preservation destinations** still exist with retained
mode/type; **23 historical plan paths / 17 Swarm artifacts** are byte/mode
identical to the earlier accepted product. All **69 roles** and **18 named shared
provider/helper files** are unchanged by this owner. The prior **28 shared method
ASTs** are identical; local-57, original Git workflow and named Brief Forge source
files retain their earlier blobs. No new historic outcomes were fabricated.

The launcher clears inherited environment and verifies synthetic HOME/USERPROFILE,
HOMEDRIVE/HOMEPATH, AppData, XDG, temp/Lintel/derived paths, PATH/PATHEXT and Git
ceilings. Git system/global config, credentials, hooks, signing and remote
protocols are disabled per process. Reviewer Python entrypoints use `-B -S`;
descendants retain the isolated environment, with user-site loading disabled.
Intentional hostile source-root fixture inputs remain intact rather than
being sanitized into a false positive. Only process isolation and bounded native
temporary-directory cleanup are adapted. All temporary test fixtures were cleaned.

The shorter review-owned fixture root is not a changed product default or a
general Windows/Git path guarantee. No global/native-client/model/DB operation,
full repository suite, actual Python 3.9 runtime or race-proof filesystem
authorization was established. The required neutral pack and typed receipt/domain
records are synthetic: they test refusal and composition, not real enterprise
controls, human independence, durable production audit or live specialist action.
Stderr cannot satisfy a declared mandatory durable-receipt control.

## Durable evidence and handoff

Private source/command/environment/exit observations are sealed in reviewer
`.claude/runtime/p04-088-private/evidence-index.json` (35 indexed files), SHA-256
`9a881cae50ed8e2da80f9da6dded37002859ad3d63d6c8090027ec02056606e7`.
It binds previous evidence index
`70a520b8bc702a7e8fafbf507dcb799a31f64a6872ee61d8862d351309851556`.
These are review observations, not P05 decisions or actor corroboration.

Commit this report alone atop `7a453b0`, naming reviewed product `08879e9`.
Prior reviewer refs `4bf5315`, `6bf86c3`, `3b2ab123`, `279dfc9`, `63cf1bb`,
`bb939e55` and original Swarming source `275a3544` remain unchanged. No
builder/master/source branch, shared ledger, memory or generated output was edited.
The coordinator may integrate this source acceptance and decide the remaining
A22.7 join; this report does not complete that parent or authorize publication.
