# A14.5 framework app independent artifact review, phase 2

**Disposition: app SPEC PASS, 10 of 10 required checks, including the restore boundary and V1,
which I observed directly. Whole app QUALITY PASS: P0 0, P1 0, P2 0, P3 1.** The P3 is the same
cosmetic heading wrap as static Q1, from shared source. Together with my Phase 1 static review
(`0602056a30228070bcc34dafbdd7828b45fe1e4a2fb1f6e134846c27388266ee`, preserved byte-exact at
`562f0386`), V1 is now independently observed for both artifacts, and every one of the 17 P05
controls has independent support. This clears the independent artifact gate for both halves. The
coordinator owns the A14.5 disposition; no P11 or parent-initiative acceptance is claimed.

## Handoff, identity and independence

| Item | Evidence |
|---|---|
| Handoff | From P11 owner `f413bdcb` at 2026-09-24T19:57:52+02:00, sent in parallel to coordinator `88aecc43-40f9-41d4-8947-6c2fb0a55481`. Same reviewer session as Phase 1 (`aba328fd-4d55-49fb-96fe-777e09849511`), where the model record and the blind pixel test were confirmed. No role in P11. |
| Report commit | `9919e23692b16d4886fe2e2afe159541a9c19152`, tree `8e8a8f1f67609522651bb3d73bc949c4fac1617d`, sole parent `d7139b5e99d189e71fb14d90fd8b80c8272efbcc`. Head of the owner branch; `Co-authored-by` trailer present. |
| Report change | Only `.claude/plans/universal-implementation/reports/P11.md`, +165/-0. Blob `ee6ea993336bbd3fab7f0b45ce50bc64b11c39b5`, Git-byte SHA-256 `182e7264fd598f01360ef226bc4772face1bece9a411011a4232cebd083e1db7`, 1561 lines, LF only. The parent's 1396-line blob `57abdbe21a41866fc5ee48c77862823a8ddd00a4` is a byte-identical prefix. I read lines 1397-1561 in full. |
| Authority | `packages/P11.md` at `02edb55685c4e699ba73cac6af9ebbc16b54417e`: the release at line 412, the 19:36 clarification at 451 and the 19:47 redirect decision at 465. Recovery head `8f418e684d2cebe4ed2cde565c1e1ede22afed4a` adds one later P11.md change (`f3db688f`): it records my Phase 1 result and asks me to review the app half, V1 included. |

## Secret handling

- I never opened, copied, hashed or printed anything under `npm-cache` (194 files). That covers
  the eight private debug logs, which the checkpoint lists by path only; one of them is flagged
  query-bearing.
- Every other file was scanned for SAS-like markers, counting only, before I viewed it. Across
  the 105 copied files the scan found no delivery URL.
  - The only query-bearing URLs are Vite's public `funding` link (parameter name `sponsor`) in
    both lock records. The same link is the only one in the hidden `node_modules\.package-lock.json`,
    which I read in place.
  - The CDP schema `provider-api.json` holds a source.chromium.org search link (parameters `ss`,
    `q`) and is byte-identical to the single-03 copy.
- This review contains no query value, signature or signed path.

## Exact target

| Identity | Value |
|---|---|
| Frozen attempt `R2` | Owner worktree `jokerman-microsoft-crispy-bassoon\.claude\runtime\p11-a145\framework-04`. I copied 105 files privately. Every copy equals its original, and every original was stable across its copy. After the review all 105 re-hash unchanged, and so do the 69 single-03 static files. |
| Excluded from the copy | `npm-cache`; `app\node_modules` (356 files, read in place); `tmp` (580 V8 compile-cache files). `home`, `npm-prefix` and `audit` contain 0 files. |
| Checkpoint | `evidence\framework-app-checkpoint.json` `db4cd44f3b17bd39e34a36788164f092b6999d27cef23a2778780a656d57e085`. All 103 `inputs_sha256` bindings match. The 12 outer command/log pairs under `p11-a16` match by hash (24/24, contents not opened). |
| Source seals | All 44 equal the owner's working-tree bytes. Against Git, 3 equal the raw `d7139b5e` blob and 41 equal it after LF-to-CRLF conversion, so the seals hash Windows-checkout bytes of unchanged source. They are identical to the single-03 seals, 44/44. |
| Handoff identities | All 13 match: checkpoint; observations `7ed522d7f8534d84ecfcd18f1eb12cc31ac7478bb6a2e125673fcac70d93baeb`; `v1-observer-inputs.json` `4c5ad45f86aadf6836e3281321b676e347310c2d27346afc4d6139eb6a2c3d1e`; feed audit `b129d55572e8be1a59d695775a7a36de2efcecb60db3c2ee2621800b44f42d02`; lockfile `1bbac3405e7fe162ba2e32c8488bee9322bb84167aef2c4fc149b1b9a428f543`; `package.json` `9e58150ce6e701d3cae517275684361af44e364c1908fc6119b7ea7b8f24f298`; `dist\index.html` `01fa7f5d81f21814be7b4e17b14b5b585a1bdf40bce641a8a695f7fd605c0bd6`; JS `c22b59282828d69233a833778c38e22ff63b92fe924ca1d1648c14c592f72f29`; CSS `f562f604b24e5af58dd6ccd7b45c08428da9c75bd1244893e89edf0afef300f1`; three full images and the contact sheet (below). |
| V1 input file | `v1-observer-inputs.json` binds exactly the static identities I verified in Phase 1 (`9d76ab47...`, `fe6274b1...`, `9a757d7f...`, `1b9a13d1...`, `007188fb...`), together with the app identities below. |

App images, each hash-verified before any view. The full images are recorded in `reports/P11.md`
lines 1508-1510 and in the checkpoint. The sheet, focused images and negative are recorded in the
checkpoint, and the sheet also in `v1-observer-inputs.json`. All are RGB 8-bit PNG.

| Image under `browser\browse-HZL4jt` | SHA-256 | Size |
|---|---|---|
| `app-1440-full.png` | `b2f9fab6cb923f94dc6dd4a7e4764851b4c61d7b4820fc67ce03dcdc3b3efad4` | 1440x995 |
| `app-768-full.png` | `42e7aef743f023e23ec9e45844a87c9dca00c47eb75e4d42296e13ac9ea045a2` | 768x1024 |
| `app-390-full.png` | `657453dc6b52d7a029e8a48f899abd8dd51297a2af5f7bde2fc4572255a6a95f` | 390x1445 |
| `app-1440-focused.png` | `bb7ca96747e7351be056097f191a5e2a8f121196b33e9ddcb2e05129e7806fb0` | 1440x900 |
| `app-768-focused.png` | `51bd3112c4b70879cc4018decbdb988a8a22be5e494fe82c92bb129e6742b2d2` | 768x1024 |
| `app-390-focused.png` | `adca9748ccb0f0e7b212f646f9787354fa9553ae8168f3206ef0fe5a99f012df` (the same bytes as static `single-390-focused.png`, see O2) | 390x844 |
| `app-negative.png` | `93236e43b2b05d0210d269b686f4e6f2014ce3dd1373b004e14c8ed8d9ceef99` (the same bytes as static `single-negative.png`) | 390x844 |
| `evidence\app-three-viewports.png` | `6050a3044dd59e53da3a3a70ed6317e829f7588db50851550fc8942cb8325d03` | 2662x1517 |

## Restore boundary (P11.md 412-489)

| Requirement | Evidence checked |
|---|---|
| Declared manifest | `package.json`: private; `react` 19.3.0 and `react-dom` 19.3.0 as exact dependencies, `vite` 8.3.0 as an exact dev dependency. Build script `node ./node_modules/vite/bin/vite.js build --base=./`. |
| Synthetic npm config, only the proxy | I read key names only. `user.npmrc` is 52 bytes with the single key `registry=https://packagefeedproxy.microsoft.io/npm/`; `global.npmrc` is 0 bytes; `npm-cache` is its own directory. In all 15 inner command records, `npm_config_userconfig`, `_globalconfig`, `_cache` and `_prefix` point to those owned paths. HOME, USERPROFILE, APPDATA, LOCALAPPDATA and TEMP are owned. No environment name is credential-like, and there is no other `npm_config_*`. `npm config get registry` returned the proxy on npm 11.13.0 with Node v24.16.0. |
| No global install | No `-g`/`--global` in any argv. `npm_config_prefix` points to the owned `npm-prefix`, which holds 0 files; the synthetic `home` holds 0 files. |
| No credential or TLS change | No auth, TLS or certificate key in the config or environment. All 16 server requests record `authorization_supplied` false. The deny proxy refused 24 browser background requests (22 CONNECT, 2 GET, all 403, none forwarded). |
| Restore commands | `install --package-lock-only --ignore-scripts --no-audit --no-fund`, 17:36:27-17:36:55Z, exit 0 ("up to date"). Then `ci --ignore-scripts --no-audit --no-fund`, 17:36:57-17:37:02Z, exit 0 ("added 18 packages"). The first stop is preserved in `restore-blocker.json`: `metadata-origin`, react, "Tarball does not name the approved feed", `retry` false. |
| Lockfile and versions | Lockfile v3 with 43 entries, all carrying integrity (19 sha512, 24 sha1). Every `resolved` is on `ms-feed-25.pkgs.visualstudio.com/1es-public/_packaging/npm-public/npm/registry/...` with no query, and no entry has an install script. The 18 installed packages match their locked versions; the hidden lockfile has the same 18 entries on the same host. The pre-install record's `packages` are identical to the final lock. |
| Hosts actually fetched | From the sanitized audit (host and status only): 98 GET requests, all 200. `packagefeedproxy.microsoft.io` served 80 (metadata); 17 `*.vsblob.vsassets.io` hosts served 18, the tarballs, query-redacted. The 18 CDN fetches equal the 18 installs, no fetch record contains `?` or `=`, and there was no 401 or 403. See O5. |
| Direct packages | The `react`, `react-dom` and `vite` `package.json` and LICENSE hashes equal `dependency-verification.json`. All three are licensed MIT. |

**Restore verdict: PASS.** The restore stayed within the release, the 19:36 clarification and the
19:47 decision.

## Same resolved profile and design contract

- The fixture sources `index.html`, `src.jsx`, `style.css` and `design-tokens.css` are
  byte-identical to the originally selected `a145-01\contrast-fix-02\artifacts\app` files. I
  verified the originals read-only: `9f230c49...`, `6e1be0fa...`, `596cb8d5...`, `a79a15ba...`.
- `design-tokens.css` and `style.css` equal the static page's two inline style blocks except for
  one boundary newline each.
- Diffing the static and app design specs finds exactly three differences:
  - `target_format`: `single-file` to `app`;
  - `binding.project.stack`: `html` to `vite-react`;
  - `binding.project.manifests`: empty to the verified `package.json` and lockfile hashes.

  Typography, motion, palette, layout, brief, retrieval, provenance, overrides, `profile_ref` (P07
  `sha256:85cb821b...c0119`) and `profile_asset` (`anthropic-default` `18505478...80c2b`) are
  identical. The app's `profile.json` carries the same P07 reference but omits the `asset` block,
  which the design spec binds.
- `src.jsx` renders the static markup with React state. The visible text is identical except the
  footer label, "React application specimen" in place of "Static HTML specimen"; the document title
  also differs.
- Build: Vite 8.3.0 transformed 16 modules; `build-result.json` records exit 0, unchanged source
  seals and no registry request during the build. The outputs are 510 B HTML, 4148 B CSS and
  222865 B JS, matching the served byte counts. The HTML references only `./assets/...`.
  - The built CSS keeps the ink, paper, neutral and focus-ring tokens, `text-wrap: balance`, the
    3px/3px `:focus-visible` outline, the border-only hover, `@media (width<=760px)`, the
    reduced-motion block and `min-height: 48px`. It has no `url(` or `@import`.
  - The built JS contains the component text. Its only hosts are `react.dev` (error-decoder
    strings) and `www.w3.org` (namespace URIs). The single `fetch(` is Vite's module-preload
    polyfill, which is inert here because the HTML has no `modulepreload` link.

**Same-profile verdict: PASS.**

## V1: direct observation of the app images

I viewed all seven app captures and the app contact sheet with `view`, each from its hash-verified
private copy. The descriptions are what I saw; afterwards I checked them against the records and the
pixels.

- **Desktop 1440x995, initial.** This is the Phase 1 desktop composition. The masthead reads "Lintel
  / local specimen" and "Synthetic data. Nothing is published." Below it are the two-line heavy sans
  heading of about 64px and the two-line serif summary. The two 544px columns follow:
  - left: the input "Synthetic release", the black "Add check +" button, "0 checks added" and
    "Current draft: Synthetic release";
  - right: three ruled entries and the warm-grey boundary panel.

  The footer reads "React application specimen" at left and "Same profile. Same design. Local
  only." at right. There is no scrollbar, clipping or overlap, and the text is crisp.
- **Tablet 768x1024, initial.** The same two-column tablet layout as the static capture, with the
  same wraps: "then add a / check...", "boundary / explicit." and "remote / page." The footer carries
  the React label, and the document fits the viewport with no scrollbar.
- **Narrow 390x1445, initial.** One column. The heading runs to three lines with "step." alone. The
  controls are full width, the checklist and panel stack below, and the footer stacks "React
  application specimen" over "Same profile. Same design. Local only." There is no horizontal
  clipping.
- **Focused 1440x900.** Scrolled 95px, with a 15px scrollbar. It shows "Synthetic keyboard check",
  "2 checks added" and the updated draft line. "Add check" has a clear near-black outline and paper
  gap, with a muted reddish-brown inner border from the hover still in flight (O1). The React footer
  is visible at the bottom.
- **Focused 768x1024.** The same post-action state. The ring is clear, and the inner border is a
  darker brown, still in flight.
- **Focused 390x844.** Scrolled 216px. The button has the near-black ring and gap with an ink
  border, and the post-action values show. The pixels are identical to the static capture.
- **Negative 390x844.** White page with the "Deliberate negative control" heading and paragraph,
  then the visibly faint mid-grey line "Low-contrast normal text: this is not an accepted design."
  The pixels are identical to the static capture.
- **Contact sheet.** The three full captures sit under the labels "App 1440px/768px/390px: exact
  retained full page". Each region equals its original pixel for pixel: 0 mismatches at (16,56),
  (1472,56) and (2256,56).

### Consistency with the records, and with the static artifact

| Check | Recorded | Observed in pixels |
|---|---|---|
| Actions and state | `results.json`: initial, pointer, focus and reduced states at each width; count 0, 1, 1, 2. Final DOM on `127.0.0.1:63891`: title "Synthetic React artifact", draft "Synthetic keyboard check", active `add-check`. Three console `info` "A14.5 React specimen ready" messages and no error. | The same visible strings. |
| Overflow and size | `scrollWidth` equals `clientWidth` (1425/768/375) in all 12 states; `scrollHeight` 995/1024/1445. | Zero non-paper pixels right of the CSS content box at every width, and the last 8 rows are empty. The content boxes are [143,33,1295,958], [31,33,735,953] and [19,33,369,1408], the same as static. |
| Focus geometry | The focus and reduced states record `add-check` with a solid 3px `rgb(20, 20, 19)` outline at a 3px offset, on buttons of 544x48, 320x48 and 335x48. | Rows 598/632/485 and column 400 show 3px of ink, 3px of paper, a 2px border, then ink, at every width. |
| Contrast | 96 samples, minimum 14.732440790112804 (boundary text on the panel, 768 pointer state); 0 samples below 4.5; focus ring 17.497722780575504. | From exact glyph-core pixels: 17.497722780575504 (heading and button), 14.732440790112804 (panel) and negative `#898989` on white 3.498154318627802. |
| Interaction diff, 768 | Input, pointer, Tab and Enter. | 5397 changed pixels in four regions: input suffix, button ring, count digit and draft suffix. This is identical to the static diff. |
| Static versus app | The same design contract. | Each full capture differs in one 1145-pixel region, the footer label ([136-311,944-959], [24-199,936-959], [16-191,1360-1375]). The 1440 and 768 focused captures differ only there and at the button border, where the hover is in flight. The 390 focused and negative captures: 0 pixels differ. |

**V1 verdict: PASS (confidence 9/10).** The app renders the selected content, palette, type roles
and layout grammar at 1440, 768 and 390, with no overflow, clipping or overlap. Text is readable,
and the focus and hover states are visible. The images agree with the recorded DOM and contrast,
and apart from the footer label they are pixel-identical to the static artifact.

## SPEC, before QUALITY

| Required app check | Verdict | Evidence checked in this review |
|---|---|---|
| Restore boundary | PASS | Restore table above. See O3 and O5. |
| `a145-app-build` | PASS | The real `npm run build` exited 0 with 16 modules. The outputs are hash-bound, were built from installed dependencies, and were served byte-for-byte. |
| `a145-app-browser` | PASS | Loopback React mount, controlled input, pointer count 1, Tab/Enter count 2, a final active `add-check` and no console error. Server `/health` returned 200 on both servers before the browser started (`server-start.json`, `before_browser` true). |
| `a145-app-contrast` | PASS, measured scope | 96 samples with a minimum of 14.732440790112804, equal to the pixel-derived values. The negative's 3.498154318627802 stays P05 `effective_status` `fail` with `blocked` true, despite advisory 100 (`negative-control.json`). |
| `framework04-local-resources` | PASS | 28 network entries, all admitted loopback on ports 63891 and 63892. Server requests: index 510 B, JS 222865 B, CSS 4148 B and a 0 B favicon (204), three times each; the negative page is 661 B. The bundle has no remote fetch path, and the CSS has no `url(`. |
| `framework04-keyboard-focus` | PASS, measured scope | The 3px outline at a 3px offset is pixel-exact, at 17.5:1 against paper; all buttons are at least 48px tall. |
| `framework04-responsive` | PASS, emulated | No horizontal overflow in any state or pixel; the layout switches at 760px as designed. |
| `framework04-reduced-motion` | PASS | In the reduced state `reducedMotion` is true, `transitionDuration` is `0s` and there is no animation; `add-check` is `0.15s` before emulation. See O1. |
| `framework04-same-design-profile` | PASS | Same-profile section above. |
| `framework04-v1-independent-images` | PASS | App images: this review. Static images: Phase 1 (`0602056a...`). |

**App SPEC: PASS, 10 of 10.** In `qa-result.json`, 16 of 17 controls pass, and the only
`unverified` blocker is `framework04-v1-independent-images`. This review and Phase 1 supply that
observation; the record's owner updates it. I did not change any record.

## QUALITY: whole app artifact

**Q1 - P3 - shared with static Q1: the narrow display heading leaves "step." alone on its last line
(confidence 8/10; cause not verified).** `app-390-full.png` shows the same three-line wrap. The
source is identical: `src.jsx` keeps the forced `<br />`, and the page CSS keeps `text-wrap:
balance`. It is cosmetic and not blocking; one fix in the shared source would clear both artifacts.

There is no P0, P1 or P2 defect. The strengths:

- Idiomatic React state with a functional count update, a controlled input and the same
  empty-draft fallback.
- Semantics identical to the accepted static page: skip link, landmarks, label, polite live output.
- A relative-base production build with no source maps, no remote reference, and exact pins.
- `--ignore-scripts` restores; no package declares an install script.
- The first render depends on JavaScript, which is expected for a client-rendered app.

## Observations without severity

- **O1 - the hover transition was still running in the focused captures.** The border pixels are
  `#a65e46` at 1440 and `#744434` at 768, about 75% and 49% of the way from ink to accent; static
  was about 95% and 94%. The transition starts in the pointer state, before reduced motion is
  emulated. The focus ring itself is settled and exact. If images must show settled states, capture
  after the transition or emulate reduced motion before the pointer step.
- **O2 - two app captures are byte-identical to static ones, and the report does not mention it.**
  `browse-HZL4jt\evidence.json` records `screenshot` operations #89 (`app-390-focused.png`) and #94
  (`app-negative.png`). They were taken after opening `127.0.0.1:63891` and `:63892` in this run.
  The observer writes CDP `Page.captureScreenshot` data with `writeFile(..., {flag:'wx'})`, with no
  copy and no single-03 reference. Identical DOM and CSS rendered by the same engine give identical
  PNG bytes: the footer is off-screen at 390, and the negative fixture is byte-identical
  (`3b3a8020...`). They are not relabelled. Stating this in the report would pre-empt the question.
- **O3 - SHA-1-only integrity.** 24 lock entries have only SHA-1 integrity, because the feed's
  metadata for them carries `shasum` but no `integrity`. Vite's metadata has sha512. This affects
  13 of the 18 installed packages:
  - the runtime-bundled `react`, `react-dom` and `scheduler`;
  - the `lightningcss-win32-x64-msvc` native build binary.

  The report discloses the SHA-1 entries (line 1438), and it is within the authorized feed. A
  stricter supply-chain bar would need sha512 from a second trusted source or from the feed.
- **O4 - transitive licenses.** The build toolchain includes MPL-2.0 (`lightningcss` and its win32
  binary), Apache-2.0 (`detect-libc`), ISC (`picocolors`) and BSD-3-Clause (`source-map-js`).
  Runtime-bundled code is MIT. The report's "all three declare MIT" (line 1417) is accurate for the
  declared packages only. That matters for any later license gate; compliance stays UNVERIFIED.
- **O5 - the CDN fetches came before their authorization.** They happened during `npm ci` at
  17:36:58Z (19:36:58 local), before the 19:47 classification. The owner's audit surfaced them and
  work stopped; `02edb556` then classified them as in scope. The recovery protocol was followed, so
  this is not a finding.
- **O6 - the dependency tree read cleanly now.** Two read-only passes over `app\node_modules`, 2 s
  apart, were identical: 356 files, inventory TSV SHA-256
  `c7073d46b628c15d8270fdba9f104e3e41e5512efc66a255f55f4c16ddbf2a3b`. Both passes read
  `.bin\nanoid.cmd`, a standard 326-byte npm cmd-shim. This does not explain the owner's earlier
  guard refusal; it records a stable state at review time.
- **O7 - Phase 1 watch item O4 is resolved.** The app uses the same page CSS, whose
  `:focus-visible` outline is ink. The 2.96:1 profile focus token goes unused, and the measured
  ring is 17.5:1.

## Limits

- **Delivery logs not inspected.** I did not open the npm debug logs or cache. The host/status
  audit is the owner's extraction from them. It is internally consistent: 80 metadata plus 18 CDN
  fetches, 18 installs, no query markers and all statuses 200. I could not confirm it against the
  originals without reading them, which the handoff forbids.
- **No rebuild or new native run.** The link from source to build rests on identical inputs and on
  the output's content and served bytes, not on reproducing the build.
- **Viewport and state coverage.** One headless Chrome 153 run with emulated viewports; only the
  initial and post-action focused states were imaged.
- **Accepted source not re-audited.** I did not re-audit accepted source `edecfdf`.
- **Compliance.** Compliance stays UNVERIFIED without a resolved required-policy source
  (`not_required`, bundled neutral). This is not a finding.

## Execution record and boundaries

Every worker ran as a PowerShell 7.6.6 `pwsh -NoProfile -NonInteractive` child in the Phase 1
synthetic environment (`%LOCALAPPDATA%\Temp\a145v`, `env.ps1` `d5ef9698...`); it inherited that
environment. No `LINTEL_*` variable was set.

- **Git.** Read-only only: `rev-parse`, `log`, `diff`, `merge-base`, `show`, `cat-file`, and
  `hash-object` without `-w`.
- **No other activity.** No npm or node execution, no browser, no server and no network.
- **No repository mutation.** No branch, commit, stash, fetch, push or config write. My worktree
  is clean.
- **Host temp file.** The host saved one oversized tool output to its own temp file under
  `%LOCALAPPDATA%\Temp`. It holds owned paths and environment names only, with no query value or
  credential. I did not create or delete it.

Mistakes I made and corrected while collecting data:

- `sas_scan.ps1`: an array argument didn't bind through `-File`, so I switched to a delimited list.
- `p2_cmds.ps1`: one run raced its own edits because I issued them in parallel; I reran it.
- `p2_lock.ps1`: it hit a JSON empty key, so I used hashtable parsing (`p2_lock2.ps1`).
- `p2_ctrl.ps1`: a key name was wrong; I read the file directly instead.
- The source-seal mismatch was resolved as EOL conversion, as described above.

Supplementary evidence is in session files `a145-phase2-evidence\`. It holds the executed scripts
plus `f04-intake.json` (`debc808050e3f56f0627af7142671c426bb919805cde0e8a46e4a3cc68728a98`),
`p2-pixel-analysis.json` (`58660a008967e99aa2d6d88bca53491e6396ba12ff0220d9b538ae3eb4d6f513`) and
`node-modules-inventory.tsv` (`c7073d46b628c15d8270fdba9f104e3e41e5512efc66a255f55f4c16ddbf2a3b`).
The staged files contain no SAS-like marker.

Next action: the coordinator's disposition of A14.5. After this review, the handoff permits
deleting the signed original logs once the coordinator confirms.
