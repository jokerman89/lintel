# P02 - Explicit private-sync binding

**Owner:** assigned isolated lintel-builder session. **Status:** authorized.
**Task authority:** ../plan.md A26. **Sources:** audit RU-03 and actual sync helpers.

## Ownership and preservation

Own `bin/li-roles-sync`, `bin/li-lessons-sync`, a shared helper under
`lib/private-sync.sh` or `lib/private_sync.py` if justified, and
`tests/integration/private-sync-binding.sh` with same-name Python support if needed.
Update directly related existing private-sync skill/docs only after confirming no
other active owner; otherwise report required integration text. Report in `reports/P02.md`.
Do not modify pack resolution, common audit/state helpers, generators, plan or memory.

Private role/lesson sync remains useful and opt-in. Keep local content on forget and
destination change; preserve old records during project-key migration. All fixtures use
temporary local bare Git repositories; no external network, personal homes, real roles,
real lessons, credentials or private remote mutations.

## Leaf contracts

| Leaf | Depends on | Observable acceptance | Verification |
|---|---|---|---|
| A26.1 | None | Setup writes one explicit enabled binding; existing Git origin matches selection or operation refuses clearly | New integration script: remote A then B, inspect config and actual origin |
| A26.2 | A26.1 | Push and pull reject disabled/missing/mismatched binding; forget preserves bytes and prevents future sync | Same script: drift origin and forget, assert failure and unchanged local files |
| A26.3 | A26.1 | Equal-basename repositories get distinct stable identities; legacy records retained | Same script: two repos named `project` under different parents; round-trip both |
| A26.4 | A26.2,3 | Both sync tools retain positive push/pull behavior and reject each negative case | `bash tests/integration/private-sync-binding.sh` exits zero with nonzero assertions count |

Run only focused tests during parallel work. Missing Git identity in fixture repositories
is configured locally to a synthetic test identity, never globally. Do not erase a real
remote or reset a destination branch to make a test pass.

## Report and stop

Commit package changes locally; do not publish. Report exact commit, per-leaf scenario
results, retained API behavior, record migration, files and limitations in reports/P02.md.
Independent spec and quality review is required before integration.
