# P12 - Artifact-specific document production

**Authority:** A15, CP-08/09/18 and AG-13; inventory of document skills/roles.
**Prerequisites:** P05/P06/P07/P08/P09 and P11 shared design schema.

## Standalone content and editable-artifact first unit

The full shared-pipeline integration retains those prerequisites. A disjoint first
unit is released now against accepted P03/P05/P06/P07 and P09's accepted role
knowledge: content fidelity and existing standalone `--brief` Word/PPT routes.
Do not consume or invent an unreleased P08/P09/P11 runtime/design interface.

Own `generate-outline`, `generate-write`, `generate-word`, `generate-ppt`,
directly coupled content/fidelity/mandatory-control corrections in `generate` and
`generate-qa`, format-owned references/minimal helpers, the new document-format
tests and P12 report. Read all existing consumers before changing their contracts.
Keep current field names, section IDs, artifact names and compatible standalone
arguments; no shared design/schema fork. Agent bodies and `generate-design` stay
with their existing owners. PDF/XLSX/Visio may be inspected for concrete capability/
interface gaps, but their implementation and shared `--from-pipeline` binding are
not part of this first unit.

Repair the universal 40-word/slide-shaped content restriction without losing
reasoning, citations, tables or uncertainty. Canonical source content is not a
slide summary; any shorter presentation view must retain the full source and its
evidence in notes/appendix or a linked long-form artifact. Preserve language from
the brief/profile/explicit choice, not a forced Swedish pronoun or fixed language
pair. Preserve mandatory configured policy; neutral style advice and arbitrary
limitations-count/voice-score proxies cannot invent or clear mandatory gates.

Discover the actual Word/PowerPoint/native tool schemas first. Prefer available
native artifact operations or existing declared libraries; observe missing tools
before any task-local restore. No global installation or made-up CLI/parser.
Produce real synthetic editable DOCX/PPTX artifacts through an available path;
inspect actual render/reopen/editability and content retention at the layer the
tool provides. Model/ZIP/file-size checks alone cannot establish rendered layout.
Keep a precise blocked observation if required rendering is unavailable.

Use a synthetic long technical brief with a claim/evidence ledger, multi-paragraph
reasoning, table, citations and a material limitation. Verify standalone output
retention and real failure/applicability behavior through accepted P05/P07, not
hardcoded all-PASS prose. No macros, personal Office documents, customer data or
external upload. Use only explicit owned artifact/profile/fixture roots.

Freeze one coherent first-unit product/report for independent review. It advances
A15.2 and standalone parts of A15.3.word/.ppt, not the shared-pipeline or remaining
format gates. This same original owner continues after later interface release;
do not close A15 parents or claim all formats supported from these artifacts.

## Standalone workbook second unit

**Authority:** existing A15.4.xlsx, recovery continuation of 2026-09-22.
The original owner's read-only format proposal is accepted for a native-workbook
unit using accepted P05/P07 and source-fidelity methods. It does not depend on
unreleased P11 serialization or P08 orchestration. Shared pipeline and all original
full-format acceptance remain separate; Word/PPT layout gates are not waived.

First use the already discovered native Excel canvas on a new explicitly owned
synthetic workbook. Test real formulas, dependency recalculation, persisted caches
and saved-copy edit/reopen: quantity 3 times 12.5 gives 37.5, changing quantity to
4 gives 50 and updates a dependent cross-sheet total. Preserve formula text and
independently inspect the saved values. Calculation flags or Python-computed
replacement constants are not execution evidence. Do not assume an unlisted
recalculate, render or export action exists.

Then implement the standalone method in `skills/generate-xlsx/`, format-local
reference/minimal checks and focused workbook tests. New test files may reuse
the established document fixtures without editing the frozen Word/PPT candidate
while its independent review runs. Keep Inputs, Calculation, Summary and
Sources/Methods, including units, assumptions, source ledger, uncertainty and
long-form reasoning. Preserve existing arguments and explicit formula versus
literal-text distinction; no macros, external links or copied shared schemas.

Use actual cache/formula/error observations through P05: missing/stale values,
unsupported/circular/broken formulas, missing required renderer or failed reopen
remain failed or unverified, not successful creation. If the native provider
cannot calculate or persist, report that exact boundary; do not invent an engine
or start/install an Office or alternative application under this release.
No Word application, COM/export/shell/alternate-launch retry is authorized after
its explicit denial, and the denied Copilot UI route remains closed.

Keep standalone PDF and Visio proposals ready without implementing them yet.
PDF may consume an independently accepted P11 print operation in a later bounded
release; neither a Word export workaround nor an unverified PDF renderer is
implied. Visio needs its actual declared writer/editor seam, not an image labelled
editable VSDX. No personal documents, remote upload, policy or global install.

Preserve product `cdb4ea6`, final report `30bd4e3` and all native artifact hashes.
Freeze a separate workbook product/report with actual tool/version/schema,
commands/actions, content/QA results and limitations for the SAME P12 reviewer.
No A15 parent closes from this unit's source or native writer success alone.

## F01 retained standalone defaults

Independent `dfb5cdd0`, preserved as `ee3ed5e`, reports P2 F01 in the frozen
Word/PPT unit. Restore omitted Word `--template` selection of `<target>.docx`
inside the already verified configured brand directory, with explicit template
precedence and the existing explicit `--use-defaults` route. Do not search a
personal directory or weaken required policy.

Restore the existing PPT-only planning hints as advisory: outline 8-15 slides,
default 12, and direct PPT 20-30 based on duration. Explicit count, brief/duration
constraints and a stated justified adjustment take precedence. Do not restore
universal 40-word truncation, Word/web quotas or a mandatory slide-count score.
The original owner may change only those three skill sources, focused default
preservation checks in the document-format test and its report for this repair.

Test all three Word variants, omitted/explicit template precedence and the
PPT-only fallback distinctions while preserving prior content/QA methods.
Freeze separately from workbook work and return to SAME reviewer `31c39265`.
Word permission-blocked layout and incomplete PPT visual inspection remain
unverified; this correction cannot clear those observations or whole QUALITY.

## Separate native presentation acceptance

The original format leaves `A15.3.word` and `A15.3.ppt` have different renderer
and permission boundaries. After F01 closes, evaluate the PPT leaf and its shared
content/fidelity methods independently; do not hold its quality review behind
Word app permission. All original Word, shared-pipeline and parent obligations
remain open. This is format-specific staging, not removal of mandatory controls.

The exact native PPTX `a5910cf4...8e93` and its six saved native SVG renderings
are unchanged. The coordinator and SAME reviewer directly inspected all six
rendered slides after file-only rasterization by existing ImageMagick. Their
recorded observation is native-canvas-rendition QA, not Office desktop/printing
or Word pagination. It may close the corresponding PPT inspection control only
after the reviewer accepts its source and artifact binding.

Preserve the original joint Word/PPT context and its failed QA receipt unchanged.
The original builder may prepare a distinct, explicitly selected `A15.3.ppt`
attempt under the SAME authoritative map using P05/P07, including all applicable
original PPT requirements: exact source/notes/citations/table retention, saved-copy
editability, actual render execution, complete rendered inspection and independent
specification/quality. Do not reuse its identity while dropping obligations,
relocate an existing pin or fabricate a reviewer/corroboration event.

The SAME reviewer then performs complete selected-PPT SPEC followed by QUALITY
if eligible, including shared generate/outline/write/QA consumers relevant to
that actual presentation. Source-default and content changes remain reviewed at
their exact revisions. Any new source defect returns to the original owner.
No new app action, denied UI retry, template-policy waiver or source rewrite is
needed for this evidence preparation. A PASS accepts only this declared native
PPT route; Word/PDF/XLSX/Visio and full P12/A15 completion remain separate.

## Standalone PDF writer and truthful inspection unit

The A16 browser component is independently accepted in `3049811` at `d3b5569`,
integrated in `7cb3812`. Release the original PDF proposal's standalone writer
portion against that accepted provider and P03/P05/P07, without importing
unaccepted A14 serialization or changing P09/shared schemas.

Own `skills/generate-pdf/`, directly coupled `skills/make-pdf/` corrections,
format-local minimal conversion/check helpers, separate focused PDF tests and
the report. Preserve existing input/output/paper/orientation/header/footer/
print-CSS/background arguments. Reuse an actually available declared Markdown/
HTML converter and the accepted browser print operation; inspect APIs before
calling them. No new PDF renderer/parser, conversion daemon or silent output
format change. Retain full reasoning, tables, citations, code, units and limits.

Use only owned synthetic local input/assets, explicit new output, and an owned
loopback server/browser context with accepted URL guards and process cleanup.
Verify real print output, searchable text, page count/content and bounds through
the existing explicitly selected reader. Capture source/tool/artifact identities,
partial-output/error cases and actual P05 non-clearing results; file size is not QA.
Remote inputs remain a documented guarded route, not exercised by this unit.

The earlier PDF-raster route was denied and must not be retried or replaced by
another launcher/provider to accomplish that denied inspection. No Word/Excel/
Copilot UI, COM/export, personal profile, credential, global setting or new app
installation is released. If complete page rendering cannot be observed through
an already permitted route, retain that exact required observation as unverified.
Writer/text-check success cannot close A15.4.pdf or claim full-format readiness.

Prefer existing dependencies. A genuinely missing converter must be observed
before any task-local, manifest-declared restore; no TLS/security bypass or
automatic global installation. Preserve original blocked joint/PPT decision
persistence and all old artifacts; do not write P12 structured review decisions
or corroboration after the explicit denial. This release does not resolve it.

Use the same original owner and an ancestry-preserving merge of the accepted
browser dependency if required. Keep source/test/report commits separate from
the already frozen Word/PPT/workbook units. Freeze for SAME reviewer `31c39265`;
shared pipeline and a real Visio writer/editor still need their own actual seams.

## W01-W04 bounded workbook integrity correction

Independent `0c1a711`, preserved as `7863190`, fails workbook source SPEC with
four P2 checker findings; QUALITY has not started. Release only the format-local
`check_xlsx.py`, its focused workbook tests, directly related reference/method
wording and report. No engine, cache publication, app access, shared provider,
XML framework or new dependency is authorized.

W01: validate required package content types and declared workbook/worksheet/
shared-string semantics before accepting cells. Bound and validate every declared
internal relationship target, not only the consumed subset; refuse malformed,
missing or inconsistent metadata and forbidden VBA/OLE/external-link mechanisms
by their semantic declarations regardless of filenames. Keep the reader's
declared bounded transitional-OOXML scope; do not infer executable safety from
`release_clearance:false`. Correct incomplete positive fixtures instead of
weakening this original refusal contract.

W02: verify shared-string root/namespace, supported cell types and unambiguous
payload multiplicity before decoding. Preserve legitimate blank/shared/inline/
rich-text and formula-looking literal values; reject duplicate inline payloads
and invalid empty-cell types rather than choosing one interpretation.

W03: refuse DTD/entity declarations for every supported XML encoding through the
existing parser's supported hooks or equally complete encoding-aware validation.
Cover inert UTF-8/UTF-16LE/BE examples and ordinary non-DTD UTF-16 positives.
Do not use amplification/external fetch probes, a substring-only false guarantee
or a new parser dependency.

W04: distinguish ordinary omitted/explicit `t="normal"` formulas from genuinely
shared/array/data-table/unknown semantics. Preserve formula/cache/type checks and
honest unsupported outcomes; no calculated replacement values or widened engine.

Reproduce the review's 13 discrepant inert packages, preserve the other expected
outcomes, existing source/typed/cache/error/ownership and size/entry limits, and
actual native empty-cache evidence. No original native workbook or old QA changes.
Keep PDF and Word/PPT source separate; pin a coherent repair/report for SAME
reviewer `31c39265`, source SPEC before eligible QUALITY. Full XLSX cache/layout,
P12 structured decision persistence and application permissions remain blocked.

### PDF D01-D03 source correction

Independent `0ea284cd73c66584216d6ccc785afd6ec96b509c` reviews final PDF
`5978def` / `9b4a8a1`: source SPEC FAIL, three P2 findings, QUALITY not started.
The coordinator read all 210 report lines and verified Git-byte SHA-256
`6197da20821420272034354c560726a8f23ad24979a1b154cb9a928c456de009`.
The original P12 owner may repair `check_pdf.py`, `prepare_html.py`, existing
PDF tests and directly related PDF method wording/report only. Preserve the
separately frozen workbook repair and accepted browser provider.

D01: use the existing reader's actual user-unit metadata. Validate a finite
positive scale, compare/report physical paper dimensions distinctly from raw
coordinates, and refuse invalid values. Ordinary unit-1 behavior and legitimate
unit-2 physical dimensions need paired actual-reader/CLI positives and negatives.
Do not silently substitute unit 1 for missing support or invalid metadata.

D02: validate page boxes and use the effective MediaBox/CropBox intersection;
empty or invalid effective regions must not clear. Test ordinary, narrowed,
oversized and translated-content contrasts. Preserve all raw observations;
do not clamp, discard origins or equate an origin check with glyph/visual review.

D03: recognize literal head text inside title/textarea RCDATA using the existing
HTML parser's supported text-context handling. Preserve full input, actual head
insertion, LF-offset/comment behavior and real duplicate-head refusals. Test
raw/escaped text pairs and retain script/style/comment controls; no new parser,
Markdown converter or browser is needed.

Reproduce the review's final PDF-1.7 and HTML saved inputs under owned copies,
including the five reader and two valid-HTML discrepancies plus paired controls.
Keep its earlier inherited-1.4 diagnostic history distinct. New source tests
and observed output statuses/exits must cover the real interfaces.

The actual four-page PDF, native HTML, 16 unexplained page-3 origins, original QA,
missing converter/TLS failure and visual obligations stay unchanged. All native
pages have user unit 1: these fixes do not explain or clear that specimen.
No native print, browser/server, alternate reader, experimental-layout retry,
raster, app/UI/COM, dependency/network action, decision/corroboration persistence
or publication is released. Return the frozen source/report to SAME `31c39265`
for affected SPEC and eligible source QUALITY; full PDF/P12 parents remain open.

### Complete standalone common and Word/PPT source review

The same reviewer's scope clarification confirms that `5858268` accepts only
the selected native PPT route; F01 `02e8028` closed Word-default source SPEC
but did not perform complete first-unit source QUALITY. Do not infer blanket
common/Word or A15.1-.2 acceptance from those reports.

After the ready PDF correction recheck, SAME `31c39265` may review the original
eleven-file standalone source unit from `3ee602f` through `cdb4ea6` and corrected
`32dac88ed1f143cf29ffe84f3fb1502c91a6c3e7`, report `b270475`. Select the six
generate/outline/write/Word/PPT/QA skill bodies, their three fidelity/Word/PPT
references, and the existing document-format pipeline Python/Bash tests.
Reuse exact unchanged PPT/F01/content observations and accepted dependencies,
but assess all relevant standalone callers, complete content/narrative identity,
three Word variants, template/default precedence and explicit QA failure modes.
Record complete bounded source SPEC before the first eligible source QUALITY.

This is read-only independent source review, not a new implementation, another
native rendition, or a relaxation of original artifact obligations. No Office,
COM, UI, renderer, network, dependency, structured decision/corroboration or
denied-route action is released. Keep exact source and report histories.
Word page/layout, actual shared pipeline/theme/work serialization, all full
format and A15.1-.2/P12 parents remain separately open. Return source findings
to the original owner, not fixes by the reviewer.

### Existing document pipeline input binding

The original owner's read-only map identifies the real remaining source join:
the existing brief/outline/content/notes hash chain and Word/PPT projections
are instructions, not an executable selected-input admission boundary.
Accepted P11 design validation intentionally governs web/app renderability;
it must not be widened into a universal renderer or faked for document input.

Choose the recommended document-only route within the existing architecture:
retain v1.0 document projections, use P11's existing compatibility validation,
and validate the documented Word/PPT references with format-owned checks under
an external current P05/P07 context. A genuinely mixed web/document envelope
also runs full existing P11 `load_design` checks. Its web validation does not
certify document layout. The mixed-only alternative would omit valid document
runs; source-identity-only admission would leave the requested design join open.
Neither narrower alternative satisfies this existing scope on its own.

After independent acceptance of the complete standalone common source, the
original P12 owner may normally merge accepted recovery dependency `2531a2a`
(which includes P08 `aa5cf0e`, accepted P04 parser dependencies, P09 `5c99612`
and P11 `a1b3a45`/`e1cb9d2`). Preserve the existing owner commits and exact
accepted helper/schema/reference closure; do not copy only the work reader,
import P10 or an unaccepted P04 final candidate, rebase/reset, or change an old
artifact's source identity. Older native observations retain their actual refs.

Own one read-only `skills/generate/scripts/pipeline_inputs.py`; source admission
and From-pipeline instructions in generate, generate-word, generate-ppt,
generate-qa, generate-pdf and generate-xlsx; the existing fidelity-and-evidence
reference; focused `document-pipeline-binding.py/.sh` tests and report. Preserve
all standalone arguments, three Word variants/defaults and PPT advice. Do not
change shared schemas/providers, agents or their runtime state.

Reuse P03 owned reads and shared Markdown source facts rather than adding another
visibility/task parser; reuse P05 strict JSON/context/current-source validation,
P07 live profile and required policy, and actual P08 original work binding and
the already demonstrated P09 admission checks. Check explicit original package/
leaf membership, prerequisites at their honest evidence level, and equality to
the externally prepared expected work. No checkbox or reader result grants
execution, review or release authority.

Require the actual canonical sibling paths AND original bytes: brief to outline,
outline to content, content to notes/design. Preserve complete Markdown/notes,
language, target formats, voice, section IDs, content slots and original design
objects. Existing template/config overrides are selected inputs, not permission
to reuse stale pre-override QA. Validate referenced sections/fields and documented
Word sections/PPT layouts without inventing new projections or native support.

If actual upstream domain data is selected, verify it against its OWN original
request, external final context and live profile with the existing P09 verifier.
Never graft document QA onto that request, invent a document domain, or turn
ordinary standalone content into a new domain envelope. Keep upstream artifacts/
evidence as explicit downstream selected inputs. Context/QA must remain outside
their own selected inputs; final changed artifacts require fresh final binding.

This helper admits inputs only: no renderer, model, checkpoint, P05 decision,
corroboration, publication or new persisted interchange envelope. Keep
`qa-report.json` v1.0 as the existing presentation report, not clearance.
Missing required observations remain non-clearing in the real QA consumer,
not a prerequisite that makes generating an as-yet-absent artifact circular.
PDF retains real HTML/print-CSS/converter provenance; XLSX retains its existing
workbook organization/formula/cache procedure. Neither gets an invented design
layout or waived native gate. Visio's missing actual writer/editor is unchanged.

Use new focused fixtures alongside the unchanged existing long-content producer
and hash-chain assertions. Verify document-only Word/PPT and genuine mixed input,
complete content/notes, same-bytes wrong-path refusal, hash/snapshot drift,
missing/unknown section and slot references, wrong package/parent IDs, live
profile/policy drift, changed template/config and real missing-QA nonclearance.
All actual inputs/calls use owned synthetic roots; no Office/browser/renderer,
network/dependency, denied record/raster route or installer invocation.
Freeze the coherent join/report for SAME `31c39265` SPEC then QUALITY.
Shared-source acceptance and all original artifact/parent gates remain distinct.

## Ownership and value

Own `generate`, `generate-outline`, `generate-write`, `generate-ppt`, `generate-word`,
`generate-pdf`, `generate-xlsx`, `generate-visio`, `generate-qa`, `make-pdf`,
`document-generate`; format methods/templates/helpers and doc-gen agents after P09;
focused tests and P12.md. P11 owns shared web/design; consume rather than fork its schema.

Preserve facts, source provenance, complete argumentation, editable outputs where
promised, narrative planning, specialist format expertise and all original entry points.
No universal 40-word section truncation or fixed language convention. Shared factual
briefs are not a slide-sized universal output schema.

## Leaves and checks

| Leaf | Dependency | Observable acceptance |
|---|---|---|
| A15.1 | Predecessor contracts | Shared source claims feed format-specific composition/QA with work/profile identity |
| A15.2 | A15.1 | A long technical explanation retains reasoning and citations across section/page boundaries |
| A15.3 | A15.2 | Actual supported Word/slide adapter produces editable artifact and rendered inspection evidence |
| A15.4 | A15.3 | PDF/XLSX/Visio each has concrete writer/renderer/recalculation/reopen procedure, implemented status only where verified |

Split rendering/validation leaves by format into stable sub-IDs before starting.
Use actual installed/native canvas/office/PDF/browser tools when available and inspect
their schemas first. Missing tool/library must be observed before installation; any
new validation dependency stays task-local and declared. No personal Office documents,
customer data, macro execution or external upload.

Add `tests/integration/document-format-pipeline.sh` (support modules allowed). Use a
small synthetic brief that includes a long explanation, table, links and an evidence
claim; slides additionally need overflow/asset checks, XLSX real recalculation and
formula/cache integrity, PDF page/text fidelity, Visio connectors/labels/reopen/editability.
A parse-only ZIP/XML test does not prove rendered layout or true application editability.

Preserve planned valuable formats with a working-code path and specific activation/
environment test, not an indefinite unsupported label. Where real render/reopen capability
cannot be obtained within authorized environment, state the exact remaining gate and
retain staged maturity instead of claiming the format fully ready. Keep that acceptance
leaf open if its required observation is missing.

Commit locally, report exact files/revisions, actual render/recalc/inspection commands
or native actions, output artifacts, retained content evidence and precise limitations.
Independent spec/quality review follows; no product capability deletion or publication.
