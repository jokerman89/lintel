# P14 - Cross-component and enterprise outcome evidence

**Authority:** A23/A24; ADR0008, ADR0021 direction, ADR0026/0028 and implemented profile ADR.
**Prerequisites:** stable combined package tree and final P04 A22.7 integration.

## Ownership and limits

Own new bounded integration/behavior scenario fixtures, test documentation and
reports/P14.md with source revisions. Shared implementation repairs return to their
owners; do not let an acceptance runner silently fix its own test subject. No private
profiles, external deployment, organizational control changes or unapproved paid model
service. This package is verification-focused: no fake product diff required.

## Leaves

| Leaf | Dependency | Observable acceptance |
|---|---|---|
| A23.1 | Package reports | Every corrected executable mechanism has discriminating negative and preserved-behavior evidence |
| A23.2 | Combined tree | Profile -> work map -> domain/result -> exact review -> resume crosses real producers/consumers |
| A23.3 | A23.2 | Zero runs/skips/missing host or policy remain distinguishable and block required acceptance |
| A23.4 | A23.3 | Generated-source checks, strict suite and clean consumer install run on stable exact tree |
| A23.5 | A23.4 | Independent final reviewer names exact merged content and all acceptance/preservation gaps |
| A24.1 | P07/P08 | Same synthetic engineering task under neutral, rapid-development and strict-change profiles |
| A24.2 | A24.1 | Relevant requirement/architecture/package/control/artifact differences trace to exact profile fields |
| A24.3 | A24.2 | Real interventions/rework/time/usage recorded where available; unknown data stays unknown |
| A24.4 | A24.2,P06 | Available native host runs real plan/build/review/cold recovery; other surfaces explicitly unverified |

Use temporary consumer repositories and a small task with a local executable acceptance
test, synthetic data boundary and a real design choice. Different profiles must change
applicable decisions, not only voice or document count. Less restrictive input must avoid
unnecessary process without skipping actual required review.

### Installed caller-to-child profile bridge

Exercise the actual canonical shell path in a separate installed consumer, not only
individual Python helpers: clear ambient LINTEL/CLAUDE selectors, source the bundled
`copilot-env.sh`, bootstrap the caller, then invoke its bundled `li-scaffold` with an
explicit child `--target`. Assert that the actual helper exists in the bundle first.
Retain only intentional fixture inputs, including an explicitly authorized shell choice
when applicable; never add an execution-policy override or fallback after denial.
Do not substitute a mocked helper, remove the inherited caller reference, shorten the
default runtime paths or add a custom home to make the primary case pass.

Verify the caller pin/home and unrelated files remain unchanged; only the child payload
and declared owned recovery-store writes are permitted. The child must not inherit or
mint the parent's profile generation. Include required-caller, drifted/missing-caller
and conflicting-target refusal before writes, plus a matching-policy positive case.
Keep custom-home, missing-helper and supported alias scenarios as separate controls,
not replacements for this canonical startup/handoff. Record source revisions and full
process/cleanup outcomes. This closes a shared P07/P08/P10 boundary only on the actual
reconciled source; passing each helper in isolation is insufficient.

Run one full suite at a time, freeze executing source for its duration, keep the true
exit status and preserve raw output under gitignored runtime. No pipeline may hide
failure. Include actual Git worktree fan-in and interrupted recovery for Swarming.
Do not convert provider docs or file-integrity checks into client acceptance.

Native model scenario delegation is the existing authorized session mechanism, with
bounded briefs and synthetic content. If a requested client is not installed or usable,
report a concrete unrun gate; do not install/configure personal clients or authenticate
another identity automatically. Recorded cost requires a real source, not a guessed rate.

## Dependency-ready core profile scenario preparation

Final P04 A22.7 source and joined contract behavior are accepted in `6e32d1f`
with independent `8a3a35c` and the recovery compatibility evidence. The P05/P06/
P07/P08 mechanical/P09 interfaces are stable. This releases preparation of the
original A24 core experiment while the P08 native workflow review, document
artifact gates and P10 installer remain separate. It does not release the
blocked installed caller-to-child test or final A23.4/.5 acceptance.

One new P14 owner may prepare a single small offline Python/standard-library
engineering task, identical synthetic input and business acceptance under the
neutral, rapid-development and strict-change profiles. Select a task with a
real design choice and an executable local oracle, not a prompt-length or
document-count proxy. Use only synthetic records and owned temporary targets.
Keep the base request/fixture identical; profile-dependent requirements must
trace to exact fields in the actual resolved P07 profiles and a concrete
decision, package, required control or delivered artifact.

Own `tests/integration/universal-profile-scenarios.py/.sh`, explicitly named
synthetic fixtures under `tests/fixtures/universal-profile-scenarios/`, and
`reports/P14.md`. Do not edit the source providers, active project profile,
shared plan/memory, installed tools, CI or original P08 fixture. Use the existing
strict readers, original task mapping, profile pin and P05/P09 boundaries.
No parallel work may overwrite common runtime state. Snapshot selected source,
request, fixtures, control inventory and expected comparisons before observations.

This first checkpoint prepares and verifies the fixture/harness and freezes
its exact source, task and profile-to-effect table for coordinator review.
Do not yet launch the native scenario implementers or fabricate their output.
The subsequent native tranche will use one actual implementer context per
profile, a genuine fresh-context pause/resume case and a distinct reviewer,
with one target writer at a time. No P08 actor or verdict may be borrowed.
Missing source/tool/permission stays visible rather than yielding an empty pass.

The preparation must explain how actual approved PLAN/BUILD/REVIEW/resume
operations will run without a new scheduler, duplicate task authority, fake
host API or an assumed automatic hook. Required policy may explicitly accept
a demonstrated local equivalent, but a hook label alone is not enforcement.
Less restrictive profiles retain necessary acceptance and independent review
without inheriting strict-only procedure. Measure real interventions/rework/
elapsed/usage only from available events; otherwise retain unknown values.
Native, source, synthetic and installed evidence remain separately labelled.

Freeze the prepared task, original-ID mapping, exact profile fields, positive
and negative oracles, process isolation, proposed actual actor sequence and
verification command. Coordinator SPEC/quality review of this bounded preparation
precedes the separately commissioned native run. The original A24 leaves remain
unchecked until their actual observations and independent review are complete.

### Preparation entry and progress correction

Coordinator review of `56ce767` / report `f106f00` leaves preparation SPEC
failed: E01 is the default shell-entry Git-object failure and P2 R01 requires
original initiative tasks to remain incomplete. No native actor is released.

The same P14 owner may correct only its harness, necessary focused fixture
controls and report. Keep immutable historical source/fixture locks separate
from current initiative progress: preserve IDs, substantive requirements and
approval, snapshot current authority before/after calls, and accept legitimate
checkbox progress without forcing a test to run old helpers. Do not introduce
a second parser, blanket hash exclusion, automatic baseline rewrite or fake
native result. Add actual progress-positive and requirement-drift negatives.

For E01, use command-local Windows Git long-path handling only inside the
already inspected owned synthetic repositories. This is explicitly released
fixture I/O configuration, not global configuration, hook bypass, a shorter
root, another Git installation, platform investigation or P10 retry. Retain the
original same-dimension shell case/executable and failure history; verify the
complete entry after the bounded correction, not only a shallow helper.
Source-checkout inspection and normal commits retain their existing controls.

No business oracle, strict policy outcome, original negative or native review/
handoff requirement may be weakened. Freeze the correction for coordinator
complete SPEC then the first eligible preparation QUALITY before the separately
authorized native tranche.

## Native profile experiment release

Preparation now passes coordinator SPEC then QUALITY in
`reviews/P14-preparation-final-9092a72.md`, after actual run07 passed all
15 methods at the original dimensions. Exact preparation history is integrated
as `c544fd0`; the native source baseline is that immutable commit. This releases
the original A24 experiment, not any blocked installer, renderer or publication.

The existing P14 owner remains the controller. The intended sequence is neutral,
rapid-development, strict PLAN, and a genuinely fresh strict-resume context.
The transport correction below permits one additional neutral continuation
after the actual plan-only synchronous dispatch: at most five implementer
contexts in total, counting that original actor. Reuse each continuation-capable
implementer for bounded rework; no nested agents, factory, model override,
cross-profile output sharing or other replacement writer. Use the host's real
multi-turn delegation/session operations and record actual returned IDs.
Canonical roles are source guidance unless native registration is observed.

The existing recovery coordinator serves as the distinct final reviewer for
the three implementations. This fills the proposed reviewer role without
creating another reviewer context; the coordinator has authored no inventory solution.
It is independent of the native implementers and the fixture controller.
The controller may assess each actual plan against the frozen requirements
before BUILD, but cannot supply its own final implementation verdict.

Before dispatch, establish three fresh owned synthetic targets from the exact
equal seed and one immutable trusted source/fixture export at the baseline.
Include the real canonical workflow/reference closure needed by the selected
phases, not only the preparation harness's mechanical imports. Keep all old
preparation roots/pins and P08/P12 fixtures unchanged. Pin the new roots, source,
business oracle, profile files and required observations before native work.
Use existing scoped setup/record helpers; do not build a general dispatcher.

Every actor receives the same R1-R6/T001-T003 request plus only its selected
actual P07 profile and applicable source text. No prepared solution, another
profile's output or desired algorithm is supplied. All modes retain the business
oracle and actual independent review. Rapid guidance remains advisory; strict
SC1-SC3 and the finite TQ handoff remain mandatory. Identical valid design choices
are acceptable; field values must cause applicable decisions/controls, not
fabricated differences.

Grant native writes only to the original fixture's `inventory.py`, permitted
design/profile/handoff/task-progress parts of its existing `plan.md`, and
explicitly named owned test/evidence outputs. The caller owns contract/checkpoint/
QA/review persistence through existing P03/P05/P07/P08/P09 APIs and original
preimages. No changes to the frozen request, oracle, policies, task IDs, provider
source, another target or original Universal checklist are permitted.

Run actual proportionate SENSE/SCOPE/DEFINE/DISCOVER/PLAN/BUILD/REVIEW/CAPTURE
work with SHIP explicitly skipped for this local experiment. Record real phase
outcomes; a diagram or populated field is not execution. Derive phase-appropriate
acceptance while retaining the predeclared final business/strict obligations;
an early plan review cannot claim final implementation acceptance.

Pause strict only after its real T001/PLAN outcome, before T002/BUILD. Quiesce
and revoke that writer. The fresh strict-resume context gets only original literal work/
profile/cycle paths and the real durable handoff, not its predecessor's chat or
a supplied next-task answer. It must derive the remaining original work and
verify the same live pin before any edit. No unknown external effect is replayed.

Use actual actor/attempt IDs for new P05 contexts; preparation identities are
not native results. Obtain the real business oracle and, where required, atomic
publication and owned-recovery observations. Strict TQ uses its existing P09
request/result boundary with its own original context, not a new schema or
another domain's QA. Separate independent review and actual host observation
precede producer/latest-reader/QA clearance. Reviewers never repair findings.

Keep the reviewed source/result and all control evidence unchanged through
consumption. Bind authority through its existing work contract; do not add
unnecessary raw mutable progress files merely to duplicate already-bound
authority. Any actually selected raw file still invalidates evidence on change;
do not ignore, rewrite or roll back an observed stale result.

Before switching targets, verify actual quiescence and revoke the prior grant.
Record exact inputs, source/profile/actor IDs, commands/counts/exits, substantive
plan/implementation differences, independent findings, rework and real timestamps.
Usage/model identity/cost absent from host records stays unknown. Do not force
ROI or compare unrelated durations. Preserve failed attempts and unchanged other
targets; freeze source once and relevant target boundaries without repetitive
unrelated corpus scans.

Stop each ready implementation for the coordinator's actual review, returning
its exact artifact/context/QA paths and hashes. Report the comparison only from
completed observations. No new P10, UI/Office/browser/raster, network/install,
jq, CI setup, private data, denied P12 record route, remote action or hook/global
configuration is released. The original A24 and final A23 acceptance remain
unchecked until the actual experiment and its reviews are complete.

### Native transport correction, 2026-09-23

The controller reports actual neutral task
`7742184c-1e60-424e-a570-7937dd6eac72` completed T001 and returned idle after
one synchronous turn. Its subsequent BUILD message was rejected because this
host's `write_agent` accepts only background agents. No BUILD message reached
that actor; `inventory.py` remains the original stub. Preserve the real T001
plan, launch/read receipts, exact refusal and blocked pre-dispatch BUILD entry.
This is a transport failure, not a business-oracle or profile failure.

Continuing that synchronous actor is unavailable. Keeping four total contexts
by omitting a profile or fusing strict PLAN/resume would weaken the experiment.
The selected correction is one new neutral background continuation, explicitly
counted as a fifth possible context overall. This coordinator-owned transport
correction stays inside the operator's existing native-experiment authorization;
it does not grant a new product, tool-permission or cross-target scope.

Before dispatch, verify the old actor is quiescent and revoke its write grant.
The new neutral actor receives the original literal work/profile/cycle paths,
frozen request and the actual T001 plan/handoff, not another profile's output or
a controller-authored solution. It must inspect the current work and live pin;
the controller must accept the existing plan before T002. Attribute T001 to
the original actor and BUILD/rework to the new actual ID. Do not reset task
progress, reuse the old actor ID or represent the handoff as same-context reuse.
If the new actor identifies a genuine plan defect, stop that affected action
and return it to the controller instead of silently rewriting accepted intent.

Use continuation-capable background tasks for this actor and the remaining
rapid, strict PLAN and fresh strict-resume actors. Give complete bounded briefs,
do independent controller work while they run, and await actual notifications;
do not poll or start concurrent target writers. No second transport replacement
is released. Any further transport failure returns for a bounded decision.

The original source remains exact `c544fd0`, with unchanged target roots, raw
business inputs, policies and oracles. Read this amended authority separately;
do not repin or modify the frozen export. The controller reports all18 original
fixture raw hashes match accepted run07 before BUILD; retain that exact receipt
and distinguish its timing from the earlier T001 dispatch. Record the extra
neutral context and intervention in the comparison; no clean timing/usage or
causal profile-performance claim may hide this asymmetry. Parent inspection
APIs did not expose this controller-local task, so retain the controller's
actual host receipts rather than claiming independent parent inspection.
Final independent review, actual consumers and all original blockers remain.

## Final handoff

MasterSession controls final independent review and remote publication. Report every
A01-A26 as accepted, still under review, failed or blocked with evidence links; describe
all 76 Swarming preservation paths and any actual unresolved loss. No main merge,
production action or 1.0/compliance/ROI claim is authorized by a green local suite.
