# P01 - Trusted local tools

**Owner:** assigned isolated lintel-builder session. **Status:** authorized.
**Task authority:** ../plan.md A04 and A25; no checkbox state lives here.
**Sources:** audited RU-02, RU-04, RU-11; ADR0008; current trusted resolver prior art.
Read current code before edits and the relevant audit finding objects, not only this card.

## Ownership and preservation

Own source-resolution fixes in `hooks/shared/*/run.sh`, `bin/li-vault-init`,
`bin/li-adr-new`, `bin/li-update`, their immediately corresponding HOOK.md descriptions
where needed, and `tests/unit/universal-trusted-tools.sh` (plus same-named Python
support if needed). A small new shared trusted-source helper is allowed under
`lib/trusted-source.sh` only if existing trusted resolution cannot be reused.
No other shared helper, pack resolver, hook registration, generated file, global state,
installer, audit artifact, plan or memory edits. Report in `reports/P01.md`.

Keep all domain checks, project configuration effects, vault integration, ADR syntax,
multi-host updater behavior and useful fallbacks. Never execute target-provided resolver
code as implementation. Do not activate optional hooks, touch a real vault or install/update
personal tooling. No network, accounts or remotes.

## Leaf contracts

| Leaf | Depends on | Change / observable acceptance | Verification |
|---|---|---|---|
| A25.1.a | None | Synthetic target resolver creates marker only if incorrectly executed; inventory affected callers | `bash tests/unit/universal-trusted-tools.sh` fixture fails on baseline boundary |
| A25.1.b | A25.1.a | Every affected hook sources trusted installation only; registration unchanged | Same fixture: marker absent and target policy still read as data |
| A25.1.c | A25.1.a | Vault helper uses trusted resolver; dry-run does not mutate real destination | Same fixture with temporary source/home/target |
| A25.2 | A25.1.b,c | Synthetic pack/config retains domain decisions; dormant hooks remain dormant | Compare hooks.json before/after and positive fixture output |
| A25.3 | A25.2 | Positive and hostile target cases pass for every modified resolver caller | Entire focused script, no skips treated as pass |
| A04.1 | None | Parse title/status as documented; do not treat arguments as commands | Temporary repository invocation under the focused script |
| A04.2 | A04.1 | Empty and existing ADR stores produce expected unique file/title/status | Focused fixture asserts contents and sequential ID |
| A04.3 | None | Stubbed updater failure keeps nonzero status or verified documented fallback | Stub each changed update branch; focused script asserts status and output |

Run `bash tests/shape/hooks-registration-safe.sh` and affected existing checks in addition
to the new focused script. Use isolated LINTEL_HOME and LINTEL_REPO_ROOT; record missing
dependencies honestly. Install a dependency only after the chosen check proves it missing,
and do not alter user-global tooling.

## Report and stop

Commit focused implementation/tests/docs locally with Conventional Commits. No push/PR.
Report leaf IDs, changed behavior, preserved behavior, files, commit SHA, exact commands
and outcomes, baseline reproduction, skipped evidence and remaining risks. Stop at package
handoff; a different reviewer will inspect the immutable result. Do not self-approve.
