# P07 - Stable effective profiles and compatibility

**Authority:** A07 and A20.3/4 in plan.md/spec.md; explicit operator demand that required
policy cannot become neutral success. **Inputs:** P06 operations and P05 outcome contracts,
audit RT-03/04/10, WF-06, CP-07/12, AG-05; current pack/adapter source and tests.

## Accepted behavior boundary

Preserve whole-top-level-block inheritance, explicit null/false/list semantics, extension
packs and source/target separation. Introduce the stricter required-profile behavior with
reserved ADR-0029 and directly related docs/tests, not by silently changing an old ADR.
Neutral first use with no requested organizational profile remains valid.

A required profile is declared outside the potentially broken manifest so load failure
cannot erase the requirement. Prefer a small optional repo data declaration under
`.claude/` plus explicit invocation selection over another configuration service.
Document precedence: repository-required constraints cannot be replaced by a user-global
preference; explicit approved source/target paths remain honored. Legacy optional
preference fallback may remain diagnostic, but never constitutes successful activation
of a required named profile. An invalid bundled neutral baseline returns an actual error.

Pin effective source/name/version/ancestry/content to the selected work/session context.
Tool-shell PID is not durable session identity. Child handoff and cold restart carry/
verify the same profile reference. Detect active-pointer and manifest/ancestor changes,
including same-mtime content changes; do not silently switch policy mid-work. Provide
explicit rebind/replan procedure preserving old evidence.

Store only approved non-secret identity/provenance in reviewed artifacts. Runtime copies
stay within existing gitignored `.claude/runtime/` or explicitly selected LINTEL_HOME.
Do not read or activate the operator's real private pack while developing tests.

## Ownership and implementation

Own `lib/pack-resolver.sh`, `lib/pack-schema.yaml`, `lib/copilot-env.sh`,
`packs/_default/pack.yaml`, a minimal structured profile helper/schema if justified,
profile-related docs/skills/tests and ADR-0029. A narrow coordinator grant includes
`skills/pack-validate/SKILL.md` for direct parser/compatibility and remaining-unverified
wording; P10 inherits it later. Other create/switch/install lifecycle ownership stays P10.
Do not change review, Swarm, routing,
shared generation or installer code concurrently. Report required consumer edits to
MasterSession for the owning package. Report path: reports/P07.md.

Use one structured parser/representation for resolution, provenance, fingerprint and
compatibility; no executable config. Keep documented manifest syntax working and
malformed/unsupported syntax explicit. Preserve public shell accessors for consumers.
Separate product version from pack schema and requested feature compatibility; do not
compare historical internal v4/v5 numbers to current public version as if equivalent.

## Leaf contracts

| Leaf | Depends on | Observable acceptance |
|---|---|---|
| A07.1 | None | ADR-0029 explicitly distinguishes required load, optional preference and neutral first use |
| A07.2 | A07.1 | Effective structured values retain whole-block inheritance and exact field provenance |
| A07.3 | A07.2 | Two fresh tool processes and a delegated/cold-resume fixture share a stable selected profile |
| A07.4 | A07.3 | Required missing/invalid pack and changed manifest/pointer are unresolved errors, not advisory success |
| A07.5 | A07.4 | Synthetic profiles visibly alter relevant routing/control/design input; no real customer data |
| A20.3 | A07.2 | Product/schema/capability versions validate independently with a documented legacy migration |
| A20.4 | A20.3 | Valid synthetic pack accepts and incompatible pack rejects for the declared reason |

Create `tests/integration/universal-profile-context.sh` (Python support allowed) exercising
actual producer -> separate process consumer -> handoff/resume, not only field reads.
Keep and extend enterprise-pack-resolution, pack-inheritance-depth-3, fallbacks and
pack-source-target-resolution tests. Update intentional old advisory-fallback fixtures
to distinguish optional legacy behavior from required policy; never weaken mandatory gates.
Include duplicate keys, quoted lists/null/false, deleted parent and same-mtime edits.

## Report and stop

Commit focused changes locally; report exact API, file paths, precedence, fingerprint,
compatibility, migration and per-leaf commands/results. State any unresolved host-selection
boundary rather than inventing a stable host ID. No private policy activation, global
configuration, remote calls, hook registration or paid model invocation.
Independent spec and quality review follows; do not self-approve.
