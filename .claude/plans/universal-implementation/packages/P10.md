# P10 - Owned consumer lifecycle

**Authority:** A12; CP-02/04/12/16, RU-06/07/08/11 and lifecycle inventory rows.
**Prerequisites:** integrated P01/P03/P06/P07; coordinate P08 shared helper release.

## Ownership

Own `install/`, `bin/li-scaffold`, `bin/li-doctor`, `bin/li-migrate-claude-home`,
`bin/li-pack-scaffold`, minimal explicit pack/profile/role lifecycle helper if needed;
skills `scaffold`, `scaffold-internal-tool`, `scaffold-mvp`, `doctor`, `health`,
`pack-create`, `pack-list`, `pack-switch`, `pack-validate`, `profile-switch`, `role`,
`role-new`, `roles-list`, `personas-rotate`, `migrations`, `v4-migrate`; directly related
docs and tests. Copilot/Universal generator modifications require explicit shared owner
handoff; reuse their protected-file inventory, do not duplicate safe installation logic.

Preserve consumer-owned prose/config, local roles/packs/hooks, idempotence, old-version
recovery paths, partial-state diagnosis and optional private destinations. Never execute
or switch another working repository merely because it has a familiar basename.
No user-global install/profile mutation, external destination activation, guessed plugin
disable marker, unattended rollback, main push or runtime hook activation.

**Operator decision (2026-09-20): installation must remain usable without Python.**
The operator explicitly rejected adding Python 3.9+ as a bare-install prerequisite.
Existing Python-based adapter/runtime operations keep their own declared prerequisites;
that does not permit raising the Git/Bash or native PowerShell installation floor.
Use a single documented ownership/recovery contract with native host implementations
where necessary and shared cross-host behavioral fixtures. No automatic interpreter
installation, unsupported copy-only success, or hidden prerequisite.

### Approved native and runtime transaction boundaries

Bare install retains native Bash 3.2 and PowerShell 5.1 paths. P10 owns their
implementation and one documented/versioned TSV receipt contract, with the same
positive/negative fixture cases. SHA-256 may use existing sha256sum/shasum/OpenSSL on
Bash and .NET on PowerShell; preflight availability before mutation, never install a
dependency automatically. Preserve spaces and ordinary Unicode; refuse unsafe path
spellings/control characters rather than evaluating receipt text.

On Windows, the Bash entry may dispatch to the same already-present native PowerShell
performer to avoid per-file Git Bash process overhead and duplicate Windows recovery
logic. Preflight host capability and permissions before writes; preserve arguments and
exit codes. Do not bypass execution policy or another denial, install a runtime, or
change global settings. Exercise both actual entry points; that is not evidence of
POSIX/Bash 3.2 execution. Retain the separate POSIX performer and shared receipt contract.

**Explicit local verification choice (2026-09-20):** after Windows PowerShell 5.1
refused script loading under its existing Restricted policy, the operator approved
explicitly selecting the already-installed PowerShell 7.6.6 for disposable local tests.
Read-only checks showed its existing LocalMachine RemoteSigned policy; no policy
changed. `LINTEL_POWERSHELL` may select this executable explicitly for those tests,
without automatic retry after denial. This neither raises the installation dependency
floor nor establishes 5.1 runtime acceptance; keep 5.1 denied/unverified.

The native receipt binds exact source/target/store, old managed inventory identity,
before/planned-after byte hashes, sizes and supported mode/readonly projection,
verified backup/staged files and per-path mutation/recovery progress. Preflight all
paths and ownership before writes. Publish intent before each atomic file replacement,
verify afterward, and publish the final inventory last. An interrupted operation
remains incomplete and returns nonzero; no automatic rollback, lock stealing, broad
root deletion or adoption of edited legacy files. Explicit recovery first verifies all
blobs and current states, refuses later user edits, and consumes each restored path's
permission so replay cannot overwrite a later edit even if it matches an old postimage.
Empty created directories may remain; ACLs/xattrs and multi-file atomicity are not promised.

Use explicit --store/LINTEL_RECOVERY_STORE when supplied. The compatible no-argument
native default may be a reported dedicated sibling `<LINTEL_HOME>-recovery`, validated
as nonoverlapping and owned; never adopt an unrelated existing directory or silently
choose another destination. Existing config/profile/packs/roles/brand remain seeds or
consumer-owned content, not overwrite permission.

For ALREADY Python-based repository/scaffold/migration operations, P10 may add
`lib/managed_transaction.py` and a thin `bin/li-managed-transaction.py` dispatcher.
Reuse P03 owned-state/snapshot/bind/restore APIs after the coordinator's 3.9 compatibility
patch. This is a file-mutation primitive, not a replacement inventory or scheduler:
the existing adapter retains its manifest/blocks/clients, EOL-aware ownership policy
and protected-file checks. Do not extract a competing `plan_update` ownership engine.

The shared runtime seam accepts root/store, exact changes (bytes or deletion), exact
expected before states, explicit planned modes, label and final-publication paths.
It validates all inputs before snapshots/writes, stores a deterministic plan before
mutation and delegates verified recovery to P03. A current-state scan alone never
confers ownership. Unknown/corrupt/legacy receipts stay inspectable but non-applicable.
Explicit store overrides win; any producer default must be a reported validated sibling,
not an overlapping store inside the target. No real home/global invocation is authorized.
The new Python-runtime default may use a compact `.lintel-recovery-<root-digest>`
sibling bound to the full resolved root, avoiding duplication of a long target basename.
Verify same-basename/different-root and foreign-store refusal; keep actual long targets
unchanged and do not shorten or relocate explicit stores. This does not prove support
for arbitrary long parent paths or change the native compatibility default.

Cross-target scaffolding verifies a caller pin against its original repo/home/source,
retains it as operation evidence, and separately resolves explicit target requirements.
Required caller policy remains an operation constraint; target neutrality cannot erase
it and conflicting required target policy blocks before writes. Matching names alone
do not establish matching content. No parent rebind, child generation, private-pack copy
or new target policy requirement is implicit. Optional render preferences may be hints,
never required policy. Child startup binds its own context explicitly; tests must preserve
parent pin/home and unrelated target bytes.

After the coordinator supplies the frozen preflight checkpoint, P10 may wire only
the transaction/CLI seam in `bin/li-copilot.py` and related interruption cases in
`tests/integration/copilot-kit.py`. Preserve its required-resource preflight and all
current navigation/installed-profile cases. Provider, navigation parsing and P07
implementation remain outside ownership. Add new runtime files to the existing
required-source inventory; bare install must never call this Python path.
Coordinator continues to own generated output and final integration.

## Leaves

| Leaf | Dependency | Acceptance |
|---|---|---|
| A12.1 | P03/P06/P07 | Skills use real owned helpers and configured source/target/profile paths |
| A12.2 | A12.1 | Fresh install/update/local conflict preserve all consumer customizations |
| A12.3 | A12.2 | Interrupted install/migration and explicit rollback touch only verified owned content |
| A12.4 | A12.3 | Uninstall limits, overdue/historical migration and client operation support are truthful |

Split each helper family into short named substeps before implementation if necessary;
coordinate new sub-IDs with MasterSession. Use existing template/inventory formats and
the P03 tested snapshot helper. Profile changes must use P07's structured contract and
report effective result, not just a write to the wrong pointer.

Dispatch refinement: plan.md now supplies A12.1.a-d (scaffold/diagnostics, pack/profile,
roles/personas, migration/recovery), A12.2.a-b (fresh/repeat, conflict preservation),
A12.3.a-b (interruption diagnosis, verified owned recovery) and A12.4.a-b (host/uninstall
limits, historical aliases). These refine the existing acceptance, not a second backlog.
P01/P03/P06/P07 are integrated and their owned contracts are released for consumption.
P05 `b023e8c` is independently accepted by `33eac071` and integrated in `c5c8f86`;
its exact dependency import and mandatory source closure are authorized separately.
ADR-0030 records the native/runtime split. The coordinator retains the current
preflight batch and all generated outputs; after its frozen checkpoint, only the
transaction/CLI seam specified above is released to P10. Do not fork the adapter's
ownership logic or change its accepted Markdown provider/navigation parser.
P08 is active, but no shared audit/context/work-map implementation is released to this
installer package. Its A13 consumer work waits for P10's exact contract artifact and
real producer fixtures, not an informal receipt-field description or a WIP import.

## Verification

Add `tests/integration/universal-lifecycle.sh`, using separate temporary source/install/
consumer repos. Exercise new install, repeated install, local edit, profile switch,
unsupported host operation, interrupted write and recovery with exact before/after hashes.
Include a real no-Python bare-install case for each available native installer, and
keep later Python-dependent operations explicit rather than advertising them as run.
Keep existing Copilot/Universal protected-file tests and install-target-boundaries tests.
Run `tests/runner/check-install.ps1` against temporary roots where applicable. Record
Windows/Bash execution evidence without claiming unrun OS/client results.

### Narrow P07 fixture follow-up

The frozen `a73cf9ee` entry now uses one actual lifecycle-dispatch block, while
`test_validation_skill_executes_shared_contract_without_activation` still expects
three old inline blocks and old PASS prose. P10 may change only that method and its
copied-source dependency list in `tests/integration/universal-profile-context.py`.
Execute the real skill block and validate its structured effective/compatibility
result. Preserve no activation/pin creation, existing-pin verification and malformed
required-policy refusal; do not replace these with a body-string presence check.
P07 runtime code, schemas and every other lifecycle/long-path method remain untouched.
An exposed product defect needs a new scoped decision, not an expanded fixture patch.
Commit this follow-up separately, rerun the actual method and relevant preservation
checks, then freeze a new report-only checkpoint. The earlier 120/128 strict result
remains historical; focused passes do not turn it into a passing aggregate.

Preserve old migration aliases as versioned, opt-in recovery procedures. Do not delete
legacy stubs just because a calendar grace date passed; unresolved migration remains visible.
Any installer provenance/version changes are coordinated with P13, not unrelated rewriting.

### F01 malformed migration-row repair

Independent report `1d871338` (report-only parent `62ffb9a`, preserved on the
coordinator as `46e8b0f`) is SPEC FAIL, P2 F01; quality has not started. The actual
migrations skill returns exit 0 with an empty inventory for this truncated active row:
`| overdue-work | 2026-06-12 | 2026-09-12 |`.
`migration_inventory` currently groups `len(cells) < 4` with ignorable table rows.

The original P10 owner may repair only this parser in `bin/li-lifecycle.py`, focused
cases in `tests/integration/universal-lifecycle.py`, and its own report. Reproduce
the report's complete valid/malformed catalogs through the unchanged real skill and
dispatcher before repair. Distinguish recognized headers/separators/placeholders from
malformed data and validate the applicable active/archived row shape. Invalid data
must produce an explicit nonzero diagnostic, no successful empty inventory and no
source/target/home writes. Retain overdue/unknown observations, archive selection,
missing-catalog/date errors and fenced literal-row exclusion.

No second Markdown parser, provider/profile/schema/receipt/native-installer change
or broader cleanup is authorized. Freeze a new product and report-only checkpoint
with exact RED/GREEN and preservation evidence for the same reviewer. The default
store setup failure D01 and validation-fixture cleanup ERROR D03 remain distinct
blocked/unverified observations, not F01 closure or permission to shorten paths,
change P03/P07 or alter global Git/OS settings. Complete spec still precedes first
whole-P10 quality; A13 remains closed.

## Reviewed native-path dependency and direct-consumer unit

**Status:** authorized staged follow-up, 2026-09-21, after independent scoped core
SPEC/QUALITY `062d0f20`. Exact core is `ce7415f` plus fixture-only `5fc657f`,
report `8170c8e`; coordinator integration is `1d44d2a`. The original P10 owner may
import those two product commits as separately attributed dependencies into frozen
`4407827` / `c1de05c7`. Preserve all prior refs and F01 closure `00a0bef`.
Resolve only necessary resource/copy-list conflicts, retaining both P10's dispatcher
fixture and the reviewed core cleanup. Do not replace a whole helper/fixture to
resolve a conflict or amend earlier commits. ADR-0031 may be read from its immutable
coordinator source or imported separately as metadata.

The remaining direct consumer scope is limited to native I/O operands in:

- `lib/managed_transaction.py`: `_roots`, `_pending`, `_lock`, `_write_change`,
  `apply_files` and `recover_transaction`, with ownership/receipt/default-store,
  preflight and consumed-permission logic unchanged.
- `bin/li-copilot.py`: `safe_path`, `read_file`, `source_bytes`, `load_inventory`,
  `generate`, existing publication and `main` filesystem operations; the additional
  metadata/read operands specified below. Preserve its managed inventory, protocol
  blocks, client selection, EOL rules and all navigation.
- Required-source inventory entries and actual copied dependencies for
  `lib/native_paths.py`; fixture-owned same-location cleanup only after exact-root
  verification in P10 lifecycle/native/transaction fixtures. No shorter fixture,
  changed assertions, P07 scenario rewrite or hidden cleanup failure.

**Operand-only scope completion (2026-09-21):** the coordinator inspected these
four functions in immutable `c1de05c7` and confirmed they equal the coordinator's
current functions. The original P10 owner may also wrap `protocol_updates`' existing
`exists`/`is_file`/`read_bytes` operands, the checked `safe_path` results used by
`verify_links` and `bundle_documentation` for `is_file`, and `runtime_ignore_errors`'
`(target / ".git").exists` operand with the reviewed `native_io_path` helper.
These are remaining reads/metadata in the same long-path consumer flow, not new
ownership or navigation policy. Keep protocol marker parsing, surrounding user
prose, EOL/hash rules, public-document selection, link interpretation, Git argv,
ignore-rule/error behavior and logical diagnostic paths unchanged, except for the
separately released read-only Git option below.

Focused evidence must show existing long-path protocol prose is read/preserved,
real public directory-index and generated-link targets are checked, and an existing
long-path `.git` entry cannot silently skip the effective ignore check (including
its negative result). Use the existing owned Copilot fixtures and verified synthetic
roots; retain namespace/link refusals. A newly exposed Git-command or policy defect
is a separate scope report, not permission for extra Git flags or a parser change.

Use the reviewed two-function boundary after existing caller authority checks;
keep logical paths and all serialized roots/hashes separate from I/O aliases.
Missing trusted code fails before writes; no target/PYTHONPATH fallback. Native
installation stays Python-free. No P03/P07 implementation, F01 parser, profile/
review schema, native receipt format, store selection, P08/A13 or global setting
change is authorized. Return a concrete additional site before expanding this list.

Required evidence is on the FINAL exact dependency, not the earlier diagnostic
overlay: real default init/check and explicit owned recovery at the original
fixture94/target113/store128 and snapshot218->260 dimensions; the unchanged classic
installed/removal scenario at outer109/source116/target182/template256->263;
actual canonical bundled caller bootstrap -> child scaffold with default home and
inherited verified reference; and the actual validation fixture with complete cleanup.
Retain missing-source/inventory-removal, user edit, corrupt/foreign/incomplete/replay,
interruption, namespace/link/outside-root and caller/home/config preservation.
Distinct shallow/explicit-store controls do not replace these default cases.

### Separate read-only Git boundary

The owner reports that the real target256/`.git`261 metadata probe now reaches
`runtime_ignore_errors`' actual Git command, but Git exits 128 for both valid and
negated ignore rules because it cannot read the long `.git` path. Preserve that
RED separately from the earlier skipped probe and Python publication failures.

The original P10 owner may compare the unchanged command with the same command
plus Windows-only per-invocation `-c core.longpaths=true` at the same target.
Keep `-c core.fsmonitor=false`, `-C` with the exact logical target, `check-ignore
--no-index --quiet` and its existing probe unchanged. This is not permission to
write Git configuration, relocate a fixture, use a different root spelling, add
other flags, or change any other Git call.

If the bounded diagnostic produces exit 0 for the valid rule and exit 1 for its
effective negation, with preserved state, implement that one Windows-only option
in `runtime_ignore_errors` and its focused existing Copilot fixtures. Otherwise
stop this seam and report the evidence; do not add fallbacks or treat exit 128
as a negative-rule pass. Retain the existing explicit error for any nonzero result
and unchanged argv on non-Windows hosts.

Record actual argv/exits at the same lengths, short-path controls, an invalid Git
state that still fails explicitly, and before/after HEAD/index/config plus seeded
content bytes/modes. Keep synthetic environment/path preflights and exact-root
cleanup. Injected non-Windows argv coverage is not native host execution evidence.
This separately scoped consumer extension of ADR-0031 does not reopen P03/P07,
alter native installation, or close any full-P10/quality/A13 acceptance gate.

**Flag-only diagnostic failed:** the owner reports that Git for Windows
2.55.0.windows.3 still returns 128 for both long-root cases with the sole option;
short controls return 0/1 and malformed HEAD returns 128. State was preserved.
The conditional implementation above was not released by those results, and no
product Git option was added.

The original owner may now compare two explicitly bounded same-location variants:
use the reviewed `native_io_path(target)` only as the Windows `-C` I/O operand,
first without `core.longpaths`, then with the per-invocation option only if the
first variant fails. All other product arguments and logical target/diagnostics
remain unchanged. Verify actual filesystem root identity before execution;
read-only fixture `rev-parse --show-toplevel --absolute-git-dir` is permitted to
confirm Git's selected worktree and Git directory. Do not infer identity from
string normalization, or use `--git-dir`, `--work-tree`, environment redirects,
path shortening or a custom alias parser.

If a variant passes the original valid/negated 0/1 distinction, explicit invalid
Git-state failure and state-preservation checks for both a real `.git` directory
and a linked-worktree `.git` file, the owner may implement the first passing
variant at this one call site and its focused fixtures. This selects one fixed
Windows invocation, not a runtime fallback/retry chain. Keep the non-Windows
invocation unchanged. Report the source/argv identities, supported fixture forms
and exact path lengths; unavailable forms remain unverified. If neither variant
passes the gate, stop this seam again without a product Git change. All historical
failures and independent acceptance gates remain open.

**Linked-worktree fixture construction:** the owner reports that ordinary setup
failed before the native-operand comparison because the new fixture placed its
controlling main repository under the long test-method directory. Its derived
`.git/worktrees/<consumer-basename>` admin directory exceeded Git's limit.
Keep that setup run FAILED; it is not a result for `check-ignore`.

Only this newly introduced fixture may instead create its synthetic controlling
main repository in an unused `git-main` child directly under its already verified,
owned Copilot fixture base (length 109). Construct a fresh fixture; do not move,
rewrite or delete retained evidence. The tested consumer path stays exactly 256,
its `.git` path 261, with its basename and parent layout unchanged. This changes
the test controller's placement, not the tested product target or its Git argv.

Preflight and record the controller, derived admin and consumer paths/lengths
before setup. Use the same ordinary `git worktree add --detach <target> HEAD`;
no setup-only option, Git configuration change or further relocation is released.
If setup still fails, stop the linked-worktree branch with exact evidence.
Retain exact-root cleanup and unrelated sentinels. Keep the fixture-only layout
change separately attributable in the existing Copilot test/diagnostic scope.
The two-form native-operand implementation gate is unchanged; this does not prove
arbitrarily long controlling Git/admin paths or waive the earlier setup failure.

**Setup-only Git option after the second setup failure:** the fresh layout reached
base109/controller118/admin221 with the unchanged consumer256/`.git`261, but the
ordinary command then failed creating the consumer's `.git`. Keep this second
exit 128 distinct from the earlier admin-directory failure; neither executed the
native-operand comparison.

The original owner may now use the already accepted P03 fixture/trial pattern
(`agents/engineering/RegressionDetective.md` in `ce7415f`): Windows per-command
`-c core.longpaths=true` on this one synthetic controller's
`git -C <controller> worktree add --detach <unchanged-target> HEAD`.
This narrowly supersedes the no-setup-option restriction for that command only.
Keep controller118/admin221/consumer256/`.git`261, ordinary argument spellings,
the verified owned roots and all other arguments unchanged. No persisted option,
other setup flag or further relocation is authorized. Verify controller config
bytes remain unchanged, distinguish expected new worktree metadata from later
command-preservation evidence, and seal the fully constructed fixture before
checking the read-only command.

Run the already authorized `.git`-directory comparisons independently of linked
fixture construction; one setup failure must not suppress the other form's
diagnostic results. The product gate still requires BOTH forms. If flagged setup
fails, retain its exact outcome and stop that branch without a product change.
Fixture success alone never releases a `runtime_ignore_errors` option or alias.

## Operator decision: refuse unverifiable Git operations

**Explicit operator choice, 2026-09-21:** document the observed Git limitation,
stop safely and continue the remaining fixes. This supersedes the unsuccessful
conditional Git-option/native-operand/setup gates above; it does not relabel their
failures as passes or claim support for the 256-character Git-root case.

The frozen consumer product `a31c5eb` / report `f7d2151` remains preserved. The
original P10 owner may now change only `runtime_ignore_errors` and its directly
coupled preflight calls in `bin/li-copilot.py`, the two new Git diagnostic methods
and their private fixture support in `tests/integration/copilot-kit.py`,
`docs/lifecycle.md`, and the P10 report.

Use the actual unchanged Git invocation with the logical target. Distinguish
not-ignored output from unavailable/failed Git verification; report the latter
as an explicit error with the real exit/error context, not advice that falsely
attributes every failure to ignore-rule negation. No error or unavailable required
verification may become successful installation/check output. Verify refusal
before managed writes, inventory/receipt publication or recovery-store mutation,
including fresh init when an ignore rule would otherwise be added. Preserve
legitimate missing-rule repair, effective-negation refusal, plain-folder operation
and supported Git repositories. Do not add a public CLI/schema or blanket numeric
path cutoff. Do not add Git flags, native-C/cwd aliases, redirects or retry fallbacks.
Recovery operations that do not require this Git check remain unchanged.

Replace the two newly introduced success-only Git diagnostic oracles with
regressions for the approved behavior, retaining their earlier source/runs in
history. Exercise actual unchanged long-root Git failure at target256/`.git`261,
accurate diagnostics and zero target/config/index/home/store mutation; retain
supported short-directory/linked-worktree controls and valid/negated distinction.
A representable linked-worktree control is not acceptance of the long linked form,
whose Git setup/identity remains unverified. Do not fake that positive result,
hide it in a skip, or assert that every future Git version must have this limit.
Each fixture must independently establish Git's result before checking Lintel's
response. No additional compatibility/setup experiment is authorized.

Document the observed Git-for-Windows 2.55.0.windows.3 boundary as an external
operation limit, not a Python native-I/O defect or universal path-support claim.
Keep the original classic/default/canonical/recovery requirements and dimensions
unchanged. Run the affected final Git/preflight and installed preservation cases
on one frozen source; retain every previous failed aggregate. Freeze a new product/
report for complete SPEC and then eligible first whole QUALITY by the same reviewer.
This decision does not accept P10, release A13 or waive unrelated missing evidence.

## F02 preserve producer ownership decisions

**Authority:** original A12.1.a/.1.d, A12.2.b and R01, 2026-09-22. Independent
`c1a38a03e0efb54454e14bddee7cd07b4af6e1e3`, preserved report-only as `3649dc5`,
rejects `a559c9f` / `4c0519e3`: P1 F02, SPEC FAIL, QUALITY NOT STARTED.
The real installed caller/scaffold route overwrites eight existing files/fields
at plain target256 while returning complete/0; the same seeded short116 control
preserves them. No Git metadata is involved. Recoverable before-images do not
authorize replacement. The original owner read the complete report and proposed
the bounded producer correction below before implementation.

Own only `bin/li-lifecycle.py`, focused regressions in
`tests/integration/copilot-kit.py` and `tests/integration/universal-lifecycle.py`,
the directly related planning-boundary clarification in `docs/lifecycle.md`,
and P10's report. Preserve the current product/report and all earlier histories;
no new dependency import, amendment or reviewer's repair.

Within the lifecycle producer, the released functions/calls are:

- `destination`: native parent-type observations only, preserving root authority.
- `migration_changes` and nested `move`: native presence/type/enumeration after
  existing path checks, logical relative names, paired `read_owned` bytes/state
  for merge/redirect/delete inputs, and changes paired with their original expected
  states. Retain collisions, hidden-file coverage, stranded redirects/non-file
  refusal and byte-identical existing v5 marker comments.
- `scaffold`: native-aware existing/absent/non-file classification. Preserve
  existing regular template/rule seeds; absent seeds carry create-only `None`
  expectations. Combine migration changes without replacing their expectations.
- `runtime_publication`: require the producer's captured expectations; remove the
  late current-state reconstruction as a source of overwrite authority. Preview
  labels/modes and the existing `apply_files` call consume those expectations.
  Fresh observations may reject a stale plan, never upgrade create-only permission.
- The `main` migrate branch forwards the paired plan. `extension_pack`, the only
  other publication caller, retains create-only `None` expectations for its four
  outputs and native collision checks; no extension/profile redesign.

Use existing expected-before/read/write seams, not a new planner, receipt schema or
transaction engine. Keep F01 `migration_inventory`, the Markdown parser, `json_field`,
profile APIs, native performers, managed transaction implementation and all accepted
P03/P07 runtime blobs unchanged. If an unchanged read dependency cannot be guarded
through the existing seam, surface that exact constraint rather than inventing
permission, rewriting unchanged user files merely to create a guard, or broadening
the shared transaction contract silently. Unknown/inaccessible metadata must refuse.

Reproduce the actual installed/default-home caller with retained304 history into
identically seeded short116 and original plain256 targets. Preserve exact protected
bytes/modes, unrelated settings and surrounding content; only the documented
`autoMemoryDirectory` update is allowed. Keep caller/home/pin/source/child invariants,
mixed existing/absent seeds, repeat/check/dry-run, pointer-only/no-pointer behavior,
long legacy moves including nested hidden files, collisions and malformed settings.

At the same long target, test real late creation of an absent seed/destination,
and edit/delete of a merge input or legacy source between planning and publication.
Refuse explicitly and preserve intervening data before publication,
receipt/store creation or other target mutation. Use test-only interception with
the real writer, not modified shipped source or mocked successful writes. Retain
the original default/classic/canonical/Git-refusal/recovery controls and exact
dimensions; no Git experiment, shorter substitute or global/home action is released.

### Guard-only admission inputs

The frozen `apply_files` requires identical bounded write/expected/mode key sets
and validates those write paths before its snapshot/publication work. An unchanged
v5 marker is not a write path. The selected bounded contract is lifecycle-only
revalidation of captured guard-only inputs immediately before delegating to
`apply_files`, while passing the original write-set expectations unchanged.
Keep guard-only observations distinct from the write plan; do not insert no-op
consumer-file writes, enlarge the transaction key sets or add a shared API/receipt
dependency mechanism.

A marker or other guard-only change BEFORE that final consumer admission check
must refuse with no publication/store mutation. A write-set creation/edit/delete
AFTER consumer preflight but before the transaction check must still be refused
by the existing expected-state contract. If a marker is genuinely a write path,
it receives that same write-set protection, not the weaker guard-only treatment.

Do not claim that an unmodified read dependency cannot change after its final
admission check. A deterministic post-admission marker edit is outside this
guarantee; preserve the intervening marker and report the limit, not a fabricated
refusal/pass. Moving the same read check into a new transaction parameter would
only move that concurrency window, not supply cross-file atomicity. ADR-0030
already excludes multi-file atomicity. This clarification limits the new
guard-only oracle; it does not weaken any write-set late-change requirement or
excuse F02's existing-file overwrite.

Freeze one coherent product and separate evidence report with per-case RED/GREEN,
scope/AST/source identities and historical outcomes. The SAME P10 reviewer rechecks
F02 and completes remaining original SPEC, then the first eligible whole QUALITY.
Do not restart unrelated accepted components or mark P10/A13/master integration done.

Freeze a new combined product/report and enumerate dependency versus owned changes.
The same P10 reviewer must complete remaining SPEC before the first whole QUALITY
stage. Core acceptance does not close D01/D03/A23.4.p2, historical failed aggregates,
A13 or final integration. Generated outputs remain coordinator-owned.

Commit locally and report P10.md with exact per-operation changed/preserved behavior,
tests, revision, recoverability and remaining host limitations. Independent review follows.

## F03 truthful native layout observations

**Authority:** original A12.4.b/R01 and the operator's 2026-09-22 continuation.
Independent `c1f6bad1`, preserved as `5f3d885`, closes bounded F02 and retains F01,
but finds P2 F03 in the unchanged `layout_observation` reader. The real migrations
skill reports an existing version-5 marker and legacy lessons as not applicable
at plain target256, while identical short153 inputs correctly report incomplete.
This is a read-only diagnosis defect, not permission to expand Git compatibility.

The original P10 owner may change only `layout_observation` in
`bin/li-lifecycle.py`, its focused existing `MigrationInventory` tests, directly
related observation documentation if needed, and its report. Reuse accepted
native I/O and `read_owned` after existing path/ownership checks for marker,
legacy-file, type and directory-enumeration observations. Preserve logical names,
the observation schema and all current/incomplete/needs-migration meanings.
Inspection errors must remain explicit, not become a definitive absent or
not-applicable result. Do not modify the F01 catalog parser, outer doctor logic,
shared transaction/path/profile implementations, native installers or Git calls.

Reproduce the exact real skill/dispatcher short153 and plain256 pair before
repair. Verify genuine absence, current/incomplete/needs-migration, redirects,
nested/hidden legacy content and inaccessible or malformed observations, using
independently expected results and unchanged source/target/caller/home maps.
Use verified synthetic per-process execution for every product call, including
direct comparisons; no real-home access, cleanup or global setting changes.

Freeze one coherent product and separate report. The same reviewer `1578dfd8`
rechecks F03, retains applicable unchanged F01/F02 evidence, completes the
remaining original SPEC, then performs the first eligible whole QUALITY review.
The separate journal WinError5 and stopped aggregate remain unresolved history,
not a passing rerun or authorization for a new retry/transaction mechanism.
A13 and final integration remain gated until the actual independent verdict.

## F04 canonical redirect visibility

Independent `131d3db`, preserved as `4b5f592`, closes F03 but finds P2 F04:
the real diagnostic calls a canonical redirect current when its destination is
missing, while actual migration dry-run refuses that same short target. The
original owner identified the finite class: four `KNOWLEDGE_MOVES`, historical
ADR README-to-directory, generated README-to-file and ordinary/nested ADR redirects.

Release only `layout_observation`, focused existing `MigrationInventory` tests,
direct observation docs if needed and its report. Reuse unchanged canonical
mapping data and existing safe/native/read-owned observations; do not change
the migration producer, shared schema/path/profile/transaction/native/Git helpers
or coordinator preflight constants. One local classification path must inspect
the correct mapped destination rather than trust a known prefix.

A valid redirect counts as a stub only with its verified correct-kind/readable
destination. A genuinely missing destination remains legacy/incomplete or
needs-migration under the existing version rules. Wrong-kind, linked/reparse or
inaccessible observations fail explicitly. Near-match user content remains
ordinary legacy content. Handle the historical README directory form before the
file form; do not invent destination parsing or require content identity beyond
the producer's actual contract.

Reproduce F04's exact three-call positive/stranded/dry-run contrast. Verify the
finite canonical matrix at existing short153/plain256 dimensions, including
valid/missing destinations, kind/error/near-match and no-marker controls, retaining
all F01/F02/F03 tests and full no-write source/target/caller/home observations.
Freeze product/report for SAME reviewer `1578dfd8`; no general migration rewrite.

The reviewer may finish the deduplicated original adapter/profile SPEC selectors
listed in `P10-final-036e903.md` on its unchanged frozen source while this
independent read-only diagnostic repair proceeds. Reuse completed unchanged
evidence rather than restarting full suites. Do not terminate a separate valid
fixture merely because the known diagnostic finding remains open. A genuine
safety/permission boundary still stops its affected action. Final F04 recheck,
all remaining original SPEC and first eligible whole QUALITY are still required.

## B01 bounded transaction-journal replacement retry

**Authority:** the operator's recovery instruction to drive the initiative to completion,
with the 2026-09-22 speed-up correction, reconciled under L-038. B01 blocks the original
A12.2.a supported linked-worktree initialization, which is the operator's own setup. This
is not a Git, path or general Windows compatibility expansion. `P10-final-6e1b1e0.md`
closes F01-F04 with P1/P2/P3 0 and reports B01 as the only full-SPEC blocker.

**Observed fact, not a proven cause:** `apply_files` and `recover_transaction` rewrite
`journal.json` through `_save_journal` twice per file plus the state changes, roughly
885 replacements for the observed 441-file init. The three retained failures occurred at
different points; each was `WinError 5` replacing a fresh `.lintel-write-*` over the
owned journal. The captured journal was mode 0666 and not read-only. Accepted P03 precedent
`aa7fad4` fixed the same Windows error class for owned snapshot activation with bounded,
reported retries. This release applies the same bounded pattern and does not claim that
antivirus, indexing or any other process caused the failure.

The original P10 owner may change only `_save_journal` in `lib/managed_transaction.py`,
focused tests in the existing transaction/kit test files, directly related recovery
documentation if needed, and its report. Specifically:

- Retry only the owned journal replacement, while the operation lock is held, for OSError
  `winerror` 5/32/33. Allow at most four retries, reported on stderr, with the P03 backoff
  of 0.05 x 2^attempt seconds.
- Before each retry, verify the journal is still in its last saved state (absent before
  the first save); otherwise refuse without retrying.
- Any other error, a non-Windows error or a failure on the final attempt propagates
  unchanged, and the existing incomplete-transaction and explicit-recovery semantics stay.
- Do not retry target, source, staged-blob, plan, binding, snapshot or restore writes.
  Do not change the journal/plan schema or the shared `context_safety`, `snapshot`,
  path, profile, native or Git helpers. No generic retry wrapper.
- No antivirus, permission, process, ACL or global-state inspection, and no real-home access.

**Evidence:**

- Record a RED injected-failure unit test before the change.
- GREEN tests cover:
  - one to four transient failures that then succeed, with visible notices;
  - persistent failure exhausting the retries, with an incomplete journal that can be recovered;
  - a non-retryable error, which is not retried;
  - a changed-journal refusal.
- Rerun the real B01 selector `test_git_verification_directory_linked_and_plain_folder_controls`
  and the 036 inventory-traversal method, then the complete `tests/integration/copilot-kit.py`
  once. Use the unchanged synthetic dimensions and verified per-process isolation.
- Report the actual retry notices observed. A pass with zero retries is only a
  non-reproduction, not proof of the cause.
- Retain all WinError5 history, the failed aggregates and every F01-F04 result.

Freeze one product/report for SAME reviewer `1578dfd8`. It rechecks B01, reuses unchanged
evidence, completes the remaining original SPEC and then the first eligible whole QUALITY.
Any new actor or subagent uses `claude-opus-5.5`, reasoning `max`, context `long_context`
under the operator directive. A13 and final integration remain gated on that verdict.

## F05/F06 adapter expectations, strict native inventory and metadata payload

**Authority:** original A12.2.b and A12.4.a, the operator's instruction to drive the
initiative to completion, and SAME `1578dfd8`'s report-only `2685bd57` (parent
`72cf78a5`), `reviews/P10-final-dea408ef.md`, Git-byte SHA-256 `46dc734e…375f`.
It closes B01 within its scope and retains the F01-F04 closures. Final SPEC is FAIL and
the first whole QUALITY is FAIL, with P1=1, P2=1 and P3=0. Compliance has not started.

**F05 (P1).** `bin/li-copilot.py:1048` rebuilds every `expected` state from a fresh read
after the plan was derived from earlier reads. The originating reads are the protocol
merge at 806-839 and the preflight and planning at 957-1047. The reviewer's
deterministic source-entry probe inserted a synthetic late `AGENTS.md` instruction just
before that capture. The unchanged transaction accepted the edited 111-byte state as its
expectation, published the stale 12,850-byte plan across all 441 paths, printed ready
and exited 0. This happened before consumer admission, so the F02 guard-only limit does
not waive it. It is the F02 ownership class in the only other `apply_files` caller.

**F06 (P2).** `install/native.sh:169`, and the consumers at 424 and 546, skip every
row whose first field is `LINTEL-INSTALL`. Bash therefore accepts
`LINTEL-INSTALL\t1\nLINTEL-INSTALL\t1\n` and `check` reports verified bytes, while
`install/native.ps1:207-220` rejects the second row. The reviewer's evidence is a static
trace, not a native run. The coordinator's source read found the same omission class in
those three `while read` loops: they drop a final record that has no newline, which
PowerShell's `ReadAllLines` validates. A harmless Git Bash snippet reproduced that
`read` behavior; this was not a product run.

**A23.4.p2 payload seam.** Accepted integration line `7b0a30cf` makes the installed
adapter's `SOURCE_METADATA` require `config/aliases.yaml` and
`install/upstream-sources.yaml`. The installed `bin/_aliases.sh` already reads
`config/aliases.yaml`. The native payload carries `install/*` and
`.claude-plugin/plugin.json`, but not `config/aliases.yaml`, so installed init/check
fails after integration.

The original P10 owner may change only:

- the adapter publication producer in `bin/li-copilot.py`;
- the inventory reader and its consumers, the managed-namespace allowlist and the fixed
  metadata payload list in `install/native.sh` and `install/native.ps1`;
- focused regressions in the existing `tests/integration/copilot-kit.py` and the native
  installation tests (`tests/behavior/install-target-boundaries.sh`,
  `tests/integration/universal-lifecycle.py`, `tests/unit/native-receipt.ps1`);
- directly related text in `docs/native-installation.md` or `docs/copilot.md`;
- its report.

Specifically:

- **F05:** pair every planned mutation with the exact state observed when its bytes or
  decision were derived: managed files, protocol blocks, create-only seeds (`None`),
  stale deletions, the `.gitignore` and `.gitattributes` appends, and the inventory.
  Pass those captured expectations to the unchanged `apply_files`, and remove the
  line-1048 current-state reconstruction as overwrite authority. A fresh observation may
  reject a stale plan, but never replaces or upgrades a captured expectation. Unknown or
  inaccessible metadata refuses. Final-admission guard checks keep their F02 semantics.
- **F06:** consume exactly one `LINTEL-INSTALL\t1` header. Strictly validate every
  following record, including a final record without a newline, under the shared
  three-field grammar in both performers and every consumer. A header-shaped body row is
  malformed. No record may be silently omitted.
- **Payload:** add the exact path `config/aliases.yaml` to both performers' fixed
  metadata list and managed allowlist, with directly related documentation. Do not
  change the integration-owned `SOURCE_METADATA` or add any other path.
- **Keep unchanged:** `lib/managed_transaction.py` including B01, the F02 lifecycle
  producer, adapter output bytes, the adapter and native inventory schemas, clients and
  default kit content. No new planner, receipt, engine, shared API or native matrix.

**Evidence:**

- RED first for both findings. F05 uses the reviewer's late-edit class through test-only
  interception with the real writer. F06 uses a duplicate header row and an
  unterminated malformed final record through the shipped Bash reader, and through
  PowerShell 7.6.6 where the approved host is available. Windows PowerShell 5.1 stays
  denied and unverified.
- GREEN: a late edit, creation or deletion of each F05 write class refuses before
  publication or store mutation and preserves the intervening bytes. Both native
  performers refuse both malformed inventories before any write, and `check` never
  prints success for them. A native install lists, installs and verifies
  `config/aliases.yaml` through the managed inventory.
- Ordinary init, upgrade, repeat, check, dry-run and valid native install/check are
  unchanged.
- Rerun the complete `tests/integration/copilot-kit.py` once with the unchanged
  synthetic dimensions. Retain B01, F01-F04 and all earlier history.

Freeze one product/report for SAME `1578dfd8`. It rechecks F05, F06 and the payload item,
reuses unchanged SPEC evidence, and then runs the first eligible whole QUALITY and, only
if eligible, compliance. Any new actor or subagent uses `claude-opus-5.5`, reasoning
`max`, context `long_context`. A13 and final integration remain gated on that verdict.

**Frozen fix and coordinator intake (2026-09-24).** The owner froze product `a2df2021`
(tree `6eb50f94`, sole parent `72cf78a5`) and report-only child `56346ad3`. The child
changes only `reports/P10.md` (1,771 lines, Git-byte SHA-256 `c95590ba…d5f4aa`). The
owner sent the handoff to SAME `1578dfd8` and the coordinator in parallel. The
coordinator's data-only intake passes:

- The product changes exactly the eight released paths (+402/−69), and all eight
  Git-byte SHA-256 values match the handoff.
- `lib/managed_transaction.py` (with B01), `lib/context_safety.py`,
  `bin/li-lifecycle.py`, `bin/li-adapter.py` and `config/aliases.yaml` are unchanged.
- The payload item adds `config/aliases.yaml` as a required source file to the
  native payload lists in `install/native.sh` and `install/native.ps1`.

The builder reports RED for both findings, then GREEN and one complete `copilot-kit.py`
run at 46/46 (7,352 s). The run shows zero journal retry notices; that is a
non-reproduction, not proof of cause. Builder passes are not acceptance. SAME's recheck
and first eligible whole QUALITY follow.

**Final review and integration (2026-09-24).** SAME `1578dfd8`'s final review is the
report-only `f460eb9f` (parent `56346ad3`), `reviews/P10-final-a2df202.md`, 314 lines,
SHA-256 `57a1e46d…010b64aad`.

- Original SPEC PASS for all ten A12 subleaves and the four parents.
- Whole-P10 QUALITY PASS, with new findings P1 0, P2 0, P3 0.
- F05 and F06 are closed, and the F01–F04 and B01 closures are retained.
- Compliance is UNVERIFIED, with clearance blocked. Nine scoped controls pass, but no
  required-policy source, version or applicability is resolved, and live dependency
  advisory freshness is unverified. This is neither a finding nor a clearance. It goes to
  A23 and delivery as an explicit limitation.

The coordinator's intake passes: sole parent, only the review path, and the hash and size
match. The coordinator integrated P10 in `68d32240` (tree `f79d40a9`):

- The seven shared-state files resolve to recovery.
- The product and test files take the verified trial resolution `1d968a2a`. Its five
  union files apply P10's final delta (`72cf78a5`→`56346ad3`) to the earlier resolution.
- The catalog is regenerated for P10's changed descriptions. `li-catalog.py --check` and
  `li-wiki-gen --check` pass. The wiki check needed `TMPDIR` set, because Git Bash's
  `mktemp` found no `/tmp` in this shell.
- All 1,124 paths follow this rule, and the tree differs from the tested trial only in
  four `.claude/` paths.

`3397311f` then preserves the earlier FAIL review `2685bd57`. The complete
`copilot-kit.py` runs once on `3397311f` with PowerShell 7, and the light checks run on
the product-identical trial tree. A12.1–A12.4 close after these joined checks pass and an
independent review of the integration.

**Integration finding and reconciliation (2026-09-24).** On the trial tree, the light
run's `universal-lifecycle` entry failed twice:

- The first run hit the host `/tmp` defect (L-049): 97 failures, 94 of them stderr
  assertions receiving the Git Bash warning, plus three native Bash-performer installs.
- After the repair, 65 of 68 tests pass. The same three native tests still fail with
  "Unsafe managed path: audit".

The cause is CRLF in the source copy:

- The test copies its trusted source from the working tree. Git's Windows checkout gives
  `install/directories.txt` CRLF line endings (294 bytes, 21 CRLF).
- `native.sh` reads that file line by line (line 395), and `native_relative` rejects the
  trailing carriage return (line 74).
- On Windows, `install.sh` normally dispatches to the PowerShell performer, so this is not
  a default Windows install failure. But the Windows CI job checks out with CRLF and
  would fail these tests.

Coordinator reconciliation `756835f6` pins `install/directories.txt` to LF in
`.gitattributes`, like the existing `*.sh` and `bin/*` rules. It is the only repository
file that `native.sh` parses line by line. On a scratch worktree with the attribute, the
file checks out as LF (273 bytes, no CR), and the three tests get past the path check.
Their Bash-performer installs then exceed the test's 600-second timeout, while the
complete kit runs concurrently on the loaded host. The three tests rerun after the kit.
SAME `1578dfd8` assesses the reconciliation in the integration review.
