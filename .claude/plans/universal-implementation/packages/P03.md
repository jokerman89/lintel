# P03 - Safe context and attributable recovery

**Owner:** assigned isolated lintel-builder session. **Status:** authorized.
**Task authority:** ../plan.md A01/A11. **Sources:** audit CP-01/02/05/13/16,
AG-01/12 and RU-07; existing context checkpoint ownership tests.

## Ownership and preservation

Own `skills/context-*`, `skills/perf-mode`, `skills/safe-install`, `bin/_context.sh`;
the unsafe recovery portions only of `agents/engineering/Refactorer.md`,
`agents/engineering/RegressionDetective.md` and the repository's Migrator role;
new minimal `bin/li-snapshot.py` and `lib/context_safety.py`/`lib/url_policy.py` only
where needed; `tests/unit/context-safety.sh`, `tests/unit/snapshot-ownership.sh`,
`tests/unit/url-policy.sh` and same-name Python support. Existing context tests may
gain regression cases. Report in `reports/P03.md`.

No work-map, pack, review, routing, generated or shared-plan edits. Full role enrichment
belongs to P09 later. Inspect existing helpers before adding one; avoid duplicate checkpoint
implementations. Host/work contract adaptation is reconciled after P06/P08, not guessed.

Preserve literal/glob context selection, warm/save/restore/budget/cool entry points,
checkpoint ownership, historical restore ability, perf-mode's useful advisory intent,
and isolated bisect/refactor expertise. A restore never discards someone else's changes.
Use synthetic fixtures only, no live URL or destructive experiment on this worktree.

## Leaf contracts

| Leaf | Depends on | Observable acceptance | Verification |
|---|---|---|---|
| A01.1.a | None | Literal/glob API keeps spaces and rejects execution semantics | `bash tests/unit/context-safety.sh`: metacharacters cannot create a marker |
| A01.1.b | A01.1.a | Warm entry points call the tested selector and show actual selected source set | Same script: spaces, valid globs, absent files and scoped selection |
| A01.2.a | None | Snapshot records normalized owned files and hashes, rejects traversal/reparse escape | `bash tests/unit/snapshot-ownership.sh`: malicious/outside manifests refused |
| A01.2.b | A01.2.a | Snapshot bytes equal selected original bytes; stale/partial snapshot not success | Same script: compare digest, incomplete manifest and prior snapshot cases |
| A01.2.c | A01.2.b | Restore preflights ownership and changes; conflicts preserve current files | Same script: unrelated edits, edited owned file and simulated interruption |
| A01.3 | A01.1.b,A01.2.c | All boundary cases hold together and old supported snapshots have explicit handling | Both scripts, positive restore and failure recovery |
| A01.4 | None | Bisect uses isolated checkout and exits bisect; repair/rollback scope is attributable | Context-safety fixture plus scenario review of the three role instructions |
| A01.5 | None | Exact host differs from explicit wildcard; redirects revalidated before following | `bash tests/unit/url-policy.sh`: sibling host and redirect denial, allowed host succeeds |
| A11.1 | A01.1.b,A01.2.c | Existing owner-aware checkpoint path is reused, bounded sources shown | `bash tests/unit/context-repository-ownership.sh` and checkpoint roundtrip |
| A11.2 | None | Unknown capacity never becomes an asserted client limit | Context-safety fixture for unavailable host capacity; explicit estimated case |
| A11.3 | A11.2 | perf-mode retains invocation with truthful resource advice, no fake setting | Focused fixture and cold instruction scenario |
| A11.4 | A11.1,2,3 | Cooling reports future selection/disk effects, not freed already-sent context | Scenario review and focused negative assertion |

Run `bash tests/unit/context-checkpoint-roundtrip.sh` and
`bash tests/unit/context-repository-ownership.sh` on final package. Documentation-only
promises are insufficient for helper behavior; exercise the implemented functions.

## Report and stop

Commit locally with focused changes and report per-leaf acceptance and preserved
behavior, exact revision, commands/results, failure boundaries and unsupported old formats.
Do not activate global changes or publish. Keep true host limits explicit. Stop for
independent review; no self-approved completion.

## Read-only joined Windows boundary investigation

**Authority, 2026-09-21:** investigate only; no new product repair is authorized.
Earlier P03 acceptance remains its bounded historical result. New immutable evidence
is P08 product/report `8d477701` / `062ffc6c`, dependency control `f9685530`,
and P10 independent report `1d871338` (preserved as `46e8b0f`).

P08 reproduces checkpoint reservation failures at 268/278/277/290 characters and
a distinct Git bisect ref-lock failure at 283 characters, on both candidate and
same-depth unchanged dependencies. P10 default setup fails at a 260-character
snapshot-blob destination before target publication; its explicit-store control
does not establish default acceptance. These observations do not yet establish
one root cause or authorize a global setting change.

The original P03 owner may perform a bounded read-only source/contract investigation
and disposable reproductions from the current coordinator baseline. Verify synthetic
HOME/USERPROFILE and all derived Lintel/Git paths before every invocation. Preserve
actual lengths/defaults, distinguish Python checkpoint/snapshot I/O from Git behavior
and fixture cleanup, and record target/lock/receipt preservation plus exact revisions.
Do not import P08/P10 WIP, modify P03/P07 or other product/tests, shorten an acceptance
path, change LongPathsEnabled/core.longpaths globally, or access actual home state.

The only repository output is a new
`reports/P03-joined-path-investigation.md`. Report confirmed cause versus uncertainty,
existing support constraints, separate ownership and viable minimal repair alternatives
with risks. A local per-command diagnostic may not be represented as a shipped repair
or new default. Stop at that report for a coordinator repair card; no acceptance waiver,
nested investigator, broad cleanup, rollback or publication.

## Coordinated joined-path repair, first implementation unit

**Status:** authorized after design-only proposal and ADR-0031, 2026-09-21.
Evidence remains `990b0daa` / coordinator `7516db4`; diagnostics are not acceptance.
The original P03 owner is the sole writer for this first unit. Preserve its accepted
and investigation branches; start a clean continuation from the coordinator's supplied
immutable baseline. Do not import P08/P10 WIP.

### Exact shared API and policy boundary

Add `lib/native_paths.py` with only `path_identity(PurePath) -> tuple[str, ...]`
and `native_io_path(Path) -> Path`, using the accepted P07 representation logic.
Python 3.9-compatible stdlib/deferred annotations; `ValueError` for representation
errors. No I/O, root/home/cwd inference, resolution, mutation, fallback destination,
configuration or generic filesystem facade. Case-preserving accepted ordinary/
extended drive and UNC identities; no arbitrary namespace stripping.

In `lib/profile_context.py`, only extract/delegate `_path_identity` and
`_native_io_path`, retaining compatibility names and `ProfileError("PROFILE_IO", ...)`
translation. No profile selection, generation, history, digest, source/target or
containment-policy change. P03 must not depend on P07's private internals.

P03 keeps its existing authority/ownership checks. Native ancestry/stat/resolution
must support those checks before mutation. Keep logical and I/O variables separate;
rebuild logical paths after iteration or temp-name return. Existing ordinary and
explicit-extended selections and all serialized identities must remain compatible.
If that requires another public primitive or a format/policy change, stop that part
and return the specific conflict before implementing it.

### Owned product and fixture paths

- `lib/native_paths.py`, `lib/context_safety.py`, `bin/li-snapshot.py`: complete
  relevant exists/stat/link/resolve/open/read/mkdir/temp/chmod/replace/iteration/
  samefile/unlink/lock/journal/retention I/O while retaining selector DP, bounds,
  retries, preflight and consumed restoration permission.
- `lib/profile_context.py`: only the extraction/wrapper seam above.
- The isolated bisect recipe in `agents/engineering/RegressionDetective.md` and its
  existing context-safety regression: Windows-only per-command Git option for the
  owned trial/admin flow, no persistent configuration or caller-state mutation.
- Direct context/snapshot support documentation; no broader role enrichment.
- Existing P03 context/snapshot tests, P07 path-identity tests, and fixture-only
  native cleanup/resource plumbing in `tests/integration/universal-profile-context.py`.
  Do not alter its validation method or any other P07 scenario semantics.
- Resource-copy list and exact-created-root `Fixture` cleanup registration only in
  `tests/unit/universal-trusted-tools.py`; no P01 product, test-body assertions or
  fixture path/depth changes. Retain `TemporaryDirectory` readonly/error handling,
  verify the same owned root before using its native spelling, and never suppress
  cleanup errors. The copied-resolver cleanup ERRORs remain historical.
- Resource-copy list only in `tests/integration/pack-source-target-resolution.sh` and
  `tests/unit/wiki-gen-idempotency.sh`.
- Required-source entry only in `bin/li-copilot.py::ADAPTER_RESOURCES` and
  `tests/integration/copilot-kit.py::JOINED_RUNTIME_RESOURCES`; no installer engine,
  navigation/provider, transaction or caller/child behavior changes.
- Own `reports/P03-joined-path-repair.md`; no shared ledgers, memory, ADRs or reducers.

These narrow cross-owner seams are coordinator-authorized; other package ownership
remains unchanged. P10's expanded copy lists and direct `managed_transaction`/
`li-copilot` consumers remain with P10 for a later exact-dependency handoff.
P08's private-alias fixture import remains with P08. Do not fix those in a second tree.

### Short refinements and evidence

Plan.md owns checkboxes. A01.2.w1 extracts/verifies the pure boundary and unchanged
P07 identities; .w2 repairs complete P03 snapshot I/O and ownership/replay negatives;
.w3 establishes faithful fixture cleanup and trusted-source closure.
A11.1.w1 verifies the actual checkpoint shell consumers at the recorded long lengths.
A01.4.w1 verifies real isolated bisect success/error at the 283+ ref length while
preserving caller HEAD/index/config/staged/unstaged/untracked bytes and modes.
A23.4.p1 records this unit's independent core/dependency acceptance; .p2 remains the
later P10 full default init/check/recovery and canonical-shell consumer gate.

Reproduce the exact 259/260 and 268/278/277/290 checkpoint cases, 218->260 snapshot
publication and 283-character Git ref boundary before repair. Cover long parents,
temp/lock/record/history paths as well as final filenames. Run actual create/read/
bind/restore/retention and inherited ownership/roundtrip/memory/bisect assertions;
syscall-only or shorter-path controls do not close them. Preserve real and injected
namespace/ADS/reserved/case-sensitive/link/junction/outside-root refusals and late
user edit/interrupted restore/replay behavior.

The existing native case-sensitive-sibling test may enable case sensitivity only
on its own empty disposable directory, under existing permissions, then verify and
clean that exact fixture. This is fixture metadata, not OS/global policy authority.
No elevation, host setting or permission workaround is allowed; a denied operation
remains explicitly unrun. Do not change the assertion or treat injection as that
native observation.

An explicitly labelled disposable composition of frozen P10 `c1de05c7` plus ONLY the
candidate core dependency may diagnose downstream default setup at the original
dimensions. It is not P10 approval; if direct P10 I/O still blocks, preserve evidence
for its owner rather than widening this unit. No P10 parser/native/transaction edits.

Before every test/hook, verify allowlisted synthetic homes and all effective derived
paths. No real-home effects investigation, global settings, shortened defaults,
pruning old failed fixtures, or implied Python 3.9/UNC/other-OS execution.
Freeze exact product/report commits and request the original P03 reviewer for complete
scoped spec then quality, including the P07 extraction and cross-source closure.
Later dependent owners and final integration must rerun their real consumers.
