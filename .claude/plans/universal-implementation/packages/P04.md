# P04 - Preserved Swarming merge and safe handoffs

**Owner:** assigned isolated lintel-builder session. **Status:** authorized early checkpoint.
**Task authority:** ../plan.md A21/A22. **Sources:** full audit reviews/swarm.md,
swarm-preservation.md, action-plan.md, ADR0008 and main ADR0026.

## Two checkpoints, not a dependency cycle

First preserve ancestry and reconcile the existing implementation, fixing SW-01/02/04/05/06
and unsafe envelope recipes with tests. A22.5 may implement the source/result identity
mechanics that are local to the swarm contract, but final reuse of P05 review and P08 work
contracts is A22.7 and remains open until those packages are available. Do not delay the
historical merge or declare final A22 completion prematurely.

## Ownership

Merge `codex/swarming-work` at `275a35447c4ad271e05816ade43ac48f1acec24f` into this
child branch whose base contains main `28061e4` and the preserved audit/implementation plan.
Use a real merge commit, not cherry-picking or replacing current main with old copies.
Do not touch the original Swarming worktree or its branches.

The 76 original delta paths may change solely for faithful merge reconciliation. Preserve
the coordinator's active Universal plan/memory if historical todo text conflicts; retain
Swarming's historical content and link it as history. After that merge, product edits are
limited to `lib/swarm*`, `bin/li-swarm*`, swarm templates, `skills/swarm`,
`docs/concepts/swarming-work.md`, swarm tests, `lib/brief-forge*`, envelope helper/schema,
`skills/brief-forge`, its concept docs/tests and ADR-0027. Record directly coupled source
changes needed in li-work-artifacts/li-copilot without overwriting later fixes.

No ongoing shared plan, memory or generated-output edits beyond resolving the original
merge. Coordinator regenerates shared catalog/adapters/protocol consumers after fan-in.
No other agent roles, context/snapshot, sync tools, domain-hook/vault/updater changes.
No remote calls, user-global hooks, private packs or network publication.

## Leaf contracts

| Leaf | Depends on | Observable acceptance | Verification |
|---|---|---|---|
| A22.1.a | None | Both recorded heads identified; merge preserves both ancestries | `git merge-base --is-ancestor` for main baseline and Swarming head |
| A22.1.b | A22.1.a | Current hybrid/session protocol retained with Swarming additions | Compare merge hunks to both parents; protocol source consistency checks |
| A22.1.c | A22.1.a | Current enterprise helpers retained plus compatible swarm pointers | Existing work-map and Copilot tests; no restoration of old helper versions |
| A22.1.d | A22.1.b,c | All 76 paths map to retained location/meaning and historic artifacts stay available | Write reports/P04-preservation.md with every source path and destination/evidence |
| A22.2 | A22.1.c | Main ADR0026 remains; Swarming decision becomes ADR0027 with historical origin explicit | `bash tests/shape/adr-numbers-unique.sh`; resolve all affected intent references |
| A22.3.a | A22.2 | Report/review cannot alias authority files/parents or other actors | `python tests/unit/swarm-contract.py`: SW-01 exact/alias negatives |
| A22.3.b | A22.3.a | Coordinator-generated outputs and reviewer attribution are protected | Same suite: SW-04/06 worker/reviewer scope positives and negatives |
| A22.4.a | A22.2 | Actual flat/phased/tree/Spec Kit leaf formats accepted | Same suite: main template numeric IDs and existing singleton fixtures |
| A22.4.b | A22.4.a | Package maps preserve each leaf's acceptance/dependencies and aggregate review depth | Same suite: missing-leaf and package-scope cases |
| A21.1 | A22.1.c | Brief Forge remains explicit opt-in; no imaginary automatic dispatcher | Actual call-path inspection and dormant-mode tests |
| A21.2 | A21.1 | Invalid/forbidden payload blocked before full audit/output | Focused envelope tests: marker/sensitive fixture not present in output/audit |
| A21.3 | A21.2 | Real Markdown brief converts to valid structured fields, original preserved as data | Real template -> producer -> validator -> evaluator integration |
| A21.4 | A21.3 | Missing/unknown evaluator or audit error fails explicitly; caller owns dispatch | Focused integration cases and trusted source-root propagation |
| A22.5 | A22.4.b | Attempt/acceptance/result/report identity invalidates stale evidence; no-change work is valid when explicit | Swarm suite: edited acceptance/result, nonexistent result and verification-only cases |
| A22.6 | A22.3.b,A22.4.b,A22.5 | Native isolated, serial and manual reviewer handoff remain usable after interruption | `python tests/integration/swarm-workflow.py` plus temporary Git worktree fan-in fixture |
| A22.7 | P05/P08/P09 integrated | Reuse shared contracts, complete preservation and joined enterprise/hybrid/swarm verification | Later coordinator follow-up; remains pending at first checkpoint |

Do not fake base/head or reviewer evidence in historic reports. Keep them historical;
new evidence identifies the new attempt. A read-only helper is not a scheduler. Missing
native delegation still allows decomposition, briefs, status and recovery; independent
substantive review stays outstanding until a real reviewer acts.

## Report and stop

Preserve the merge commit and add focused fix commits. Report both merge parents, every
finding's status, leaf results, exact commands, preservation map and final integration
dependencies in reports/P04.md. Do not claim A22.7 complete or push/PR. Send the early
merge checkpoint as soon as stable so dependent contracts can start; continue only within
this card. Independent review is performed by another agent, not by this implementer.

## Shared-evidence consumer unit

**Authority:** existing A22.7, recovery continuation of 2026-09-22. The original
owner's read-only proposal is accepted for the following dependency-ready unit.
P05 v2 review/QA, P07 v1 profile and P09 v1 domain data are independently accepted
on recovery `42900fa`; the new P08 work-context/resume seam is not yet accepted.
This releases shared-evidence consumption, not final A22.7 completion.

Own the existing Swarm contract/snapshot/CLI/schema/templates, its skill/concept
documentation, focused Swarm tests and P04 report. Preserve existing inspection,
topology, scope, package parsing and rich reports. Reuse P05 context/snapshot/work
binding, immutable QA obligations, latest-log reader and verified review/QA;
reuse P07's explicit live reference and required-policy verification; consume
P09 results as non-clearing domain observations. Do not introduce a replacement
hash, policy/parser, latest-review selector, scheduler or shared backlog.

Use accepted original work-map/package APIs and caller-selected P05 contexts for
this unit. Do not import, execute, copy or emulate the unaccepted P08 reader.
The CLI may pass validated selected data into its contract; the contract must not
import a reader that calls back into Swarm validation. Final newer P08 work/resume
binding waits for its independently accepted release, with a focused later join.

Keep old v1/v2 artifacts intact as attributable local observations/history, not
automatically corroborated shared clearance. Separate retained local inspection
from new shared acceptance explicitly. Every acceptance-capable status/frontier/
close path must use the same shared gate; missing or later rejecting evidence
cannot be hidden by a different command. New expected/QA/corroboration pointers
are coordinator-owned and review JSON is reviewer-owned, with the existing
alias/ancestor/link/hardlink protections. Different version domains remain separate.

Use the proposal's noncircular order: initial context, actual reports/results,
external final preparation, fresh domain/QA verification, then actual independent
review and separately supplied corroboration. No-domain work uses ordinary P05
QA. Verification-only work needs no invented product change. Raw P05 snapshots
and P04's normalized local digests are not interchangeable; stage/commit/fan-in
changes may require fresh context and integrated review. Profile pins remain
target-specific. Surface a genuine shared-provider incompatibility rather than
silently repairing its implementation or adding fallback clearance.

Retain actual Git fan-in/conflict/recovery, isolated/serial/manual use, all original
ownership/type/mode/symlink cases and the 76-path/17-artifact preservation record.
Add real accepted-producer tests for missing/retyped obligations, later rejection,
changed attempts/outputs/profile and absent/failed domain checkpoints. Synthetic
actor strings are not proof of independent people. All calls use verified
synthetic process roots; no real-home, hook, remote or production action.

Start from the exact accepted recovery dependency using a clean ancestry-preserving
merge into the same original owner lane; preserve its existing commits. Freeze a
coherent product/report for the same independent reviewer. Later accepted P08
binding and joined enterprise/hybrid/Swarm evidence still gate final A22.7.

## F06/F07 shared-consumer correction

Independent `63cf1bb`, preserved as `e20ada2`, rejects the shared unit with
P1 F06 and P2 F07; QUALITY has not started. The original owner may now repair
only its Swarm contract/shared-consumer functions, focused existing shared tests,
directly related wording and report. Shared P03/P05/P07/P09 implementations,
schemas, current P08 providers and generated outputs remain outside this repair.

F06: runtime stays protected by default. The reviewer receives only its exact
declared ordinary canonical review JSON slot plus its existing Markdown review,
not any runtime object reachable through an alias. Reuse accepted rooted path/
native metadata helpers in one shared-destination guard at topology validation,
actual reviewer changed-path admission and consumption. New mutable shared JSON
destinations must have safe ordinary ancestors and, when present, a regular,
uniquely owned leaf. Reject symlink/reparse, hardlink, special/non-file and
uninspectable aliases before granting scope. Missing planned files are allowed
only through safe ordinary ancestors.

Coordinator metadata pointers are references, not grants of worker/reviewer
ownership of state/audit/jobs. Retain all existing authority/reducer/cross-lane
physical comparisons outside the narrow valid slot. An alternate changed filename
resolving to the same review slot is not permission. Keep this stricter rule
scoped to new shared metadata; preserve legitimate legacy product-data symlink,
type/mode and snapshot semantics.

F07: accept a caller-selected P05 directory that actually covers the whole lane
scope, including descendants/future files. Do not require redundant exact child
selectors when the actual P05 verifier accepts a complete parent. Still reject
missing/partial child coverage, siblings or misleading textual prefixes.
Use the accepted validated snapshot/selection semantics, not a new acceptance
hash, alternate parser or weaker raw identity.

Reproduce F06's six real link cases through both actual public gates, with
unchanged protected sentinels, and F07's real P05-positive parent selection through
all four shared consumers. Add the bounded parent/leaf/reverse-alias/type and
positive ordinary/coordinator-slot contrasts from the owner's proposal; retain
old product-link, 76-path/17-artifact and shared-provider cases. No broad
filesystem/permission experiment, new schema or scheduler.

Freeze repair and report for SAME reviewer `ed672f58`, complete released-unit
SPEC before first eligible QUALITY. The independently accepted P08 provider may
be used in a later explicit final join only after this shared unit passes;
neither these fixes nor the earlier component acceptance closes A22.7.

## Final selected-work and cold-resume consumer join

The shared unit passes independent SPEC/QUALITY in `bb939e55` at `4f10837`,
integrated as `1f1b0d2`; eight joined real provider/guard cases and explicit
required-source preflight pass. The report's separate domain-fixture path failure
and shorter private rerun remain distinct, with no arbitrary-length guarantee.
Release the original owner's minimal final consumer proposal using the already
accepted P08 provider `70469296` / integration `e1edc0c` and P09 data core.
The unaccepted P09 module unit and whole P08 semantics are not imported.

Compose at `bin/li-swarm.py`, not by importing a callback reader into
`swarm_contract` or `swarm_evidence`. All accepting status/wave/resume/verify
entries use the actual trusted `work_context` to check original selection and
coordination backpointer/artifact/package/leaf identity. An optional explicit map
must agree; absence may use only the already explicit validated coordination
map, never recency, environment guessing or an unrelated active initiative.
Source-status checkboxes remain non-clearing. Preserve the same existing shared
P05/latest-log/QA/P07/P09 gate after that preflight, not another identity hash.

An explicit cycle ID on resume uses the actual trusted `workflow_resume` in a
fixed, quoted subprocess with explicit source/target/Python/profile paths and
the declared state location. Preserve returned phase/operation/profile/policy
and verify their selected map/artifacts, without bootstrap, ledger writes or
permission escalation. Missing/mismatched/drifted requested cycles fail. With no
cycle request retain labelled artifact-only serial/manual recovery; do not call
it persisted-cycle recovery. Inspect/brief/snapshot/review-input remain useful
non-clearing operations.

Close advisory F08 by consuming actual P05 `validate_shape(value, "skill")` and
referencing its canonical skill definition from the Swarm schema. Do not copy
another grammar, change P05 or retain P04's incompatible length/name limit.
Test actual CLI/provider agreement for invalid and valid skill names.

Own only the CLI, F08's contract/schema declaration, focused shared/shape tests,
Swarm skill/concept documentation and report. Existing `swarm_evidence.py`,
all P03/P05/P07/P08/P09 providers, ledgers and generated output remain unchanged
unless a concrete seam defect returns for separate coordination. Merge the exact
accepted recovery dependency normally, preserving original histories.

Verify real original-map/grouped/tree/Spec Kit and two-initiative links, map/coord
and missing/empty/budget/hostile-source refusal, genuine progress reuse versus
acceptance drift, and actual interrupted-cycle fresh-process resume with old/
decoy/lost-pin/drift/truncated/absent controls and unchanged state. Retain actual
Git fan-in/conflict/recovery, shared reprepare, serial/manual/corroboration and
verification-only/no-domain cases. Exercise real P09 data through the cycle-aware
consumer without calling it a live specialist or accepting an unknown result.
Use the exact accepted 22-provider runner and work-artifacts tests where relevant.

Every call uses verified synthetic process roots; no platform investigation,
new actor, publication, private profile or hook activation. Freeze coherent
source/report for SAME reviewer `ed672f58`; final A22.7 closes only after exact
join acceptance and coordinator integration evidence, not from this release.
