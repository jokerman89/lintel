# P13 - Discovery, optional capability selection and provenance

**Authority:** A18/A19/A20.1/2 and compatibility integration with P07 A20.3/4.
**Prerequisites:** P06/P07/P09/P10/P11/P12 have stable interfaces.

## Compact discovery first unit

The final capability selection/install/version/preservation mapping retains those
prerequisites. A disjoint read-only discovery unit is released now against the
accepted P06 registry, existing frontmatter and generator. Own only
`bin/li-catalog.py`, `skills/catalog`, `skills/help`, `skills/skill-router`,
direct catalog metadata tests/reference, the directly scoped provenance guidance/
registry checks described below, and P13's report.

Extend the EXISTING deterministic catalog implementation with a documented compact
metadata output/query path and make these three callers consume it before loading
only the selected canonical bodies. Preserve ordinary Markdown generation and
`--check`; do not hand-edit generated output or introduce a second inventory/parser.
Source and data roots must remain explicit and trustworthy. Reads/querying must
not regenerate files, load every prompt body into model context, execute target
code, activate a plugin, write markers or claim that source declarations are live
host capability. Preserve existing aliases and unknown/staged descriptions.

No skill/agent body corpus, P08 intent classifier/state, profile, registry, installer,
shared domain/design/format schema, generated surface or version bump is released.
Do not invent the final capability manifest or current maturity while other
producers are still changing. Optional install selection, native wrapper/asset
closure, status/welcome integration and the final 126-row map remain later work.

Read and preserve the coordinator's A20 provenance correction and actual existing
notices. Correct only a concrete remaining source/notice guidance discrepancy;
do not import third-party content or guess historical import commits. A requested
distribution/installer seam is reported to its owner, not changed in parallel.
Metadata/reference tests remain distinct from actual packaged notice acceptance.

Verify metadata-only reads, literal query/filter behavior, deterministic/default
output preservation, empty/malformed input refusal and no source/target writes.
Use synthetic fixture sources plus the actual current canonical tree; do not
advertise future producer/interface support from fixtures. Freeze this unit for
independent review and continue in the same owner after later releases.

Coordinator already corrected the scoped A20.1/A20.2 original-only/similarity claims and
added existing design-dna provenance without importing third-party content. Read
`reports/A20-provenance.md` and retain those changes; independent P13 acceptance still
needs the actual distributed-notice/selection test. Historical import commits remain
explicitly unknown rather than substituted with the later comparison SHAs.

## Ownership

Own capability selection metadata/schema and shared catalog generator `bin/li-catalog.py`,
catalog/help/router/status metadata integration with P08, `skills/catalog`, `help`,
`skill-router`, `skillify`, `uniformity`, `eval`; `bin/li-uniformity` where directly
coupled; provenance registry/notices, obsolete-method archive annotation, public
version metadata and package/migration docs. Coordinate native wrapper/generator changes
with the released P06/P10 implementation. Generated artifacts remain coordinator-owned.
Do not take underlying specialist implementations, role corpus or effective profile parser.

## Leaves and acceptance

| Leaf | Dependency | Observable acceptance |
|---|---|---|
| A18.1 | P06/P09 | Optional capability metadata names source, dependencies, maturity, inputs/outputs and example |
| A18.2 | A18.1,P07 | Core lifecycle/security remains shared; company identity is a separate overlay |
| A18.3 | A18.2,P10 | Consumer selects relevant capabilities without loading unrelated role/prompt bodies |
| A18.4 | A18.3 | Every moved/aliased use case retains method, discoverability and dependency assets |
| A19.1 | A18.1 | Existing generator is canonical for compact catalog/help/router/welcome views |
| A19.2 | A19.1 | Route over compact metadata; planned formats never advertised as complete |
| A19.3 | All source packages | 126-row preservation/disposition map accounts for every original skill and additional Swarm entry |
| A19.4 | A19.2,3 | Worked/negative examples and all aliases/dependencies validate in a consumer |
| A20.1 | None | Source/revision/license/notices/local adaptation remain traceable; no false original-only claim |
| A20.2 | A20.1 | Historic synonym/similarity rewriting explicitly superseded as quality/rights guidance |

Default installations must retain current behavior and access. Selection is not a license
to delete canonical content. Where an adapter cannot dynamically enable plugins, expose
the honest install/read-on-demand path rather than writing a fictional marker. Keep each
use case's knowledge rather than replacing it with a generic link.

## Verification and report

Add `tests/integration/capability-selection.sh` and provenance/version fixtures. Use
existing catalog drift checks; validate real selected consumer installation/discovery,
dependency closure, source-relative links, upgrades and locally modified file conflicts.
Record no-change decisions for strong skills as deliberate preservation, not unread gaps.

Update `reports/P13-skills-preservation.md` with all original names, owner/action,
retained output/method, final path/alias, actual example/verification and status.
Report per-capability maturity and missing live-client evidence separately from metadata
validity. Use a unique final deployable version consistently across manifests, agreed
with MasterSession after integration; no release/tag/publication.

Commit source changes and report P13.md. Independent review checks completeness and
actual optional installation paths, not only row counts.

## Source selection and preservation second unit

**Authority:** existing A18/A19/A20, recovery continuation of 2026-09-22.
This unit may start only after independent acceptance of the frozen compact
discovery first unit (`2a4ba25` / `e20588d`, or its accepted repair).
The original owner's read-only A+B proposal is approved with additive source
selection, retaining the full bundle and all default access. Physical pruning or
moving sources into external packs is not selected or implied.

Own `lib/capability-selections.json`, its metadata consumption in the existing
catalog generator/reference/tests, `reports/P13-skills-preservation.md` and the
P13 report. Reuse the existing strict data reader and canonical member inventory;
do not add a second parser, copied frontmatter corpus, installer or scheduler.
The descriptor binds existing member IDs, dependencies, required source resources
and provenance IDs, useful input/output/examples and conservative source-stage
evidence. Keep selection versions separate from P07 feature/product/schema versions
and P06 host observations. Missing or changed evidence cannot create maturity.

Start with the stable demo-script selection: DemoNarrativeArc's Plan/Critique
modes, DemoNarratorJunior and SlideNarrationCritic. Preserve all receiver modes,
the plan/draft/separate-critique sequence and its artifact contract. This is not
PPT production or proof of native registration or independence. The neutral
lifecycle/profile/review/security core remains shared; company identity remains
an explicitly selected P07 overlay, not an inferred category preference.

Read-only queries return deterministic deduplicated closure and inclusion reasons.
Reject unknown or repeated members, dependency cycles, unsafe paths, missing
resources/notices and alias conflicts before output. No selection preserves the
same full catalog/default behavior. Selection limits bodies loaded into context,
not on-disk access, permissions, policy checks or installed client activation.
Do not turn incidental Markdown see-also links into an invented dependency graph.

Build the preservation map from the exact 126 original audit records, with Swarm
as a separately attributed addition. Each row needs its retained method/output,
original recommendation, current source/alias and argument mapping, responsible
package/leaf, change or grounded no-change rationale, example/evidence and limits.
Use the existing 69-role and 76-path Swarm maps rather than replacing their history.
Current same-path/count equality is not evidence of functional completion.

Verify actual pilot selection, default output identity, alias preservation,
malformed/cycle/missing-resource negatives, no writes or unrelated body exposure,
and exact original-inventory/evidence-pointer coverage. Freeze for the SAME P13
reviewer. Actual installed alias/selection/provenance/resource closure is a later
coordinator/P10 consumer join; no installer prerequisite, receipt or resource-list
change is released here. Status/welcome wiring, final version/generated outputs
and all original parent acceptance remain separate.

## F01/F02 unambiguous catalog identities

Independent `466a454b`, preserved as `f7a09a3`, completes first-unit SPEC with two
P2 findings; QUALITY has not started. The original owner may correct only the
existing generator, focused catalog regressions, directly related metadata
reference and its report. Keep the rejected source/report and all prior outcomes.

F01: validate agent aliases against all canonical names and other aliases in
their namespace, case-insensitively, before filtering or JSON output. An unmatched
query must not hide an invalid source. Preserve distinct skill/agent namespaces
and reuse the existing ownership/collision rules, not another parser.

F02: ordinary generation and `--check` must reject duplicate/case-equivalent
canonical skill names before opening or certifying a catalog. This is an explicit
retained-baseline acceptance gap, not a newly introduced regression. Keep valid
generation's exact output/messages and stdlib-only dependency floor.

Reproduce alias/name, alias/alias, case variants, `agent`/`all`, unmatched-query
and duplicate-name generation/check cases. Preserve a pre-existing valid catalog
byte-for-byte on refusal and retain all prior metadata/registry/alias tests.
Freeze product/report for SAME reviewer `4865e4f7`, complete first-unit SPEC then
first eligible QUALITY. The conditional second-unit release stays gated on PASS.

## Remaining discovery and authoring consumers

A+B source selection passes independent SPEC/QUALITY in `7bf3f253` and is
integrated as `895bb35`; joined selection tests pass. The coordinator additionally
verified actual init/check/repeat, installed alias/role queries, exact public
metadata/notices, and an independent managed-files-only clone under an empty
synthetic home. This uses the already accepted P06 adapter plus reviewed resource
closure, not the blocked P10 transaction candidate or its default/linked acceptance.

Release the original owner's read-only UX proposal for `catalog`, `help`,
`skill-router`, `skillify`, `uniformity`, `eval`, plus the directly coupled
`scaffolding/01-foundation/TEMPLATE-skill.md` and focused consumer tests/reference.
The coordinator explicitly assigns `welcome` and the read-only `status` consumer
to this same P13 unit, coordinating their earlier P06/P08 ownership. Their
underlying host/profile/work/state/job providers remain immutable and owned by
those packages. No generator, descriptor, alias registry, installer, version or
generated-output change is released.

Expose the existing literal selection/query operations before loading only the
selected bodies. Preserve ordinary discovery and the router's bounded shortlist.
Welcome remains task-first, host-honest and nonmutating. Status reads the explicitly
selected original map/current cycle first, repo-local jobs only as supplementary
observations; absent dormant jobs are not proof of no active work. Personal/
cross-repository status requires explicit selection and authority, never a default
home scan. Catalog metadata supplies names/paths, not task progress or clearance.
Use accepted work/profile/state readers without begin/bind or state creation.

Skillify must preserve lesson-to-draft value, check canonical names/aliases through
the existing catalog and write only an explicitly owned target draft. Keep bare
canonical names and actual host wrapper names distinct. Repair the existing
template's opening frontmatter and validate the actual draft with the existing
`validate_lintel_frontmatter <file> skill`, not a whole-install scan or new parser.
Report invalid drafts explicitly; no automatic symlink, activation or personal-tree
scan. Preserve examples, exclusions and permission boundaries.

Uniformity uses the trusted source separately from inspected target data; retain
the floor check's exit and distinguish unavailable/error/stale results. No matrix
regeneration or target-supplied executable fallback. Eval retains known-good/
known-bad per-cell comparison, explicit input/output scope and actual permitted
evaluation or honestly unrun/manual evidence. Empty/insufficient/partial cells
cannot certify the full corpus; no fixed cell-count shortcut, undeclared model
invocation, automatic audit/ship enforcement or paid benchmark.

Verify literal aliases/selections, invalid/colliding drafts, actual uniformity
caller failure/absence/staleness and no-write behavior, truthful partial eval
examples, and two-initiative/status/missing-jobs/profile-drift scenarios without
state creation. Structural instruction checks are not native model execution.
Retain all canonical frontmatter/counts, 126+Swarm map, accepted generator and
existing source-stage proofs. Use same original owner and SAME reviewer
`4865e4f7`; freeze a coherent source/report for SPEC then eligible QUALITY.

### Consumer F01 existing-draft observation

Independent `5a3ab4c337a842011428aba05df08976e46c648a` rejects the remaining
consumer unit `7884ddd` / `5df8e99` with one P2; QUALITY has not started.
The coordinator read its complete 148 lines and verified Git-byte SHA-256
`769ed6345de88107b0aaddc7d39b6c9f41c47cd01ac333ecec2aceda1c991680`.
The actual shipped skillify preflight admits an existing owned 297-character
draft because ordinary `draft.exists()` returns false, while accepted native
I/O sees the file. No authoring or overwrite occurred in the reviewer probe.

Release only the original owner's skillify existence observation, focused
existing consumer regression/reference if needed and report. Use the accepted
native spelling for the same owned logical path; preserve short-path refusal,
new-file success, canonical/alias collisions, containment and the immediate
pre-write recheck. Do not change providers, roots, installation or host policy.
Exercise the actual shipped caller with a real existing deep draft and preserve
its complete content and directory/file snapshot. No overwrite test is needed.

Keep the other eleven passing source controls and prior 97-method evidence
attributed; rerun affected and retained consumer checks on a stable correction.
Freeze source/report for SAME `4865e4f7` complete SPEC and first eligible QUALITY.
This is a nonoverwrite consumer fix, not a new platform initiative or a waiver
of the remaining installed/native/P10 gates.

### Accepted format source-stage reconciliation

Coordinator fan-in of independently accepted PDF/XLSX source removes their old
`TEMPLATE ONLY` descriptions. The unchanged strict selection reader correctly
refuses those stale descriptor hashes; the stable joined consumer run records
that failure. Use the existing explicit `unknown`/null-evidence representation
for these two now-implemented but not fully verified format methods. Retain
Visio's actual template evidence and every native, installer and parent gate.
Own only the descriptor entries, their canonical-data assertions, one focused
unknown-stage regression and directly related selection reference. Do not change
the parser/schema, restore a false template claim, use arbitrary description
substrings as stage proof or promote maturity. Recheck selected consumers and
the retained selection/metadata contracts on the stable joined source.

### Remaining optional source families and current preservation

The coordinator read and hash-verified the original owner's finite read-only
proposal against accepted recovery `9f8885b`. Release its source-only remainder
of A18/A19/A20 now; describing retained methods does not require pretending that
P10 or native format gates passed. Normally merge accepted dependency `42abfcb`
into the same original P13 owner lane, preserving its accepted consumer history.
That dependency contains the pipeline rejection report, not the rejected product.

Keep `core` and `demo-script`, schema version 1, parser/generator and default full
access unchanged. Add these ten ordinary descriptor records with existing
repeatable selection unions: `design-knowledge`, `frontend-design`,
`document-content`, `document-word`, `document-ppt`, `document-pdf`,
`document-xlsx`, `document-visio`, `customer-communication` and
`regulatory-review`. Use the proposal's inspected canonical members and explicit
resource files; no copied inventory, conditional dependency language or new
selection/runtime implementation.

Design knowledge requires core; frontend and common document content consume
design knowledge. Standalone Word/PDF/XLSX require core, not an invented shared
pipeline or every other format. PPT retains the real Design DNA slide-retrieval
dependency. Visio is explicitly staged-slot discovery only, with no writer/file
promise and no invented NetworkArchitect member. CostAnalyzer/CapacityPlanner
and all other members use their actual accepted canonical paths. Customer drafts
do not require the demo roles; an explicit union remains available. Regulatory
expertise is optional, but generic security, the selected company policy and
applicable required controls never become optional.

Own only `lib/capability-selections.json`, the existing selection reference with
bounded worked/negative examples, existing focused selection tests,
`reports/P13-skills-preservation.md` and the P13 report. Resources are explicit
source-relative file paths in the existing schema spelling, not globs,
directories, private input, external libraries, runtime records or output files.
Use exactly the existing `design-dna-corpus` and `design-dna-example-profile`
provenance records where their paths apply. Preserve their MIT/Apache notices,
attribution, recorded version and unknown historical import revisions. Empty
additional provenance is not an originality or legal-sufficiency assertion.

Every example must identify selected inputs, retained method/output, a concrete
negative and actual source-versus-execution limits. Do not launch models/native
tools, install anything, advertise an unverified format or edit canonical skill/
role bodies, source providers, aliases, installer, version or generated output.
PDF/XLSX stay unknown/null; Visio retains its exact template evidence. The P12
pipeline candidate is rejected for B01 and remains unaccepted; no source
selection may import it or advertise its shared serialization as verified.

Refresh the existing 126-original-plus-Swarm preservation map against the exact
accepted dependency, not a new corpus audit. Preserve original recommendations,
method/argument/alias value and historical evidence. The inspected baseline
delta is 43 changed original skills plus Swarm since `204ea725`; compared with
original `28061e4`, 73 originals changed and 53 remain byte-identical. Verify
those identities rather than declaring method acceptance from counts. Update
P04/P09/P11/P12/P13 accepted-source pointers, the five frontend role pointers
beside the preserved 69-role map, and the completed accepted-P06 alias/provenance
pilot. Retain all 76 Swarm destinations and 17 historical artifacts. Pending
P08/P10/P12 changes and final installed observations remain explicit.

Verify all new source closures, exact example headings, minimal standalone
dependencies, shared-core/identity boundaries, deterministic unions, literal
aliases, no writes/unrelated body disclosure, every applicable notice and
unknown/staged semantics. Keep retained malformed/member/cycle/path/resource/
provenance refusal tests; add family-specific positives and negatives without
weakening their assertions. Freeze the coherent unit/report for SAME486 complete
SPEC then QUALITY. Native utility, new-family installed distribution, P10 and
final version/parent acceptance remain separately owned gates.

## Native selected-method utility

**Release: coordinator88, 2026-09-23; original A18.3/A19.4 evidence only.**
The accepted twelve-family P06-engine clone already establishes distributed
metadata/resource/notice closure. Its remaining native body-selection utility
can be observed without waiting for an unrelated P10 repair or changing that
installer's acceptance. Release one finite `demo-script` worked example through
the existing P13 owner and original reviewer486. This is not a new audit,
format experiment, P08 case, installed-plugin activation claim or parent closure.

Use exact accepted recovery source `9c15d690c539eba3271ef7041447fa93d7b0ff15`
as a read-only input. Preserve source, descriptors, versions, generated output,
the 126+Swarm map and every prior report. The owner may create one explicitly
owned private synthetic consumer/evidence area using the already accepted P06
repository-kit route and its previously verified resource closure. Bare/native
installation, P10 WIP/default/linked/recovery routes, new dependencies, personal
configuration and all denied tools remain outside this release. An obstacle is
a concrete blocker, not permission for a shorter substitute or other installer.
No source merge/import into the owner branch or new product commit is required.

The owner prepares literal source/consumer paths, neutral synthetic process roots,
input/manifest hashes and the actual available delegation surface before use.
Use the trusted accepted helper for metadata; consumer files are data until
their exact managed-source identities have been verified. Capture metadata first,
then load only the chosen methods from the selected closure. Do not read every
role/skill body or substitute self-reported restraint for available host evidence.
Shared bootstrap/policy material and metadata are distinct from optional bodies.

### One declared synthetic brief

Produce an eight-minute **proposed** internal engineering demo of a fictional
local duplicate-submission service, `Retry Ledger`. The intended example sends
two identical illustrative requests with the same idempotency key and explains
one logical operation rather than two. There is no running service or measured
result: label the behavior as the proposed demo scenario, not a verified system.
No market demand, ROI, performance, customer or live-success claim is supplied.
No fallback recording is supplied; the narrator must acknowledge that absence
rather than inventing recorded evidence. Pacing is estimated, never measured.
No implementation, network call, slides, rendering, screenshot or rehearsal is
requested. The outputs are the arc, spoken draft and independent critique.

Use the existing published `demo-script` example and actual receiver contracts:
DemoNarrativeArc Plan mode first, then DemoNarratorJunior only after the original
P13 controller records its actual task-local arc approval against this brief.
The operator's existing verification authority includes that bounded content
decision; it does not approve implementation or publication. Preserve both arc
and approval before drafting. After the draft exists, original reviewer486 may
apply DemoNarrativeArc Critique mode and SlideNarrationCritic independently.
Respect actual method inputs/outputs; no generic replacement rubric.

### Execution and evidence boundary

At most **one fresh native drafting context** is authorized for this P13 example;
keep the existing P13 controller and reviewer. Use a real continuation-capable
transport if Plan/approval/Draft require separate turns. No factory, model
override, replacement actor or assumed native registration of named roles.
Use available native delegation with selected canonical methods as explicit
inputs when native role profiles are unavailable, and label that distinction.
If a fresh attributable context or required observation is unavailable, preserve
the blocker rather than role-playing a fresh session or independent critique.

Record actual requests, host returns, selected helper invocation/output, body
read paths and hashes, artifacts, approval and observed phase order. Use actual
host tool/read traces where exposed; disclose any unavailable automatic-context
visibility. Do not claim a complete negative about unseen injected context.
Keep counts, tokens, timing and model identity unknown unless actually observed.
No prior mechanical run becomes a new native run.

The owner writes only its new private fixture/evidence and, after review, an
append-only report update. Reviewer486 independently assesses the complete
bounded native input/selection/artifact chain, SPEC then eligible QUALITY,
without correcting its own findings. Route the frozen input through coordinator88
before commissioning that review. No global P05 clearance, hook activation,
source maturity promotion, whole-family/all-role verification or publication
follows from a useful example. A18.3/A19.4 and P10-dependent final gates remain
open until their full original acceptance is established.

### Accepted native utility and report-only capture

Original reviewer486 actually applied both selected critique methods to the
frozen case2817e492. Its handoff SHA
`2180ffb4b128efdf0882c222e9626098b396393a2a6f5286644a00bbd0f5b712`
records bounded SPEC PASS followed by QUALITY PASS_WITH_ADVISORY_P3,
P1/P2/P3=0/0/1. N01 concerns repeated proposal/proof caveats in the middle of
the spoken draft; it is optional editorial advice, not a mandatory gap.
Coordinator88 accepts this one-case result without a new drafting pass.
Both actual critiques and the complete bounded report were read; parent
`p13-native-review-intake-01` verifies52 reviewer evidence files,11 artifacts,
stage order,1,055 unchanged owner inputs and the separately sealed receipts.
No native/helper/test execution was replayed and no parent checkbox closes.

Release the original P13 owner only to append the completed case and independent
result to `.claude/plans/universal-implementation/reports/P13.md`, then make one
normal report-only feature commit, sole parent the unchanged owner
`e797bd48c31257e79c7ccc3eb369d584bfd7db8b`. Preserve the entire old report prefix,
all original source/report history and every frozen input/reviewer artifact.
Name exact source, release, actor/continuation/approval, method outputs,
review stages and advisory finding, actual checks and remaining limits.
Keep raw Windows versus Git-LF identities separate. Verify sole changed path,
full prefix, parent/branch, whitespace and final status with existing Git
controls. Return the commit, report hashes and exact handoff; do not push.

No product/source/map/descriptor/maturity/version/generated/ledger/memory/ADR
edit, new context, re-draft, extra critique, P05 record, installer/rendering
case or full A18.3/A19.4 acceptance is released. The old owner-file equality
seals describe their actual pre-report phase; only this report may now change.
P10, final preservation and original79/113 boundaries remain separate.

### Status and welcome fan-in input (not released)

The coordinator's P08 trial merge found that P08 and P13 both changed
`skills/status/SKILL.md`; see the P08 card, "Coordinator trial-merge findings for
integration". The P08 integration keeps P13's accepted reader. The later status and
welcome release then adds P08's read-only job observations to the supplementary jobs
section: `list_jobs --read-only` and `stale_jobs 24` from the trusted source's
`bin/_jobs.sh`, with `LINTEL_JOBS_NO_INIT=1` and `LINTEL_JOBS_DIR` set explicitly to
the selected repository's `.claude/runtime/jobs`.

The explicit directory matters: without it, the helper falls back to its global
directory for a repository without a v5 layout marker, and this display becomes a home
scan. Blocked, stale and unknown records stay visible, and an absent registry stays
"unobserved".

P08's `skills/welcome/SKILL.md` change is already contained in HEAD. The release
follows P08 integration and precedes the final whole-P08 review; nothing here is
released now.

### Installed consumer closure after P10 integration (2026-09-24)

P10 passes its final independent review (`f460eb9f`) and is integrated in recovery
`68d32240`. The joined kit and an independent integration review are in progress. This
releases the "later coordinator/P10 consumer join" for installed alias, selection,
provenance and resource closure. The original P13 owner implements it as one bounded,
test-first unit on a new branch and worktree from recovery `8754515a` or later, and
records the exact base.

- Install the integrated tree only through P10's accepted surfaces: the native installer
  with the approved PowerShell 7, or `li-lifecycle`. Use synthetic homes and targets
  under short roots, export no `LINTEL_*` selectors, and read no real profile.
- From the installed source, show:
  - A18.3: a consumer selects relevant capabilities from metadata without loading
    unrelated role or prompt bodies;
  - A18.4: every moved or aliased use case keeps its method, discoverability and
    dependency assets;
  - A19.4: the worked and negative examples and all aliases and dependencies validate in
    a consumer.
- Add discriminating negatives: an unresolvable alias target, a removed dependency asset
  and unrelated body exposure must each refuse or report visibly.
- Reuse the accepted discovery tests and the kit's installed methods. Change only tests
  and the report. A product defect returns as a finding for coordinator intake.
- Freeze the product and report for SAME `4865e4f7`, which runs SPEC and then QUALITY if
  eligible. Send the handoff to the coordinator in parallel. Any helper follows the
  operator model directive.

A19.3's preservation map, the version decision, the generated fan-in and the status and
welcome input stay with the P13 fan-in after P08 integration. If the joined P10 checks
change P10, the unit revalidates on the corrected base.

**Frozen blocked unit (2026-09-24).** The owner froze test candidate `235102a8` (six
paths, base `b5c0fcf6`) and report-only child `6f1de58b` (report SHA-256
`61b11ce7…6585f55`), and sent them to SAME `4865e4f7` and the coordinator in parallel.

- The corrected alias-refusal method passes, and so does the reused twelve-family
  installed method.
- The third method fails in install setup on IC-F01, P10's intermittent "File changed
  while reading: journal.json".
- The suite exits 1, and several cells stay incomplete.

The coordinator routes IC-F01 to the P10 owner (P10 card, "Integration defects F-INT-2
and IC-F01 and repair release"). The unit revalidates on the corrected, accepted P10
base.

### Fan-in preparation on the P08 preview (2026-09-24)

The P13 fan-in waits for P08 integration. The coordinator's local branch
`jokerman-microsoft-p08-integration-preview` (`14b0b51c`) now carries P08 and the accepted
A13 subset on P10-integrated recovery, with only A13.1.b/.4.b and the F06 report
appendix still to come. To shorten the endgame, the original P13 owner prepares the
fan-in on a new branch from `14b0b51c`:

- **A19.3.** Build the map in `reports/P13-skills-preservation.md` from the exact 126
  original audit records, with Swarm attributed separately, following this card's row
  rules. Use the existing 69-role and 76-path maps.
- **Status and welcome.** Add P08's read-only job observations to the status skill's
  supplementary jobs section, as "Status and welcome fan-in input" specifies. Use the
  trusted source's `bin/_jobs.sh` (`list_jobs --read-only` and `stale_jobs 24`), with
  `LINTEL_JOBS_NO_INIT=1` and an explicit `LINTEL_JOBS_DIR` for the selected
  repository. P08's welcome change is already in HEAD.
- **Generated fan-in.** Regenerate the catalog, wiki and README only through their
  generators.
- **Version.** Propose one unique final deployable version for every manifest, with no
  bump, tag or publication. The coordinator agrees it after the final integration.
- Change only the status skill, its focused tests, the map and the reports. The
  coordinator re-verifies against the real P08 integration.
- Freeze for SAME `4865e4f7` once its session runs the required model; until then, hold
  the handoff. Send it to the coordinator in parallel.

**Frozen fan-in preparation and coordinator intake (2026-09-24).** The owner froze
product `e7f799ec` (base `14b0b51c`) and report-only child `9096135a`. The report has LF
SHA-256 `b4814579…6c5b29ad4` and is 1,355 lines, with the earlier 1,196-line report as
an exact prefix.

The product changes exactly four paths:

- `reports/P13-skills-preservation.md`, the A19.3 map (`8b6afd12…`), with all 126 rows
  and Swarm attributed separately;
- `reports/P13-fanin-proposal.md` (`e00f94ac…`);
- `skills/status/SKILL.md`, with the read-only job observations, explicit repository
  paths and `NO_INIT`, and an absent registry reported as "unobserved";
- the focused tests in `tests/unit/catalog-consumers.py`: 4 new regressions RED on the
  old body, and all 18 status methods pass.

The generators produced no content drift on the preview. The owner proposes version
`0.11.0` for all seven identity manifests and both nested marketplace entries, all of
which stay at `0.10.0` for now. The coordinator agrees the version after the final
integration.

The coordinator's data-only intake confirms the parents, paths, hashes, report prefix
and unchanged manifests. The review waits until `4865e4f7` runs the required model. The
coordinator re-verifies the unit against the real P08 integration, including A13.1.b/.4.b
and the F06 report.

**Installed-consumer revalidation and alternate reviewer (2026-09-24).** With IC-F01
fixed, the owner revalidated the installed-consumer unit on recovery `f740141e`:

- The new tests `5c77a984` and report-only child `9d7851a4` (LF SHA-256
  `d2f9b5f4…ec07d99f1eb31db50`) change the same six test paths. Five blobs equal
  `235102a8`. `copilot-kit.py` carries the current P10 and P09 changes plus P13's two
  query extractions.
- The full six-method suite passes 6/6 with no skips in 1,015 s. The largest fixture path
  is 141 characters, and every expected refusal is retained.
- The coordinator's data-only intake confirms the parents, paths, blob identities, report
  and trailers.

SAME `4865e4f7` still runs gpt-6-astra, and the operator has not switched it. The P13
reviews are close to the critical path, so the coordinator assigns SAME `447d97f5` as
the alternate independent reviewer, for this unit and for the fan-in preparation, on
condition that it confirms `claude-opus-5.5` with max reasoning and long context. It is
independent of P13's implementation and has reviewed P09's equivalent installed closure.
`4865e4f7` stays on hold and is not replaced as a record. Its earlier P13 reviews stand.

SAME447 replied that it runs gpt-6-astra, so the condition is unmet and it did nothing.
The coordinator therefore created a new independent reviewer session, "P13 installed
review" (`30146eab-ddeb-467e-8f1b-8da575256ba2`), with `claude-opus-5.5`, max reasoning
and long context. It got a complete brief: the frozen pair, the synthetic isolation and
short-root rules, and a report-only review on the owner's report child. It reviews this
unit now, and the fan-in preparation after the real P08 integration.

**Installed-consumer closure accepted and integrated (2026-09-24).** The new reviewer
`30146eab` confirmed from its own session record that it runs `claude-opus-5.5` with max
reasoning and long context. Its report-only review `d0eb793e`
(`reviews/P13-installed-5c77a98.md`, 216 lines, LF SHA-256 `97da2857…55ed6ab3`) returns
scoped SPEC PASS and then eligible QUALITY PASS with advisories: P1 0, P2 0, P3 2.

- **SPEC.** Every released obligation passes. The consumer selects from metadata and
  reads only the chosen body (196 header reads, one body). All 46 aliases resolve, all 196
  bodies equal the source and all 35 module rows exist. Examples, negatives and all
  twelve selections validate in an installed clone under an empty home. The three
  discriminating negatives refuse with their specific diagnostics and no write.
- **Independent runs.** The shell entry passed twice with no skip: once retained
  (1,097 s) and once in `run-all.sh`'s form (1,758 s). The 99 child calls match the
  owner's exits exactly, all six journals are complete with 473 applied entries, and no
  retry or IC-F01 notice appears. The two changed kit methods pass in their class, and
  the catalog metadata, selection and consumer suites pass 31, 29 and 33.
- **N1 (P3).** The entry costs 17 to 29 minutes on this host against CI's 30-minute
  job. It is input to the P15 timing decision for the final PR, not a change in this
  unit.
- **N2 (P3).** The fixture-root link guard in `catalog-installed.py:43-45` checks
  after `resolve(strict=True)`, so it never fires. This is test hygiene only. The owner may
  correct it with the fan-in if it touches that file; no separate cycle is opened.
- Limits stay explicit: Windows with Python 3.11.9 only, a body observer that sees only
  `Path.read_*`, the parent isolation supplied by the reviewer's launcher, and no
  compliance assessment.

The coordinator's intake confirms the sole parent `9d7851a4`, the single added file,
its hash, and the trailer. Recovery `1e41deb0` merges `d0eb793e` without conflict. Its
tree equals the tested `5c77a984` outside `.claude/`, and the report and review equal
the reviewed bytes.

A18.3 and A18.4 close. Their acceptance rows depend only on A18.2 and P10, and on A18.3,
and the accepted source, native utility and installed evidence cover them. A19.4 stays
open because it depends on A19.3 as well: it closes with the 126-row map in the fan-in
review, where this installed evidence carries over.

**Fan-in rebase and review release (2026-09-24).** The P08 integration branch
`jokerman-microsoft-p08-integration` (`ad1045b8`) carries P08's final product except the
F06 appendix (P08 card, "P08 integration branch and reconciliation 6"). The coordinator
releases the fan-in rebase onto it to the original owner:

- Re-apply exactly the four paths of `e7f799ec`. Re-verify the 126-row map against the
  integrated inventory, and disclose any row that changes.
- Append the fan-in appendix after the installed-revalidation appendix in `reports/P13.md`.
- Correct N2 in a separate commit, with a negative that fails on the old check order.
- Run the focused catalog and status tests and the generator checks under the synthetic
  launcher discipline. Regenerate only through generators.
- Keep the 0.11.0 proposal without a manifest change.
- Freeze for `30146eab`.

The reviewer runs SPEC and then QUALITY on A19.3, A19.4 (with the installed evidence
carried over), A20.4's P13 part (external-contribution provenance), the status union,
generated drift, N2, and reconciliation 6's descriptor change.

**Rebased fan-in frozen and coordinator intake (2026-09-24).** The owner froze branch
`jokerman-microsoft-p13-final-fanin-0924` on exact base `ad1045b8` and sent the handoff
(`73f4c19a…e3a9793f`, manifest `32adc9f3…163216f0`, 704 files) to `30146eab` in
parallel.

- `e922515c` is N2 and changes only `tests/integration/catalog-installed.py`. A real
  PowerShell 7 junction is RED on the old check order and GREEN 2/2.
- `f581b072` holds the four fan-in paths. The status skill and the consumer tests are
  byte-identical to the reviewed preparation `e7f799ec`.
  - The map changes 49 of 126 rows for integrated facts. One row updates `hooks-status`
    for A13's doctor-JSON installer read, still with unverified execution. The other 48
    change only their limits.
  - The retained counts are unchanged: 118 covered and 8 unmapped, 58 bodies since
    `42abfcba`, and 46 aliases.
  - Reconciliation 6's resources are recorded. Every selection closure now includes
    `bin/li-lessons.py` through the shared core.
- `d12bbf6d` is report-only. `reports/P13.md` (`1a4f251c…be5fd7fe`) keeps the 1,353-line
  revalidation report as an exact prefix, then the preview appendix verbatim, then the new
  fan-in section, for 1,639 lines in total.
- The consumer (37 tests, 18 of them status), metadata (31) and selection (29) suites
  pass. The catalog, wiki and README generator checks pass, and regeneration produced no
  content drift, so there is no generated commit. The proposal stays at 0.11.0, with every
  manifest at 0.10.0.

The coordinator's data-only intake confirms the sole parents, the exact changed paths,
all six hashes, the trailers, the report prefix and appendix, and the byte identity with
`e7f799ec` for the status skill and the tests. `30146eab` reviews next.

**Fan-in accepted and integrated; A19.3, A19.4 and A20.4 close (2026-09-24).** Reviewer
`30146eab` reviewed `e922515c`, `f581b072` and report child `d12bbf6d` on `ad1045b8`. It
returns scoped SPEC PASS and then eligible QUALITY PASS with advisories: P1 0, P2 0, P3 3.

- **(a) A19.3.** The map has exactly 126 rows, each with its audit action, canonical path
  and link. The C/U labels equal Git identity (118 C, 8 U), and all 46 aliases, 35 module
  capabilities, 23 evidence keys, links and 157 package/leaf pairs check. Swarm is
  attributed separately, and the 49 changed rows match their sources.
- **(b) A19.4.** The installed suite reran on the N2 tree: 8/8, with the same 99 child
  calls and exits.
- **(c) A20.4, P13's part.** Both adapted-component records are complete, with
  `import_commit` null. The four design-dna selections declare both provenance IDs, and
  the installed notices, attribution and registry equal the source.
- **(d) Status union.** It matches this card, and the 4 new tests fail on the base body.
- **(e) Drift and N2.** The generator checks pass, and the old guard order admits a real
  junction (RED) while the new file passes.
- **(f) Reconciliation 6.** It fits the selection contract.
- **(g) Version.** The proposal is report only; all 9 fields stay at 0.10.0.

The operator declined the reviewer's own review-branch creation as an unauthorized
repository mutation. The coordinator therefore preserves the review byte-exact in
recovery as `reviews/P13-fanin-f581b07.md` (`2172dd31`, 184 lines, LF SHA-256
`72b22dd2…4e4490d7`). Recovery `cd877232` merges `d12bbf6d` without conflict, and its tree
outside `.claude/` equals the reviewed `d12bbf6d`.

**The coordinator closes A19.3, A19.4 and A20.4**, with P07's accepted synthetic-pack part
of A20.4.

The P3 findings:

- **N4**, a coordinator decision: `bin/li-lessons.py` resolves its store through
  `lib/memory.sh`, which reconciliation 6 did not declare. The coordinator adds it to
  `ADAPTER_RESOURCES` as reconciliation 7. That completes reconciliation 6's goal that an
  incomplete installed source refuses cleanly, and it serves A23.4.g3.
- **N3 and N5**, released to the original owner as one optional test-and-comment unit,
  rechecked by `30146eab`:
  - **N3:** in the N2 negative (`catalog-installed.py:319-328`), give the `pwsh` child
    the class's synthetic HOME, USERPROFILE, APPDATA, LOCALAPPDATA and TEMP, plus
    `POWERSHELL_TELEMETRY_OPTOUT=1`, and keep the fail-closed PowerShell 7 requirement.
  - **N5:** reword the `install/upstream-sources.yaml:7` comment so the bundled copy
    points to `docs/provenance.md` in the Lintel source repository. Parsed registry data
    must stay unchanged.

Neither finding blocks the closures.

Disclosed by the reviewer: the Defender pre-tool hook errored and denied two read-only
`Select-String` calls. The reviewer made the same lookups with the built-in search tool.
It also retained one over-strict provenance oracle and two probe-script errors.

**Reconciliation 7 (2026-09-24).** `ac216479` adds `lib/memory.sh` to `ADAPTER_RESOURCES`,
next to `bin/li-lessons.py` (N4). It was verified first on the scratch commit `bbc8cafc`:
the kit's two data-driven resource methods passed, including the missing-source refusal
for every declared resource (`recon7-kit-resources`, 328 s), and so did `li-catalog.py
--check`. The recovery commit carries the same bytes. The A23.5 final review covers it,
together with reconciliation 6.

**Version agreed (2026-09-24).** The coordinator agrees the proposal: `0.11.0` on all seven
product manifests and both nested marketplace entries, nine fields in all (`99443cf8`),
with a `CHANGELOG.md` entry that states its known limits. Installers read the `main`
branch, which has served `0.10.0`, so the integrated behavior needs a new version for
installed clients to update. The tag set holds only `v0.9.0-beta`, and no tag or release is
made.

- Parity was checked in Python across all nine fields, because jq is denied locally
  (L-046).
- The catalog, instructions, adapter, wiki and `install/verify.sh --all` checks pass, and so
  do `manifest-identity`, `no-swedish` and the plugin-manifest checks (`ver-precheck`).

**N3/N5 follow-up frozen and coordinator intake (2026-09-24).** The owner froze N3 `03604b2f`
and N5 `f703ee3f` on recovery `1fda8ee0`, for a recheck by `30146eab`.

- **N3** changes only `tests/integration/catalog-installed.py`. The `pwsh` child of the N2
  negative now gets synthetic HOME, USERPROFILE, APPDATA, LOCALAPPDATA, TEMP and TMP, and
  `POWERSHELL_TELEMETRY_OPTOUT=1`, and the test asserts them. The negative is RED on the old
  order and GREEN 2/2.
- **N5** changes only comments in `install/upstream-sources.yaml`. The non-comment lines are
  identical, and the parsed YAML is equal.

The coordinator's data-only intake confirms the parents, paths, trailers and blobs, and that
recovery has not touched either path since the base. `git merge-tree` onto recovery is
clean. The follow-up merges after the recheck passes.

**N3/N5 accepted and integrated (2026-09-24).** `30146eab`'s recheck returns scoped SPEC PASS
and then QUALITY PASS, with P1, P2 and P3 at 0. It reproduced N3's RED on the old guard
order and GREEN 2/2 on the frozen file. The child's environment stays entirely inside the
test root, with the opt-out set. For N5 it confirmed equal parsed data and a content-equal
bundled comment.

The review is preserved byte-exact as `reviews/P13-n3-n5-f703ee3.md` (`8a1ebf33`, 120 lines,
LF SHA-256 `bc1ca679…7303a6f4`). Recovery `fc2ea583` merges `f703ee3f`, and both paths equal
the reviewed bytes. All three P3 findings of the fan-in review are now closed: N3 and N5 by
this unit, N4 by reconciliation 7.

The final head's `catalog-installed` entry reruns with the other deltas, because the
`fin2` runs predate this merge.

**Integration finding F-INT-6 and test-only release (2026-09-24).** In the joined `fin2` run,
all four `InstalledDiscovery` methods of `tests/integration/catalog-installed.py` failed in
`setUp` with "302 not less than 220". The fixture root was short, 62 characters. The
worst-case guard at lines 101–104 measures the base, plus `worst-case/.github/lintel`, plus
the longest file anywhere under the source root, gitignored files included.

- The source root held `.claude/runtime/` evidence that tests keep by design: 249-character
  paths from `document-pipeline-binding`, and further files from `domain-result-handoff`,
  `design-contract` and the browser tests.
- The adapter never bundles `.claude/`: `COMPONENTS`, `DOCS` and `SOURCE_METADATA` in
  `bin/li-copilot.py` exclude it.
- The guard therefore makes the test depend on order and state. A second full run in the
  same checkout fails it.
- CI's fresh shard checkouts do not trigger it, because `catalog-installed` is the first
  test of integration shard 2/4. No product behavior is affected.

The coordinator releases one bounded test-only repair to the original owner:

- Compute the worst case over exactly the files the adapter would bundle: the source
  files under `adapter.COMPONENTS`, `DOCS` and `SOURCE_METADATA` (or `git ls-files` of those
  paths). Keep the 220-character limit and its message unchanged.
- Add a discriminating regression: a long ignored file under `.claude/runtime/` must make
  the old guard fail (RED) while the new guard passes (GREEN). A long bundled path must
  still refuse.
- Change only `tests/integration/catalog-installed.py`. Branch from current recovery.
  Run the full entry once in a checkout containing long runtime evidence, and once in a
  clean one.
- Freeze for a recheck by `30146eab`, delivered as a file outside the repository, which the
  coordinator preserves.
