# P08 - One work lifecycle and observable learning

**Authority:** A08/A10/A13, spec R05, audit RT-05/06/08 and WF-03/04/05/06/10/11/12,
CP-10/11/16, RU-09/10/12. **Prerequisites:** P04 historical merge, P05/P06/P07 contracts,
and P03 context ownership released. A08.1 already has a coordinator implementation at
`b72ab47` plus `1d40193` with 33 negative/preservation assertions; review and extend it rather than
overwriting it. It is not independently accepted yet.

## Current dispatch boundary

P05 `b023e8c` has complete independent spec/first whole quality acceptance `33eac071`
and is integrated as `c5c8f86`; P06/provider and P07 including the long-path repair
are accepted and integrated. P03 context ownership is released. The coordinator
explicitly releases `bin/li-work-artifacts.py` and its directly coupled reader tests
to P08 while preserving the accepted P04 task/package APIs and parser behavior.

Execute ready A08/A10 leaves now. A13's installer observations additionally depend
on P10's actual lifecycle result/receipt seam: coordinate its fields and real fixtures
before changing the common audit/event interface or claiming that integration complete.
Do not import P10 WIP or modify its installers/transaction helpers. This retains the
original A13 dependency on A12 without blocking independent lifecycle work.

A13.1/.2/.4 remain gated, including their common audit/event and learning/promotion
changes, until the coordinator supplies an exact contract artifact and explicit release.
An informal field list is not that release. Do not change `bin/_audit.sh` or the gated
memory/promotion/observation path merely to make those leaves independent. A13.3 may
retain the already authorized advisory-only boundary. If ready work finishes first,
freeze a partial checkpoint rather than inventing installer events or importing WIP.

Use the accepted `skills/review/references/evidence.md` and shared implementation,
not old positive-string/empty-commit snippets. Bound review/context/QA are v2;
profile/work-map/corroboration domains retain their existing versions. Profile
verification must actually precede consumption; parent/other-target references are
not silently transferred. P05's mapped-excerpt digest binds relative normalization
permission; no consumer recomputes a raw or text-only substitute.

Approved subdivisions remain under their original parents in plan.md:
A08.2.a owns cycle-start/transition and interruption, .b status-grounded footer,
.c canonical entry/no duplicate SENSE/SCOPE; A08.3.a owns the shared selected-map
reader/bounded artifact manifest, .b persisted map/profile/policy resume identity,
.c the ANALYZE/CAPTURE/BUILD/budget consumers. Reuse accepted selectors, work-map
parsers, profile validation and review identity; do not create a second authority
or acceptance hash under a new manifest name.

P08 may correct its owned `_context.sh` interpreter probe/message to the existing
Python 3.9 floor after the accepted `ca28074` annotation correction. Preserve helper
semantics and test supported/unsupported probe behavior; an injected version probe
or grammar check is not actual Python 3.9 execution.

## Ownership

Own `lib/state.sh`, `lib/cycle-footer.sh`, `lib/orientator-routing.sh`, `lib/memory.sh`,
`lib/scale-estimator.sh`, `bin/_audit.sh`, `bin/_jobs.sh`, `bin/li-lessons-promote`;
work-map readers after P04 releases them; new minimal event/workflow helpers as needed.
`bin/_context.sh` and context entry points may change only after P03 is integrated.
Own lifecycle/intake skills `sense`, `scope`, `cycle`, `define`, `discover`, `plan`,
`build`, `analyze`, `capture`, `resume`, `spec-kit`, `fix`, `research`, `autoplan`,
`office-hours`, `plan-tune`, `plan-and-build`, `review-and-ship`, `plan-eng-review`,
`plan-ceo-review`, `plan-devex-review`, `handoff-size-check`; observation/learning skills
`audit`, `hooks-status`, `usage-log`, `learn`, `lessons`, `lessons-surface`,
`lessons-promote`, `jobs`, `status`, `clean`, `maintenance`, `retro`, `landing-report`,
`code-freeze` and `code-unfreeze`. Direct concept docs/tests and report P08.md are owned.

Do not modify P05 review/QA/SHIP, profile/host contracts, private sync, installers,
Swarm/Brief Forge, specialist/domain/design roles or generated outputs. Propose a minimal
consumer amendment to its owner rather than independently changing a shared contract.

## Leaves and integration evidence

| Leaf | Dependency | Acceptance |
|---|---|---|
| A08.1 | Existing b72ab47 | Keep read operations from becoming build/ship; preserve direct mutation and unknown/compound intent honesty |
| A08.2 | A08.1 | Cycle segment begins before any phase; each start phase runs once; interrupted transition remains resumable |
| A08.3 | P05/P07,A08.2 | ANALYZE/CAPTURE/budget/BUILD/RESUME share explicitly selected original paths and IDs |
| A08.4 | A08.3 | Markdown/YAML ADR status understood; overdue/blocked work remains visible |
| A08.5 | A08.4 | Two initiatives, Spec Kit, loop-back and cold restart preserve correct history and task source |
| A10.1 | P06/P07 | Question asks only unresolved material decision through actual host channel |
| A10.2 | A10.1 | Maintenance/migration/research do not trigger founder questions; venture lens remains opt-in |
| A10.3 | A08.3,A10.2 | Alternate entry points compose canonical work/status/authority without duplicate backlog |
| A10.4 | A10.3 | plan-tune is honestly dormant unless settings have a real reader; no fake conflict resolver |
| A13.1 | P05,P04 released boundary; P10 installer producer seam/fixtures | Actual hook/review/event producer fields agree with structured reader; absence means unobserved |
| A13.2 | A13.1 | Add/update/supersede/promote preserve L-NNN retrieval and configured destination |
| A13.3 | A08.3 | BUILD/QA see frozen scope through accepted contract, or enforcement explicitly remains advisory |
| A13.4 | A13.1,2,3 | Roundtrip event, promoted lesson and incomplete-log/freeze scenarios pass |

Add `tests/integration/universal-work-lifecycle.sh` and
`tests/integration/observation-learning.sh` with real producer/consumer fixtures.
Retain existing state-segment, jobs-concurrency, context-ownership, memory, audit and
enterprise-workflow tests. A malformed event must not silently disappear into a healthy
summary. Preserve audit writer compatibility; mandatory record writers must propagate
storage failure even if ordinary warn hooks remain advisory. No global destination,
hook registration, auto-spawn or calibration writer is activated.

Additional observed A08.2 regression: on MasterSession's real ledger, `phase=BUILD`,
`status=STARTING`, `next=REVIEW` rendered "BUILD done" through
`render_cycle_footer --compact`. The ledger was correct, the presentation was not.
Add a discriminating STARTING/blocked/done footer test; do not let next-phase metadata
serve as evidence of phase completion.

`bin/li-lessons-promote` must never silently switch another checkout's branch or commit
unrelated changes. Preserve explicit source/destination, generalized content and record
provenance. Prefer an inspectable scoped change over implicit publication.

## Method and handoff

Use small stable sub-IDs if any leaf must be split; coordinator adds them before scope
expansion. Preserve useful intake questions, learning heuristics and recovery examples;
do not replace detailed methods with generic boilerplate. Keep package execution and
independent substantive review. Reports bind exact work/profile/result as supported by
the predecessor contracts. Commit locally and report exact tests and remaining limits.
Independent spec/quality review follows; no own signoff, network or global mutation.

## Frozen partial review boundary

Product `8d477701bf05c545790d4c5fdbdc84945dd130db`, report-only
`062ffc6cc0cfecee80646fda2a54bad4db837fad`, parent/control `f9685530`, is eligible
for independent review of A08.1-.5 and their approved refinements, A10.1-.4 and the
explicitly advisory A13.3 only. A13.1/.2/.4 remain intentionally deferred under the
existing gate; this is not a whole-P08 completion candidate.

The reviewer must distinguish attributable specification, scoped quality and remaining
preservation/integration readiness. The failed 14/18 preservation aggregate and 22/23
context-safety result are not waived because the same-depth dependency control reproduces
them. Any acceptance that lacks proof remains BLOCKED/UNVERIFIED. Quality may start only
after the selected subset's specification actually passes; no whole-package PASS or
source integration follows merely from a partial verdict.

Review the final two post-run cycle authority sentences as unexecuted instruction
changes, not bytes covered by the earlier behavior run. Preserve shell27 INVALID and
unknown real-home effects. New review executions require recorded synthetic-path
preflight; no real-home inspection/rollback, P03/P07 repair, global long-path settings
or shorter-path substitute is authorized. A separate P03 investigation owns attribution
of the inherited groups; do not duplicate or repair that trace in the review.

## F01/F02/F03 partial-specification repair

Independent report `9faebd2b` (sole parent `062ffc6c`, preserved as `a56aab4`)
is selected SPEC FAIL: F01/F02 P1 routing defects and F03 P2 PLAN/ANALYZE caller
inconsistency. Quality has not started; inherited preservation and deferred A13
remain blocked. The original P08 owner may now change only:

- `lib/orientator-routing.sh` and focused routing/producer-consumer tests for
  A08.1, preserving original direct-mutation, caller-IFS and retained safe routes.
- `skills/plan/SKILL.md` and directly focused PLAN-to-ANALYZE selection/history
  regression cases in the existing lifecycle test for A08.3.c.
- Its own `reports/P08.md`.

Reproduce all six exact report inputs through `classify_intent`, `match_workflow`
and `score_confidence` before repair. Negation must retain its clause/operation
scope rather than exposing a subject such as "release" as write intent. Safety/
information questions stay read-only or request clarification; an affirmative
`Deploy the reviewed release.` remains a legitimate action recommendation. A safe
clarification is acceptable where intent is genuinely ambiguous; do not fix the
three strings with phrase-specific exemptions or turn all requests into reads.

PLAN must delegate to the same explicitly selected analysis-report contract as
ANALYZE, retaining global reports as history. Test the actual caller/producer
selection across two initiatives, not merely manually created correct report paths.
Keep ADR-0004 advisory semantics and P05 mandatory-control/evidence rules unchanged.

No P03/P07/shared path, provider, audit/memory/promotion, P10, generated output or
authority-ledger edit is authorized. Use verified synthetic preflight for every
test/hook, preserve all invalid/failed historical runs, and freeze a new narrow
product/report checkpoint for the same reviewer. F01/F02/F03 local passes cannot
waive inherited preservation or grant whole-P08/A13 acceptance.

## Root re-plan after the nonpassing closure recheck

**Status:** authorized bounded correction, 2026-09-21. Immutable `af5c9399`
(parent `8bde7005`, preserved as `92502e6`) keeps F01/F02/F03 open. The owner read
the whole report and supplied a design-only proposal before this authorization.
This section supersedes continued action-token patching, not original acceptance.

### Operation recognition

Replace the first-recognized-action-token assumption with a bounded request grammar.
Preserve clause/sequence/heading/quote structure while normalizing; keep variables and
IFS local. Derive a candidate only from a positively identified governing request-head
slot: direct imperative, supported polite/emphatic form or explicit request/assistance
wrapper. A subject, heading or quoted example does not itself supply action authority.

Inspect the governing clause before returning. Scoped prohibitions and direct/indirect
inquiries remain in force; operation words in their subjects/complements cannot become
new affirmative heads. Collect independent request heads before resolving a compound
request. Preserve object coordination and read-only follow-ups, but unresolved/conflicting
or multi-operation authority stays unclear/low. Keep existing affirmative controls,
Swedish/multiword routes, read routes and symptom-only triage where no governing read,
prohibition or ambiguous instruction exists.

This is deliberately not full natural-language interpretation. Unsupported or ambiguous
structure clarifies rather than granting high-confidence writes. Do not use a growing
counterexample blacklist, universal read-only output, a new enum/input format or an
implicit model call. `match_workflow`, risk/confidence signatures and pack defaults
remain unchanged. Recommendations never grant execution permission.

### Native analysis-report identity

The sole additional source seam is internal
`lib/workflow.sh::_workflow_guard_analyze_report_path <candidate> <state-directory>`,
called once by actual PLAN preparation after successful `workflow_resume` and before
output selection. Use the existing selected Python/platform path APIs and accepted P03
containment/reparse checks; relative input anchors to the explicit target. Honor existing
declared target/state roots, never authorize an arbitrary candidate parent as a new root.

Existing-file identity uses actual samefile semantics. An absent leaf uses verified
existing-parent identity and native-parsed components; all supported native backslash,
forward-slash and relative spellings reach the same guard. Proven distinct existing
case-sensitive files remain distinct. Uncertain aliases, inaccessible identity or
unsupported input returns explicit INCOMPLETE, not assumed-different success.
The known global history location returns exit 2 and reconciliation diagnostics.
No creation, fallback selection, stored-spelling rewrite, blanket case folding,
custom drive parser, P03/P07 edit or unreviewed native_paths import is authorized.

### Ownership and verification

Own only `lib/orientator-routing.sh`, the one internal workflow guard/call support,
`skills/plan/SKILL.md`, the existing focused routing/lifecycle tests and P08's report.
No broader workflow/state, schema, A13, dependency, generated or shared-ledger edits.
Stable refinements are A08.1.r1/r2 and A08.3.c.r1/r2 in plan.md.

Retain the exact 21-case independent matrix, all 183 assertions and eight mechanical
scenarios. Add structural-class contrasts with independently varied subjects/operation
lexemes: inserting action words into topics/read complements must not increase authority.
Verify affirmative actions, scoped prohibitions, indirect assessment, headings, quotes,
object coordination versus independent sequences and IFS preservation; don't merely
assert the newly chosen implementation's output.

Execute actual PLAN blocks for three spellings times present/absent, a proven distinct
same-basename directory, normal cycle reports and inaccessible/uncertain refusal.
Retain wrong-map/profile drift, no-write/history preservation and cold persistence.
Prose remains explicitly synthetic INCOMPLETE, not live analysis evidence. Use the
recorded synthetic preflight and preserve every previous failure. Freeze a new
product/report for the same reviewer; all inherited, A13 and quality gates remain.

## Post-freeze reviewed dependency join

**Status:** authorized bounded continuation, 2026-09-21. Root product
`171daa8d44f8e758bc1f6e98ff1c3a11a477ea18` and report-only
`723b067f1bbfe4f0cbd2b86614973b4c556f36c7` remain immutable. The coordinator
verified their sole parents, exact five-path product scope, unchanged modes,
report-only delta and byte-preserved 805-line prior report. This is intake, not
F01/F02/F03 closure. The original P08 owner may now compose the accepted dependency
in separate attributed commits before one complete selected-subset review.

Import exact core `ce7415f6342220e49746880dd7338a6e7d9294ed` and fixture-only
`5fc657f14123daca9ec126cadc1e9d64ee60691c`, independently accepted by `062d0f20`
and integrated by `1d44d2a`. The core's 16 product paths do not intersect P08's
authored range from `f9685530` through `723b067f`. Preserve both histories; no
reset, amendment or P10 WIP import. Resolve only additive source/copy-list
conflicts; report a product-content conflict before changing either owner's logic.
Do not modify the accepted P03/P07 implementation or import a new design.

The only new P08-owned fixture edit is in
`tests/integration/universal-work-lifecycle.py`: replace its two private
`profile_context._native_io_path` imports/usages in cleanup and the missing-pin
case with the exact trusted shared `native_paths.native_io_path`. Keep
`ProfileConfig`, `context_path`, root lengths, cleanup/error behavior, method
bodies and all assertions otherwise unchanged. Commit this consumer migration
separately. The root router, workflow guard, PLAN and routing assertions stay
byte-identical to `171daa8`; no further root repair is silently mixed into the join.
Any additional fixture/resource or product seam requires an exact scope report.

Run the original p8r5 18-wrapper preservation selection from the immutable
report's "Focused preservation command", retaining its slot-specific original
dimensions. Explicitly compare all 16 effective preflight fields for slots
c4/c5/c6/c10 against p8r5/p8r6: checkpoint paths 268/278/277/290 and Git ref-lock
283 are the required same-depth observations, not substitute shorter controls.
Run the final combined 574-assertion routing, eight mechanical scenarios,
39-method lifecycle and observation-learning suites on one sealed source.
The routing wrapper may be shared with the 18-wrapper selection when the source,
recorded environment and result identity are exactly the same; do not rerun it
merely to count it twice or manufacture an aggregate across different trees.

Retain actual exit, cleanup, byte/mode and source/EOL evidence, no skips presented
as passes. The newly accepted dependency does not retroactively repair shell27,
p8r2, p8r5/p8r6 or any earlier run. No real-home access/rollback, global settings,
shorter defaults, A13 producer/learning changes or generated output is released.

Freeze one new combined product/report with dependency and fixture attribution,
the unchanged root-correction blobs and the full historical report prefix retained.
Then the SAME reviewer `9dbf0a9b-750d-45c2-968c-41a5acb11c92` rechecks F01/F02/F03
at class level and completes SPEC for the original selected A08/A10/advisory-A13.3
subset. QUALITY starts only if that subset actually passes. Missing semantic/host
or preservation evidence remains explicit; no full-P08, A13 or release acceptance
is implied. Do not commission a duplicate standalone root-only run before this
combined candidate is frozen.

### Joined verification harness correction

**Scope:** verification only on immutable combined `2f4cc38`, 2026-09-21.
Dependency imports `7a4fcdba` / `1bb75560` attribute exact `ce7415f` / `5fc657f`;
the sole fixture consumer commit is `2f4cc38` (+5/-4). The coordinator verified
the four root-correction blobs, four reviewed runtime blobs and unchanged prior
report. No product, accepted test, root repair, shared ledger or A13 edit is released.

p8j1 remains INCOMPLETE: the first 18 wrappers failed 16/18, c19 passed, c20 was
interrupted and c21 did not run. Newly reached c4 plain-folder Git discovery found
the outer checkout; c6's two exact-size probes cannot fit their required target
lengths beneath its 140-character temp parent. The successful original checkpoint
paths and bisect case remain individual evidence, not a complete aggregate.

The original P08 owner may revise only its private verification driver/preflight
and execution commands, preserving prior driver bytes and evidence:

- Set per-process `GIT_CEILING_DIRECTORIES` to the verified synthetic command
  sandbox boundary or explicit boundary list covering its fixtures. The two
  special probes may name both their deep source sandbox and owned short-temp
  boundary, never their common personal-home ancestor.
  Prove actual Git discovery refuses an uninitialized plain directory and still
  recognizes an explicitly initialized child repository. Record the effective
  ceiling. Do not change `_context_branch`, Git config or the production environment;
  source-checkout Git verification must not inherit fixture isolation (L-036).
- Preserve original c4/c5/c6/c10 roots and all 16 existing preflight values. Run
  the 26 representable `ContextSafetyTests` methods at original c6 depth. Run
  only `test_native_checkpoint_thresholds_keep_logical_paths_and_owned_bytes`
  and `test_native_snapshot_atomic_publication_uses_218_to_260_without_shortening`
  separately, unchanged, under an explicitly verified unused synthetic temp child
  of this session's already assigned `files` directory, with parent length below
  94. No other personal-home/config path is authorized. Prove containment/ownership
  before creation, retain exact-root cleanup, and record the two allowed fixture
  roots without weakening the containment check globally.
- The two special probes must still assert actual 259/260/268/278/277/290 and
  218->260 operation lengths. Only their outer padding space changes; no tested
  path or original default is shortened. Enumerate the unchanged 28-method source
  and prove the named 26+2 partition is complete/disjoint, with no skips. Label
  this a split-fixture group, not a passing invocation of the old whole c6 wrapper.
- Redirect outer runner stdout/stderr to named in-repository runtime logs before
  launch, preserve the actual exit separately, and return only bounded summaries.
  Do not pipe the runner through tail/head or inspect/remove the reported host
  output capture. Verify any earlier owned test process has stopped before rerun;
  manage only known owned process IDs, never unrelated host processes or old fixtures.

First verify the affected discovery/deep/special groups. Once they pass, run the
final joined selection with the corrected isolated harness, including lifecycle
and observation. The source remains `2f4cc38`; track each real invocation, split
group, source/EOL seal, exit and cleanup. Do not relabel p8j1 or concatenate its
partial results into a fictitious passing run.

The tool-reported AppData capture is uninspected host-managed output, not proof
of a test HOME escape or proof that no external effect occurred. No personal-home
inspection/cleanup/rollback is authorized. Synthetic-only continuation remains
within the operator's existing boundary. Append the factual interrupted-run and
new verification evidence to P08's own report in a report-only commit; preserve
its full prior prefix. Freeze that checkpoint for the same selected-subset review.

## F04/F05 request-boundary correction

**Authority:** existing A08.1.r1/r2, 2026-09-22. Independent `402fd804`, preserved
report-only as `26c80a0`, is selected SPEC FAIL with two P2 mechanisms and QUALITY
NOT STARTED. All prior 21 cases pass; four of 12 predefined compound contrasts
fail. F03's bounded mechanical history-selection defect closes from valid c20,
not from q02, which remains INVALID with actual-home effects UNKNOWN.

The original P08 owner may implement this correction directly on preserved
`2f4cc38` / report `8eb00b87`, after reading the complete independent report.
Own only `lib/orientator-routing.sh`, its two existing focused routing tests
(`intent-operation-boundary.sh`, `orientator-mechanical-routing.sh`) and P08's
report. No workflow guard, PLAN, core/profile/parser, dependency, A13, generated
or shared-ledger change is released.

F04: initial and conjunction-position governing heads must consume one maintained
recognition rule/vocabulary, not independently edited verb lists. Resolve the
whole request before returning. Preserve useful object coordination, informational
complements, prohibitions and the established primary-head controls. Do not add
three isolated word exceptions, reject every conjunction or collapse independent
requests merely because they map to the same enum.

F05: retain structural line-boundary information until governing requests are
resolved. An independent newline request must not disappear into the first
clause; formatting-only wrapping must not invent an extra operation. Preserve
quotes/code, read/prohibition scope and supported whitespace/line-ending behavior.
No new public enum/signature, parser dependency, model call or lifecycle redesign.
The full nine-phase cycle and resume's utility status remain unchanged.

Retain all 574 assertions, eight mechanical scenarios and exact 33-row independent
matrix. Add class contrasts across supported primary heads in independent
conjunction/semicolon/newline positions, with wrapped single requests, object
coordination and read-complement controls. Expectations come from the declared
operation contract, not the implementation's own output. Preserve caller IFS and
all failed/invalid historical evidence. Use only verified synthetic child execution,
including any direct Python comparison; a child's preflight does not cover an
outer-process product call. Do not replay q02 or inspect/recover actual-home state.

Freeze the coherent routing correction and report its exact source/result scope.
Also provide a bounded evidence protocol for the ORIGINAL still-unverified
A08.2.c/.3.c/.5 and A10.1-.3: actual agent dispatch, semantic analysis/finding
carry-forward, selected external-task updates, cold resume and task-relevant
question/entry-point behavior. Synthetic INCOMPLETE prose cannot substitute.
Identify native actors, fixture-only write scopes, actual question/answer handling
and observable artifacts before those new executions; obtain the coordinator's
specific fixture/actor release. Do not build a scheduler or new result contract.
The next complete selected-subset review must cover these original controls as
well as the routing class, not begin another phrase-only acceptance loop.

## Bounded native semantic evidence

The F04/F05 product `70469296` / report `8318c308` is frozen. Intake verified
its three source paths, report-only parent, complete 1,456-line prior prefix
and LF hash `2651fb98...`; the entire 328-line append/protocol was read.
Mechanical self-results are not semantic acceptance. Execute the appended
"Proposed bounded original-proof execution" only under the limits below.

The original P08 owner is the delegated fixture controller, not a new runtime
engine. It may create the proposed previously unused
`.claude/runtime/p08-semantic-01/` in its own worktree, with verified ownership,
non-link containment and separate source/seed/target/home/temp/evidence paths.
Use a sealed read-only resource copy of exact `70469296`. Do not modify product,
shared plans/memory/ADRs, prior evidence or any other working repository.

Before any semantic actor, seal the actual fixture inputs and an oracle for:
the LOCKED source-order versus sorting-plan contradiction; the distinct advisory
documentation gap; original Alpha T011/T027/T042 and Beta's reused T011; deferred
T042 preservation; selected-only updates and prohibited research/publication writes.
Do not seed reports, accepted findings, completed phases, answers or checked tasks.
The oracle states required behavior, not text to be copied into an agent's result.

Use at most three sequential native fixture contexts: the actual registered
lintel-planner, lintel-builder and a fresh default/general-purpose resume context.
No nested writers/reviewers/factory. Exactly one actor may own a fixture's writes
at a time; the controller writes only dispatch/observation receipts while an actor
owns that target. Keep original source and sibling seals. Record actual selected
tools/context IDs and direct artifacts, not claimed native registration or labels.
All actor shell/product calls must use the inspected synthetic launcher/preflight;
outer collectors remain data-only. Missing/denied capabilities stay explicit.

MasterSession is the authorized SYNTHETIC FIXTURE decision owner. Forward an
actual native pending question with child ID/question/choices/request identity,
or the explicitly unavailable-tool bridge, before answering. MasterSession
chooses from the real question and records its response through the actual host
path. The same applies to accepting a genuinely produced advisory finding.
Do not preselect answers, invent a transcript or claim human/customer approval;
this is an actual tool interaction with a delegated fixture operator. A denied
question path is not bypassed.

Run the protocol's finite native orchestration, semantic analysis/carry-forward,
original Spec Kit task/cold-resume and intake/alternate-entry comparisons.
The selected positive cycle is local-only with SHIP explicitly SKIPPED, never
executed or DONE. Delivery aliases must still stop on missing publication/review
authority or rejecting evidence; do not capture a failed shipment as completion.
Use the existing P08 reviewer for any actual fixture review via MasterSession,
not a new synthetic independent actor or a self-approved review.

No broader client matrix, paid benchmark, publication, external action, hook
activation, real-home access/rollback or new scheduler/schema is authorized.
If a concrete product defect prevents a case, stop that case and report its
actual inputs/source/trace; do not patch the frozen resource during execution.
Return a report-only evidence checkpoint distinguishing actual native behavior,
mechanical checks, unavailable observations and every earlier failure. The same
independent reviewer decides which original controls close; no parent auto-closes.

## Independent mechanical-provider checkpoint

Recovery may independently accept the unchanged mechanical provider before the
whole P08 semantic unit. This staging is limited to A08.2.a/.b and A08.3.a/.b;
it does not accept routing, actual agent orchestration, semantic ANALYZE/intake,
original-task execution, A13 or any original compound parent. The original owner
confirmed no inseparable code/write dependency on those unfinished consumers.

Pin exact source `70469296`, report `8318c308`, and these unchanged `2f4cc38`
product blobs: `bin/li-work-artifacts.py` `771fc6cb79dcd5888ab75ffcceab53e92c2d7fc1`,
`lib/workflow.sh` `27afa35ef9a826da57c8972b94d959bec60fbd9f`, and
`lib/state.sh` `a7a3997bbef77d4e85942d5eebde1e21abf8d672`.
Keep actual accepted P03/P04/P05/P07 dependency closure, `paths.sh` and
`cycle-modes.sh`. Do not package three files with missing trusted dependencies.

The SAME P08 reviewer may review complete provider SPEC then QUALITY using its
own isolated source/fixtures and original authoritative contracts. Reuse valid
independent c20 evidence where unchanged, never invalid q02. Execute the existing
work-artifacts suite and selected real WorkSelection/Lifecycle/Recovery controls
for original maps/IDs, exact budget/dedup, P05 task-progress binding, fresh-shell
profile/map resume, drift/missing-pin/override refusal, two initiatives, cycle
identity, truncated/failed state writes and nonduplicated selected ranges.
All product calls and outer comparisons require verified synthetic processes.

The lifecycle module always loads `tests/unit/review_evidence.py`; preserve its
accepted dependencies and the two exact native-path fixture-consumer migrations.
Actual PLAN snippet claims require the frozen PLAN resource and its caller tests;
otherwise exclude those claims rather than substituting synthetic prose.
Do not run source-root-mutating cycle-continuity tests on a live checkout.
No frozen source repair, native fixture launcher takeover or new actor is released.

Write one report-only provider verdict under `reviews/`. On SPEC/QUALITY PASS the
coordinator may integrate exact providers and applicable owned tests, verify
installed closure and release P09/P04 consumers. Final semantic P08, original
parents and integrated workflow still require their existing separate review.
If a real provider defect appears, return it to the original P08 owner.

Recovery ownership refinement: the read-only `skills/status/SKILL.md` consumer
and its focused status tests are assigned to P13's remaining discovery/authoring
unit. P13 may consume accepted explicit-map/profile/state APIs but not edit them,
write a ledger or claim A13 event/learning acceptance. P08 retains every other
lifecycle/native-fixture/A13 responsibility and its frozen semantic sources.

### Selected legacy singleton recognition

P04's final CLI join reproduced an accepted-parser compatibility gap: the
explicitly selected Swarm lane `core` and authoritative `### core` definition
pass P04 validation/package derivation, but default `work_context` discards the
name because it sends no recognition hints. Do not force P05 approval/binding
merely to read a declared original ID or add a second heading parser.

The coordinator owns this narrow fix in the recovery checkout only:
`bin/li-work-artifacts.py`, focused existing provider regressions/runner and
direct documentation. Derive recognition from the map's explicit validated
coordination and existing P04 package/task APIs. Include the consumed
coordination in bounded input accounting; do not scan other initiatives or
promote arbitrary headings. Ordinary non-Swarm behavior and optional actual
acceptance binding remain separate and unchanged.

Verify named singleton, normal IDs, unrecognized ordinary prose, DRAFT read-only
inspection, wrong coordination/backpointer and exact budget/no-write behavior,
then relevant original provider and P04 consumer checks. Preserve the P08 owner's
original workspace and sealed `70469296` native fixture; no native case is re-pinned
or rewritten. SAME independent provider review must accept the frozen correction
before P04 consumes it; this reopens only the affected A08.3.a refinement, not
unrelated accepted component evidence or the original semantic task source.

Builder evidence in the recovery session's `files/verification/`: `named-work-red`
reproduced both omissions (three methods, two failures); `named-work-green`
passed all six selected new/retained controls. `provider-named-full` then passed
all 25 provider methods through the actual Bash entrypoint (219.954s, zero
failures/skips), including the original 22. All 39 pre-existing lifecycle test
method ASTs remain unchanged. Independent review and the P04 composed consumer
run remain outstanding; these results do not re-pin or clear native fixtures.

Independent `78e3c09` accepts exact `aa5cf0e` after SPEC then QUALITY, with
zero findings, eight new/retained provider methods, three P04 parser controls
and two actual CLI/binding/refusal probes. Its separate report history is
preserved as `14f97fa`, not imported as product ancestry. A08.3.a is reclosed
and P04 may consume the exact accepted provider. Its composed caller and
frozen `70469296` native workflow remain separate; no new native pin is implied.

## Original MAIN local CAPTURE after real integrated consumption

Recovery88 accepts the concrete proposal
`evidence/resume/main-local-capture-proposal-01.md`, SHA-256
`56155ed6fc20b313fc5ab60ba80fa7188199d45402cd4ab2ece0c0367bab3f6d`,
within original native authority `820884e`. This is the existing local positive
cycle's next bounded step, not a new task, broad approval or independent review.
The actual consumed packet is
`913d6864e9be1f8e5f23e26bff4b480aa58839e5453ac42ecc61e4da8aa36ca7`;
controller intake03 and recovery's data-only intake02 pass. Both original
T011/T027 producers, latest readers and same-context QA are real passes.
Their exact decisions remain `b369afc6` and `6ceab2a4`, not a combined package.

Original controller `f2c305ac-e8b0-4b02-b6cd-c7de598964cf` may grant only
existing writer `c2d5b8df-d84b-4e36-893a-cd7e28b63e1b` the MAIN target.
Preserve frozen source `70469296`, `specs/alpha/work.json`, cycle
`p08-alpha-main`, original b5 generation1 profile and required-policy binding.
Use the same inspected launcher; no additional actor or controller takeover.

The only durable target writes are:

- Append canonical original-cycle entries to `.claude/runtime/state/00-state.md`.
- Create `.claude/runtime/state/p08-alpha-main-capture.md` from an inspected
  original absence; do not overwrite an intervening file.
- Create/append only the canonical target-local `.claude/runtime/audit/cycle.jsonl`.

New request, raw-stream, receipt, preservation and final-pin evidence stays in
the already owned `evidence/resume` namespace. The controller alone changes
ownership/controller evidence. All other target/source/seed/sibling/original
workspace bytes remain protected.

First reverify `workflow_resume`, original map/profile/policy, both real latest
readers and their unchanged typed QA. Reconcile the actual REVIEW BLOCKED and
paused state through the existing retry/append APIs, preserving every old entry.
Do not start a new CYCLE or duplicate SENSE/SCOPE. Record REVIEW DONE only on
current passing gates, explicitly as owner reconciliation of already completed
independent stages, not a new review or test run.

Then perform actual local CAPTURE: aggregate only the original cycle and linked
work, record current selected-objective outcome and evidence, retain the real
history/limits, and run the advisory original-map/P03 budget reader with explicit
same-work warming paths. Unknown capacity/usage remains unknown; any byte-derived
estimate is labelled and cannot claim a verified fit or reclaimed context.
Do not enable optional budget audit, calibration, global lessons/promotion,
ADR/role/vault sinks, a dogfood actor or another model.

The proposed no-edit reaffirmation is explicitly accepted for this fixture.
Reassess original requirements/design/tasks/prompt against actual code/evidence
and record that assessment in the cycle-linked capture artifact. Current
T011/T027 completion and T042 deferral already have the correct original
checkboxes. Preserve raw bound task/prompt/build-log/analysis and all QA,
contexts, decisions, hosts, code/index and other control files. Explain dated
paused/draft prose as historical, with the actual closing outcome linked by the
original cycle's `capture_report_path`. This creates no replacement trio or
task source and is not a general exemption from reaffirmation or drift review.
If substantive drift is discovered, stop; this grant does not authorize its edit.

Recheck both actual consumers/QA after capture output and after final state/audit
writes. Use DONE or DONE_WITH_CONCERNS only for the verified local T011/T027
objective; preserve advisory unknowns and original scoped F002 disposition.
Keep T042 deferred/unchecked and the map APPROVED, never all-backlog COMPLETE.
SHIP remains SKIPPED: no SHIP helper, phase start, publication or DONE claim.

The existing audit helper is fail-open with a warning. Exit0 therefore does not
prove its required cycle record exists: inspect the actual target-local JSONL,
its original prefix and the newly written cycle/outcome fields before reporting
successful closure. Likewise verify complete appended ledger entries, not only
command exit. Any unexpected write/readback/post-gate failure stays explicit;
preserve the partial history and stop rather than rewriting old entries/digests
or claiming current completion.

Return the exact three-path delta, append/readback evidence, actual budget
result, both final consumers/QA, final preservation pin and post-exit host
quiescence. Whole P08/A13, other sixteen targets, broad quality, installed and
parent release gates remain separate. Earlier controller observer failures and
recovery's wrong ownership-path intake failure remain history; only the
collector path was corrected to the manifest's `evidence/controller/ownership.json`.

## Remaining original delivery-refusal pair

MAIN's selected local cycle is now empirically complete, not shipped. Its final
packet `6d1edc70c96f7c935f810ebb808cb3318e474e67dbf5e6ab5df8c35690d98bd2`
has actual complete ledger/audit readback, unchanged bound inputs, and both
current readers/QA through all six boundaries. Recovery's data-only final
intake02 verifies 1,310 snapshots, 97 current MAIN pairs, five complete appended
entries, the actual cycle JSONL, twelve real reader/QA observations and the
honest unknown-budget result. The earlier intake01 blank-line parsing error
remains retained; no fixture byte or product behavior was changed. MAIN stays
quiescent and outside subsequent writes.

Release only the already scoped `ship-direct` / `ship-alias` comparison in the
original `evidence/case-inputs.md` and prepared
`evidence/controller/resume-delivery-brief.md`. The existing controller may grant
the same final c2 context one target at a time. No fourth actor, replacement
context or expanded comparison matrix is authorized. Source remains exact
`70469296`; all completed MAIN/planning/research/build/fix targets remain frozen.

Use the existing sealed `targets/ship-direct` and `targets/ship-alias`, with
equivalent attributable implementation and original Alpha work/approval input.
Before any transfer, record their original preimages and trace each copied
product or original-work artifact to its exact genuinely implemented snapshot.
Retain the original seed/history, T011/T027/T042 and Beta. Copy only the needed
code, tests, README and original selected Alpha artifacts; never copy MAIN's
profile storage, ledger, audit, completed cycle, review/host/QA clearance or
personal data. Record inherited implementation as inherited, not code newly
authored in the comparison. Each target must have its own actual verified P07
reference and original-map/cycle identity; different location-bound digests are
expected, not evidence of substantive inequality.

Execute the real canonical CYCLE REVIEW-to-CAPTURE method in `ship-direct` and
the real `review-and-ship` composition in `ship-alias`, using the original
frozen callers, explicit maps and inspected per-target launcher. The two cases
must receive equivalent substantive input, authority and applicable obligations.
No extra SENSE/SCOPE, founder interview, task source, approval or private alias
status is introduced. Any local validation actually required by the reached
method must run in that target and retain real counts/exits; inherited test
evidence is not a new local run.

Record the actual earliest unmet prerequisite and phase. A missing applicable
review is not a publication-authority refusal, and neither is a genuinely later
rejecting review. Preserve those distinctions. If a real target-specific review
is needed to reach the remaining original control, freeze exact obligations/
context/QA and commission only existing9db through recovery88. Do not invent
PASS/FAIL, reuse another root's clearance, demand a rejecting verdict, or alter
a successful input merely to manufacture the rejection branch. Any requested
condition not actually observed remains explicitly unverified for final review.

Publication authority is absent throughout. No network, GitHub, push, PR,
deployment, registry, credential or external-delivery operation may run.
Do not ask for unrelated permission to turn this comparison into real delivery.
Do not mark SHIP DONE, skip its denied/missing gate to reach a success message,
or mark CAPTURE/CYCLE complete as if shipment occurred. If the existing method
captures an unfinished result, keep `cycle_complete=false`, actual blocked
outcome and original unfinished phase. This is not MAIN's local positive-cycle
closure and cannot inherit that outcome.

Only the currently granted comparison target's explicit original product/work
transfer, local canonical workflow/evidence records, necessary real validation
outputs, and its new resume-owned requests/receipts are writable. Controller
ownership changes remain separate. All actions use the existing inspected
launcher and original authority; no generic dispatcher, parser, source repair,
policy weakening, cleanup, real-home/global operation or denied-tool workaround.
Finish and observe quiescence before switching the single-target grant.

Freeze the paired artifact/provenance/status comparison and concrete gaps,
with actual gates, before/after preservation and final host quiescence. Return
it to recovery88 for the existing review path and report-only P08 freeze. This
completes only the observed finite case boundary; whole P08/A13, the original
selected-subset SPEC/QUALITY and final initiative gates remain separate.

## F06 original negative-control verification re-plan

**Status: bounded verification design approved by coordinator88; target execution
is gated on the existing independent reviewer's scope/method check below.**
This is a continuation of original A10.3, not a product repair or a new initiative.
The full independent report `624b554f`, preserved separately in recovery, leaves
selected SPEC UNVERIFIED and whole QUALITY NOT STARTED for one P2 evidence gap:
the real direct/alias reviews passed, so their missing-review and publication
refusals do not establish a later rejecting native review. No product defect was
reproduced by that review and no original parent is closed here.

Three alternatives were considered. Leaving F06 open preserves evidence but
does not finish original acceptance. Reclassifying mechanical FAIL fixtures or
other-package findings as the missing native verdict would be a false substitute.
The selected approach is a declared, narrowly controlled regression of the
original known sorting defect in the two existing disposable delivery targets,
followed by actual unbiased independent review and actual native refusal
consumption. It does not depend on an accidental bug appearing in already-correct
work, and does not instruct anyone to fabricate a review result.

This explicitly supersedes the earlier no-more-attempts/no-mutation limitation
**only for the two negative attempts described here**, within the operator's
existing local verification authorization. It is not an exception to truthful
evidence, independent review, tool permissions or publication limits. It grants
no new target, fourth actor, model, parser, provider/source change or cleanup.

### Pre-execution method check

Before any target mutation, SAME9db must assess this proposed evidence method
against F06 and the original A10.3 criterion, read-only. Confirm whether actual
independent assessment of an explicitly regressed candidate followed by the
native current-record consumers would supply the missing observation. This
check is not product acceptance or a requested future FAIL. If it identifies
a scope contradiction or insufficient isolation, return that concrete issue;
the coordinator resolves it before BUILD. No negative attempt starts merely
because this section or a proposed file exists.

### M01 post-report launcher guard correction

Actual method review `f06-method-8ba5770/method-review.md`, SHA-256
`c08335a268921d1bff4ca3dc225016de08da4510d52beda78207490447028af3`,
passes evidentiary SPEC fit but requires this P2 plan-quality correction before
target execution. Changing result bytes legitimately needs a new current
context. Actual latest same-scope rejection can satisfy F06; it must not be
described as revoking a still-applicable identical-byte PASS by verdict alone.

The unchanged private launcher still expects the original831 report and would
refuse before its payload after the authorized report-only229 commit. No failed
launcher run is claimed: this mismatch was established by source and byte reads.
The coordinator authorizes original controllerf2 to implement and verify only
an additive private `p08-semantic-01/launch-f06.py` guard correction now.
Keep the original `launch.py` SHA
`3954894aee93d7f316fc697297f5add8a429a53410ef9db5c31f317673d0e62c`
and original-workspace manifest SHA
`d5c1968f37d3bcc633d47904b49aef3ecd11537373d75292ea2706fdfefa11c6`
unchanged and pinned by the new guard. Pin the existing source-seal identity too.

Derive the new file from the exact old launcher at the same private root.
Preserve every non-guard behavior: ownership, paths, environment, preflight,
captured commands/streams, seed/oracles, sibling inventories and before/after
seals. Check all960 frozen704 source bytes/inventory and959 other original
checkout files as before. Only the ORIGINAL checkout's
`.claude/plans/universal-implementation/reports/P08.md` may differ from the
old manifest: assert its old expected hash
`9755271d86d72ba6b6118b897ce229268af1aaafa4288eecf36177849c44d5b6`,
then require the exact authorized229 raw248073 bytes/SHA
`b613452e0b21862ddf26892018d72d38972d1234ade1d571e7d346fad73f8a39`.
Return explicit old/current report identities in the before/after seals.
There is no arbitrary override option, additional exemption, manifest rewrite,
source repin, original-report restoration or permission to skip `frozen_seals`.

Validate this new PRIVATE read-only guard on actual current frozen inputs plus
clearly labelled synthetic/in-memory mismatch cases for report, other original
file, source and manifest changes. Never mutate those protected actual inputs
to test the guard. Do not call launcher `main`/`environment`, run product/test
payloads, grant ownership or touch targets during this correction. Freeze the
derived code/diff, exact identities, checks and next native request for SAME9db's
M01/plan-quality recheck and the coordinator's concrete execution release.
The bounded private correction is authorized; the negative native attempts are
still gated. It is not a product/source repair or an expansion of tool authority.

### Exact candidate and ownership

Use only existing controllerf2, original native writer c2 and original reviewer9db.
Keep one target writer at a time, in `ship-direct` then `ship-alias`.
All seventeen targets are initially quiescent. Preserve their already-frozen
positive/missing-review/publication-refusal packets, original decisions, hosts,
contexts, QA, report229 and review624 unchanged. Source stays exact704; MAIN
and the other fifteen targets are not negative-test subjects.

The sole candidate product mutation is in each selected target's `labels.py`:
replace the single `return labels` statement in `normalized_labels` with
`return sorted(labels)`, preserving every other byte and the CLI. This restores
the original seed's actual known sorting behavior, which conflicts with the
unchanged LOCKED FR-ORDER and existing unit/CLI assertions. It is deliberately
introduced test input, not a newly discovered Lintel defect or an accidental
regression. The same one-line delta is used in both targets; no test, README,
requirement, task, map, policy or original acceptance criterion is changed.

First verify current original P07 references and the exact prior reviewed
source. Capture the original owned `labels.py` preimage through existing P03
before its conditional write. A different preimage, unexpected active process
or other-target change stops the affected action. Record the full before/after
bytes, hashes, diff, source-seed attribution and explicit injection purpose.
No whole-file EOL rewrite, automatic baseline replacement or hidden exclusion
is permitted. Do not restore or repair the negative subject in this tranche;
it stays an intentional synthetic negative until separately authorized.

### Real observation and independent review

Use fresh, uniquely named negative attempts and outputs under each target's
existing runtime/evidence area. Preserve original singleton/package/leaf
identities T011 and T027; there is no P1 or combined delivery package. Retain
same-root profile/policy and the full existing required-control and typed-QA
declarations. Original positive contexts remain immutable historical results.
Prepare the new exact result context rather than relabel an old PASS.

Both original cycles are REVIEW DONE and SHIP BLOCKED. Use their original
resume and explicit REVIEW retry/reconciliation APIs, not plain completed-phase
begin, a new cycle, fabricated BUILD or implicit continuation into SHIP.

Run the unchanged target-local full command once in each target under its
existing inspected launcher. Retain every actual case ID, failure/error/skip,
exit and raw stream; a failed required test is failed QA, not a passing run.
Perform the real author README-versus-candidate check and preserve its actual
nonindependent outcome. A missing tool, zero run, unrelated unexpected error or
permission denial remains a separate stop, never an intended test failure.
The schema-valid QA envelope may be blocked and its documented nonzero CLI
exit must be retained; do not downgrade or retype controls to get a green log.

Pin the actual negative candidate and observations, quiesce/revoke its writer,
and commission SAME9db through recovery88 for a new target-specific SPEC
assessment. Disclose the deliberate original-defect injection and exact scope.
The reviewer decides from the unchanged requirements and actual bytes/evidence,
not a requested status or a controller-authored decision. QUALITY and compliance
must not be represented as performed if their SPEC prerequisite fails. Preserve
all mandatory IDs, exact typed QA and complete original-singleton coverage in
the returned v2 record, including honest fail/unverified outcomes. No hand-edited
`status: fail`, invented actor event, borrowed verdict or reviewer repair.

If the actual independent result is not rejecting, retain it and return F06
open; do not coerce a different verdict, add further faults or repeat review
until a desired status appears. A rejected candidate is evidence about this
declared synthetic negative, not rejection of the preserved earlier valid one.

### Native negative consumption and final boundary

For an actually returned rejecting decision, recovery88 supplies a new real
decision/attempt/actor-bound observation. Only the original writer, under a
renewed single-target grant, publishes those exact bytes through original P03
preimages and invokes the real frozen review producer, latest applicable reader
and read-only SHIP readiness consumer. The new context must match the rejecting
decision and its real failed/unverified controls. Record whether the latest
reader consumes the genuine rejection and refuses, rather than choosing an
older PASS; verify both original singleton contexts. An old PASS's changed-
snapshot mismatch, failed QA, later actual review rejection and absence of
publication authority are distinct observations, not interchangeable labels.

Exercise the same canonical direct versus alias entry methods and preserve
the actual earliest rejection stop. Do not progress past rejection into SHIP
execution or CAPTURE success; do not attempt any network/publication or ask to
authorize it. Read back actual ledger/audit records and retain the old prefixes.
No cycle-complete, valid-deliverable or arbitrary-input-equivalence claim follows.
The read-only P05 `ship` consumer is only a scoped diagnostic while canonical
REVIEW remains BLOCKED/resume REVIEW. Its actual review rejection precedes QA
evaluation; report that short-circuit, not a QA pass or SHIP-phase advance.

Freeze both attributed negative outcomes and all earlier positive histories,
then return a report-only appendix to the original P08 report. SAME9db rechecks
F06 within the complete selected specification and may begin the first whole
selected QUALITY only if the original SPEC actually passes. The prior blocked
report remains immutable. Any additional condition or source defect returns
for a specific decision; this release does not start an open-ended adversarial
campaign or change A13/P10/installer/format/CI/remote boundaries.

### First F06 native candidate release - direct only

SAME9db's immutable M01 recheck
`d116b057962dc255a66efe57c37e83face3c6e065e4738fa5ab31e7c00157c13`
closes M01 and passes the bounded preparation/next-direct plan QUALITY, with
zero P1/P2/P3 findings. Prior c083 method SPEC fit is retained. This is not
original F06 closure or whole selected-subset QUALITY. Recovery read the whole
recheck and verified its report, passive check, process and final-preservation
identities; all68 reviewer input records are retained unchanged.

Coordinator88 now releases original controllerf2 to grant **only existing
ship-direct to SAMEc2** (`c2d5b8df-d84b-4e36-893a-cd7e28b63e1b`) and deliver
the exact parked request from the independently checked private packet:

| Immutable input | SHA-256 |
| --- | --- |
| Preparation packet | `c5d00ee8dcbd0eed0dd384d836ba15ac118a047a37f108b61b738c475c83cd25` |
| Derived launcher | `0ada32c28018405f086adc89a3e446c7c5d40b0c78cd1516f61dd42fc9684c7e` |
| Complete244-line direct brief | `d23421d0c813f2d06f3fc9fd73db7b1d88c65020860f5674d58d39241fc8396d` |
| Parked next-direct request | `8a7794ab4c4253ddaa2e17431f8469f450f7e5430455461717fa49d26a4b10f5` |
| Exact candidate path set | `fdaacfa823732b0f3032d2ee5616edc17f67e130b482589be0e25b50407818da` |

These files live at the existing
`p08-semantic-01/evidence/controller/f06-preparation-01` boundary; the launcher
is the separately named file at that same original fixture root. Preserve every
issued byte and supplement delivery with this actual release, not a rewrite
that pretends the parked request was already authorized.

Apply the complete inspected brief: verify the original profile/map/base/index
and exact938-byte source, explicitly resume/retry REVIEW in the old cycle,
use actual P03 preimage/conditional-write/result binding for the sole946-byte
postimage, and preserve all other product/control inputs. Only the18 named
fresh target runtime files, the two named append-only ledger/cycle-audit paths,
one existing `labels.py` replacement and the declared actor-side request,
P03 store, capture/pin/quiescence evidence are writable. Original tests, README,
work/task/approval/profile, old reviews/QA and all sixteen sibling targets
remain unchanged. No staging, source commit, restore or broader write follows.

Run the unchanged own13-case suite once, retaining its actual failures, counts,
streams and exit; assess the unchanged README honestly and retain actual blocked
QA bytes/nonzero results. Actually prepare only the proposed fresh T011/T027
contexts. Preserve the new-candidate pending-review stop separately from old
PASS drift and any later genuine rejection. No review verdict is preassigned.

End candidate-only at REVIEW BLOCKED, CYCLE PAUSED/incomplete, resume REVIEW.
Read back all canonical appends, freeze the exact candidate and preservation
evidence, observe actual post-exit quiescence and revoke the single-target
grant. Return through88 to SAME9db for independent candidate SPEC. Negative
decisions, reports, hosts and the existing review audit remain outside this
candidate grant; their real publication/consumer step needs a later release.
Alias remains ungranted. All source704/report229, private-home, denied-tool,
no-new-actor and no-publication boundaries remain intact.

### F06 direct pin-only preservation correction

The five-call direct grant ended with actual outer/child exits0/0/1/3/1.
The one13-case suite produced four genuine order-assertion failures, zero
errors/skips; fresh T011/T027 preparation succeeded and actual QA remained
blocked, including failed author documentation. The final pin separately
failed its grant-time full-native MAIN comparison before producing a candidate
packet. That exit1 is neither the intended negative test outcome nor an
independent rejection. The exhausted lease is revoked; F06 and review624 remain
open, and no candidate review may start on an absent final packet.

Controller diagnosis SHA
`5c8cd35cd3e3576892c9c3c500291d689badbd3c2ddcd451d10593a0726a97d7`
and parent intake `p08-f06-pin-diagnosis-binding-01` establish the actual
distinction. All566 grant-time MAIN file hashes/sizes/modes and directories
are preserved, but six same-c2 checkpoint refs and six loose commit objects
were added, with seven directories. Each verified object names the same
preexisting tree `9118276b4cdc0313b5c26c9bf4d873532acf2bd8` and sole parent
`e2ec89dd67158291a7410f4fd9c24e433462a6bd` as checkpoint58. The other15 siblings
are exact. Direct has only its declared sorting change,18 fresh records and
two prefix-preserving appends.37 diagnosis snapshots and290 stopped actor
artifacts are bound. Literal checkpoint metadata is consistent with host
bookkeeping, not authenticated proof of its writing process or full equality.

Options were to keep F06 blocked, accept only the currently enumerated additions
and risk another stop on the next host checkpoint, or distinguish this exact
content-preserving checkpoint class through a narrow observer. The coordinator
selects the third within original local verification scope, without relaxing
product preservation or granting a writer access to MAIN.

**Released now: private correction and preparation only, owned by controllerf2.**
Create one additive private observer/test/preparation area
`evidence/controller/f06-pin-preservation-01` under the existing fixture.
Do not change `launch.py`, reviewed `launch-f06.py`, source704, report229,
any historical manifest/failed caller/stream, or any target file. Keep the
original grant-time full-native manifest944bd58f and diagnostic current
manifeste5e8b1a8 separately pinned; neither becomes a replacement baseline.

The observer must preserve every original MAIN file and every diagnosed
addition, including hashes, sizes, modes and directory membership. HEAD,
index, branches, previous checkpoint refs, non-Git files and existing objects
remain exact. The only admissible further additions during the later single
pin attempt are regular, non-reparse ref files under precisely
`.git/refs/copilot/checkpoints/c2d5b8df-d84b-4e36-893a-cd7e28b63e1b`,
with new ordinal/GUID suffixes, and their one-to-one corresponding new loose
COMMIT objects. Require exact ref syntax, verified Git object SHA-1/type/body
length, bounded complete decompression without trailing bytes, exactly the
preexisting tree and sole parent above, and internally matching context/event
metadata. Record author/message fields as declarations, not authentication.
Only the minimal new directories needed for these admitted files may appear.
An original-file change/deletion/mode change, new tree/blob/pack/log, other ref
namespace, unresolved/malformed/extra object, type/link change or unexplained
file/directory addition must stop. Do not run Git or modify metadata to make
this check pass. The other15 siblings retain exact full-native comparison.

Retain the original false equality assertion and failure as history. New
evidence must report preserved original states PLUS explicitly listed
checkpoint additions, never `all_siblings_unchanged` for a nonidentical tree.
Validate the observer with the actual frozen diagnostic bytes and positive/
negative in-memory projections; do not mutate protected inputs to test it.
Include malformed/ref-object mismatch, wrong tree/parent/context/namespace,
extra object/directory and changed/deleted/mode/type original-state cases.
No launcher main, profile/work/test/QA/native payload or target write is
released during preparation. Preserve actual test streams/counts and all
stopped candidate evidence; a private observer test is not a native case.

Prepare a separately named pin-only caller/request for SAMEc2, derived from
the exact failed pin with a complete diff. Its intended changes are the
reviewed MAIN accounting seam, truthful output fields and fresh evidence
destinations. It must not repeat source mutation, tests, document assessment,
context preparation, QA generation, state/audit writes or P03 capture/binding.
Existing P03/context/QA may be verified read-only with actual results kept
distinct from their original generation. Preserve the partial failed-pin
outputs; never reuse an occupied output path or overwrite the old request.
No new actor, target, source repin or same-context identity substitution.

Freeze the observer, discriminating checks, exact pin-only diff/request and
immutable preservation inputs for SAME9db's bounded SPEC then QUALITY review.
Only after that result and a separate concrete88 grant may ONE SAMEc2 pin-only
attempt run. That future grant may write only new actor/controller-side
evidence and ownership/quiescence records, not target runtime or Git content.
Account for the same narrowly admitted additions again at post-exit sealing.
An unexplained delta still stops without fishing retries or automatic repair.
Alias and negative-record publication/consumption remain ungranted.

### Reviewed direct pin-only release

Independent SAME9db review `f96d6b85c9e0e020c24384d690e233669ba9ba31836bd102f9a0cc07e76076f0`
and handoff `6a65e6a7cf880fb2d9722d13c3a979e2bb2d57540a2d6435eb951fb799c70510`
pass bounded SPEC then QUALITY with zero findings. The coordinator read the
complete report and verified its identities and unchanged package/empty lease.
Prior parent375-artifact intake remains applicable; do not rebuild that proof.

Coordinator88 now authorizes controllerf2 to issue the reviewed
`evidence/controller/f06-direct-pin-release-01/grant.json` and grant only SAMEc2,
existing ship-direct, exactly one `ship-direct-f06-pin-02` invocation.
Bind this release's actual commit and immutable packet
`bdb85a5a32c04faf544e6527b1162183b43f6f3e80dfc76139d9228d82cfbeda`;
require `pin_only=true` and `target_writes_authorized=false`. Deliver exact
parked request `e1e191d076c6207455d9b11e20cfd0e93bd9756b3a62fc1948c01d7d291c96ed`
with this release. The packet pins the complete brief, caller5af8fdd1,
native requestc1d47cd4, observerba4e0df9 and postexitcbe2804b; no edits to them.

Follow the reviewed pin-only brief and finite new evidence destinations.
No target content/Git/runtime write, mutation/restore, test/document/QA/context
generation, state/audit append or P03 capture/bind is authorized. Preserve
every stopped record and actual earlier failure. A successful pin is evidence
assembly, not a passing candidate or an independent rejecting review.

After the actual callback, record real exit/quiescence and invoke the reviewed
`postexit.inspect_postexit` against the actual pin-after observation, preserving
EVERY pin-time addition. Standalone current-state comparison is insufficient.
Retain post-exit accounting separately from the earlier packet, revoke the lease,
and return the exact packet/host/preservation result through88 for candidate
SPEC. Unexpected differences stop without another retry. Alias, review-record
publication, later negative consumers and original F06 acceptance stay gated.

### Pin03 raw-byte transport correction

Controller bundle `evidence/controller/f06-direct-pin-intake-01` (handoff
`37a307a81c379ea5f4586d8b823124bdd7429cc0d59eb7c01424e1f415607ee2`, failure packet
`021bf29e`, byte diagnosis `56304c12`, revocation `88660fac`, ownership `624e6b4c`)
records that reviewed pin02 stopped at caller line126. SAMEc2's copied request
gained eight CR bytes: 1,625 bytes `95d4ad54` versus reviewed 1,617 bytes
`c1d47cd4`. The unchanged caller correctly refused before any MAIN observation,
so no candidate packet or post-exit result exists. Parsed equality never waives
raw identity. Every pin01/pin02 output stays immutable history and is never reused.

Coordinator88 authorizes controllerf2 one transport-only correction. Reviewed
logic is unchanged, so no new independent review precedes it:

- In new area `evidence/controller/f06-pin-transport-01`, derive
  `ship-direct-f06-pin-03.py` and `ship-direct-f06-pin-03.request.json` from
  reviewed caller `5af8fdd1` and request `c1d47cd4`. Allowed changes are the
  enumerated fresh identifiers only: pin-02 to pin-03, candidate-02 to
  candidate-03, quiescence-02 to quiescence-03, `f06-direct-pin-release-01` to
  `-02`, plus one path variable for the derived self-copies in this new area.
  Keep LF bytes. A checked script must prove that the reviewed bytes, after
  exactly these substitutions, equal the derived bytes. Retain the complete diffs.
  Observer `ba4e0df9`, postexit `cbe2804b` and packet `bdb85a5a` do not change.
- SAMEc2 places both files in `evidence/resume` by byte-exact file copy
  (for example `Copy-Item -LiteralPath`). It then asserts each destination's
  SHA-256 and byte count BEFORE the launcher invocation. A mismatch stops without
  consuming the invocation. Create/edit/apply_patch or re-serialization are not
  permitted for these files.
- Issue `evidence/controller/f06-direct-pin-release-02/grant.json` binding this
  commit, packet `bdb85a5a` and the derived identities. It sets `pin_only=true`
  and `target_writes_authorized=false`, and allows exactly one SAMEc2
  `ship-direct-f06-pin-03` invocation through unchanged `launch-f06.py`.
- After the callback, record the real exit and quiescence. Apply reviewed
  `postexit.inspect_postexit` to the actual pin-after observation, preserving
  every pin-time addition, then revoke the lease. An unexpected result stops
  without retry.
- On success, send the exact frozen candidate-03 handoff to SAME9db for candidate
  SPEC, then eligible QUALITY, under "Real observation and independent review".
  Send the same handoff to coordinator88 at the same time. Coordinator intake
  runs in parallel and may halt that review. On failure, return only the failure
  identities.

Any new actor or subagent follows the operator model directive: `claude-opus-5.5`,
max reasoning, `long_context`. No new actor is released here. Alias, review-record
publication, later negative consumers and original F06 acceptance stay gated.

### Candidate-03 intake and parallel remaining-F06 preparation

Controller handoff `evidence/controller/f06-direct-pin-intake-02/handoff.json`
(6,204 bytes, SHA `50022870509e42c596f06c85924ffedda228d202db22800c0077092ff724c69d`)
binds candidate-03 packet `2890b808` (431,417 bytes). Coordinator88 ran a
data-only intake without any Git command in the fixture, and all 37 checks
passed:

- every bound artifact's size and hash matches;
- an independent recomputation of the pin03 derivation from reviewed `5af8fdd1`
  and `c1d47cd4` equals derived `ab3d61f8` and `5584c0f0`;
- both transported copies are byte-exact;
- observer, postexit, packet and brief are unchanged;
- the grant binds `55dec314` and `bdb85a5a`;
- revocation `ee84c45c` is consistent, current ownership `a5b085fc` is empty,
  and the outer exit is 0.

There is no halt. SAME9db's candidate SPEC continues independently, and no
verdict is assumed or requested.

To remove serial idle time, controllerf2 is released now to **prepare only** the
remaining F06 native packages. No grant, target or Git write, launcher `main`,
native payload, test or QA run is permitted. Three packages go in new private
areas under the existing fixture:

1. **`f06-direct-consumption-preparation-01`**
   - Derive it from the reviewed positive review-publication/consumer mechanism
     and the reviewed observer/postexit seam.
   - Parameterize the decision input; never guess it. The exact decision record
     bytes, SHA, attempt, actor and observation are bound only when coordinator88
     supplies an actually returned rejecting SAME9db record.
   - The caller publishes those exact bytes through original P03 preimages and
     conditional writes.
   - Through the canonical direct entry, it invokes the real frozen review
     producer, the latest applicable reader and the read-only SHIP readiness
     consumer for both T011 and T027.
   - It records whether the latest reader consumes the rejection and refuses,
     rather than choosing an older PASS.
   - It keeps the P05 `ship` review short-circuit distinct from a QA result.
   - It reads back ledger and audit with their old prefixes retained, and stops
     at the earliest rejection.
2. **`f06-alias-preparation-01`**
   - Derive the ship-alias negative candidate from reviewed preparation `c5d00ee8`
     (launcher `0ada32c2`, brief `d23421d0`, request `8a7794ab`, path set
     `fdaacfa8`) and the reviewed pin seam (`ba4e0df9`, `cbe2804b`, pin03
     lineage).
   - Apply the same single `labels.py` line mutation, after verifying the
     alias preimage.
   - Use the observer-based pin from the start, with byte-exact transport and
     SHA-256 and byte-count assertions before invocation.
   - Preserve ship-direct as an exact sibling at its actual pre-alias state.
3. **`f06-alias-consumption-preparation-01`**
   - Mirror package 1 for ship-alias through the canonical `review-and-ship`
     entry, again parameterized on a future actual alias decision.

Every package requires:

- a checked derivation proof that the reviewed bytes, plus only enumerated
  changes, equal the derived bytes;
- complete diffs, with every non-substitution logic change listed explicitly;
- private checks on actual frozen inputs plus labelled in-memory mismatch
  projections, without mutating any protected input.

controllerf2 may use at most one own-session helper per package under the
operator model directive. A helper writes only that package area and is not a
native actor, writer or reviewer.

Freeze all three packages and send one handoff to SAME9db for a bounded
preparation SPEC, then eligible QUALITY, queued after its candidate-03 result.
Send the same handoff to coordinator88 at the same time.

Execution stays separately gated, with one target writer at a time. After an
actually rejecting candidate-03 record and one concrete88 release, f2 may run
direct consumption and then the alias candidate. Each needs its own grant,
post-exit, quiescence and revocation. Afterward, the alias candidate goes to
SAME9db, and alias consumption needs that actual result plus its own grant.
A non-rejecting candidate-03 result leaves F06 open and package 1 unused; it
is never tuned toward a desired status. No SHIP execution, CAPTURE success,
network or publication, restore, repair or new target follows.

### Candidate-03 rejecting record and chained execution release

SAME9db's independent candidate review under `55dec314` returned **SPEC FAIL** for
both original singleton packages:

- **F06-D01 (P1).** `labels.py:14`, propagating through `:24` and `:29-30`: the
  deliberate `sorted(labels)` contradicts the unchanged LOCKED FR-ORDER source-order
  acceptance.
- **F06-D02 (P2).** `README.md:12-13`: the source-order promise is false for the
  injected candidate, so mandatory FR-DOC fails.

Both findings have confidence 10/10. QUALITY and mandatory local compliance were
**not run**; they remain unverified and ineligible.

The return is bound as follows:

| Artifact | Bytes | Hashes |
|---|---|---|
| Sealed delivery manifest, `reviewer-9db/f06-direct-candidate-03` (24 artifacts) | 7,413 | `744167d6` |
| Report | 13,530 | `f0547876` |
| T011 decision | 17,277 | raw `83921d88`, canonical `efb1bce5`, context `f54a6ba6` |
| T027 decision | 22,933 | raw `900f49fc`, canonical `4a9e1255`, context `085bd818` |

Coordinator88's data-only intake passed 27/27 checks. It rehashed every bound
artifact and recomputed both canonical and context digests with the integrated P05
`content_digest`. Both records pass P05 `validate_review`, a pure shape and
internal-binding check, not clearance. It also confirmed status `fail`, the
mandatory controls and the named findings.

**This is the actually rejecting candidate-03 record that the preceding section
requires.** The findings describe the declared synthetic injection, not a newly
discovered defect.

To remove round trips, coordinator88 releases the remaining F06 execution now as one
ordered chain. Each step is conditional and runs only after the previous step
completes. There is one target writer at a time: the existing native writer SAMEc2,
on its current model. Every invocation also needs:

- its own controller grant file binding this authority commit, the exact reviewed
  package, the exact decision bytes it consumes, writer, target and single
  invocation;
- the reviewed pin-state-preserving post-exit contract;
- quiescence and revocation.

The chain:

1. **Direct consumption.**
   - Prerequisites: SAME9db's bounded preparation review returns SPEC PASS, then
     eligible QUALITY PASS, with P1 and P2 at 0 for
     `f06-direct-consumption-preparation-01`.
   - The grant binds the two decision records above by raw, canonical and context
     digest. The caller publishes exactly those bytes through the original P03
     preimages and conditional writes.
2. **Alias candidate.**
   - Prerequisites: step 1 finished as an execution, meaning the expected exits
     and a passing post-exit, quiescence and revocation. Whether its consumer
     refused or not is an observation, not a gate. The preparation review must
     also pass for `f06-alias-preparation-01`.
   - The alias baseline preserves ship-direct exactly at the state recorded by
     step 1's chronological post-exit.
   - Send the frozen candidate to SAME9db for candidate SPEC, then eligible
     QUALITY, and to coordinator88 at the same time.
3. **Alias consumption.**
   - Prerequisites: SAME9db returns an actually rejecting alias record, and the
     preparation review passed for `f06-alias-consumption-preparation-01`.
   - Before its grant, f2 performs and records the same binding checks as above:
     raw, canonical and context digests, `validate_review`, SPEC `fail`,
     QUALITY/compliance `unverified`, and the exact T011/T027 IDs.
4. **Capture and recheck.**
   - f2 appends one report-only F06 appendix to `reports/P08.md` in one normal
     commit on the P08 branch.
   - It then sends SAME9db the F06 recheck with whole selected SPEC, then
     eligible QUALITY, of `624`, and sends coordinator88 the same handoff.

Every step sends its frozen handoff to coordinator88 at the same time as it
proceeds. Coordinator88 may halt any invocation that has not started yet.

The chain stops, and f2 returns only the failure identities, on any of these:

- a non-rejecting review result;
- a preparation finding at P1 or P2;
- a fail-closed stop or unexplained delta;
- a transport or hash mismatch;
- a host checkpoint outside the reviewed class;
- a coordinator88 halt.

A stop allows no retry, repair or tuning without a new release. No SHIP execution,
CAPTURE success claim, network, publication outside the fixture, restore, new target
or new actor follows. A helper, if used, follows the operator model directive.
F06 stays OPEN until SAME9db's recheck and coordinator integration.

**Preparation handoff and step-1 host receipts (2026-09-24).** f2 froze the three
preparation packages in one handoff (`handoff.json`, SHA-256 `4c87ce41…c2c0a6e1`) to
SAME9db and the coordinator:

- direct consumption, packet `acc73c2b…80cce36cb5a` (30 artifacts, 34 private checks);
- alias candidate, packet `9bcdf20c…2a31ebd` (60 artifacts, 51 checks);
- alias consumption, packet `88b2d0f6…79a97e30` (25 artifacts, 12 checks).

The handoff records no native execution, grant, or target or Git write. Package 1
names one missing step-1 input: coordinator88's host corroboration receipts for the
two rejecting records. The coordinator now supplies them in its private session files:

| Receipt | Bytes | SHA-256 | Binds |
|---|---|---|---|
| `p08-ship-direct-f06-t011-host-corroboration.json` | 2,873 | `29b575c0fdb19fb7e40dc26866d279604b87927dee5450f0456fd7c041d963a0` | T011, canonical `efb1bce5`, attempt `p08-ship-direct-f06-t011-01`, F06-D01 |
| `p08-ship-direct-f06-t027-host-corroboration.json` | 2,885 | `7f96932d80eeb9f8f5999d5823d5f0e7113e1b79426c295c7dabfac692f5e995` | T027, canonical `4a9e1255`, attempt `p08-ship-direct-f06-t027-01`, F06-D01 and F06-D02 |

Before writing them, the coordinator re-verified both decisions: raw, canonical and
context digests with the integrated P05 `content_digest`, P05 `validate_review`, status
`fail`, builder `c2d5b8df` and reviewer `9dbf0a9b`. The sealed manifest's 24 bindings
all match. The stage dispositions record SPEC failed, and QUALITY and local compliance
not performed. The host session status showed the reviewer, native writer and controller
as three distinct sessions on distinct branches. Each receipt passes P05's corroboration
shape and the same binding checks as P05 `verify` and f2's grant template.

One discrepancy is disclosed in both receipts. SAME9db's return message gave the
manifest hash with `c` for `a` at its ninth character. The file (`744167d6a1bd…e941a`)
was written once, before that message, and the card's 8-character binding matches it.
The receipts are caller-trusted host observation, not authentication, publication
authority or F06 acceptance.

### Preparation review and step-1 re-release (2026-09-24)

SAME9db's bounded preparation review (`review-report.md`, SHA-256
`2515b47f…62130264`; manifest `bd52dce5…b8c203e28c`) returns:

| Package | SPEC | QUALITY | Findings |
|---|---|---|---|
| 1, direct consumption (`acc73c2b`) | PASS | PASS | 0/0/0 |
| 2, alias candidate (`9bcdf20c`) | FAIL | not started | P2 R01, R02 |
| 3, alias consumption (`88b2d0f6`) | FAIL | not started | P2 R03 |

- **F06-PREP-R01.** `issue_alias_grant.TEMPLATE.py:22` calls `native` before its
  definition at `:39`, so the module cannot initialize.
- **F06-PREP-R02.** The same template writes `all17-targets-before.json` as full
  `{files, directories}` inventories (`:128`, `:235`), while
  `ship-alias-f06-tests-01.py:85-88` reads flat filename-to-hash rows. The tests caller
  would stop before the suite even after R01.
- **F06-PREP-R03.** `issue_alias_consumption_grant.TEMPLATE.py:34` leaves
  `CANDIDATE_SHA` as `None`, and `:215` serializes that null. The consumer's digest check
  at `:134` must then reject it.

All three findings come from static definition, use and data-flow analysis; nothing was
executed. f2 stopped the chain under the preparation-finding rule and accepts all three.
No step, grant or repair followed.

The coordinator releases the following, keeping `6e056855`'s conditions otherwise
unchanged:

1. **Step 1 proceeds now.** Its package passed, and packages 2 and 3 do not feed it. The
   grant binds `6e056855` and this commit. It also binds:
   - the exact reviewed package 1 (`acc73c2b…80cce36cb5a`);
   - both decision records by raw, canonical and context digest;
   - the host receipts `29b575c0…` and `7f96932d…`;
   - writer SAMEc2, target ship-direct and the single invocation
     `ship-direct-f06-consume-01`.

   The reviewed pin-state-preserving post-exit contract, quiescence and revocation
   apply, and the frozen handoff goes to the coordinator in parallel.
2. **Packages 2 and 3 get one bounded repair,** in f2's controller preparation code
   only:
   - R01: define every helper before module-level use, so the template initializes.
   - R02: make the grant-time `all17-targets-before.json` match the flat rows that the
     reviewed tests caller reads, or record why the caller must change instead. Leave
     executed direct-caller lineage and the other reviewed callers unchanged.
   - R03: bind `CANDIDATE_SHA` from the actual alias candidate packet at grant time, and
     refuse to issue while it is unbound.

   Add a labelled private check for each finding that fails on the old bytes. These are
   not native runs. Update the derivation proofs, diffs, logic-change records and
   packets. Refreeze both packages under new identities, and send them to SAME9db and
   the coordinator in parallel. SAME9db rechecks R01–R03, completes each package's SPEC
   and runs QUALITY only if eligible. Package 1 stays frozen.
3. **The chain order stays.**
   - Step 2 needs step 1 completed as an execution and package 2's PASS.
   - Step 3 needs an actually rejecting alias record and package 3's PASS.
   - The stop rules apply to every step, and a stop again returns only failure
     identities.

No other scope, retry, native run, new actor or target follows. F06 stays OPEN.

**Refrozen packages 2 and 3 and coordinator intake (2026-09-24).** f2 refroze both packages
under new identities, and sent them to SAME9db and the coordinator in parallel:

- `f06-alias-preparation-02`, packet `d62ff2ab…a7b422bb40d71` (77 artifacts, 57 private
  checks), supersedes `9bcdf20c`. R01: the parameter load now uses only names defined
  earlier at module level. R02: `all17-targets-before.json` now holds the flat collector
  rows that the tests caller reads, and the native step manifest moves to
  `all17-native-boundaries-before.json`.
- `f06-alias-consumption-preparation-02`, packet `2bba0f03…e215065c` (32 artifacts, 18
  checks), supersedes `88b2d0f6`. R03: the candidate hash comes from a required
  64-hex-character grant parameter, and the issuer refuses without it.

The coordinator's data-only intake confirms both packet hashes. Each `repair-delta.json`
shows that only its grant template differs from the frozen -01 bytes beyond the area
rename. Package 1 and the -01 areas are unchanged. SAME9db's recheck follows.

### Step-1 stop and recovery release (2026-09-24)

Step 1 ran once as `ship-direct-f06-consume-01` (grant `6c37c2de`) and stopped
fail-closed, with child and outer exit 1. The stop is the caller's assertion at line 300
that producer stderr is empty. T011's `li-review-log` exited 0 and recorded `fail`, but
its stderr held exactly `bash.exe: warning: could not find /tmp, please create!`. The
same host warning appears in launcher operations 04, 06 and 07, and not in pin03.

f2 froze the failure bundle `f06-direct-consumption-intake-01/failure-packet.json`
(SHA-256 `be01177b…7d3765d1`, 73 snapshots). The ownership file (`f7216cda…`) records the
lease as revoked, and SAMEc2 is idle. Ship-direct is half-published and preserved as it
stands:

- the five publications match the bound bytes exactly;
- `reviews.jsonl` keeps its prefix plus exactly one new envelope, T011 `fail`;
- T027 was never produced;
- the ledger and cycle audit are unchanged;
- the P03 snapshot `snapshot-f82285770eb949d2972780ef75f0a885` is complete but unbound.

The chronological post-exit could not run. The other 15 targets are exact to the step-1
grant-time manifest.

P03's `restore_snapshot` refuses an unbound snapshot whose files have changed, because it
cannot tell the failed invocation's writes from another writer's. The coordinator
releases one bounded preparation, for SAME9db's review before any grant:

1. **Diagnose the warning, data only.** Compare the environment and process records of
   operations 04, 06 and 07 with pin03, and name the cause.
   - If the cause is in the caller or launcher environment, for example a `TEMP` that
     does not resolve for MSYS `/tmp`, correct that environment.
   - Only if the host warning cannot be controlled, tolerate exactly that one line in
     producer stderr. Record it as a host observation, and keep every other stderr byte
     fatal.
   - Apply the same decision wherever packages 2 and 3 make the same assertion, and
     refreeze them.
2. **Build one combined invocation, `ship-direct-f06-consume-02`, derived from the
   reviewed step-1 caller.** It:
   - verifies that ship-direct is exactly the failure bundle's recorded partial state;
   - binds that verified partial result to the unbound P03 snapshot, then restores
     through P03;
   - verifies that ship-direct is exact to the step-1 grant-time manifest;
   - runs the reviewed consumption, changed only by item 1;
   - records checkpoint accounting before and after, and writes the immutable packet.

   The failure bundle stays as history. Deliver the enumerated derivation, diffs,
   logic-change records, labelled private checks and the grant template as before.
3. **Freeze the new package, and the refrozen packages 2 and 3 if item 1 changes them.**
   Send them to SAME9db and the coordinator in parallel. SAME9db reviews them together,
   runs SPEC and then QUALITY if eligible, and holds the -02 recheck of packages 2 and 3
   if they are refrozen.
4. **On PASS, the combined invocation proceeds under this release.** It uses its own
   grant, which binds `6e056855`, `8754515a` and this commit, the reviewed package, the
   decision records and the host receipts. Writer SAMEc2, target ship-direct, a single
   invocation, the reviewed post-exit, quiescence and revocation all apply. The chain
   order and stop rules are unchanged.

No retry of `consume-01`, no unreviewed restore, bind or execution, and no other target
follows. F06 stays OPEN.

**Coordinator diagnosis of the warning (2026-09-24), superseding item 1.** Git for
Windows mounts `/tmp` as `usertemp`. MSYS evaluates that mount once and keeps it in
per-user shared memory while any MSYS process is alive. `mount` showed `/tmp` bound to
`%LOCALAPPDATA%\Temp\a13t1\r10\temp`, the synthetic temp directory of entry 10 of the
coordinator's own targeted A13 trial run. That run's `review-evidence` entry had started
an MSYS process while the shared table was fresh. When the entry's directory was later
removed, every Git Bash process on the host, in every session, printed the warning.
Pin03 ran before this. The coordinator's run is therefore the most likely cause of the
step-1 stop, not the fixture or its launcher.

The coordinator recreated the empty directory in its own synthetic root. A probe then
showed no warning, empty stderr and a working `mktemp`. The directory now backs the
host-wide `/tmp` and must stay until no MSYS process uses the old mount.

Consequences for the recovery release:

- No stderr tolerance is needed. The producer stderr assertion stays strict.
- Before each grant, the controller runs a host probe (`bash -c true` in the
  invocation's environment) and refuses to grant if stderr is not empty. This is a
  controller runbook step outside the reviewed packages.
- Packages 2 and 3 are unaffected. SAME9db proceeds with the -02 recheck as frozen.
- `ship-direct-f06-consume-02` remains necessary to verify, bind and restore the
  preserved partial state and to rerun the reviewed consumption. Item 2 stands without
  item 1's change.

**Recovery package and coordinator intake (2026-09-24).** f2 froze
`f06-direct-consumption-preparation-02`: packet `ecaad598…05f8d4c81`, 26 bound artifacts
(32 files on disk) and 19 private checks. It went to SAME9db and the coordinator in
parallel.

- The caller, `ship-direct-f06-consume-02.py` (`10c4eb83`), derives from the reviewed
  package-1 caller (`b3a1e0cc`) by 4 name substitutions and 5 listed hunks.
- The recovery block requires ship-direct to equal the post-failure observation exactly.
  It binds the six owned partial states to the unbound snapshot, restores through P03,
  and then requires the step-1 grant-time manifest, the five absent destinations and the
  restored review audit (`79004b53`).
- It runs the reviewed consumption with a fresh P03 store and the strict producer-stderr
  assertion.
- The private checks exercise the frozen `li-snapshot` bind and restore only in a
  sandbox holding the actual owned bytes, including refusal of a wrong partial state and
  of a second bind.

The coordinator's data-only intake confirms the packet, caller, request, recovery block,
grant template and proof hashes, and that package 1, its caller and the failure bundle
are unchanged. SAME9db reviews this package and the -02 packages 2 and 3.

**Recovery package review and repair release (2026-09-24).** SAME9db's review of
`f06-direct-consumption-preparation-02` (report `8d627642…b97c06d7`) returns SPEC FAIL
with P1 0, P2 2, P3 0; QUALITY is not started.

- **F06-PREP-R04 (P2).** The issuer (`:164-168`) derives the historical ownership and
  audit state from the live snapshot manifest in the external P03 store. The caller
  (`:185-191`) binds and restores without comparing that manifest with the failure
  bundle's copy, `failure-snapshots/063.bin` (1,305 bytes, `fcf6b418…c407f7e0f4`). Exact
  target equality does not cover the P03 store. A mismatch against the original baseline
  is checked only after the restore. The actual metadata matches history today; no
  corruption was observed.
- **F06-PREP-R05 (P2).** `consumption-brief.md:56` still names the existing, immutable
  `consume-quiescence-01.json`, while the template reserves `-02`. Line 22 still names
  the old issuer filename.

Both are released to f2 as one bounded repair of the recovery package:

- R04: before any bind or restore permission, the issuer and the caller each require
  the live snapshot manifest and its owned set to equal `063.bin` byte for byte, and
  refuse otherwise.
- R05: correct the brief so it names only fresh -02 names and the current issuer file.
- Add a labelled private check per finding that fails on the old bytes. Refreeze under a
  new identity, and send it to SAME9db and the coordinator in parallel for the recheck,
  SPEC, and QUALITY if eligible.

No grant, bind or restore follows before PASS. Packages 2 and 3 keep their PASS.

**Refrozen recovery package and coordinator intake (2026-09-24).** f2 froze
`f06-direct-consumption-preparation-03`: packet `f3acab08…3983ddca29`, 28 bound artifacts,
29 private checks. It derives from the preserved -02 bytes by one identity substitution
and 15 listed hunks.

- R04: the issuer, and the caller both before `bind_result` and again before
  `restore_snapshot`, require the live P03 manifest to equal `063.bin` byte for byte.
  They also require the owned set to be `reviews.jsonl` plus the five publications. The
  caller checks that the bind receipt's snapshot digest equals the pin, and the grant
  binds `b8803e29`, `2b5d02a3` and the pin.
- R05: the brief now names the current issuer and `consume-quiescence-02`.
- Three labelled discriminators fail on the -02 bytes.

The coordinator's data-only intake confirms the packet and caller (`0bf4c609`) hashes,
that the -02 package and `063.bin` are unchanged, that no release-02 exists, and that
the live ownership has no targets. SAME9db's recheck follows.

**Recovery recheck and go-ahead (2026-09-24).** SAME9db resolves R04 and R05. The package
`f06-direct-consumption-preparation-03` passes preparation SPEC and then QUALITY with P1,
P2 and P3 at 0 (report `ed9cce1a…ce75d97d6`). The combined invocation
`ship-direct-f06-consume-02` proceeds under `6e056855`, `8754515a`, `dcdaa44e`,
`b8803e29` and `2b5d02a3`, with its own grant, the pre-grant probe, the reviewed
post-exit, quiescence and revocation. The coordinator's host check at 07:34 showed the
probe clean.

**Step 1 completed (2026-09-24).** `ship-direct-f06-consume-02` ran once under grant
`release-02/grant.json` (`25973f81…`). The pre-grant probe had empty stderr and exit 0,
and the launcher's child and outer exits were 0.

- **Recovery:** the old snapshot is now bound, with its snapshot digest equal to the pin
  `fcf6b418…`. P03 restored all six owned paths, and ship-direct was then exact to the
  step-1 grant-time manifest.
- **Consumption:** the packet `ship-direct-f06-consumption-02/packet.json` is 68,584
  bytes, SHA-256 `8e3fad7a…4510c89d5`, with 158 snapshots.
  - The producer envelopes are `fail` and `fail`.
  - For T011 and T027 the latest reader exits 3 with status `fail`, and ship readiness
    refuses at the review before QA.
  - No older PASS was selected, and the ledger and cycle audit are unchanged. Only the
    five publications and `reviews.jsonl` changed.
- **Close-out:** the chronological post-exit preserves every pin state, and it admits six
  later MAIN checkpoint additions of the reviewed class. SAMEc2 is idle, and the
  ownership is revoked again (`679bae75…`, no targets).
- **Disclosures:** the actor's quiescence record names the transport shell's PID, with
  the correction in adjacent fields. f2's first intake refused because of f2's own
  comparison defect, and both runs are preserved.

The coordinator's data-only intake confirms the packet, the grant and the revoked
ownership. Step 2, the alias candidate, proceeds under the chain.

**Step-2 stop and issuer repair release (2026-09-24).** The reviewed alias issuer
(`issue_alias_grant.TEMPLATE.py`, package 2 `d62ff2ab`) ran once as
`f06-alias-release-01/issue_grant.py`. It exited 1 at line 175 with "Positive current
input changed", before issuing anything. The pre-grant probe had been clean.

- **F06-PREP-R06 (latent).** The issuer compares live inputs outside ship-direct with the
  historical positive snapshots in `delivery-pair-final-01`. The only differing row is
  `evidence/controller/ownership.json`: the snapshot's 684 bytes (`4312f7a7…`) against
  the current step-1 revocation (`679bae75…`). Because ownership changes with every lease
  and revocation, the check cannot pass after any later lease.

Nothing was issued: there is no grant, no dispatch, and no change to ownership,
ship-alias or any target. Step 1 stays completed. The coordinator releases one bounded
repair:

- In the alias issuer, exclude the controller's lease state from the historical positive
  comparison. Instead, require it to equal the verified current revocation: no targets,
  and the hash of the latest recorded revocation. Keep every other positive row exact.
- Check the package 3 issuer, and any other derived issuer, for the same pattern, and
  correct it the same way if present.
- Add a labelled private check that fails on the old bytes against the actual current
  inputs, without issuing a grant.
- Refreeze package 2, and package 3 if it changes, under new identities, and send them to
  SAME9db and the coordinator in parallel.

On SAME9db's PASS, step 2 proceeds under the chain with a fresh pre-grant probe. No other
change, retry or grant follows. F06 stays OPEN.

**Issuer repair and coordinator intake (2026-09-24).** f2 froze the delta package
`f06-alias-preparation-03` (packet `67c40443…1cfd32fd`), which replaces only the alias
issuer (`84b43875…`, one file, six listed hunks).

- The live `ownership.json` leaves the historical positive comparison; its snapshot bytes
  are still verified. It must instead equal the latest revocation: `592261f5…`, whose
  revoked ownership `679bae75…` is the live file, with no targets.
- The issuer also verifies the repair packet and its own identity, and the grant binds
  the repair, `e7c11c31` and the revocation.
- The fixed release directory keeps the stopped attempt. The repaired issuer uses fresh
  `-02` file names and overwrites nothing.
- Two discriminators fail on the -02 bytes. Package 3 does not have the pattern and is
  unchanged.

The coordinator's data-only intake confirms the packet and issuer hashes, that packages
2 (-02) and 3 (-02) are unchanged, that the ownership is still revoked, and that no
grant exists. SAME9db's recheck follows.

**R06 recheck (2026-09-24).** SAME9db resolves R06 (report `c5b7c38d…c74456`). The
issuer-only preparation passes SPEC and then QUALITY with P1, P2 and P3 at 0.

- 16 memory-only guard cases pass. The old comparison rejects the actual current
  ownership, and the repaired one accepts it. Wrong revocations, active or remaining
  targets, live-row drift and a wrong issuer are all refused.
- Across the 269 historical snapshots, ownership is the only applicable live difference.
  Three other issuers have no equivalent pattern.

Step 2 proceeds under the chain with a fresh probe. The coordinator's host check at 08:14
showed the probe clean.

**Latent pin defect and decision (2026-09-24).** Step 2 was dispatched under grant
`45cb7508…`, with the lease on SAMEc2 and ship-alias. f2 then found, from the data alone,
that the reviewed `ship-alias-f06-pin-01.py:419` snapshots
`ship-alias-f06-quiescence-01.json`, the candidate run's quiescence.

- In the direct lineage, pin-03 read the quiescence of an earlier candidate run, so the
  file existed. The alias derivation collapsed pin and candidate into one grant, so no
  quiescence exists before this pin. The grant template even requires the file to be
  absent.
- The pin would therefore stop fail-closed at line 419 with `FileNotFoundError`, after
  writing only early evidence. The pin writes no targets.
- f2 sent SAMEc2 a hold before the pin, and did not revoke mid-run.

The coordinator decides f2's option (a), the direct lineage's own pattern:

1. Let the four candidate invocations (entry, inject, tests, prepare) finish, or stop at
   the hold. Complete the chronological post-exit, `ship-alias-f06-quiescence-01.json`
   and revocation for this grant, and record which invocations ran. If the pin ran and
   failed, report its failure identities.
2. Then prepare one pin-only retry, `ship-alias-f06-pin-02`, under its own grant. Derive
   it from the reviewed pin-01 by identity substitutions only, because
   `quiescence-01` will then exist and line 419 is correct as reviewed. List any other
   change as a hunk with its reason. It goes to SAME9db for preparation review before its
   grant, with the pre-grant probe, the reviewed post-exit, quiescence and revocation.
3. The frozen alias candidate goes to SAME9db for candidate SPEC only after the
   successful pin, as in the direct lineage.

No other change or retry follows. F06 stays OPEN.

**Candidate tranche, host checkpoint rotation and decision (2026-09-24).** Under grant
`45cb7508`:

- entry-01 and inject-01 exited 0, tests-01 exited 1 (13 cases, 9 pass, 4 fail) and
  prepare-01 exited 3, as for the direct negative candidate.
- pin-01 was held and did not run; it has no evidence and no candidate-01. SAMEc2 wrote
  its quiescence-01 (`4a0e2089…`) and is idle.
- The reviewed chronological post-exit refused before writing any output: "Grant-time
  baseline: original file deleted".

f2's data-only recompute shows why. The host deleted SAMEc2's oldest MAIN checkpoint
refs, ordinals 1–8, and added refs 96–108 with 13 loose objects. No existing file
changed, and there is no packed-refs file. This looks like host rotation that keeps
about 100 checkpoint refs. The 15 other siblings are exact, and ship-alias changed only
inside the declared candidate scope. f2 froze the failure packet (`0ba4e3db…`) and
revoked the lease again (`119b203d…`, no targets).

The reviewed contract admits only additions in that namespace, so every later observer
check, pin-02 included, would refuse in the same way. The coordinator decides:

1. **One bounded observer and post-exit repair.** Admit a *host checkpoint rotation*
   class, which is deletion of the oldest ordinal refs, under all of these conditions:
   - only 20-digit ordinal ref files in SAMEc2's exact checkpoint namespace;
   - only as a contiguous prefix of the lowest ordinals present at the preceding
     observation;
   - only refs that the baseline or an earlier admitted addition accounts for;
   - no other deletion, change of any existing file, packed-refs file or change outside
     that namespace.

   Record each rotation as a host observation, never as writer evidence. Keep every
   other preservation rule. Apply the same class in `observer-alias` and
   `postexit-alias` and in pin-02's observer. Add labelled discriminators: an
   out-of-order or non-prefix deletion, a deletion outside the namespace and a changed
   existing file must each refuse.
2. After SAME9db's preparation PASS, rerun the chronological post-exit for grant
   `45cb7508` under the repaired contract. It is a controller observation with no target
   write. Then continue the pin-02 preparation on the repaired observer.
3. pin-02 then follows as already decided: its own grant, a fresh probe, and the
   reviewed post-exit, quiescence and revocation.

No rebaselining, retry of the candidate or other change follows. F06 stays OPEN.

**Rotation repair and coordinator intake (2026-09-24).** f2 froze
`f06-alias-observer-repair-01` (packet `a6bb027c…cd0b41606`), derived from the unchanged
-02 bytes.

- It contains `observer-alias.py` and `postexit-alias.py` for the post-exit rerun, and
  `observer-alias-pin.py` and `postexit-alias-pin.py` for pin-02.
- The rotation class admits only the stated conditions, plus the emptied ordinal
  directories. Rotations are recorded as host observations.
- Disclosed: because the rerun comes after revocation, the step loader accepts grant
  `45cb7508`'s issued lease or its exact revocation. The pin observer also accepts the
  future pin lease, or its revocation, only if that grant binds candidate `45cb7508` and
  is pin-only with no writes.
- 14 private checks on the actual state admit rotation 1–8 and keep the 15 siblings
  exact. Every required discriminator refuses, and the -02 observer refuses the actual
  state.

The coordinator's data-only intake confirms the packet and the four file hashes, the
unchanged -02 base and the revoked ownership. SAME9db's recheck follows.

**Rotation repair review and follow-up authorization (2026-09-24).** SAME9db's review
(`review.md`, SHA-256 `94bb1993…43224f`) returns SPEC FAIL; QUALITY and compliance are not
started.

- **F06-ROTATION-R01 (P1).** An admitted oldest-prefix rotation through ref 59, an
  earlier-admitted addition, passes every preservation comparison. Both full observers
  then raise `KeyError` when they reread that absent ref.
- **F06-ROTATION-R02 (P2).** The actual surviving counts, 558 and 570, are reported as
  566 and 578. Both post-exit variants also claim that every pin file and directory was
  preserved after an admitted deletion.

The coordinator authorizes f2 to repair both findings within the same bounded scope as
`8f6be850`:

- No observer path may read an admitted-deleted ref.
- Counts and preservation statements must report the actual surviving set. They must
  name admitted rotation deletions separately, as host observations, and never call them
  preserved.
- Add labelled discriminators for both findings.
- Refreeze, and send the result to SAME9db and the coordinator in parallel.

The PASS path then continues as decided. No candidate retry or rebaselining follows.

**A13.b reviewer-harness stop and decision (2026-09-24).** SAME9db stopped its own A13.b
suite run, `p08-a13b-suite-01`, after 123 completed child calls. Its
`GIT_CEILING_DIRECTORIES` named `…\b01\suite`, while the frozen source directory is the
sibling `…\b01\current`, so Git discovery from the source was not bounded. This is a
reviewer error, not a product finding, and the partial run is invalid as evidence.

The coordinator checked read-only that no `.git` exists in the source directory or in
any ancestor up to `C:\`. Discovery from the source therefore could not reach a foreign
repository; Git commands there could at most fail as "not a git repository". The
coordinator decides as SAME9db proposes:

- Repair only the reviewer harness, so that the ceiling covers every source and fixture
  working directory, with a recorded preflight proof.
- Keep the stopped run as invalid history, and restart the A13.b review in a fresh
  synthetic slot.
- No actual-home investigation, cleanup or other change is needed.

**Rotation repair -02 and coordinator intake (2026-09-24).** f2 froze
`f06-alias-observer-repair-02` (packet `6e9db575…b458e2b2`), derived from the frozen -01
bytes by listed hunks only.

- R01: no path reads an admitted-deleted ref. Such a ref is rebuilt only from its bound
  admission hash matched to exactly one present object. Unpaired objects are admitted
  only as objects of host-rotated refs, under count and validity conditions.
- R02: counts are the actual survivors (558 and 570), with the rotated files listed
  separately. Post-exit survival is literal, with a separate field saying the rules are
  satisfied with admitted host rotation.
- 27 private checks pass, and four discriminators fail on the -01 bytes.

The coordinator's data-only intake confirms the packet and the four file hashes, the
preserved -01 repair and the revoked ownership. SAME9db's recheck is under way.

f2 also reports that pin-02 needs a hunk to skip rotated known refs when snapshotting,
and that package 3's `observer-consume` and `postexit-consume` share the old logic. To
avoid a stop at step 3, the coordinator authorizes f2 to prepare the same rotation class
for package 3 now, within the scope of `8f6be850` and `8932f2c9`. That preparation goes
to SAME9db after the -02 PASS. pin-02 carries its hunk as already allowed.

**Packages 2 and 3 recheck (2026-09-24).** SAME9db resolves R01–R03, and both -02
packages pass bounded preparation SPEC and then QUALITY with P1, P2 and P3 at 0 (report
`bfe4a511…a3ccabd`, manifest `ee6cccfb…`). The early native lookup is
gone, the flat collector rows stay separate from the full native inventories, and the
validated candidate digest reaches the handoff. The strict stderr assertion stays, and
the coordinator's pre-grant probe remains required. Step 2 still waits for step 1's
recovery, and step 3 for an actually rejecting alias record with its host receipts.

### A13 contract release

The coordinator contract `.claude/plans/universal-implementation/a13-contract.md`
(Git-LF SHA-256 `92d677c8eac60375220f18223bb08b010b32c6eeae6819ae1670be833897b903`,
35,096 bytes, 727 lines) is the exact artifact this card requires before the common
audit/event and learning/promotion changes. An independent read-only review raised 24
findings. A focused recheck confirmed 23 resolved (the 24th was this byte binding) and
raised five new P2 defects, which were fixed with the reviewer's recommended remedies
without a further recheck. The pre-existing hook `mkdir` defect that the recheck exposed
is routed separately and is not released here.

**Released now:** A13.1.a, A13.2, the A13.3 corrections and A13.4.a, within the
contract's write scope. **Still gated:** A13.1.b and A13.4.b, until P10 is accepted
and integrated and the coordinator releases them. They read P10 surfaces only. The
parent items A13.1 and A13.4 and the plan's A13 acceptance stay open until the `.b`
leaves pass.

This lifts the line above that required coordinating P10's fields before changing the
common audit/event interface, but only for the released leaves. P10's native installers
write no audit records, and its runtime replaces one producer (the Claude-home
migration event). The coordinator reconciles that producer's catalog entry at fan-in.
The contract lists the other coordinator fan-in items: installed-resource inventories,
the `hooks.json` doctor comment, catalog regeneration and scaffolding pinning.

Execution:

- f2 owns the work and implements it in a **new worktree and branch stacked on the
  current P08 branch head** `229656cf602f7f651801dfd0b14862415a930158`, recording the
  exact base. The existing worktree and the F06 fixture under
  `.claude/runtime/p08-semantic-01` are not touched: no A13 build, test or Git
  command runs there.
- f2 may use one own-session implementer helper under the operator model
  directive. Only f2 or that helper writes the A13 branch. F06 native work,
  grants and actors stay separate and keep priority.
- Evidence goes to a new report, `.claude/plans/universal-implementation/reports/P08-A13.md`,
  so the later F06 appendix to `reports/P08.md` does not conflict.
- Record a RED run before implementation for the reader, allocation and promotion
  cases, then the green run. Report exact commands, real exits, hashes and every
  deviation from the contract, with its reason.
- Freeze one handoff (product commit, report-only child, hashes) to SAME9db for
  A13 SPEC, then eligible QUALITY, queued after its F06 work. Send it to
  coordinator88 at the same time for a parallel intake that may halt the review.

**Frozen handoff and coordinator intake (2026-09-24).** f2 froze product head `c7fbfd6f`
with report-only children ending at `e687d848`. The children change only
`reports/P08-A13.md` (711 lines, SHA-256 `2dd5115e…0a8fb3`). f2 sent the handoff to
SAME9db and the coordinator in parallel. The coordinator's data-only intake passes:

- The contract at `5c13c68c` is exact (36,144 bytes, SHA-256 `7008b114…40d072`).
- The fifteen-commit chain from `229656cf` has no merges and is in the stated order.
- `bin/li-review-read` gains exactly the released line.
- `skills/CATALOG.md` and `.claude/runtime` are unchanged.
- The report cites `5c13c68c` and records reconciliation 4.

The handoff's 49 changed paths include the report, so 48 are product paths; their scope
passed pre-intake on the same product head. The coordinator does not halt the review.

**SAME9db review and repair release (2026-09-24).** SAME9db's frozen review returns
selected SPEC FAIL with P1 0, P2 2, P3 0; QUALITY and compliance are not started. Its
private `review-report.md` has SHA-256 `ff66a474…e6ed984`, and its manifest
(`81e13059…4aa17d213`) binds 68 artifacts. The coordinator's intake verifies the three
return files and the cited contract lines and code:

- **A13-R01 (P2).** `bin/li-events.py:204-214` applies `--kind` before the `--since`
  undated check. An undated record of another kind is therefore dropped silently: exit 3,
  or exit 0 beside a matching dated record. Contract lines 193–194 and 211 instead
  require the `undated` diagnostic and exit 4, regardless of filters.
- **A13-R02 (P2).** `bin/li-events.py:109-113` dereferences `rules[-1]` before checking
  its type. A syntactically valid catalog with `rules: [null]` therefore ends in an
  `AttributeError` and exit 1, instead of the catalog-failure exit 2 of contract line 210.

Both findings are released to f2 as one bounded repair on the same A13 branch:

- Change only `bin/li-events.py`. Raise the undated diagnostic under `--since` before
  kind selection, and keep ordinary filtering of valid dated records. Validate every
  catalog container and element type before dereferencing it, so that any structurally
  invalid catalog exits 2 through `ReaderError`. Do not mask other exceptions.
- Add discriminating regressions to the existing A13 reader tests, and show them RED on
  `c7fbfd6f` first:
  - combined `--kind`/`--since` cases for summary and records, including a matching
    dated record beside an undated record of another kind;
  - a syntactically valid, structurally invalid catalog (at least `rules: [null]`) for
    both summary and records, beside the existing malformed-JSON case.
- Rerun the A13 module and the shape and consumer checks that load the reader.
- Add a report-only appendix to `reports/P08-A13.md` with exact commands and exits. The
  appendix also corrects the handoff wording: the 49 changed paths include the report.
- Change no other file. Make no catalog change on the branch, because reconciliation 4
  stays at integration. Leave F-HOOK-MKDIR and the fan-in items alone.

Freeze a new product commit and report-only child, and send the handoff to SAME9db and
the coordinator in parallel. SAME9db rechecks R01 and R02 and completes the remaining
selected SPEC. It runs QUALITY and compliance only if SPEC makes them eligible. Any new
actor or subagent uses `claude-opus-5.5`, reasoning `max`, context `long_context`.

**Frozen repair and coordinator intake (2026-09-24).** f2 froze the repair on top of the
reviewed `e687d848`, with no amend or rebase:

- `583f765a` changes only `bin/li-events.py` (+73/−16).
- `5f58da04`, the new product head, changes only
  `tests/integration/observation-learning.py` (+89).
- `af24721a` is the report-only child. Its report is 812 lines, SHA-256
  `f345230d…a07f15d5`, and appendix A cites `5faeb072`.

The coordinator's data-only intake passes: each commit has its sole expected parent and
only its path, the branch is clean, and the catalog is unchanged. The earlier report
text has four in-place status and pointer edits. They name SAME9db's FAIL and appendix
A, and attribute the earlier sections explicitly to `c7fbfd6f`. No earlier evidence
value changed, and the prior report remains at `e687d848`. The builder reports the new
regressions RED on `c7fbfd6f`'s reader (14 failures), then 42/42 GREEN, and exits 0, 4,
4, 4, 4, 2 and 2 for the reviewer's seven probe inputs. These are builder results, not
acceptance. The coordinator does not halt SAME9db's recheck.

**Recheck result and A13-V01 decision (2026-09-24).** SAME9db's recheck resolves R01 and
R02: seven independent exact-byte probes give the required exits 0, 4, 4, 4, 4, 2 and 2
with no traceback. The private report `p08-a13-review-02/review-report.md` has SHA-256
`9a5740e2…2800ab1aa`, and its manifest binds 79 artifacts. The remaining selected SPEC is
blocked by A13-V01, a verification blocker: in the reviewer's fail-fast run of the
unchanged module, the third case's fixture write raises `FileNotFoundError` before the
hook runs.

The coordinator's data-only diagnosis:

- The failing fixture path is 269 characters, and its parent directory 247, under the
  reviewer's isolation temp root.
- The host has `LongPathsEnabled = 0`, so the open exceeds Windows' 260-character path
  limit. The fixture helper creates the parent first; only the file open fails.
- The same unchanged module passed 40/40 from the coordinator's short synthetic root
  (`a13t1`) on the pre-repair head.

f2's separate data-only diagnosis reaches the same result. It recommends the same rerun
and advises against changing the global setting.

A13-V01 is therefore an environment limit, not a product defect. The coordinator
authorizes SAME9db to rerun the unchanged module and the remaining selected SPEC from a
short disposable root, such as a new directory directly under `%LOCALAPPDATA%\Temp`,
with every fixture path well below 260 characters. The same allowlisted synthetic
environment and source-export verification apply. No global long-path setting, product
or test change, or other environment change is authorized. If the failure persists at a
short root, it returns as a finding for intake.

**A13-V02 decision (2026-09-24).** From the short root, A13-V01 is resolved: the freeze
case and the R01/R02 regressions pass. The private report is
`p08-a13-review-03/review-report.md`, SHA-256 `656a9769…e971bd2ef0`. The fail-fast run
then stops at a second environment limit. `observation-learning.py:525` runs the literal
`python3`, and the reviewer's allowlisted PATH provides only `python.exe`, so the Bash
child reports `python3: command not found`. Of 42 cases, 11 started and 10 passed.

This is the repository's own declared test environment, not a product defect.
`.github/workflows/ci.yml:41-50` exposes the selected Python as `python3` on every
platform with a one-line wrapper, and the coordinator's launcher does the same. The
coordinator authorizes SAME9db to create, inside its synthetic scope, a private
directory holding an executable `python3` wrapper that runs the same allowlisted Python
3.11. The directory goes first on the child PATH. SAME9db then continues the unchanged
module and the remaining selected SPEC. No install, global setting, other PATH change,
product or test change is authorized. Any further missing executable returns as a
limitation.

**A13-R03 and repair release (2026-09-24).** With the `python3` binding, V01 and V02 are
resolved. The unchanged 42-case module passes with no failure, error or skip, and 16
retained checks exit 0, plus the expected catalog exit 1. The private report
`p08-a13-review-04/review-report.md` (SHA-256 `09592ff6…35a7c9a5`) returns selected SPEC
FAIL on one P2 finding; QUALITY and compliance are not started.

- **A13-R03 (P2).** `lib/scale-estimator.sh:218` selects the first *readable* history
  file, although contract lines 142–144 and 152–153 require the first *existing* one. If
  the primary file exists but cannot be read, the function silently calibrates from the
  legacy file. In the reviewer's labelled predicate projection (not an OS permission
  test), a primary of 1000 and a legacy of 9000 give 9000, calibrated. The function's own
  comment already requires the uncalibrated default for an unreadable log.

Released to f2 as one bounded repair on the same A13 branch:

- Change only `lib/scale-estimator.sh`. Select the first existing file. If that file
  cannot be read, report it on stderr and return the uncalibrated default. Never
  substitute a later file. Keep the notice for later files that exist but were not read.
- Add a discriminating regression to the existing A13 tests, RED on the current head
  first. Where the platform cannot make a file unreadable, use a disclosed test-only
  projection of the readability check. Keep all existing cases.
- Rerun the A13 module and the affected checks, and add a report-only appendix.
- Freeze the product and report for SAME9db, which rechecks R03 and completes the
  selected SPEC, then QUALITY and compliance if eligible. Send the handoff to the
  coordinator in parallel.

No other file, catalog change or fan-in item is released.

**Frozen R03 repair and coordinator intake (2026-09-24).** f2 froze `3696a18c`, changing
only `lib/scale-estimator.sh` (+19/−8), and the new product head `1c5bb1fb`, changing
only `observation-learning.py` (+53). The report-only child `400c0316` adds appendix B,
67,404 bytes, SHA-256 `9e955b99…ac6a319e`. The data-only intake passes: sole parents, one
path per commit, a clean branch and an unchanged catalog. The earlier report text has
three in-place status and pointer edits, and no evidence value changed. The builder
reports RED on `af24721a` under a disclosed `[ -r ]` projection, then 43/43 GREEN.
SAME9db's recheck follows.

**A13.a acceptance (2026-09-24).** SAME9db's recheck resolves R03, and R01 and R02 stay
resolved. The released subset passes SPEC, then QUALITY, then scoped local compliance,
with P1, P2 and P3 at 0. This covers product `1c5bb1fb` and the clean report head
`400c0316`. The private report `p08-a13-review-05/review-report.md` has SHA-256
`5138f9f8…a24b4bef2`, and its manifest binds 128 artifacts.

- The new regression passes all four primary, legacy and readability scenarios, and
  `scale-calibration` and `scale-estimator` exit 0.
- The earlier 42/42 and the 16 retained passes are carried only after verifying all 42
  old method ASTs and the unaffected source bytes. They are not a new full rerun.

This accepts A13.1.a, A13.2, the released part of A13.3, and A13.4.a. A13.1.b and A13.4.b
stay gated, and the parents A13.1 and A13.4 stay open. There is no whole-P08, F06,
integration or host-enforcement acceptance.

### P08 integration preview and A13.1.b/A13.4.b release (2026-09-24)

P08 is not accepted until the F06 recheck, so the real integration waits. To let the
gated leaves proceed in parallel, the coordinator built a local preview branch,
`jokerman-microsoft-p08-integration-preview`:

- `3c7ce67e` merges the accepted A13 head (`400c0316`, which contains P08 `229656cf`)
  into recovery `0e1746a8`, with P10 integrated. It has the 14 predicted conflicts and
  resolves them with the verified map: shared state and ten recovery-owned files take
  recovery's side, `state.sh` and `cycle-footer.sh` take A13's side, and skillify gets
  the port. A13 changed none of the recovery-side files. The three test reconciliations
  and reconciliation 4 apply cleanly, and the catalog and wiki are regenerated.
- `14b0b51c` is **reconciliation 5**. After the P10 fan-in, `li-migrate-claude-home`
  no longer writes `migration/claude_home_migrated`, `li-lifecycle migrate` writes no
  audit event, and the rewritten pack skills no longer name
  `pack-lifecycle/pack_created` or `pack_validated`. The coordinator removes those kinds
  and the empty category (catalog 24/69 → 23/66), as the catalog's note assigned to
  fan-in. It also corrects the `hooks/hooks.json` comment, because the doctor
  (`bin/li-doctor` → `li-lifecycle doctor`) reports only whether the hooks log exists.
  Event-catalog, hooks-registration, catalog, wiki and manifest-identity shapes pass.

The coordinator releases A13.1.b and A13.4.b to f2 on a new branch from `14b0b51c`:

- **A13.1.b.** Read installer evidence only through P10's accepted surfaces: the native
  receipt check, `li-lifecycle` inspect and doctor JSON, and `li-managed-transaction`.
  Map P10's states to A13's classes as the contract table requires. Add no second
  parser of `meta.tsv`, `plan.tsv`, `journal.json` or snapshot records. Confirm that
  the integrated doctor derives no firing or not-firing verdict from logs.
- **A13.4.b.** Through P10's accepted reader, cover a complete, an interrupted and an
  absent-store transaction produced by the integrated P10 surface. Each is reported as
  verified file state, incomplete or unobserved, with host activation `unverified`.
  Assert the doctor's no-verdict rule.
- Work test-first, change only what the leaves need, and add an appendix to
  `reports/P08-A13.md`. If the pending P10 integration repair changes these surfaces,
  revalidate on the corrected base.
- Freeze for SAME9db, which runs SPEC and then QUALITY and compliance if eligible, and
  send the handoff to the coordinator in parallel. F06 keeps priority. Any helper
  follows the operator model directive.

Preview checks (label `p08-preview-checks`, launcher `d288e74b`):

- On `3c7ce67e`, observation-consumer wording, cycle-footer-present, no-swedish, catalog
  and wiki regeneration, audit-writes-via-helper and `universal-work-lifecycle` (1,450 s)
  pass. The jobs reconciliation therefore holds.
- `event-catalog-producers` failed there on the three P10 fan-in kinds, which led to
  reconciliation 5. On `14b0b51c` it passes, together with hooks-registration, catalog,
  wiki and manifest-identity shapes.
- Part of the `universal-work-lifecycle` entry ran while reconciliation 5 was
  uncommitted in that worktree. The test reads neither the catalog nor `hooks.json`, so
  the result carries over to `14b0b51c`.

**Remaining coordinator fan-in at the real P08 integration.** A13's report lists five
coordinator items. The preview covers three of them: the regenerated catalog, the
`hooks.json` comment and the migration entry (reconciliation 5). Of the other two:

- **Reconciliation 6 is still to do.** `bin/li-events.py`, `bin/li-lessons.py` and
  `lib/event-catalog.json` are in neither `ADAPTER_RESOURCES` in `bin/li-copilot.py` nor
  `lib/capability-selections.json`, so an installed kit would lack A13's reader and
  lessons tooling. P10's integration repair is changing `bin/li-copilot.py` now, so this
  lands at the real integration after that repair, with any count-sensitive kit-test
  update as a disclosed coordinator reconciliation. The final whole-P08 review covers
  it.
- **No re-pin is needed.** `install/verify.sh:470` and `tests/runner/check-install.ps1`
  name the scaffolding lessons baseline by path only, without a hash.

The final whole-P08 review covers the F06 and A13 branches after integration.
The existing `universal-work-lifecycle.py` jobs expectation and the 229/624
ancestry are reconciled at that integration, not in the A13 branch.

### Coordinator trial-merge findings for integration

The coordinator made a trial merge of P08 head `229656cf` onto recovery `14c54d46`
(merge base `5c3e7533`) to prepare the integration. It was preparation only. Nothing
was committed, and no P08, F06 or reviewer path was touched. It produced ten conflicts:

- Nine resolve to ours. The three shared-state files are coordinator records. In
  `bin/li-copilot.py`, `bin/li-work-artifacts.py`, `skills/welcome/SKILL.md`,
  `tests/integration/copilot-kit.py`, `tests/unit/test-runner-contract.sh` and the
  add/add `tests/integration/universal-work-lifecycle.py`, HEAD already carries P08's
  content plus later reviewed additions. For example, the test file is P08's plus the
  63 lines of `aa5cf0e4`.
- `skills/status/SKILL.md` needs a union rather than one side. P13's accepted reader
  (HEAD) keeps its selected-map, cycle and profile structure. Its supplementary jobs
  section adopts P08's read-only observations (`list_jobs --read-only`,
  `stale_jobs 24`) with `LINTEL_JOBS_NO_INIT=1` and an explicit repo-local
  `LINTEL_JOBS_DIR`. The explicit directory matters: for a repository without a v5
  layout marker, `_jobs.sh` falls back to `$LINTEL_HOME/jobs`, which is the home scan
  that P13's text forbids. This union belongs to P13's remaining status and welcome
  fan-in after P08 is integrated.
- P08's own head fails `li-catalog.py --check` (seven changed skill descriptions) and
  `li-wiki-gen --check` (`docs/wiki/skills.md`). Both files are regenerated at
  integration, under the catalog item in the contract's coordinator fan-in list.

The coordinator then ran clean, CI-like tests: no `LINTEL_*` export besides the approved
PowerShell 7 selector, and a synthetic home and temp. They covered every P08-changed
test entry point except the kit, plus `jobs-system-present`. Of these, 16 pass
unchanged on the trial tree. Two genuine P08 findings reproduce on `229656cf` alone, and
the remaining-suite run below adds a third that also reproduces there:

1. `tests/shape/cycle-footer-present.sh:34` requires `cycle_mode[:=]` in
   `skills/cycle/SKILL.md`. P08 moved the write into `workflow_begin`, which calls
   `state_cycle_begin`, and `lib/state.sh:290` still records `cycle_mode=$mode`. The
   behavior is kept; only the text contract is stale.
2. Two `universal-work-lifecycle.py` tests read `$LINTEL_PYTHON` from the caller's
   environment: `test_plan_analysis_caller_produces_selected_reports_and_retains_history`
   and `test_plan_analysis_identity_refuses_denied_probe`. Neither `setUp` nor the new
   `.sh` wrapper sets it, so both fail in CI's environment. With it exported, both pass
   (220 s).
3. `tests/shape/no-swedish.sh` finds nine Swedish-letter lines in
   `tests/unit/intent-operation-boundary.sh`. P08 commits `8d477701` to `70469296` added
   Swedish operator requests there as routing inputs. They are functional, not residue,
   like the already allowlisted `tests/unit/orientator-mechanical-routing.sh`. Recovery
   HEAD passes the check.

The coordinator reconciles all three at P08 integration, like the jobs expectation above.
The shape check follows the actual persistence path: `workflow_begin … "$mode"` in the
cycle skill, `state_cycle_begin "$id" "$mode"` in `lib/workflow.sh` and
`"cycle_mode=$mode"` in `lib/state.sh`. Each clause refuses its negative variant. The
fixture calls `setdefault` for `LINTEL_PYTHON` with the running interpreter, so an
explicit caller selection still wins. The English-only guard gains that one exact test
path in its header and case lists. A draft of all three passed on `229656cf` alone in the
clean environment with `LINTEL_PYTHON` unset: the shape check, the complete module
(39 tests OK in 1,436 s) and the guard. The guard scans only `git ls-files`, so its
negative used an intent-to-add probe named `intent-operation-boundary-probe.sh` and a
Swedish line appended to a tracked test. Exactly those two were reported, and the scratch
tree was restored. None of the three changes product behavior, and the final whole-P08
review covers them. On the trial tree the module has 42 tests (868 s). It joins CI's
`run-all.sh` and adds to the P15 timing risk.

`tests/e2e/harness-critical-path.sh` fails differently on each tree:
- On `229656cf` alone it also fails the footer next-command assertion. HEAD's
  `ebfbcd0d` aligns that test with status-grounded resume, and the trial tree passes it.
- On the trial tree, only installed Copilot init and check fail, exactly as on recovery
  HEAD. They refuse because the installed home lacks `.claude-plugin/plugin.json`. P10's
  installer copies it, and the released P10 aliases item closes the remaining
  `config/aliases.yaml` refusal.

So this is not a P08 finding.

The remaining 124 test entry points on the trial tree (everything except the kit) then
ran in the same clean environment: 119 pass and 5 fail, in 7,942 s. None of the five is
a new P08 finding:
- `no-swedish.sh` is finding 3 above.
- `harness-critical-path.sh` fails exactly as on recovery HEAD (P10-owned, above).
- `universal-profile-scenarios.sh` refuses in its freeze step. That step compares each
  P14-selected production source with `HEAD`. P08 changes 5 of those 35 sources, and the
  trial merge was staged but not committed. On a scratch commit of the same merge
  (`39384b38`, detached, never pushed; no branch moved), the test passes in 124 s.
- `cycle-footer.sh` fails its P0-A check, which gives each render `timeout 5`.
  `frontend-design-surface-hook.sh` fails its 2,000 ms threshold, and in isolation Git
  Bash's "could not find /tmp" warning also leaks into its captured output. Both fail
  identically on recovery HEAD under the same launcher, so they are pre-existing local
  load and environment sensitivities rather than P08 behavior. Main CI passes both.

### Preview with the A13 head and P10 first

A later preview used only `git merge-tree`, with no working tree or ref change. It merged
A13 head `c7fbfd6f` onto recovery `13eda085`, and onto an unreferenced commit of the
staged P10 trial (`72cf78a5` onto `13eda085`). The A13 head adds three product conflicts
against recovery, and P10 adds one test conflict:

- `lib/state.sh` and `lib/cycle-footer.sh`: both sides carry P08's integrated code. A13
  adds the UNTRUSTED and legacy diagnoses, their footer labels, a comment and the review
  date. Taking A13's side loses nothing from ours.
- `skills/skillify/SKILL.md`: recovery holds the reviewed rewrite (`7884ddd0`,
  `4922a6b3`), while A13's by-ID amendment edits the older text. The resolution keeps ours
  and ports A13's three remaining changes: `--from-lesson <L-NNN>` read by ID, the exact
  `li-lessons.py get --id L-NNN` step with exits 1 and 2, and those exits in the failure
  mode. Ours already uses `L-042`. The consumer tests run only the two code blocks, which
  the port leaves unchanged. The coordinator's port is `p08-skillify-port.md` (LF SHA-256
  `466fd24d1207bcd250751cf4792bcbf52f3e4d59f53ddda3f99a45e79a258cfd`).
- `tests/integration/pack-source-target-resolution.sh` (with P10 only): P08's single change,
  the `native_paths.py` copy line, is already in ours, so ours with P10's
  `context_safety.py` copy stands.

Only four files merge P10 and P08 changes textually. `universal-profile-context.py`
auto-merges to ours. A13 changes neither `bin/li-copilot.py` nor
`tests/integration/copilot-kit.py`, so their resolution to ours still holds with P10.
The preview is repeated at integration with the final P10 and A13 heads.

### Fourth reconciliation: event categories retired by P09

A trial integration tree shows one further seam. It is the detached scratch commit
`6da1e9c6`: recovery `7b760b87` merged with A13 head `c7fbfd6f`, resolved by the map
above, with the three test reconciliations applied and the catalog and wiki regenerated.
On it, A13's `tests/shape/event-catalog-producers.sh` fails six assertions. Accepted P09
removed the instruction-only producers `audit_log <x>-decisions <x>_module_complete` from
`skills/{da,dh,sc,ta,tq}/SKILL.md`, and `full_engineering_pass_complete` from
`skills/full-engineering-pass/SKILL.md`. A13's catalog, based on `229656cf`, still lists
them. Nothing else in the merged tree names these categories, kinds or log names.

The catalog has no retired status, and `bin/li-events.py` reports an uncatalogued kind as
`unknown_kind`. The coordinator therefore removes the six categories at integration, in
the same way the catalog's note already assigns P10's `claude_home_migrated` producer to
fan-in. Historical records of those kinds are then reported as unknown rather than
attributed to a producer that no longer exists. The A13 branch cannot make this change,
because its skills still carry the producers.

The removal takes the catalog from 30 categories and 75 kinds to 24 and 69, and leaves
every other entry unchanged. With it, the shape test passes on an export of `6da1e9c6`
(58 producer calls, 33 hook directories). The patch is `p08-event-catalog-reconciliation.patch`
(LF SHA-256 `f072432925e4cf6727cb0ee7cb33b9e5caea3590b1bf34027f791dac3adf2fef`,
3,398 bytes, 78 lines removed), verified with `git apply`. f2 has been told. The final
whole-P08 review assesses the removal.

**Targeted trial run on `6da1e9c6`.** The run covered A13's module, 26 recovery
consumers of A13 files, the profile scenarios and all shape tests. The coordinator
stopped it after 26 of 68 entries to free the host for P10's integration kit. Of the 26:

- 25 pass.
- `universal-profile-context` failed one concurrency case,
  `test_concurrent_rebinds_publish_current_and_selected_as_one_transaction`, with a
  `ProfileError` in the profile lock. A13 changes neither `lib/profile_context.py` nor
  the test, and the file is identical in recovery. A targeted rerun on the same tree
  passes (430 s). That is a non-reproduction, not proof of cause; the failed run is
  retained.

The remaining entries run at the real P08 integration with the final heads.
