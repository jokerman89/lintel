# P11 - Working design and real browser operations

**Authority:** A14/A16; CP-03/06/07/08/13/15, AG-03/07/10, existing design ADR0015-17.
**Prerequisites:** P03 URL/recovery, P05 controls, P06 host, P07 profile, P08 work and
P09 specialist contracts. Read relevant frontend/browser skills before implementation.

## Browser-only first unit

The coordinator releases A16.1-.4 now against accepted P03/P05/P06/P07 and the
existing approved work-map reader. Those leaves do not require an unaccepted P08
runtime or P09 domain-result implementation. Own only `browse`,
`open-managed-browser`, `setup-browser-cookies`, `scrape`, their browser-operation
references/minimal helpers and focused tests plus the P11 report for this unit.
Do not import P08/P10 WIP or edit the registry/profile/control/work-map contracts.

P09 is concurrently improving agent/domain knowledge. No frontend-agent,
shared-design-schema, A14 renderer/library or document-format change is released
yet; those parts retain the package's original prerequisites and later owner handoff.
Preserve the nine-phase cycle. A useful actual browser operation is the goal, not
a new browser daemon or generic host engine.

Inspect the real provider schemas and establish an isolated synthetic browser
context before any browser action. Never inspect/use personal profiles, tabs,
cookies or credentials. The explicit loopback fixture and its owned temporary
browser data are allowed; user-authentication instructions must keep authentication
on the user's chosen surface without copying credentials. Existing installed tools
come first; follow the declared missing-dependency boundary before restoring any
task-local dependency. A provider/isolation/permission gap is a concrete blocked
observation, not permission to invent an API or bypass a denial.

Freeze one attributable A16 unit with actual open/read/action/screenshot/print and
ownership evidence, plus precise unsupported observations. Do not claim whole P11
or A14 completion. Continue in this same owner session after the later design/agent
handoff; no parallel overlapping writers or generated-output edits.

## Ownership

### Bounded synthetic browser continuation

On 2026-09-22 the first full native pipeline produced no CDP endpoint. Its owned
log reported synthetic AppData lookup failure and browser-background Google
activity despite `--disable-background-networking`. No remote page/action was
requested, but background effects are unverified. The owned process/profile and
synthetic servers were stopped/cleaned; logs remain evidence. Do not inspect or
roll back personal-home state, and do not call that failed launch successful.

One bounded synthetic fixture-hardening/retry batch is authorized: create and
verify all already-declared synthetic application-data directories before launch;
use `--disable-default-apps` for the owned fresh profile; and, if needed, start an
owned loopback deny-only proxy before Chrome and select it only through the child
process's proxy argument. The proxy must never forward traffic, authenticate,
intercept/decrypt TLS, modify certificate trust or listen beyond loopback. Record
refused requests without credentials/payloads. Allowed fixture page traffic stays
explicit loopback traffic under the existing P03 admission/interception boundary.
Keep every server/process/profile attributable and tear down only exact owned IDs
and verified roots. No system proxy, firewall, OS policy or provider switch after
a permission denial is authorized.

This proxy is private test machinery, not a shipped daemon, general network stack
or proof of OS-wide egress isolation. Do not claim that a flag/proxy eliminates
all browser background effects. Record its actual bounds and unresolved observations.
If startup/context ownership or permitted navigation still cannot be established,
stop native execution and freeze that precise live gate as blocked; do not continue
a configuration-variant search. Independent source/contract work can continue.
No external/customer page, personal profile/cookie, credential transfer or TLS
bypass is released. Report this attempt separately from the earlier failure.

### Native location-metadata observation only

The frozen A16 source `d389905` / report `84f0d38` remains blocked. Read-only
comparison found no evidence of split/quoted/MSYS corruption of the single
`--user-data-dir=<path>` argument. Pinned Chrome source shows that its later
default-directory check uses Windows `SHGetFolderPathW`, not the environment-folder
existence test. Unknown default identity is rejected, but the exact failed native
input/HRESULT was not retained. No corrective launch configuration is established.

Authorize one bounded read-only comparison of that same native location lookup
in the two already recorded synthetic environment shapes, using fresh owned
fixtures at their recorded dimensions. Use current-user `CSIDL_LOCAL_APPDATA` /
`SHGFP_TYPE_CURRENT` without creation, alternate flags, elevation, registry/policy
inspection or provider invocation. Only this native location-metadata query is
released; no personal directory contents, browser profiles, cookies, credentials
or incident capture may be read. Record HRESULT and inside-approved-root/outside/
unresolved classification only; do not log an outside personal path or treat an
error buffer as a usable path. Do not stat, create or alter an outside result.

Keep product/source unchanged and all output in owned runtime evidence. Native
browser use remains stopped. No fixture alignment, new Windows configuration,
API-variant search, browser launch, TLS workaround or new package install follows
from a query result. Return the exact observation and smallest evidence-backed
proposal; an outside/unresolved result is not permission to broaden access.

### Computed-location diagnostic and conditional fixture correction

Both fresh recorded shapes returned `0x80070002` from the unchanged CURRENT
lookup, despite their declared environment directories existing. This is not a
proven path-length cause or a successful prerequisite. The coordinator checked
the primary [CSIDL documentation](https://learn.microsoft.com/en-us/windows/win32/shell/csidl):
`CSIDL_FLAG_DONT_VERIFY` can return an unverified location without creating or
initializing that folder. It is a diagnostic, never a substitute for Chrome's
normal check.

Authorize one computed-location query for the failed-pipeline synthetic shape,
using CURRENT plus DONT_VERIFY, with no CREATE/default/other flags. Preserve its
dimensions and the existing metadata-only privacy rules. Only an S_OK result
provably inside the already owned synthetic HOME/fixture can be used, through the
existing containment/reparse checks. An outside/unresolved result must stop here:
no contents/stat/create, path disclosure, ambient-home copying or policy query.

If that result identifies a missing in-scope native folder, create only that exact
owned fixture directory, leaving the recorded environment/profile arguments
unchanged for the causal check. Re-run the ORIGINAL CURRENT lookup with zero
diagnostic flags. Only its S_OK result at the same owned location satisfies this
prerequisite. Do not call DONT_VERIFY success validation or pass it to a product
gate. No registered known-folder, global setting or personal path may change.

After that explicit gate, the owner may retain the minimal fixture-construction
correction in the existing pipeline test and run the normal sealed A16 pipeline
with its unchanged launch/profile arguments and already approved deny-only fixture.
If native startup/context ownership still fails, stop native execution again
without a startup-variant search. If ownership is established, complete the
original A16 operation/guard/inspection work and fix concrete in-scope functional
defects, preserving each failed run and never editing executing source. No
provider/OS-policy/security/credential change or general browser engine is released.

A native default-folder reference outside a custom profile is not by itself a
generic product path violation. Do not turn this synthetic-fixture boundary into
an invented universal outside-root cutoff. Source/report stay frozen until the
conditional owned-folder correction is actually supported by the observation.
Return exact evidence and a new immutable checkpoint; no A16 acceptance is implied.

### Owned endpoint readiness correction

The continuation `f4cd1f3` / report `a49e7b8` has real headless operation evidence,
but no complete current-source live outer pass. Run 07 completed eight browser
cases, then its selected print inspector crashed. Run 08 later stopped on `EBUSY`
while reading its own `DevToolsActivePort`; the existing readiness loop retries
only `ENOENT`. The lock holder is unknown, and prior failures remain failed.

Authorize the original owner to handle that specific `EBUSY` at the same owned
readiness-file read within the EXISTING bounded polling limit/delay. Preserve
launch/profile arguments, endpoint validation, child-exit handling, cleanup and
all other errors, especially permission failures. No deadline increase, broad
I/O retry, lock stealing, file replacement, new metadata query or provider/
configuration search. Add focused transient-success and exhausted-busy tests,
labelled fault injection, and retain the real failure evidence.

Freeze the code before a final sealed normal live pipeline using the already
available explicitly selected `pypdf` reader. This is not a silent fallback or
permission to retry/bypass the policy-denied raster script through another shell.
Do not repeat the consumed DONT_VERIFY diagnostic; retain the verified in-owned
fixture construction. Inspect actual screenshots and printed page/text evidence,
with hashes and exact tool provenance, not file-size proxies.

Acceptance remains the original A16 headless operation/ownership/guard contract.
Do not turn unrequested comprehensive document-layout certification into another
platform project, and do not claim PDF raster/glyph-bound, headed login, persistent
profile or physical-print verification that was not observed. Any genuinely
required unavailable control stays visible to the independent reviewer. P05's
measured contrast failure and later browser error remain blockers at their own
evidence contexts; a later good run cannot relabel them.

Own only the existing Chromium helper, focused browser test(s), directly related
operation reference and report. No shared contracts, A14, agent, generator,
security-policy or OS setting change. Return the final immutable source/report
and complete run outcome for independent SPEC before eligible QUALITY.

Own design/frontend/browser skills and their reference/schema/helper assets:
`design-dna`, `design-consultation`, `design-html`, `design-review`, `design-shotgun`,
`frontend-design`, `frontend-design-review`, `frontend-motion`, `frontend-shader`,
`frontend-style-extract`, `frontend-typography`, `generate-design`, `generate-style-learn`,
`generate-web`, `generate-app`, existing framework generator skills, `plan-design-review`,
`browse`, `open-managed-browser`, `setup-browser-cookies`, `scrape`.
Corresponding frontend agents may change after P09 relinquishes them. Do not take
document renderers, `make-pdf`/`generate-pdf` (P12), central profiles/controls or catalog.
Report P11.md with actual local preview/browser artifacts in gitignored runtime storage.

### Direct design-contract second unit

**Authority:** existing A14.1-.4, recovery continuation of 2026-09-22. The original
owner's read-only proposal is approved against accepted P03/P05/P07 and P09 data
core `ea92df8`, integrated in `6144721`. Direct selected-design validation does
not need the still-unaccepted P08 lifecycle reader. Final automatic workflow,
installed closure and A14.5 native artifacts retain their separate gates.

Use one design schema and helper under `skills/design-dna/references/` and
`skills/design-dna/scripts/`, adapting the existing frontend v1 and pipeline
v1.0 envelopes without changing their filenames or introducing a format-wide
migration. Preserve readable legacy inputs, but refuse unresolved, contradictory
or unknown-version rendering rather than inventing provenance or defaults.
Reuse accepted profile/control/work/owned-I/O definitions and external preparation;
do not fork their parsers, schema objects or acceptance authority.

The released owned consumers are the existing frontend/design/generate-design/
generate-web/generate-app methods and direct tests/reference docs from the proposal.
P09's accepted frontend-role knowledge is retained; only directly necessary
schema/caller wiring within `agents/frontend/` may change. Preserve role names,
frontmatter, substantive decisions and no-change evidence. Other agents and P12's
document-format producers are excluded.

Correct actual variant/stack/brief/output argument mappings and the six long
emitted review-dimension keys, with short aliases mapped once. Reject unknown,
duplicate or missing options. Explicit none/CSS/library motion and no-shader
decisions are valid without forced dependencies; they cannot remove mandatory
keyboard, contrast or other P05 controls. Existing project technology and required
policy constrain the brief; no unknown-stack React fallback or automatic install.

Retain ADR-0015/16/17 retrieval, brief/profile/corpus precedence and token emission.
Verify selected profile assets and their bytes under the existing P07 selection,
without silent alternate-brand fallback or personal-home scanning. Record real
library/font source, version/license and rationale rather than guessed latest or
outdated blanket licensing advice.

Test the real shared helper, argument mapping, legacy/current envelopes,
none/CSS/no-shader, missing policy/assets and profile drift, required controls and
actual P09 artifact handoff. Preserve accepted browser source/report `3115790` /
`043019a` and all prior failures; do not modify its eleven source files or run a
new browser/native-location probe during its independent review. New design tests
may be separate from that frozen pipeline. Use the same original owner and a
clean ancestry-preserving merge of accepted recovery dependencies, not WIP import.

Freeze a separate design product/report for independent review. A14.5 still needs
explicit release after A16 acceptance: actual static page and supported app from
one profile, build/health/UI/contrast/reduced-motion/responsive evidence. Schema
fields, old A16 evidence or source checks alone cannot close that requirement.

### Q1 complete numeric extraction

Independent `5504a2c`, preserved report-only as `d8887a2`, passes original A16
SPEC but fails subsequent QUALITY with P2 Q1. The actual `number_extract` returns
`$.50` and `-.50` as 50, and `-$9.50` as positive 9.5 without a field error.
The original owner may now correct only `skills/scrape/scripts/extract.mjs`,
its focused existing browser-operation regressions, directly relevant transform
documentation if needed and its report.

Recognize the complete supported signed/decimal form or explicitly reject an
unsupported/ambiguous form; never coerce an internal numeric substring.
Preserve ordinary positive/negative amounts, existing locale/multiple-number
errors, trim/multi/diff behavior, prototype-like field names and all URL guards.
No locale/currency framework, browser provider/readiness change or native rerun
is required or released. Reproduce the three reported failures, add whole-class
positive/negative contrasts and run the retained focused suite.

Freeze a separate correction/report from `043019a` for SAME reviewer `1f655ca9`.
Reuse applicable unchanged original headless SPEC and retained artifacts rather
than rewriting their provenance or rerunning a platform investigation. Complete
the affected independent QUALITY recheck before A16 acceptance. After this
correction is frozen, the already authorized direct-design unit may proceed in
the same owner lane with separately pinned scope; final A14.5 remains gated.

### Q2 deterministic numeric parsing

Independent `4cf84d0`, preserved as `44ba13e`, closes Q1 but finds introduced P2
Q2: the new matcher stalls on 4096 whitespace characters within the existing read
bound. The original returns an explicit field error promptly. The observed
five-second cutoff is evidence, not a new performance specification.

The same owner may replace only numeric token/whitespace consumption in
`extract.mjs`, its focused existing regressions, directly coupled documentation
and its report. Use deterministic full-input consumption, preserving every Q1
signed/decimal positive and unsupported-form error. No overlapping nullable
whitespace branches, locale engine, browser/readiness change or native rerun.
Use owned bounded subprocess/thread probes so malformed-field tests cannot hang
the runner; retain 4096-space, larger allowed whitespace and decorated-invalid
contrasts plus all existing extraction tests.

Preserve A14 WIP separately. The accepted P09 merge `7aea848` leaves all browser
blobs unchanged and may remain the repair base; identify it as a dependency, not
an extraction change. Commit only the released repair/report paths, then return
the immutable pair to SAME reviewer `1f655ca9` for affected SPEC/QUALITY.
Original headless and Q1 evidence retain their actual source identities.

### A14.5 bounded native artifact verification

A16 now passes independent SPEC/QUALITY in `3049811` on `d3b5569`, integrated
with full history in `7cb3812`; joined four shared and 30 Node checks pass.
The original owner may execute A14.5 after its direct design source is stable,
using this accepted provider. This is a new artifact-specific observation unit,
not reassignment of A16's older native evidence.

Generate one real static page and one explicitly selected supported-framework
app from the SAME resolved profile/design contract. Preserve project/brief
choices, provenance and no-animation/CSS/no-shader validity. Keep fixture source,
build output, profile reference and both artifacts attributable. Use actual
build output and a responding loopback server, then inspect real DOM/actions,
keyboard/focus, responsive overflow, reduced motion and measured contrast.
Retain actual images and errors; a schema/shape check is not visual acceptance.
An intentionally low-contrast negative must remain a P05 mandatory failure.

Only explicitly owned synthetic fixture/app/browser paths and loopback origins
are allowed. Reuse accepted paused-target ownership, URL guards, synthetic native
folder construction and deny-only process-local proxy. No personal browser,
cookies, credentials, remote page, global setting, new launch variant or consumed
native-location diagnostic. Use exact owned processes and scoped teardown.
Do not repeat the denied PDF-raster, Copilot UI, Word or Excel application routes.

Prefer existing tools; any genuinely missing build dependency must be declared
in the owned fixture manifest before a task-local restore using its explicit
synthetic configuration/cache. No global install, TLS/security bypass, credential
operation or automatic framework replacement. A missing or denied capability
remains explicit and blocks only its dependent observation.

This permits the planned A14 artifact checks and directly coupled generator/
review corrections, not a browser engine or platform project. Freeze the coherent
design source/report and exact observed artifact bindings for SAME reviewer
`1f655ca9`. Shared P12 format wiring, automatic P08/module integration and final
cross-component acceptance remain separate until their actual accepted inputs.

### D1-D3 direct-design correction

Independent `5d3404a`, preserved as `82d4b28`, rejects the direct design unit
with three P2 findings; QUALITY has not started. The original owner may repair
only the existing design helper, directly coupled renderer instructions/reference,
focused design tests and its report. No schema migration, new framework catalog,
provider/installer/P12 change or accepted browser-source change is released.

D1: renderer handoff must consume the exact design file that was validated.
Use the existing canonical filename/envelope interface or a genuinely supported
exact-file interface; never drop an alternate basename and silently select a
different canonical file. Preserve valid frontend AND pipeline mappings, literal
paths with spaces and existing variant/stack/output arguments. Refuse unsupported
or mismatching selections explicitly rather than inventing a new consumer flag.

D2: a present bound project manifest constrains stack selection even when the
caller sets `existing:false`. Actual unsupported project evidence cannot become
a new-project fallback by changing a Boolean. Preserve genuinely new projects
and supported existing stacks; no broader framework support is needed.

D3: generate-web must use the same active/non-none shader predicate as the
accepted design contract/app consumer. Both null and the accepted non-null
no-shader representation emit no GPU dependency. Mandatory unrelated controls
remain intact; no artificial no-shader score or dependency is restored.

Reproduce all three exact independent cases and add bounded positive/negative
contrasts for both envelopes, manifest/flag contradictions and both no-shader
forms. Retain the original direct/legacy/DNA and shared-provider cases.
Do not edit executing source: finish and seal any currently running artifact-only
batch first. Native artifacts and their failed contrast history retain their
actual source identities; reuse unchanged observations only when applicable.

Freeze the coherent repair/report for SAME reviewer `1f655ca9`, complete scoped
SPEC before first eligible QUALITY. A14.5 framework-network and any unavailable
rendering observations remain separate and must not be declared successful.

### D4/D5 exact pipeline inputs and retained stdout fragments

Independent `1ce6f8c`, preserved as `17732be`, closes D1-D3 but fails whole
direct-unit QUALITY on two P2 consumer-boundary defects. Release only the
existing design helper, the three fragment caller recipes, directly coupled
reference/tests and report to the original owner; preserve all earlier closures.

D4: an existing directory-based pipeline handoff must bind the actual sibling
`content.md` that its renderer reads, including its path and hash. A valid hash
of a different selected brief is insufficient. Preserve canonical pipeline
positives and frontend brief behavior; no new schema, provider or unsupported
exact-input flag. Check the complete producer/consumer input set rather than
fixing one literal example.

D5: preserve the typography/motion/shader solo stdout mode. Validate the actual
fragment value through the existing API before emitting stdout, or validate an
explicitly owned relative staging file then emit it. Do not pass `/dev/stdout`
to a rooted-file reader, create a special-file/absolute-path exception, silently
require `--out`, or add a new public helper API. Preserve named-file validation,
explicit errors, no-motion/no-shader and unrelated mandatory controls.

Reproduce D4's different bound brief/current content and all three D5 defaults.
Exercise valid canonical content, mismatched/missing content, stdout/named output,
invalid fragment refusal before output and retained D1-D3 cases. Run the existing
direct/legacy/provider regressions on stable source. No native/browser/OS/dependency
operation is required; earlier artifacts keep their original attribution and limits.
Freeze repair/report for SAME reviewer `1f655ca9`, then affected SPEC/QUALITY.

### Self-contained static artifact completion

Complete source `a1b3a45` now passes independent `edecfdf` A14.1-.4 SPEC and
whole direct-unit QUALITY; integration `e1cb9d2` retains all 24 exact product
blobs with 18 joined contract tests and legacy compatibility passing.
The two earlier native batches remain their actual `f72f316` observations.

The selected single-file static route still lacks its promised self-contained
HTML and complete directly observed responsive images. The original owner may
correct only that owned synthetic artifact in a NEW attempt/output, retaining
the old HTML/CSS/JS, failed hover and all original controls unchanged. Embed the
same selected content, style and behavior without changing the profile, brand,
framework choice or design to sidestep a requirement. Preserve script execution
order. No required external asset, stylesheet, module or script may be necessary
to open the resulting HTML by itself.

One bounded additional native confirmation batch is released after the new
artifact is stable. Serve only an owned isolated copy of that HTML, with no
old sibling assets available, through the accepted provider/deny-only loopback
environment and unchanged native ownership construction. Check actual resource
requests and meaningful interactions; inspect desktop/tablet/narrow images
directly and retain their hashes, not a DOM/file-size substitute. Retain existing
keyboard/focus, overflow, reduced-motion and measured-contrast obligations.
The deliberately failing low-contrast control must remain a failure.

Use a fresh artifact context and record exactly which accepted source produced
it; do not relabel old images or shrink existing mandatory obligations. Stop and
close only owned processes/contexts after this batch. No further native-location,
launch-variant, dependency/network/framework restore, private app or denied
inspection route is authorized. If the single-file or image/control requirement
still fails, freeze that actual result rather than starting another visual loop.

This is the original static half of A14.5, not a replacement app. The declared
framework's TLS/missing-Vite/build/runtime evidence remains blocked and separate.
No new product/provider API, generic exporter or framework substitution is
needed. Report-only tracked changes and existing owned runtime fixtures remain
with the original owner; SAME `1f655ca9` reviews the exact artifact evidence
separately from source acceptance.

### A14.5 framework restore through the Microsoft feed proxy (2026-09-24)

**Authority:** the operator's instruction of 2026-09-24 19:25: "Can't you fix this, by using the
Microsoft npm registry, since public npm is blocked; we have an MS repository for it."

This machine's npm configuration already names `https://packagefeedproxy.microsoft.io/npm/`, with
no credential in it. A read-only probe from a synthetic npm configuration, with separate user,
global and cache files and no credential, resolved `vite` 8.3.0 and `react` 19.3.0. The earlier
restore used a synthetic configuration without that registry, so it defaulted to the public
registry and failed on TLS.

The coordinator releases the framework half of A14.5 to the original P11 owner:

1. Declare the selected framework's build dependencies in the owned fixture manifest, as the
   A14.5 section above requires. Restore them task-locally with an explicit synthetic npm
   configuration inside the owned fixture (`npm_config_userconfig`, `npm_config_globalconfig` and
   `npm_config_cache`, each its own file or directory) that sets only
   `registry=https://packagefeedproxy.microsoft.io/npm/`. Record the lockfile and the resolved
   versions.
2. Nothing else relaxes:
   - no credential or token, and no read of the real `~/.npmrc`;
   - no global install and no TLS or certificate change;
   - no other registry, and no automatic framework replacement.
   If the proxy refuses a package, freeze that actual result.
3. Build the app from the same resolved profile and design contract as the static page. Serve
   the build output on loopback through the accepted provider and deny-only environment, and run
   the same native checks: DOM and actions, keyboard and focus, responsive overflow, reduced
   motion and measured contrast. The deliberately low-contrast negative must stay a P05
   mandatory failure. Retain the actual images with their hashes.
4. **V1, direct image observation, for both the static page and the app.** It needs a
   reviewer whose tools return visible pixels; the SAME review found none. The coordinator
   confirms with SAME `1f655ca9` whether it can view images, and otherwise assigns an
   image-capable independent observer for V1 only. SAME reviews everything else.
5. Change only the owned fixture, the directly coupled generator or review corrections and the
   report. Freeze for review, and send the handoff to the coordinator in parallel.

The Word, Excel, PDF-raster and P12 record-persistence denials that block A15 are unchanged by
this release.

Preserve design-dna data/provenance, profile precedence, useful motion/type/shader
expertise, extraction/variation modes, runtime/performance evidence, cookie/session
ownership and familiar entry points. No mandatory fashion library or animation.

## Leaves and observable scenarios

| Leaf | Dependency | Acceptance |
|---|---|---|
| A14.1 | P05/P07/P09 | Shared design schema consumed from brief through renderer and review |
| A14.2 | A14.1 | Caller arguments and emitted dimension names round-trip without mismatches |
| A14.3 | A14.2 | Static/CSS-only/no-animation/no-shader are valid first-class decisions |
| A14.4 | A14.3 | Existing project technology and sourced versions/licenses govern library choices |
| A14.5 | A14.4,A16.3 | Static page and app become runnable artifacts using the same resolved profile |
| A16.1 | P06 | Shared open/read/act/screenshot/print/session-ownership browser operations |
| A16.2 | A16.1,P03 | Actual host provider and validated URL/redirect boundaries; no invented browser daemon |
| A16.3 | A16.2 | Observe local page read, UI action and print result with actual available browser |
| A16.4 | A16.3 | Authentication remains on user-chosen surface; a profile folder is not engine availability |

Use real available browser tool APIs after discovery, not assumed MCP schemas. A synthetic
loopback fixture is authorized; verify a started local server responds and stop that
specific process after the scenario. No personal browser profiles, remote/customer pages
or credential transfer. If a native operation truly is unavailable, preserve a concrete
fallback and leave that observation unverified.

Add `tests/integration/design-browser-pipeline.sh` for actual contract/fixture flow,
and record native browser read/action/print evidence separately. Static source checks
cannot establish layout, accessibility interaction or performance; mandatory unavailable
measurements remain unverified via P05. Recheck low contrast, reduced motion, keyboard
flow and exact schema/argument negatives. Do not fake visual inspection with file size.

Commit coherent code/method/test changes and report actual artifacts, per-leaf outcomes,
profile effects and preserved scenarios. Independent reviewer must not repair the design.
