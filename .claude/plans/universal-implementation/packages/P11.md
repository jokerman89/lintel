# P11 - Working design and real browser operations

**Authority:** A14/A16; CP-03/06/07/08/13/15, AG-03/07/10, existing design ADR0015-17.
**Prerequisites:** P03 URL/recovery, P05 controls, P06 host, P07 profile, P08 work and
P09 specialist contracts. Read relevant frontend/browser skills before implementation.

## Browser-only first unit

The coordinator releases A16.1-.4 now against accepted P03/P05/P06/P07 and the
existing approved work-map reader. Those leaves do not require an unaccepted P08
runtime or P09 domain-result implementation. Own only `browse`,
`open-managed-browser`, `setup-browser-cookies`, `scrape`, their browser-operation
references/minimal helpers and focused tests plus the P11 report for this unit.
Do not import P08/P10 WIP or edit the registry/profile/control/work-map contracts.

P09 is concurrently improving agent/domain knowledge. No frontend-agent,
shared-design-schema, A14 renderer/library or document-format change is released
yet; those parts retain the package's original prerequisites and later owner handoff.
Preserve the nine-phase cycle. A useful actual browser operation is the goal, not
a new browser daemon or generic host engine.

Inspect the real provider schemas and establish an isolated synthetic browser
context before any browser action. Never inspect/use personal profiles, tabs,
cookies or credentials. The explicit loopback fixture and its owned temporary
browser data are allowed; user-authentication instructions must keep authentication
on the user's chosen surface without copying credentials. Existing installed tools
come first; follow the declared missing-dependency boundary before restoring any
task-local dependency. A provider/isolation/permission gap is a concrete blocked
observation, not permission to invent an API or bypass a denial.

Freeze one attributable A16 unit with actual open/read/action/screenshot/print and
ownership evidence, plus precise unsupported observations. Do not claim whole P11
or A14 completion. Continue in this same owner session after the later design/agent
handoff; no parallel overlapping writers or generated-output edits.

## Ownership

Own design/frontend/browser skills and their reference/schema/helper assets:
`design-dna`, `design-consultation`, `design-html`, `design-review`, `design-shotgun`,
`frontend-design`, `frontend-design-review`, `frontend-motion`, `frontend-shader`,
`frontend-style-extract`, `frontend-typography`, `generate-design`, `generate-style-learn`,
`generate-web`, `generate-app`, existing framework generator skills, `plan-design-review`,
`browse`, `open-managed-browser`, `setup-browser-cookies`, `scrape`.
Corresponding frontend agents may change after P09 relinquishes them. Do not take
document renderers, `make-pdf`/`generate-pdf` (P12), central profiles/controls or catalog.
Report P11.md with actual local preview/browser artifacts in gitignored runtime storage.

Preserve design-dna data/provenance, profile precedence, useful motion/type/shader
expertise, extraction/variation modes, runtime/performance evidence, cookie/session
ownership and familiar entry points. No mandatory fashion library or animation.

## Leaves and observable scenarios

| Leaf | Dependency | Acceptance |
|---|---|---|
| A14.1 | P05/P07/P09 | Shared design schema consumed from brief through renderer and review |
| A14.2 | A14.1 | Caller arguments and emitted dimension names round-trip without mismatches |
| A14.3 | A14.2 | Static/CSS-only/no-animation/no-shader are valid first-class decisions |
| A14.4 | A14.3 | Existing project technology and sourced versions/licenses govern library choices |
| A14.5 | A14.4,A16.3 | Static page and app become runnable artifacts using the same resolved profile |
| A16.1 | P06 | Shared open/read/act/screenshot/print/session-ownership browser operations |
| A16.2 | A16.1,P03 | Actual host provider and validated URL/redirect boundaries; no invented browser daemon |
| A16.3 | A16.2 | Observe local page read, UI action and print result with actual available browser |
| A16.4 | A16.3 | Authentication remains on user-chosen surface; a profile folder is not engine availability |

Use real available browser tool APIs after discovery, not assumed MCP schemas. A synthetic
loopback fixture is authorized; verify a started local server responds and stop that
specific process after the scenario. No personal browser profiles, remote/customer pages
or credential transfer. If a native operation truly is unavailable, preserve a concrete
fallback and leave that observation unverified.

Add `tests/integration/design-browser-pipeline.sh` for actual contract/fixture flow,
and record native browser read/action/print evidence separately. Static source checks
cannot establish layout, accessibility interaction or performance; mandatory unavailable
measurements remain unverified via P05. Recheck low contrast, reduced motion, keyboard
flow and exact schema/argument negatives. Do not fake visual inspection with file size.

Commit coherent code/method/test changes and report actual artifacts, per-leaf outcomes,
profile effects and preserved scenarios. Independent reviewer must not repair the design.
