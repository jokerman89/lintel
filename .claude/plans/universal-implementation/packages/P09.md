# P09 - Retained specialist methods and portable agent roles

**Authority:** A09/A17; all 69 inventory rows, WF-07/08 and AG-06/08/09/10/14/15.
**Prerequisites:** P05/P06/P08, integrated P03 safety corrections and P07 profile.

## First staged unit

The original P08 work contract is frozen and in independent review, not released
for new runtime binding. The coordinator authorizes a disjoint knowledge/
preservation unit now: read all 69 original role rows and current role bodies,
produce the required preservation map, and implement source-grounded improvements
to role expertise, worked decisions, responsibility and evidence methods in the
owned agent/domain-reference bodies. Accepted P03/P05 safety/review corrections
and P06/P07 host/profile contracts stay intact.

Do not change shared schemas, work-map/profile/review parsers, domain-loop runtime
or result-format contracts in this first unit; A09.1/.2/.5 integration waits for
explicit P08 release. Do not alter the nine-phase cycle or pretend resume is a
phase. Retain frontmatter contracts, role names, useful methods and provenance.
Formatting, generic boilerplate or renaming alone is not a substantive improvement.
Classify unchanged strong roles explicitly rather than rewriting them for volume.

This unit advances A17.1-.4 and the knowledge portion of A09.3/.4, but cannot close
their dependency-bound parents by itself. P11's parallel first unit owns only
browser skills/helpers, not frontend agents or shared design. P12 is not active.
Preserve narrow predecessor-owned executable recipes and report any needed shared
contract change instead of editing it. Freeze coherent content commits and the
preservation report, then continue in this same owner session when the remaining
binding is released. No nested writers or remote publication.

### Accepted content checkpoint and remaining design

Content `4a9b946` / report `6581339` passed independent staged SPEC then QUALITY
in `19eb776`, with all 69 dispositions read and no actionable finding. Full
history/report integration is `4983af2`; all 76 integrated paths match the
reviewed blobs/modes. Joined 12-method documentary/scenario and catalog checks
pass with unchanged source and verified synthetic process isolation. This is
content acceptance only, not runtime/parent completion. Do not reimplement the
accepted role knowledge or turn its structural checks into live-role claims.

While P08 finishes its original gates, this owner may prepare a READ-ONLY design
for the remaining domain result/iteration/dispatch binding. Inspect the approved
interfaces and exact frozen P08 mechanical reader plus accepted P05/P07 contracts;
do not import/execute unaccepted helpers, create a schema/runtime file, or change
result formats yet. Return the precise shared types/APIs to reuse, minimal owned
files, agent-owned iteration/checkpoint procedure and discriminating consumer/
failure tests as a proposal, not another backlog. Implementation still needs the
explicit accepted P08 seam release.

Preserve both modes identified by the independent review: Migrator and
ReleaseEngineer retain exact-authorized execution as well as their scoped
artifact/planning modes. A planning-only handoff is not a blanket restriction
on the whole role. Future dispatch must select the actual receiver mode rather
than silently deleting that execution value.

## Data-core second unit

The coordinator has read the complete read-only binding proposal and releases
its data-only portion now. This unit depends on accepted P03/P05/P06/P07, not
execution/import of the unreleased P08 workflow. Implement only the new
`lib/domain_result.py`, `lib/domain-result-schema.json`,
`bin/li-domain-result.py`, the proposed domain-handoff reference and
`tests/integration/domain-result-handoff.py` / `.sh`, plus owned reports.
Keep existing module/adjacent skill procedures and accepted role bodies unchanged
until their binding release.

One schema/helper owns the request/checkpoint/result wrappers and thin
validate/record/verify/summary CLI. Delegate P05 work/context/control/evidence/QA
definitions and validators; use real accepted P05 `bind_work`/prepare outputs
and P07 producers as the data-core test inputs. Do not fake or duplicate P08's
`work_context`, task parser, package grouping, workflow resume or state semantics.
Any test specifically requiring that provider remains pending, labelled explicitly.

Recording is data publication, not execution or acceptance. Require explicit owned
output and original expected file state through P03; never infer overwrite permission
from a late read. Raw records and producer-supplied `verified`/actor labels cannot
clear anything. Every fresh verification/composition decision must verify current
P07 reference/policy, caller-selected P05 expected context, exact immutable obligations
and actual artifact evidence. CLI summary must reverify raw inputs, not trust an
on-disk Boolean. Missing expected domains/checkpoints or required observations block;
scores only advise and `release_clearance` is always false.

Retain the proposal's noncircular identity: initial input identity is historical
dispatch evidence; final handoff/output selection goes through external P05 prepare
after those files exist, with QA inventory retained. Do not embed the final context/
digest into its own hashed handoff or implement another latest-review selector.
No scheduling, command execution, task/phase completion writes or automatic replay.

Accept the profile distinction: verified pack data is not personal `profile.yaml`
preferences. In this unit accept explicit advisory invocation values, bound in the
request, and ordinary selected repo-local input evidence. No automatic personal-file
read/migration, new preference parser or promotion into mandatory policy is released.
Legacy preference-file adaptation and module procedure wiring remain pending.

Run real accepted producer -> data CLI -> fresh verifier/QA-reader cases, ownership/
late-edit/path/malformed-input negatives, missing/failed required results and stale
profile/evidence refusal. Preserve the original 12 content checks separately. Label
copied-source tests honestly; actual installed-resource closure stays with the
coordinator/P10 after acceptance. Freeze this unit for the existing P09 reviewer;
it cannot close the original dependency-bound A09/A17 parents.

## Ownership and preservation

### Remaining module-consumer release

The exact P08 mechanical providers now pass independent SPEC and QUALITY in
`e98e7da8` and are integrated as `e1edc0c`. All 18 inspected accepted dependency
blobs match the reviewed source. The joined 22-method provider suite passes,
as does the 21-resource no-write preflight check. This releases the original
owner's already reviewed module-binding design; whole P08 semantic/A13 acceptance
is not a prerequisite for these accepted provider calls and is not implied.

Use the accepted `work_context` / `--view context`, `workflow_inspect` /
`workflow_resume` and current-cycle state APIs, not copied parsers or recency
selection. Retain the accepted P09 six-file data core and 69 role bodies.
The released consumer paths are the five engineering module orchestrators,
`full-engineering-pass` and its handoff reference, the narrowly related
`investigate`, `perfbench`, `devex-review` methods, their existing/new focused
tests and module concept docs. Other owners' frontend/doc-gen bodies are excluded.

Implement original map/package/leaf selection, real P07 pin/policy and explicit
advisory preferences, P05 initial immutable obligations, actual receiver mode/scope,
owned checkpoint/result publication and external noncircular final preparation.
Fresh domain verification and actual independent review/QA remain mandatory;
scores cannot hide missing/failed domains. No new schema/envelope, scheduler,
automatic global-preference reader, task ledger or policy override.

Persist explicit attempt/iteration paths; cold resume verifies the actual saved
checkpoint and selects the incomplete step without replaying unknown external
effects. Retain both planning and exact-authorized execution modes for Migrator
and ReleaseEngineer. Preserve original nine phases and resume's utility status.

Use a bounded synthetic native scenario for real dispatch/output/cold handoff
where available, with original IDs, same-ID decoys, missing-domain/high-score
negative, profile drift and original source preservation. Independent reviewers
report rather than repair. Existing 39 data-core and 12 documentary checks remain
their own evidence, not new native execution. Every product call/import uses
verified synthetic process roots; no personal state, remote mutation or invented
host/model capability. Installed consumer execution follows accepted P10; direct
provider/module work may proceed now without treating that separate gate as passed.

Merge the released recovery dependency normally into this original owner lane,
preserving history. Freeze coherent module source/report for SAME reviewer
`447d97f5`; no original compound parent closes before its full relevant evidence.

### N1 semantic correction and original receiver-mode proof

Module product `5c99612` passes independent SPEC/QUALITY in `114ddfe3`,
integrated as `2d789a4`; all 13 joined consumer methods pass. Its historical
native TA draft has separate known P2 N1, not product acceptance: the draft
strengthens R1's at-most-one safety into exactly-once liveness.

Release the original owner's bounded new TA-only correction attempt. Preserve the
83-file archive and every old record. Keep original work/requirements/IDs as
read-only inputs, actual fresh P07/P05/request/checkpoint/final-context records
for the new owned target and output preimages, and a new corrected draft/oracle.
Do not transplant the archived pin or erase the old DA obligation/history.
Only an append to P09's report is a tracked change; no provider/schema/role fix.

The guarantee, oracle and derivation must all retain at most one distinct committed
effect per paid-order identity cumulatively across retries/restarts. Zero and one
satisfy that safety property; two distinct effects for the same order fail; one
each for two orders passes; duplicate observations of the same effect do not create
another effect. Unknown observation coverage stays unverified. Add no eventual
delivery/deadline/lower bound. The small standard-library truth table proves only
the oracle's quantifier, not a live fulfillment system. SAME reviewer `447d97f5`
must assess the corrected artifact SPEC then QUALITY before semantic acceptance.

Also release the two original, finite mode-selective effect probes, at most TWO
sequential source-guided native contexts reused within their own two modes:
Migrator artifact-only then exact-authorized execution; ReleaseEngineer planning-
only then exact-authorized local packaging. Record actual tools/context IDs;
canonical role files are source guidance, not claimed native registration.
No nested reviewers, factory or replacement product writer.

For Migrator use one explicitly owned disposable JSON fixture and an approved
`legacy_name` to `name` transformation, preserving its value and every unrelated
field/file; an existing conflicting `name` must refuse. Planning
must not mutate its destination. The later execution may apply only that exact
transformation via P03 original-preimage protection; verify postcondition and
stale-preimage refusal with real effects, not a mode label. For ReleaseEngineer
use one explicitly pinned synthetic Git commit and one new owned local archive:
planning creates no bundle, authorized execution runs actual `git archive`;
verify archive contents/digest and unchanged source/index/refs. No tags, pushes,
remote publication, database, service, deployment or customer data.

Each mode has its own immutable request/obligations and actual output/evidence,
not a mutable request that upgrades planning into execution. Only the declared
fixture destination/archive is granted to the corresponding actor; the original
caller owns records and checkpoints. Keep one writer per root and actual
quiescent handoffs. Every shell/product call/import uses verified synthetic
process roots; denied tools/permissions are explicit and cannot be bypassed.

Use actual final producer/consumer and SAME independent reviewer for these
artifact/effect boundaries. Do not fabricate reviewer or host/human corroboration
or execute a denied P12 record through this unrelated scope. Installed A09.5,
whole A17.5 and compound parents still need their remaining applicable evidence.

Own `agents/` after predecessor narrow fixes have landed; `skills/ta`, `da`, `sc`,
`dh`, `tq`, `full-engineering-pass`, `investigate`, `perfbench`, `devex-review` and their
domain references; one shared domain-result schema/helper and focused tests; related
specialist docs. Preserve existing agent entry names and specializations by default.
No merger/removal merely for size: an alias/mode needs per-method preservation proof.
Keep migration planner versus authorized executor distinct, bounded evidence lookup
versus synthesis distinct, and independent reviewers separate from implementers.

No installer, profile, host, review, Swarm, core work-map or generated-output ownership.
Use established predecessor contracts; do not create a second backlog or copied policy
parser. P12 later owns format-specific enrichment, not concurrent changes to these agents.
Report P09.md plus a 69-row `P09-agent-preservation.md` mapping each original role to
retained responsibilities, methods, changes/justified no-change, example and evidence.

### Installed-source closure after P10 integration (2026-09-24)

P10 passes its final independent review (`f460eb9f`) and is integrated in recovery
`68d32240`. The joined complete kit and an independent integration review are in
progress. This releases the installed coverage that this card left to the coordinator
and P10:

- A09.5: installed-source discovery, Windows filenames and interrupted domain handoff.
- A17.5: the remaining installed and final receiver coverage.

The original P09 owner implements one bounded, test-first unit on a new branch and
worktree from recovery `8754515a` or later, and records the exact base.

- Install the integrated tree only through P10's accepted surfaces: the native installer
  with the approved PowerShell 7, or `li-lifecycle`. Windows PowerShell 5.1 stays denied.
  Use synthetic homes and targets under short roots, export no `LINTEL_*` selectors, and
  read no real profile.
- Work from the installed location, not the repository:
  - discover the domain modules and their data CLIs;
  - produce and read domain results with Windows-safe filenames;
  - interrupt a domain handoff and show that the failed or missing mandatory result
    stays visible;
  - exercise the dispatch/receiver contracts and a cold-start example against the
    installed roles.
- Give each positive case a discriminating negative that must refuse rather than pass,
  such as a removed installed resource or an interrupted handoff.
- Reuse the existing consumer tests and the kit's joined installed methods rather than
  duplicating them. Label copied-source checks honestly.
- Change only tests and the report. A product defect returns as a finding for coordinator
  intake, not as a repair.
- Freeze the product and report for SAME `447d97f5`, which runs SPEC and then QUALITY if
  eligible. Send the handoff to the coordinator in parallel. Any helper follows the
  operator model directive.

If the joined P10 checks or the integration review change P10, the unit revalidates on
the corrected base. The completed finite attempts, N1 and the mode evidence are not rerun.

**Frozen unit and coordinator intake (2026-09-24).** The owner froze product `e6a84ce6`
(one commit on base `860da27c`, tree `ad5a801d`) and report-only child `0247a867`, whose
report has LF SHA-256 `e94d27d0…aa14c4d9`. The unit went to SAME `447d97f5` and the
coordinator in parallel, on the new branch `jokerman-microsoft-p09-installed-closure`.

The coordinator's data-only intake passes:

- The only changes are six test files and the report: the new
  `domain-installed-consumers.py`/`.sh` and `installed_consumer_checks.py`, plus small
  changes to `domain-result-handoff.py`, `domain-module-consumers.py` and
  `copilot-kit.py`. No product file changes.
- `copilot-kit.py`, P10's kit test, now calls the shared helpers for the two A23.4.g3
  methods, installed review controls and installed profile continuity. The moved code is
  equivalent: the test object becomes a parameter, the controls call gets an explicit
  `cwd`, and the profile bootstrap passes bundle and target as arguments with
  `--noprofile --norc`.

The owner reports both installed routes at 8/8 domain methods with no skips: the native
P10 install with PowerShell 7, and `li-lifecycle` scaffold with Copilot init/check. The
owner also reports product negatives, and a maximum path of 225 characters. For the
cold-start example, the owner ran one read-only native context (`cd1837f5`, under the
operator model directive) against the installed kit bodies. That is not native
registration or acceptance. At integration, the coordinator also runs the two affected
kit methods on the integrated tree. SAME447's SPEC and then eligible QUALITY follow.

## Leaves

| Leaf | Dependency | Required result |
|---|---|---|
| A09.1 | P05/P07/P08 | One domain handoff with work/profile/revision, decisions, controls/evidence and next owner |
| A09.2 | A09.1 | Agent-owned iteration/checkpoint/recovery replaces shell-shaped invented scheduler |
| A09.3 | A09.2 | Architecture/data methods have real failure mechanisms, worked choices and verification |
| A09.4 | A09.2 | Security/hosting/testing methods have applicable criteria, examples and false-positive boundaries |
| A09.5 | A09.3,4 | Installed source, Windows-safe artifacts, interrupted domain loop and failed mandatory domain work |
| A17.1 | Predecessors integrated | Every role gets exact inputs/decisions/responsibility/output/evidence and preservation row |
| A17.2 | A17.1,P06 | Portable role methods use actual host operations; native memory/resource intent retained under ADR0028 |
| A17.3 | A17.1 | Narrative arc planning and later critique work from a blank session without circular prerequisites |
| A17.4 | A17.1 | Distinct specialist knowledge retained, source-backed rules replace arbitrary vendor/retention rules |
| A17.5 | A09.5,A17.2,3,4 | Dispatch matches receiver, actual examples expose omissions, reviewer never repairs their own findings |

Read each original role before changing it. Repeated generic sections are not a
substitute for useful expertise. For architecture cover invariant versus topology,
failure isolation and tail-latency composition; for data include locking/rollback/replay/
late data/idempotency and lineage; for security distinguish implementation evidence,
policy applicability and unknowns; for operations cover state-compatible rollback,
actual SLO/capacity/cost sources; for tests cover consumer-specific compatibility,
baseline comparability, statistical uncertainty and actual runtime evidence.

Reuse public-source method insights only with provenance; never strip attribution or
copy restricted standards. Regulatory roles cite current authoritative sources without
claiming legal certification. Remove unsupported blanket assumptions, not the expertise.

## Verification and report

Add `tests/integration/domain-result-handoff.sh` and a bounded scenario corpus/check
under `tests/behavior/agent-contract-scenarios.sh`. Exercise producer/consumer and negative
result parsing; label documentary role checks as structural, not model behavior.
Run actual native scenarios where available only within user-authorized task scope.
Preserve existing domain tests with updated deliberate contracts; do not make numeric
averages hide a failed mandatory domain or claim a static grep is runtime acceptance.

Commit in coherent groups; report every leaf/role, exact checks and limitations. Request
independent spec/quality review via MasterSession. No nested writers, remote actions,
global model selection or source capability deletion.
