# P13 - Compact discovery first unit

**Status:** implementation candidate frozen; independent SPEC then QUALITY review pending.
**Builder:** original P13 owner, project session `bef15fa8-0d3a-4d84-841c-bb22b7f01fd6`.
**Date:** 2026-09-22.
**Selected work:** `work.json`, `spec.md`, `plan.md`, `interfaces.md`, `handoff.md` and the
complete `packages/P13.md`, specifically its released "Compact discovery first unit".
No original parent checkbox or whole-package acceptance is closed by this report.

## Authority and immutable product

Starting HEAD was exactly `4b98a4d88e8b58ed5b83d49459a076dfe6bac191`.
The supplied integration branch's ancestry included that authority. The worktree was clean
before edits; the branch was renamed to `jokerman-microsoft-universal-discovery` before
creating files. No WIP or other worker product was imported.

Product commit: **`2a4ba255f989f0a411bddaedc2738a6b6b7604d1`**.
Its sole parent is **`4b98a4d88e8b58ed5b83d49459a076dfe6bac191`**.
The commit contains exactly the eight product paths below. The report-only child carries
this file; its exact commit and report hash are supplied separately to MasterSession so
this report does not attempt to hash itself. Both commits use Conventional Commit titles
and the required Copilot trailer. Source commit identity is explicitly scoped to the
commit command; no personal/global Git configuration was changed.

SHA-256 values below identify **Git blob bytes**, not the checkout's possible CRLF projection.

| Product path | SHA-256 |
|---|---|
| `bin/li-catalog.py` | `e618756285c5f6732a96c6223afdd5627a736e4d5322eb490e46adbddd0b3cd8` |
| `install/upstream-sources.yaml` | `0167c2b530d53d3ae53a509102366b87c1ab677e875d29b1889c33028ea7ff5e` |
| `skills/catalog/SKILL.md` | `561e3591f377eea696ef74c7ac83dbebff1712089c12d70fc916a4af4e9960b9` |
| `skills/catalog/references/metadata.md` | `4b1bdbebeb5d0f2f67b26ca6e77ae829af7ea819cdc6f9af6467dea6a7b3fc35` |
| `skills/help/SKILL.md` | `c5a6d10d1638a865186b5b443d1dd8da6b20d240df63a09ee651d1f7dcf6f794` |
| `skills/skill-router/SKILL.md` | `df3d68dbb4946dad257abb3c0cc90e6152ed63d6baa985ca804e2128c38e9c60` |
| `tests/unit/catalog-metadata.py` | `11e625b9f0a7251bbeeb04c65d7f6b17d7046cb701c0bd74f9186b2e9793e0d3` |
| `tests/unit/catalog-metadata.sh` | `bb2b3e9dd030f12e8dfaf52082724a9f32d0605c424d628a3fe75ad9a94bd5a3` |

## Implemented behavior

The existing generator now exposes a documented `--json` read/query mode, not a second
inventory or routing engine. It reads only bounded opening frontmatter for discovery,
uses the accepted P06 registry reader for surface aliases, and reuses the existing strict
`envelope_contract.load_text` data loader for YAML. All executable readers remain bound
to the invoked trusted helper. An explicit absolute `--source-root` changes data only;
target-provided Python decoys are not imported.

Queries/searches, exact names, families, kind/category/voice and surface filters are
literal data. They cannot trigger generation, regex/glob evaluation, shell execution,
activation, profile binding or target writes. A malformed requested-kind inventory cannot
be hidden by an unmatched query. Missing/empty/malformed data, ambiguous identities or
aliases, invalid filters and missing helpers/dependencies fail before JSON publication.
Valid zero matches remain distinguishable from invalid input.

Catalog/help/router now query this helper first. Help no longer independently globs and
parses the corpus or advertises fixed hook counts as live state. Router shortlists at most
three entries before reading only selected bodies, removes personal telemetry access and
the unsupported offline/speed claims, and names actual host operations or explicit
canonical/serial/manual fallbacks rather than fictional tool names.

The current inventory has **127 skills, 69 agent roles and 46 retained skill aliases**.
Stable kind/name identities, source-relative paths, full descriptions and alias migration
notes remain visible. All maturity values are explicitly unknown. A frontmatter `full`
hint does not promote `TEMPLATE ONLY` to a completed format, or prove host execution.
Synthetic future-looking fields are not treated as an accepted capability contract.

The three caller frontmatters are unchanged in their canonical LF text representation.
The generated catalog was **not edited or regenerated**. Ordinary generation and
`--check` retain their default behavior, messages, platform stdout line endings and exact
canonical rendered bytes. Generation additionally refuses empty/repeated required fields,
unterminated quotes, collection/block values and bounded-header errors before opening
the output; it retains its existing three-scalar rendering contract.

Canonical rendered catalog SHA-256:
`adc2232421ac2b60b44aba37fa39d6a439c199ba936f419c159f1f2bc34af181`.

Local byte-size observation on this exact tree: all source prompt files total 1,409,056
bytes; all compact metadata JSON totals 120,078 bytes; exact `match` alias selection totals
864 bytes. These are file/output byte measurements, not token estimates, model accuracy,
latency benchmarks or observed client behavior.

## Provenance preservation

Read and retained `reports/A20-provenance.md`, the coordinator's corrections, actual
design-dna attribution/notices and `docs/provenance.md`. One concrete discrepancy remained
in the registry's field comments: they still said the installer expanded `install_path`
and ran `post_install`. The actual declaration-only listing in `install/install.sh`
does neither. Only those comments now identify historical recipe metadata.

Parsed registry data is exactly equal to the authority version. All eight reference
entries and both adapted-material records remain; their local paths, attribution and
nonempty notice files resolve. Historical import commits remain null/unknown, separate
from pinned later comparison revisions. No license/notice text, adaptation body, historical
commit, comparison claim or installer behavior was rewritten. No new upstream content was
copied or executed, no declared upstream recipe ran, and no private material was exported.

These are **source metadata/notice checks**, not selected-package distribution acceptance.

## Execution boundary and retained evidence

All command and product-import processes used inspected allowlisted synthetic
HOME/USERPROFILE, app-data, temporary, Lintel, target and fixture roots. The final launcher
uses an environment allowlist; inherited credential/profile/policy redirects are not
passed through. It verifies derived paths and fixture Git ceilings before execution.
Tests add their own synthetic per-suite roots before importing the product. The outer
exact-source comparison process receives the same preflight, not just its shell children.

Fixture-only Git configuration isolation does not apply to source Git identity/EOL
operations. Source checks retain the source's actual system/local EOL handling or compare
immutable Git bytes and independently captured raw source seals. No source normalization,
global setting, policy change, real-home incident inspection/recovery, network call,
authenticated GitHub operation, push, PR or other-worktree edit occurred.

Runtime: Windows, Python **3.11.9**, existing PyYAML **6.0.3**, with actual Git Bash
entrypoint runs. No dependency was installed/restored. Python 3.9 grammar was checked;
an actual Python 3.9 runtime was not exercised.

Session-local evidence is retained under `files/p13/logs/` in this session's artifact
directory. Each command has stdout, stderr, argv, actual exit, execution location and
source-before/source-after seals. `baseline.json`, `product-freeze.json`, the final
report freeze record, `p13-env.ps1`, `p13-run.py`, `p13-probe.py` and `p13-final-check.py`
retain the setup and exact comparison procedure. These local paths are not installed
product resources or committed personal configuration.

## Checks and outcomes

| Actual check | Outcome and evidence |
|---|---|
| Starting authority, integration ancestry, branch and clean source seal | PASS; `baseline.json` |
| `python bin/li-work-artifacts.py --repo . --map .claude/plans/universal-implementation/work.json` | Exit 0; selected APPROVED work map, `work-map.*` |
| Initial canonical strict-loader probe | Exit 0; all 127 skill and 69 role frontmatters parse; `canonical-parser-probe.*` |
| Existing `bash tests/unit/catalog-generator.sh` | Exit 0 before and after implementation; deterministic Unicode/pipe/source-link/invalid-field behavior preserved |
| Existing `bash tests/shape/catalog-regenerates-clean.sh` | Exit 0; committed catalog remains unchanged |
| `bash tests/unit/catalog-metadata.sh` on frozen product commit | **24 tests PASS**, zero skips; `metadata-committed.*` |
| `python bin/li-catalog.py --check` on frozen product commit | Exit 0; exact existing success message, `catalog-committed.*` |
| Existing `python tests/unit/client-capabilities.py` | **16 tests PASS**, zero skips; `retained-client-registry.*` |
| Existing `bash tests/shape/deprecated-aliases-resolve.sh` | Exit 0; current alias destinations preserved, `alias-shape.*` |
| Existing `bash tests/unit/frontmatter-boundaries.sh` | Exit 0; body fields cannot satisfy missing metadata, `frontmatter-boundaries.*` |
| Exact authority/current generator comparison | PASS on canonical source and four varied synthetic headers (layers/order/quotes/Unicode/truncation/pipes/BOM/CRLF); `preservation-committed.*` |
| Frontmatter and registry preservation comparison | PASS; three caller headers retained, parsed provenance registry unchanged; all unowned original source paths retain raw hashes |
| Python 3.9 grammar check | PASS for generator and new Python tests; syntax evidence only |
| Source/staged `git diff --check`, exact staged scope, commit parent/path/hash and clean-state checks | Exit 0; exactly eight product paths, no source-byte change from commit, clean product freeze |

The 24 methods include canonical inventory/aliases, literal hostile queries, composed
filters, exact surface alias normalization, unknown/template maturity, stable identity
and differing name/folder paths, BOM/CRLF and invalid body bytes, malformed/oversized
frontmatter, duplicate/conflicting aliases, missing/invalid registries and helpers,
read-only source/target snapshots, target executable decoys, and unchanged default
generation/check without site packages. PyYAML absence is deliberately tested with
`-I -S`: metadata fails visibly, while ordinary generation/check remains dependency-free.
Reparse refusal uses an injected file-attribute observation; it is not a native
symlink/junction acceptance claim. Caller tests are structural instruction checks, not
proof of a model following those instructions.

### Retained failures and corrections

`metadata-red.*` is retained with actual exit 1: 20 initial tests, 12 failing assertions
and eight errors before the metadata operation existed. One assertion also incorrectly
expected LF from the old Windows `--check` stdout; it was corrected to the observed
platform line ending, **without changing or normalizing the product's legacy output**.
The later metadata-only JSON LF contract is separately asserted.

`metadata-first-green.*` records 20 passing tests; `metadata-expanded.*`,
`metadata-final.*` and `metadata-committed.*` record the expanded 24-method suite.
No failed run is relabeled as passing. Error-path subprocesses within passing tests
are expected refusals with preservation assertions, not silently suppressed failures.

## Scope mapping, self-review and remaining gates

This implements the released source-discovery portion of A19.1/A19.2/A19.4 and supports
A18.3's metadata-before-selected-body behavior. It does not close their original wider
consumer/integration acceptance. The scoped A20.1/A20.2 correction and source checks retain
the coordinator's earlier evidence without claiming distributed selection acceptance.

Implementer self-review only: **open P1: 0; P2: 0; P3: 0** in this first-unit candidate.
The self-review tightened case-insensitive alias collision and duplicate-record refusal
and verified no unsupported maturity/tool claims. There is no independent-review verdict,
ship clearance, full suite/CI result, actual model/client scenario or enterprise-policy
enforcement claim. Independent SPEC must run first; QUALITY follows only if eligible.

MasterSession/P10 distribution seam: metadata currently needs the existing trusted
`client_capabilities.py` and `envelope_contract.py` readers, P06 registry, alias data and
PyYAML for canonical YAML. This unit does not change their installed-resource preflight,
wrapper selection or installation prerequisites. Retain the documented snapshot fallback;
separately verify the selected distribution and notices when that scope is released.

Still unreleased/open: final capability manifest/selection, optional installation and
wrapper/resource closure, status/welcome/core-lifecycle integration, final 126-row
preservation map, mature-capability/version decisions, generated fan-in, installed/default
acceptance, actual host execution and independent final integration review. P08 state/
classifier, P09 domain/role bodies, P10 installer, P11 browser/design, P12 formats, shared
profiles/review/path/host implementations and shared plans/memory/ADRs were not changed.
All nine canonical phases remain unchanged; resume remains a utility.

**Next action:** MasterSession verifies the frozen product/report identity and dispatches
a separate independent SPEC review, then eligible QUALITY. Preserve this original P13 owner
for corrections and later explicitly released selection/version/integration work.

**Position:** BUILD candidate frozen -> independent REVIEW; no SHIP/integration performed.

---

## F01/F02 identity repair - 2026-09-22

**Current status:** bounded repair implemented and frozen; independent recheck pending.
The preceding 198-line report is preserved as historical builder evidence. Its zero-finding
self-review did not clear the later independent rejection. The complete 189-line review at
`466a454b685d35d78671bd917ea50e80eed9cc1a`, path
`.claude/plans/universal-implementation/reviews/P13-discovery-2a4ba25.md`, was read in full
and verified against Git-byte SHA-256
`f2231fd8c9f398c6dbaf68187e3b52babfa69ec1ec88989e79078cecba8973be`.
That review is SPEC FAIL, P1: 0 / P2: 2 / P3: 0; QUALITY has not started.
The coordinator preserved it as `f7a09a3`; this repair does not rewrite or replace it.

Authority is the full P13 card at **`c2f485c7a6a749f9683703ecf242edc481f231aa`**,
specifically "F01/F02 unambiguous catalog identities", and the coordinator's explicit
instruction to implement both findings. The original owner remains app-surfaced session
`c4e9de1c-1c81-4897-ba82-019d58b635d7`; `bef15fa8` in the earlier report is the distinct
project metadata identity, not the messaging route. The recovery coordinator is
`88aecc43-40f9-41d4-8947-6c2fb0a55481`.

### Immutable repair scope

Starting repair HEAD was the clean rejected report-only checkpoint
`e20588d5cfd914cc1bc861143fc9d1a4b39bcaca`. No integration tree, new producer, review file
or WIP was imported. The released card and independent report were read from their exact
Git objects into owned session evidence.

Repair product: **`b1d4caf34227d147a428050debf0f81827e43938`**.
Sole parent: **`e20588d5cfd914cc1bc861143fc9d1a4b39bcaca`**.
Exactly three product paths changed; modes are unchanged. The appended report is a
separate child commit, whose exact identity/hash is sent with the handoff.

| Repair product path | Git-blob SHA-256 |
|---|---|
| `bin/li-catalog.py` | `7c78c69ad5152da96068e51457d736c900f59ad0b10ab59250481bc100a0269f` |
| `skills/catalog/references/metadata.md` | `a784f80131059561af853bd6ba2e2d612bdd13b2082883f27ca5892ec01601da` |
| `tests/unit/catalog-metadata.py` | `269fcb0d671ea86b929e64ac76f5d4aa43fd6c82ba9de0c9577a7b1722b8ff32` |

No corpus/frontmatter, caller skill body, registry, alias source, provenance data/notice,
installer, profile, host/review/path implementation, generated output, version, shared
plan/memory/ADR or second-unit descriptor was changed.

### Findings and changes

**F01 - agent identity ambiguity:** the former skill-only alias ownership routine is now
shared by skill and agent entries. It collects every canonical name in the selected
namespace before validating aliases, refuses an alias matching any canonical name
(including itself), and rejects an alias already owned by another entry, using casefold
comparison. The full requested-kind inventory is validated before all filters or JSON
publication. Central skill-registry precedence remains skill-only and unchanged.
Skill and agent namespaces stay separate: the same spelling in different kinds is valid
and retains distinct `skill:` and `agent:` identities.

**F02 - ordinary generation ambiguity:** generation now uses the same small stdlib
canonical-identity registration check as metadata. Duplicate/case-equivalent skill names
across folders or layers fail before output is opened. `--check` also fails before
comparing/certifying even an already-generated matching ambiguous catalog. This corrects
the explicitly released **retained-baseline acceptance gap**, not a new P13 regression.
It adds no YAML/parser prerequisite to the ordinary generation/check path.

Seven focused methods were added without changing any of the original 24 test methods.
They cover cross-record alias/name in both input orders, identical and case-equivalent
alias/alias, self-alias, retained canonical-agent duplicate refusal, distinct-kind positive
identity/alias controls, valid-catalog preservation on duplicate generation/check, and
refusal to certify matching pre-generated ambiguity. Agent negatives cover both `agent`
and `all`, unfiltered listing, exact-name filters and unmatched queries. F02 CLI cases run
with `-I -S`; they also cover direct `generate` refusal and names across different layers.

### RED and preserved evidence

Before the product repair, `f01f02-red.*` ran the seven new methods against the unchanged
candidate: **exit 1, 50 failed assertions, no errors or skips**. Five methods exposed
failures; the duplicate-agent and distinct-kind positive controls already passed.
F01 produced successful ambiguous inventories and successful zero-match responses.
F02 produced a new ambiguous catalog, and exact seeded ambiguous catalogs received the
old success message from `--check`. These synthetic outcomes remain in the retained log.
The stale-good-catalog check returned its old stale-catalog diagnostic rather than the
required ambiguity refusal; that distinction is also retained.

No failed aggregate is relabeled. The original first-unit RED and all independent
rejection evidence remain history. All generation/mutation probes were confined to
owned fixtures; the real generated catalog was never regenerated or edited.

### Checks actually run

| Check | Outcome and retained log prefix |
|---|---|
| Exact release/review/base intake | Exit 0; clean `e20588d`, full 189-line report/hash; `f01f02-intake.*` |
| New seven-method RED on rejected source | Exit 1; 50 failed assertions; `f01f02-red.*` |
| Git Bash metadata entry after repair | Exit 0; **31 tests**, zero skips; `f01f02-metadata-green.*` |
| Same Git Bash metadata entry on frozen `b1d4caf` | Exit 0; **31 tests**, zero skips; `f01f02-metadata-committed.*` |
| Existing `tests/unit/client-capabilities.py` | Exit 0; **16 tests**, zero skips; `f01f02-registry.*` |
| Existing catalog unit, catalog drift shape and alias shape | All exit 0; `f01f02-catalog-unit.*`, `f01f02-catalog-shape.*`, `f01f02-alias-shape.*` |
| Existing frontmatter boundary test | Exit 0; `f01f02-frontmatter.*` |
| `bin/li-catalog.py --check` on frozen repair | Exit 0; unchanged success message/platform stdout ending; `f01f02-catalog-committed.*` |
| Exact prior/current canonical metadata and 18 kind/filter comparisons | Exit 0; equal JSON bytes and results; `f01f02-preservation-committed.*` |
| Authority renderer and four varied valid fixture headers | Exit 0; byte-identical Markdown; `f01f02-authority-render.*` |
| Original 24 test-method AST and Python 3.9 grammar comparisons | Exit 0; original methods exact, seven additions; `f01f02-preservation-committed.*` |
| Working/staged/committed diff checks and immutable product freeze | All exit 0; three paths/sole parent/blob hashes/clean state; `f01f02-product-freeze.json` and `f01f02-*-check.*` |

Canonical render SHA-256 remains
`adc2232421ac2b60b44aba37fa39d6a439c199ba936f419c159f1f2bc34af181`.
Canonical compact JSON remains byte-identical to the rejected candidate for valid input:
120,078 bytes, 127 skills, 69 roles, 46 retained aliases and unknown maturity. All 196
canonical prompt files, not just their headers, retain their pre-repair raw bytes.
The original 24 test-method ASTs are exact; the shared readers/registry and original
catalog/alias tests were not changed.

Every command and outer product comparison used the inspected synthetic environment
allowlist, explicit HOME/USERPROFILE/app/temp/Lintel/target roots and fixture Git ceilings.
Fixture-only configuration stayed separate from source Git/EOL checks; retained command
seals report zero source changes during execution. Actual Windows Python 3.11.9,
existing PyYAML 6.0.3 and Git Bash were used, with no package installation, security/policy
bypass, private-home/global-setting change, source normalization, network/publication,
other-worktree edit or broad cleanup. Python 3.9 validation remains grammar-only.

### Self-review, lesson and remaining gates

Implementer self-review found no additional P1/P2/P3 issue in this bounded repair.
F01 and F02 are **addressed in builder evidence, not independently closed**. No QUALITY
verdict or first-unit acceptance is claimed; the same independent reviewer
`4865e4f7-240b-4721-a64d-ce6df9a75a9c` must recheck complete first-unit SPEC, then perform
the first eligible QUALITY pass. Structural/canonical/synthetic evidence remains distinct
from live client/model behavior and installed distribution acceptance.

Dated preventive lesson, retained here within ownership rather than editing shared
memory: validate identity ownership across **every supported kind and publication path**
before filtering or output. A skill-only negative test cannot cover agent aliases, and
metadata refusal cannot prove ordinary generation/check refusal. Pair collision negatives
with separate-namespace positives and preserved-good-output checks.

The conditional A+B card at `bdfd127` is still gated on actual first-unit SPEC/QUALITY
PASS. Once the coordinator supplies its exact accepted ref, the already approved
additive/full-bundle second unit may proceed without another shape decision. Actual
installed alias/config/selection/provenance/notice closure remains coordinator/P10-owned;
no version, status/welcome, lifecycle or generated-output work starts from this repair.

**Next action:** send the immutable repair product and report-only child to recovery for
the SAME reviewer's complete SPEC recheck and eligible QUALITY; retain this original owner.

**Position:** BUILD repair frozen -> independent REVIEW; A+B and SHIP remain gated.

---

## A+B source selection and preservation - 2026-09-22

**Current status:** approved second unit implemented and frozen; independent A+B SPEC
then eligible QUALITY pending. The preceding 341-line report prefix remains unchanged.
The first-unit prerequisite is now independently accepted, not assumed from builder
results: review **`09a3c6ec3c3fa2c3232e921f52accc9262088e8b`**, sole parent
`204ea7253b18fb1849fa6de94e1b283b098039b5`, records complete first-unit SPEC followed
by its first whole QUALITY, with zero open findings and F01/F02 closed. The entire
136-line report was read and its Git-byte SHA-256 verified:
`695e934d0b393e1c8c3a34644db0bb67964344d62830527de40f7c071fb3de58`.
That acceptance is retained; this is not a request to re-review unchanged first-unit work.

The second-unit authority is **`bdfd1274b47fb54687076ef89082fddf2ceda3b5`**,
the full P13 card's "Source selection and preservation second unit", plus the recovery
coordinator's explicit start instruction after verifying the prerequisite. Its selected
direction is additive source projection with full default access, not pruning/relocation.
Original owner: `c4e9de1c-1c81-4897-ba82-019d58b635d7`. Coordinator:
`88aecc43-40f9-41d4-8947-6c2fb0a55481`. Same independent reviewer:
`4865e4f7-240b-4721-a64d-ce6df9a75a9c`.

### Frozen product and exact scope

Product **`72253ed82e92cbe085a8cd40cb950195e210c4db`** has sole parent
**`204ea7253b18fb1849fa6de94e1b283b098039b5`**, the clean accepted source/report
checkpoint. No WIP, new producer or other worktree was imported or edited. The independent
acceptance and release were inspected from exact Git objects, not merged into the worker.

The product contains exactly these seven paths. Hashes identify Git blob bytes; the
report-only child supplies this append and is handed off with its own exact hash.

| Product path | Git-blob SHA-256 |
|---|---|
| `bin/li-catalog.py` | `7d0475f7400f32deb1e1bdafbc240575af3836755017d65c20af8614c988b32d` |
| `lib/capability-selections.json` | `ff04d77d587f4633fa75c62f659b1b5ae1ca439e7503f72577ef034fc45c2db0` |
| `skills/catalog/references/metadata.md` | `871fe67b6bab4e76d48622eafaaef55cf18ddf0c08f4352ad61018c31767f87d` |
| `skills/catalog/references/selections.md` | `3978096003343f3db08f22287df663c20c2b1f193c26c7b084b5baeb4da8d3f5` |
| `tests/unit/catalog-selection.py` | `443e5e3c7d75dfeb94363c044805fc62043d4ce3179bf4a80f5cdbde11269186` |
| `tests/unit/catalog-selection.sh` | `cb1146366fd7f63e4e103638cb661ef53c1202bf4b3729657401d146b62de017` |
| `reports/P13-skills-preservation.md` (in this initiative) | `a4f7ed86611ced7a6e71e4223580dafe107840a15df13d1147f51506d55d6965` |

The new shell test has executable mode; existing product modes are unchanged. No
canonical skill/agent body or frontmatter, accepted metadata test, alias/provenance
registry, notice, installer/resource list, shared schema/parser/provider, version,
generated file, status/welcome/lifecycle instruction, shared plan/memory or ADR changed.

### Delivered source projection

The existing `bin/li-catalog.py` adds explicit read-only `--json --list-selections` and
repeatable literal `--json --selection=<id>`. It reuses the same canonical inventory,
case-insensitive identity/alias checks, literal matcher, strict data loader and P06
surface reader. No second skill inventory, YAML/profile parser, router, installer,
scheduler or actor-dispatch mechanism was created.

One descriptor binds canonical member IDs, source, selection dependencies, required
source resources/provenance IDs, inputs, outputs, example pointers and limitations.
Unknown fields, malformed/empty members, duplicates, unknown IDs, dependency cycles,
unsafe/missing resources, missing/empty notices, omitted known provenance obligations,
alias conflicts and stale stage evidence fail visibly before output. Paths require exact
source spelling and source containment; linked/reparse and hidden/runtime paths refuse.
This is source resource closure, not an inferred complete runtime import graph.

The shared `core` projection retains all nine phase entries plus cycle, resume utility and
compliance-gate, with pointers to the existing P05/P06/P07 and security contracts. It is
included even if a requested selection does not redundantly name it as a dependency.
Those contracts are referenced, not reinterpreted or executed: no profile is bound or
switched, no required policy is relaxed, and no review/host permission is granted.

`demo-script` selects the three accepted P09 role methods. Its metadata preserves
DemoNarrativeArc Plan and Critique modes, DemoNarratorJunior's consumption of the initial
arc, and SlideNarrationCritic's later findings-only role. Separate independent critique
still needs an actual separate actor. The authorized caller persists artifacts; queries
do not write a script, generate PPT, register roles or invoke a model.

Closure is deterministic and deduplicated, with every selection/member/resource inclusion
reason. Query/kind filters affect displayed entries, not required closure. Source
definitions retain input/output/examples in the projection. No prompt body, example body
or notice text is emitted; default discovery still reads bounded frontmatter only.

General maturity remains `unknown`. Three additional source-stage declarations bind the
current PDF/Visio/XLSX `TEMPLATE ONLY` descriptions by exact path, literal quote and SHA-256
of the full parsed UTF-8 description. Missing/changed evidence refuses; no `mature`,
`implemented` or `verified` state is accepted. These are source declarations, not readiness
derived from `full` hints, historic audit quality, role content acceptance or future work.

### Preservation and useful-method map

The 256-line [preservation map](P13-skills-preservation.md) joins the exact 46+80 original
audit records, retaining each recommendation, current source, useful method/output,
responsible package/leaf, change or grounded retention rationale, worked/recheck example
and explicit evidence limit. It adds Swarm separately, preserving its historical origin
and the existing 76-path map rather than replacing that work with a one-file summary.
The existing 69-role map and accepted P09 role modes remain separate retained evidence.

An immutable Git-byte comparison establishes **86 unchanged and 40 previously changed**
original skill files at `204ea725`, plus Swarm. All 126 current original paths/names
remain. Every map U/C label matches that comparison; exact current source hashes are
retained in `ab-preservation-baseline.*`. This unit does not claim it freshly reread
every unchanged full body: the original full-reading audit and exact identity are its
declared basis, with current method headings, accepted component reports and directly
needed bodies inspected. Source equality is not functional completion.

All 46 existing aliases are explicitly represented, including source-grounded argument
mappings for context/role modes and all 35 module sub-capabilities. The original alias
registry is unchanged; discovery does not fabricate executable rewrites or remove names
after a recorded date. Map tests verify exact keys/actions, pointers and alias/sub-method
presence, not semantic success or a whole-P13 completion percentage.

Canonical valid metadata is byte-identical to accepted first-unit output: 127 skills,
69 agents, 46 aliases, 120,078 compact JSON bytes. All **196 prompt files and headers**
are byte-unchanged. The actual pilot has 15 closure members (12 shared skill entries and
three roles), 21 source-resource paths, and 16,270 JSON bytes; its agent-only display has
three entries and 8,441 JSON bytes, with required core closure still present. These are
local byte/count observations, not measured token savings or model performance.

### Actual verification and retained failures

All commands and outer product imports/comparisons used the inspected synthetic environment
allowlist, HOME/USERPROFILE/AppData/temp/Lintel/target roots and fixture Git ceilings.
Fixture-only configuration did not determine source Git/EOL identity. Each retained run
records argv, actual exit, stdout/stderr and before/after source seals; every verification
run reports zero source changes. Owned temporary directories were individually managed by
the fixture framework, without broad cleanup or any real-home inspection/recovery.

| Check actually run | Outcome and retained evidence |
|---|---|
| Exact accepted-review/release/start intake | Exit 0; 136 review lines/hash, approved release and clean `204ea725`; `ab-intake.*` |
| New selection suite before implementation | Exit 1; 15 methods, five failures/four errors; `ab-selection-red.*` |
| Final selection suite through actual Git Bash on frozen product | Exit 0; **19 methods**, zero skips; `ab-selection-committed.*` |
| Entire accepted metadata/identity suite on frozen product | Exit 0; **31 methods**, zero skips; original test file exact; `ab-metadata-committed.*` |
| Existing P06 registry suite | Exit 0; **16 methods**, zero skips; `ab-registry-retained.*` |
| Existing catalog unit, catalog drift shape and alias shape | Each exit 0; `ab-catalog-unit.*`, `ab-catalog-shape.*`, `ab-alias-shape.*` |
| Actual frozen-source `--check` | Exit 0; `ab-catalog-committed.*`; source catalog was not regenerated |
| Prior/current canonical metadata and 18 kind/filter comparisons | Exit 0; exact equality; `ab-preservation-committed.*` |
| Original/current Markdown and four varied valid source fixtures | Exit 0; exact bytes; `ab-authority-render.*` |
| Original126/Swarm map, 86 U/40 C, exact unchanged prompt hashes and alias coverage | Exit 0; private immutable comparison plus source tests, not model execution |
| Python 3.9 grammar | PASS for changed/new Python; actual runtime is 3.11.9, not a real 3.9 run |
| Working/staged/committed diff and exact product freeze | Exit 0; seven paths, sole parent and hashes; `ab-product-freeze.json`, `ab-committed-diff-check.*` |

Canonical Markdown SHA-256 remains
`adc2232421ac2b60b44aba37fa39d6a439c199ba936f419c159f1f2bc34af181`.
No default generator, `--check` success/stale message, platform stdout ending or no-site
dependency-floor behavior was intentionally changed. Missing selection data does not
affect ordinary metadata; it is read only for explicit selection/list operations.
Existing PyYAML 6.0.3 was used; no dependency was installed/restored.

New methods cover the actual demo selection and stage records, deterministic union/reasons,
shared-core preservation, filter/closure separation, malformed fields and literals,
cycles, absent members/resources/notices, known-component provenance omission, stale
description evidence, source/target executable decoys, no prompt-body exposure or writes,
and the map's exact original keys/aliases/pointers. Reparse refusal remains an injected
attribute observation, not native symlink/junction acceptance. Current example headings
are checked as source references; no demo was generated/rehearsed or independently
critiqued by this unit.

Retained intermediate failures are explicit:

- `ab-selection-partial.*`: 14 of 15 methods passed; the missing not-yet-written map
  produced the remaining error.
- `ab-selection-first-green.*`: despite the prospective log name, actual exit was 1,
  with one failed structural map assertion in 17 methods. An arbitrary greater-than-ten
  character condition incorrectly rejected valid short owner IDs; it was removed, not
  satisfied by padding the map.
- `ab-selection-expanded.*`: actual exit 1 in 19 methods because the structural oracle
  demanded the English token `not`, rejecting the explicit limit "No fresh-clone workflow
  was executed". The language proxy was removed; exact inventory, substantive fields,
  owner IDs, links and alias tests remain. A size/word check cannot prove useful content.
- Later `ab-selection-final.*`, `ab-final-selection.*` and the committed run pass.
  Earlier failed outputs/exits remain retained and are not relabeled.

All evidence is under this session's `files/p13/logs/ab-*`. `p13-ab-check.py` records
the exact data-only/source comparisons under the same preflight as product calls.
Historic original/repair logs and reports remain intact.

### Scope limits and next gate

Implementer self-review: no additional open P1/P2/P3 finding identified in this bounded
A+B source candidate. This is **not independent A+B acceptance**. The same reviewer must
check complete released A+B SPEC, then eligible QUALITY, without discarding the accepted
unchanged first-unit evidence.

Default full access is preserved; no capability is physically pruned, externally moved
or activated. No installer/receipt/native-wrapper/source-resource preflight changed.
For the later coordinator/P10 join, actual installed sources must carry the specific
alias data, new selection descriptor/reference and existing source/provenance/notice
obligations through the existing engine, not whole configuration/install trees.
The known missing `config/aliases.yaml` seam remains unmodified. Provenance checks here
read the existing registry and actual source notices; they do not establish packaged
notice delivery or legal sufficiency.

No P08 classifier/state, P09 domain binding/role body, P10 installer, P11 browser/design,
P12 format, shared profile/review/path/host code, status/welcome/core lifecycle procedure,
version or common generated output was changed. No original parent/leaf or final
126-row integrated acceptance is marked complete. Real installed/client/model behavior,
policy enforcement, final version reconciliation and generated fan-in remain their
separately authorized gates. No remote publication, paid benchmark, credential/policy
change or private-data export occurred.

**Next action:** coordinator intake of exact product/report/map hashes, then the SAME
reviewer `4865e4f7` for A+B SPEC followed by eligible QUALITY. Preserve this original
owner for scoped repairs and later release work.

**Position:** BUILD A+B frozen -> independent REVIEW; installed closure and SHIP remain separate.

## Remaining discovery and authoring consumers (2026-09-22)

### Authority, identity and freeze

This is the original P13 owner's released A19.1/A19.4 consumer unit, not another
audit, installer or general runtime. Coordinator release
`a5d77f93dc40f2968bff7fd6aa3eacd5ccaee92e` appended the complete "Remaining discovery
and authoring consumers" card and explicitly assigned the welcome/status consumer
bodies to P13. P06/P08/P07 retain their underlying providers and runtime ownership.
The complete 214-line card was read before edits.

- Original owner / app routing identity: `c4e9de1c-1c81-4897-ba82-019d58b635d7`.
- Recovery coordinator: `88aecc43-40f9-41d4-8947-6c2fb0a55481`.
- Same independent reviewer, still required: `4865e4f7-240b-4721-a64d-ce6df9a75a9c`.
- Accepted source dependency: `895bb35`, resource closure `ad605de`, then release `a5d77f9`.
  A normal fast-forward from `cc3de22` preserved the existing histories.
- **Frozen product:** `7884ddd0e4880d09c6cf3025822c6f5a0ed0c161`.
- **Sole product parent:** `a5d77f93dc40f2968bff7fd6aa3eacd5ccaee92e`.
- Product commit: `fix(discovery): wire safe metadata-first consumers`, with the required
  Copilot co-author trailer. Product bytes did not change during commit.
- This append is the report-only child; its exact commit/parent/report-blob hash are
  retained in `ux-report-freeze.json` and supplied with coordinator intake.

The accepted first-unit `b1d4caf` / `204ea725` and A+B `72253ed` / `cc3de22` remain
unchanged history. The preceding A+B review-pending text records that earlier freeze;
independent `7bf3f253` and integration `895bb35` subsequently accepted it. No old report
or failed evidence was rewritten to manufacture a later result.

### Exact product scope and Git-byte hashes

Only these twelve paths differ from the released parent. Hashes below are SHA-256
of committed Git blobs, not Windows checkout EOL-dependent hashes.

| Path | SHA-256 |
|---|---|
| `scaffolding/01-foundation/TEMPLATE-skill.md` | `a8caf187f751cef57f75989fc3e9040d94b0c514e4236d86000b067ae4502555` |
| `skills/catalog/SKILL.md` | `bbe53325f95fee7c3c199f8c62c8243141eb26eb170398dc7675f091588e0550` |
| `skills/catalog/references/consumer-checks.md` | `bfe6f96f0cb9c5d63d6a296df74b36cad9f2e689c293024f4a51822a355b98af` |
| `skills/eval/SKILL.md` | `196f98591454dedc342fdcc611416115b8b9000eb5279cfec6552daf541f3b45` |
| `skills/help/SKILL.md` | `6da84a778fd2347c7b03e19606503323cbb7310498943f63b267e97d767af279` |
| `skills/skill-router/SKILL.md` | `7ef8fdc7b0400104488d6e0b98d5a73b858d9824d1e62724b87cf047ce2dccd6` |
| `skills/skillify/SKILL.md` | `1e0f3cc1ce967ab7014da4f44036761f6115021634173de9d067b17502308253` |
| `skills/status/SKILL.md` | `318dae193a79a32b9bdd47ecae6b62df93b12bb2a871226bd279707a7d89cbf9` |
| `skills/uniformity/SKILL.md` | `c1deccf3eb9eeb878240be1d3bed9335dea3d1b4a41dba21b6a8bad65ac183f3` |
| `skills/welcome/SKILL.md` | `05bbeec15b632c577934a67d3a805e4d51f6e1a406b486e54aad8851eed73770` |
| `tests/unit/catalog-consumers.py` | `5a9392c1d0c06cd908321a5816054fa8ef9c2a88f0560fd49ddf3348c47ea216` |
| `tests/unit/catalog-consumers.sh` | `de68784b1b8131d0499b99120c2f93fec45382c3fa42d5340a1f754d66c35ec4` |

The canonical inventory remains **127 skills, 69 roles and 46 skill aliases**.
All 196 opening headers match the released source; no role body, alias definition,
descriptor, provider, installer, version or generated output was edited.
The ordinary catalog still passes the exact existing `--check` message/byte contract.
Canonical JSON still reports `executed: false`, `source-metadata` evidence and unknown
maturity for every entry. This is source evidence, not host observation.

Additional unchanged Git-byte identities:

| Protected source | SHA-256 |
|---|---|
| `bin/li-catalog.py` | `7d0475f7400f32deb1e1bdafbc240575af3836755017d65c20af8614c988b32d` |
| `lib/capability-selections.json` | `ff04d77d587f4633fa75c62f659b1b5ae1ca439e7503f72577ef034fc45c2db0` |
| `config/aliases.yaml` | `557f0d35ae9b43059d7b2c991b3fa22cc3829378e20a9b0c4ff7d1d8a6b6a799` |
| `reports/P13-skills-preservation.md` (relative to this plan) | `a4f7ed86611ced7a6e71e4223580dafe107840a15df13d1147f51506d55d6965` |
| `skills/CATALOG.md` | `9d8fa4805c5997c3410c4d671b76d63e584c574fff5b43942350d78d70a0bce5` |

`ux-identity.json` also records exact unchanged work-map, state, footer, workflow,
profile, frontmatter and uniformity provider hashes and all canonical header hashes.
The original 126+Swarm map was preserved, not re-audited or rewritten.

### Implemented behavior and retained methods

| Surface | Change and preserved value |
|---|---|
| Catalog/help/router | Add the existing literal selection/index operations and their common reference. Keep ordinary discovery, aliases, full source access, shared closure and the router's at-most-three skill-body shortlist. Role selections retain help/manual dispatch, not a new router or activation engine. |
| Welcome | Query task-scoped metadata before selected specialist bodies. Preserve task-first onboarding, actual surface/tool evidence, the nine phases, existing authorization and nonmutating orientation. No profile/job/cycle is created merely to explain the workflow. |
| Status | Read the selected original map and current/explicit cycle, preserving original IDs, incomplete tasks, STARTING/BLOCKED/INCOMPLETE and `release_clearance: false`. Verify an existing profile pin without binding. Repo-local jobs/opt-in archives are supplementary; absence is not no-work proof and personal registries are not scanned. |
| Skillify/template | Retain lesson-to-draft, exclusions, method, worked/negative examples and failure guidance. Query canonical names/aliases literally, distinguish bare names from wrappers, check the owned target through the existing path helper, refuse overwrite, instantiate real opening frontmatter, and validate the exact draft with the existing required-field validator. No automatic activation/symlink or private-tree scan. |
| Uniformity | Retain the existing per-kind floor and descriptive adoption method. Execute only trusted-source helpers, preserve floor exits and distinguish unavailable resources, execution errors and a genuine warn-only stale matrix. No matrix generation or target-executable fallback. |
| Eval | Retain expected-versus-observed known-good/known-bad comparison with separate cell denominators, explicit corpus/rubric/evidence/output selection and useful discrepancy reporting. Empty, insufficient and partial scope cannot certify the full corpus. No invented model/gateway, audit writer, snapshot stamp or ship control. |

The status composition deliberately calls data-only `profile_context.py verify`,
then `required-policy` with the exact verified reference. The shell profile/workflow
wrappers source the audit writer and can create directories or log failed reads.
Using those wrappers would violate this consumer's no-write requirement, including
profile-drift negatives. No audit function is disabled or replaced. State fields and
phase selection still use the unchanged shared readers; map/task/package and policy
interpretation remain with their accepted providers, not another parser or ledger.

Self-review additionally removed the possibility of a final `printf` masking a failed
resume-phase read. The caller now reports the error, preserves its exit and does not
publish a successful status first; an explicitly injected reader failure covers it.

### Actual verification

All commands ran under the inspected session-only allowlist/preflight with synthetic
HOME/USERPROFILE, application, temp, XDG, Lintel/profile/audit/jobs and target roots.
Fixture Git discovery ceilings and configuration stayed separate from source Git
identity/EOL verification. The consumer test also checks its actual parent environment
before importing the accepted native-path helper and inspects every child environment.
No real-home incident inspection/rollback or private policy access occurred.

Verbose stdout/stderr, actual exits, argv and Git-visible source seals are retained in
this session's `files/p13/logs/ux-*`; successful validation commands report zero source
changes. Consumer assertions compare complete synthetic file **and directory** snapshots,
including native long-path profile files. Source content was not normalized to silence
diagnostics. Python runs used the existing interpreter/dependencies; none was installed.

| Actual command/check | Outcome | Retained receipt |
|---|---|---|
| `python -B -X utf8 tests/unit/catalog-consumers.py` | 31 methods PASS | `ux-consumers-final.*` |
| `bash tests/unit/catalog-consumers.sh` on committed `7884ddd0` | 31 methods PASS, exit 0 | `ux-committed-consumers.*` |
| `python -B -X utf8 tests/unit/catalog-metadata.py` | retained 31 methods PASS | `ux-retained-metadata.*` |
| `python -B -X utf8 tests/unit/catalog-selection.py` | retained 19 methods PASS | `ux-retained-selection.*` |
| `python -B -X utf8 tests/unit/client-capabilities.py` | retained 16 methods PASS | `ux-retained-registry.*` |
| `bash tests/shape/catalog-regenerates-clean.sh` | PASS; actual check executed, no skip | `ux-catalog-shape.*` |
| `bash tests/shape/deprecated-aliases-resolve.sh` | PASS | `ux-alias-shape.*` |
| `bash tests/shape/frontmatter-lint-all.sh` | PASS; 127 skills / 69 roles | `ux-frontmatter-shape.*` |
| `python -B -X utf8 bin/li-catalog.py --json --kind=all` | 196 declarations, 46 skill aliases, unknown maturity, no execution | `ux-canonical-json.*`, `ux-identity.json` |
| `python -B -X utf8 bin/li-catalog.py --check` on committed product | exit 0; `Catalog matches skill frontmatter.` | `ux-committed-catalog.*` |
| Python 3.9 AST grammar + `bash -n` on the owned examples | PASS; 17 Bash blocks | `ux-syntax.*`, `ux-syntax-evidence.json` |
| Exact changed-path/header/provider checks and `git diff --check` | PASS; source clean at product freeze | `ux-identity.json`, `ux-final-diff-check.*`, `ux-product-clean.*` |

The 97 distinct Python methods above are 31 consumer + 31 metadata + 19 selection +
16 registry methods; reruns are not counted as additional coverage. Actual execution
was Windows/Python 3.11.9/Git Bash with existing PyYAML. The 3.9 check is grammar only,
not an actual Python 3.9 runtime result.

The new checks execute the shipped Bash caller blocks with real accepted readers,
not a parallel implementation. They cover unrelated cwd/target import decoys,
literal aliases/selections, canonical demo-role selection, invalid or existing drafts,
template boundary/required-field failures, real uniformity floor/diff/error/absence,
two initiatives, current and explicit cycles, bound native cycles without a map,
missing artifacts/jobs/pins, profile reference mismatch/drift, incomplete phase
records and repo-local archive opt-in. Additional injected uniformity/reader exits
test caller propagation and remain labeled injections. Actual no-write assertions
cover the refused cases as well as the successful previews.

Eval assertions check the illustrative separate-denominator arithmetic and partial/
unrun guidance. Draft instantiation is fixture setup. Neither establishes model
authoring/evaluation, actual host skill discovery, independent critique or tool permission.
The source-query checks likewise do not prove how a model will choose subsequent bodies.

### Retained failures and fixture recovery

- `ux-consumers-red.*`: six failures, exit 1, before product edits. The old uniformity
  caller used cwd rather than trusted source and masked missing-helper errors as stale;
  old status read the synthetic personal registry instead of original mapped work.
  The old template lacked opening frontmatter, selection/welcome caller sections were
  absent, and the eval example lacked the required truthful comparison/scope shape.
- `ux-consumers-green-1.*`: actual exit 1, 22 methods, two failures and four error
  entries. These exposed fixture defects: ordinary Windows I/O/cleanup missed deep
  synthetic profile files; the synthetic required pack omitted required voice/navigation
  fields; and text-mode fixture output used CRLF rather than the generator's actual bytes.
  Provider behavior was not changed to satisfy those incorrect fixtures.
- The fixture now uses the accepted native-path spelling for snapshot/unlink/cleanup,
  valid source-contract pack fields and exact fixture bytes. Logical roots, default
  stores, source EOL settings and assertions were not shortened, relocated or weakened.
- `ux-consumers-green-2.*` passed 22 methods; `ux-consumers-green-3.*` passed 28 after
  additional negatives; final and committed runs passed all 31. Earlier failures remain
  retained and are not relabeled as passing evidence.
- The one leftover owned failed-test root, `catalog-metadata-dr_ma2oh`, was explicitly
  inventoried with per-file hashes and reparse checks, compared unchanged, then removed
  via the accepted native spelling. `ux-failed-fixture-inspect.*`,
  `ux-failed-fixture-inventory.json` and `ux-failed-fixture-clean.*` retain this bounded
  cleanup. No wildcard, broad session/root cleanup or real-home action was performed.

### Review limits and next runnable action

Implementer self-review found **no remaining P1/P2/P3 finding** in this released unit.
This is not independent SPEC or QUALITY acceptance. The separate SAME reviewer must
review this immutable product against the full released card, then perform QUALITY
only after SPEC passes. Unchanged accepted first-unit/A+B evidence is preserved; no
whole-package or whole-initiative completion is inferred.

The coordinator's actual installed alias/selection/notice pilot is recorded in the
release at `a5d77f9` using the accepted P06 engine. This unit did not rerun or replace
that evidence and does not accept the blocked P10 transaction/default/linked engine.
No P08 native fixture/controller/runtime was touched or run. No P10/P11/P12 WIP was
imported; no shared ledgers, plans, memory, ADRs, role bodies or generated outputs
were edited. Final versions, integrated checks, host/model behavior and delivery remain
with their original owners and gates.

**Next runnable action:** coordinator verifies the product/report pair and supplies it
to `4865e4f7` for released-consumer SPEC then eligible QUALITY. The reviewer can run
`bash tests/unit/catalog-consumers.sh` from the immutable source under an inspected
synthetic environment, then the retained checks above. Preserve this original owner
for any scoped repair and later P13 release; no new proposal approval is needed.

**Position:** BUILD consumer product frozen -> independent REVIEW. No SHIP clearance.

## Consumer F01 existing-draft observation repair (2026-09-22)

### Authority and immutable ancestry

Coordinator release `b1ca886c37f469d705603638632eda6bb7f25831`, final P13 card
"Consumer F01 existing-draft observation", authorizes only this bounded consumer
repair. The complete released card and all 148 lines of independent report
`5a3ab4c337a842011428aba05df08976e46c648a` were read directly through Git.
The report's sole parent is `5df8e9977a091b1a2c207cae2274dbae0f3a9969`; its Git-byte
SHA-256 is `769ed6345de88107b0aaddc7d39b6c9f41c47cd01ac333ecec2aceda1c991680`.
Those identities were verified, not inferred from the summary.

The independent verdict supersedes the preceding implementer self-review: consumer
SPEC failed one P2 (F01), the other eleven controls passed at their stated evidence
level, and QUALITY had not started. This is distinct from closed compact-discovery
F01/F02. No earlier report or outcome is rewritten.

**Frozen correction:** `4922a6b3c347f4b442ce3cbda4bb69969cf33fce`, sole parent
`5df8e9977a091b1a2c207cae2274dbae0f3a9969`. The original owner continued directly
from its frozen branch; **no coordinator ancestry or other product was merged**.
This report append is a report-only child, with its exact hash/parent/line count
in `ux-f01-report-freeze.json` and the coordinator handoff.

| Exact correction path | Committed Git-byte SHA-256 |
|---|---|
| `skills/skillify/SKILL.md` | `6b47510b5a54b3751ee9b6793bf16ae586a991ec7f739d6525193a099d705e46` |
| `tests/unit/catalog-consumers.py` | `5d407b1514680e650ded1ba5bbe53e71d21f1102dc7835df0e7165997e215bc8` |
| `skills/catalog/references/consumer-checks.md` | `b53584dbc903fb8510480431bfeaebdfe8443847075500bff76e28d1ff8789e3` |

All twelve original consumer product paths remain present; the other nine product
paths are byte-identical. All 196 canonical opening headers, 69 role bodies,
127-skill/46-alias inventory, original 126+Swarm map, generator, descriptor, providers,
installer, versions and generated output remain unchanged. The preceding complete
736-line P13 report remains an unchanged prefix. Both local commits use Conventional
Commits and the required Copilot trailer; no amend/reset or remote operation occurred.

### Root cause, correction and actual reproduction

After the accepted `safe_path` resolves a safe logical destination, ordinary Windows
`draft.exists()` can return false for an existing deep file. The consumer incorrectly
admitted that path as new even though the shared native I/O spelling sees the file.

The only product logic change imports the existing `native_io_path` helper and uses
`native_io_path(draft).exists()` for that observation. The same logical destination
still goes through the existing containment/ownership checks and is printed unchanged
on a valid new-path result. This adds no provider, parser, root shortening, permission
or installer behavior. The immediate pre-write recheck and refusal to overwrite in
the authoring instructions are retained.

The original shipped Bash block was executed against an actual **297-character**
owned synthetic draft with the reviewer's content `EXISTING OWNED DEEP DRAFT\n`,
SHA-256 `bf6ce5e717df278e50d11785693086c68ebb6e34cd6ec2db1a6daa46a7b36d72`.
Its relative hierarchy uses the same bounded/existing components; only fixture
component padding accounts for this session's different selected root length.
No selected root was shortened or changed between RED and GREEN.

| Observation | Original caller | Corrected caller, including committed rerun |
|---|---|---|
| Logical path length | 297 | Same 297-character path |
| Ordinary `exists()` / native `is_file()` | false / true | false / true |
| Actual caller exit | 0, incorrect admission | 2, expected refusal |
| stdout | Printed logical draft path | Empty |
| stderr | Empty | `INVALID destination: target must be a new owned SKILL.md; refusing overwrite` |
| Content and complete directory/file snapshot | Unchanged | Same bytes/tree as RED; unchanged |
| Authoring or overwrite performed | No | No |

`ux-f01-red-297.receipt.json`, `ux-f01-green-297.receipt.json` and
`ux-f01-committed-297.receipt.json` retain exact caller hashes, outputs, paths and
complete snapshots. The source identity check compares RED/GREEN snapshots directly.
The owned fixture remains retained as evidence, not a target for later authoring.

The two new focused regressions also exercise a real existing deep draft and a new
deep destination through the shipped block. On this host their logical paths are
332 characters; the existing case uses the exact same content hash. The new path
still succeeds with unchanged logical stdout and no directory/file creation. Existing
short-path, canonical/alias collision, wrapper-name and containment cases are unchanged.

### Checks and retained failures

All product imports/calls and fixture setup ran under the existing inspected allowlist,
synthetic HOME/USERPROFILE/application/temp/XDG/Lintel roots and Git ceilings. Exact
source Git/EOL checks used a separate invocation without fixture Git configuration.
No dependency, global setting, host policy or private-home access was added.
Logs opened before execution retain actual exits and source seals under
`files/p13/logs/ux-f01-*`; every validation/commit reports zero source-byte changes.

| Check on stable correction | Actual result / receipt |
|---|---|
| Same retained 297-character caller | Refuses with exit 2, full bytes/tree unchanged; `ux-f01-green-297.*` |
| Complete consumer suite | 33/33 PASS; `ux-f01-consumers.*` |
| Committed Bash suite entrypoint | 33/33 PASS; `ux-f01-committed-consumers.*` |
| Committed same-file reproduction | Refuses with exit 2; `ux-f01-committed-297.*` |
| Retained metadata / selection / registry suites | 31/31, 19/19, 16/16 PASS; `ux-f01-retained-{metadata,selection,registry}.*` |
| Catalog drift / alias / frontmatter shapes | PASS; actual catalog check, no skip; `ux-f01-{catalog,alias,frontmatter}-shape.*` |
| Stdlib-only `python -I -S -B bin/li-catalog.py --check` | Exit 0, exact existing success message; `ux-f01-stdlib-catalog.*` |
| Syntax | 17 Bash blocks and Python 3.9 grammar PASS; `ux-f01-syntax.*` |
| Scope, headers, preserved dependencies, report prefix and RED/GREEN identity | PASS; `ux-f01-final-identity.*`, `ux-f01-committed-identity.*`, `ux-f01-identity.json` |
| `git diff --check` and commit scope/parent checks | PASS; `ux-f01-diff-check.*`, `ux-f01-product-freeze.json` |

This is **99 distinct Python methods**, the original 97 plus two caller regressions;
repeated executions are not additional coverage. Actual runtime remains Windows,
Python 3.11.9, Git Bash and existing PyYAML. Python 3.9 remains grammar-only evidence.

Retained RED receipts: `ux-f01-red-297.*` has wrapper exit 1 because the actual caller
returned 0 instead of required 2; the preserved caller receipt records that actual 0,
not an invented failure exit. `ux-f01-regression-red.*` ran two methods: new-path success
passed, existing-path refusal failed with exit 0 and unchanged bytes/tree. No repair
was applied before those failures, and they remain historical failures.

The initial `ux-f01-authority.*` collection inherited fixture-only Git switches for
its source diff read. It is retained but not used as authoritative source EOL evidence.
`ux-f01-source-identity.*` and later identity runs use the separate source invocation
and explicitly reject those switches. No source normalization or configuration change
was used. The report Git-byte hash/parent is reverified there as well.

### Findings and next gate

Implementer self-review: the bounded F01 defect is corrected by the actual caller
evidence; no additional open P1/P2/P3 finding was identified in this correction.
**Independent F01 closure is still pending**. The original eleven passing controls
and 97-method history stay attributed to the prior reviewer, not reissued by the builder.

Same reviewer `4865e4f7-240b-4721-a64d-ce6df9a75a9c` must recheck the complete released
consumer SPEC using unchanged evidence where valid, then perform the first eligible
QUALITY review. The coordinator receives exact source/report identities and may run
`bash tests/unit/catalog-consumers.sh` under the inspected synthetic environment.
No new reviewer, model/client/native fixture, P10/platform/installation task or wider
acceptance is implied. The same original P13 owner remains available for scoped feedback.

**Position:** BUILD F01 correction frozen -> same independent SPEC then QUALITY.

## Remaining optional source families and current preservation (2026-09-23)

### Authority, accepted base and frozen product

The coordinator approved the finite 275-line source-only recommendation, SHA-256
`cbd3ca674ef3b19b8d1ec036713a5922381077d00818339a0d37845628805ee8`,
and committed release `a4262426b83b0eae93cf9695c3c1d8cb5303d478` in the original
P13 card. Its "Remaining optional source families and current preservation"
section and the P12 B01 rejection were read before execution.

The same original owner, `c4e9de1c-1c81-4897-ba82-019d58b635d7`, normally
fast-forwarded from `1442aa7d` to the explicitly named accepted dependency
`42abfcba39ac80a271ff57e838ffccb1d7f6ed7c`. Histories remain intact.
That dependency includes the rejected pipeline's review, **not its source**.
The release commit was inspected through Git; no unrelated moving coordinator tip
was imported.

**Frozen product:** `20493b74005d349f6fd311503fe179de992a3506`.
**Sole parent:** `42abfcba39ac80a271ff57e838ffccb1d7f6ed7c`.
Commit: `feat(catalog): describe optional source families without pruning`.
Only these four product paths changed:

| Exact path | Committed Git-byte SHA-256 |
|---|---|
| `lib/capability-selections.json` | `975cf134a8124000624d0599a3b88a1c8eb6e5c0e8bdf0a75f3790fbdd86752a` |
| `skills/catalog/references/selections.md` | `4859d15dfc00ee5a4d933dfdb98185e2c42990d16b2a8e65fb2900396442e801` |
| `tests/unit/catalog-selection.py` | `26278a1ab9dcdc7bf6d896e70bc892ea0cdc271111308d405dddbdcb7140d3e2` |
| `.claude/plans/universal-implementation/reports/P13-skills-preservation.md` | `052a7136edb5b232d77ed0f7c9f364568dcb4bf6c2b6aae762b4abd9df534918` |

This append is the report-only child. Its exact commit/parent, report hash and
line count are retained in `families-report-freeze.json` and sent to the recovery
coordinator. Both commits have the required Copilot trailer. The preceding report
bytes are preserved; historical failures and accepted verdicts are not rewritten.

### Delivered source behavior

The existing schema/parser now reads ten additional ordinary records, for twelve
selections including the unchanged `core` and `demo-script`:

| Added selection | Existing dependency and retained source value |
|---|---|
| design-knowledge | Core; existing retrieval, tokens, consultation, explicit corpus/profile resources |
| frontend-design | Design knowledge; director/submethods/renderers, real source roles and browser references |
| document-content | Design knowledge; full outline/content/QA and repository-documentation methods |
| document-word | Core only; standalone Word and its exact fidelity/native procedure |
| document-ppt | Design knowledge; required slide retrieval, editable slides, notes and critique |
| document-pdf | Core only; accepted prepare/print/reader and browser source |
| document-xlsx | Core only; workbook/integrity with actual CostAnalyzer/CapacityPlanner |
| document-visio | Core only; retained staged-slot discovery, no writer or nonexistent NetworkArchitect |
| customer-communication | Core; eight existing draft/empathy/follow-up roles and explicit scoped voice eval |
| regulatory-review | Core; actual EU AI Act/GDPR/SOC2 methods, never optional generic security or company policy |

Existing explicit unions compose desired methods. Standalone Word/PDF/XLSX do
not inherit the common pipeline, every format or frontend roles. An email does
not require demo narration. Selection returns metadata/paths and inclusion
reasons; it neither loads all prompt bodies nor creates actors, output, policy,
installation or permissions.

Resource arrays contain explicit canonical files, not globs/directories/private
input/output. Design closure includes its actual six scripts, contract/token
references, existing profile, 35 CSV files and accepted shared helper/schema
references. Existing runtime/install preflight remains its owner's contract.
The two recorded design adaptation IDs preserve both actual MIT/Apache notices,
attribution, recorded release and null historical import revisions. Empty
additional provenance is not an originality or legal-sufficiency statement.

All ten examples identify selected inputs, retained method/output, a concrete
negative and the precise evidence limit. They are worked source scenarios, not
newly executed native/model workflows. General maturity remains unknown.
PDF/XLSX unknown/null records mean implemented source with remaining gates, not
missing methods. Visio's exact TEMPLATE ONLY digest is unchanged.

The new P12 pipeline candidate `d4e9188` remains SPEC FAIL for P2 B01 under review
`e85f70a`, retained in dependency `42abfcb`; QUALITY has not started. No rejected
`pipeline_inputs.py` was imported and no shared serialized-pipeline PASS is claimed.

### Current preservation without erasing history

The original 126 recommendation/method/argument/alias rows plus separate Swarm row
are retained **verbatim**, explicitly labeled as the historical `204ea725`
comparison. A new accepted-current delta covers exactly the 43 original skill
bodies plus Swarm that changed by `42abfcba`. Direct original `28061e4` comparison
confirms 73 changed originals and 53 byte-identical originals, not semantic
acceptance. The delta supersedes stale historical pending wording at its stated
evidence layer instead of pretending those old rows were a new whole-corpus audit.

Current J04/J09/J11/J12/J13 pointers name accepted Swarm/module/design/document/
consumer source and retain native/installed boundaries. Five changed frontend
roles link to their P11 source evidence alongside the unchanged 69-role map.
The 76-path Swarm map and 17 historical artifacts remain preserved; bounded A22.7
evidence is identified without a new parent-closure claim.

The old missing-alias-bundle statement now records the actually completed
`ad605de`/`a5d77f9` accepted-P06-engine pilot. New-family distribution and the new
P10 engine remain distinct. The current PDF/XLSX stage reconciliation is recorded
without discarding the historical audit's recommendations or promoting maturity.

### Actual checks and evidence

All product calls, including in-process catalog calls/comparisons, used the existing
inspected synthetic HOME/USERPROFILE/application/temp/XDG/Lintel/source/target
preflight and fixture Git ceilings. Source Git/EOL identity checks ran separately
without fixture config overrides. No dependency restore, global setting, native
actor, model invocation, network, installed consumer or new reviewer was used.
Logs were opened before commands; actual exits and before/after source seals
remain under this session's `files/p13/logs/families-*`.

| Check | Actual outcome | Retained receipt |
|---|---|---|
| Full expanded selection suite | 29 methods PASS | `families-selection-final.*` |
| Committed Bash selection entrypoint | Same 29 methods PASS, exit 0 | `families-committed-selection.*` |
| Retained metadata | 31 methods PASS | `families-retained-metadata.*` |
| Retained source consumers | 33 methods PASS | `families-retained-consumers.*` |
| Retained registry | 16 methods PASS | `families-retained-registry.*` |
| Stdlib-only catalog `--check` | Exit 0, exact existing success message, also on committed product | `families-catalog-check.*`, `families-committed-catalog.*` |
| Actual committed `--json --list-selections` | Twelve records, executed false; PDF/XLSX unknown/null, Visio staged | `families-committed-index.*`, `families-index-summary.*` |
| Existing alias shape | PASS | `families-alias-shape.*` |
| Exact source/header/default/provenance/map identity and Python 3.9 grammar | PASS | `families-final-identity.*`, `families-committed-identity.*` |
| Whitespace, exact commit parent/path scope and clean product tree | PASS | `families-final-diff-check.*`, `families-product-freeze.json`, `families-product-clean.*` |

This is **109 distinct Python methods**: 29 selection + 31 metadata + 33 consumer +
16 registry. The named accepted dependency already had 20 selection methods after
the coordinator's stage regression; this unit adds nine, preserving the earlier
99-method and later stage-fan-in evidence as their exact history. Reruns are not
counted as new coverage. Actual runtime is Windows/Python 3.11.9/Git Bash with
existing PyYAML; Python 3.9 evidence is grammar-only.

New checks cover exact canonical members/dependencies, all ten real source
closures, minimal standalone format dependencies, deterministic/deduplicated
unions/reasons, both notices and unknown import IDs, literal alias/query behavior,
unknown/staged facts, explicit file resources, exact example headings, unchanged
source-body boundaries and the current preservation pointers. Retained malformed,
cycle, path, member, missing/empty-resource and provenance refusal cases still pass.
New per-family descriptor fault injection is explicitly injection, not a native
filesystem/client run. Example label/heading checks are structural; they do not
prove a model produced the illustrated result.

`families-final-identity.evidence.json` and the committed counterpart preserve
data-only identity proofs separately from `.json` command receipts. All 127
canonical skill bodies/headers and 69 role bodies/headers match the base. Every
non-owned source byte, 46 aliases, underlying providers, parser/generator,
installer, versions, generated files and actual notice/attribution files are
unchanged. Core/demo definitions, schema version 1 and all stage records are
equal to the named base. The original 127 historical map rows compare equal,
and current-delta names match the actual Git path set.

Protected Git-byte SHA-256: generator
`7d0475f7400f32deb1e1bdafbc240575af3836755017d65c20af8614c988b32d`;
catalog `615d1ebbc5d9853bde82f9e3d0d0e321cad92420e7dcf794d13f24e64ccedc99`;
alias registry `557f0d35ae9b43059d7b2c991b3fa22cc3829378e20a9b0c4ff7d1d8a6b6a799`.
The full protected provider/notice/map identities are in the retained evidence.

### Retained failures and review limit

`families-red.*` records the actual pre-implementation failure: the required family
set was absent and standalone queries correctly raised unknown-selection errors.
It ran two methods with one failure and four error entries; no product edit preceded
that RED evidence.

`families-first-closure.*` ran seven new methods after source records/examples were
added: six passed, while the fault-injection method recorded ten error entries
because its oracle expected ValueError for missing files. The unchanged reader
correctly raises FileNotFoundError at that boundary. The test now expects that
specific documented I/O exception for the resource case, retaining ValueError for
bad members/dependencies. No reader change, silent fallback, skipped assertion or
source normalization was used. Later full 28-method, expanded 29-method and
committed 29-method runs pass; both earlier failed receipts remain retained.

Implementer self-review: no additional open P1/P2/P3 finding identified in this
bounded source unit. This is **not independent acceptance**. Same reviewer
`4865e4f7-240b-4721-a64d-ce6df9a75a9c` must review the complete released source
SPEC, then eligible QUALITY, after coordinator intake of the exact product/report.

Word/Excel/PDF native gates, static V1/framework TLS, P10 linked positive, jq/CI
availability and denied record routes are unchanged. No generated/source integration,
final version, installed new-family distribution, original parent closure, remote
publication or whole-initiative acceptance follows from this unit.

**Next runnable action:** coordinator verifies the frozen product/report and assigns
SAME486 complete SPEC then QUALITY. `bash tests/unit/catalog-selection.sh` exercises
the committed source projection under the same inspected synthetic-root discipline.
Keep the same original owner for scoped feedback; no replacement actor is needed.

**Position:** BUILD optional source families frozen -> independent REVIEW; native,
installer and final delivery gates remain separately owned.

---

## Accepted native selected-method utility (2026-09-23)

**Bounded case accepted:** complete independent SPEC PASS, followed by eligible
QUALITY **PASS_WITH_ADVISORY_P3**, P1/P2/P3 = **0/0/1**. The single N01 remains
optional advice; no polishing, re-draft or new native case is commissioned.
This append records that one later result without rewriting earlier evidence
or closing an original parent.

Native-case authority: `8c9ab7148033518c18b4e8d959082d16a2928f98`.
Accepted read-only source: `9c15d690c539eba3271ef7041447fa93d7b0ff15`.
Report-only capture authority: `53f5e0c9c025ef551a044931d5e90158c8acc560`,
the complete P13 card's "Accepted native utility and report-only capture".
The report-only feature commit has sole parent
`e797bd48c31257e79c7ccc3eb369d584bfd7db8b`; no source merge/import is involved.
Its exact commit and final report identities are supplied in the private capture
handoff rather than attempting a self-hash here.

### Actual selected-method execution

The original controller is `c4e9de1c-1c81-4897-ba82-019d58b635d7`.
Exactly one fresh native general-purpose context,
`b9fd8733-0b32-4cbe-9640-5d9aee1a899a`, applied the selected canonical methods
as explicit Universal role inputs, not assumed native named-role registration.
The accepted P06 repository-kit route supplied the one isolated synthetic
consumer; this was not P10 transaction/default/linked/bare/native installation.

Recorded kit initialization/check both exited 0, with 453 managed files and no
installed hooks. Exact managed-source identities were checked before consumer
helper use. Actual source/consumer `--json --selection=demo-script` and literal
role-name queries exited 0 from an unrelated fixture cwd. Their entry arrays
matched; source roots correctly differed. Metadata remains source-only,
`executed: false`, with unknown maturity, distinct from later observed activity.

| Actual native step | UTC on 2026-09-23 |
|---|---|
| One fresh context dispatched | 13:37:04.015 |
| DemoNarrativeArc Plan returned | 13:41:11.695 |
| Controller's hash-bound approval persisted | 13:45:52.636 |
| Continuation sent to the same context | 13:47:30.376 |
| Same context's DemoNarratorJunior draft returned | 14:00:07.366 |

Exposed actor-linked reads show each receiver's literal metadata before its
selected body and the recorded approval before Junior drafting. The read-only
receivers returned artifacts; the controller persisted their exact UTF-8 bytes.
The host exposed `gpt-6-astra` and `multiTurn: true`, with no requested model,
reasoning or context override. Its initial `resumable: false` is retained:
actual continuation and the second return, not a declaration, prove transport.
Automatic injected-context visibility remains **UNKNOWN**; explicit read traces
do not establish an all-context negative. Shared bootstrap/policy reads are
separate from optional role bodies.

The outputs preserve fictional Retry Ledger, two identical illustrative requests
with the same unspecified key and one proposed logical operation. No running
service, result, screen, recording, implementation, customer/market/ROI/performance
fact, slide, network call, renderer or rehearsal was supplied or invented.
The eight-minute allocation remains estimated, not a measured performance.

### Independent assessment and exact artifacts

Original reviewer `4865e4f7-240b-4721-a64d-ce6df9a75a9c` actually applied
DemoNarrativeArc **Critique mode** and SlideNarrationCritic to the existing
approved inputs. These are two substantive methods by one distinct reviewer,
not a new Plan, two reviewers or the drafter's self-review.
SPEC S01-S12 passed at `2026-09-23T14:25:26.806337Z`; only afterward QUALITY
Q01-Q05 passed with advisory N01 at `2026-09-23T14:26:16.253636Z`.
The complete critiques, bounded report, handoff and both stage records were
read and their supplied hashes verified for this capture.

| Frozen artifact | SHA-256 |
|---|---|
| Owner `arc-DRAFT.md`, 9,460 bytes | `d684ffda2a2a5cbad9de25b9863442fa28bcb92784869cb997c3572c301d62b6` |
| Owner `arc-approval.md` | `12ab1cebeeb6bce8902711ad9e44756e74ce6811ea510593007c62cbf095b0ca` |
| Owner `narration-DRAFT.md`, 14,145 bytes | `e076db4f811533c82299ef048a68abc7c8eae6aba05830849e73dc78682221f5` |
| Reviewer `narrative-arc-critique.md` | `4d24b62274f7ab45524119c7e05b462336bd2b3c4c4bcf3388bc5b73e7d32ae1` |
| Reviewer `slide-narration-critique.md` | `6e8851ee7cdb9d4fede1e5fcf11c0cd3cde346241d7052c9efbd3de3ed6685bb` |
| Reviewer `bounded-review.md` | `0894c9ba4225eef2d5c79cde6b422786daa3fc04d129cb2738e0b77b6933fe1b` |
| Reviewer `review-handoff.md` | `2180ffb4b128efdf0882c222e9626098b396393a2a6f5286644a00bbd0f5b712` |
| Reviewer `spec-decision.json` | `fb68ae9a870b1d178880120c320f8d7c188648d019a722bd948e8d1e4b18b824` |
| Reviewer `quality-decision.json` | `639ad6574d9ad4ade095398dfc9e56fe554a4962f31f40b9cfd60569c578c801` |

**N01 - P3 advisory, confidence 8/10:** narration lines 61-69, 95-105 and 115
repeat proposal-versus-proof qualifications before the dedicated evidence scene
at 121-129. A separately authorized later edit could consolidate repetition
within the approved scenes while retaining material labels and factual recovery.
It is the same finding in both critiques, not two findings. No edit was made.
The critic's `ship-ready` craft enum is **not Lintel SHIP or publication
permission**. Local bounded compliance/consistency assessment is not activated
hook/voice enforcement, enterprise clearance or global P05 acceptance.

### Retained evidence, failures and capture boundary

Owner evidence remains in this session's `files/p13/native-demo/`, sealed by
manifest SHA-256
`2817e492d483cd6f7f4f741420b4d0701d2b23f72fd1dca0cb34a271bc07f509`.
The complete helper requests/actual exits, metadata, managed identities,
selected-body hashes, exposed host chronology, approval and artifact returns
remain there. Reviewer evidence is in that reviewer's `files/p13-native-review/`;
its evidence-manifest SHA-256 is
`f6b37959e7990d50ada193e1fb83486823c20948c7c9a675d01bc8f7c5a5a9cf`.
These are private session evidence pointers, not installed resources.

Coordinator88 accepted the result after parent
`verification/p13-native-review-intake-01` exited 0: 52 bound review-evidence
files, 11 artifacts, ordered stage decisions, 1,055 unchanged case inputs and
three separately identified outside-seal receipts. This capture read that
actual result/output and independently rechecked the named hashes as data,
without rerunning the native case, P06 kit/catalog helpers or product tests.

The initial private ZIP-export preparation remains **RED, exit 1**: its first
member's CRLF conversion failed Git-blob identity before a canonical write,
kit operation or native dispatch. Exact immutable `git cat-file --batch` bytes
then succeeded, without source normalization, dependency/configuration change,
another installer or another drafting context. The original collector, archive,
stderr and real exits remain. The reviewer's initial extra-file assertion and
refined timing arithmetic are also retained, not relabeled as fresh successful
execution. Text-based timing sensitivity is not measured pacing, audience
learning or a guaranteed eight-minute performance.

Report capture uses the new owned `files/p13/native-report-capture/` area:
inspected synthetic HOME/USERPROFILE/app/temp/XDG/Lintel roots and Git ceilings,
actual command/exit receipts, unchanged source Git controls, and no global
settings, private-home mutation, push or remote action. The full preceding
1,053-line prefix is preserved: 73,477 raw CRLF bytes, SHA-256
`2c8be18231b1e7bb48ccc41d906643ca7e14ccf296bcc4a8c539359a09915693`;
72,424 Git-LF bytes, SHA-256
`87777ff464519b9685ea5639f624c3505a88258f791a7a814c39e0439e742f79`.
The old 1,067-file equality belongs to the **pre-report** observation. After
this authorized delta, only this report changes; the other source bytes and all
frozen native/reviewer inputs stay unchanged. Exact one-path/parent/branch,
raw-versus-Git hashes, whitespace and clean-state receipts accompany the normal
report-only commit with the required Copilot trailer.

No product, descriptor, maturity, alias, provider, installer, preservation map,
version, generated output, ledger, memory or ADR is changed. Original
**79/113** and the 126/69/76 history remain unchanged. Full A18.3/A19.4, late P10,
final preservation, native-format, global P05 and final delivery gates remain
separately open; a useful single case does not replace their original acceptance.

**Position:** accepted bounded native utility captured; original owner parked
after the report-only handoff, with no further actor, critique or rework.

---

## Installed consumer closure revalidated on repaired P10 (2026-09-24)

**Builder result: full six-method suite PASS, zero skips. Independent review
remains held/pending.** This is the coordinator-authorized continuation of the
original installed-consumer unit, not a new capability implementation or a
self-issued A18/A19/P10 acceptance.

### Authority, corrected base and new pair

Coordinator88's 2026-09-24 continuation authorizes revalidation after accepted
P10 integration repair `0caba6fe50be52c82763cf4c3e326ad629b1b3b1`,
review `9ca6402a095c48818ece2affad297324feeb31ca` and integration `b0598b0b`.
The complete [integration review](../reviews/P10-integration-0caba6fe.md) was
read: SPEC and QUALITY PASS, P1/P2/P3 = 0/0/0; compliance remains UNVERIFIED.
It establishes the bounded IC-F01 verification-read repair and preflight-before-
target-observation ordering, not universal immunity to outside interference.

Exact selected base: `f740141e9ab2d860d1fc2c824a5a4b7015cdc805`.
New worktree: `E:\Workspace\p13c4-0924\r\w`, within the existing authorized
workspace. Branch: `jokerman-microsoft-p13-installed-recheck-0924`.
The six owned test changes were recreated from
`235102a858fefd3c39ad763caf6f8d1f71f132fb` using its exact Git diff, checked
before applying without conflict. No old report, failed installation or
producer file was copied over the corrected base.

**New test commit:** `5c77a984a0686e8659ec8294da6aa9084702cec7`.
**Sole parent:** `f740141e9ab2d860d1fc2c824a5a4b7015cdc805`.
Only the original six test paths changed:

| Test path | New Git-byte SHA-256 |
|---|---|
| `tests/unit/catalog-metadata.py` | `d6ceb34a6a7ff98bee0796d1011043e54a98b25411a6fa9fc5e793cfd2e170ab` |
| `tests/unit/catalog-selection.py` | `8f5e78b53fadd82a0e183736baecf3c833c666980ad0efba2fd12d78e742d99a` |
| `tests/integration/copilot-kit.py` | `49e9a23496d8e4fce6d5fa68098e01c6682ab4a5b597abd2e10238af8731ff62` |
| `tests/integration/catalog-installed.py` | `808668f3911011db1619507600f5f3ca19020971394f3f8e9acd4b73dc4ceb7f` |
| `tests/integration/catalog-installed-probe.py` | `a47692b5beccd37193fb03074068126d1322cf971d154c7c65f1b8ec150ca027` |
| `tests/integration/catalog-installed.sh` | `368fea9218223bcba331a3c69fa2eb59a2a1afd19fca21e98b84fc0abdc97685` |

Five blobs are exact to the original test candidate. The kit test file keeps
the corrected base's P10/P09 work; its 50 test methods remain present, with only
the same two P13 selected-catalog/family methods using the extracted query
transport. The other 48 method ASTs are unchanged from this corrected base.
No assertion, source inventory, parser, descriptor or production function was
weakened or changed by P13.

This section is a report-only child. Its exact commit/report identities accompany
the handoff, with the full preceding 1,196-line raw Windows/Git-LF prefix retained.
Both commits use normal Git controls and the required Copilot trailer.

### The complete actual run

The existing Python entry ran all six methods once with
`--fixture-root <owned short root> --keep-fixtures`, without method filtering.
**6/6 PASS, zero skips, actual exit 0; 1,014.624 seconds reported by unittest.**
Runtime: Windows/Python 3.11.9 through the approved PowerShell 7 launcher.
No Windows PowerShell 5.1, Bash native performer or additional host was exercised.

| Actual method | Result and retained value |
|---|---|
| All methods, aliases and worked examples | PASS: current canonical source/installed entries and bodies, all 46 aliases, all 35 module-capability rows and declared examples/negative/evidence labels |
| Literal and malformed installed selections | PASS: literal hostile input remains data; invalid selection and empty/malformed descriptor refuse without query writes |
| Metadata-first body observation and injected exposure | PASS: its previously failing fresh installation now completes, metadata precedes one selected body, injected unrelated body access is visibly reported |
| Optional-family closure | PASS: twelve selections, minimal standalone dependencies, deterministic unions, source/provenance/notice bytes and removed-asset/notice refusals |
| Portable selection | PASS: init/check/repeat, copied installed-source lookup without source fallback, alias/demo-role discovery and missing-parser/resource refusals |
| Unresolvable alias target | PASS: invalid installed alias refuses both its named query and an unmatched selection query |

All installation/check calls went through the accepted `li-lifecycle.py`
`scaffold init|check --client copilot-cli` surface, with literal source/target/
store arguments. Managed/canonical identities were checked before consumer
helper use. All process environments had inspected synthetic HOME/USERPROFILE,
application/temp/XDG roots and Git ceilings, with **zero `LINTEL_*` selectors**.
The longest actual fixture path was **141 UTF-16 code units**, not a long-path
workaround. No real profile or global installation was used.

The actual 99 child receipts contain expected refusals, not 99 successful exits:

| Operation | Actual exits |
|---|---|
| Lifecycle init/repeat | 7 x exit 0 |
| Lifecycle check | 7 x exit 0; 3 x expected exit 2 |
| Catalog metadata/query | 70 x exit 0; 10 x expected exit 1 |
| Test-owned body observer | Positive exit 0; injected unrelated-body refusal exit 3 |

All six new transaction journals are `complete`, each with 473 applied file
entries. No B01 replacement-retry or IC-F01 verification-read notice was
observed in these 99 saved stderr streams. This successful run did not reproduce
the external timestamp disturbance; the accepted P10 discriminating regression
and diagnosis remain separately attributed evidence, not a fabricated new
interference test.

The body-observation traces have SHA-256
`f6132d0f98f12e0879c423c6dc616bd902d38380b75eab40d732ccaa14277811`
(positive) and
`45ed4553ce1e50df35b53270d4e9396c0eb0d827a2ed6e7b39d26dc1a11995bd`
(injected negative). This is the existing **test-only** Path read observer,
not shipped access control, a fresh model/client invocation or complete
visibility into automatically injected model context.

### Verification, preservation and earlier failures

Private evidence is under `E:\Workspace\p13c4-0924\r`. The retained full run
is `logs/full-six.*`; `e/full-six-verified.json` binds all child arguments,
exits, timestamps, synthetic roots, observer traces and completed journals.
The actual fixture root is `cases/p13i-i8q61vly`. Fault-modified consumer
inputs are intentional negative fixtures, not deployed copies or proof that
their final corrupted data is an intact installation.

The focused existing source body-boundary method also passed once. Python 3.9
grammar passed for the five Python test files; actual Python 3.9 execution and
the shell test entrypoint were not run. The full six-method source remained
byte-unchanged during execution, and exact committed test blobs match that run.

One private receipt verifier initially exited 1 because it wrongly required
`CLAUDE_CONFIG_DIR` in every saved environment. The retained kit clone deliberately
removes `CLAUDE_*`; HOME/USERPROFILE and other roots remain synthetic, and no
client is launched. The data-only verifier now records that specific optional
omission rather than inventing an exported value. It rechecked the unchanged
saved run successfully; no installation or test was repeated for this correction.
The first failed verifier receipt remains.

All 4,861 files in the old IC-F01 packet and all 680 files in the separate fan-in
packet were rehashed unchanged. The original failed target's applying journal,
324 applied/149 pending state, initial wrong exit-code oracle and earlier
preparatory failures remain at their original references. They are not relabeled
as passing. No retry or recovery of that old failed target was performed.

The old installed test/report refs remain
`235102a858fefd3c39ad763caf6f8d1f71f132fb` /
`6f1de58b69ec5b69a492780e3ebfacf0afe179db`.
The frozen fan-in remains
`e7f799ec6022a13ff8ff089eddea56106d9d1c24` /
`9096135aa4e3f0060110db5fe17f97b1c0498da7`.
Their worktrees and the original `ec5dfef` checkout are unchanged.
At pre-freeze comparison, recovery
`23078fe809e5fab94121e4060167429e6fc78098` adds only shared-state/card updates
relative to the chosen base; no consumed P10/P09 source changed after this run.

### Review and remaining authority

These checks supply the previously blocked full installed-consumer evidence for
the released A18.3/A18.4/A19.4 unit. They do **not** self-close original leaves,
prove all methods ran in a native model, clear required-policy/global P05,
complete the full P10/platform acceptance or release the final preservation/
version/generated/status/welcome fan-in.

The new pair goes to coordinator88 and the held reviewer. SAME486 cannot start
SPEC/QUALITY until its actual required `claude-opus-5.5` / max / `long_context`
configuration is established; coordinator88 alone may assign the authorized
replacement Opus reviewer. No new reviewer/helper is created here. SPEC must
precede eligible QUALITY, and no independent verdict is claimed by this builder.

**Position:** full six-method revalidation passed on the corrected base; frozen
test/report handoff for independent review, with all prior failures and the
separate fan-in preserved. No push, tag or publication.

---

## P08 preview fan-in preparation (2026-09-24)

**Status: bounded preparation frozen; independent review and real integration
remain gated.** Authority `5b6479b52cde51af2003232c58940af9b0604d4c` releases
the original P13 map/status/generated/version-proposal input, not a restart of
the installed-consumer unit. The complete amended P13 card was read through its
exact Git object.

### Exact branch and source

New worktree: `E:\Workspace\p13c4-0924\f\w`, inside the already authorized
workspace. New branch: `jokerman-microsoft-p13-fanin-preview-0924`.
Exact base: `14b0b51c4167afd6f88acf9f7b2c409d8a7ee3c0`, the supplied local
P08 integration preview. No moving recovery tip, replacement source or new
drive-root directory was used.

**Product/proposal commit:** `e7f799ec6022a13ff8ff089eddea56106d9d1c24`.
**Sole parent:** `14b0b51c4167afd6f88acf9f7b2c409d8a7ee3c0`.
Its four Git-changed paths are:

| Path | Git-byte SHA-256 |
|---|---|
| `skills/status/SKILL.md` | `b68aa9cf74b1a1d59cd4d13eb2ce76d8ecdfda46ccc8aaf107e7d2c7ebc75ffe` |
| `tests/unit/catalog-consumers.py` | `b837fa1fdd0768488baf7621e7adffe1b677b0fd6494192ac61f51f96b3d7b50` |
| `reports/P13-skills-preservation.md` in this initiative | `8b6afd12e619f7f034d81ae4f23861dcedffb7fe781062b54bf6b7f6fd46dfd9` |
| `reports/P13-fanin-proposal.md` in this initiative | `e00f94ac78eb17b6ba41f59e8f80cbbbdd75e22b635c641a06ee73946fc87526` |

The report-only child adds this section. Its commit, parent and raw/Git hashes
are supplied with the frozen handoff. Both commits use ordinary Git controls
and the required Copilot trailer; no amend/reset or publication occurs.

### Current preservation map

The [current 126-record view](P13-skills-preservation.md#p08-preview-fan-in-map)
joins the exact 46 workflow and 80 capability audit records, cross-checking the
original `28061e434be455ca02f135b73244eaf4f73f3a69` inventory and Git blobs.
One-based WF/CAP row labels identify original audit records, not new task IDs.
Every row retains original recommendation, current path and alias/argument
mapping, method/output, accountable package/leaf, rationale, worked/recheck
example, evidence pointer and limit.

All 126 original canonical paths remain. Original-to-preview identity is
**118 changed / 8 byte-identical**; 58 original skill bodies changed since the
earlier `42abfcba` map. These are source identities, not semantic PASS counts.
The eight deliberate unchanged entries are adr-new, design-consultation,
document-generate, generate-style-learn, generate-visio, landing-report,
orientator and plan-design-review. Their original useful methods and known
limits remain visible rather than treating no diff as an unread gap.

The new view is appended without rewriting the old map's 71,781 raw bytes.
It explicitly supersedes older current/pending descriptions at its stated
layer: P08/A13 preview source is present, P10 has its accepted scoped review,
and accepted P12 pipeline source is no longer a missing method. Outstanding
native/installed/policy evidence is still separate. All 46 aliases and 35
module-capability mappings remain. General maturity stays unknown; only Visio
has the current descriptor's explicit staged proof.

Swarm is a separately attributed +1, preserving origin
`275a35447c4ad271e05816ade43ac48f1acec24f` and the full existing
[76-path record](../../../engineering/audits/2026-09-20-universal-quality/swarm-preservation.md)
and [P04 map](P04-preservation.md). The [69-role map](P09-agent-preservation.md)
is unchanged. No body, alias, historical record or valuable method was removed.
The finite bulk join used immutable audit JSON, existing map rows, accepted
catalog metadata and explicitly authored dispositions; no new production
inventory/parser, routing engine or competing task ledger was added.

### Status and already-integrated welcome

Only status's supplementary jobs display changes. Original selected map, task
IDs, cycle and profile verification remain first. The caller sources trusted
`bin/_jobs.sh`, sets `LINTEL_JOBS_NO_INIT=1` and an explicit selected-repository
`LINTEL_JOBS_DIR`, then calls `list_jobs --read-only` and `stale_jobs 24`.
Explicit active/archive/registry paths prevent inherited global selectors
from widening the read, even without a v5 marker.

Blocked, stale, absent-metadata and unknown-age records remain visible.
Partial list errors do not suppress stale observations or become exit 0;
the real nonzero status is retained with an explicit unverified diagnostic.
An absent registry remains **unobserved**, not proof of no mapped work.
No job, audit, profile or state is initialized. The status frontmatter,
already-integrated P08 welcome body and all underlying readers are unchanged.

### Actual verification and retained failures

All commands used inspected short synthetic HOME/USERPROFILE/app/temp/XDG/
Lintel roots, explicit Git ceilings and the approved PowerShell 7 launcher.
Git Bash's `/tmp` mapping was checked before and after shell groups, stayed
valid and unchanged, and emitted no host warning. This task did not delete or
change its backing directory. Source Git/EOL checks used normal controls,
not fixture-only configuration overrides.

| Check | Actual result / owned evidence |
|---|---|
| Four new status regressions before the body edit | Exit 1, four failures; `logs/status-red.*` |
| All focused status controls after the edit | **18 PASS**, exit 0; `logs/status-green.*` |
| Exact original/current map, aliases, local references and retained map tests | PASS, including **2 existing tests**; `e/map-verification.json`, `logs/verify-map.*` |
| Status frontmatter, welcome/provider/descriptor/alias and companion maps | Byte-preserved; `e/product-ready.json`, source seals |
| All seven product identity manifests and nested marketplace records | Still `0.10.0`, byte-unchanged; proposed version checked against local tags only |
| Catalog and wiki pre/post `--check` | All exit 0; `logs/catalog-*-check.*`, `logs/wiki-*-check.*` |
| Actual catalog/wiki/README generation and repeat | Exit 0, deterministic bytes; `e/generated.json` |
| Exact source/proposal scope, Python 3.9 grammar, staged whitespace and product blobs | PASS; `e/product-ready.json`, `e/product.json` |

The four RED cases cover missing layout/inherited-global selectors, partial
unknown records with stale age, trusted-helper/missing-registry observation,
and visible provider-error propagation. The later 18 include original two-map/
cycle, STARTING/BLOCKED/INCOMPLETE, profile drift/missing pin, opt-in archive and
no-write controls. The injected stale-reader exit 7 is explicitly a test fault,
not an actual failing production provider. No full repository suite or fresh
model/client/native run is claimed; Python 3.9 is grammar-only evidence.

Generated `skills/CATALOG.md`, all five wiki Markdown outputs and the README
capability section were produced only by the canonical generators. The preview
already had current generated semantics: **zero Git-content changes** remained.
Raw generator output differs from checkout CRLF, including the generator's
mixed-ending schema page; repeat output is identical. Showcase was not edited.

One retained wrapper failure occurred **after** the successful source commit:
Git commit exited 0, then the clean-state assertion saw the seven regenerated
paths and the wrapper exited 1. Read-only diff/EOL/blob checks proved exact
LF-equivalent Git content. Ordinary `git add` of those authorized outputs
produced an empty staged diff and clean status without changing raw file bytes,
rewriting source, overriding configuration or making another commit.
Both actual exits and the reconciliation are preserved in
`e/generated-index-observation.json` and `e/product.json`.

### Proposed version and held gates

The [proposal](P13-fanin-proposal.md#one-proposed-deployable-version) recommends
**`0.11.0`** for every product identity manifest and both nested marketplace
plugin entries after final integration and coordinator agreement. Current
`0.10.0` remains untouched; schema/pack/feature/managed-inventory versions are
separate. No local tag collision was observed; remote uniqueness is unqueried
and remains a final authorized check. No bump, tag or release was made.

The installed-consumer branch and its IC-F01 applying journal are unchanged.
Original owner checkout remains `ec5dfef2ea068c759e24c965095b561b20800699`;
the parked test/report branch remains `6f1de58b69ec5b69a492780e3ebfacf0afe179db`.
This disjoint preparation neither retries that installation nor clears it.

Private receipts are under `E:\Workspace\p13c4-0924\f`; the exact handoff binds
commands/exits, source and original-map seals, generated bytes and report hashes.
The complete preceding 1,196-line P13 report prefix is preserved in raw Windows
and Git-LF forms. No shared ledger, memory, ADR, provider, alias, descriptor,
version manifest or welcome body changed.

This is implementer self-review and verification, not an independent verdict.
SAME486 review is held until its actual required
`claude-opus-5.5` / max / `long_context` configuration is confirmed; no new
helper/reviewer was created. Coordinator88 receives the prepared packet for
real P08 integration re-verification. A13.1.b/.4.b and the F06 report appendix
are still absent from this preview. Original acceptance counts, IC-F01,
native-format/visual, required-policy/global P05 and final delivery gates
remain unchanged.

**Position:** P13 preview fan-in prepared and frozen; final integration,
independent review and version agreement remain with their existing owners.

---

## P13 fan-in rebased onto integrated P08 input (2026-09-24)

**Status: integrated fan-in and N2 frozen for independent review; final selected
P08 SPEC, F06 report and version agreement remain gated.** The preceding preview
appendix is appended verbatim from `9096135aa4e3f0060110db5fe17f97b1c0498da7` as
history. This section records the coordinator's later rebase release; it does not
rewrite that earlier evidence or the installed-revalidation appendix before it.

### Exact base and commits

Base: local `jokerman-microsoft-p08-integration` at
`ad1045b8b10a56fa3bcc5c310a8cad84f7ccf2c4`. It contains accepted installed closure
`371343eb`, P08/A13 `9cd2be5b` with A13.1.b/.4.b, the selected-review ancestry and
reconciliation 6. The base report was the exact accepted revalidation report,
Git SHA-256 `d2f9b5f4169beafe6e17c4b1b6effa71052b6967577e584ec07d99f1eb31db50`.
New worktree `E:\Workspace\p13c4-0924\j\w`, branch
`jokerman-microsoft-p13-final-fanin-0924`, is inside the existing authorized workspace.

| Commit | Sole parent | Exact change |
|---|---|---|
| `e922515cc552bdc6150043e0e1b1987b41693b26` | `ad1045b8b10a56fa3bcc5c310a8cad84f7ccf2c4` | N2 only: `tests/integration/catalog-installed.py`, Git SHA-256 `ece581d95c8980563ecb67f1403736713b2e7accf1bd7b901dfb20b608b33e2f` |
| `f581b07299d5f89b5e878bba112e8e769a9fab8e` | `e922515cc552bdc6150043e0e1b1987b41693b26` | Four-path integrated fan-in |

No generated Git-content drift existed, so no generator commit was created.

| Fan-in path | Git SHA-256 |
|---|---|
| `reports/P13-skills-preservation.md` in this initiative | `fc24a67bfa15df557d6bbfbfa742e010a94cff6d5f9ca2b34732b371fb4826d7` |
| `reports/P13-fanin-proposal.md` in this initiative | `87e7ff5f6f7c6dadf9fcee0b145c3bed37d5bc296992750e7caed3a9517d51f2` |
| `skills/status/SKILL.md` | `b68aa9cf74b1a1d59cd4d13eb2ce76d8ecdfda46ccc8aaf107e7d2c7ebc75ffe` |
| `tests/unit/catalog-consumers.py` | `b837fa1fdd0768488baf7621e7adffe1b677b0fd6494192ac61f51f96b3d7b50` |

The four paths were first reapplied from `e7f799ec6022a13ff8ff089eddea56106d9d1c24`
with a checked exact patch; their Git blobs equalled that original product before
integration reconciliation. Status and its tests remain byte-identical to it. Only
the map and proposal changed further.

### Integrated map re-verification and disclosed row changes

All 46 workflow and 80 capability original records, plus every current canonical
path, were rechecked against `ad1045b8`. Original-to-current identity remains
**118 changed / 8 unchanged**. The map's 58 original bodies changed since the
`42abfcba` pin also remain exact: `hooks-status` was already in that set, and no
path entered or left it (`e/body-count.json`). Swarm remains separately attributed;
the 69-role, 76-path and P04 maps are unchanged. All 46 aliases and 35 module
sub-capability names remain.

Since `14b0b51c`, the only changed canonical skill body is `hooks-status`. Its row's
method, example and limit now include the actual optional integrated doctor JSON read
through `li-events.py installer`; registration, activation and execution remain
`unverified`. Its action, path, aliases, owner and rationale are unchanged.

The other 48 changed rows changed only the limit column because an integration fact
changed:

| Integration fact | Rows |
|---|---:|
| P08 integrated source; final selected P08 SPEC/F06 outstanding | 24 |
| P10 installed A18.3/.4 separately accepted | 14 |
| A13 learning/installed source and remaining selected-P08 review | 5 |
| P13 installed consumer accepted at mechanical scope | 3 |
| A13.1.b/.4.b event source included | 2 |

The exact before/after cells for all **49 changed rows** and the count of 77
unchanged rows are in the private `e/map-reconciliation.json` and
`e/delta-summary.json`. No original recommendation, action, path, alias mapping,
owner/leaf or rationale changed.

Reconciliation 6 is recorded without changing providers. The adapter preflight
includes `bin/li-events.py`, `lib/event-catalog.json` and `bin/li-lessons.py`.
All twelve current selections include the shared core `bin/li-lessons.py`, and every
selected resource exists. The map and proposal disclose this new resource.

### N2 correction

The accepted installed review's N2 was reproduced with a real owned Windows junction
created by the approved PowerShell 7. On the old order, the linked root resolved
first and was admitted: the test created a `p13i-*` run root inside its target and
removed it in teardown. `logs/n2-red.*` records exit 1, one failing and one passing
method. The test now checks the unresolved path's symlink/reparse identity and refuses
it before resolution; the ordinary root still resolves. `logs/n2-green.*` records
both admission methods passing, no admitted linked root and an unchanged target
sentinel. No installation ran. This is a test-fixture correction, not a product change.

### Actual checks

| Check | Actual result |
|---|---|
| `catalog-consumers.py -v` | **37 PASS**, exit 0, including all **18 status** methods |
| `catalog-metadata.py -v` | **31 PASS**, exit 0 |
| `catalog-selection.py -v` | **29 PASS**, exit 0 |
| N2 affected installed-entry class | old order exit 1 (1 fail/1 pass); corrected 2/2 PASS |
| `bin/li-catalog.py --check` | exit 0 before and after regeneration |
| `bin/li-wiki-gen --check` | exit 0 before and after regeneration |
| README `tests/shape/cli-tiers-sync.sh` | exit 0 before and after regeneration |
| Integrated 126-row/alias/core-resource/link/manifest verifier | PASS; `e/map-verified.json` |

Canonical `li-catalog.py` and `li-wiki-gen --wiki-only` rewrote the catalog, README
and five wiki pages only at the raw line-ending level; their Git-content diff was
empty. No output was hand-edited. Git Bash `/tmp` remained unchanged and warning-free
before and after shell groups. No outer `LINTEL_*` selector, real profile, drive-root
directory or Git configuration command/write was used. Existing test helpers set only
their inspected fixture selectors.

A private reapplication receipt initially collided with an already created seal name
after the source patch had applied. Its summary was completed without replaying the
patch. During report preparation, a read-only body-count helper also failed before
reading source because its preflight receipt name collided; the uniquely named retry
passed. Both collector failures are retained.

### Version, prior evidence and review

The `0.11.0` product proposal remains a proposal. All seven existing manifests and
nested marketplace entries remain `0.10.0`; no tag or publication occurred. At intake,
the earlier IC-F01, preview fan-in and installed-revalidation packets rehashed unchanged:
4,861, 680 and 7,643 files. N1 remains the coordinator's CI-budget input.

Reviewer `30146eab-ddeb-467e-8f1b-8da575256ba2` owns SPEC, then eligible QUALITY, for
this product and report child. No helper or independent verdict is claimed. The F06
report appendix and final selected P08 SPEC remain coordinator gates; merge, final
version agreement and delivery remain separate.

**Position:** integrated P13 fan-in and N2 frozen; independent review and final
P08/version gates remain with their owners.
