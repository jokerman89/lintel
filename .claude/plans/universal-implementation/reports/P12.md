# P12 standalone content and editable-artifact checkpoint

Date: 2026-09-22.
Owner: original P12 builder, project session
`09666ec2-ff03-4517-9169-8793149ea8cd`.

**Status: source candidate frozen; complete rendered acceptance remains BLOCKED.**
This report advances the released A15.2 and standalone A15.3.word/.ppt unit.
It does not close A15 parents, shared binding, later formats or independent review.

## Immutable source and authority

- Starting HEAD verified exactly:
  `3ee602f026486da037844322428a7f1d50728d33`.
- Its parent: `7d316d85c0dc622d09c4ae493fa83879a74ad28a`.
- Source product: `cdb4ea660135dbddfd70bb1bfedf676d46f6b670`,
  sole parent `3ee602f026486da037844322428a7f1d50728d33`.
- Local branch: `jokerman-microsoft-universal-document-formats`.
- The report-only history starts directly at the product commit. Its exact
  commits, parents and final Git-byte hash are supplied in the immutable coordinator handoff;
  the report does not attempt to contain its own commit hash.
- Authority remains `work.json`, `spec.md`, `plan.md`, `interfaces.md`, `handoff.md`
  and the complete `packages/P12.md`, especially the released first unit.
  Original task text/checklists and common ledgers were not changed.

Loaded li-build and li-review through the Copilot adapter. Read the Universal
adapter, project startup/memory, relevant accepted ADRs, current consumers and
accepted P09 Word/PPT/narration role methods. This is implementation plus builder
self-review, not a restarted audit or an independent package verdict.

## Changed and retained behavior

The product contains exactly these eleven paths:

| Path | Change |
|---|---|
| `skills/generate-outline/SKILL.md` | Full-source coverage, stable section IDs, language choice, advisory format counts rather than universal slide-shaped limits |
| `skills/generate-write/SKILL.md` | Multi-paragraph bodies, tables/citations/limitations, uncapped supporting notes, no forced Swedish pronoun or neutral vocabulary hard gate |
| `skills/generate-word/SKILL.md` | Native-first standalone route, declared library alternatives, real edit/readback and page-render boundaries, claim/evidence-based disclosure |
| `skills/generate-ppt/SKILL.md` | Native-first standalone route, preserved source behind concise slides, actual saved notes and rendering, no global install or fixed brand/voice proxy |
| `skills/generate/SKILL.md` | Directly coupled fidelity/control corrections, owned output selection, honest staged format/shared-binding gates |
| `skills/generate-qa/SKILL.md` | No destructive truncation, format-appropriate inspection, immutable P05 requirements, missing mandatory observations cannot pass |
| `skills/generate-write/references/fidelity-and-evidence.md` | Source retention and existing P05/P07 producer/consumer procedure, without a new envelope/schema |
| `skills/generate-word/references/native-word.md` | Discovered native operations, actual reopen method and absent page-render boundary |
| `skills/generate-ppt/references/native-powerpoint.md` | Discovered operations, serialized notes creation, table readback and SVG limits |
| `tests/integration/document-format-pipeline.py` | Long synthetic source, source/control negatives, actual OOXML retention and native notes-alias regression |
| `tests/integration/document-format-pipeline.sh` | Existing-runner-compatible Bash entry; explicit interpreter and failure, no install |

Retained: skill names; `--brief`, `--from-pipeline`, template/target/audience/voice/
duration/slide-count/default/staleness/output arguments; the three Word variants;
outline.md/content.md/speaker-notes.md/design-spec.json/qa-report.json names;
Title/Subtitle/Body/Bullets/Data-viz and source-hash fields; section IDs/anchors;
format-specific role methods; Design DNA retrieval guidance; declared Node-library
alternatives. A supplied shared design is not synthesized or silently reinterpreted.

No edits to agents, generate-design/A14, P09 domain data, shared profile/review/path/
installer/registry/Swarm APIs, P08/P10 work, other worktrees or master ledgers.
No generated catalog/adapter/wiki/README edit. **Coordinator regeneration is needed
for the two changed Word/PPT source descriptions**; this worker did not regenerate
or claim a clean generated catalog.

## Exact local evidence roots

Source worktree:

```text
C:\Users\jokerman\reference-repos\copilot-worktrees\jokerman-session-setup\jokerman-microsoft-literate-adventure
```

In the remainder, `R` means this explicitly owned, retained session artifact root:

```text
C:\Users\jokerman\.copilot\session-state\0a75211f-455c-4318-864f-bc8023ce9142\files\p12
```

No personal Office document is an input. The **1,258-word synthetic brief** has ten
long paragraphs (each over forty words), a capacity table, a three-claim evidence
ledger, two local synthetic citations, reasoning across failure boundaries and a
material limitation. Its analytical capacity values are explicitly not benchmarks.

| Artifact relative to `R\target` | SHA-256 |
|---|---|
| `brief.md` | `53bf1469c034035eb7a5fbdc3144c25ef3f56cc19485299e8d87a147de3cd7e4` |
| `outline.md` | `d0dec1962d802a780f1540102269513faf2b127626cccb6c2dafde2d4c305f0c` |
| `content.md` | `392b947b56cff9fd0ac3ddea3c357a7be49666f837a137e213f1b236b2d6b2d5` |
| `speaker-notes.md` | `faa6a778c614353f02762da32050288ca3f4e1c119b2e4294c960386393ed8ff` |
| `verified-artifacts\long-technical-brief.docx` | `8c8698d12d634e4358bf5f136b6397e066470e6a3a1eada9aca9c0d1099f6bf5` |
| `verified-artifacts\long-technical-brief.pptx` | `a5910cf41de35a0ab10433089fee8c3b58201a764c765a54f9636d639cdd8e93` |
| `observations\p05-binding\context.json` | `ee79ec5923bab764abd8e0ef46b21ffc7f29496b44e32ba17967f942146ae13e` |
| `observations\p05-binding\qa.json` | `610d106e0d9787b8e40d3e01e7eba91dc1d769e1d2a126c1d2656e9d0f98111c` |

Source references are `sources\analytical-model.txt` and
`sources\failure-model.txt`. Citation paths resolve from the retained brief's
bundle root, explicitly stated in both outputs. These are literal local references,
not claims that the native API created clickable hyperlinks.
The full inventory, sizes and hashes are in `R\logs\artifact-inventory.stdout.log`.
File size is inventory only, not acceptance.

## Actual native actions

Tool discovery: `functions.list_canvas_capabilities`, Word and PowerPoint,
provider `connection:1`. Creation: `functions.open_canvas`. Editing/read/render:
`functions.invoke_canvas_action`. Host build version was not supplied; it is not
guessed from the provider ID. The native host is **not an OS sandbox** and does not
inherit the shell fixture's HOME.

### Word

`p12-long-word` created `artifacts\long-technical-brief.docx` through a 51-operation
native `batch`: title/styles, complete paragraphs, two real tables and footer.
`get_model` returned 25 paragraphs, two tables and a 1,224-word model count.
These model counts are descriptive, not a substitute for exact retention tests.

A hash-identical saved copy at `artifacts\reopen\long-technical-brief.docx` was
opened as `p12-docx-copy-open`. Native `batch` edited paragraph 0 and table 1,
row 3, column 2; `get_model` returned both edit markers. A second batch restored
both; subsequent readback returned the original exact artifact hash.
The final delivered candidate is the identical file under `verified-artifacts`.

Actual raw models:
`observations\word-authored-model.json`,
`word-reopened-model.json`, `word-edit-readback.json`, `word-restored-model.json`.
`inspect_document` returned no security warnings/external relationships, but its
feature hints overcounted tables/changes. Actual model/XML checks, not those hints,
established the two tables. No macro was executed.

**Word page inspection remains unverified.** The discovered schema has no page
render action; `inspect_document` says `richRendererSelected:false`.
Model page dimensions, styles, text and ZIP validity do not prove pagination,
clipping, table continuation or rendered page layout.

### PowerPoint

The accepted *builder artifact*, not an independent acceptance, was created as
`p12-ppt-serial` at `verified-artifacts\long-technical-brief.pptx`: 46 batched
slide/text/table operations, followed by **six separate serialized `set_notes`
calls**. All ten complete source paragraphs, both citations, both table/ledger
contexts and the material limitation survive in six distinct native notes parts.
The capacity table is also an actual editable slide table. Visible claims retain
their limiting qualifiers. There are no requested external images/assets.

`p12-final-ppt-readback` opened a byte-identical saved copy at
`reopened-final\long-technical-brief.pptx`. Native title and table edits succeeded.
`get_model` returned the title edit. That model omits table cell contents, so a
separate cell edit was read through `read_package_entry` on
`ppt/slides/slide3.xml`; the exact `10 [cell editability probe]` value appeared in
the selected cell. Restoration returned the original exact artifact hash.

Raw models are `observations\ppt-serialized-notes-model.json`,
`ppt-final-reopened-model.json`, `ppt-edit-readback.json`, `ppt-restored-model.json`.
Exact action/path/version observations are in `observations\native-actions.json`.

`render_slide` with `{}` on `p12-ppt-serial` actually returned all six slides as
960 x 720 SVG at the final artifact hash. The six files under
`observations\native-render\slide-1.svg` through `slide-6.svg` are literal
transcriptions of those returned SVG strings plus a final newline, **not fabricated
screenshots or renderer-written export files**. All six were checked for returned
text, line wrapping, table cells, positions and text baselines. Those diagnostics
are recorded in `R\logs\final-data-checks.json`.

**Complete visual inspection remains unverified.** SVG text/geometry is an actual
rendered-output layer, but glyph metrics/pixel clipping and PowerPoint desktop
behavior were not inspected. A browser canvas opened the owned SVG but returned no
page_id; no page_id was guessed. Direct UI observation of GitHub Copilot via
`computer-use.computer-use-get_window_state` was rejected by built-in safety policy.
That UI route stopped; no screenshot, foreground workaround, policy bypass or
personal Office document inspection followed.

## Discriminating failures retained

1. Initial native batches reported success but **aliased every slide's notes to
   notesSlide1.xml**, retaining only the final note. `get_model`, two actual slide
   relationship reads and the retention suite caught it: 16 methods, two failures,
   exit 1. Failed artifact `artifacts\long-technical-brief.pptx` remains at
   `ad5edca90b2be495446fcc97071c38afacd5e00ebf04afbe10cd595636c085d4`.
   It is not the final output. A two-slide native separate-call probe preserved
   distinct A/B notes, then the full deck was rebuilt at a new path. The procedure
   and negative alias test now guard this actual loss mechanism.
2. Opening an already-open path returned new apparent handles
   `p12-word-reopened`/`p12-ppt-reopened`, but actions said those instances were not
   open. The host reused the original handles. Distinct owned byte-identical
   copies, with recorded hashes and actual successful actions, supplied reopening
   evidence rather than treating the open responses as proof.
3. Initial source regression run: five expected documentary failures, plus six
   Windows long-path fixture cleanup errors. The original log/result is retained.
   Test cleanup was corrected for its exact owned native paths; later runs pass.
   A separate residue-cleanup attempt hit a read-only synthetic Git object.
   After recording its actual read-only attribute, only owned fixture files had
   that attribute cleared. All six explicitly recorded residue cases were removed.
   Both failed exits and the successful cleanup manifest remain.
4. The very first parallel scratch preflight raced creation of its empty Git
   config file; that source-status child did not run. Shared-open creation fixed
   the scratch runner. This was not a product or native-artifact pass.

## P05/P07 binding and actual refusal

P07 actually bound neutral `_default` v1.0.0, context
`p12-standalone-synthetic`, generation 1, digest
`sha256:9ad8de56182b43195f3e657f84628f8905fcfc6e42061d98a5d4e6a72bf545d0`.
The full v1 reference is retained and reverified. No optional/required company
policy activation is inferred from neutral first use.

An explicit synthetic Git target contains **byte copies of the existing approved
map/spec/plan/prompt/P12 card**, not a new backlog. The actual work-map validator
passed. P05 `prepare` bound the original leaves, source Markdown/citations, P07
reference and final DOCX/PPTX. Original helper/schema bytes were verified against
the starting commit, with Git-byte and working-byte hashes and explicit CRLF/LF
comparison recorded separately. None of those shared providers was edited.

Actual `li-review-evidence.py qa` emitted a valid v2 receipt and **exited 3**:
`word-rendered-inspection` and `ppt-rendered-inspection` remain unverified.
`qa-report.json` preserves its existing v1 fields and reports `qa_pass:false`.
Four observed controls (retention tests, Word editability, PPT editability, native
SVG-render execution) do not average away those two missing inspections.
Required independent spec/quality review remains unsatisfied; no decision or
corroboration was fabricated.

This fixture proves producer/consumer binding and refusal on the actual artifacts.
It is not a P12 independent package review, release clearance, full shared pipeline,
theme serialization or P09 domain-result binding.

## Verification and isolation record

Runtime: Python **3.11.9**, Git Bash **5.3.15(1)-release**.
Python 3.9 grammar parses; actual Python 3.9 execution is not claimed.

| Check | Actual outcome | Evidence under `R\logs` |
|---|---|---|
| Initial source regression | exit 1: five source failures, six cleanup errors | `regression-red.*` |
| Corrected source/control checks | 12/12, exit 0 | `regression-green-1.*` |
| Initial native retention | exit 1: two real notes-loss failures | `native-retention-1.*` |
| Serialized native artifact retention | 17/17, exit 0 | `native-retention-serial.*` |
| Bash entry plus physical notes-alias negative | 18/18, exit 0 | `shell-native-retention.*` |
| Final source candidate | 18/18, exit 0 | `final-regressions.*` |
| **Committed product `cdb4ea6`** | **18/18, zero skips/errors, exit 0** | `committed-product-regressions.*` |
| Source scope, 14 local links, 3.9 grammar, six returned-SVG diagnostics | exit 0 after the disclosed cleanup correction | `final-data-checks-retry.*`, `final-data-checks.json` |
| Staged product whitespace | exit 0 | `staged-product-check.*` |
| Actual artifact P05 QA | **exit 3, two mandatory inspection blockers** | `native-bound-qa.*`; exact nested command under `target\observations\p05-binding` |

The executed entry was `tests\integration\document-format-pipeline.sh` with an
explicit `--fixture-root R\checks` and
`--native-artifacts R\target\verified-artifacts`, using Git Bash's actual native
path conversion and the observed Python executable. Each log label has its
preflight/argv/environment, stdout, stderr and actual result file.
Tests without `--native-artifacts` explicitly exclude native acceptance rather than
silently claiming it. The suite does not author the positive native DOCX/PPTX
specimens or run a renderer. Its notes-alias negative test mutates an owned copy
of the actual PPTX package; that mutation is not presented as native authoring.

The allowlisted child processes clear inherited environment and explicitly bind
HOME, USERPROFILE, AppData, temp, Lintel, trusted source and target. Fixtures set
real Git ceilings, verify ancestor discovery refuses, and check their exact
initialized Git root. Source Git checks use separate configuration and
checkout-EOL handling; the initial CRLF-only status projection was not normalized
into a source edit or interpreted as user WIP.

Task-local runner/collector/binding/check scripts are retained beside `R`, in the
session's `files` directory: `p12-run.ps1`, `p12-evidence.py`,
`p12-bind-evidence.py`, `p12-final-checks.py`. Tool-managed response files were read
only at their exact returned paths and copied into owned observations.
No dependency install, credential operation, GitHub remote operation, external
upload, macro, global setting, personal profile/template scan or broad cleanup
was performed by this task. Native-host network/OS isolation is not asserted.
No whole repository suite, live CLI matrix, library fallback, PDF/XLSX/Visio
implementation, actual browser screenshot or Office desktop run is claimed.

## Review and remaining gates

Builder self-review checked specification trace, retained consumers, type/field/
argument compatibility, test behavior, links and the actual tool limitations.
No unresolved P1/P2 owned-source finding is asserted. The real notes-aliasing
loss was addressed through the verified native procedure and a regression, not by
pretending to repair the external canvas implementation.

**Independent SPEC: NOT RUN. Independent QUALITY: NOT RUN.** Formal staged review
cannot be cleared by this builder. Two rendered-inspection requirements remain
unverified, regardless of source/test results.

| Leaf | Current evidence | Remaining gate |
|---|---|---|
| A15.2 | Long source retained exactly through real editable Word and PPT notes; restrictions repaired | Independent source/package acceptance |
| A15.3.word | Native creation, saved-copy reopen, paragraph/table edit/readback, exact retention | Actual page renderer and complete rendered layout inspection; independent review |
| A15.3.ppt | Native editable six-slide deck, distinct full notes, saved-copy edits, actual SVG rendering | Permitted complete visual inspection; independent review |
| A15.3.shared / A15.1 full shared binding | No new design/domain/workflow contract imported or invented | Explicit release of accepted predecessor interfaces and joined scenario |
| A15.4.pdf | Existing export possibilities inspected only | Later release; real writer/export, page/text fidelity |
| A15.4.xlsx | Existing workbook possibilities inspected only | Later release; real recalculation, formula/cache integrity and reopen |
| A15.4.visio | Existing diagram possibilities inspected only | Later release; writer, connectors/labels, rendered editable reopen |

MasterSession owns independent review, integration, generated fan-in and remote
delivery. This same original P12 owner continues after an explicit later release.
No push, PR, main merge, amended commit or reset was performed.

Cycle position: BUILD candidate frozen for independent REVIEW. The canonical
SENSE -> SCOPE -> DEFINE -> DISCOVER -> PLAN -> BUILD -> REVIEW -> SHIP -> CAPTURE
cycle is unchanged; resume remains a utility.

## Generation presence test reconciliation (2026-09-23)

Authority: recovery coordinator `88aecc43`'s scoped 21:08 release for the legacy
unit-test defect, not new format/product work. New branch
`jokerman-microsoft-p12-skill-presence`, worktree
`C:\Users\jokerman\p12-presence-0a75211f\src`, starts exactly at
`84e63a4e2bf3a0109b09abb4c5332b30b51276cf`.
Product **`49ba9eb5175a1e689babb6a76420fe7a1ca1e2f8`**, sole parent that base,
changes only `tests\unit\generate-skills-present.sh`. This appendix is a separate
report-only child; its exact identity/hash is supplied in the handoff.

The test now rejects TEMPLATE ONLY claims for concrete PDF/XLSX source methods
and requires their four existing helper files and five integration support/
entry files. Visio retains the original TEMPLATE ONLY/L-001 assertion.
The header states source presence is not native/runtime verification.
All original frontmatter, color, mapping and legacy-builder checks remain
byte-identical; 21 skill/helper/mapping/integration inputs are unchanged.
No skill, product, CATALOG.md, capability record or other test was edited.

| Actual case | Test exit / result |
|---|---|
| Unchanged test at exact base (RED) | 1; exactly the two PDF/XLSX marker failures, 24 other PASS lines |
| Corrected test (GREEN) | 0; 35 PASS, no failures or stderr |
| PDF description regains TEMPLATE ONLY, disposable copy | 1; exactly the new concrete-method assertion fails |
| Visio marker removed, separate disposable copy | 1; exactly the original slot assertion fails |
| PDF `check_pdf.py` deleted, separate disposable copy | 1; exactly that required-file assertion fails |
| XLSX `check_xlsx.py` deleted, separate disposable copy | 1; exactly that required-file assertion fails |
| Committed source | 0; 35 PASS. Private inspector exits 1 on the retained Bash `/tmp` warning, not a test failure |

Each mutation changes only its named path in a fresh copy; the real worktree is
unchanged by all checks. Every execution has an allowlisted CI-like environment,
short owned roots, synthetic HOME/USERPROFILE/AppData/temp/XDG and **zero
LINTEL_* exports**. Source Git uses normal configuration/hooks separately.
The committed rerun's `could not find /tmp` stderr and inspector failure are
preserved; no global directory repair, suppression or rerun was attempted.

Evidence root:
`C:\Users\jokerman\.copilot\session-state\0a75211f-455c-4318-864f-bc8023ce9142\files\p12-skill-presence`.
Preflights, actual stdout/stderr/exits, file hashes and private driver snapshots
are retained per case. `green\results.json` SHA-256:
`20145c256480595ad27c92c86df23c2c978cf65cc9e92e4ce4ffae949c6e166b`;
`mutations\results.json`:
`b61511586f563615529209c424deb9c2144e98de626de154789e0bdbb2ef3507`;
`seal\results.json`:
`209cb72c8f8a13652526f8b9c26b7bf44ebac4d74aab94bf39576105e5176888`.
This is source-test reconciliation only, not format maturity, native inspection,
parent closure or independent acceptance. SAME31 bounded SPEC then eligible
QUALITY and coordinator intake remain required. No native/network/install,
structured decision/corroboration, jq, push or remote action occurred.
