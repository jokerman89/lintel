# P09 staged specialist depth

**Authority/base:** `56df03d1757b483048e68ac846a3863ff558bb6b`, parent
`5f07eb33ecacb9a5080487867c8271d632287c39`. The selected approved
`../work.json` and complete `../packages/P09.md` govern this original-owner unit.
The branch is `jokerman-microsoft-universal-specialist-depth`.
**Frozen content product:** `4a9b946dab02723ec986d2dd8e592f2fd765a326`.

## Intended result and boundary

Improve the existing roles' actual decision methods, examples, evidence and
responsibility boundaries, preserving every original role and native frontmatter.
Advance A17.1-.4 and only the knowledge portions of A09.3/.4. The 69-row
[preservation map](P09-agent-preservation.md) records changes and justified retention.
This is implementation against the approved plan, not a replacement audit or backlog.

P08 is not released for runtime binding. A09.1/.2/.5 and A17.5 remain gated;
no dependency-bound parent closes from this content unit. No shared schema, parser,
domain-loop/result format, installer, lifecycle/router, generated output, shared
plan/memory/ADR, P11 browser surface or P10/P08 work-in-progress is owned here.
Review remains the coordinator's separately attributable spec/quality gate.

## Startup and evidence handling

Read all 69 original audit rows and all 69 current canonical bodies before edits,
the complete P09 card, selected work map/spec/plan/interfaces/handoff, both adapters,
repository entry/principles, relevant memory and ADR-0012/0026/0028/0029.
ADR-0028 already supplies the approved, limited supersession of ADR-0012; native
model/memory frontmatter is retained, not silently migrated or deleted.

Source HEAD matched the exact authority (exit 0). With global/system Git config
excluded, initial source status reported CRLF projection differences (exit 0,
not a clean-tree claim). A source-only, per-command `core.autocrlf=true` comparison
then established a clean tracked tree (exit 0); no files or persistent Git settings
were normalized. Source Git checks are separate from fixture discovery isolation.

The session-local launcher uses an explicit environment allowlist, verified
non-reparse owned paths, synthetic HOME/USERPROFILE/temp and derived Lintel paths.
It removes inherited token, policy, Python and shell redirects by constructing a
new child environment. Fixture processes run beneath an owned Git discovery
ceiling. Stdout/stderr streams are opened under `.claude/runtime/p09-depth/logs/`
before child start; metadata retains argv, environment, timestamps and exit code.
No personal home/profile, live/customer data or authenticated GitHub access occurs.

Two launcher defects caused four failed invocations before any child ran (the two
source probes were attempted in parallel): PowerShell's read-only HOME variable
collision, then multiple executable matches passed as one filename. Each returned
1. They were corrected in the private launcher; empty capture files are not test
evidence. No policy override, installation or shorter-path workaround was used.
The first RED summary exposed a long assertion line despite a line-count limit;
the complete streams were already captured in owned logs. Later summaries also cap
each displayed line at 500 characters. No failing run was relabelled as passing.

The preliminary regex-based local Git-config query returned 1 and was not treated
as absence of hooks. Exact per-key preflight found the configured source Git-admin
hooks directory and safely refused (exit 1) pending inspection. A scoped read showed
only sample hooks; the second preflight verified no active commit hooks and passed
(exit 0). No hook was disabled or edited. Local commits use a per-command
`jokerman89` noreply identity and the required Copilot trailer, not global Git changes
or GitHub authentication.

## Acceptance state

The content unit is frozen for independent review: **61 role bodies improved, eight deliberately
retained, all 69 names/frontmatters preserved**, with five domain decision references
and a 69-row evidence/preservation map. The exact committed product passes 12/12
scenario methods, all seven selected existing structural scripts and preservation/
ownership checks. This report-only update does not alter that tested product.
No independent package acceptance, domain runtime integration or whole-initiative
completion is claimed.

| Leaf | Content result | Remaining acceptance |
|---|---|---|
| A17.1 | Every original role mapped to inputs, decisions, responsibility, output, example and evidence limits | Dependency-bound parent and independent review remain open |
| A17.2 | Actual host operations/configured resources in changed bodies; native hints preserved under accepted ADR-0028 | No new native registration, model selection, memory binding or generated adapter claim |
| A17.3 | Blank-brief Plan mode precedes narration; later critique stays distinct; N01 observes that bounded sequence | Independent critique and real dispatch/receiver integration still required |
| A17.4 | Unique specialties retained; concrete methods replace arbitrary policy/vendor/lifetime/retention assumptions | Applicable real policy, implementation and host evidence are not supplied by prose |
| A09.3 | Invariant/topology, failure isolation, percentiles, locking/recovery/replay/late data/lineage methods | A09.1/.2 prerequisites and actual domain handoff remain gated |
| A09.4 | Security applicability, state-compatible rollout, queryable SLO/cost inputs, consumer compatibility and uncertainty | Actual domain results/mandatory failures/iteration acceptance remain gated |
| A09.1/.2/.5; A17.5 | Not implemented in this staged unit | Explicit coordinator release in this same owner session |

## Product chain

1. `3e6d5730343a3a2fcce630ef28f0778f22ce3784`, parent
   `56df03d1757b483048e68ac846a3863ff558bb6b`:
   engineering/operations methods plus TA/DA/DH/TQ reference links/content (41 paths).
2. `faf95445abc27a094411246e1426b141788468a7`, parent
   `3e6d5730343a3a2fcce630ef28f0778f22ce3784`:
   specialist evidence/responsibility methods plus SC reference; includes the final
   APIDesigner reviewer-versus-implementer clarification (31 paths, one overlaps step 1).
3. `4a9b946dab02723ec986d2dd8e592f2fd765a326`, parent
   `faf95445abc27a094411246e1426b141788468a7`:
   scenario checks, both initial reports and the final customer-visible RFP
   limitation clarification (five paths). This is the frozen tested content product.

This final report is a report-only direct child of that product; the immutable
report commit ID is supplied in the coordinator handoff rather than self-hashed here.
Product tree: `e650a15d4ce8149a4309c00be91ca4b34b6c8af5`.
Preservation-map Git blob: `52ae1a5fc43b2d2e9cabc445f37bf851324595b4`;
Git-byte SHA-256: `7bbe6af02f74684dfc96829c13a1f5c8935c8d2372c171d33bcf4fcdda7997dc`.
There are **75 unique changed paths** from the exact authority, listed below.

No commit is amended, pushed, merged or published. The original base ancestry and
accepted P03/P05/P06/P07/native-path products remain in place.

## Checks actually run

Each label below has `.json`, `.stdout.log` and `.stderr.log` under
`.claude/runtime/p09-depth/logs/`. JSON records the actual absolute argv, executable,
working directory, explicit synthetic environment, times and exit code; the table
uses root-relative command names for readability. Fixture checks never inherit
source-checkout Git settings. Source comparisons use the source-only mode and its
explicit CRLF projection, without rewriting files.

Actual tools: Python **3.11.9**, Git **2.55.0.windows.3**, Bash **5.3.15(1)**,
on Windows. No dependency was installed or restored.

| Evidence label | Actual command/check | Exit and result | What it does not establish |
|---|---|---|---|
| `roles-red` | `python tests/behavior/agent_contract_scenarios.py --root <source>` before content edits | 1; 12 methods, 24 failing subcases and 4 missing-artifact errors | Expected baseline failure, not a passing run or model evaluation |
| `roles-green-v1` | Same Python entry after content/map additions | 0; 12/12 methods | Marker/link and synthetic-example coverage, not semantic completeness |
| `scenario-entry-v1`, `scenario-entry-final`, `scenario-entry-candidate` | `bash --noprofile --norc tests/behavior/agent-contract-scenarios.sh` | Each 0; 12/12 methods, no skips | Working-content evidence; committed-tree rerun is recorded separately |
| `frontmatter-v1` | Existing `tests/shape/frontmatter-lint-all.sh` | 0; 127 skills and 69 roles, optional native metadata valid | Native client discovery/model availability |
| `categorized-v1` | Existing `tests/shape/agents-categorized.sh` and its unit delegate | 0; all 69 roles and eight categories | Role behavior |
| `ta-shape-v1`, `da-shape-v1`, `sc-shape-v1`, `dh-shape-v1`, `tq-shape-v1` | Five existing `tests/shape/<module>-module-contract.sh` scripts | Each 0 | Static declarations/hook-source presence, not running hooks/domain loops |
| `preservation-v1`, `preservation-final` | Private source-only `p09-preserve.py` against exact authority | Each 0; 69 frontmatters, eight whole retained bodies, two P03 executable recipes, eight P05 passages; five module bodies differ only by reference links; ownership exact | Native adapter or independent acceptance; later refinements need final rerun |
| `source-diff-check-v1`, `source-diff-check-final`, staged group checks | Source/staged `git diff --check` with per-command CRLF handling | Each 0 | Runtime behavior |
| `python-grammar` | `ast.parse(..., feature_version=(3,9))` on the new check | 0 on Python 3.11.9 | Actual Python 3.9 execution, which was not run |
| `commit-preflight-v2` | Explicit environment/tool/local-hook inspection | 0; inspected source-admin path contains no active commit hooks | Any enterprise hook registration/enforcement |
| `committed-scenarios` | Actual Bash scenario entry on `4a9b946` | 0; 12/12 methods, no skips | Same documentary/synthetic limits, not all-role model behavior |
| `committed-frontmatter`, `committed-categories`, `committed-ta`, `committed-da`, `committed-sc`, `committed-dh`, `committed-tq` | All seven selected existing shape scripts on `4a9b946` | Each 0 | No live hook/module/client acceptance |
| `committed-preservation` | Source-only preservation guard on `4a9b946` | 0; 69 frontmatters/eight bodies/two P03 recipes/eight P05 passages and five link-only module changes preserved; no unowned path | No dependent runtime binding |
| `product-freeze-check` | Git-byte/parent/trailer/manifest guard | 0; clean committed source, exact 75-path manifest, map hash and product tree above, required trailers and shell mode 100755 | An identity check is not independent acceptance |
| `fixture-git-ceiling` | `git rev-parse --show-toplevel` inside the owned fixture process | **128 expected refusal**; no source-root discovery | Negative isolation probe, not a product-test failure or success-shaped fallback |

The six executable examples calculate the stated percentile counterexample, contrast
thresholds, strict/tolerant enum toy outcomes, replay totals, retry/SLO arithmetic and
narration duration. They do not execute a token library, consumer serializer, database,
broker, browser or domain-loop producer/consumer. The intentionally gated
`tests/integration/domain-result-handoff.sh` is **not** created or claimed to run.
No full suite, generated-output refresh, consumer installation or live policy/model
matrix was run; those belong to the subsequent released/integrated work.

## N01: bounded native-tool observation

One synchronous `functions.task`, `agent_type="general-purpose"`, named
`p09-narrative-demo`, read only DemoNarrativeArc and DemoNarratorJunior. No commands,
files, home/profile/memory reads, child actors or external actions were allowed.
The tool returned a result; no separate host actor ID/model ID was exposed, so
neither is invented. This is source-guided use of two canonical methods through
one real native binding, not proof that either role name is natively registered.

Input was explicitly synthetic: six-minute English operations demo, one minute for
questions, no arc/script, a supplied capture showing one accepted submission and a
second flagged duplicate, plus a supplied static fallback. No production reliability,
money claim, detection mechanism or live access was supplied.

Observed output returned `arc-DRAFT.md` in the response and narration consuming
exactly that arc. Scenes were S1 00:00-00:45 framing, S2 00:45-02:00 acceptance,
S3 02:00-03:30 duplicate, S4 03:30-04:30 evidence/unknowns, S5 04:30-05:00 recap,
S6 05:00-06:00 questions. The recovery line named the supplied static synthetic
fallback as **not a live run**; a second failure was honestly acknowledged instead
of inventing a customer success story. No script prerequisite was requested.

The role content used has LF-normalized SHA-256:

- DemoNarrativeArc: `95fa4e30018a75c3fc21ceaa94631219ef64b973e780cba877710c6997c52f8b`
- DemoNarratorJunior: `0897544648ea6724cd8e54d5efaaaf9274ce4e04a1c81677bb338c1bf7d37ae0`

Those bodies are unchanged after N01. Asset contents/UI details were not inspected;
this was not rehearsal, independent critique, a multi-host benchmark or A17.5
acceptance. The same actor performed planning and narration and explicitly did not
claim independent approval.

## Sources and self-review

New technical prose is original synthesis with retained attribution. Direct primary
reads included RFC 8725/9700 text, RFC 8252 native redirects, PostgreSQL index/default
documentation, Apache Beam watermark semantics, Google SRE overload/retry/SLO
methods, W3C contrast guidance, Kubernetes QoS, EDPB role guidance, Commission AI Act
Articles 6/43 and GSAP's current pricing statement. Exact method links live with the
roles/references rather than a copied standards body.

Search summaries were leads, not evidence: overstatements about percentile sums,
universal OAuth state/PKCE and confused GDPR/AI Act article numbers were not adopted.
Direct primary text settled the corrections. Some HTML retrievals returned only a
section or no further content; RFC plain text supplied the needed sections.
Terraform's rendered provider-requirements page did not expose its body through the
fetch tool and two proposed public raw-source locations returned 404. Its primary
documentation link and invocation-time version verification remain; no current
provider/runtime validation is claimed. No proprietary standard was copied.

`li-build` and `li-review` were loaded. This implementer's review checked the staged
content scope first, then technical/example consistency and policy/authority limits.
Self-review corrected remaining canned severity totals, JWT report-column meaning,
template-versus-required library wording, effective Actions permissions, probe/QoS
assumptions and customer-visible RFP limitations. These are not independent findings
or acceptance. **Outstanding identified within this staged content scope: P1 0,
P2 0, P3 0** after the recorded corrections; unrun/gated acceptance remains below.
No actual enterprise-policy or legal-compliance clearance is claimed.

The simpler design is retained: five on-demand domain references plus surgical body
corrections, no new scheduler/schema/role family or duplicated policy implementation.
Eight already useful/accepted role bodies remain entirely unchanged.

## Exact owned changed paths

This list is the source-diff manifest, not a second task ledger. Paths are relative
to the source checkout; unchanged retained roles are listed in the preservation map.

```text
.claude/plans/universal-implementation/reports/P09-agent-preservation.md
.claude/plans/universal-implementation/reports/P09.md
agents/communication/BlogPostDrafter.md
agents/communication/EmailCustomerDrafter.md
agents/communication/LinkedInPostDrafter.md
agents/communication/SlideNarrationCritic.md
agents/compliance/EUAIActReviewer.md
agents/compliance/GDPRReviewer.md
agents/compliance/SOC2Reviewer.md
agents/customer/CustomerEmpathyCheck.md
agents/customer/DemoNarrativeArc.md
agents/customer/DemoNarratorJunior.md
agents/customer/ExecutiveBriefingDrafter.md
agents/customer/PostDemoFollowup.md
agents/customer/ProposalDrafter.md
agents/customer/RFPResponseDrafter.md
agents/devops/DevOpsToolchain.md
agents/devops/GHActionsReviewer.md
agents/devops/K8sManifestReviewer.md
agents/devops/PerformanceAnalyzer.md
agents/devops/TerraformReviewer.md
agents/doc-gen/PPTNarrativeArchitect.md
agents/doc-gen/WebExperienceCritic.md
agents/doc-gen/WordTechnicalEditor.md
agents/engineering/ADRDrafter.md
agents/engineering/APIDesigner.md
agents/engineering/AccessibilityChecker.md
agents/engineering/Architect.md
agents/engineering/BackendArchitect.md
agents/engineering/CapacityPlanner.md
agents/engineering/ChangelogMaintainer.md
agents/engineering/ContextBudgetAdvisor.md
agents/engineering/ContractTestArchitect.md
agents/engineering/CostAnalyzer.md
agents/engineering/DataPipelineDesigner.md
agents/engineering/DatabaseDesigner.md
agents/engineering/DebugForensics.md
agents/engineering/DeploymentEngineer.md
agents/engineering/DocWriter.md
agents/engineering/Explorer.md
agents/engineering/FrontendBuilder.md
agents/engineering/MigrationPlanner.md
agents/engineering/Migrator.md
agents/engineering/ObservabilityArchitect.md
agents/engineering/PerfBudgetEnforcer.md
agents/engineering/Planner.md
agents/engineering/ReadOnly.md
agents/engineering/ReleaseEngineer.md
agents/engineering/ResearchSynthesizer.md
agents/engineering/SanityChecker.md
agents/engineering/SystemArchitect.md
agents/engineering/TestRunner.md
agents/frontend/FrontendArchitect.md
agents/frontend/MotionDirector.md
agents/frontend/ShaderEngineer.md
agents/frontend/TypographyCurator.md
agents/security/DependencyAuditor.md
agents/security/JWTSecurityReviewer.md
agents/security/OAuthFlowReviewer.md
agents/security/PrivacyBoundaryAudit.md
agents/security/SBOMAuditor.md
agents/security/SecretsScanReviewer.md
agents/security/SecurityAuditor.md
skills/da/SKILL.md
skills/da/references/decision-methods.md
skills/dh/SKILL.md
skills/dh/references/decision-methods.md
skills/sc/SKILL.md
skills/sc/references/decision-methods.md
skills/ta/SKILL.md
skills/ta/references/decision-methods.md
skills/tq/SKILL.md
skills/tq/references/decision-methods.md
tests/behavior/agent-contract-scenarios.sh
tests/behavior/agent_contract_scenarios.py
```

## Concrete remaining binding seams

- **P08/P09:** after explicit release, consume the exact accepted selected-work reader
  and reference; no mtime initiative guessing or copied tasks. A09.1 must carry the
  real work/profile/revision, decisions, controls/evidence and next owner using the
  accepted P05 v2 evidence/QA requirements and P07 v1 profile reference.
- **A09.2/.5:** replace the unchanged shell-shaped domain loops with the approved
  agent-owned checkpoint/recovery procedure, verify source/target lookup, actual
  producer/consumer parsing, Windows-safe artifact names and interrupted/mandatory
  failures. Existing module scoring/profile defaults are deliberately not repaired
  by this content-only unit and cannot be taken as safe runtime acceptance.
- **A17.5:** match actual dispatch to receiver modes (especially artifact-only
  Migrator, planning-only ReleaseEngineer, narrative Plan versus Critique), and
  independently review resulting outputs without the reviewer repairing them.
- **P11:** frontend bodies now recommend framework-compatible/no-motion/no-shader
  choices; later shared design/renderer changes must represent and test them.
  No browser skill/helper or shared design schema is touched here.
- **P12/P13/coordinator:** actual format extraction/rendering/editability, native
  discovery descriptions, packaged reference closure and common generation/final
  integrated review remain with their owners. Frontmatter was intentionally not
  rewritten to advertise unobserved host support.

Request MasterSession's independent **spec review, then eligible quality review**
of the immutable content candidate and both P09 reports. This original owner stops
before runtime binding and waits for explicit release in the same session.
Local runtime logs/private harnesses are retained as owned evidence for that review,
not published or removed through broad cleanup.

**Lifecycle position:** SENSE -> SCOPE -> DEFINE -> DISCOVER -> PLAN -> BUILD ->
REVIEW -> SHIP -> CAPTURE. This is a staged BUILD content handoff for review; resume
is only a utility, and no parent or final delivery gate is closed.

## Data-core second unit (2026-09-22)

Authority is coordinator `3ee602f026486da037844322428a7f1d50728d33`,
P09 "Data-core second unit" and the P09 interface section. Attributable start is
`6581339c0442d11ea070ebf06a707291bbf6132b`, clean source/index. The earlier content
unit is independently accepted by `19eb776018de927691947beb84948347fd2c132d` and
integrated with its full history at `4983af2693491d336be3ed9e036742c4589f0cfb`;
its report above remains the historical content freeze, not a runtime verdict.

This unit implements only a data request/checkpoint/result schema, one helper and
thin validate/record/verify/summary CLI, a domain-handoff reference, focused integration
tests and this owned report. It consumes the **accepted** P03/P05/P07 APIs directly.
The twelve selected dependency files match the new authority exactly (read-only
Git comparison exit 0). No P08 helper is imported, executed, reproduced or required
by these tests. Existing module/adjacent skills, accepted roles and shared contracts
remain unchanged; actual installed wiring and task/package construction stay gated.

The implementation target is a non-clearing data record, not a scheduler or approval
service. The caller fixes expected domains/checkpoints, receiver modes, original
output preimages and immutable P05 QA obligations in a request before observations.
An initial real P05 context is historical input identity; the final caller-selected
P05 context is prepared separately after artifacts exist and never embedded in its
own hashed handoff. Verify and summary must recheck that final context, the live
P07 reference/policy, raw request/result files and artifact evidence. Recording
requires the explicit original expected file state and reports that verification
was not performed. All outputs retain `release_clearance: false`.

Planned evidence: real accepted profile/context producers -> data CLI publication ->
fresh verifier -> existing P05 QA consumer; missing/failed required checkpoints,
malformed/duplicate/nonfinite JSON, changed obligations, late edits, unsafe/linked
paths, stale policy and artifact/context drift; copied-source trust/refusal checks
labelled separately from installation. Preserve the 12 accepted content checks as
their own run. Spec then quality self-review precede an immutable checkpoint for
the existing independent reviewer. No A09/P09/A17.5 parent acceptance follows.

### Implemented data-core scope

Only these seven paths differ from `6581339`:

```text
lib/domain_result.py
lib/domain-result-schema.json
bin/li-domain-result.py
skills/full-engineering-pass/references/domain-handoff.md
tests/integration/domain-result-handoff.py
tests/integration/domain-result-handoff.sh
.claude/plans/universal-implementation/reports/P09.md
```

The original 69-row preservation map, all role bodies/native metadata, five module
procedures/references, adjacent skills, P03/P05/P07 helpers/schemas, P08/core/state,
installers, Swarm, generated outputs and shared authority files remain unchanged.
This section is appended to the exact historical content report; no previous
failure/evidence text is rewritten.

The [data contract reference](../../../../skills/full-engineering-pass/references/domain-handoff.md)
documents the actual APIs, shape/verification distinction, file bounds and exit codes.
Request construction remains with the caller using **actual P05 prepare output**;
no task/package parser or P08 work-context substitute was added. The initial context
is historical input identity; current final context comes from a second external
P05 prepare after the request/starts/results/artifacts/evidence exist.

The domain schema references, rather than duplicates, P05 shared object definitions.
The helper delegates work/context/control/QA semantics to P05, live profile and
required-policy verification to P07, and explicit rooted no-link reads/expected-state
publication to P03. Its small shape reader handles only the new wrapper schema.
`record_checkpoint` requires the original expected state both in the request and
as a required call argument, compares before staging, passes it unchanged to P03's
atomic writer, and verifies readback. It never learns overwrite permission from a
late read. Recording stale-profile observations is allowed only as non-clearing
data publication; fresh verification refuses them.

`verify_result` checks every expected result and `summarize` invokes that same
fresh verifier; neither accepts serialized verified flags. Complete raw evidence
produces a real P05 v2 QA envelope checked through `verify_qa`. Missing checkpoint,
artifact, source binding, immutable obligation or live profile blocks. Required
failure/error/unknown/zero/skipped observations cannot be hidden by a score.
Negative mandatory producer status also remains visible even when a typed control
claims grounded N/A. Advisory failures remain advisory, not silently mandatory.

Receiver mode is explicit nonblank role-specific data, matched unchanged from
request through start and result. It is not a curated model/role capability registry
or action authorization. Migrator's artifact-only/authorized-execution and
ReleaseEngineer's planning-only/authorized-execution modes are both representable.
No role, domain action, phase, task completion or automatic recovery is executed.
Every receipt/verdict retains `release_clearance: false`; independent review is
explicitly `not_evaluated` by the data verifier.

### Data-core verification and failures retained

Outer command logs use `.claude/runtime/p09-depth/logs/dc-*.{json,stdout.log,stderr.log}`.
Each integration child opens separate stdout/stderr files **before** execution under
`.claude/runtime/p09-data-core/<run-id>/<test-method>/`; per-command JSON records argv,
cwd, exit, synthetic HOME and Git discovery ceiling. Test subprocesses reconstruct
an allowlisted environment, retain PATHEXT and use synthetic HOME/USERPROFILE/temp,
XDG/AppData, pack/profile/audit paths. No inherited credentials, policy redirects,
Python path or shell startup injection is retained. All Git fixture settings are
per-command and fixture-scoped; source checks remain separate.

| Label | Actual outcome | Scope/limitation |
|---|---|---|
| `dc-red-link` | Runner exit 1; one expected RED failure, absent new CLI child exit 2 | Real P07/P05 fixture setup ran; no placeholder P08 or success claimed |
| `dc-first-green` | Runner exit 1; 3 methods, one failure | Live drift correctly refused, but wrapper omitted P07's error **code**; fixed by preserving `ProfileError.code` in explicit diagnostics |
| `dc-integration-v1` | Exit 0; 29/29, zero skips, 125.712s | First full data-core suite, not final expanded suite |
| `dc-focused-v2` | Exit 0; six additional methods, 14.038s | Late staged edit, both receiver modes, missing checkpoint, native long publication, negative mandatory status and invalid direct objects |
| `dc-integration-v2` | Exit 0; 37/37, zero skips, 151.469s | Expanded full working-source suite |
| `dc-final-extra` | Exit 0; 2/2, 7.391s | Added byte-bound/empty-mode refusal plus real fresh CLI/QA link on final precommit source |
| `dc-neutral-pin` | Exit 0; 1/1, 4.458s | Real P07 neutral producer through actual fresh summary; no required enterprise-policy activation is claimed |
| `dc-content-preservation` | Exit 0; original 12/12 documentary/synthetic methods, 0.646s | Separate retained content evidence, not new runtime/role acceptance |
| `dc-ownership-grammar` | Exit 0 | Exact seven-path scope, unchanged accepted content/map, historical report prefix, ASCII code/schema, local links and Python 3.9 grammar; actual runtime remains Python 3.11.9 |
| `dc-commit-preflight` | Exit 0 | Same explicit synthetic source environment; configured source Git-admin hooks inspected inactive, no hook/policy/config bypass |

The final new integration script contains **39** methods, including real neutral-pin
consumption without an enterprise-policy claim. Its committed-tree run
and immutable product/report identities are recorded in the final freeze below;
do not relabel the earlier 29- or 37-method run as that final run.

Actual communication evidence includes: real P07 producer and pin -> actual P05
prepare/bind_work -> data CLI start/result publication -> fresh CLI verify/summary
-> P05 `verify_qa` and actual QA CLI. A separate case uses actual `li-review-log` and
`li-review-read`: later rejection blocks the outer gate. Its actor/corroboration
values are explicitly synthetic fixtures, not claims of real independent reviewers.
The domain summary still says `review: not_evaluated` and never grants release.

Negative coverage includes five-domain composition with a missing result despite
all other scores at 100, an additional missing checkpoint after fresh preparation,
changed/missing/extra selected files, artifact/evidence digest drift, omitted/
retyped/downgraded obligations, substituted applicability/policy, raw verified flags,
request/start/actor/mode mismatch, live same-mtime policy drift, deleted pin without
bootstrap, wrong profile target/context, malformed/duplicate/nonfinite/oversized
JSON, missing explicit preimage, late user edits and an injected edit during P03
staging. Observed failure precedes any fixture restoration. An injected publication
error is reported without overwrite; temporary staging is cleaned by the accepted
P03 writer. No source/caller rollback is attempted.

Native Windows evidence: a dangling publication symlink is refused without writing
its target; original outside sentinel bytes survive. A **290-character destination**
is actually published/read back through P03 and checked by the CLI at its original
logical location. This is data-file I/O/syntax evidence, **not** full P05 Git/snapshot
verification at that length or an installed-client long-path acceptance claim.
The role-mode cases verify transport/consistency only, not a new native role run.

Copied-source checks copy only the explicit data-core/P03/P05/P07 resources and
invoke the new CLI from a conflicting target containing hostile same-named modules.
Missing primary helper, P05 helper or domain schema refuses before output and never
executes target code or creates bytecode. This proves that scoped trusted-source
boundary, not installation/preflight closure; no installer, package or tool restore
ran. Python 3.9 execution, non-Windows runtime, P08-specific reader/selection/grouping/
warmup, module procedures/iteration recovery, installed wiring and live model/
domain behavior all remain unrun and explicitly outside this released data unit.

### Data-core self-review and remaining owner gates

`li-build` and `li-review` were invoked for this unit. This implementer's spec pass
checked the seven-path boundary and real accepted-provider consumption before
quality review. Quality refinements preserve typed profile diagnostics, reject
non-object record kinds explicitly, count the publication newline within byte
bounds, keep negative mandatory status from disappearing under N/A, and accept
role-specific receiver-mode strings without inventing a role/model registry.
Existing role knowledge was not reopened.

Identified outstanding findings in this **data-core self-review**: P1 0, P2 0,
P3 0 after the recorded fixes. This is not independent acceptance. Source/version,
snapshot and evidence consistency do not authenticate actors or prove log truth/
semantic correctness; P05's independent context/actual corroboration gate remains.
Publication assumes one serialized owner, not a new cross-process transaction or
race-proof authorization system.

The next owner is the existing independent P09 reviewer through MasterSession:
review data-core SPEC, then eligible QUALITY on the immutable checkpoint.
After acceptance, coordinator/P10 own actual installed-resource/preflight closure.
P08 must still explicitly release its accepted semantic work/iteration seam before
P09 wires module/adjacent procedures, original task/package grouping, warm readers
or cold workflow resume. P11 A14/frontend and P12 format integration are not supplied
by this data unit. No dependency-bound A09, P09 or A17.5 parent closes here.

### Immutable data-core freeze

**Product:** `ea92df855bc1e0dfea5c9deb913bdcdf2e5faa8c`, sole parent
`6581339c0442d11ea070ebf06a707291bbf6132b`.
Product tree: `b9e3314e32eb3fc2c92b25506923251d5f26aff5`.
This final evidence append is a **report-only direct child**, whose immutable ID
is sent with the coordinator handoff rather than embedded in its own hash.
No code/schema/test/reference byte changes follow the product commit.

| Committed-tree check | Exit | Actual result |
|---|---|---|
| `dc-committed-integration`: actual `bash tests/integration/domain-result-handoff.sh` | 0 | **39/39**, zero skips, 333.167s on the exact product; per-child logs under `.claude/runtime/p09-data-core/8a3fd5129645/` |
| `dc-committed-content`: unchanged `bash tests/behavior/agent-contract-scenarios.sh` | 0 | **12/12**, zero skips, 1.005s; documentary/synthetic content checks remain separately labelled |
| `dc-product-freeze`: source-only Git-byte/scope/parent/trailer guard | 0 | Clean source/index, exact seven paths, required trailers, CLI/runner executable modes |
| `dc-posttest-preservation`: source-only post-run preservation/grammar/link check | 0 | Accepted roles/modules/shared helpers/map unchanged; historical report prefix intact; 39 test methods; all owned synthetic outer-temp fixtures removed |
| `dc-staged-check` | 0 | All seven newly staged files, not only the previously tracked report, passed `git diff --cached --check` |

The final suite exceeded the initial synchronous wait, then completed normally.
The same process/output was collected; it was not restarted, detached or relabelled.
Raw stdout/stderr was captured in owned files before execution throughout.
Actual runtime remains Windows/Python 3.11.9/Git 2.55.0.windows.3/Bash 5.3.15;
the Python 3.9 assertion is grammar-only, not a minimum-runtime run.

Exact product file identities (Git LF bytes; the report-only update is excluded):

| Path | Mode | Git blob | SHA-256 |
|---|---|---|---|
| `lib/domain_result.py` | 100644 | `1709ffa8e87e077e40676f1e8cbe35e91729435b` | `3ccd4266ac597b57eb108dad78c9b466b9182faa848f07ef6a080218e2e2da61` |
| `lib/domain-result-schema.json` | 100644 | `a87193ef1a23923fe54e8039bd54144bf48f6207` | `fef5937bdb52d3b54a7c75b57b19d19a5c863c35075ab8d775766b155e9a9bbd` |
| `bin/li-domain-result.py` | 100755 | `64a2c5de0b01b49f741837d97402add776ae4736` | `357ba58c8f29f63af153ac74be6c7ef10c6eede890029c8447c11ef814e1ae7a` |
| `skills/full-engineering-pass/references/domain-handoff.md` | 100644 | `aa5eed9dfa1638ef9602fcceed4b64ee63ddd4a9` | `823cdd9bdabb2198301030ccebec79ea0e7f23eb81496c1f9e011f5930e1d580` |
| `tests/integration/domain-result-handoff.py` | 100644 | `39cf49d1bd07ce698d226fe30c0a0fbf998bcfbd` | `ff4e456895832f821bd728d8163768026e525cc53bfe14081ba5475d5b80671a` |
| `tests/integration/domain-result-handoff.sh` | 100755 | `49136a9d5087f172d8ac8ba58446b30efcf4f0ba` | `6946a14aa056e894b7053ac5bda8e2a130db5c27a8684d862b296e211180f2f4` |

The second unit is now stopped for the existing reviewer's independent data-core
SPEC, then eligible QUALITY. No push, PR, merge, installer change, source/task/
phase completion, automatic domain action or release clearance occurred. The
remaining P08/consumer/installed/native-role gates listed above remain explicit.

## Released module-consumer unit

**Authority:** `866edf8ce2b6771c113a5d70cd9feb4bb8bed372`, P09
"Remaining module-consumer release", using the original Universal work map and
A09/A17 leaves. The normal merge fast-forwarded this original clean lane to that
release: accepted report `53145bd762410cceaadde213fdb3dc435639be8d` remains an
ancestor; no reset, amend, discard or history rewrite occurred. The release includes
accepted P13 first unit `03df1db` and P08 mechanical integration `e1edc0c`,
not unaccepted P08 semantic orchestration, Swarm gates or P10 WIP.

The three provider files remain exact: `bin/li-work-artifacts.py` blob
`771fc6cb79dcd5888ab75ffcceab53e92c2d7fc1`, `lib/workflow.sh` blob
`27afa35ef9a826da57c8972b94d959bec60fbd9f`, and `lib/state.sh` blob
`a7a3997bbef77d4e85942d5eebde1e21abf8d672`. No work/package/profile/review
parser, policy bridge, data schema/helper/CLI, lifecycle ledger implementation,
installer or scheduler changed. The accepted data reference is extended with the
caller procedure; accepted data code/schema/CLI and both 39-method test files are
unchanged. All 69 role bodies, current additional roles and the original preservation
map remain unchanged from this released base, including frontend/doc-gen ownership.

### Owned implementation and requirement trace

The caller procedures, not a new runtime, now perform explicit original-work
admission, mode-aware dispatch, checkpoint publication and cold reconciliation.
`work_context` supplies original artifacts/tasks/grouping and the P05 work binding;
`workflow_inspect` / `workflow_resume` retain actual cycle/map/profile identity.
Recognized package membership is checked against that reader. Explicit linked
handoffs are bound as acceptance inputs, with semantic membership still reviewed
by the caller rather than a new prose parser. Ungrouped work uses original singleton
IDs. A newer same-ID decoy never becomes the selected source.

| Original leaf | Result in this unit | Acceptance still outside this freeze |
|---|---|---|
| A09.1 | Modules use the one accepted request/start/result contract, real P05 context/control/QA and P07 live reference/policy; final preparation is external and noncircular | Actual independent product review; installed consumer closure |
| A09.2 | Agent-owned checkpoint tables, original output preimages, explicit attempt/iteration, fresh evidence inspection and no unknown-effect replay replace invented shell dispatch/revert/score functions | Whole P08 semantic orchestration is separate, not claimed here |
| A09.3 | TA/DA retain seven capabilities each and source-backed failure, consumer, locking/replay/recovery and analytics methods | Full compound-parent acceptance |
| A09.4 | SC/DH/TQ retain actual applicability, auth, rollout/SLO/cost, compatibility/performance/recovery evidence boundaries | Live service, database, fault-injection and host-specific enforcement are not exercised |
| A09.5 | Direct trusted-source consumption, literal Windows paths, fresh-shell resume, missing required source/domain/artifact and profile drift refusal are exercised | Accepted P10 installed execution; cross-platform runtime |
| A17.5 | Explicit receiver modes, actual bounded native method/cold handoff evidence and reviewer-versus-implementer separation | SAME independent P09 reviewer; original A17/A09 dependency-bound parents remain open |

The five modules retain their original names, discovery/frontmatter identity,
seven capabilities, five checkpoints, six advisory dimensions and method references.
DA/SC/DH/TQ regain explicit invocation examples and original rubric names rather
than weakening those structural checks. Raise-help criteria now express actual
unresolved policy, recovery or consumer decisions, not universal row-count,
consumer-count, percentage, SLO/window or retention assumptions.

Full composition preserves TA -> DA/SC -> DH -> TQ and 30 advisory dimensions.
Missing/failed required domains still block with all other scores at 100. Parallel
eligibility is not execution: default serial; an already approved Swarm map and
attributable isolation remain necessary for concurrent writers. Trusted-source
discovery never executes a target's same-named file. Historical token hints do not
choose models, authorize paid work or establish measured capacity.

The shared procedure requires live P07 verification and explicit advisory inputs,
not a personal-preference reader. Every P05 QA obligation is fixed before output.
Record is data publication; fresh summary/QA reverify raw evidence against the
caller-selected external context. Only actual independent spec/quality and the
existing latest-review reader can satisfy outer review. `release_clearance` remains
false and original tasks are not completed by a checkpoint/score.

Migrator retains artifact-only and separately exact-authorized execution;
ReleaseEngineer retains planning-only and separately exact-authorized execution.
This invocation's mode and target authority govern dispatch, not blanket role
restrictions. Explorer locates, architects synthesize, reviewers report rather
than repair. Resume remains a utility in the unchanged nine-phase lifecycle.

Investigation now binds selected observations and evidence-ranked hypotheses rather
than invented probabilities or an assumed outside CLI. Perfbench keeps explicit
comparable baselines, counts/failures/uncertainty and owned baseline publication;
no automatic home store, NaN-dropping, fabricated tails or percent-only severity.
Devex inspects effects and confines recovery to an authorized disposable fixture;
denied/missing isolation never falls back to in-place mutation. Concept docs retain
the specialist rationale/examples and point to canonical procedures rather than
duplicating fictional runtime or v4.x blanket completion claims.

Exact owned paths (26 product paths plus this report):

```text
skills/ta/SKILL.md
skills/da/SKILL.md
skills/sc/SKILL.md
skills/dh/SKILL.md
skills/tq/SKILL.md
skills/full-engineering-pass/SKILL.md
skills/full-engineering-pass/references/domain-handoff.md
skills/investigate/SKILL.md
skills/perfbench/SKILL.md
skills/devex-review/SKILL.md
docs/concepts/engineering-modules.md
docs/concepts/ta-module.md
docs/concepts/da-module.md
docs/concepts/sc-module.md
docs/concepts/dh-module.md
docs/concepts/tq-module.md
docs/concepts/full-engineering-pass.md
tests/integration/domain-module-consumers.py
tests/integration/domain-module-consumers.sh
tests/shape/full-engineering-pass-contract.sh
tests/unit/ta-routing.sh
tests/unit/da-routing.sh
tests/unit/sc-routing.sh
tests/unit/dh-routing.sh
tests/unit/tq-routing.sh
tests/unit/full-engineering-pass-dag.sh
.claude/plans/universal-implementation/reports/P09.md
```

### Module checks and observed failures

All commands use the verified allowlisted synthetic launcher, PATHEXT, explicit
HOME/USERPROFILE/temp/derived Lintel paths and fixture Git ceilings. Source Git
checks remain separate. Raw stdout/stderr is opened before execution under
`.claude/runtime/p09-depth/logs/module-*`; integration children additionally retain
per-command metadata and streams under unique `.claude/runtime/p09-data-core/`
directories. No inherited tokens/policy redirects, global install/configuration,
path shortening, actual-home command or remote publication occurred.

| Evidence label | Exit and actual result | Meaning and limitation |
|---|---|---|
| `module-red` | 1; two methods, seven failing subcases | Observed missing caller admission and old invented execution before replacement |
| `module-consumers-v1` | 0; 8/8, 78.554s | Initial real released-provider consumer suite |
| `module-structural-first` | 1 | Original declaration checks exposed lost explicit entry/rubric labels and intentionally obsolete numeric trigger names |
| `module-structural-v2` | 1 | One label wrapped across lines; fixed the label, not a weaker assertion |
| `module-consumers-v2` | 1; 13 methods, one error | New test incorrectly expected `status` instead of the accepted successful result's `declared_status`; fixed the test, left data API untouched |
| `module-consumers-v3` | 0; 13/13, 105.879s | Expanded provider, source-discovery, linked/singleton work, changed-input and required-domain cases |
| `module-consumers-v4` | 0; 13/13, 136.172s | Includes literal-path/unchecked-task distinction added after an actual native cold-read failure |
| `module-structural-final` | 0; all 12 shape/routing scripts | Retained declared capability/checkpoint/rubric contracts, conditional concurrency and non-clearing composition; no live-role claim |
| `module-adjacent-final` | 0; 2/2, 2.431s | Focused structural procedure/adjacent checks after final prose corrections |
| `module-retained-data` | 0; unchanged 39/39, zero skips, 262.315s | Separate accepted P03/P05/P07 data suite, logs `p09-data-core/00b85756418b`; not new native proof |
| `module-retained-content` | 0; unchanged 12/12, zero skips, 0.752s | Separate documentary/illustrative role checks, not model behavior |
| `module-ownership-v1` | 0 | Exact released ancestry/scope, unchanged accepted APIs/roles/shared state, primary frontmatter identity, local links and Python 3.9 grammar |
| `module-commit-preflight` | 0 | Actual configured source hooks/signing inspected; no active hook bypass or personal configuration read |

The new consumer suite uses the accepted fixture and actual released reader rather
than a mock P08 or another task parser. It explicitly selects its own 13 methods,
so inherited 39 data methods are not double-counted. Grouped and singleton original
IDs, linked-handoff binding, missing/parent/draft/wrong-package refusals, checkboxes
versus changed criteria, selected-vs-decoy map, live policy drift, fresh-shell cycle/
map/operation resume, trusted-source missing-module refusal, mandatory missing
domain despite four scores of 100, and preserved prior result across an explicit
new attempt are covered. A new attempt's syntax receipt is explicitly not completion.

Python 3.9 evidence is grammar-only; actual execution is Windows/Python 3.11.9,
Git 2.55.0.windows.3 and Bash 5.3.15. A source check is not installed client discovery,
actual browser/renderer behavior or role execution. Current provider/native evidence
and final immutable committed-tree checks are recorded separately below.
