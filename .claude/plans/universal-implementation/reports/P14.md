# P14 dependency-ready core profile scenario preparation

**Initial checkpoint status:** bounded preparation; explicit-root checks pass; the
separate automatic shell-entry case has unresolved finding E01. Native execution
and independent SPEC/quality review are NOT RUN. This is not P14 acceptance.
Coordinator subsequently returned preparation SPEC FAIL / whole QUALITY NOT
STARTED in `2c4fc3d`. See the released correction record below; the original
run evidence remains attributed to `56ce767` / `f106f00`.
**Run-06 correction status:** candidate `175a03d` fixes the tested R01
progress boundary and E01's fixture Git operation, but the complete default
entry remains BLOCKED by E02 in the unchanged P05 provider. No preparation
acceptance, whole QUALITY or native execution is claimed.
**Latest caller recheck:** after the explicitly released, independently accepted
P05 integration, the unchanged default entry passes all 15 methods on joined
source `9092a72`. See the final section for exact evidence. Coordinator complete
preparation SPEC/QUALITY and the subsequent native packet remain outstanding;
run 06 and its E02 evidence are not relabeled.
**Authority:** original `work.json` -> `plan.md`, P14's dependency-ready preparation
section, and recovery coordinator `88aecc43-40f9-41d4-8947-6c2fb0a55481`.
**Exact accepted base:** `9f8885be132b5af0f579dd64019d32aef5501d45`.
**Owner:** `00181e45-3979-4f33-bd31-562e63dc48f0`, original P14 preparation owner.
No other owner, reviewer, factory, native context or external operation is commissioned.
**Product:** `56ce767df652a5c86334e76c469945079e94e3a3`.
**Product tree:** `c34679728fe399ee0aa0470d2574352a03bfe40e`.
**Fixture subtree:** `66d2a823d6343e606c35c9be28bf8fcaf71ae6ed`.
The later commit containing only this report is the report-only checkpoint;
its exact ref and report hash accompany the coordinator handoff.

## Frozen-before-observation experiment

One offline standard-library inventory reconciler processes the same seven seed
records under neutral, rapid-development and strict-change. R1-R6, the original
T001-T003, seed bytes and executable business oracle are identical. A real design
choice is in-memory ID indexing versus sorting/grouping or transient SQLite,
including the validation/publication boundary. Negative and zero stock remain
valid, exact repeated IDs deduplicate, conflicting reuse rejects the whole batch.
The seed's hand-calculated totals are BOLT 5, NUT -2, WASHER 0, six unique events.
This acceptance vector is not a native implementation or observation.

The fixture `controls.md` declares C01-C13, the business cases, comparison
criteria and proposed actual actor order before execution. The harness freezes
accepted source bytes, fixture bytes, task authority hashes and itself before
product imports/observations. It refuses drift. Source text EOL normalization
is used only to reconcile Git's stored bytes with the checkout; both raw frozen
SHA-256 and base-blob SHA-256 are retained. Executed copies are byte-pinned.

## Profile-to-effect and acceptance map

| Actual P07 field / selected source | Neutral | Rapid-development | Strict-change | Required observable effect |
|---|---|---|---|---|
| `compliance.mode` | `advisory` | `advisory` | `hard` | Required-policy loading and actual control outcomes remain separate |
| `compliance.hooks` | `[]` | `[]` | `inventory-atomic-publication`, `inventory-owned-recovery` | Strict adds typed immutable publication and recovery obligations, not installed hooks |
| `navigation.high_risk_workflows` | `cycle, plan, ship` | `ship` | `cycle, plan, build, ship` | Actual `assess_risk plan` returned high / medium / high, never authorization |
| `opinions.source` | `null` | `policies` | `policies` | Read exact selected `inventory.md`; rapid's map preference is advisory; strict SC1-SC3 are required |
| `knowhow.source` | `null` | `null` | `quality` | Strict's consumed `acceptance.md` selects one finite TQ checkpoint using the existing P09 boundary |
| Base R1-R6 | unchanged | unchanged | unchanged | Same business oracle and genuine independent SPEC/quality review for all |
| Package/algorithm | native decision pending | native decision pending | native decision pending | Do not force differences; coincidentally identical valid designs/packages remain valid |

Strict's policy explicitly accepts the demonstrated local publication-failure
oracle and actual P03 owned-recovery rehearsal as equivalents. No hook is armed.
The toy atomic writer emits non-inventory text and must fail business acceptance;
it is only a positive control for the local publication mechanism.

Original A24.1 maps to fixture equivalence, A24.2 to actual field/source/effect
observations, A24.3 to real event/elapsed/usage provenance, and A24.4 to later
native plan/build/review/cold recovery. C01-C13 prepare bounded A23.1-.3 evidence.
No original checkbox is changed. A24.1-.4 and final A23.4/.5 remain unchecked.

## Authorized preparation steps

These steps are report progress, not a competing initiative/task source.

1. Prepared the identical task/seed/acceptance and resolved policy-to-effect sources.
2. Verified accepted P03/P05/P06/P07/P08 interfaces and selected P09 request/result
   boundaries in synthetic fixtures, without inventing native decisions.
3. Retained raw logs, exact exits/counts and hashes, including E01 and prior failures.
4. Recorded bounded self-inspection, not independent clearance. Committed the
   product for coordinator SPEC/quality review; native execution remains gated.

## Scope

Only `tests/integration/universal-profile-scenarios.py`, its `.sh` entry,
explicit files under `tests/fixtures/universal-profile-scenarios/`, and this
report are owned. No production provider, original plan/map, shared memory,
version, generated output, P08 fixture, P12 artifact, or other worktree is edited.

All product imports, tests and children require inspected synthetic HOME,
USERPROFILE, AppData, temp, XDG and LINTEL roots, explicit executable PATH/PATHEXT,
separate trusted source/target and Git config/discovery boundaries. Fixture
selectors replace caller selectors rather than inheriting a foreign pointer or
absolute state root. Logs are opened before children start; failures are retained.
These are process-environment controls, not a security sandbox or native support
claim. Python 3.9 and macOS execution are not supplied by this Windows run.

## Review and continuation gates

Preparation verification results and immutable refs are recorded below.
The source providers are not repaired here. A provider defect returns to its
owner with exact evidence. No installer invocation or P10 diagnostic is released.
All UI/document/registry/network/jq/CI/private-profile boundaries remain closed.

Later native sequence is frozen in fixture `controls.md`: one actual context
per profile sequentially, genuine strict PLAN pause and fresh-context resume,
then a distinct reviewer covering every exact result. P05 contexts must be
rebuilt with actual native actor/attempt IDs; preparation contexts cannot clear
native acceptance. Missing native interventions, rework, elapsed, usage and
model/host version remain unknown, not zero or estimated ROI.

## Retained preparation failures

`p14-prep-01` is retained as a failed run, exit 1: 14 preparation methods,
one failure, five errors, zero skips, 133 logged child commands. Lock SHA-256:
`60ba2df88420ec738db4bfa10d6f508277b887b9f0d798e4c3854536a26c3922`.
The fixture supplied prose instead of the package reader's exact `substantive`
review-depth value, and used singular `runtime/review` instead of P05's existing
`runtime/reviews` self-exclusion namespace. Those are owned fixture/harness
corrections, not provider defects. Neither base business acceptance nor control
assertions were weakened; the frozen failed source/logs remain unchanged.

`p14-prep-02` is also retained, exit 1: 14 methods, one failure, five errors,
zero skips, 133 child commands. Lock SHA-256:
`82e6ef2083d3d8e3cfaaf41658688a63719cf0193ebc9220565d9de0c2ee43b9`.
Further fixture mismatches were the package's prose edit-boundary cell instead
of literal path tokens, and `verification` instead of P05's exact
`verification_only` purpose. The existing complete reader/schema definitions
were re-read before correcting those inputs. No provider/assertion change or
shortened path is used to turn either historical run green.

## Observed checks and evidence

Evidence is retained below the current session's `files` artifact directory,
not in shared repository runtime state:

`C:\Users\jokerman\.copilot\session-state\8fa44739-f562-4213-a6c4-fb7719fc8c9e\files`

The explicit launcher is `launch-p14.ps1`. It clears the child environment,
constructs and inspects synthetic roots and executable paths, and preopens
`outer.stdout.log` / `outer.stderr.log` before starting Python. `launch.json`
retains the allowlisted environment, exact argv, timestamps and process exit.
The harness verifies a 55-file pre-observation lock: 35 accepted source resources,
18 fixture files and both harness entries. Source inventory is exactly the
fixture's `source-paths.txt`; original authority files are hashed separately.

| Run / actual check | Methods | Failures / errors / skips | Exit | Meaning |
|---|---:|---|---:|---|
| `p14-prep-01`, preparation | 14 | 1 / 5 / 0 | 1 | Retained fixture-input failures described above |
| `p14-prep-02`, preparation | 14 | 1 / 5 / 0 | 1 | Retained additional fixture-input failures |
| `p14-prep-03`, preparation | 14 | 0 / 0 / 0 | 0 | All C01-C13 pass at the declared explicit root |
| `p14-prep-03`, oracle `self` | 2 | 0 / 0 / 0 | 0 | Output/refusal oracle's positive and negative unit controls |
| `p14-prep-03`, `business` against unimplemented seed | 11 | 4 / 0 / 0 | 1 | Expected negative; no implementation supplied |
| `p14-prep-03`, `atomic` against non-inventory toy | 2 | 0 / 0 / 0 | 0 | Actual local os.replace success/failure probe, not business success |
| `p14-prep-03`, `business` against that toy | 11 | 32 / 4 / 0 | 1 | Expected negative; failures include subtests, so can exceed method count |
| `p14-prep-03`, `atomic` against direct-write toy | 2 | 2 / 0 / 0 | 1 | Expected negative; direct write cannot satisfy strict's publication control |
| `p14-prep-shell-04`, ordinary no-argument `.sh` entry | 14 | 1 / 0 / 0 | 1 | Retained external Git failure E01; not replaced by the explicit-root pass |

The actual 14 preparation methods are:

```text
test_c01_source_and_fixture_lock
test_c02_isolation_and_process_boundaries
test_c03_original_authority_and_identical_seed
test_c04_required_missing_conflict_and_drift
test_c04_resolved_fields_and_consumed_policy_sources
test_c05_fresh_process_verification_and_missing_pin
test_c06_work_binding_and_policy_text_drift
test_c07_owned_read_and_snapshot_recovery
test_c08_typed_missing_zero_skipped_and_failed_evidence
test_c09_declared_host_is_not_execution_or_review
test_c10_mechanical_pause_resume_keeps_original_identity
test_c11_strict_domain_request_without_manufactured_result
test_c12_oracle_self_checks_and_unimplemented_seed
test_c13_atomic_equivalent_is_not_business_success
```

`p14-prep-03` logged 148 harness-level subprocesses. Five required negative
profile/resume commands returned 2 (66, 68, 69, 96, 127); the three expected
negative oracle commands returned 1 (140, 147, 148). All other harness-level
commands returned 0. Every command has preopened stdout/stderr and its exact
exit record under `logs`. Oracle grandchildren have their own `case-*/exit.txt`
and raw logs; 148 is not a claim about total descendant processes or native work.
Measured preparation-suite elapsed time was 96.390 seconds, excluding source
freeze/bootstrap. Native timing/interventions/rework/usage remain unknown.

Observed tools in the explicit run: Python 3.11.9, Git
2.53.0.windows.4 at the explicitly selected Copilot Git executable, and GNU Bash
5.3.15. The shell-entry run selected Git for Windows 2.55.0.windows.3 from Git
Bash's command discovery. This is not a controlled Git-version comparison.
No packages, clients, hooks or global settings were installed or changed.

| Retained file under `p14-prep-03` | SHA-256 |
|---|---|
| `experiment-lock.json` | `9e459bed63a83dad8e401dbad8d44185fea952691d8dbda5113feaa3010d9fd6` |
| `profile-effects.json` | `e4a014d07c39091d0f2604f0c33433c0f581d1ca8d1401149abc1456e20d2a5d` |
| `summary.json` | `e59501fd77a7a7cf69930a2167492c40d1c4920d2cd6158ae3e2c99218ffc3ed` |
| `preparation.log` | `7f4a682e3e3b62cf38f804ca103c8f4cd6f7c0543f1fd8218eba6349f24dc06a` |

The profile-effects record includes actual field provenance, consumed policy
text, live generation-1 P07 references and accepted typed obligations. Its P05
obligation revision is `p14-fixture-1`, distinct from P07's actual pack release
`1.0.0`. All three business-request hashes equal
`255621f9e4ffae079e50a875a6948f618990c6b9d715c6ec6a37dd1f86f94f5f`;
all seed hashes equal
`9af0670dbd905f572be6f0f0fd8baa2ca50310c66a10234e9f908a6a67dec3cb`.
Profile reference digests are location/context-bound observations, not portable
pins to copy into a newly created target:

| Profile | Actual preparation reference digest |
|---|---|
| Neutral `_default` | `sha256:1cac655e688948de44ac20e3cb4a1e89ce6f62c83e2e02454bf3655bb10eb444` |
| Rapid-development | `sha256:7bb54f73a2482ef4d85908da6d0c43ce3daf3cc8d9046d162509ebc71a3f18cb` |
| Strict-change | `sha256:6495f814b6ba2823a73dafe2ca588d0615e3adc7deeff45b1db3592a31a56023` |

## Exact owned product inventory

All following paths are new test data/code; no existing provider was changed.
The fixture-relative paths below are rooted at
`tests/fixtures/universal-profile-scenarios/`.

```text
acceptance.py
base-source.txt
controls.md
source-paths.txt
packs/rapid-development/pack.yaml
packs/rapid-development/policies/inventory.md
packs/strict-change/pack.yaml
packs/strict-change/policies/inventory.md
packs/strict-change/quality/acceptance.md
seed/.claude/lintel-layout.yaml
seed/.gitignore
seed/AGENTS.md
seed/data/events.jsonl
seed/inventory.py
seed/plan.md
seed/prompt.md
seed/spec.md
seed/work.json
```

The remaining two product paths are
`tests/integration/universal-profile-scenarios.py` and
`tests/integration/universal-profile-scenarios.sh`. This report is the only
additional repository path. Their executed raw SHA-256 values are respectively
`2336b22e02009beabdb0da655472111e191ae8e3d8dff3b39454da48aaa65615`
and `1dfa0ea06ded31d1f39b12479546e09111c65e949542d284cbfeec12b27cb742`.
The oracle is
`31ad2e60d9842df27b20e6fcf7eb352f43eb433c7ff5a173a434e8968fbbb9ec`;
declared controls are
`d5422662154ee4ecf250ff77ff9dd4aeaaaaf3d9f1b69d023b51a580f3d8e755`.
All per-file raw hashes, including consumed source documents, remain in the lock.

Post-commit read-only verification matched all 55 current raw file hashes to
the passing run's lock, with no tracked/staged product diff. Original authority
hashes also remained unchanged:

| Original file in `.claude/plans/universal-implementation/` | SHA-256 |
|---|---|
| `work.json` | `540185096a8f10257010a21f50223e068e20b0031d8ba73ce414e43b41bca805` |
| `spec.md` | `9dd77f9b4d9822c456c0fe0767fe47bb46420c3d34bc32cdf8ced9b439e457ae` |
| `plan.md` | `c1e19b1cb09228eebdb3707d68a9ed419f2d5d0fcbddc31152201fb835c7b969` |
| `packages/P14.md` | `fd673ff7f55cbba9e0cdd9a2bee082e406cece2188f37477a1eb68abccd4fb6b` |

The actual historical verification invocations were:

```powershell
& '<session-files>\launch-p14.ps1' -RunName p14-prep-03
& '<session-files>\launch-p14.ps1' -RunName p14-prep-shell-04 -ShellEntry
```

`<session-files>` denotes the exact artifact directory above, not a proposed
shell variable. Do not reuse those occupied evidence roots. The underlying
explicit-root command was the inspected Python executable with `-I -B`,
`tests\integration\universal-profile-scenarios.py --root <source-checkout>
--run-root <p14-prep-03> --git <explicit-git.exe> --bash <explicit-bash.exe>`,
under `launch.json`'s complete isolated environment. An ambient Python import
or a shortened-root retry is not this evidence.

## E01: Automatic shell entry is not accepted at the exercised deep root

The no-argument `.sh` entry established its own inspected synthetic root under
the outer launcher's temporary directory. At:

```text
files\p14-prep-shell-04\temp\p14-profile-prep.lapbOqLe\
  cases\test_c04_resolved_fields_and_consumed_policy_sources\
  rapid-development\target
```

its actual Git `add .` returned 128 before rapid profile preparation completed:

```text
error: unable to write file .git/objects/93/3157da484fa7c0ccd53ae71c9845cf0937a94d: Filename too long
error: .claude/lintel-layout.yaml: failed to insert into database
error: unable to index file '.claude/lintel-layout.yaml'
fatal: adding files failed
```

The exact native path/argv is retained in `logs/0078.json`, SHA-256
`ef72963ecf45295053dbefe055f07e1c9e075a06c4162cb9130874dde004e4a1`;
stderr SHA-256 is
`33e70f521b19b7b3223418b106057e9640e1a11a36dad8351529abbb37a40af2`.
The shell run's lock is
`34cbf89c70c556b1c090131287f58db2a62a02d3950e72afb34f5fd723041491`;
summary SHA-256 is
`89d2d2d3bf4a083fe7b0296605d8b891f245766306379b47ce777a946e93317e`.
It logged 138 harness-level commands and 83.063 seconds of preparation-suite
time. It used a different Git executable as well as a deeper root than the
explicit run; no universal numeric path limit or sole cause is inferred.

No retry, root shortening, Git flag/configuration change, platform/ACL/process
probe, installer call or speculative provider repair follows. E01 is a retained
limitation of this exercised entry, not P10's exhausted linked-init journal
failure and not permission to reopen P10. The explicit-root pass does not close
this case or supply full-suite/installed acceptance. Coordinator disposition
is required before claiming the automatic entry accepted.

## Self-review, correction and remaining gates

This owner inspected the full owned diff against the released preparation.
Source/task authority, finite inputs, immutable controls, clear failure exits,
equal base acceptance and no native dispatch were checked. Fixture-input errors
were corrected against actual producer contracts; no production code, required
oracle, source path or original task status was changed to hide a failure.
E01 remains open. No independent SPEC/quality/compliance verdict is authored.

**2026-09-23 operator correction:** a proposed product commit included a
process-local hook-disabling override and was rejected before execution. HEAD
and staged files were verified unchanged. The subsequent normal `git commit`,
with existing Git controls unchanged and the required Copilot co-author trailer,
succeeded as the product ref above. Do not carry synthetic test-repository hook
isolation flags into product-checkout commits; "do not activate hooks" is not
permission to disable existing controls. Shared lessons/memory are coordinator
owned, so this correction is recorded here rather than written outside scope.

No positive native business implementation has run. The oracle's unit positives
and failed toy/stub programs prove bounded preparation properties only. Actual
Windows-native host actors, fresh model-context resume, independent review,
other clients, Python 3.9, macOS, installed caller-to-child bridging, full strict
suite and final integrated A23.4/.5 remain unverified/not run. No grammar check
is substituted for a runtime. No ROI, model quality or all-client support claim.

## Cold next action

The coordinator should review exact product `56ce767` and this report, including
E01, for preparation SPEC then quality. Resolve the entry/root/tool selection
for a separately released native packet without relabeling or shortening the
failed case. No native actor is authorized merely by this checkpoint.

The available host operations for that later packet are `functions.view`,
`functions.apply_patch`, `functions.powershell`, and actual separate contexts
through `functions.task` or the coordinator's app-session mechanism. They were
not invoked as native scenario actors here. A bounded `lintel-builder` task can
receive an explicit synthetic target/source and write scope; a new task without
the prior actor's chat can supply the genuine cold context. A distinct
`lintel-reviewer` context then owns review only. No new scheduler or project
registration is needed to execute a bounded task in an explicit fixture target.

Use fresh equal seeds, only the selected pack, and a newly verified P07 pin for
each actual target. Start the real P08 cycle/map under its declared synthetic
environment; the script's mechanical pause is not prior native PLAN work.
Have the strict actor pause after actual T001/PLAN, persist its real handoff,
then end it. The cold actor verifies the same target/pin and runs
`workflow_resume` against the unchanged original map before identifying T002.
Apply SC2 before its actual BUILD. Generate P05 contexts from actual actor/
attempt IDs, freeze typed requirements before observation, then bind the actual
result snapshot for independent SPEC/quality. When strict TQ is actually run,
consume the existing P09 request/result boundary; no preparation record is
upgraded into its result.

The coordinator's packet must explicitly grant any required P14 evidence writes
and retain host permission decisions. Do not reuse denied P12 persistence or
borrow P08/P09 actors/verdicts. Record real tool/actor IDs, interventions, rework
and timestamp/usage provenance; leave unavailable values unknown. Proceed one
target writer at a time. Original A24 leaves and final A23.4/.5 stay unchecked
until actual observations and their required independent review are complete.

**Position:** bounded preparation checkpoint -> coordinator review; native
continuation awaits its specific released packet.

## Released E01/R01 correction

**Authority:** coordinator `2c4fc3d30b9349ebf6e16d5cff24c1c94c555153`,
complete P14 card and `reviews/P14-preparation-56ce767.md` read before changes.
The coordinator's complete preparation SPEC found P2 R01: requiring all
original tasks to remain incomplete, and hashing current whole authority
against historical files, would break ordinary integration after legitimate
initiative progress. Whole preparation QUALITY has not started.

Only the Python harness, focused `controls.md` and this report are changed.
The business oracle, both pack manifests, all policy texts, equal seed,
original IDs and future native actor/handoff boundaries are unchanged.

R01 correction separates `base-source.txt`'s immutable historical experiment
provenance from the current declared execution subject. A run snapshots the
current HEAD's selected provider bytes, refuses uncommitted provider drift,
executes their exact frozen copies, and checks both current and frozen byte
identities throughout. Historical helpers are not substituted. Current
map/spec/plan/prompt/P14-card bytes are locked at entry and verified unchanged
before/after reads, rather than required to equal an old completion state.
The original map must retain its task identity and actual P05 approval boundary.
No historical lock, baseline, observed result or initiative task is rewritten.

The additional C03 case copies actual current authority into its owned
synthetic test repository, not another real backlog. It uses the existing P05
Markdown progress spans to edit only A24.1's checkbox. Actual P08 must report
the same IDs and completed A24.1; actual P05 must preserve accepted identity.
Changing the selected substantive requirement must fail P05 verification;
withdrawing approval to DRAFT must also fail. No replacement task parser or
blanket plan/config exclusion is introduced.

E01 correction is limited to Git's command-scoped `core.longpaths=true`
environment for inspected Windows fixture targets, including the unmodified
providers' Git subprocesses. It is absent from source-checkout commands and
rejected outside `cases/` or if any other command-config key/value is supplied.
The existing synthetic test Git settings stay fixture-only; source inspection
and product commits keep their existing controls. A real configuration probe
must report `command` scope, no local longpaths setting and unchanged local
config bytes. The no-argument shell entry, root dimensions and selected
executable are unchanged. No tool replacement, root shortening, persistent
configuration, hook bypass, P10 retry or provider change is released.

Verification planned before observation: run the complete unchanged default
shell entry in `p14-prep-shell-05`, matching `p14-prep-shell-04` name length
and its existing generated-directory/target layout. Keep the original fourteen
methods and all expected negative oracle outcomes, plus the new paired C03
case. Retain raw exits and failed histories. Freeze the resulting correction
for coordinator complete SPEC before the first eligible QUALITY; no native
context or reviewer is released by a passing preparation run.

### Retained correction run 05

The original default-entry layout at `p14-prep-shell-05` (generated child
`temp\p14-profile-prep.n5jDzaMR`) ran 15 methods, zero failures, one error,
zero skips, exit 1; 148 harness-level commands and 71.281 seconds. Lock:
`8e1657c8080b99da02e4a2ecf9f5b0f7a47a43f5edb7745fd503f340bb15bbe5`.
The new actual P05/P08 progress-positive and requirement/approval negatives
passed, as did command-scope/no-persistent-config verification.

Git completed the formerly failing fixture object writes. The same unchanged
deep target then exposed the harness's plain `shutil.copytree` into
`rapid-development\target\packs\rapid-development\policies`: WinError 206.
This is an owned fixture I/O site, not a source-provider/P10 failure. It now
validates both copy paths through the accepted P03 owned-root/path checks and
passes their same-location `native_io_path` spellings to `copytree`. No root,
target name, Git executable, assertion or shared helper was changed. This
failed run stays retained; the next full same-dimension entry verifies the
focused fixture-copy correction rather than relabeling run 05.

### Frozen correction and actual outcome

Correction product: `175a03df10428eb211d9f5bd73af01bd9af079e1`, parent
`f106f007cc8b3549a1460289eb60c9c35beb4b8a`, tree
`af25829f695510a25b4794370a1948b31c26960c`.
Its two changed paths are exactly:

```text
tests/integration/universal-profile-scenarios.py
tests/fixtures/universal-profile-scenarios/controls.md
```

Only this report changes in the following report-only checkpoint. Existing Git
controls were left unchanged for both commits, with the required Copilot
co-author trailer. The correction's executed raw SHA-256 values are
`77600b126bde3aecd1548c8851d3b135d90f4d116cbca6f49153e90b7ea2624a`
for the Python harness and
`e52934eb26a51aa976b978ed23ec15d1e7adbcb445a17819ff9f223061442930`
for `controls.md`. The shell entry and all other original product paths
remain unchanged.
After that product commit, read-only verification matched all 55 executed
source/fixture/harness byte pins and all five current original-authority pins
to run 06. The source providers were not re-executed or modified.

The complete original default entry was run as:

```powershell
& '<session-files>\launch-p14.ps1' -RunName p14-prep-shell-06 -ShellEntry
```

Its inner evidence root is
`p14-prep-shell-06\temp\p14-profile-prep.nrprcPzf` beneath the previously
named session-files directory. Run 06 executed **15 methods: 14 passed,
zero failures, one error, zero skips, process exit 1**. It retained 149
harness-level commands and 88.656 seconds of preparation-suite elapsed time.
No three-profile success result is produced after the failing rapid case.
The older explicit-root comparison is historical evidence, not a substitute.

R01's added `test_c03_legitimate_progress_requirement_and_approval_controls`
passed actual P08 completed-status/identity checks and actual P05 unchanged
acceptance after checkbox progress, then rejected substantive requirement
drift and DRAFT approval. The original C03 preservation/equal-seed case passed
without requiring A24/A23 tasks to remain incomplete. Current original
authorities stayed byte-identical throughout the run.

E01's exact formerly failing operation was matched read-only against the
retained original event: old `logs/0078.json`, new `logs/0086.json`.
Both used `C:\Program Files\Git\mingw64\bin\git.exe` (2.55.0.windows.3);
both actual logical target paths were **216 characters** and had the identical
suffix beginning `cases\test_c04_resolved_fields_and_consumed_policy_sources\
rapid-development\target`. The outer run-name and random generated-directory
components retained identical lengths. Old `git add .` exited 128; the new
one exited 0 with the declared command-scoped longpaths environment.
No shorter primary root or replacement executable was used.

The actual C02 Git probe returned `command true` for the setting, exit 0;
`--local --get core.longpaths` returned 1 (absent), and local config bytes
were unchanged. Other command-config keys and source-checkout commands with
fixture options were refused before execution. Source Git inspection no longer
receives the synthetic-repository `-c` settings. This is bounded fixture
evidence, not a global configuration change or new host support guarantee.

Unchanged oracle results in run 06: `self` 2/2 exit 0; business against the
unimplemented seed 11 methods / four expected failures, exit 1; atomic toy
2/2 exit 0; business against the non-inventory toy 11 methods / 32 failed
subtests / four errors, exit 1; direct-write atomic toy two expected failures,
exit 1. Their logs are 140, 141, 147, 148 and 149 respectively. No native
business solution, decision, review, corroboration or domain result was supplied.

### E02: P05 rejects the existing policy file at the unchanged deep target

The same-dimension run cleared both the original Git failure and the owned
copytree failure, then stopped its C04 profile-comparison method at:

```text
bin/li-review-evidence.py:51 prepare
lib/review_contract.py:487 bind_work
lib/review_contract.py:278-279 _path(..., regular=True)
ContractError: Evidence/authority must be a regular local file:
packs/rapid-development/policies/inventory.md
```

Immediately beforehand, this method's `profile_sources` read that exact file
through accepted P03 `checked_root` / `read_owned`, then supplied its original
relative path to P05. The retained target file is an 848-byte regular file,
SHA-256 `226d954634ea3f0728d8dfdffcbfc1eca29f6316440af5cef854d4ce7611545e`,
matching the frozen fixture exactly. A read-only file/hash check confirmed
that observation; no alternate provider invocation or platform probe was run.

The current declared provider subject was
`f106f007cc8b3549a1460289eb60c9c35beb4b8a`, not an implicitly selected
historical execution copy. The executed `lib/review_contract.py` raw SHA-256 is
`602991d1acb922675c3982a9e34e171bf7d4a6ed121af99254841cc34d5b8d1d`
(Git LF blob SHA-256
`c6d004cc3fdb69d2c30a4244749a0e8069ddbaebc9b9cc884565a02b5d4f91de`);
`bin/li-review-evidence.py` SHA-256 is
`19f16a8c2ae6fa631bcccdc8d00cdf121a776f33783a769127ac6e6048fb38a6`.
Both are unchanged by P14. The Python traceback identifies P05's ordinary
regular-file guard; it does not establish a universal numeric Windows limit.

| Retained run-06 file | SHA-256 |
|---|---|
| `experiment-lock.json` | `822e9828a28b2982b38e26d21e5d79769fc5e2243ac60525942eb0c2eeeab134` |
| `preparation.log` | `4cb3aeebf0d4f498462a04addc9b6673576a8c9470af897be6b1818dad6177f5` |
| `summary.json` | `5a556087921ac164f20f5f937287fee53c186bb8af427f9b12e2ac470bd33f6d` |
| `logs/0086.json`, matching Git operation | `0146b3345f5cb997c11810c3874389f0dc19bfc9cf7d00775cd03a2c69f269bc` |
| `logs/0057.stdout.log`, command scope | `e3cc78d625872ce141f64c09297f77fcf75ed17c71efa3916fc898c3cd818cf0` |

This is a concrete dependency finding outside P14 ownership. The affected
execution is stopped. No P05 patch, root shortening, source-selection change,
assertion waiver, alternate provider path, P10 investigation or new attempt is
made after E02. The full entry remains non-accepted despite R01 and the
formerly failing fixture operations now passing. All prior failures remain
retained under their original refs/locks.

### Correction handoff and cold next action

Coordinator should intake this exact product/report, review the owned R01/E01
changes, and route E02 to the existing P05 owner or make an explicit dependency
decision. Do not promote the historical explicit-root pass into the required
same-dimension entry. Any accepted provider correction must return through the
original owner/review path before this frozen default-entry case is separately
released for rerun against the then-current declared source.

Whole preparation SPEC/QUALITY and native release remain outstanding. No
current-request owner/reviewer/session was created, no original task state was
changed, and original A24/final A23.4/.5 remain unclosed by this checkpoint.
Native measurements remain unknown.

**R01 preventive rule (2026-09-23):** historical experiment provenance is not a
permanent assertion that an initiative is unfinished. Capture current authority
and preserve it across observation; use the shared acceptance semantics to
distinguish checkbox progress from requirement or approval changes. This
correction is recorded only here because shared memory is coordinator-owned.

**Current position:** owned correction frozen; full entry blocked on E02;
return to coordinator, no native continuation or provider retry.
The coordinator confirmed that run 06 remains frozen and that E02 returns to
the original P05 owner/reviewer, without expanding P14 or reopening P10.

## Accepted P05 dependency and original caller recheck

**Release:** recovery coordinator explicitly released the original stopped
caller only after the SAME independent P05 reviewer completed scoped SPEC then
QUALITY PASS in `06a41fe203e2c210d0279fa481d729a39cd2e690`. Its complete
377-line `reviews/P05-native-observation-final.md` was read before the merge.
The accepted integration dependency is
`b22443f9c1b465637529236de7a3120618aca2ee`. No native actor, P12/P10 operation,
global/tool configuration, jq execution or further provider correction follows.

Only that named ref was normally merged, without cherry-pick, rebase, reset,
hook override or conflict editing. The resulting exact current source is:

| Identity | Immutable ref |
|---|---|
| Joined caller source / merge | `9092a721e8a383a8d5cf1581a7b617157225c1ab` |
| First parent: frozen P14 correction/report | `9120bb263f6169125e3dd61c6cd12cbf9ff6fc60` |
| Second parent: accepted dependency | `b22443f9c1b465637529236de7a3120618aca2ee` |
| Joined tree | `1b4c1998352f4be1ca0c0d469f1b3f83f237a3ae` |

Before execution, all six accepted provider/test/report Git blobs matched the
independent review exactly, and all P14 product/fixture/report bytes matched
the first parent. The named dependency's other integrated history was preserved
by the merge; it is not authorization to execute its unrelated workflows.
There is **no new P14 product/harness/fixture edit** in this recheck tranche.
Only this report is authored after the merge.

The unchanged ordinary no-argument shell entry was invoked by the existing
inspected synthetic launcher:

```powershell
& '<session-files>\launch-p14.ps1' -RunName p14-prep-shell-07 -ShellEntry
```

The actual Bash argv uses `--noprofile --norc` and the absolute existing
`tests\integration\universal-profile-scenarios.sh`, with no test-script arguments.
The new, previously unused run root is
`p14-prep-shell-07\temp\p14-profile-prep.r0lTFAFb` beneath the session-files
directory recorded above. Run 06 and all older roots, logs and pins remain
untouched. No shorter explicit-root route or copied old successful pin was used.

### Actual complete caller result

**15 methods PASS, zero failures/errors/skips, child and outer launcher exit 0.**
This includes the original C01-C13 methods and the added R01 progress method,
not P12's distinct method count. Unittest time is 84.976 seconds; measured
preparation-suite time is 85.125 seconds. All **156 harness-level commands**
have preopened raw logs and recorded exits matching their declared expectations.
This is one complete joined caller run, not combined partial runs.

The exact originally failing rapid-development target is still 216 logical
characters and its policy file is still 262. The relative target suffix and
all generated-directory/name lengths match run 06. The actual executable remains
`C:\Program Files\Git\mingw64\bin\git.exe`, version 2.55.0.windows.3.
The same `git add .` is command 86, exit 0. The existing fixture-only
command-scoped longpaths configuration is unchanged; command 57 reports
`command true`, local-setting absence is command 56 exit 1, and C02 confirms
unchanged local config plus source-command/foreign-option refusal. Python is
3.11.9 and GNU Bash is 5.3.15. No global/local config or executable is replaced.

The unchanged C04 now completes real P07 resolution/source consumption and
P05 context preparation/verification for **all three profiles**, including the
same rapid policy that E02 rejected. Its bytes remain 848 / SHA-256
`226d954634ea3f0728d8dfdffcbfc1eca29f6316440af5cef854d4ce7611545e`.
E02's joined-caller observation is now green on this exact source; run 06
remains a failed observation of its earlier source, not a rewritten pass.

R01 passes the actual P08 unchanged-ID/completed-A24.1 case and actual P05
acceptance preservation, then refuses substantive requirement change and DRAFT
approval. The actual current authority read is preserved, not forced incomplete.
Required missing/conflicting/drifted profiles, missing resume pin, wrong map,
typed QA downgrades, zero/skipped/failed/unknown evidence, late-edit recovery and
absent independent/domain results retain their original refusal assertions.
The mechanical pause/resume remains a subprocess case, not native cold context.

Expected nonzero subprocess exits are explicit: local-config absence 56 -> 1;
required-policy/resume negatives 74, 76, 77, 104, 135 -> 2; deliberately invalid
oracle subjects 148, 155, 156 -> 1. All other harness commands exit 0.
Oracle grandchildren retain their own logs/exits, not a claim of 156 total
descendant processes. Unchanged oracle results:

| Oracle / subject | Methods | Failures / errors / skips | Exit |
|---|---:|---|---:|
| `self`, output/refusal checks | 2 | 0 / 0 / 0 | 0 |
| `business`, unimplemented seed | 11 | 4 / 0 / 0 | 1, expected |
| `atomic`, non-inventory publication toy | 2 | 0 / 0 / 0 | 0 |
| `business`, that non-inventory toy | 11 | 32 subtest failures / 4 / 0 | 1, expected |
| `atomic`, direct-write toy | 2 | 2 / 0 / 0 | 1, expected |

### Complete three-profile effects and pin identity

The final `profile-effects.json` contains actual P07 values/provenance, consumed
source text, P05 typed obligations and newly created target-bound references.
No reference from a shorter or earlier root is reused. Every reference has
schema 1, context `p14-preparation`, generation 1 and pack version `1.0.0`.

| Actual field/effect | Neutral | Rapid-development | Strict-change |
|---|---|---|---|
| `compliance.mode` | `advisory` | `advisory` | `hard` |
| `compliance.hooks` | empty | empty | `inventory-atomic-publication`, `inventory-owned-recovery` |
| `navigation.high_risk_workflows` | cycle, plan, ship | ship | cycle, plan, build, ship |
| Actual `assess_risk plan` | high | medium | high |
| `opinions.source`, consumed file | null, none | policies, inventory.md | policies, inventory.md |
| `knowhow.source`, consumed file | null, none | null, none | quality, acceptance.md |
| Immutable QA obligations | business | business | business, atomic publication, owned recovery |

All three retain identical business request SHA-256
`255621f9e4ffae079e50a875a6948f618990c6b9d715c6ec6a37dd1f86f94f5f`
and seed SHA-256
`9af0670dbd905f572be6f0f0fd8baa2ca50310c66a10234e9f908a6a67dec3cb`.
The strict-only source hashes remain the unchanged SC1-SC3 policy
`2ab80f09b6f4795e9040056fe00a1b7e98b75edaa5147d1206216e36614f1156`
and quality guidance
`a1e984b96f6e393dfa1155eef1a025d6a96622bcf921fdd64e2aacf14b1bdc01`.
Rapid advice is not upgraded to a mandatory procedure; strict's named local
equivalents remain explicit policy requirements, not automatic hook enforcement.
Real design/package decisions and business output still await native actors.

| Profile | New run-07 reference digest |
|---|---|
| `_default` | `sha256:2545c49762992f643b0728983b2f6e2c4cd016eb76ca395b6ed9e0c941bcf6f7` |
| `rapid-development` | `sha256:2fc466991ef8ce8a6001d2e7bdafcb080a72932224e2e39d4be1e029917aab8b` |
| `strict-change` | `sha256:4130653629ee94483f0f3607f7c71c342401e258a13a574dc6e3f9e936cb7a0c` |

### Frozen evidence and before/after preservation

The run-07 lock declares **current subject `9092a72`**, with historical
`9f8885b` recorded only as experiment provenance. It binds the current 35
selected resources, 18 unchanged fixture files, both unchanged harness entries,
and five separate current authority snapshots before product imports/calls.
After execution, all 55 current/frozen input hashes and all five original
authority hashes were rechecked unchanged. The previous run-06 frozen 55+5
inputs were separately rechecked unchanged before and after this run.
The source worktree remained clean before this report edit.

Accepted `lib/review_contract.py` is Git blob
`f3320a90c2728bc129debaf3802e943b48a84c5b`, Git SHA-256
`d4e05e6f14709c97c1dae0bc5e48c60527631830b8bbbeb129ab63c39dea9461`,
executed raw SHA-256
`0bc76859e253c2deeaf53535619b7a75cd355e03f1849a6bb2d34cb459470add`.
`bin/li-review-evidence.py` is blob
`954eebf552953ba153be9966cf33790206d395af`, raw/Git SHA-256
`25972a78300d9f9a8dc813242073d43c768303887c230e37a9640e4118c24490`.
Raw checkout versus Git-byte differences are recorded, never normalized into
the execution source. No production or fixture bytes were edited for this run.

| File within the run-07 inner root | SHA-256 |
|---|---|
| `experiment-lock.json` | `6fa870b0d9a7498dc1d76558ba98c5af83709ff44919ff7c8f24e037523c0845` |
| `profile-effects.json` | `bd2b3d7d80b9814eb9ee51c6b6fde378353cf0c82e0ab29933b6c582cf7c6bd5` |
| `summary.json` | `4839249e41f06369034c636d65e4fa65f116035c84156adaf26a8654820f13aa` |
| `preparation.log` / `launcher.stderr.log` | `387b2f6d59eb03e098266733a3b10bdd4171380151df0251a1b3358d09bba081` |
| `launcher.stdout.log` | `3a7c41b54946b806f5377e96dafb5e7b804526445e434022573f2363d7a0bc2e` |
| `logs/0086.json`, same Git operation | `8dc0b7c31b248782548a2cca08a90045d373079c89033f9f2941b56ec38596b7` |
| `logs/0057.stdout.log`, command scope | `e3cc78d625872ce141f64c09297f77fcf75ed17c71efa3916fc898c3cd818cf0` |

All artifacts are synthetic preparation evidence. No native inventory solution,
decision/corroboration, reviewer or model context was created. Native
interventions/rework/elapsed/usage remain unknown. No jq execution or dependency
was needed; the provider review's PATH/hash statement does not lift the recovery
jq denial. Its unrelated empty-directory cleanup ERROR remains separate and
was not inspected, retried or relabeled here.

### Exact stopping point

This actual same-dimension caller result returns to the recovery coordinator
for **complete preparation SPEC, then the first eligible whole QUALITY**.
Neither this owner nor the accepted provider review automatically closes
A03.2.n3, A24, full P14 or final A23.4/.5. No original checkbox is edited.
The final commit after `9092a72` is report-only; exact ref, Git-byte hash and
clean-state verification accompany the handoff.

The next action is coordinator review of the unchanged P14 product at this
joined source and the full run-07 evidence. Only its subsequent **specific
native packet** can authorize the previously frozen actor sequence and real
cold-context experiment. No further execution is initiated by this checkpoint.

## Released native experiment: actual neutral T001 and transport stop

**Later authority:** complete preparation SPEC then QUALITY passed in
`f690eae`, `reviews/P14-preparation-final-9092a72.md`. The native release in
the full P14 card at `acc18df1b76a6fa1ef54f725d83aa7240e62764f` names exact
source `c544fd07491b1bf6c16363b60470cb5ed77ba3b2`, at most four implementer
contexts and the existing recovery coordinator as final independent reviewer.
Those complete documents were read before this tranche.

One fresh native root, `files\p14-native-01`, was declared before writes in
`files\p14-native-declaration.md`. One exact source archive includes 565
canonical workflow/helper/reference/role/adapter/foundation/document/decision
and relevant fixture files, not just the preparation's 35 mechanical imports.
Source selection is pinned to `c544fd0`; exported source bytes remain immutable.
Three equal seed targets were created under `cases\neutral\target`,
`cases\rapid-development\target` and `cases\strict-change\target`.
The controller initialized actual P07 profile references, P03 owned preimages,
P08 cycles and final typed QA obligations under inspected synthetic environments.
Setup made 21 recorded child calls, all exit 0. No model actor was active during
setup; no old target, pin or output was reused.

The initial pre-import export check stopped because it compared an expected
Git LF SHA to Git archive's ordinary Windows CRLF serialization. This failure
is retained, not a provider defect: exported review_contract.py is the accepted
raw `0bc76859...`; its read-only LF projection is exact Git `d4e05e6f...`,
blob `f3320a90...`. No source byte was normalized or rewritten. A later compact
read-only comparison command also exited 1 without output; its bounded
standalone replacement explicitly checked all 18 approved fixture hashes and
succeeded. Neither failed command is counted as a passing check.

The setup froze its exact exported bytes and checked identical seed copies
before native dispatch. When the coordinator's additional input-identity
instruction arrived with the completed first actor return, all 18 fixture
files were compared against the approved run-07 exact raw pins, including
business request, seed, oracle and policy. All matched, without EOL
normalization of data, before any BUILD. This comparison's timing is explicit;
it is not retroactively claimed to precede the first plan call.

| Native setup artifact | SHA-256 |
|---|---|
| `source-c544fd0.zip` | `ac8772c9ede73b65ce8c29e622f02b4d498e70f5ea0c332f7b885e0de9a4357d` |
| `source-files.json`, 565 raw source pins | `259238428e19ce83c65317b9b56946226bb9fff6055483091c8ac23312227ef4` |
| `native-input-lock.json`, three initial target inputs | `6ffec93d1ffc9688b64ef05734b00c2281d68041f5f72112988287f666735d79` |
| `approved-input-comparison.json`, exact 18 fixture matches | `5e791ce47a984c80dcd2edcea881fa70c24e4e8588a156135460504cf692f693` |

### First real neutral context

Actual `functions.task` selected registered `lintel-builder`,
name `p14-neutral-native`, **sync mode**, context
`7742184c-1e60-424e-a570-7937dd6eac72`. No model/reasoning override,
nested agent, factory or other native context was used.
The actor read only its granted target/profile and relevant canonical guidance,
authored a real T001 plan and stopped before T002.

Its sole product change is the neutral target's existing `plan.md`, SHA-256
`5f306d39310b7320c74be9bdfc41e64758c29e92dc7cc5ef2612497eae8973ac`.
Only T001 is checked; original task text, R1-R6, package and T002/T003 remain.
`inventory.py` is still the original failing stub. The actor independently
chose transient dictionaries for reconciliation and optional staged replacement,
compared a sorting/grouping alternative, and explicitly stated that the neutral
pack does not mandate that choice. The controller supplied no algorithm/code.
This is a real planning observation, not an implemented or accepted solution.

Its exact live generation-1 neutral reference is context `p14-native-neutral`,
`_default` version 1.0.0, digest
`sha256:eebc79dd3a11f21b36ea3ae72f65575408370e293d901e4ef9cc19e0fab80460`.
The actual neutral final QA inventory remains only mandatory typed
`inventory-business`; strict-only procedures were not imposed.
A broad acceptance-file name lookup was denied, returned no data and was
not retried. The controller's already authorized exact frozen oracle path is
known; neither actor nor controller performed another broad lookup.

The controller read the complete authored plan and recorded a phase-appropriate
assessment in `.claude\runtime\p14\plan-assessment.md`: planned coverage of
R1-R5, preserved R6/final independent review, and no implementation verdict.
Actual SENSE/SCOPE/DEFINE/DISCOVER/PLAN read/reason/design outcomes were then
recorded through the accepted P08 helper following live P07/work verification.
That recording exited 0; it is not evidence that a shell helper did the actor's
reasoning. Final business observations and independent review remain unrun.

### Follow-up transport failure and current stopped boundary

The controller attempted same-context BUILD through `functions.write_agent`.
The host rejected it before delivery:

```text
write_agent only supports background agents. Agent
7742184c-1e60-424e-a570-7937dd6eac72 is in sync mode.
A new background task must be started for follow-up messages.
```

`read_agent` confirmed this original actor idle with one returned turn.
The initial sync choice was a controller error for this explicitly multi-turn
experiment. A tool listing idle is not evidence that it accepts follow-ups.
No BUILD message reached the actor; no second context or replacement writer
was created. The proposed grant was revoked and the prior controller
`BUILD STARTING` entry was followed by **BUILD BLOCKED**, referencing the real
`.claude\runtime\p14\transport-stop.md` response. That isolated record operation
exited 0; no inventory execution occurred.

Recovery88 was informed immediately. A decision on the actor-count/continuation
boundary is required before another context may be launched; this controller
will not invent session bindings, bypass the transport limit, become the
inventory implementer, or count a new context as the same one. Existing actor
work and setup remain retained. The original four-context and separate-review
constraints have not been silently expanded.

This is a real bounded T001 milestone with a concrete host limitation, not a
new planning initiative or completed A24 comparison. No native business oracle,
implementation, QA clearance, review decision, corroboration or strict TQ
result exists yet. Actor tool elapsed status is not measured implementation
time; actual model identity, usage and cost remain unknown. No ROI is claimed.
No original A24/final A23 checkbox, provider, prior preparation root, personal
profile, P12/P10 route or global setting changed.

**Current native position:** original neutral PLAN preserved; same-context
BUILD blocked by actual sync transport; await the coordinator's explicit
continuation decision. Rapid and both strict contexts have not been launched.
