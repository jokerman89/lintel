# P12 F01 standalone defaults repair

Date: 2026-09-22. Original builder:
`09666ec2-ff03-4517-9169-8793149ea8cd`. Recovery coordinator:
`88aecc43-40f9-41d4-8947-6c2fb0a55481`.

**Status: bounded source repair frozen for the same independent reviewer.**
No rendered-layout or full P12 acceptance is asserted.

## Authority and immutable product

Read all 236 lines of independent
`dfb5cdd0a56d2146fedc3c69e497213a67f9f99a`, whose report is
`reviews/P12-standalone-cdb4ea6.md`: SPEC FAIL, P1=0/P2=1/P3=0,
QUALITY NOT STARTED. F01 identifies lost default behavior, not a native layout bug.

Read the exact appended F01 card at
`50bd3fdb8f0cd97b4fb42658f59566525b631799`, parent
`ee3ed5e8c2efb5cae4529b902781a1e56245c8c8`.
This authorizes only three skill sources, focused preservation tests and this
owned report. The original map, common ledgers and review are not edited.

Product `32dac88ed1f143cf29ffe84f3fb1502c91a6c3e7`, sole parent
`30bd4e303749819178c76beda036b103baa9ebb4`.
It contains only:

- `skills/generate-word/SKILL.md`
- `skills/generate-ppt/SKILL.md`
- `skills/generate-outline/SKILL.md`
- `tests/integration/document-format-pipeline.py`

This report is a separate report-only child. Its exact identity/hash is in the
coordinator handoff. The workbook unit has no source files in this product.

## Changed and retained behavior

Word without `--template` or `--use-defaults` now explicitly selects the target's
filename inside the already verified configured brand directory:
`technical.docx`, `customer-summary.docx` or `transparency-note.docx`.
An explicit template overrides that implicit filename. The existing explicit
neutral/default route remains distinct; omission does not imply defaults.
No personal directory scan, missing-template success or mandatory-policy waiver
was added.

The outline's PPT-only advisory starting hint is restored to 8-15 slides,
default 12. Direct PPT retains its distinct 20-30-slide duration-based fallback.
Explicit counts, brief/duration constraints and explained adjustments take
precedence over advisory hints. Conflicting explicit requirements are surfaced.
No Word/web quota, mandatory slide-count gate, universal 40-word truncation or
forced language/pronoun was restored.

The first-unit complete-source, tables/citations/limitations, actual notes,
native-reopen and configured-control behavior remains unchanged.

## Exact checks and evidence

Evidence root `R`:

```text
C:\Users\jokerman\.copilot\session-state\0a75211f-455c-4318-864f-bc8023ce9142\files\p12
```

Each command ran through the existing allowlisted `p12-run.ps1` with synthetic
HOME/USERPROFILE/AppData/temp/Lintel roots, separate source Git configuration,
preopened logs and retained real exits. Original product helpers and fixture
paths were not redirected to personal data.

| Check | Observed result | Evidence under `R\logs` |
|---|---|---|
| Four new documentary methods before source repair | exit 1; original 18 pass, four new methods fail with seven failed assertions including the three variant subtests | `f01-regression-red.*` |
| Repaired working source | 22/22 methods, zero errors/skips, exit 0 | `f01-regression-green.*` |
| **Committed product `32dac88`** | **22/22 methods, zero errors/skips, exit 0** | `f01-committed-tests.*` |
| Original test AST preservation | all original 18 test methods byte-for-byte-equivalent ASTs; four new methods only | `f01-preservation.*`, `f01-frozen-test-bytes.*` |
| First-unit artifact/evidence retention | all 135 inventory files unchanged, including DOCX/PPTX/source/notes/P05 receipts | `f01-preservation.*` |
| First-unit report preservation | original normalized Git SHA-256 remains `b194d2951387e4979f805f7e2885138b41a97ee106b602fb6614d8164deac015` | `f01-preservation.*` |
| Source ownership and whitespace | exactly four approved paths; diff check exit 0 | `f01-scope.*`, `f01-diff-check.*` |

The exact runner is the existing
`tests/integration/document-format-pipeline.sh`, with `--fixture-root R\checks`
and `--native-artifacts R\target\verified-artifacts`. The Python command is
the previously observed Python 3.11.9 interpreter under Git Bash. Python 3.9
grammar parsed; no Python 3.9 execution is claimed.

The four new methods test the **instructional contract**, not an invented CLI
parser: all three omitted template mappings, explicit template precedence,
explicit defaults and no-personal-scan semantics, PPT-only outline hint with
Word/web contrast, and direct-PPT duration fallback/adjustment priority. They
do not claim native customer-summary/transparency-note application runs.
All earlier executable P05/P07 and real saved-artifact retention methods ran too.

No native artifact was edited for F01. DOCX hash remains
`8c8698d12d634e4358bf5f136b6397e066470e6a3a1eada9aca9c0d1099f6bf5`;
PPTX hash remains
`a5910cf41de35a0ab10433089fee8c3b58201a764c765a54f9636d639cdd8e93`.
The prior blocked QA and historical failures were not rewritten.

## Review and limits

Builder self-review maps the four file changes directly to F01 and confirms
preserved defaults without resurrecting the rejected content restrictions.
**Independent recheck is not performed by this builder.** Return this exact
candidate to reviewer `31c39265-13e5-4057-9e78-49bf658749a5`.
No formal QUALITY/compliance/SHIP pass or A15 parent closure follows.

Word application page inspection remains **permission-blocked** after the
coordinator's explicit access denial; the native Word canvas also has no page
renderer. No retry, COM/export/shell or alternate-launch route was attempted.

The coordinator subsequently reported a separate, permitted file-only raster
inspection of all six exact native SVGs using an already installed ImageMagick.
That is new coordinator evidence, not an action performed or independently
verified in this F01 repair. Its applicability and any new P05 evaluation belong
in a later separately bound evidence checkpoint and the same reviewer's decision.
This source correction does not silently clear the prior PPT inspection control.

No workbook, PDF, Visio, shared API/schema, role, installer, registry, generated
surface, common memory or master-plan change is included. Workbook work continues
separately under its existing release; its observed missing persisted caches and
unverified grid rendering are not hidden by the F01 pass.

## Preventive lesson for coordinator capture

2026-09-22: removing an arbitrary universal restriction must not also remove
useful format-specific advisory defaults. Preserve omitted-argument behavior,
explicit selection precedence and all variants in the same contract test;
checking only flag names is insufficient. Keep default hints advisory where
appropriate, and separately retain mandatory content/policy requirements.
This note does not edit the coordinator-owned shared lessons store.

Cycle position: BUILD repair ready for independent SPEC recheck, then eligible
QUALITY. The nine-phase cycle and resume utility remain unchanged.
