# P02 private-sync implementation evidence

**Status:** staged-role-deletion quality repair awaiting independent re-review.
The F1-F3 repair at `17a9f69` passed independent spec re-review; the subsequent quality
stage found a **P2 preservation regression** in staged Markdown deletion. The package
is **not accepted**. This is implementer evidence, not closure or review signoff.
MasterSession owns both reviews and the acceptance checkboxes.

**Authority:** P02, R10 and A26.1-A26.4 in the approved Universal implementation plan;
RU-03 in the runtime audit. Package start: `21261f1ff76be994246060a7817924f2893f2454`;
main baseline: `28061e434be455ca02f135b73244eaf4f73f3a69`.

## Revisions and ownership

| Revision | Purpose |
|---|---|
| `3be51ebd07648b0b41112d7ed81e8ea26aa78936` | Shared binding implementation, both entrypoints and regression tests |
| `53b9462e2dd23f8f8db0bee382c010d712a2e2be` | Test-only correction: even identity-only project origins use temporary bare repositories |
| `a8b36a983499d5e2bcdd7968fd452156eac8442f` | Original evidence report; rejected candidate preserved in history |
| `17a9f69eb8ee48a4fd92762958d0adafad598b64` | Repair F1-F3 and extend preservation regressions; subsequently passed spec re-review |
| `82d0f3980989bfc8cd6a2722982898d68fc92623` | F1-F3 repair evidence; spec passed, then quality identified staged-role deletion regression |
| `9bdaeb425be6cc2a868cde4bdf02c264d0862112` | Current product/test revision: preserve staged owned role deletions and retain unrelated-index guards |

Branch: `jokerman-microsoft-universal-private-sync`. All commits are local.

| Owned file | Change |
|---|---|
| `bin\li-roles-sync` | Retained CLI entrypoint; delegates to the shared implementation |
| `bin\li-lessons-sync` | Retained CLI entrypoint and canonical `lib\paths.sh` source resolution |
| `lib\private_sync.py` | One binding schema, destination guards, safe setup and Git operations, project identity |
| `tests\integration\private-sync-binding.sh` | Discoverable focused integration runner |
| `tests\integration\private-sync-binding.py` | Real subprocess/Git scenarios with isolated homes and local bare remotes |
| `.claude\plans\universal-implementation\reports\P02.md` | This evidence and migration handoff |

No common audit/state/pack helper, generated output, shared plan/todo/memory, other
worktree, personal configuration or public documentation was modified. The shared
helper is justified by the identical binding and transfer contract of the two tools;
there is no second schema or duplicated implementation.

## Independent review corrections

Review report: commit `2d6030d3d392e2a70cc3eae8e12e5f6c231df893`,
`.claude\plans\universal-implementation\reviews\P02.md`. It covers the immutable
original candidate, not these repairs. No reviewer-owned file was edited or imported
into this worktree. The repair changes only `lib\private_sync.py`, its same-name
integration support and this report; all previous candidate commits remain ancestors.

| Finding | Original failure and citations | Repair and discriminating evidence |
|---|---|---|
| F1, P1 | `lib\private_sync.py:45-51,122-130,191-206,268-269` at the reviewed source: Python discarded a file URL fragment that Git treated as part of a distinct repository path. Setup chose the wrong repository; drift passed both CLIs' transfer guards. | URLs remain opaque for selection and exact comparison against Git-expanded fetch/push URLs. Literal and percent-encoded suffixes have positive setup/push/pull cases. Fetch-only and push-only drift to the distinct suffixed bare repository fail before any transport or local staging. |
| F2, P1 | `lib\private_sync.py:273-277`: `git pull --ff-only` honored a forced fetch mapping into the local branch, replacing pending HEAD/index/content before returning failure. | Fetch uses one full branch source, no destination ref and empty `--refmap=`, with tags/pruning/submodule fetching disabled. Only FETCH_HEAD/objects are fetched. Ancestry, binding and unchanged branch/HEAD are checked before a separate fast-forward-only merge. Divergence preserves all local branches, tags, tracking refs, index entries and file bytes; legitimate fast-forwards, first pulls and local-ahead cases remain useful. |
| F3, P1 | `lib\private_sync.py:208-226,248-261`: identical raw origin aliases with different repository-local rewrites shared a lesson key. | Identity uses `git remote get-url --all origin` in each source repository. Different effective endpoints get distinct records; a different alias for the same endpoint keeps identity. Historical raw-alias and basename records remain intact and round-trip. |

Before product repair, nine added scenarios produced **17 failing subcases** (exit 1,
60 assertions, zero skips), reproducing all three review mechanisms. A preservation
test then caught a first-pull regression in the initial fetch/merge split; both
unborn-cache cases failed before that was corrected. No failing version was committed.

The independent report's exact five-case executable replay was run against the
repaired source. It exited **0**, reporting **ACCEPTANCE VIOLATIONS: 0**. Both F1
variants rejected drift without reaching the unapproved remote; both F2 variants
retained the pending HEAD, index tree and record bytes; F3 retained separate records.
This is an implementer replay of independent test material, not an independent
approval of the repaired revision.

**Correction lesson for coordinator capture (2026-09-20):** test the actual Git
transport/refmap behavior, not a parser's presumed URL equivalence or the eventual
exit code. A nonzero command can already have mutated local state. Source identity
must resolve under the source repository's effective configuration. The original
539 green assertions did not cover these contracts. Shared memory remains
coordinator-owned and was not changed here.

### Q1 staged role deletion quality correction

Independent recheck: commit `a8fe1cd23590eb4d7fb4a699e76af7758761e46c`,
`.claude\plans\universal-implementation\reviews\P02-recheck.md`. It records
spec **PASS** at `17a9f69`, F1-F3 closed, and quality **FAIL: 0 P1, 1 P2, 0 P3**.
Q1 is the only finding in that completed bounded quality pass. The report was read
from Git objects, not copied or edited in this worktree.

MasterSession returned Q1 on 2026-09-20 against
`lib\private_sync.py:258-264` at product revision `17a9f69`: after publishing
`architect.md` and `retired.md`, `git rm retired.md` followed by role push failed
as "unrelated staging." The HEAD-to-index deletion was no longer present in
`git ls-files --cached`. The reviewer compared the original `21261f1` role helper
and confirmed this was a preservation regression, not a new feature request.

The repair explicitly recognizes only Markdown deletions in the HEAD-to-index
diff as owned role changes. It keeps those paths separate from the index/worktree
paths passed to `git add`, so an already staged deletion is neither re-added nor
rejected. A validated deletion-only index is committed even when no role files
remain. `--no-renames` exposes both sides of renames to the ownership check;
renaming an unrelated text file to Markdown does not authorize deleting its source.
Lesson selection, endpoint guards, fetch isolation and source identity are unchanged.

Before repair, the focused five-scenario run exited **1** (47 assertions, two
failures): partial and all-role staged deletion failed; unstaged deletion and the
unrelated modification/deletion controls passed. After repair the extended focused
run exited **0**: **8 scenarios, 86 assertions, 0 failures, 0 skipped**:

```powershell
bash tests\integration\private-sync-binding.sh --case test_a26_4_staged_owned_role_deletions_round_trip --case test_a26_4_unstaged_owned_role_deletions_round_trip --case test_a26_4_staged_deletion_of_all_roles_round_trips --case test_a26_4_staged_role_deletion_does_not_readd_a_retained_local_copy --case test_a26_4_unrelated_staged_changes_and_deletions_remain_unpublished --case test_a26_4_renaming_an_unrelated_file_to_markdown_does_not_grant_ownership
```

Root/nested and all-role deletions round-trip to another temporary cache, while
the active architect role and historical content remain preserved. A local copy
retained with `git rm --cached` survives that push without being re-added. This is
not a permanent exclusion rule: future untracked Markdown remains selectable unless
ignored. Both tools reject unrelated staged modifications, deletions and
cross-scope renames before transfer, with local state and remote HEAD unchanged.
Existing unrelated-addition negative cases are retained.

The report's exact Q1 replay, including its original-base control, also ran against
`9bdaeb4`: exit **0**, `CONFIRMED QUALITY REGRESSIONS: 0`. The candidate returned
exit 0 for both staged and unstaged deletion, synchronized removal, and preserved
`architect.md`. The unchanged base-control invocation returned 0 as well.

```powershell
@'
import subprocess
text = subprocess.run(["git", "show", "a8fe1cd23590eb4d7fb4a699e76af7758761e46c:.claude/plans/universal-implementation/reviews/P02-recheck.md"], check=True, capture_output=True, text=True, encoding="utf-8").stdout
code = text.split("```python\n# p02-q1-repro\n", 1)[1].split("\n```", 1)[0]
exec(compile(code, "<P02-Q1-independent-review-replay>", "exec"))
'@ | bash -c 'export LINTEL_TEST_BASH="$BASH"; python3 -B -'
```

**Correction lesson for coordinator capture (2026-09-20):** index inventory alone
cannot establish ownership of staged removals. Compare HEAD, index and worktree,
and keep authorized deletion records separate from paths that need staging.
Exercise the final-file/deletion-only case as well as deleting one of several files.
No shared memory file was changed by this worker.

## Acceptance evidence

Integration commands were run from this worktree in PowerShell through Git Bash,
Git `2.55.0.windows.3` and Python `3.11.9`. Subprocess exit codes were captured
before reading logs; no output pipeline converted failures to successes.

The per-leaf table records the separate runs for the F1-F3 repair at `17a9f69`.
The later quality-repair run retains these scenarios and adds the eight deletion/
ownership scenarios above; counts are not independent requirement counts.

| Leaf | Exact command | Result | Discriminating and preservation evidence |
|---|---|---|---|
| A26.1 | `bash tests\integration\private-sync-binding.sh --leaf A26.1` | Exit 0; 12 scenarios, 102 assertions, 0 skipped | A then B binds actual origin and enabled config to B; A's ref is unchanged. Repeated setup preserves bytes/index/history. Existing README, ignore file and local content survive. Missing remotes and clone conflicts fail without enabling. A failed rewritten rebind disarms prior authorization. Literal and encoded file-URL suffixes select the exact real remote through complete round trips. |
| A26.2 | `bash tests\integration\private-sync-binding.sh --leaf A26.2` | Exit 0; 10 scenarios, 430 assertions, 0 skipped | Missing, disabled, URL-only, malformed, wrong-version, wrong-cache and non-boolean bindings reject push/pull. Extra URLs, both Git rewrite types and fetch-only/push-only suffix drift reject. Forget retains dirty/untracked/staged content, HEAD, index and origin. Git trace proves no upload-pack/receive-pack starts during rejected operations. Each drift variant starts from a verified healthy binding; CLI arguments cannot override internal paths. |
| A26.3 | `bash tests\integration\private-sync-binding.sh --leaf A26.3` | Exit 0; 7 scenarios, 65 assertions, 0 skipped | Same-basename projects remain distinct beside untouched legacy records. Repeated/subdirectory calls and linked worktrees keep identity. Canonical local/file URL aliases stay stable across clone names; distinct local `.git` suffixes and Git-literal URL suffixes cannot collide. Differently rewritten raw aliases retain separate lesson records and historical raw-alias files. |
| A26.4 | `bash tests\integration\private-sync-binding.sh --leaf A26.4` | Exit 0; 23 scenarios, 192 assertions, 0 skipped | Both tools retain positive round trips, explicit origin, failed-transfer retries, dirty conflict refusal, unrelated-history preservation, nested roles and Git symlinks. Forced fetch mappings cannot replace pending branches/index/content or unowned refs. Legitimate fast-forwards, unborn-cache first pulls and local-ahead no-ops pass; conflicting first pulls preserve local data. |

In the original candidate, after its final fixture correction, the two-machine scenario was rerun with:
`bash tests\integration\private-sync-binding.sh --case test_a26_4_two_machine_push_pull_and_explicit_origin`
(exit 0; 2 scenarios, 25 assertions, 0 skipped).

### Current quality-repair committed-tree run

`bash tests\integration\private-sync-binding.sh` at
`9bdaeb425be6cc2a868cde4bdf02c264d0862112` exited **0**:
**60 scenarios, 875 assertions, 0 failures, 0 skipped**. All 52 prior scenarios
remain, plus eight staged-deletion and unrelated-index ownership scenarios. The
complete run covers A26.1-A26.4 through both public wrappers, real Git commands
and isolated local bare remotes. Product and test files were unchanged throughout
verification; the following report-only commit does not alter that tested content.

Both unchanged independent-review replays ran at the same product revision:
F1-F3 exited 0 with **zero acceptance violations**, and Q1 exited 0 with
**zero confirmed quality regressions**. These implementer runs are supporting
evidence, not independent closure of the new candidate.

### F1-F3 repaired committed-tree run (historical)

`bash tests\integration\private-sync-binding.sh` at
`17a9f69eb8ee48a4fd92762958d0adafad598b64` exited **0**:
**52 scenarios, 789 assertions, 0 failures, 0 skipped**. All original positive and
negative scenarios remain; the additional scenarios cover F1-F3 and the first-pull/
local-ahead preservation requirements. Product and test inputs were unchanged
throughout the run and remain identical in the following report-only commit.

The exact independent five-case replay used this command (exit 0, zero violations):

```powershell
@'
import subprocess
text = subprocess.run(["git", "show", "2d6030d3d392e2a70cc3eae8e12e5f6c231df893:.claude/plans/universal-implementation/reviews/P02.md"], check=True, capture_output=True, text=True, encoding="utf-8").stdout
code = text.split("```python\n# p02-local-repro\n", 1)[1].split("\n```", 1)[0]
exec(compile(code, "<P02-independent-review-replay>", "exec"))
'@ | bash -c 'export LINTEL_TEST_BASH="$BASH"; python3 -B -'
```

That replay reads the committed review from Git objects without changing or
checking out the reviewer branch. It creates only its own local synthetic fixtures.
The same exact replay was rerun at `9bdaeb4`: exit **0**, **zero F1-F3 violations**.

### Original committed-tree run (historical, insufficient for acceptance)

`bash tests\integration\private-sync-binding.sh` at
`53b9462e2dd23f8f8db0bee382c010d712a2e2be` exited **0**:
**36 scenarios, 539 assertions, 0 failures, 0 skipped**. The source and tests were
not edited during this run. It exercises both helpers through their real Bash
entrypoints and covers all four leaves together. Only this report was added after
that verified product/test revision. Independent review confirmed these counts but
found F1-F3 outside their coverage; they do not establish package acceptance.

### Red evidence

Before either product helper changed,
`bash tests\integration\private-sync-binding.sh` exited 1: 21 scenarios, 97
assertions, 50 failing subcases, 0 skipped. Failures reproduced stale destination
selection, sync after forget, missing/invalid configuration being ignored, basename
collisions and success-shaped transport failures.

An expanded ten-scenario red run then exposed twelve candidate failures before
repair: failed-rebind authorization, wrapper argument isolation, overly aggressive
identity normalization, readable Windows names and help/long-option handling.
Those regressions are included in the final tests.

### Additional checks

The following passed during both repair rounds. The shape/shell checks were repeated
before `9bdaeb4`; grammar, ancestry, clean-input and whitespace checks also ran after it:

- `bash tests\shape\bin-scripts-executable.sh`: all 22 tracked CLI mode assertions passed.
- `bash tests\shape\claude-home-paths.sh`: all 20 canonical-path/layout assertions passed.
- `bash -n bin\li-roles-sync`, `bash -n bin\li-lessons-sync` and
  `bash -n tests\integration\private-sync-binding.sh`: each exited 0 separately.
- Both Python files parsed with `ast.parse(..., feature_version=(3, 9))`.
  This checks grammar compatibility, not execution under Python 3.9.
- `git diff --check` and staged whitespace checks passed; both CLI entrypoints retain
  mode `100755`. The product/test tree was clean at `9bdaeb4` before the report update,
  with `82d0f39` and all earlier candidate commits preserved as ancestors.

Source inspection confirmed that both installers recursively copy `lib` and `bin`,
and the portable Copilot generator includes both trees. No installer change or
generated-output update is needed to include the new shared helper. This is static
inclusion evidence, not a claimed consumer installation run.

## Migration and retained behavior

The existing per-tool configuration filenames are retained:
`<HOME>\.lintel-roles-remote.txt` and `<HOME>\.lintel-lessons-remote.txt`. Explicit
`setup <repo-url>` writes JSON with `schema_version: 1`, a boolean `enabled`, the
selected URL and canonical local cache directory. A legacy URL-only file never
authorizes transfer automatically; the operator must run setup again.

Setup verifies exactly one effective fetch URL and one effective push URL, including
Git URL rewrites, by exact endpoint-string comparison. No URL components are dropped
or decoded for binding authorization. A spelling change requires explicit setup,
even if the operator knows that both spellings address the same remote.
Rebinding updates the actual origin, clears a stale local push URL,
and enables only after verification. It disables the previous binding before changing
an existing origin. It never resets, commits or deletes the existing cache. Initial
clones are staged and checked for local conflicts before importing missing content.
Existing local files are not overwritten by setup.

`forget` atomically records a disabled binding. It does not delete local files,
history, staged changes or the Git origin. Repeated forget is safe; reactivation
requires explicit setup. Missing/malformed bindings and mismatched origins fail
before staging, copying lessons or starting a Git transfer.

Lessons use `<readable-name>--<sha256>.md` records. Identity comes from the source
repository's effective fetch origin after Git's rewrite rules, or its canonical Git
common directory when no origin exists. Only losslessly round-trippable canonical
file URIs share plain local-path identity; other spellings are not guessed to be
aliases. Git-literal file-URL suffixes remain part of identity. Historical raw-alias
records remain rather than being silently reassigned. Source-repository names and
identities are never written in plaintext to a second project registry. Legacy basename-only records
are retained byte-for-byte rather than ambiguously reassigned or deleted.

Both entrypoints retain setup/push/pull/status/forget/help, default and `--status`,
and help aliases. `--push` and `--pull` also work. Role Markdown, nested role files,
ignore rules and Git symlink representation are retained. Lesson paths still come
from `lib\paths.sh`, including the existing legacy fallback. Pull updates the
private cache, never the source repository's lessons file. Fetch intentionally does
not update configured tracking refs, local branches or tags: it fetches the requested
branch into FETCH_HEAD, checks ancestry, then fast-forwards only the current branch.
Rejected divergence may leave fetched objects and FETCH_HEAD but cannot replace
pending branch/index/worktree content. Failed pushes retain their local commit for
retry; Git errors remain nonzero and visible.

Role push accepts intended staged or unstaged Markdown deletion, including deletion
of the last role. It does not erase historical commits. An owned role deletion never
authorizes an unrelated staged addition, modification, deletion or rename.

## Findings, boundaries and handoff

RU-03 remains subject to package acceptance. The original independent spec verdict
was **FAIL: 3 P1, 0 P2, 0 P3; quality NOT REACHED**. F1-F3 subsequently passed
independent spec re-review at `17a9f69`; quality then found the staged-deletion P2.
The new repair has recorded test evidence but no independent approval yet. The
implementer did not delegate a nested agent or approve its own changes.

All transfer fixtures use temporary local bare Git repositories, with file-only Git
protocols, isolated HOME/USERPROFILE/XDG paths, disabled system/global Git config
and synthetic identities configured in the individual fixture repositories.
There was no GitHub/network operation, credential use, real private synchronization,
push/PR/default-branch mutation or user-global change.

Remaining boundaries:

- Independent review of the latest quality repair is pending with MasterSession;
  prior spec approval is not blanket approval of the changed revision.
- Actual remote privacy, credentials, network transports, Linux/macOS execution,
  native Bash 3.2 and actual Python 3.9 execution are unverified here.
- A changed effective origin or moved remote-less Git directory creates a new
  identity; previous records stay intact. Cross-protocol and noncanonical file-URL
  aliases are deliberately not auto-merged. Already-overwritten historical records
  are not reconstructed automatically from Git history.
- Rebinding to an unrelated nonempty vault requires explicit manual history
  reconciliation; push/pull refuse rather than force-pushing, resetting or merging it.
- This is not a lock against a separate process changing Git/configuration during an
  operation. Enabled/current binding is checked at the action boundary, including
  again immediately before push or merge. A branch/HEAD change observed during fetch
  refuses integration; no atomic multi-process lock is claimed.
- No full repository suite, consumer installation, CI or generated-output
  regeneration was run in this isolated package. Those belong to integration.

No outside-card documentation edits are required to keep existing descriptions true.
The entrypoint help documents explicit re-setup, disabled forget and retained records;
an optional coordinator-owned migration note can reuse the migration section above.
