# P07: Stable effective profiles and compatibility

**Date:** 2026-09-20

**Status:** Implemented and locally exercised; not independently approved or final
joined acceptance. P05's direct control-consumer case and P06/P08 integration remain
explicitly pending below. No common plan, work map or working-state file was changed.
The initial component specification review found F01 (P1) and F02 (P2). MasterSession
reported that re-review closed those individually but rejected the component again
for F03 (P1): ID-only bootstrap could still recreate or silently adopt a context.
The latest revision repairs the complete selection-entrypoint invariant, not just
the reported convenience call. Renewed independent spec review remains required;
quality review must wait for its pass.

## Authority and exact revisions

- Authorized package: [P07](../packages/P07.md), A07.1-5 and A20.3/4.
- Original baseline: `40c279520a86945cc7e607692355bf5231446ff1`.
- **Initial product commit:** `912420f91729f1618b8f22804ea7ccd95bd7f6e8`.
- **First repair:** `d02bb248b61dbbc703eb5252f5b46d86bdfc102b`.
  This owned four-file follow-up retains the initial product and addresses the
  two independent specification findings; do not treat `912420f` alone as accepted.
- **Current frozen product:** `56981edc4bd73020fea78e20526b10d62e822305`.
  This owned five-file follow-up addresses F03 across bootstrap, bind, verify,
  rebind, cache-clear, inherited accessors and retained-history creation/recovery.
  `d02bb248` alone is not component acceptance.
- Branch: `jokerman-microsoft-universal-profile-context`.
- Explicit P04 dependency, not P07 implementation:
  `8ea0fc4e4280803d1093468d06828daa54f9c700`, cherry-picked with MasterSession
  authorization as `b4e93162b95ea2562a04316fd0cfd251010a4028`.
  It changes only `skills/brief-forge/SKILL.md` (+3/-33) to use the public accessor.
  MasterSession already integrated that dependency separately as `116d74b`.
  Integrate the owned product and repair commits, not a duplicate of the dependency.

The report is a separate evidence artifact so its product SHA identifies an immutable
code revision rather than attempting a self-referential commit hash. Independent
specification/quality review is arranged by MasterSession; this is implementer evidence.

## Changed files and ownership

| Owned paths | Change |
|---|---|
| `lib/profile_context.py` | One stdlib parser, typed resolver, provenance, compatibility, digest, bind/verify/rebind and CLI implementation |
| `lib/profile-context-schema.json` | Versioned profile record, reference and external requirement data shapes; no competing P05 control schema |
| `lib/pack-resolver.sh` | Preserved public shell accessors over the shared implementation; explicit errors instead of emergency success |
| `lib/copilot-env.sh` | Real durable bootstrap with explicit-root preservation and no PID/PPID-derived identity |
| `lib/pack-schema.yaml`, `packs/_default/pack.yaml` | Independent schema/product/feature compatibility; neutral policy behavior retained |
| `.claude/decisions/0029-required-profile-context.md` | Explicitly accepted required-policy, drift, compatibility and migration contract |
| `docs/concepts/pack-resolver.md`, `pack-inheritance.md`, `pack-defaults.md` | Current source precedence, types, lifecycle, errors, compatibility and limits |
| `skills/pack-validate/SKILL.md` | Narrow ownership extension approved by MasterSession; real shared checks, no activation or live-policy claim |
| `tests/integration/universal-profile-context.sh`, `.py` | Actual subprocess producer/bootstrap/handoff/resume, synthetic requirements and negative cases |
| `tests/unit/enterprise-pack-resolution.sh`, `pack-inheritance-depth-3.sh`, `pack-resolver-fallbacks.sh` | Preserved parser/inheritance cases plus explicit changed-cache acceptance |
| `tests/integration/pack-source-target-resolution.sh`, `enterprise-pack-impact.sh` | Installed-source dependencies and preserved real routing/evaluator/digest consumers |
| `tests/shape/extension-pack-contract.sh` | Retained extension/scaffolder assertions; Python executes JSON checks when jq is absent |

No host registry, review implementation, Swarm runtime, audit writer, installer,
generator, global configuration, private company pack or historic ADR was edited.

## Per-leaf implementation and acceptance

| Leaf | Original defect and implemented behavior | Verification and acceptance boundary |
|---|---|---|
| A07.1 | A failed company load could look like neutral first use. ADR-0029 distinguishes repository/invocation-required load, optional legacy preference and no-profile first use. | Required missing/malformed/incompatible fixtures fail; optional fallback is labeled; invalid neutral baseline fails. Architecture decision implemented; independent review pending. |
| A07.2 | Text extraction, merge and freshness logic could disagree. One parsed representation supplies whole-block inheritance, typed values, exact field origins and compatibility. | Existing enterprise/inheritance suites pass; typed JSON tests distinguish null/false from quoted strings, preserve comma/hash list items, verify ancestor order and exact source path/version/raw-byte SHA-256. |
| A07.3 | PID-derived caches did not identify work across shells. Explicit context/reference APIs and the documented bootstrap now establish and verify durable work selection. | Fresh producer/consumer, delegated reference-only handoff, cold resume with a different ambient host ID, two no-ID bootstrap shells and simultaneous no-ID bootstraps pass. No live model/client session identity is invented. |
| A07.4 | Pointer changes, deletion and same-time edits could silently refresh or retain stale policy; failures returned success values. Bound access now fails with explicit drift/required errors. | Pointer/manifest/parent changes preserve old record bytes and fail without policy values. Explicit rebind archives the prior generation and revokes old references. Required-policy error bridge tested; direct P05 consumption pending its code. |
| A07.5 | Company effects were not reliably attributable to the selected profile. Synthetic profiles now drive real routing, gate-list/evaluator and session-digest inputs plus distinct design-input artifacts. | `enterprise-pack-impact.sh` passes real existing consumers. Rapid/strict fixtures produce different routes, gate inputs and design token references with bound identity. This is not a renderer, company-control execution or enterprise ROI claim; joined vertical acceptance remains P11/P14. |
| A20.3 | Historical internal v4/v5 numbers could be mistaken for public product versions. Schema, pack release, public product and feature ranges now validate independently. | Missing legacy schema remains schema 1; exact historical `>=4.0.0` is recorded as pack-v1, not compared to public 0.x. Product metadata and capabilities round-trip independently; ambiguous historical ranges require migration. |
| A20.4 | Pack compatibility could be reported without executing it. Valid synthetic declared constraints pass; incompatible schema/product/feature and unknown-feature fixtures reject for their own reason. | Every ancestor is checked, so child replacement cannot erase parent requirements. Missing product metadata rejects a declared product constraint. External-contribution provenance beyond this package remains the P13 portion of A20.4. |

## Exact shared API

### Python producer and consumers

All functions below come from `lib/profile_context.py`; no caller should reparse
pack manifests or rebuild a fingerprint independently.

```python
config = ProfileConfig(
    source=approved_source,
    repo=temporary_repo,
    home=temporary_home,
    packs=temporary_home / "packs",
    pointer=temporary_home / "packs" / "active-pack",
    context_id="synthetic-work",
)
record = load_profile_context(config, create=True)
reference = profile_reference(record)
verified_record = verify_profile_reference(reference, config)
policy_load = required_policy(verified_record)
```

`ProfileConfig` normalizes paths itself. Additional fields are
`context_file: Path | None = None`, `explicit_pack: str = ""`, and
`expected_reference: dict | None = None`. The latter uses the same strict reference
schema and makes every `load_profile_context` call verify the expected generation,
even if `create=True` was supplied; it cannot recreate a lost resume pin.
Other public operations are `resolve_profile(config)`,
`validate_profile_reference(reference)`, `bootstrap_profile_context(config)` and
`rebind_profile_context(config, reason)`. Normal consumers verify before use;
`create=False` is the default for `load_profile_context` and missing resume state
is not silently recreated. `ProfileError.code` distinguishes required load,
baseline, drift, reference/context/input and compatibility failures.

The strict reference is exactly:

```json
{
  "schema_version": 1,
  "context_id": "synthetic-work",
  "generation": 1,
  "digest": "sha256:<64 lowercase hexadecimal characters>",
  "name": "synthetic-strict",
  "version": "1.2.3"
}
```

The record additionally holds source/target roots, selection mode/status, complete
ancestry and baseline identities, typed effective values, field provenance, input
hashes, compatibility decisions, rebind reason and previous reference.

P05 owns the canonical `required_policy` schema. The producer emits
`{required, status, source, version, applicability}`, a valid subset of that schema
(optional `reason` is omitted). Loaded required policy has nonblank source/version
and known applicability; neutral/optional is `not_required`/`not_applicable`, not
verified compliance. No competing control schema is defined here.

### Shell compatibility and new functions

Preserved: `get_active_pack_name`, `get_loaded_pack`, `validate_pack`,
`resolve_pack_field`, `pack_field_is_true`, `pack_is_extension`, `pack_namespace`,
`pack_workflow`, `clear_pack_cache`. Known internal directory/chain/field wrappers
also remain; `_pack_dir` retains native-to-MSYS spelling for existing shell callers.

Added: `pack_compatibility`, `resolve_pack_field_json`,
`profile_field_provenance`, `profile_context_json`, `profile_context_reference`,
`profile_required_policy`, `bind_profile_context <id>`,
`verify_profile_context [reference-file]`, `rebind_profile_context <reason>`.

Profile failures exit nonzero without success-shaped values. The deliberate
exception is `profile_required_policy`, which emits structured `status:error`
metadata **and still exits nonzero** for P05's consumer. Missing optional legacy
field reads remain empty; typed field reads distinguish absent status 1 from
explicit JSON null status 0. Raw cache parsing is no longer an API.

Successful bootstrap/bind/verify/rebind/cache-clear exports the exact JSON reference
as `LINTEL_PROFILE_REFERENCE` through one `_profile_accept_reference` helper.
The same operation also sets its validated context ID; no successful lifecycle
selection is ID-only. Every subsequent accessor and inherited process uses
the shared validator to check its context, generation and digest. A resumed
invocation requirement is retained without repeating `LINTEL_PROFILE_PACK`.
A later rebind, missing pin or corrupted reference cannot neutralize subsequent
policy reads. Missing/invalid transported policy remains `required:true`,
`status:error`, `applicability:unknown` with a nonzero exit.

### Precedence, lifecycle and fingerprint

Repository `.claude/profile-requirements.json` is read before the possibly broken
pack. It contains exactly `schema_version: 1` and `required_pack`. Conflicting
explicit selection fails. Otherwise explicit `LINTEL_PROFILE_PACK` is required,
the legacy active pointer is optional, and no selection is neutral first use.
Named source precedence stays configured store -> target packs -> source bundle.

`lintel_copilot_env <target> [context-id]` preserves approved explicit source,
target/home/store configuration and binds/verifies before returning. Without a
supplied stable identity it atomically selects a repository work reference at
`.claude/runtime/profiles/selected.json` and exports its ID. This is not a fake host
session ID. It now always exports the complete expected reference too. One-shot
direct reads remain usable but cannot claim a pinned handoff.

SHA-256 covers canonical profile data and raw-byte hashes for pointer, requirements,
manifest ancestry, neutral defaults and compatibility source. It includes source
identity, effective values and origins, not only names/versions/mtimes. A reference
also binds context and generation. Rebind requires a reason, validates first, keeps
`history/`, updates only a matching selected work reference and invalidates old
references. Different target/source roots are not silently transplanted as resume.
Rebind now uses the same repository-selection -> context lock order as bootstrap
and publishes both current and selected records inside that transaction. A stale
same-context selected reference can be recovered only if retained history
corroborates it; a different context's selection is preserved.
Initial binding and every subsequent publication retain the current record as well
as prior records in immutable history, using the existing schema. `create=True`
cannot reset a lost retained context to generation 1. A reason-bearing rebind can
recover an unambiguous latest retained record into the next generation, preserving
its required selection. A rolled-back/conflicting current record or corrupt/ambiguous
history is refused rather than silently repaired by a read.

### Compatibility and migration

- `schema_version`: supported manifest syntax, independent of the product.
- `version`: the pack release.
- `requires_lintel_product`: actual source `.claude-plugin/plugin.json` version.
- `requires_capabilities`: resolver feature contracts from `lib/pack-schema.yaml`,
  not host permissions or observed operations.
- Exact `requires_lintel: ">=4.0.0"`: historical pack-v1 marker. Other historical
  ranges need explicit migration, never a guessed public-version comparison.

Ranges use full semver with `=`, `==`, `<`, `<=`, `>`, `>=` and conjunctions.
Unsupported syntax is explicit. Missing product metadata is allowed only when no
product constraint was requested; it is not verified product compatibility.
Legacy YAML cache files are not trusted as content-bound resume evidence and are
not deleted. Use a new verified context/replan rather than silently importing one.

## Intentional legacy assertion changes

These changes enforce ADR-0029, not a weaker test expectation:

| Specific old assertion | Old behavior | New assertion and preserved positive case |
|---|---|---|
| `enterprise-pack-resolution`: malformed edit after priming | Automatically returned `_default` | Nonzero `PROFILE_DRIFT`, empty identity output; restoring valid bytes and explicit rebind loads `team`. Initial invalid optional preference still has diagnostic neutral fallback. |
| `enterprise-pack-resolution`: edited missing ancestor | Automatically returned `_default` | Same explicit drift refusal; valid ancestry can be restored/rebound. Dedicated required missing-parent cases never fall back. |
| `enterprise-pack-resolution`: invalid active-pointer switch | Silently kept cached `team` | Pointer change blocks. Explicit `clear_pack_cache`/rebind permits the **optional** preference fallback with `_default`, `advisory` and recorded `fallback` status in agreement. |
| Fallback scenarios 7/8 and enterprise-impact switch | Continued stale values after valid pointer switch or pack deletion | Nonzero drift without policy values. Rebind preserves evidence and valid source/extension behavior; initial no-profile and explicit `_default` still pass. |
| Inheritance missing optional field | Accepted empty or hardcoded `2000` | Requires exactly `2000` from validated neutral data and verifies neutral provenance; no parent deep merge or emergency constant. |
| Invalid neutral baseline | Getter could return hardcoded advisory success | Explicit `PROFILE_BASELINE` error; valid no-profile first use remains unchanged. |

The old positive cases for scalar/list quoting, whole-block replacement, extension
validation and scaffolding, installed-source lookup, gate lists, existing routing,
evaluator dispatch, session digest and optional capture all still execute.

## Commands, results and environment

Execution used Windows Git Bash and Python 3.11.9, with temporary synthetic homes
and repositories. No actual private pack/profile was read or activated.

| Command/scenario | Actual result |
|---|---|
| Initial new lifecycle harness before implementation | Failed because the shared helper did not exist; no false passing skeleton |
| First structured lifecycle matrix | 12/12 pass |
| Expanded bootstrap/required-policy matrix | 18/18 pass |
| Stable scoped lifecycle run | 21/21 pass, 134.256 seconds; no skips |
| Exact provenance/hash, unchanged drift-record and recorded-compatibility assertions | 3 targeted cases pass, 21.878 seconds |
| Actual three runnable `pack-validate` blocks | 1 additional case pass, 3.487 seconds; required-selector failure propagates, no activation |
| Initial committed full 22-case lifecycle run on `912420f` | 22/22 PASS, 119.326 seconds; zero skips. Insufficient for the two subsequently found spec defects; superseded by the expanded coverage below. |
| `bash tests/unit/enterprise-pack-resolution.sh` | PASS |
| `bash tests/unit/pack-inheritance-depth-3.sh` | PASS |
| `bash tests/shape/pack-resolver-fallbacks.sh` | PASS, all nine original scenarios retained with documented changes |
| `bash tests/integration/pack-source-target-resolution.sh` | PASS, real creation snippets and source/store precedence |
| `bash tests/integration/enterprise-pack-impact.sh` | PASS, existing routing/evaluator/digest consumers |
| `bash tests/shape/extension-pack-contract.sh` | PASS with jq assertions actually run; no skips |
| `bash tests/unit/capture-vault-sink.sh` | PASS |
| `bash tests/unit/brief-forge-evaluator-runs.sh` | PASS after the explicit P04 dependency; unknown evaluator still blocks and audits |
| `bash tests/shape/adr-numbers-unique.sh` | PASS, 29 unique ADR numbers |
| `git diff --check`, Bash syntax, JSON syntax and owned Markdown link checks | PASS |
| `ast.parse(..., feature_version=(3, 9))` for Python implementation/tests | PASS; grammar compatibility, not an observed Python 3.9 runtime |

The nine existing scripts ran as one sequential `&&` matrix. The new integration
suite used separate temporary fixtures and ran independently; no concurrent full
repository suites or common-state mutation occurred.

jq was initially absent. MasterSession restored an official, task-local jq 1.8.2;
this worker independently checked its SHA-256
`a6fc67fedaf9128a3309a1e2ebb8b986aeccf70122ee46d2cb4849e423f0c627`
and version, then prefixed only that test process's PATH. All former jq-only
assertions were rerun. No global installation/PATH/configuration was modified.

## Independent specification corrections

MasterSession authorized reopening only P07-owned scope after the exact-revision
component review. The repair changes `lib/pack-resolver.sh`,
`lib/profile_context.py`, `docs/concepts/pack-resolver.md`, and
`tests/integration/universal-profile-context.py`; this report is separate evidence.
No reviewer artifact, common state or accepted historical decision was edited.

| Finding | Reproduced before repair | Repair and discriminating acceptance |
|---|---|---|
| **P1: reference-only shell resume did not govern later accessors** | With no host ID, verification returned strict while `profile_required_policy` returned `required:false/not_required/bundled-neutral` and `compliance.mode` returned `advisory`. With a changed host ID, the next calls created a second neutral binding. The original test only checked the verification output. | The shell exports the exact verified reference, consumed by the same validator on all subsequent calls and inherited processes. Both host variants now retain required/loaded/applicable, strict source/version, `hard`, and the original reference without extra bindings. Failed verification leaves prior selection intact; changed generation and missing pin block rather than neutralize. |
| **P2: concurrent rebinds could leave selected behind current** | Two real rebind processes with a scheduling-only barrier after A released its context lock both returned success, but current reached generation 3 and selected stayed generation 2. A later explicit rebind left a previously stale selection unchanged. | Rebind uses one selection -> context lock order and updates both records before releasing the transaction. The deterministic interleaving now leaves selected/current at generation 3 with history 1/2 retained and a successful fresh bootstrap. Explicit stale recovery is history-backed; fabricated history and taking another context's selection are refused/preserved. |

Exact test-first runs:

| Command/scenario | Result |
|---|---|
| `bash tests/integration/universal-profile-context.sh ProfileLifecycle.test_reference_only_resume_controls_accessors_and_inherited_process_without_host_id ProfileLifecycle.test_reference_only_resume_controls_accessors_and_inherited_process_with_new_host_id` on the original implementation | **RED**, 2/2 failures, 10.312 seconds; exact required-to-neutral symptom observed |
| `bash tests/integration/universal-profile-context.sh ProfileLifecycle.test_concurrent_rebinds_publish_current_and_selected_as_one_transaction ProfileLifecycle.test_rebind_recovers_history_backed_stale_selected_reference` before the lock repair | **RED**, 2/2 failures, 11.324 seconds; current 3 / selected 2, and stale selection 1 / current 3 observed |
| Extended missing-pin policy assertion before mandatory-error correction | **RED**, `required:false` despite prior required resume; then corrected to required/error/unknown with nonzero exit |
| Four focused resume/continuation cases | **GREEN**, 4/4 pass, 33.608 seconds; includes actual inherited process reads, not just a verifier response |
| Three focused transaction/recovery cases | **GREEN**, 3/3 pass, 32.584 seconds; deterministic scheduling, retained history and other-context preservation |
| Full repaired lifecycle suite | **29/29 PASS**, 178.995 seconds; zero skips |
| All nine existing scoped scripts listed above, rerun after both repairs | **PASS**, zero skips, jq 1.8.2 assertions executed |
| Bash syntax, Python 3.9 grammar and `git diff --check` after repair | **PASS** |
| Full committed lifecycle suite on `d02bb248b61dbbc703eb5252f5b46d86bdfc102b` | **29/29 PASS**, 187.611 seconds; zero skips |

The concurrency test wraps only lock scheduling; both OS processes execute the real
`rebind_profile_context`, persistence and bootstrap consumers. It does not replace
the writer with a mock, force expected data or weaken the success invariant.
Approved source/target roots still must match a transferred reference. An isolated
worktree plus explicitly carried policy target/context is supported; automatic
cross-target identity transfer is not claimed.

**Correction lesson for coordinator capture (2026-09-20):** a correct producer or
verifier response is not proof of lifecycle propagation. Test the full documented
receiver sequence, its next policy-dependent operation and an inherited fresh
process; assert that no unexpected neutral binding is created. For multi-record
state, test a deterministic publication interleaving and recovery, not only
simultaneous startup. Shared lessons/memory remain coordinator-owned and were not edited.

### Exact independent probes and replay provenance

The immutable independent report is commit
`9edf8c1e44c33a0530772eefd2d6537182d7fdda`, path
`.claude/plans/universal-implementation/reviews/P07-component.md`. Read its exact
findings and its two complete executable Python reproductions without importing
the reviewer's files into this worktree:

```bash
git --no-pager show 9edf8c1e44c33a0530772eefd2d6537182d7fdda:.claude/plans/universal-implementation/reviews/P07-component.md
```

That review names product `912420f91729f1618b8f22804ea7ccd95bd7f6e8`,
report snapshot `01c189e9d18373df59a5c2a4731f24a91c304b86`, specification findings
P1 = 1 / P2 = 1 / P3 = 0, and quality **NOT RUN**. Its source citations refer to
that old product: F01 is `lib/pack-resolver.sh:82-89` with selection at `:59-60`;
F02 is `lib/profile_context.py:814-833`, especially `:828-832`. They are not
line-number claims about the repaired file.

| Probe from the independent report | Exact mechanism and observed replay | Relationship to the committed regressions |
|---|---|---|
| `ResumeConsumer.test_reference_only_resume_keeps_required_policy_for_actual_consumer` and minimal F01 block | A separate Bash process runs `verify_profile_context "$HANDOFF" && profile_required_policy && resolve_pack_field compliance.mode`, without the old context/pack selectors and with a changed ambient host ID. The report observed loss with both absent and changed IDs. This worker extracted and ran its minimal Python block **unchanged** against frozen `d02bb248`: the original strict reference, `required:true/status:loaded/source:invocation:LINTEL_PROFILE_PACK/applicability:applicable`, and `hard` were observed, exit 0. | The retained two host-variant tests extend that actual consumer sequence with selected/loaded identity and a further inherited Bash process. They are not represented as the reviewer's unchanged source. |
| `RebindConcurrency.test_concurrent_rebinds_keep_selected_reference_current` and minimal F02 block | The original child uses `sys.settrace` at the old `if config.selected.is_file():` line, after releasing the context lock. Both actual Python processes call the real writer. This worker extracted the block **unchanged** and replayed it on a temporary, Git-archived `912420f` source snapshot: both rebinds succeeded at 2/3, selected/current were 2/3, fresh bootstrap rejected, and documented recovery reached current 4 while selected stayed 2. Its final `selected reference is stale` assertion failed as expected. | The original line-specific trace target no longer exists in `d02bb248`; running that unchanged harness there would test an obsolete trace location, not correctness. The retained repaired-product regression schedules the same publication boundary through a lock-timing wrapper around two real writer processes. The distinct recovery/preserved-history tests then verify repaired outcomes. |
| `IsolatedConsumer.test_real_worktree_consumer_and_explicit_target_boundary` | **Reviewer-observed on `912420f`, not rerun by this worker during this report reconciliation:** real `git worktree add --detach`, separate Bash cwd, approved original policy target/home/source and explicit context preserved exact identity and hard policy. Changing the policy target to the new checkout rejected with `PROFILE_CONTEXT_MISSING`; passing the old context file under the new target rejected with `PROFILE_DRIFT`. | This is genuine isolated-checkout evidence, not the same-directory proxy used by some fixture cases and not a live delegated model session. Source/target transfer restrictions remain intentional; no automatic cross-target transfer or joined P06/P08 acceptance is claimed. |

The exact F02 worker replay printed:

```text
successful rebind generations: 2 3
selected/current generations: 2 3
fresh bootstrap: PROFILE_REFERENCE_MISMATCH
after documented recovery: current 4 selected 2
```

Both replay blocks were extracted directly from the immutable report's Python
fences, without changing their assertions. The F02 wrapper recognized only the
expected stale-reference assertion as successful reproduction of the **old bug**;
it did not call that a passing repaired-product check. The snapshot included only
the old fixture's required helper/manifest/test resources, remained under a
temporary directory and was removed afterward. `PYTHONDONTWRITEBYTECODE=1` was
set for these processes. Frozen product files, reviewer artifacts and global/private
configuration were unchanged.

The same independent reviewer is responsible for renewed specification review and
the first quality pass. P05 owns the unchanged actual four-case Python/public-shell
bridge probe against `d02bb248`; no P05 implementation or substitute was imported here.

## Second specification rejection: complete selection invariants

On 2026-09-20 MasterSession conveyed the same reviewer's closure of F01/F02 and a
new **Stage 1 P1 F03**, at exact `d02bb248`, citing `lib/copilot-env.sh:32-34`.
The actual invocation-scoped documented call was:

```bash
source "$LINTEL_SOURCE_ROOT/lib/copilot-env.sh"
LINTEL_PROFILE_PACK=strict lintel_copilot_env "$LINTEL_REPO_ROOT"
profile_required_policy
```

Deleting only its current profile file let following policy/field calls recreate
neutral data and return `not_required` / `advisory` with exit 0. A persistent
repository requirement kept the policy name but still wrongly recreated the lost
pin. Ordinary bootstrap parents also silently adopted child rebind generations.
The first repair guarded the resume path but left this other selecting path ID-only.

The repaired contract was written to MasterSession **before implementation** and
tested as a matrix:

| Invariant | Mechanism and acceptance |
|---|---|
| Every successful selection carries an exact reference | Bootstrap, bind, verify, rebind and first-use cache clearing share `_profile_accept_reference`. Both reference JSON and context ID are validated by the one Python implementation; no new shell JSON parser or alternative profile schema. |
| Continued work verifies, never refreshes implicitly | Every subsequent field/policy/reference read checks the expected context/generation/digest. Parent and inherited child refuse a missing/replaced pin. Parent refuses a child rebind until explicit verification of its new approved reference. |
| Same-ID bind is not a bypass | A bind to the currently expected ID retains its expected generation. An explicitly different unused work ID may initialize new work, subject to unchanged repository requirements. |
| Missing state is not proof of first use | `load_profile_context(create=True)` checks retained history and the selected reference before initial creation. Every initial/current publication is retained in history, not just the predecessor. Lost generation 1 cannot silently become a new neutral generation 1. |
| Recovery is explicit and attributable | Reason-bearing rebind may recover only an unambiguous, validated latest retained record. It preserves required invocation selection, increments generation and retains all evidence under the F02 selection -> context transaction. Conflicting, incomplete or corrupt history blocks. |
| Replaced/rolled-back state is not accepted | Current records are checked against retained generation identity before live policy verification. A lower or conflicting current generation cannot be accepted even by an ID-only caller. |
| First use and approved boundaries remain available | Valid neutral first use and explicit new work still initialize. Failed bootstrap preserves the prior binding. Approved source/target identities are unchanged; no automatic cross-target transfer. |

### Complete entrypoint test matrix

All rows below execute public shell entrypoints and subsequent consumers. Bootstrap
uses the exact documented `source copilot-env -> invocation-scoped lintel_copilot_env
-> source resolver` order, without hidden pre-sourcing or prebinding. Verify/rebind
rows have explicit producer prerequisites because they operate on existing work.

| Selection entrypoint | Exact reference exported | Parent + inherited child retain required policy | Child rebind blocks parent until explicit verify | Missing and replaced current file block parent + child |
|---|---|---|---|---|
| `lintel_copilot_env` | PASS | PASS | PASS | PASS |
| `bind_profile_context` | PASS | PASS | PASS | PASS |
| `verify_profile_context` | PASS | PASS | PASS | PASS |
| `rebind_profile_context` | PASS | PASS | PASS | PASS |
| `clear_pack_cache` on genuine first use | PASS | PASS | PASS | PASS |

Additional negative/preservation cases execute actual Python `create=True`, the
first-generation and later-generation history guard, explicit missing-current
recovery from generations 1 -> 2 -> 3, restored child reads, rolled-back current
state with no expected environment reference, corrupt/ambiguous history, failed
bootstrap preserving selection, and genuine new-work initialization.

`bootstrap` in the internal Python CLI now returns canonical reference JSON,
matching the selecting operations' output contract. The shell helper signature is
unchanged. The new internal `context-id` accessor derives an ID **from a verified
reference**; an ID alone is not a lifecycle continuation token. Python
`bootstrap_profile_context` still returns the structured record and the strict
reference schema is unchanged. No shared consumer code or P05 schema was edited.

### F03 test-first results and retained evidence

| Command/scenario | Result |
|---|---|
| Six initial F03 matrix methods on `d02bb248` | **RED: 11 failed assertions**, 69.005 seconds. Both exact bootstrap loss variants returned `0 0`; bootstrap/cache-clear lacked references and adopted child generations; same-ID bind discarded its expected generation; four ID-only paths recreated retained loss. |
| Same six methods after the coherent repair | **6/6 PASS**, 118.664 seconds; no weakened failure expectations |
| Six extended lost/replaced-pin, actual Python creation, explicit recovery, rollback/corruption and first-use preservation methods | **6/6 PASS**, 106.589 seconds |
| Five focused F01/F02/reference/history preservation methods | **5/5 PASS**, 51.196 seconds |
| `bash tests/integration/universal-profile-context.sh` on stable F03 candidate | **43/43 methods PASS**, 452.160 seconds, zero skips; table-driven subcases exercise each entrypoint independently |
| All nine existing scoped scripts listed above, rerun after F03 | **9/9 PASS**, zero skips, actual jq 1.8.2 assertions executed |
| Bash syntax, Python 3.9 grammar and `git diff --check` | **PASS** |
| Full committed matrix on `56981edc4bd73020fea78e20526b10d62e822305` | **43/43 methods PASS**, 436.282 seconds; zero skips |

The immutable-history assertions intentionally become stronger: after two rebinds,
history must contain exactly generations `[1, 2, 3]`, including the current record,
rather than only the old `[1, 2]`. Initial lost-pin recovery therefore has real
retained evidence. Prior snapshots and their digests remain unchanged; no old test
was changed to accept missing data, neutral fallback, a reset generation or an
implicitly adopted child result.

### Exact F03 re-review source and replay

The full immutable second specification report is
`ce386bad173c85d2628a89f26542d5608026f5c7`, path
`.claude/plans/universal-implementation/reviews/P07-recheck.md`
(preserved separately by MasterSession as `ac5fb93`). It reviews product
`d02bb248` at report snapshot `5c1d98e`, closes F01/F02 for their reviewed paths,
and records F03 at `lib/copilot-env.sh:32-34` with consuming code at
`lib/profile_context.py:772-789,953-967`. These are **old-revision citations**.
Its verdict is specification FAIL, P1 = 1 / P2 = 0 / P3 = 0; quality NOT RUN.

The reviewer separately observed three F03 paths on that immutable product:
`BootstrapPin.test_bootstrap_does_not_recreate_missing_pin` (persistent
repository requirement) and two `BootstrapPropagation` methods (invocation-scoped
required load followed by pin loss; parent adopting a child generation). The report
also retains actual isolated-Git-worktree reference-only consumption and the honest
different-target rejection boundary. Those reviewer probes are attributed to that
report, not claimed as new executions by this builder.

This worker extracted the report's single executable Python block **unchanged**
and replayed it in two environments, without importing or editing the reviewer file:

| Exact block replay | Observation and evidence category |
|---|---|
| Temporary Git-archived `d02bb248` helper/fixture snapshot | **Original defect reproduced.** Required/loaded strict policy became `required:false/not_required/bundled-neutral`, the field was `advisory`, `pin-recreated=yes`, and its assertion raised `bootstrap lost required policy`. The isolated snapshot was removed afterward. |
| Unchanged frozen product `56981edc4bd73020fea78e20526b10d62e822305` | **Expected fail-closed behavior observed, not an unchanged-harness PASS.** The old helper expects rc0; the repaired command exits 2 at the missing-pin policy read with `PROFILE_CONTEXT_MISSING` and `required:true/status:error/applicability:unknown`. The `|| exit $?` prevents later field/pin-print assertions from running. No neutral/advisory success appears. |

The repaired-product replay therefore is not counted as an additional passing test.
The committed exact-order regression cases use `source_resolver=False`, check both
policy/field failure statuses and verify that no replacement pin exists; the full
43-method matrix supplies the repaired acceptance evidence. Original versus adapted
expectations are explicit rather than treating an old harness failure as success.

Only this report changed during replay. Product `56981ed`, all reviewer artifacts,
private/global configuration and consumer implementations stayed untouched.
`PYTHONDONTWRITEBYTECODE=1` was set and temporary snapshots were cleaned up.
MasterSession is sending the current frozen product/report to the same reviewer for
the third spec pass, then the first quality pass only if spec passes. The unchanged
P05 four-case bridge checks interoperability with an immutable source; it does not
certify ordinary bootstrap or supply independent P07 approval.

**Second correction lesson for coordinator capture:** fixing one propagation
helper is not enough when multiple public entrypoints establish the same invariant.
Enumerate every producer/selector and its next consumers first; explicitly test
destruction/replacement of selected state and a concurrent generation change.
Exercise `create=True` with retained evidence, not only an intact happy-path pin.
The earlier 29-case green run did not establish that matrix. This note remains in
the owned report; shared memory and reviewer artifacts remain coordinator-owned.

## Findings, limits and integration handoff

**Review gates:** the initial implementer self-review was not independent clearance.
MasterSession reports F01 (P1) and F02 (P2) individually closed by the same reviewer,
but the second component specification pass found F03 (P1) and did not pass.
F03 is locally repaired at the current frozen SHA and awaits renewed independent
specification review. No quality or final acceptance is claimed. Earlier repaired issues:
native/MSYS `_pack_dir` path spelling; stale raw-cache Brief Forge consumer
(P04-owned fix); and a validation-skill selector failure that could otherwise
empty-select the default. Each has executable regression evidence.

**Pending, not waived:**

1. P05: execute the direct
   `evaluate_controls(controls, required_policy=required_policy(verified_record))`
   producer/consumer test against its committed canonical implementation. Its schema
   is agreed, but its code is not in this baseline. Required loading is not proof
   that any company control passed.
2. P06/P08: transport the exact profile reference through the real operation/work
   handoffs, capture and resume. Session bindings remain declared until observed;
   a work UUID is not claimed as host-correlated identity.
3. P10/P13: include `lib/profile_context.py`, `lib/profile-context-schema.json` and
   `lib/pack-schema.yaml` in bundles. Preserve authoritative product metadata when
   product constraints must be evaluated. Do not duplicate parsers or reintroduce
   raw `PACK_CACHE_FILE` readers. Python 3.9+ is now an explicit resolver dependency.
4. P14/P15: final integrated review, generated outputs, full strict suite, installed
   consumer acceptance and actual client/host acceptance. Live company policy,
   renderer/model behavior, external provenance and ROI are not certified here.

No credentials, GitHub/account/network mutations, private activation, hook
registration, paid models, nested workers or nested reviewers were used. The only
non-owned change imported was the specifically authorized P04 dependency. The next
coordinator action is independent review of the product commit and joined acceptance
against the preserved profile reference, not marking the whole initiative complete.
