# P12 separate native presentation acceptance packet

Date: 2026-09-22. Builder `09666ec2-ff03-4517-9169-8793149ea8cd`.
Coordinator `88aecc43-40f9-41d4-8947-6c2fb0a55481`.

**Status: evidence packet ready for independent selected-PPT SPEC then eligible
QUALITY.** Actual P05 QA passes for this separately authorized presentation
attempt; no builder-issued independent review, release clearance or A15 parent
completion follows.

## Authority and source

Read exact `69f2ef64a7e12e389f81f0a554968db3bd4e5d12`, including the complete
"Separate native presentation acceptance" card. The original A15.3.word,
A15.3.shared and other format/parent obligations stay open.

PPT-relevant source is unchanged from F01 candidate
`b270475f6c45ac234b18133c0326542bace2fe50`, product
`32dac88ed1f143cf29ffe84f3fb1502c91a6c3e7`, over original product `cdb4ea6`.
The coordinator reports F01 independently closed at `02e8028` and its exact
native-PPT visual evidence accepted by the SAME reviewer. This packet creates
no source rewrite. Workbook `5b8818c` / `26bc070` is a separate preceding unit,
not an input to native PPT acceptance.

The selected snapshot includes exact PPT-relevant generate/outline/write/PPT/QA
source, fidelity/native references, the existing document tests, complete brief/
content/notes/citations and the actual PPTX. Source copies are review inputs, not
a new installed bundle or trusted runtime selected from a target.

## Exact packet paths and identities

`P` means:

```text
C:\Users\jokerman\.copilot\session-state\0a75211f-455c-4318-864f-bc8023ce9142\files\p12-presentation
```

| Artifact | Relative path under P | SHA-256 |
|---|---|---|
| New P05 context | `target\observations\p05-binding\context.json` | `59de3714079b126251f0fcec8dbd1b4351420838e180f0b5e27d72cb91bfacb5` |
| New P05 QA receipt | `target\observations\p05-binding\qa.json` | `9ac2c82527d0c70c9ae5d34078cca21af860a998c02936d46687c6129fa27e23` |
| Exact packet file manifest | `manifest.json` | `c4f40f2a11b65a6f3d4474d6fb41b0044d805a73cec7d31c15263ad7aa3a8d68` |
| Actual unchanged native PPTX | `target\artifacts\long-technical-brief.pptx` | `a5910cf41de35a0ab10433089fee8c3b58201a764c765a54f9636d639cdd8e93` |

The manifest binds 60 target files. Request, immutable obligation inventory,
QA input and evaluation accompany the context. Exact source/acceptance Git bytes,
environment preflights, command arguments, stdout/stderr and exit records are
under `P\logs`.

New attempt: `p12-native-ppt-selected-20260922`.
Original work-map path: `.claude/plans/universal-implementation/work.json`.
Package P12, selected leaf **A15.3.ppt only**.
Required controls are spec, quality, source-retention, ppt-editability,
ppt-render-execution and ppt-rendered-inspection. The four QA requirements were
declared in a fresh prepared context before its QA receipt; review obligations
remain required and unsatisfied by the builder.

Fresh P07 context `p12-native-presentation-selected`, generation 1, neutral
`_default` 1.0.0, digest
`sha256:412314b47f5cd07ae1ec2203b2accd544286e502755df5476f49b219296075ac`.
It binds its own target/home and was live-verified. No old pin was copied or
relocated. Synthetic Git baseline `242ccf1422beea051d0c9b263e6341c76282d4ae`
contains the original map/acceptance sources at the authorized revision and
the fixed source selection. It is fixture identity, not a product commit.

## Added actual visual observation

The coordinator's exact receipt, separate visual review, six SVGs, six PNGs and
contact sheet were copied read-only after hash verification. Native SVG hashes
also match the original builder's actual returned renderings. Copies, rasterizer
source (inert evidence), execution preflight/result/stdout/stderr and the builder's
explicit self-observation are under `target\observations\visual`.

External evidence origin is the coordinator's own session files, not a remote
upload or a personal document. Receipt SHA-256:
`9ff45a070dec702e82f9d9a718b85cb4449a1344e8063aa7222f61a56708085e`.
Visual-review SHA-256:
`8fc3fc6bd674f80d65dbf906fe25b562cb322a3b88f32ae749613057ac03f3e7`.
Six-slides image SHA-256:
`f7b90cb0f6fd0b3e3e8266396b3d05a2bb38f880501aa091a480a711da6663e3`.

The actual renderer chain is native `render_slide` SVG, then the already installed
ImageMagick 7.1.1-20 file-only rasterizer, then direct `functions.view` observation.
The coordinator performed rasterization and visual review. This builder also
directly viewed the supplied six-slide image: complete titles, body, footers,
citations and aligned capacity table are visible without overlap/clipping; claim
qualifiers and material limitations remain present.

This is the **native-canvas SVG rendition** of the pinned PPTX, not PowerPoint
desktop/physical-print/all-client acceptance. No new native renderer, app,
denied Copilot UI or Word access was attempted by this packet. Image viewing is
not portrayed as a new Office rendering operation.

## Original joint history stays blocked

All 135 original inventory files, including the original joint context and failed
QA receipt, remain byte-identical. They were not replaced or narrowed.

A **new receipt using the exact original context** was emitted under:

```text
C:\Users\jokerman\.copilot\session-state\0a75211f-455c-4318-864f-bc8023ce9142\files\p12\target\observations\ppt-visual-followup
```

Only newly observed result/evidence fields changed for PPT visual inspection;
the old immutable QA requirement inventory is unchanged. Actual P05 QA still
**exits 3** for `word-rendered-inspection`. Word remains unverified, with no native
page renderer and a separately permission-blocked application path.
Copies of the new joint QA/evaluation are in this presentation packet for
comparison. Neither old nor new joint receipt grants the separated leaf a pass.

## New selected-PPT QA and verification

The separate attempt consumes the complete applicable PPT obligations rather
than dropping Word controls from an old identity. Its actual P05 `qa` invocation
**exits 0**, and `verify_qa` finds no mandatory QA blocker. This does not execute
independent spec/quality or the release reader.

Source-retention evidence includes the actual committed F01 **22/22** test run,
the unchanged original 18 methods, saved native table/notes checks, and new
read-only packet checks against every complete source paragraph and both citations.
Six distinct actual notes parts contain their original section arguments.
Prior native saved-copy edit/readback and exact artifact restoration evidence is
preserved. No live native action is inferred from this later read-only check.

The actual original-map validator passes. Source closure hashes match accepted
unchanged P05/P07 code before product imports. Both source and fixture Git scopes
are explicit; ancestor discovery refuses and the initialized target root matches.
The process uses cleared HOME/USERPROFILE/AppData/temp/Lintel/derived-path bindings,
preopened logs and actual exit capture. It makes no OS-sandbox claim.

The preparation script is retained in the builder's session files as
`p12-ppt-acceptance.py`. No app, renderer or dependency installation was performed.
No shared API, catalog, agent, work-map source, parent checkbox or old QA was edited.

## Exact next gate

SAME reviewer `31c39265-13e5-4057-9e78-49bf658749a5` reviews this exact selected
PPT source/artifact/context, first complete SPEC then eligible QUALITY, including
the relevant shared generation/fidelity methods. The reviewer/coordinator supplies
any actual independent decision/corroboration; the builder does not fabricate it.

Word permission, workbook cache/render observations, PDF/Visio, shared design
binding and all P12/A15 parent obligations remain separate. This evidence-only
checkpoint accepts none of them.
