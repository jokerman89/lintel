# P11 browser-only A16 implementation report

**Owner:** original P11 builder, session `0517db27-55b8-43a3-a31f-39d0d6d24d74`.
**Authority/base:** `56df03d1757b483048e68ac846a3863ff558bb6b`.
**Preserved source:** `d389905eec58ea8fe51df00e50755df05eb5cbfd`, direct child of that base.
**Preserved report:** `84f0d38d61ab4d34c0cf5e4e64de7d7d203fa2a0`, child of `d389905`.
**Preserved continuation source:** `f4cd1f3bf8c58702fd68e99f52f1569c415428d1`,
parent `84f0d38d61ab4d34c0cf5e4e64de7d7d203fa2a0`,
tree `5c0df68799fae53a9f61c292b651191c307c3f81`.
**Preserved continuation report:** `a49e7b87556a0bb53e5eb5e2f79a20310f156ba1`,
child of `f4cd1f3`.
**Frozen readiness source:** `311579071100bfaaa0f1762d4672939c643d7129`,
parent `a49e7b87556a0bb53e5eb5e2f79a20310f156ba1`,
tree `12cfcc1d68f36e69d1e9b1024494540c220c28f4`.
**Scoped authorities:** `75ca6ed4d549a1c757fc8886989b514e107e9e03`,
`e182479bf5a62a0d1d80d974fad345671c831db8`,
`307367f8e2da7f6a8396e5e07ac221b673eb1883` and
`810ae686adf5e2bbbc072e7c4efeeff533744ea4` were read from their exact P11
package subsections, not imported as product dependencies.
**Branch:** `jokerman-microsoft-universal-browser-operations`.
**Selected work:** `.claude/plans/universal-implementation/work.json`, package P11,
only A16.1-A16.4 and the complete P11 card's "Browser-only first unit".
**Current status:** frozen A16 review candidate. The single final sealed normal
live pipeline on `3115790` exited **0**: four shared-contract checks, 23 Node
contracts and eight real headless browser scenarios, including actual print-text
inspection with explicitly selected installed `pypdf`. Retained-artifact pixel/
PDF inspection and exact owned cleanup checks also passed without another browser.
The original bounded A16 headless gate is observed; independent SPEC then eligible
QUALITY acceptance remains pending. Earlier failures and mandatory negative
controls remain failures in their original contexts. Direct two-image visual
clearance, PDF raster/glyph measurements, physical print and headed/login behavior
are not claimed. Native execution has ended; no whole-P11/A14 completion is claimed.

## Ownership and preserved methods

Only the four released skills, their browser/extraction references and helpers,
focused integration tests and this report are changed. No agents, A14 design
schema/renderers, P12 document producers, P08/P10 WIP, registry/profile/control/
work-map implementation, installers, generated outputs, shared plans/memory/ADRs
or other worktrees were edited. The nine-phase cycle remains unchanged.

- `browse`: URL/local preview, goto/click/fill/wait, viewport, DOM excerpt, screenshot,
  optional console capture and browser printing remain explicit operations.
- `open-managed-browser`: read-only preflight, visible operator session and debugging
  remain; no fictional installed engine, default-profile scan or implicit detach.
- `setup-browser-cookies`: first login, revalidation and account/surface separation
  remain user-driven. No password, cookie or storage-state transfer is implemented.
- `scrape`: parsed selector schemas, trim/numeric/multi extraction, missing-field
  errors, pacing/concurrency/robots method and prior-run diff remain. The small
  extraction helper does not pretend to implement YAML parsing or batch scheduling.

The concrete fallback is one explicit installed Chromium executable using Node's
built-in WebSocket and its observed CDP protocol. It is not a daemon or a generic
host engine. It consumes P03's existing URL helper; caller preflight and focused
tests use the accepted P05/P06/P07 and existing work-map reader unchanged.

## Per-leaf current evidence

| Leaf | Changed behavior and retained value | Current outcome |
|---|---|---|
| A16.1 | One operation/ownership reference plus concrete provider methods; four entry points retained. | Implemented and observed on frozen `3115790`: open/read/action/keyboard/extraction/screenshot/print and owned-context disposal. Independent acceptance pending. |
| A16.2 | Actual tool discovery, P03 exact/wildcard/raw redirect admission, explicit origins, pre-request/pre-response guard. | Observed final allowed redirect chain and foreign-origin/redirect/loop/script/resource/popup refusals; no MCP-default-session or OS-wide egress claim. |
| A16.3 | Real synthetic page state, image pixels, printed pages/text and explicit failed inspectors. | Original headless gate observed: complete final live exit 0, both actual PNGs pixel-inspected, two actual PDF pages/text/origins inspected. Direct two-image/PDF-raster/glyph/physical-print clearance remains unavailable, not silently passed. |
| A16.4 | User-chosen authentication surface, explicit owner/lifetime, no profile-folder availability claim. | Observed final synthetic password-fill and HTTP-auth refusals. Headed mode, real login, persistent authentication and profile reuse remain unverified; no credential transfer. |

## Discovery and actual native probe

Deferred Playwright tool schemas were inspected before browser use, including
navigate/tabs/snapshot/screenshot and `browser_run_code_unsafe`. They do not expose
a verifiable fresh-context setup at invocation. No MCP browser, existing tab or
personal profile was opened/read. The Vercel browser wrapper also lacks this
isolation input and can auto-install; it was not invoked.

Local Python and Node Playwright probes both genuinely failed (exit 3). Installed
Pillow was found; Playwright, PDFium, PyMuPDF, Selenium and Python WebSocket packages
were absent. Task-local pinned PyPI restore was attempted only after those failures:
Playwright 1.55.0 (Microsoft, Apache-2.0) and pypdfium2 4.30.0 (PDFium rendering).
Both pip and a standard system-TLS download failed with TLS HandshakeFailure.
No certificate verification was disabled, no browser engine was installed, and
no global package/configuration change occurred. No downloaded package was executed.

The installed dependency-free alternative was observed:

- Node `v24.16.0`, native WebSocket/fetch.
- Executable `C:\Program Files\Google\Chrome\Application\chrome.exe`.
- Actual `Browser.getVersion`: `Chrome/153.0.8010.53`, CDP `1.3`,
  revision `@792bf6722e73a45aa9e47c163b9901bdc17f3230`.
- New temporary user-data directory and isolated `Target.createBrowserContext`;
  own `about:blank` target created, context disposed, exact PID `61700` exited 0,
  owned temporary profile removed.
- Raw provider schema and lifecycle retained in
  `.claude/runtime/p11-a16/native-protocol-01.json` and `native-provider-01.json`.
  That probe is isolation/API evidence, not page/action/print acceptance.

## Initial failed fixture and first continuation

This section preserves the initial two failed launches and their original
observations. Later conditional evidence below does not rewrite their outcomes.

`browser-pipeline-01` exited 1. Its four shared-contract methods passed and its
then-11 Node contract cases passed, but the first actual full browser launch
produced no verified owned CDP endpoint. The synthetic loopback server's health
response was observed; no page scenario ran.

Owned evidence:
`.claude/runtime/p11-a16/a16-bwj6v40a/`.
The `browse-CaO5oC/browser.log` records a native AppData lookup failure, default
web-app installation attempts referencing Google URLs and a GCM
`QUOTA_EXCEEDED` message despite `--disable-background-networking`.
No remote page/action was issued by the fixture. Browser-background network
effects are **unverified**, not declared harmless or rolled back.

The exact owned browser PID `24324` was terminated, its newly created profile
removed, and both owned loopback server handles stopped. Subsequent exact-PID
read-only checks report both `24324` and `61700` absent. No personal-home incident
inspection/rollback, firewall, global setting or security-policy bypass occurred.
This failed run is not acceptance.

Native browser use was paused and the coordinator was notified immediately after
the logs exposed that boundary. The proposed continuation was fixture-only: create all
declared synthetic AppData directories, disable default-app initialization and
use a process-local, owned loopback deny-only proxy without forwarding.
The coordinator authorized exactly one bounded fixture-hardening/retry batch in
`75ca6ed4d549a1c757fc8886989b514e107e9e03`; its complete P11 subsection was read
from that exact commit without importing P08/P10 or changing the base.
The private fixture appends the child-process proxy argument and records actual
argv/PIDs. Its handler returns 403, never forwards/authenticates/decrypts TLS, and
records only method/host/port/status. This is not a shipped proxy or OS-wide
egress guarantee. Startup/context/allowed-navigation failure stops native work
without a configuration-variant search.

The missing synthetic AppData directory setup was corrected in the test harness;
the adapter now refuses missing declared Windows AppData directories before launch.
All declared synthetic directories existed and passed containment checks for the
bounded retry.

### First authorized retry outcome

Exact command (inside the sealed synthetic outer environment):

```text
bash tests/integration/design-browser-pipeline.sh --live --browser "C:\Program Files\Google\Chrome\Application\chrome.exe"
```

Invocation record: `.claude/runtime/p11-a16/browser-pipeline-bounded-02-command.json`;
outer log: `browser-pipeline-bounded-02.log`; exit **1**.
Detailed evidence: `.claude/runtime/p11-a16/a16-hjhkep6b/`.

The four shared contract methods and all 13 Node contract cases passed, zero skips.
The first native scenario then failed before any page operation:
`No verified owned browser endpoint`. `browse-kXoVgQ/browser.log` again reports
"DevTools remote debugging requires a non-default data directory" and "Can't
retrieve app data directory", despite the explicit fresh `--user-data-dir` and
verified declared synthetic directories. At that checkpoint the root cause was
**not established**. No path relocation, provider switch or configuration search
followed; the later metadata-only authorities and conditional correction are
recorded separately below.

The live attempt used three attached, loopback-only server handles in fixture
process `61248`: page origin `127.0.0.1:54456`, denied-destination fixture
`127.0.0.1:54454`, and deny-only proxy `127.0.0.1:54455`. Page health returned 200
with expected content; proxy health returned 403 before Chrome started.
`actual-launches.json` records the executable, complete argv and exact owned
browser PID `61652`, including `--disable-default-apps` and the child-only proxy
argument. No global proxy or other setting was changed.

`server-lifecycle.json` records 12 proxy refusals: one local health request plus
11 browser-background attempts, all 403 and `forwarded:false`. Recorded metadata
is method/host/port/status only, with no payload or credentials. This verifies
those refusals, **not** all network effects or OS-wide confinement. The page server
received only its health check; the denied-destination fixture received no request.

The owned browser was stopped, its new profile removed, and all three server
handles shut down/joined. Read-only checks of exact PIDs `61652`, `61248`, `24324`
and `61700` found none running. Only known owned roots/PIDs were touched.
Earlier unverified background effects remain unverified; no personal-home
inspection or rollback occurred.

The adapter's `operations` and page `network` lists are empty for this attempt.
The eight planned live scenarios are **not eight passes**: the first is an error
and the remaining seven were not attempted. No screenshot, DOM read, keyboard or
reduced-motion observation, measured page contrast, PDF or print inspection exists
for this attempt.
The test's source-only 3.5:1 negative is separate from an actual page measurement.
No fictional artifacts were substituted.

The recorded failure was subsequently consumed by the unchanged P05/P07 helpers
without launching a browser. `.claude/runtime/p11-a16/frozen-blocked-control.json`
contains the standalone mandatory browser control with effective status **error**,
its blocker, a SHA-256 evidence manifest and the reverified original P07 reference.
The six actual provider/extraction/fixture source files match the failed invocation's
recorded working-byte seals on the original `d389905` source checkout. Git stores normalized
LF while the Windows working files retain CRLF; these are explicitly different
byte representations, not a second code version. This record is not a v2
independent review decision or clearance.

## Conditional computed-location correction

The bounded read-only argv/source comparison found no retained evidence that the
single equals-form `--user-data-dir=<owned profile>` argument had split, acquired
literal quotes, become URL/MSYS spelling, or lost the requested profile identity.
The generic Chrome default-directory error alone did not establish its cause.

Under `e182479bf5a62a0d1d80d974fad345671c831db8`, the same native
`shell32.dll!SHGetFolderPathW` CURRENT query in both fresh recorded environment
shapes returned `0x80070002`, classification **unresolved**, child exit 2.
No error buffer was treated as a path. Evidence remains
`location-metadata-01\results.json` and `native-location-metadata-01-command.json`
under `.claude\runtime\p11-a16\`.

Authority `307367f8e2da7f6a8396e5e07ac221b673eb1883`, "Computed-location
diagnostic and conditional fixture correction", then authorized one diagnostic
query in the failed-pipeline shape and a conditional in-owned correction:

| Actual call | Result | Permitted consequence |
|---|---|---|
| `SHGetFolderPathW(NULL, 0x401C, NULL, 0, buffer[260])`: CURRENT plus DONT_VERIFY | `S_OK`, `0x00000000`, **inside-approved-root**, owned HOME-relative `AppData\Local` | Existing containment/reparse checks admitted only this genuinely missing owned directory. |
| After creating only that directory: `SHGetFolderPathW(NULL, 0x001C, NULL, 0, buffer[260])`, original CURRENT with no diagnostic flag | `S_OK`, `0x00000000`, **inside-approved-root**, same `AppData\Local` | The original native prerequisite, not diagnostic-only success, released the unchanged-configuration fixture run. |

The two calls retained the recorded environment/profile dimensions; the browser
launch arguments were unchanged. `APPDATA=HOME\appdata` and `LOCALAPPDATA=HOME\local` had not supplied the
native CURRENT reference `USERPROFILE\AppData\Local` in this synthetic fixture.
Evidence: `computed-location-01\results.json`, `native-computed-location-01.log`
and its command record. Exit 0; browser invocations 0; outside-path I/O/disclosure
false; source seals unchanged.

The only resulting fixture-path edit is constructing `HOME\appdata\Local` in
`tests\integration\design-browser-pipeline.py:49-55`. This is evidence for that
missing synthetic native reference, not a length limit, quoting defect, or a
blanket product prohibition on a native default reference outside custom browser
storage. No new product known-folder policy was added. The old failure records
remain intact. The one-shot diagnostic is consumed and must not be repeated.

## Native operation continuation

The observed provider remained the same installed Chrome 153 executable, CDP 1.3
and Node 24.16.0 native WebSocket. The fresh profile shape and launch flags stayed:

```text
--headless=new
--user-data-dir=<fresh owned run>\profile
--remote-debugging-port=0
--remote-debugging-address=127.0.0.1
--no-first-run
--no-default-browser-check
--disable-background-networking
--disable-default-apps
--disable-component-update
--disable-sync
--proxy-server=http://127.0.0.1:<owned deny-only proxy port>
about:blank
```

The test-only proxy was started and health-checked before Chrome and selected
only by the child argument. `actual-launches.json`, `server-lifecycle.json` and
each `browse-*\evidence.json` retain exact ports, PIDs, argv, context/page/session
handles, inputs/results and lifecycle. The proxy has no forwarding, authentication
or TLS interception; method/host/port/status refusals are not an OS-wide guarantee.
The outer wrapper's authority field still identifies the original `56df03d` base;
the later authorization is the exact `307367f8` subsection and diagnostic record,
not a claim that the initial base itself released these retries.

Each following invocation has its full outer `.log` and `-command.json` under
`.claude\runtime\p11-a16\`. Native commands used the normal
`design-browser-pipeline.sh --live --browser <installed Chrome executable>`;
run 08 additionally selected `--pdf-reader pypdf`. These are retained historical
commands, **not authorization to rerun them**.

| Invocation | Detailed run | Actual result |
|---|---|---|
| `browser-pipeline-conditional-03` | `a16-1z9rt_ch` | Exit 1 after owned endpoint/context creation; explicit CDP auto-attach filter rejected. |
| `browser-pipeline-functional-04` | `a16-j7hf23gc` | Exit 1: an earlier auxiliary attachment was not the created page. |
| `browser-pipeline-functional-05` | `a16-natfqqgo` | Exit 1 after real operations: Enter did not activate the focused button. |
| `browser-pipeline-functional-06` | `a16-sdhu4t8s` | Exit 1: six cases passed; popup-trigger input timeout masked the recorded refusal. |
| `browser-pipeline-functional-07` | `a16-lay3ek2s` | Eight browser cases passed; outer exit 1 when installed `pdftotext` crashed. |
| `browser-pipeline-functional-08` | `a16-g_wxhud_` | Exit 1: four browser cases passed; fifth startup failed reading its own new `DevToolsActivePort` with `EBUSY`. Native execution stopped. |

Ownership was established before the in-scope functional corrections:

- Removed unsupported `filter: [{}]` from the two `Target.setAutoAttach` calls,
  retaining CDP's default filter rather than adding flags or another provider.
- Correlated the exact `Target.createTarget` result instead of the first attachment:
  Chrome emitted paused `browser_ui` targets before the requested page. The selected
  page must be paused; guards precede its resume and other owned startup targets
  are closed. Additional targets remain refused, with unsuccessful closure an error.
- Supplied Enter's `text` and `unmodifiedText` carriage return metadata. Retained
  `browser-keyboard-red-01` proves the prior missing-character regression; actual
  mouse/Tab/Enter then advanced the count to 2.
- Preserved a policy refusal as the primary error when the triggering provider
  action also timed out, retaining `provider_error` separately.
  `browser-refusal-red-01` preserves the expected failing regression.

At that checkpoint five focused Node regressions brought the contract suite to
18. These mocks are distinct from the real scenarios. No source was edited while
its invocation ran. No launch/profile/security configuration change or generic
engine was introduced.

### Actual page, screenshot and print observations

Run 07 observed the admitted two-hop local redirect, DOM/HTML read, synthetic fill,
mouse increment to 1, Tab/Enter increment to 2, extraction `99.5` to `101.5`,
multi-value features and prior-run diff. It observed reduced-motion true and
animation `none`, a console message, and actual screen/print-media PNGs and
`Page.printToPDF`. Overwrite, password fill and missing-selector operations failed
explicitly. Foreign initial origin, foreign redirect, redirect loop, script
navigation, subresource, popup and HTTP-auth challenges were refused. No
credentials were supplied and the denied-destination trap received no request.

The real artifacts are in `a16-lay3ek2s\browse-WT5zKp\`:
`screen.png`, `print-media.png`, `preview.pdf` and `dom.json`.
The installed `C:\Program Files\Git\mingw64\bin\pdftotext.EXE` returned
**3221225477 (`0xC0000005`)**, with an empty log; the true exit remains in
`print-text-command.json`. It was not silently treated as missing or successful.

Already installed `pypdf 6.13.2` (BSD-3-Clause) and `Pillow 12.3.0` (MIT-CMU)
were used for an explicit alternative **inspection**, without installing anything.
The pipeline retains `pdftotext` by default and adds only explicit
`--pdf-reader pypdf`; absent/broken selected readers fail rather than falling back.
The attempted native PDF-raster script `native-print-render-01` was denied by
Windows PowerShell execution policy before its body ran (exit 1,
`UnauthorizedAccess`). No bypass, alternate invocation or renderer search followed.
No PDF-raster pages were produced.

Browser-free `retained-artifact-inspection-01` exited 0, writing
`a16-e4mjnjgm\artifact-observations.json`. After freezing source, the same observer
exited 0 as `retained-artifact-committed-01`, writing
`a16-lns14psw\artifact-observations.json`; browser/server invocations were both 0:

- Both real PNGs are 1440 by 900. Actual DOM-aligned regions contain 100 count-text
  pixels `(23,43,77)`, 295 low-contrast pixels `(137,137,137)`, 576 stopped-motion
  pixels `(35,98,120)` and 57 print-summary pixels `(23,43,77)`. Sampled regions
  are inside the actual images, not inferred from file sizes.
- The actual PDF has two pages, media box `[0,0,594.95996,841.91998]`, and 12
  transformed text origins inside page bounds. Expected summary and continuation
  occur on the correct pages; screen-only controls are absent. Extracted text
  and origins are retained beside each observer record.
- These are programmatic image-pixel and printed-text/origin observations, not
  comprehensive human visual review, complete glyph bounds or PDF-raster review.
  The print-media screenshot is not a rasterization of the PDF.

Both observers preserve input SHA-256 hashes and verify that the actual provider,
extraction, live-driver and Node-contract working bytes match run 07's four
corresponding source seals. Live runs identify HEAD `84f0d38` **plus working-byte
seals**, not an unchanged committed tree. The frozen observer identifies
`f4cd1f3` separately; it is not a fresh browser pass on that commit.

Unchanged P05 evaluates the run-07 browser observation as a standalone pass, the
actually measured normal-text contrast **3.498154318627802:1** as mandatory fail,
and run 08's later browser error as a separate mandatory blocker. Neither negative
is averaged away or replaced by successful static inspection. The original P07
contexts are separately reverified by the shared resolver, not rebound:

| Context | Retained P07 digest |
|---|---|
| Run 07 | `sha256:20c1f279ceea736e11b571d890c139cce4edf052c7490724b19cda56432f745e` |
| Run 08 | `sha256:c17e5c818d99e49dd933cd57d6b8bb9168f48e16de67daf839917f3c8a0302a0` |
| Frozen-source artifact observer | `sha256:50f5821d5faebee3f4cfefc6ddfad498a16e1eea19208d28abf70c7fffea365b` |

All retain schema version 1, original session/context ID
`0517db27-55b8-43a3-a31f-39d0d6d24d74`, generation 1 and `_default` version
`1.0.0`; distinct fixture roots legitimately have distinct content identities.
This is implementer inspection through shared contracts, not independent review
or a strict v2 clearance record.

### Owned lifecycle and renewed stop

`owned-cleanup-observation-01` exited 0 with no browser invocation or process
mutation. `cleanup-observations-current.json` records read-only identity/absence
checks for exact recorded PIDs and all 21 owned profiles from runs 05-08:

| Run | Fixture PID | Recorded browser PIDs | Profiles absent; proxy refusals |
|---|---|---|---|
| 05 | 41956 | 30852 | 1; 25 |
| 06 | 49632 | 59620, 27536, 42372, 59892, 56104, 4932, 39304 | 7; 101 |
| 07 | 61716 | 55924, 64912, 7076, 4468, 62964, 55924, 48300, 57140 | 8; 116 |
| 08 | 2700 | 4420, 63664, 47376, 63840, 38172 | 5; 63 |

The proxy counts include its health request. Server handles are recorded stopped,
trap requests are empty, no credentials were observed, and context/profile cleanup
records contain no errors. The observed parent instances are gone. PID `59892`
was reused by a later process created at `2026-09-22T10:19:21.959261+00:00`, after
its owned run ended; it was not touched. PID `55924` was also reused across
sequential launches within run 07; a numeric PID is not a timeless identity.
This does not claim observation of every unknown descendant or OS-wide isolation.

The run-08 failed launch is
`a16-g_wxhud_\browse-DMlSz0\evidence.json`: owned PID `38172`,
`EBUSY` reading its own profile's `DevToolsActivePort`, empty operation/network
lists, terminated with `{code:null, signal:"SIGTERM"}`, owned profile removed,
cleanup errors `[]`. The locking actor/cause is unknown. At the `a49e7b8`
checkpoint no native launch or readiness correction followed. The separately
authorized correction and final run are recorded next; no new native-location
query, configuration variant, provider switch or denied-renderer workaround
occurred. Earlier background effects remain unverified.

## Bounded readiness correction and final sealed run

Exact authority `810ae686adf5e2bbbc072e7c4efeeff533744ea4`, "Owned endpoint
readiness correction", was read completely into `readiness-authority-01.log`.
The clean entry checkout was `a49e7b8`. Only the exact owned readiness-file
`EBUSY` was released, not a new deadline, provider, configuration or native
location diagnostic.

`skills\browse\scripts\chromium.mjs:186-196` extracts the existing loop into
`_waitForOwnedEndpoint`, called at line 216. Its production defaults remain the
same UTF-8 `readFile(join(this.profileDir, 'DevToolsActivePort'))` and delay.
Only the catch predicate at line 192 changes behavior: `ENOENT` **or `EBUSY`**
may retry. The existing 100 attempts and 100 ms delay remain exact. Launch
errors, child exit, endpoint syntax checks, permissions and every other read
error keep their prior failures. No generic I/O retry, lock stealing, file
rewrite, profile/argv change or additional public runtime option was introduced.

The five added cases at `tests\integration\browser-operations.test.mjs:60-157`
are explicitly injected, not claims of observed Windows locking:

- `ENOENT`, `EBUSY`, `EBUSY`, then ready: exact owned path/UTF-8 reads and three
  100 ms waits.
- Exhausted `EBUSY`: exactly 100 reads and 100 waits of 100 ms, then the existing
  endpoint error.
- `EACCES`, `EPERM` and `EIO`: the same error object, one read, no wait.
- Launch/child-exit failures remain failures, including exit between polls.
- Empty/malformed endpoint contents still fail immediately, without retry.

With the old ENOENT-only predicate, the meaningful RED was 23 tests, 21 passing
and exactly the two busy cases failing. The fixed static pipeline then passed
four shared checks and all 23 Node contracts, explicitly `browser:not_run`.
The three-path normalized source diff was 127 insertions/14 deletions and was
committed before the final live invocation as `3115790`; its checkout was clean.
The final native run does **not** prove EBUSY happened during that run: bounded
retry behavior is fault-injection evidence, while native success is separate
normal-pipeline evidence. The original locking actor remains unknown.

### Commands, true exits and source identity

Every row retains its full `.log` and `-command.json` under
`.claude\runtime\p11-a16\`, with synthetic environment, Git boundaries, argv and
source seals. No browser/helper source was edited during execution.

| Invocation | Actual result |
|---|---|
| `endpoint-readiness-red-01` | Exit 1; 21/23 pass, two expected injected busy regressions fail with the old predicate. |
| `endpoint-readiness-static-01` | Exit 0; four shared checks plus 23/23 Node contracts; no browser. |
| `endpoint-readiness-whitespace-01`, `endpoint-readiness-staged-whitespace-01` | Exit 0 with per-command source `core.autocrlf=true`; no persistent config change. |
| `endpoint-readiness-source-identity-01`, `endpoint-readiness-frozen-status-01` | Exit 0; exact source SHA/parent/tree/trailer/three paths and clean frozen checkout. |
| `browser-pipeline-readiness-final-01` | Exit 0; normal sealed live pipeline below, with four shared checks, 23/23 Node contracts and eight real browser scenarios. |
| `readiness-artifact-observation-01` | Exit 1 in private browser-free inspection: already evaluated P05 controls were incorrectly supplied as raw controls; the shared validator rejected `effective_status`. No browser/server was started. |
| `readiness-artifact-observation-02` | Exit 0 after removing only the evaluator's three output annotations before re-evaluation and asserting identical shared results; frozen product unchanged, no browser/server/process mutation. |

The only final native invocation was:

```text
bash tests\integration\design-browser-pipeline.sh --live --browser "C:\Program Files\Google\Chrome\Application\chrome.exe" --pdf-reader pypdf
```

It ran from `2026-09-22T11:32:58.5399426Z` to
`2026-09-22T11:34:06.8758686Z`, producing `a16-awm92xgk\`.
`summary.json`, `live-results.json` and all eight context records identify
source `311579071100bfaaa0f1762d4672939c643d7129`; all 18 source seals remained
unchanged. Actual provider/executable were `Chromium CDP`,
`C:\Program Files\Google\Chrome\Application\chrome.exe`, Chrome
`153.0.8010.53`, CDP `1.3`, revision
`@792bf6722e73a45aa9e47c163b9901bdc17f3230`, Node `v24.16.0`.
The observed `provider-api.json` SHA-256 is
`b314ffc94555a973adeb65dbd0ce18d1bf82bad81061e5cd3811423eff93fd12`.
After substituting only fresh owned run paths and loopback proxy ports, actual
argv is identical to retained run 08. No native-location query was repeated.

All eight named scenarios passed: read/action/keyboard/motion/screenshot/print/
extraction; initial origin refusal; second-hop foreign redirect refusal; loop
refusal; script navigation refusal; subresource refusal; paused popup refusal;
and HTTP-auth refusal. Actual mouse then keyboard actions produced count **2**,
price extraction **101.5**, reduced motion true and animation `none`. Guards
operated before unauthorized requests; the trap received none and no credentials
were supplied. These are local synthetic results, not remote/customer execution.

### Final artifact inspection and its limits

Actual final artifacts live in `a16-awm92xgk\browse-Ew9VrL\`; printed text and
origins live in the parent run. SHA-256 bindings:

| Artifact | SHA-256 |
|---|---|
| `screen.png` | `7f8ce48af4bda43005030da32038f0e950d278b8e9c2f3396c3050ad90669347` |
| `print-media.png` | `1c1f7aab25fc48d2930809e8dfc1a32a2434673d08389487fe7e140a87aa32b2` |
| `preview.pdf` | `a524b5178746d532e8ec3bf4d20bcb5230ad68f71b60751b6f175d432bfcdc45` |
| `dom.json` | `ae81e28ed3c24dc24c0e315f1cc718c6f8a915ec4e4ffa0e53bee24b86811af7` |
| `printed-text.txt` | `97fc29c3ceaaf1691924fee453efc40cb0dd40599d598fb2d0a3f45d91151b85` |
| `printed-origins.json` | `81d9e6516bf471322867f9afa100d3be59611f770855e5b83caa4b394e2a8103` |

The browser-free record `a16-83x7usho\readiness-observations.json` binds original
inputs, provider/tool identities, the private observer's source hash, original
P07 contexts, source seals and cleanup observations. Its inspector uses already
installed Pillow `12.3.0` (MIT-CMU) and explicitly selected pypdf `6.13.2`
(BSD-3-Clause); no installation or reader fallback occurred.

Both actual 1440 by 900 PNGs were decoded and compared with measured DOM regions:
100 count-text, 295 deliberately low-contrast, 576 stopped-motion and 57
print-summary pixels match their measured colors, all within image bounds.
These are actual pixel observations, not file-size/ZIP checks or complete visual
clearance. Separate `functions.view` calls acknowledged each final PNG but
surfaced no image pixels in the model-visible results, so direct visual inspection
of both bound images is **unverified**. The operator's earlier observation of
count 2, price 101.50 and the low-contrast row applied to only one displayed
retained image; it is not reassigned to both final images or the PDF.

The actual PDF was text-inspected in the final live pipeline and read again
without a browser. Both inspections agree byte-for-byte on printed text:
page 1 contains the title, count 2, price $101.50 and `Print summary: 2`;
page 2 contains `A16 second printed page` and
`Print-only continuation remains readable.` Screen-only credential/low-contrast
labels are absent. Both media boxes are `[0,0,594.95996,841.91998]`; all 12
transformed text origins are within them. No full glyph bounds, PDF raster,
physical output or document-layout certification is claimed. The earlier
policy-denied raster script was not invoked again by any route.

### Final owned lifecycle and unchanged control boundaries

Fixture process `18984` owned page `127.0.0.1:51769`, trap `127.0.0.1:51767`
and deny-only proxy `127.0.0.1:51768`; page/proxy health was observed before
Chrome launch. The eight recorded browser PIDs were `44952`, `42540`, `3276`,
`35976`, `9592`, `44488`, `59780` and `64616`. Every owned context was disposed,
its browser exited, its profile was removed and `closed.errors` is empty.
The read-only observer found all nine exact recorded PIDs absent and all eight
known profile paths absent after containment/reparse checks. Server handles were
stopped/joined. No process mutation or broader cleanup followed.

The final proxy recorded **119** refusals including its health check, each 403
and `forwarded:false`, with no payload/credentials. Trap requests and credential
observations were empty. This does not resolve earlier unverified background
effects or prove all-descendant/OS-wide egress isolation.

The final original P07 reference is
`sha256:d3afe8d019447f3d76dc21efa042655e925058e312697c5cf737e6be0961ade9`.
The separate browser-free observer reference is
`sha256:21326afb0f2097c69916a7f51d6c803a2f43a32b57ec3952ba8d9bbb36bd348b`;
the pre-freeze static reference is
`sha256:2aa7d5afee9c5d005e5d00606e7c2599f0c45d9f0320a2ebde783a3f33b02038`.
Original schema/context/generation/pack identities are preserved by the shared
resolver; these references are not rebound to one another.

Unchanged P05 gives the final standalone browser control `pass`, while the
same deliberately measured **3.498154318627802:1** normal-text contrast remains
mandatory **fail/blocked** in its negative context. The historical run-07
contrast and separate run-08 browser error also re-evaluate identically as
blockers in their original contexts. No averaging, relabeling, strict v2
clearance or independent acceptance follows from the final successful command.

## Earlier checks and committed checkpoint

Every test/helper invocation used an allowlisted synthetic HOME/USERPROFILE/temp,
explicit Lintel/source/target roots, fixture Git discovery/configuration boundaries
and sealed accepted source files. Test/helper outer output was redirected before
invocation to `.claude/runtime/p11-a16/`, preserving real exits and command/environment
records. No aggregate test output was piped through a truncating runner. The
separate source-Git EOL diagnostic exception is disclosed below.

- Shared work-map validator: exit 0 on the explicit approved map.
- Initial browser contract RED: missing new module, exit 1.
- Browser contracts: first 8/8, later 11/11, 12/12 and 13/13; zero skips. These are policy,
  arguments, retained entry points and synthetic extraction tests, not a browser.
- Static pipeline: four shared P05/P06/P07/work-map methods plus 12/12 Node cases;
  explicit `browser:not_run`. The fresh-process P07 CLI verifier returns the exact
  reference produced by the shared resolver. Same-profile drift correctly blocks.
- Node source syntax: exit 0.
- Existing `tests/shape/frontmatter-lint-all.sh`: exit 0.
- Existing `tests/unit/url-policy.sh`: five unchanged P03 methods pass, exit 0.
- Source `git diff --check`: exit 0. One combined read-only probe earlier returned
  aggregate exit 1 because an exact stopped PID was absent; the later separated
  check records the diff exit and `running:false` independently.

Native evidence is bound to the pre-commit working source and its recorded SHA-256
seals over authority `56df03d`. Later committed-tree static verification is recorded
separately; a passing static rerun cannot relabel the two failed native invocations.

### Original committed-tree verification

At exact `d389905eec58ea8fe51df00e50755df05eb5cbfd`:

| Actual invocation | Exit and scope |
|---|---|
| `bash tests/integration/design-browser-pipeline.sh` | 0; four shared-contract methods plus 13 Node cases, zero failures/skips; explicitly `browser:not_run`. |
| `bash tests/shape/frontmatter-lint-all.sh` | 0; existing source contract, no generated-output edits. |
| Private `p11-freeze-evidence.py --product d389905eec58ea8fe51df00e50755df05eb5cbfd` | 0; consumes the recorded failed native run through P05/P07, preserves error/blocker and exact source seals. |

Logs are `browser-committed-static-01.log`, `frontmatter-committed-01.log` and
`frozen-blocked-control-01.log` with their command/environment records under the
same owned runtime root. Actual Python was 3.11.9; Python 3.9, other operating
systems, headed mode, other clients and installed-consumer execution were not run.

### Source staging/EOL diagnostic

The synthetic Git environment omitted the source's global `core.autocrlf=true`
when first staging the eleven owned files, so their CRLF working bytes entered
the index without the source's normal LF normalization. The initial cached
whitespace check returned 2 and unexpectedly exceeded the host tool's display
limit. That tool reported an automatic host-temp capture; it was **not opened,
inspected or cleaned**, and no real-home investigation followed.

All subsequent full diagnostic output was redirected to owned runtime logs.
Read-only source configuration and `git ls-files --eol` established the mismatch.
Re-adding alone retained existing CRLF index entries; a **path-scoped**
`git -c core.autocrlf=true add --renormalize -- <the eleven owned paths>`
restored canonical index LF. No working file, unrelated index path, persistent
Git setting, commit history or other worktree was changed. The final staged
whitespace check passed. The failed diagnostics remain `staged-whitespace-01.log`
and `staged-whitespace-02.log`; `staged-whitespace-03.log` records the passing gate.
This is the L-036 source-versus-fixture Git distinction, not a browser/platform fix.

### Continuation committed-tree verification

At exact `f4cd1f3bf8c58702fd68e99f52f1569c415428d1`:

| Actual invocation | Exit and scope |
|---|---|
| `bash tests\integration\design-browser-pipeline.sh --pdf-reader pypdf` | 0; four shared-contract methods plus 18 Node cases, zero failures/skips; explicitly `browser:not_run`. Log `continuation-committed-static-01.log`. |
| `bash tests\shape\frontmatter-lint-all.sh` | 0; 127 skills/69 agents checked, no generated-output edits. Log `frontmatter-continuation-committed-01.log`. |
| Private `p11-inspect-artifacts.py` | 0; retained run-07 screenshot/PDF inspection, P05 blockers and original P07 contexts preserved; zero browser/server launches. Log `retained-artifact-committed-01.log`. |

Each has a full command/environment/exit record in the owned runtime. The static
profile digest is
`sha256:e830fb06538e5f32e12aa67a03ffe3ef0ebd573cd3325b7c2fe9477fc19f1385`;
it is not substituted for either live context. Nine accepted shared-source seals
remain unchanged. No new package was installed for this continuation.

The source-freeze cached whitespace check without explicit source EOL configuration
again returned 2; its complete output stayed in `continuation-index-whitespace-01.log`.
With per-command `core.autocrlf=true`, `ls-files --eol` confirmed all six index
entries LF, the staged whitespace gate returned 0 and the normalized diff was
240 insertions/62 deletions across six paths. No global/persistent configuration
or unrelated index path changed; the old host-output spill was not accessed.

## Frozen changed paths

The preserved `d389905` source changes exactly these eleven source paths;
`84f0d38` adds the twelfth, report-only path. Both use Conventional Commit messages and the
required Copilot trailer. No amend/reset, push, PR, remote authentication or merge
was performed.

```text
skills/browse/SKILL.md
skills/browse/references/browser-operations.md
skills/browse/scripts/chromium.mjs
skills/open-managed-browser/SKILL.md
skills/setup-browser-cookies/SKILL.md
skills/scrape/SKILL.md
skills/scrape/scripts/extract.mjs
tests/integration/browser-operations.test.mjs
tests/integration/browser-live.mjs
tests/integration/design-browser-pipeline.py
tests/integration/design-browser-pipeline.sh
.claude/plans/universal-implementation/reports/P11.md
```

Generated catalog/adapter/wiki/README output remains coordinator-owned and unchanged.
Later generated summaries must preserve the exact observed headless subset and
the unknown original lock cause, unavailable visual/headed/authentication
observations and independent-review gate.

The continuation source `f4cd1f3` changes only this six-path subset:

```text
skills\browse\references\browser-operations.md
skills\browse\scripts\chromium.mjs
skills\open-managed-browser\SKILL.md
skills\setup-browser-cookies\SKILL.md
tests\integration\browser-operations.test.mjs
tests\integration\design-browser-pipeline.py
```

The preserved report-only child of that source is `a49e7b8`. The latest source
`3115790` changes exactly three paths, within the same original eleven:

```text
skills\browse\scripts\chromium.mjs
skills\browse\references\browser-operations.md
tests\integration\browser-operations.test.mjs
```

This updated report is a separate report-only child of `3115790`; its exact
SHA/parent/blob/hash are supplied in the coordinator handoff, avoiding a
self-referential commit ID. All earlier frozen commits are unchanged. No
amend/reset, remote GitHub authentication, push, PR or merge occurred.

## Review and next gate

Implementer self-review only, using the selected map/A16 leaf text, exact
three-path readiness diff, injected RED/GREEN and sealed final observations.
**Open implementation findings: P1 0, P2 0, P3 0** in this bounded pass, not an
independent quality or security verdict.

The previous P2 at old `chromium.mjs:205-209` is resolved by the narrow authorized
predicate at current lines 186-196 and the exact injected budget/error regressions.
The normal frozen-source live invocation passed independently of that fault
injection. The actual earlier lock holder/cause remains unknown; success does not
identify it or prove a busy retry occurred during the final native run.

Separate limitations remain: the default `pdftotext` reader previously crashed;
PDF-raster inspection was policy-denied; direct visual clearance of both bound
PNGs, full glyph bounds, headed mode, actual login/profile persistence, physical
printing, other platforms/clients and installed-consumer execution were not
observed. Earlier browser-background effects remain unverified. The original
A16 gate is headless operations/ownership/guards/print result, not an invented
full document-layout certification. Required unavailable observations, if any
are selected by independent review, remain unavailable rather than passed.

No nested writer/reviewer or factory was launched.
Independent spec then quality review remains with the coordinator on the immutable
source/report pair. Formal Stage 1 is not cleared; subsequent quality/compliance
stages and strict v2 release clearance are not claimed. P05 missing-browser,
measured contrast and actual browser-error controls retain their mandatory effect.
Shared plan/memory/ADR state was not modified to manufacture acceptance.

Next owner action: MasterSession takes the immutable source/report pair through
independent SPEC, then eligible QUALITY. No further native run, configuration
search, metadata query or automatic continuation is authorized by this freeze.
A14/frontend-agent work remains unreleased; this original P11 owner session is
retained for any explicit later release.

## Q1 numeric extraction correction

This addendum is the current checkpoint; preceding observations retain their
original source identities. Exact authority `d3f94db`, "Q1 complete numeric
extraction", was read in full. Independent report
`5504a2c2089621113eb284f968b7f5bb409808cf`, preserved by the coordinator as
`d8887a2`, passed complete original A16 SPEC before QUALITY found one P2, Q1.
Its 171-line Git-byte SHA-256 is
`ded59a0bc2de86d2f2ee959f6c0b8781323785f20f4832825838ffdd17de3204`.
The reviewer found no missing original headless observation. This correction
does not reinterpret that result as A16 acceptance.

**Frozen Q1 source:** `14b419c378b82807a9abbfeb386c20f145636f7f`.
**Parent:** `043019ace7916170de9376c8bd8726c1cd5d4744`.
**Tree:** `37ebe023d7e9d92c3845eb433220b968e81934b3`.
Exactly three paths changed, 76 insertions and 3 deletions:
`skills\scrape\scripts\extract.mjs`, `skills\scrape\SKILL.md`, and
`tests\integration\browser-operations.test.mjs`. This addendum is frozen in a
separate report-only child; exact identity is supplied in the handoff.

The transform now validates the complete supported amount rather than selecting
an internal digit substring. `$.50`, `-.50` and `-$9.50` produce `0.5`, `-0.5`
and `-9.5`. At most one ASCII sign can occur before or after one optional currency
symbol; leading-dot fractions and an optional whitespace-separated alphabetic
unit/currency label are supported. This is numeric extraction, not currency
conversion or a locale parser.

Grouping/locale separators, multiple values, repeated/conflicting signs,
exponents, accounting parentheses, incomplete decimals, base prefixes and
nonfinite values produce explicit field errors, never partial amounts. Existing
trim/text/multi/diff behavior and prototype-like field names are preserved.
No URL guard, provider, readiness, profile or shared contract changed.

### Focused evidence

All commands used the existing synthetic allowlisted wrapper, with owned logs
and true exits under `.claude\runtime\p11-a16\`. No browser/server, native-location,
platform investigation, dependency install or policy change occurred.

| Invocation | Outcome |
|---|---|
| `q1-extraction-red-01` | Exit 1 on the old product: seven numeric tests, one pass and six failures, including all three exact Q1 cases. |
| `q1-extraction-green-01` | Exit 0: seven numeric tests pass, no skip. |
| `q1-retained-static-01` | Exit 0 before freeze: four shared checks and 29 Node contracts; `browser:not_run`. |
| `q1-whitespace-01` | Exit 0; normalized diff has no whitespace errors. |
| `q1-committed-static-01` | Exit 0 on exact frozen `14b419c`: four shared checks and 29/29 Node contracts, no failures/skips; `browser:not_run`, source seals unchanged. |

The committed static run retains the same explicit work map and stable context ID,
schema 1, generation 1, `_default` 1.0.0, with its own legitimate profile digest
`sha256:43112df6f1e755e656d745f037c08206c7c678a87a37c2b7773736352a8aed18`.
It does not rebind the original native run's profile or relabel its results.
Tests use explicitly injected selector text, not a new page/browser observation.

### Review and continuation boundary

Implementer self-review finds no remaining Q1 implementation issue (P1 0/P2 0/P3 0);
the same independent reviewer must recheck the affected correction before A16
acceptance. Original SPEC/headless evidence remains attributed to `3115790`;
the new source has browser-free correction evidence only. All earlier failures,
unknown background/lock causes, mandatory contrast/error controls and visual,
PDF-raster/glyph, headed/login, physical-print and other-platform limits remain.

Recovery coordinator `88aecc43-40f9-41d4-8947-6c2fb0a55481` receives this pair for
reviewer `1f655ca9-eb2b-4325-9996-72ed8539c13b`. After this freeze the separately
committed `235d5bf` direct A14.1-.4 release may proceed in the same owner against
accepted P09 ancestry, with separate source/report boundaries. A14.5 native
observations, automatic P08 lifecycle and whole-P11 acceptance remain gated.
