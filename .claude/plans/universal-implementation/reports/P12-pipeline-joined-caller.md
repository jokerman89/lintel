# P12 original caller accepted-provider recheck

Date: 2026-09-23. Original builder `09666ec2-ff03-4517-9169-8793149ea8cd`,
runtime `0a75211f-455c-4318-864f-bc8023ce9142`.
Coordinator `88aecc43-40f9-41d4-8947-6c2fb0a55481`.

**Both original caller routes now PASS on the named accepted provider tree:
33/33 each, zero failures/errors/skips, actual exit 0.** The default and explicit
parents, path dimensions, Git choices and all 33 assertions are unchanged. Fresh
run/case IDs and contexts were created; the old stopped inputs were not relocated,
activated or rewritten. This report does not make the coordinator's A03.2.n3,
whole-parent, final-review or native-format acceptance decisions.

## Exact authority, reviews and source

The coordinator released this original-owner recheck in the 2026-09-23 04:21
message, naming only **`b22443f9c1b465637529236de7a3120618aca2ee`** for intake.
The source branch started clean at
`67a6e82b4f1e85139532373852f224981802cc40`. Normal Git merge fast-forwarded it
to that exact named commit; owner history is preserved and no conflict or local
helper/schema edit was needed.

| Identity | Exact revision / observed boundary |
|---|---|
| Source actually executed | **`b22443f9c1b465637529236de7a3120618aca2ee`** |
| Accepted dependency parents | `095c43597b41c49202edce165e57e42d663744d3`, `06a41fe203e2c210d0279fa481d729a39cd2e690` |
| Retained P12 entry product/report | `e7e97d463e7960305b1249fbf97f8dbb5e20fe7d` / `67a6e82b4f1e85139532373852f224981802cc40` |
| Accepted entry review | `d2a2cfe2f6b854c7651c5e580d998fc0935cf884`: scoped entry/isolation/propagation SPEC then QUALITY |
| Entry review bytes | 222 LF lines / 17,910 Git bytes; SHA-256 `e6226f664e13119cb310a099039fadecaa76a092b9531b24bd77c7882708706d` |
| Accepted P05 provider review | `06a41fe203e2c210d0279fa481d729a39cd2e690`, effective reviewed source/report `fb08cb8409de4025513626a50790b7e76e17b3b1` |

Both complete reviews were read. All six P05 source/test/report/review files were
verified exact in the named dependency. The two runtime producer files also match
the reviewed Git-byte SHA-256 identities:

- `lib\review_contract.py`: `d4e05e6f14709c97c1dae0bc5e48c60527631830b8bbbeb129ab63c39dea9461`
- `bin\li-review-evidence.py`: `25972a78300d9f9a8dc813242073d43c768303887c230e37a9640e4118c24490`

The P12 helper, callers and test entry are byte-identical to the prior accepted
P12 source; all 33 assertion-method ASTs are unchanged. The dependency's other
accepted changes retain their existing attribution. This owner authored only
this report in the repository, as a report-only child of the executed source;
the handoff supplies the resulting exact commit and report digest.

Source Git used its normal existing configuration and hooks throughout, with no
synthetic HOME, global-config suppression, hook override, EOL override or new
longpaths setting. Source commands and real exits are separately logged. Fixture
Git retains its existing inspected synthetic configuration; it is not source Git.

## Original dimensions and actual commands

The existing Bash entry was executed with closed stdin and unchanged arguments.
The ordinary Bash path conversion still selects the same existing interpreter;
no alternative interpreter, jq, installation, namespace caller argument or
shortened fixture was supplied.

Default parent, unchanged:

```text
C:\Users\jokerman\reference-repos\copilot-worktrees\jokerman-session-setup\jokerman-microsoft-literate-adventure\.claude\runtime\p12-pipeline-binding
```

Its test argument array is **empty**. Git Bash still discovers
`C:\Program Files\Git\mingw64\bin\git.exe`, exactly as in the original failed run.
The new run is `pipeline-tests-wa571c73`, replacing no existing directory.
Its upstream result-file logical paths remain **263 characters**.

Explicit parent, unchanged:

```text
C:\Users\jokerman\.copilot\session-state\0a75211f-455c-4318-864f-bc8023ce9142\files\p12-pdf\pipeline-auto-entry\working-explicit\explicit-fixtures
```

The exact original arguments remain `--fixture-root <that parent> --git
C:\Program Files\Git\cmd\git.exe`. The new run is `pipeline-tests-ub0f10o5`;
the original `pipeline-tests-rwzmmb4m` stays intact. Upstream result-file logical
paths remain **260 characters**. Python remains the original Python 3.11.9
executable; each actual preflight confirms the same selected Python/Git.

| Original route, fresh run | Raw unittest result | Entry exit | Unittest / command duration |
|---|---|---:|---|
| Zero-argument default | **33 PASS**, 0 failures, 0 errors, 0 skips | **0** | 188.615 s / 189.296 s |
| Original explicit arguments | **33 PASS**, 0 failures, 0 errors, 0 skips | **0** | 254.402 s / 255.016 s |

These are two actual original-owner runs, not the reviewer's different-root
33-method result. They use current declared source seals before calls, verified
unchanged afterward. No selector or missing-tool case was rerun unnecessarily;
their earlier exact baseline evidence remains preserved.

## Real P05/P09 caller behavior

The three formerly failing unchanged upstream methods now complete in each full
run: own-context/profile consumption, partial/changed-profile refusal, and
unselected/stale-artifact refusal. The real P09 verifier and P05 preparations/
current-context checks execute; no cached context or private replacement consumer
is supplied.

For each new long `01-result.json`, actual successful P05 CLI output includes
the existing logical relative path:

```text
.claude/runtime/state/domains/pipeline-fixture/i0001/ta/01-result.json
```

The returned snapshot has `worktree.kind: file` and the actual SHA-256 below,
with base/head/index states absent. The repository's real `.gitignore` contains
`.claude/runtime/`; these are explicitly selected ignored, untracked files.
Every captured `prepare --repo` uses the ordinary logical repository spelling,
never a substituted `\\?\` argument. This verifies actual snapshot admission,
not only an existence test. Fresh context profile references are retained in the
command evidence; successful P09 positive and negative assertions remain real.

Let `A` be the default new run above and `E` the explicit new run. Each full
logical file is `<run>\<case>\repo\` plus the relative path above.

| Run / case | Logical length | Actual file SHA-256 | Successful captured preparations |
|---|---:|---|---:|
| A / `case-dxr2jbo6` | 263 | `f13440f4f2e84bd046ca45525d558c4ef158f59dfdcc42a76354614e82690125` | 2 |
| A / `case-f8c1d_w7` | 263 | `03e6c359aa874e4b9f556ded210fc87948d20bf74db45766fe919c64b4f6a48c` | 2 |
| A / `case-gwi49fzm` | 263 | `61c288de82073d1ef9b1de0a9e5ae9e066fd41d789ea1bdfb17ebcaf5f082077` | 3 |
| E / `case-ptweix1y` | 260 | `ba359d4d0131e8c36c2b566eb2b6c5c817cd12f1c453a8299eb49abde4492e41` | 2 |
| E / `case-6eukyd3k` | 260 | `bbd21d3dbbf033c4cef601f28a0f813a2006606d266657fdb307f712d93252da` | 2 |
| E / `case-2ykic1st` | 260 | `52f0bec0250f6188556b802ebd48db8f9c6f8021ee8721f76b9a0e5e675c30d6` | 3 |

Ordinary `Path.is_file()` is still false for all six while native filesystem
inspection is true. The accepted P05 provider now observes and binds the real
bytes internally; the caller roots were not changed to make ordinary observation
succeed. Source inputs, P05/P09 schemas and declared QA/actor semantics are unchanged.
These domain-shaped records are the original tests' inert fixtures, not live
domain actions or structured review/decision/corroboration publication.

## Isolation and old-evidence preservation

Inspected **356** initial/case/command environment records per run, **712 total**.
All synthetic ambient credential/profile/cache/Git-configuration markers are
absent from the reconstructed environments. HOME/USERPROFILE/AppData/temp/XDG/
Lintel/config paths remain inside the fresh owned run; PATH/PATHEXT and strict
Git ceilings match the actual selected tools. The original per-case ancestor
refusal and exact initialized-root assertions execute in all 33 cases.
These are inspected process controls, not an OS sandbox or enterprise assurance.

Before merge/calls and afterward, byte manifests were compared for:

- The original default stopped tree: **3,201 files**, unchanged.
- The original explicit stopped tree: **3,201 files**, unchanged.
- Prior automatic-entry outer evidence: **40 files**, unchanged.
- All **397** original artifact/QA inventory entries and the six exact earlier
  failed result files: unchanged against their preserved hashes.

Every pre-existing sibling under the two original fixture parents was also sealed
before its new run and rechecked afterward: three default siblings and one explicit
sibling remain unchanged. New run IDs were absent beforehand and have the same
path lengths as the old IDs. No old pin, context, result, failed log or root was
activated, overwritten, moved or relabelled.

The old full-entry results remain **30 PASS / 3 FAIL, exit 1**, with their original
outcome digest `63d0695244ca853cad8a5ca8c30bef7b5963705ab2f7778c85774025529289ca`.
New outcome-only JSON files both hash to
`435d29064443d5e6c0a32344ccf3c4b7c1d666ab1d2b7e99b83b71bfb2fe6f88`;
equal counts do not identify a run. Exact source, new roots, command records,
profile references and snapshot/file hashes distinguish the two real executions.

## Immutable evidence and remaining decisions

`D` is
`C:\Users\jokerman\.copilot\session-state\0a75211f-455c-4318-864f-bc8023ce9142\files\p12-pdf`.
New evidence is only under `D\pipeline-joined-provider`; old failure data stays
in its original locations.

| Evidence relative to that new directory | SHA-256 |
|---|---|
| `default\results.json` | `f9e48f8f52c91ab41fd6a81666e304ed6a8bf1824e9e87c13864e2f21540aa9b` |
| `explicit\results.json` | `b891c8dd1ab3dce1245e47ac046446ee0bcdd4566ed79c8f4067d783906cdb77` |
| `old-inputs-before.json` | `cb5a351f7095c687eeef2dde54f0ea9c61a6fd7ab0b07f56a6533c54f2cc4641` |
| `old-inputs-after.json` | `bce8dcb05f7f31530f299471282ef028c68a3a2c893ad61389194507eb2fc5f4` |

Each new route retains preflight, executed driver, argv, raw stdout/stderr,
actual exit/duration, source seals, old-sibling seals and copied raw test-result
bytes. Actual P05 preparation command prefixes and complete snapshot entries are
in the route results. Outer commands and normal-config source Git records are
under `D\logs\joined-caller-*`. No command exit is pipe-masked.

Source is clean and remains exact `b22443f9` after execution; only this report is
authored afterward. No helper/schema/test assertions, global settings, installers,
native Office/UI/COM/browser/server/renderer, network or denied route was changed
or invoked. No jq or alternative jq was supplied or executed. No structured P05
decision/corroboration or SHIP/publication action occurred.

This completes the released original P12 default/explicit caller recheck only.
The coordinator owns A03.2.n3, full-parent acceptance and final review. Prior
P05 review/cleanup limitations keep their own attribution; this report does not
reevaluate or erase them. Word layout, workbook caches/layout, PDF unresolved
origins/complete inspection, Visio writer/editor and other unmet native/shared
format obligations remain open. No wider client/runtime or full repository
acceptance is inferred from these two successful original caller runs.
