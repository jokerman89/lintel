# P08 partial implementation checkpoint

**Status: PARTIAL, NOT ACCEPTED.** Ready A08/A10 work and the explicitly advisory
A13.3 option are frozen for coordinator-scoped review. A13.1, A13.2 and A13.4 remain
blocked by the explicit producer-interface gate. This report is implementation and
self-review evidence, not independent SPEC, QUALITY, policy or release clearance.

## Immutable scope and authority

- Product: `8d477701bf05c545790d4c5fdbdc84945dd130db`.
- Product parent/control: `f9685530c458b456021e30ddab699c03b49614ca`.
- Original integration baseline: `5c3e7533335eeae15556aeabba422bfdbbe07f46`.
- Date: 2026-09-21. Implementer: sole P08 session
  `036fe896-b2de-4362-8db9-0c4697356afc`.
- Coordinator: MasterSession `a9a3c216-633c-43e8-b0c8-c297a7baef07`.
- Authority remains `../work.json`, its mapped plan/spec/prompt,
  `../packages/P08.md`, `../interfaces.md`, and the original A08/A10/A13 audit
  acceptance. This report neither changes task status nor creates another backlog.
- A08.2.a/b/c and A08.3.a/b/c were authorized in coordinator
  `e3799480dbeb3f80fb50299e923eabf62fab4f9b`; parent acceptance is unchanged.
- The later A13 pause in coordinator
  `fefbc9de1531092c9e2290ec0941ef2248136827` remains effective. Reported P10 frozen
  contracts do not themselves authorize consumption. No P10 WIP was imported.

The product is one local Conventional Commit: 46 files, 2,747 insertions and 942
deletions. This report is intentionally a separate commit. Its containing commit
identifies the immutable report; its exact SHA is supplied in the handoff rather
than attempting a self-referential hash here. The product commit left tracked and
nonignored status clean before this report was added. Runtime evidence is ignored
and preserved, not part of the product.

### Separately attributed dependencies

| Local commit | Coordinator origin | Purpose |
|---|---|---|
| `8a2429e631841358d05e8497422af546b59b57f9` | `e3799480dbeb3f80fb50299e923eabf62fab4f9b` | Approved metadata/sub-IDs and owned context-probe boundary; imported metadata not authored by P08 |
| `acc728eadf3343e76b7ae37eb0dc411af84dad51` | `c80584975fac0f36d22cd06733561bd968a097c2` | SHIP's selected-analysis advisory read; no P05 API/schema change |
| `35aff7e31bf67b7bed6ed05f00d71565775d31a9` | `d53dc38efa41a8ea562ca5078f36b0ce4010b712` | Existing evidence CLI executable mode |
| `cce69c82fcea8de0c88af850c20d828cc5faf7fa` | `aa56a672b0db187db20201d969f990ab0093802a` | Literal runner diagnostics and runner regression |
| `f9685530c458b456021e30ddab699c03b49614ca` | `935b640c0038a28921f130b7e48c6262cf6ecfd5` | Trusted welcome footer and current ADR-0025 parity oracle |

Accepted source dependencies were supplied by the coordinator, not independently
reaccepted by this session:

- P03 selection/context contract at
  `500adb370c726b86047046998a6a5240ebd8ea8f`, with Python-annotation correction
  `ca280747b9fe8c16ea54598c4192f40567b8430e` already in the baseline.
- P04's original work-map parser/task/package APIs, explicitly released for this
  consumer seam. No new task parser or acceptance hash was introduced.
- P05 product `b023e8c25a922ffc536ffde6dd911865bf2eea21`, first whole-quality
  acceptance `33eac071a26f5eb931c8ca7ed220fee91ea4469b`, integrated history
  `c5c8f8638f4ae7a93331c4b615f3b9f275ab18ca`.
- P06 provider-742 blob `0b3da55046358864fcd3075ba5bfb6c2348b1fec`,
  19,988 bytes, supplied SHA-256
  `331c1c62e932b5555089336d1fdcc7031f545780508f1d0f2e11bd9f2a7a8ebf`.
- P07 long-path product `4d0014639204e5b5ad84838272a840c5283e28d0`,
  acceptance `a745059786933c049a9fa52b74c0d2f5a9d19da1`,
  integration `c344133968f01a1f3710ce3e3264ef73359c220e`.

P05's bound review/context/QA v2 immutable obligations, P07 reference verification
and the declared profile/work-map/corroboration versions are unchanged. The
context manifest consumes P05 `bind_work`, including the accepted selection-relative
normalization/end-marker rules. Direct inspection remains
`release_clearance: false`. Native question/capability data is selection, not
execution, permission or independent-review evidence.

## Exact P08-authored product paths

These are precisely the paths in the product commit, relative to its parent.
The six added files are the workflow helper, intake reference and four integration
test files. The two new shell wrappers have mode 100755 and explicitly invoke the
Python tests. The report is not included in this 46-file product list.

```text
bin\_context.sh
bin\_jobs.sh
bin\li-work-artifacts.py
docs\concepts\jobs-system.md
lib\cycle-footer.sh
lib\orientator-routing.sh
lib\state.sh
lib\workflow.sh
skills\analyze\SKILL.md
skills\autoplan\SKILL.md
skills\build\SKILL.md
skills\capture\SKILL.md
skills\clean\SKILL.md
skills\code-freeze\SKILL.md
skills\code-unfreeze\SKILL.md
skills\cycle\SKILL.md
skills\define\SKILL.md
skills\define\references\intake.md
skills\discover\SKILL.md
skills\fix\SKILL.md
skills\handoff-size-check\SKILL.md
skills\jobs\SKILL.md
skills\maintenance\SKILL.md
skills\office-hours\SKILL.md
skills\plan-and-build\SKILL.md
skills\plan-ceo-review\SKILL.md
skills\plan-devex-review\SKILL.md
skills\plan-eng-review\SKILL.md
skills\plan-tune\SKILL.md
skills\plan\SKILL.md
skills\research\SKILL.md
skills\resume\SKILL.md
skills\review-and-ship\SKILL.md
skills\scope\SKILL.md
skills\sense\SKILL.md
skills\spec-kit\SKILL.md
skills\spec-kit\references\work-map.md
skills\status\SKILL.md
tests\integration\observation-learning.py
tests\integration\observation-learning.sh
tests\integration\universal-work-lifecycle.py
tests\integration\universal-work-lifecycle.sh
tests\shape\discover-scans-all-agent-categories.sh
tests\shape\handoff-cap-wired.sh
tests\unit\intent-operation-boundary.sh
tests\unit\review-source-target.sh
```

Gated `bin\_audit.sh`, `lib\memory.sh` and `bin\li-lessons-promote` were not
modified. Neither were P03/P07 helpers, `lib\paths.sh`, `lib\scale-estimator.sh`,
P05 review/QA/SHIP implementation, P06/provider/host code, P10 installers/lifecycle
helpers, private-sync, Swarm/Forge implementation, specialist/design roles, or
shared plan/map/memory/ADR files. The SHIP change above is an attributed coordinator
dependency, not a P08 edit.

Generated catalog/wiki/README tables and native kit were left unchanged.
Generated consistency/drift remains unreconciled and coordinator-owned; this
checkpoint does not claim a passing generator check or regenerate those files.
The edited canonical skills must be reconciled with generated outputs during
coordinator integration. No unrelated full-suite correction is included.

## Per-leaf implementation and evidence

**Built** below means authored and self-checked, not accepted. Behavioral references
are to the actual Python tests in `tests\integration\universal-work-lifecycle.py`
unless a different file is named. Instruction review is not a live model/host
execution result.

| Leaf | Built/deferred status | Evidence and remaining limits |
|---|---|---|
| A08.1 | Built, extends coordinator implementation | The original operation/topic separation, direct mutation and caller-IFS cases remain. `intent-operation-boundary.sh`: 54 assertions, zero failures. Added read/compound/negative cases; ambiguous compound instructions do not silently become execution authority. |
| A08.2 | Built through .a/.b/.c | The lifecycle starts an identified segment before phases; completion is status-grounded. Integration and existing continuity/footer checks are listed below. Real host orchestration remains subject to independent review. |
| A08.2.a | Built | `test_begin_once_and_interrupted_resume`, `test_truncated_done_transition_stays_incomplete`, `test_cycle_read_error_cannot_become_a_fresh_start`, `test_failed_state_write_and_field_injection_are_errors`, and identity-override negatives exercise actual state writers/readers. A failed read/write is not success or an automatic new cycle. |
| A08.2.b | Built | `test_footer_uses_status_not_next_metadata` discriminates BUILD/STARTING/next=REVIEW, blocked and done states. Existing `cycle-footer.sh` also exits zero. Next-phase metadata is not a completion signal. |
| A08.2.c | Built | `test_selected_phase_range_never_duplicates_entry`, unresolved-scope and range-resume tests exercise canonical entry/selection. The cycle instruction now dispatches one selected phase loop rather than duplicate SENSE/SCOPE entry. Its final two authority-confirmation sentences were self-reviewed after testing, as disclosed below. |
| A08.3 | Built through .a/.b/.c | One explicit original map supplies paths and IDs to continuity and downstream instructions. No timestamp/basename initiative inference, scheduler or second backlog was added. |
| A08.3.a | Built | Explicit-map/no-write, missing-selected-artifact, actual distinct/warming byte measurement, capacity-boundary and P05-binding tests. Shared P03 selectors and existing work-map parser are reused; bounded manifest defaults are 40 files and 262,144 bytes, not fabricated model capacity. |
| A08.3.b | Built | Real P07 reference creation/verification, fresh-shell resume, target/map override rejection, required-profile drift and missing-profile no-recreation tests. Context/generation/digest/required policy are verified before consumption; neither PID identity nor automatic different-target transfer is used. |
| A08.3.c | Built | ANALYZE/CAPTURE/BUILD/RESUME/budget instructions use the selected context and original IDs. `test_analysis_pointer_and_binding_ignore_stale_global_report` covers two initiatives, linked current report versus stale global report and changed binding. A binding is not review clearance. |
| A08.4 | Built in owned scope | Actual DISCOVER snippets exercise Markdown/YAML/unknown ADR conventions. Actual jobs readers retain old, blocked and unknown work without eager read-side writes. P10's migration reader and missing-catalog evidence are outside this product; no WIP or informal receipt parser is consumed. |
| A08.5 | Built | Two-initiative resume/history, original Spec Kit IDs, loop-back, selected range, older-cycle segmentation and cold-shell restart fixtures pass. Existing work-artifacts, enterprise workflow and Swarm preservation selections also exit zero. |
| A10.1 | Built; live-host observation unverified | Shared material-decision intake preserves prior authority and directs unresolved questions through the available host channel. `test_host_question_binding_not_a_fixed_tool_or_permission_bypass` uses the real capability resolver with synthetic bindings. It is not a paid-model call or observed host question interaction. |
| A10.2 | Built instruction routing | Maintenance/migration/research are not venture interviews. DEFINE, office-hours, research and CEO-review retain optional venture questions/lens and useful methods/examples. Intent routing is behavior-tested; semantic prompt adherence is self-reviewed, not independently or live-model verified. |
| A10.3 | Built | Alternate plan/build/review entries compose canonical selected work, status and authority. `test_actual_plan_review_snippet_to_latest_qa_ship` exercises prepare, the actual writer, applicable latest reader and same-context QA/SHIP, including a later rejecting record. `review-source-target.sh` retains three source/target/history-only cases. P05 schemas and immutable requirements are unchanged. |
| A10.4 | Built as explicit dormancy | Plan-tune does not invent an executable conflict resolver or pretend unread settings are active. The advisory settings/methods remain available with honest reader/activation limits. This is instruction review, not evidence of an activated runtime consumer. |
| A13.1 | DEFERRED/BLOCKED | No common audit/event reader or writer change. Needs reviewed exact producer contract, hashes, real fixtures and explicit coordinator release. Absent telemetry cannot be declared healthy/dead. The existing P05 review bridge fixture does not complete this cross-owner leaf. |
| A13.2 | DEFERRED/BLOCKED | No new add/update/supersede/promote implementation or promotion invocation. Existing L-NNN/superseded retrieval preservation passes, but is not configured-destination/provenance/promotion acceptance. Promotion safety remains open under the gate. |
| A13.3 | Built using the authorized advisory option | Freeze/unfreeze and BUILD/clean/maintenance guidance explicitly describe advisory scope rather than universal enforcement. `observation-learning.py::test_freeze_records_are_advisory_and_legacy_hook_only_warns` exercises actual existing audit/marker/warn behavior in synthetic isolation. No hook registration, universal write gate or enforcement claim. |
| A13.4 | DEFERRED/BLOCKED | Two observation preservation tests are not the complete event/promoted-lesson/incomplete-log round trip. Installer/runtime producer-consumer integration, malformed/incomplete-event semantics and promoted-destination evidence remain open with A13.1/.2. |

The narrow `_context.sh` probe/message now matches the existing Python 3.9 floor.
Supported/unsupported injected probes are tested while helper semantics remain
unchanged. Actual execution used Python 3.11. Actual Python 3.9 was unavailable:
neither injected version responses nor grammar/annotation checks establish 3.9
runtime compatibility.

## Synthetic isolation, incident and prevention

The historical cycle-continuity invocation labeled **shell27 is INVALID authorized
evidence**. It was reported as an actual-home boundary crossing and was stopped.
The actual-home effects remain **unknown**. No claim of no effect, cleanup,
restoration or rollback is made; no such investigation or rollback was authorized
or performed during the resumed work.

The operator's explicit recovery choice was: **resume with synthetic-only isolation;
no real-home access or rollback**. The resumed checks used the local ignored driver
`.claude\runtime\p08-isolated-run.py`, not the invalid invocation. Before each test
or direct fixture hook execution, it:

1. Constructs an allowlisted child environment with synthetic HOME, USERPROFILE,
   HOMEDRIVE/HOMEPATH, APPDATA/LOCALAPPDATA, XDG paths, temp paths, LINTEL_HOME,
   GSTACK_HOME and Git configuration. Inherited pack/profile/session/audit/registry
   selectors, Git redirects, BASH_ENV and Python overrides are not carried over.
2. Sets explicit trusted-source and fixture-repository roots, suppresses Python
   user-site/bytecode, system/global Git configuration, signing and registered
   hooks, then launches Bash without profile/rc loading.
3. Resolves actual paths through `paths.sh`, the pack resolver and jobs readers
   with `LINTEL_JOBS_NO_INIT=1`. It containment-checks and writes each command's
   `preflight.json` before invoking its test: HOME, USERPROFILE, LINTEL_HOME,
   packs, active-pack pointer, profile, global/hook/resolver audit, jobs, registry,
   canonical registry, state, sessions, repo and source.
4. Uses separate per-command home/temp fixtures and records actual exit status,
   stdout/stderr and source byte manifest. Tests run serially; no script was edited
   while running and no full-suite rerun was started after the freeze direction.

The explicit read-only jq exception was used only as a child-process PATH entry,
with approved SHA-256
`a6fc67fedaf9128a3309a1e2ebb8b986aeccf70122ee46d2cb4849e423f0c627`.
There was no download or global PATH/configuration change.

Source Git operations used the separate ignored `.claude\runtime\p08-git.ps1`
wrapper with synthetic configuration, disabled hooks/signing and explicit
`core.autocrlf=true`. Fixture Git settings were not misrepresented as source
checkout settings. No network, GitHub/credential operation, push, PR, release,
main mutation, nested implementer/reviewer, paid-model invocation or global
installation was performed for this checkpoint.

**Preventive rule for coordinator capture:** verify effective resolved paths in
the exact child fixture before invoking tests/hooks, not only the presence of a
temporary HOME variable. Keep source Git/EOL inspection separate from fixture Git
configuration. A failed preflight or cleanup is not a green run. This incident and
rule are recorded only here; the coordinator owns shared lessons.

Existing invalid-run diagnostics and synthetic failed-run fixtures were preserved.
No new access to actual home or external host-generated diagnostic files was used
to reconstruct the incident. A successful isolated rerun does not retrospectively
validate shell27.

## Actual commands and aggregate outcomes

All command paths below are workspace-relative. `$Jq` denotes the one expressly
approved read-only executable in MasterSession's
`.claude\runtime\tools\jq-1.8.2\jq.exe`; this notation avoids committing an operator
home prefix. Actual expanded child commands, paths, outputs and durations are
recorded in the local run evidence. The driver invokes the listed wrappers with
`bash`, retaining their exit codes.

The owned behavior selection was:

```powershell
python -I -S .claude\runtime\p08-isolated-run.py --run p8r4 --jq $Jq `
  tests\integration\universal-work-lifecycle.sh `
  tests\integration\observation-learning.sh
```

p8r2 selected those same two wrappers under `--run p8r2`. p8r3's recorded child
command selected only the two named regression cases under its verified fixture:

```text
bash tests\integration\universal-work-lifecycle.sh LifecycleTests.test_cycle_identity_cannot_be_overridden_by_metadata WorkSelectionTests.test_cold_resume_does_not_recreate_missing_profile
```

| Run | Actual outcome | Interpretation |
|---|---|---|
| Historical shell27 | INVALID | Outside the resumed isolation boundary; not acceptance evidence; actual-home effects unknown |
| p8r1 | Setup exit 1, zero tests | Windows text-mode Git stdin introduced CRLF in setup; not a test pass |
| p8r2 | Lifecycle exit 1: 28 tests, 7 errors, 251.745 s; observation wrapper exit 0 | Failed overall; six cleanup errors plus one explicit fixture unlink error, detailed below |
| p8r3 | Exit 1: 2 tests, 1 expected RED failure, 9.745 s | Canonical metadata override regression reproduced; repaired missing-profile fixture case passed |
| p8r4 | Lifecycle exit 0: 29 tests OK, 218.743 s; observation exit 0: 2 tests OK, 3.382 s | Two-wrapper aggregate exit 0; not a repository full suite |
| p8r5 | Aggregate exit 1: 14 of 18 wrappers exited 0, four exited 1 | Focused preservation selection FAILED; no green aggregate claim |
| p8r6 | Aggregate exit 1: all four selected wrappers exited 1 | Same-depth committed-dependency control reproduced the four failures; attribution only, not acceptance |

### RED, repair and preservation details

In p8r2, `test_cold_resume_does_not_recreate_missing_profile` failed on the fixture's
explicit `current.unlink()` before reaching resume. The P07-created default
`sessions\profiles\<64-character-context-id>\current-profile.json` path was 282
characters. `WinError 3` there was **not merely cleanup** and was not a P07 product
unlink. Six other errors were teardown `WinError 145` profile-directory cleanup
failures; that test also had a teardown error.

The owned fixture now uses P07's accepted same-location native I/O alias for that
unlink and cleanup. It does not shorten the path/store/context ID, change P07, skip
cleanup failures or weaken the assertions. It performs the actual deletion, then
actual resume must exit 2 with empty stdout and leave the profile absent.
The old p8r2 profile/diagnostics remain preserved; its inspected synthetic profile
SHA-256 was `61543015e4d0c8637ab17b4120541a6866fd7de6ef052b3d4048b63eed62a8f1`.

p8r3 then exposed a real identity-protection gap: supplied metadata could override
canonical cycle/work/profile/policy identity. The fix rejects protected metadata
overrides in the state writer. The negative fixture first failed (exit 97 from its
guard, yielding one unittest failure) and is included in the 29-test green p8r4.
Existing segment/history/failed-write behavior is retained.

### Focused preservation command

```powershell
python -I -S .claude\runtime\p08-isolated-run.py --run p8r5 --jq $Jq `
  tests\unit\intent-operation-boundary.sh `
  tests\unit\cycle-footer.sh `
  tests\unit\cycle-continuity.sh `
  tests\unit\context-repository-ownership.sh `
  tests\unit\context-checkpoint-roundtrip.sh `
  tests\unit\context-safety.sh `
  tests\unit\jobs-steps.sh `
  tests\unit\jobs-registry-concurrency.sh `
  tests\unit\jobs-system-present.sh `
  tests\unit\memory-v2.sh `
  tests\unit\work-artifacts.sh `
  tests\integration\enterprise-workflow-snippets.sh `
  tests\unit\review-source-target.sh `
  tests\integration\swarm-workflow.sh `
  tests\shape\claude-home-paths.sh `
  tests\shape\audit-writes-via-helper.sh `
  tests\shape\handoff-cap-wired.sh `
  tests\shape\discover-scans-all-agent-categories.sh
```

| Slot | Wrapper under `tests\` | Exit | Actual result |
|---|---|---|---|
| c1 | `unit\intent-operation-boundary.sh` | 0 | 54 assertions, zero failures |
| c2 | `unit\cycle-footer.sh` | 0 | ALL PASS, 46 PASS lines; skipped-phase behavior is a tested case, not a skipped test |
| c3 | `unit\cycle-continuity.sh` | 0 | ALL PASS, 19 PASS lines; fresh verified isolation, not shell27 |
| c4 | `unit\context-repository-ownership.sh` | 1 | Initial checkpoint reservation fails before ownership assertions; stdout empty |
| c5 | `unit\context-checkpoint-roundtrip.sh` | 1 | Reservation fails, path/list assertions fail; empty producer output cannot prove later equality-based preservation messages |
| c6 | `unit\context-safety.sh` | 1 | 23 tests, 22 pass/1 failure, 8.906 s; isolated bisect ref-lock failure |
| c7 | `unit\jobs-steps.sh` | 0 | ALL PASS, 28 PASS lines |
| c8 | `unit\jobs-registry-concurrency.sh` | 0 | Four preservation cases pass |
| c9 | `unit\jobs-system-present.sh` | 0 | All tests pass, 38 PASS lines; synthetic registry/audit lifecycle exercised |
| c10 | `unit\memory-v2.sh` | 1 | Lesson ranking/superseded exclusion, budgets, jobs and warn-hook checks pass; checkpoint path/list checks fail |
| c11 | `unit\work-artifacts.sh` | 0 | Original Spec Kit/native mapping and unsafe/missing negatives pass |
| c12 | `integration\enterprise-workflow-snippets.sh` | 0 | Four cases: selected work, scope/resume, inheritance and BUILD continuity |
| c13 | `unit\review-source-target.sh` | 0 | Three source/target/history-only cases pass |
| c14 | `integration\swarm-workflow.sh` | 0 | 56 Python tests OK, 69.588 s, plus shell scenarios; no Swarm product edits |
| c15 | `shape\claude-home-paths.sh` | 0 | 19 structural assertions pass |
| c16 | `shape\audit-writes-via-helper.sh` | 0 | Two structural assertions, 74 files scanned; not event-schema behavior acceptance |
| c17 | `shape\handoff-cap-wired.sh` | 0 | 17 structural assertions pass |
| c18 | `shape\discover-scans-all-agent-categories.sh` | 0 | Two structural assertions pass |

### Same-depth committed-dependency control

The coordinator explicitly authorized this bounded control and accepted its
inheritance attribution, not the failures as passing evidence:

```powershell
python -I -S .claude\runtime\p08-isolated-run.py --run p8r6 `
  --source-ref f9685530c458b456021e30ddab699c03b49614ca `
  --slot 4 --slot 5 --slot 6 --slot 10 --jq $Jq `
  tests\unit\context-repository-ownership.sh `
  tests\unit\context-checkpoint-roundtrip.sh `
  tests\unit\context-safety.sh `
  tests\unit\memory-v2.sh `
  *> .claude\runtime\p8r6-driver.log
```

The enclosing command captured and returned the Python status after displaying a
log tail. Actual aggregate exit: **1**. `--source-ref` exported the exact local
committed revision, not P08 WIP. Slots kept command directory depths identical.
No source/path/profile repair, fixture default relocation, OS long-path policy or
global Git setting change was made.

Each c4/c5/c6/c10 preflight verified the same 16 effective locations. Every
corresponding p8r5/p8r6 length matched (16/16 per slot):

| Effective location | c4/c5/c6 p8r5 = p8r6 | c10 p8r5 = p8r6 |
|---|---|---|
| HOME | 140 = 140 | 141 = 141 |
| USERPROFILE | 140 = 140 | 141 = 141 |
| LINTEL_HOME | 148 = 148 | 149 = 149 |
| packs | 154 = 154 | 155 = 155 |
| pointer | 166 = 166 | 167 = 167 |
| profile | 161 = 161 | 162 = 162 |
| global_audit | 154 = 154 | 155 = 155 |
| hook_audit | 159 = 159 | 159 = 159 |
| resolver_audit | 154 = 154 | 155 = 155 |
| jobs | 158 = 158 | 158 = 158 |
| registry | 164 = 164 | 165 = 165 |
| canonical_registry | 164 = 164 | 165 = 165 |
| state | 159 = 159 | 159 = 159 |
| sessions | 162 = 162 | 162 = 162 |
| repo | 137 = 137 | 137 = 137 |
| source | 137 = 137 | 137 = 137 |

The failure operations also matched, with exact failed-path lengths:

| Distinct failure | p8r5 length | p8r6 length | Evidence |
|---|---|---|---|
| Ownership checkpoint reservation | 268 | 268 | `context_safety.reserve_checkpoint`, exclusive `path.open("xb")`: `[Errno 2] No such file or directory` |
| Roundtrip `mywork` reservation | 278 | 278 | Same reservation error, followed by empty-path downstream assertions |
| Roundtrip `later` reservation | 277 | 277 | Same reservation error; no successful checkpoint creation established |
| Memory checkpoint reservation | 290 | 290 | Same reservation error; expected path/list checks fail |
| Git bisect ref-lock creation | 283 | 283 | `.git\worktrees\trial-success\refs\bisect\good-<hash>.lock`: `Filename too long` |

The context-safety control again ran 23 tests: 22 passed and
`test_isolated_bisect_recipe_preserves_dirty_source_on_success_and_error` failed,
in 9.380 s. It failed while creating a Git ref lock, not at checkpoint reservation
or cleanup. **No common root cause between these two failure groups is established.**

Before/after preservation is bounded precisely: retained source snapshots are
byte-unchanged after the runs (see hashes below), passing tests retain their stated
assertions, and the same failed wrappers reproduce without P08 code. c4 never
reached ownership assertions; c5's empty results invalidate downstream apparent
equality successes; c6's failing bisect case does not prove the promised complete
success/error preservation path. Those missing proofs are not waived by attribution.

## Source identity, EOL and retained evidence

Evidence resides under ignored `.claude\runtime\p8r2` through `p8r6`:
`source-manifest.json`, `results.json`, and per-command
`cN\preflight.json`, `stdout.log`, `stderr.log`. The runtime driver and fixtures are
local evidence, not a new shipped tool or authoritative work artifact.

Read-only reconciliation at freeze found no changed/missing manifest-listed source
bytes in any p8r2/p8r3/p8r4/p8r5/p8r6 snapshot after execution. p8r2-p8r5 each contain
958 source files; p8r6 contains the 952-file committed dependency tree.

| Run | Source-manifest SHA-256 | Results SHA-256 |
|---|---|---|
| p8r2 | `e1fc074b8fa7464dd3f2490934d5c8159cde151fc338ca796fca18ccaa52c8e3` | `f7651ce7e52fbdedfde9e171514161346c4dbe030954ca4b970dd068c3271351` |
| p8r3 | `9e9fc1a3e1633c99579f212e0b46aecdc421608a2806ea8a4a9649ed34eaeaba` | `c1b2c92651eb687d6a9c33f992adcff54a4c6a5f402099239da8a817682eac62` |
| p8r4 | `946b7c93cd9732816a26ff4952e51599147d948efe18a6f1aabdfd1cebfb6808` | `daac6bb310603f79206301fbb12ccac5847d5b6618a332c84880f8ed725c6a66` |
| p8r5 | `946b7c93cd9732816a26ff4952e51599147d948efe18a6f1aabdfd1cebfb6808` | `de7217f3eb5743cf5d79ac7cfe93bca44e1fbfe6afbd8dbb4523042a2083d824` |
| p8r6 | `c43b40eec60ffe215be0a3778d2ccef7384595675856907e622894f7427afb82` | `022471a0c203fd3407fab514645e8c5ff609a349c4616e950c591d349dd34897` |

All record dependency HEAD `f9685530c458b456021e30ddab699c03b49614ca`.
p8r4/p8r5 are byte-for-byte snapshots of the tested working source, including WIP;
that HEAD alone does not identify the candidate. The manifest does. p8r6 is a
committed-revision export using the explicitly isolated source Git settings; it
is not claimed to be an unconverted raw-blob LF snapshot.

The compared immutable files really are raw-byte equal in p8r5/p8r6:

| Source file | Observed EOL | SHA-256 in both snapshots |
|---|---|---|
| `lib\context_safety.py` | CRLF, 493 lines | `9291dd8989109ae2b897f6757d9235a22922e265f17b219d963b7daea518800c` |
| `lib\profile_context.py` | CRLF, 1,143 lines | `cd96361982dfd1f5a3130e17376e022caec02ba71e6aec0b69d8df13c0e86c71` |
| `tests\unit\context-safety.py` | CRLF, 503 lines | `a71fd331070d6be9eea1d6477a386163592ccc104f411c96bee8b9394d50205c` |
| `bin\_audit.sh` | LF | `e12b98dda8852cfc54c1270bfe968f22f49559e9ef20e0bf1fb31914b19344ba` |
| `lib\memory.sh` | LF | `714561fde4e11bcd65dac42263745a04c9a05d5c5d264ef538f0e8ee560b1886` |
| `bin\li-lessons-promote` | LF | `1f1b13f64fd4dac3d9e5b32067ceb3584be80e35eb84e73e2bc62b356e0b8102` |
| `lib\paths.sh` | LF | `a325a2780484a03b527ac4f481fb5e0281f77160f2fcf23d005c9a2f1ec09bba` |
| `tests\unit\context-repository-ownership.sh` | LF | `7f284617338a2656cc950795ce4c527da396d18b055dc10ad7e610b46c870fd8` |
| `tests\unit\context-checkpoint-roundtrip.sh` | LF | `2ff9bbeea2514a8600235032245a6bf44dc2cff700e15bb0b0388af20190922e` |
| `tests\unit\memory-v2.sh` | LF | `e9fddba5b4e06ccbd9ec23d5ad96c6c6f43f1bb66e19e276ff20ba66eb038202` |

The owned `bin\_context.sh` deliberately differs: candidate
`b91ad52266e5d2f9b158387ef544d287d1e78cc24af419e54c680bdd51584efb`
versus control
`1bf27608685f69f1038667eceae4b867bd7491b7818fb7478fdf67014f2b1905`
(both LF, 207 lines). This is the authorized interpreter probe/message change, not
a hidden common-dependency edit.

Source `git ls-files --eol` separately reported index LF, working CRLF for those
Python files, and index/working LF with `text eol=lf` for the existing shell files.
Git applied its existing clean conversion when staging new CRLF-working shell
files (`lib\workflow.sh` and the two integration wrappers) as LF blobs. No source
file was manually normalized to silence a diagnostic. Tested working bytes,
repository-normalized commit blobs and file modes are distinct facts.

The final product's working bytes differ from the p8r4/p8r5 manifest in **only**
`skills\cycle\SKILL.md`: two remaining unconditional confirmation sentences were
corrected to reconcile existing authority after SENSE and ask pre-BUILD only for
unresolved material choices. This small documentation-only delta was self-reviewed,
not rerun through the behavioral suites. The new wrapper executable modes were
recorded at staging; the evidence ran those same wrapper contents through Bash.
There is no claim that every final product byte/mode was executed after freezing.

Final runtime driver SHA-256:
`5e6f6b786838efccbf9529128931d821ae61247b5d7cf770575fcc64b661441c`.
Source Git wrapper SHA-256:
`07ed433d2a98370af95afc23cc03cc6f553508d2094a731540a3361aa43b14b9`.
The driver gained only the bounded committed-source/slot control options before
p8r6; earlier manifests/logs were not overwritten.

## Shape-oracle correction authority

Both owned oracle corrections are grounded in accepted P03
`500adb370c726b86047046998a6a5240ebd8ea8f`, not a new preference for a green test:

- `skills\context-budget\SKILL.md`, **Shared admission reader** and **Local
  thresholds and compatibility**, and `lib\context_safety.py::context_budget`
  state that absent capacity/usage/headroom is unknown. `ceil(bytes / 4)` is an
  input estimate, capacity needs a supplied observation source, and historical
  500k/750k thresholds are advice, not model-capacity defaults.
  `handoff-cap-wired.sh` therefore retains PLAN/CAPTURE invocation, selected-map
  continuity, advisory/off-switch semantics, mechanism existence and substantive
  review-preservation assertions while checking the real shared reader/unknown
  boundary. It does not restore a fictional cap.
- `skills\context-warm\SKILL.md`, `context_safety.select_files` and the Universal
  adapter installed-source boundary distinguish trusted source resources from
  target project data. `discover-scans-all-agent-categories.sh` retains the
  directory-derived inventory and negative hardcoded-category assertion while
  requiring explicit trusted source rather than target-root execution.

The shapes establish structural wiring only. Actual behavior comes from the
distinct-artifact byte/threshold tests, missing-selection and P05-binding
negatives, and the real DISCOVER source-versus-target/future-category fixture.
The preserved P03 context-safety tests also exercise unknown/estimated/observed
capacity and selection refusal, but their full wrapper still exits 1 because of
the separate bisect failure. Disk cleanup is never counted as reclaiming
already-sent context.

## Self-review disposition and handoff limits

Self-review has **two outstanding P1 verification-failure groups** (checkpoint
reservation: three wrappers; Git bisect ref lock: one wrapper), both reproduced
on unchanged committed dependencies. These are not independent reviewer findings,
not proven to share a root cause, and not accepted as passing preservation.
No additional unresolved P2/P3 product defect was identified by this bounded
self-pass; that is not a claim of absence. A13 gates and unobserved host behavior
are acceptance limitations, not hidden successful tests.

The coordinator instructed P08 not to repair those inherited paths or global
settings. The partial checkpoint therefore preserves the failed aggregate and
returns the blockers rather than weakening an oracle, changing P03/P07 or
expanding scope. `git diff --check` and the staged equivalent exited zero, owned
path checks excluded shared/gated/generated files, and the original baseline is
an ancestor of the product.

Outstanding before an all-P08 or all-A01-A26 completion claim:

1. Coordinator-scoped distinct independent SPEC, then whole-package QUALITY, on
   the immutable partial product and its dependencies. No nested reviewer was
   invoked and no independent gate is self-approved here.
2. Explicit A13 producer-interface release plus actual producer-consumer fixtures;
   only then implement A13.1/.2/.4, including malformed/incomplete event behavior,
   lesson provenance/configured destination and safe promotion.
3. Coordinator disposition of the two inherited failure groups and fresh
   evidence for the preservation assertions that never completed.
4. Coordinator-generated catalog/wiki/README/native-kit reconciliation and
   applicable joined validation. There is no passing repository full-suite
   claim, actual Python 3.9 run or live host/model-question execution.
5. P14's separately assigned installed canonical-shell bootstrap to bundled
   scaffold child-target bridge. These isolated helpers do not replace that
   end-to-end evidence, caller-pin/home preservation or refusal controls.

No report line marks the authoritative plan complete. The next authorized action
belongs to MasterSession: scope independent review of this partial checkpoint and
resolve the held interfaces. P08 is frozen at this handoff, not released or shipped.

## Narrow SPEC repair checkpoint - F01/F02/F03

**PARTIAL: ready for the same reviewer's SPEC re-review, not independently
accepted. QUALITY has not started.** This append records the later, explicitly
authorized repair. It does not erase the earlier checkpoint, incident, failed
runs, inherited preservation gaps or A13 gate.

### Authority, ancestry and exact owned delta

The complete immutable SPEC report
`9faebd2b7d027283a21a470c8e81473e39bd7a23` (parent
`062ffc6cc0cfecee80646fda2a54bad4db837fad`, preserved by the coordinator as
`a56aab4`) reported **two P1 findings and one P2 finding**. Coordinator
`04e0ba94c1d42f6b7198e7175f67c4e45d8b4333` authorized only their bounded
repair under A08.1 and A08.3.c, focused existing tests and this report. Both
authority objects were read through the isolated source-Git wrapper, not imported.
No new task source, sub-ID or approval was invented.

The new product is
**`44e2ef7d77ee1d9d68e466f2c7b11cd58be0d148`**, whose sole parent is the
earlier report-only checkpoint
`062ffc6cc0cfecee80646fda2a54bad4db837fad`. The earlier product
`8d477701bf05c545790d4c5fdbdc84945dd130db`, dependency parent
`f9685530c458b456021e30ddab699c03b49614ca` and original integration baseline
`5c3e7533335eeae15556aeabba422bfdbbe07f46` remain in that ancestry. No new
dependency commit, P03/P07 repair or P10 source was imported.

The product changes exactly **four paths, +377/-30**, with existing modes retained:

| Owned path | Added/deleted lines | Product Git blob |
| --- | --- | --- |
| `lib\orientator-routing.sh` | +52/-26 | `ef6d1b25ccd414dbf90934db7d07c06daf2414c1` |
| `skills\plan\SKILL.md` | +58/-4 | `7d511d86f13c6fb119aef81d2ea34139b1950320` |
| `tests\integration\universal-work-lifecycle.py` | +187/-0 | `abe75158cd862e010bcbcfaa82de3e8ba8fbe135` |
| `tests\unit\intent-operation-boundary.sh` | +80/-0 | `11cade8ecabfdd6108713b54b2d46989e7f6020c` |

The report-only follow-up changes only
`.claude\plans\universal-implementation\reports\P08.md`; its immutable commit
identifier is supplied in the coordinator handoff. Shared plan/map/todo/memory/ADR,
generated/distribution, provider, installer, P03/P07, QA/SHIP/review and gated
audit/memory/promotion sources remain unchanged. The existing fixture's private
P07 native-I/O alias imports also remain unchanged: a future shared-helper
migration needs the separate owner's reviewed handoff and explicit scope.

### F01/F02 - operation, clause and inquiry boundaries

The exact six review inputs were first run against the frozen router through
`classify_intent`, `match_workflow` and `score_confidence` in p8r7, before product
edits. Seven assertions failed across the three unsafe cases; the other three
controls and the original assertions passed. No recommended workflow was executed.

The repair retains negation throughout its clause rather than dropping it after
one skipped verb. Punctuation preserves clause boundaries without splitting
dotted subjects into new operations. Negative contractions and common prohibition
forms are handled before operation matching. Clause-leading auxiliary/wh questions
and assessment verbs remain read/review requests. Modal mutation requests,
question-mark mutation requests and a mutation following a prohibition stay
unclear/low rather than granting mechanical write authority. Explicit affirmative
deployment remains unchanged. This is a bounded mechanical classifier, not proof
that every natural-language formulation can be interpreted without clarification.

| Exact input | Frozen result | Final p8r13 result |
| --- | --- | --- |
| `Do not change the release plan; explain it.` | ship / SHIP / high | research / research-dive / high |
| `Do not edit the deployment code; describe the release process.` | research / research-dive / high | unchanged |
| `Is it safe to deploy this release?` | deploy / SHIP / high | research / research-dive / high |
| `Do not build the release.` | ship / SHIP / high | unclear / cycle / low |
| `Review the release plan.` | review / review / high | unchanged |
| `Deploy the reviewed release.` | deploy / SHIP / high | unchanged |

The original **54 assertions are retained**, not replaced. The final focused
test has **183 assertions, zero failures**, covering the full producer/consumer
chain, clause punctuation, subject words, contractions, prohibitions, inquiries,
assessments, genuine ambiguity, direct mutation, compound requests and caller
IFS preservation. The unchanged mechanical-routing wrapper also passes all eight
scenarios, including Swedish input, deploy/ship separation, risk policy and
escalation. These are observed helper results, not a live host question or workflow
execution.

### F03 - actual PLAN selection and persistence

PLAN Step 8 no longer instructs ANALYZE to overwrite the global report. Its actual
preparation block calls `workflow_resume` with the original cycle and map, verifies
the saved P07 reference/policy and selects that cycle's report. Its persistence
block re-verifies the same context in a fresh shell process, refuses a missing or
empty report and records the exact path/status using `state_append ANALYZE`.
This is the canonical ANALYZE persistence step once, not a second PLAN-return
entry, and does not complete or advance PLAN.

The new cases execute those two blocks extracted from the real PLAN skill, plus
the real workflow, state, work-map and profile helpers. They start two initiatives
with **no prepopulated report pointers**, then produce and consume the reports:

- One map is approved; the other deliberately remains DRAFT. Plain accepted
  context inspection preserves the original task/package IDs without turning draft
  inspection into acceptance binding. The approved first initiative is also
  checked against the real P05 `bind_work`; `release_clearance` remains false.
- Both selected reports are created in the unchanged state directory. Cold
  process handoff and return to the first initiative retain its map, artifacts,
  P07 reference, required policy and original IDs. Original maps/artifacts and the
  other initiative's report remain byte-identical; stale global GREEN remains
  unchanged history. Both canonical phases remain PLAN, with actual INCOMPLETE
  utility status and one initial ANALYZE entry per initiative.
- A different-map request and same-mtime required-profile drift fail before report
  or ledger writes. The accepted error is `PROFILE_DRIFT`; the required pack uses
  the existing complete synthetic manifest contract.
- Missing persistence refuses the link without altering the ledger. Legacy-global
  links are refused for repository-relative and native-drive/forward-slash
  spellings, with both present and absent report files. PLAN compares the existing
  parent directories' identity for this history guard; it does not shorten,
  relocate or rewrite default paths, change P03/P07 or introduce a native-path API.

**Evidence limit:** report prose is explicitly synthetic, with no semantic legs
checked and verdict INCOMPLETE. Selection, profile verification, writing and
linking are real. This does not prove a live model's DEFINE/PLAN analysis,
operator-accepted finding carryforward, or report-identity reasoning in prose.
The instruction to verify identity before reuse remains explicit; the tests do
not claim a new automatic report-identity parser or a new clearance schema.
ADR-0004 advisory behavior, declared mandatory controls and P05's immutable QA
obligations remain unchanged.

### Recorded RED, failed and final runs

Every invocation below used the preserved synthetic isolation driver. The
commands shown are the actual argument vectors; repeat only with a fresh run
name, not by overwriting these evidence directories.

```powershell
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r7 tests\unit\intent-operation-boundary.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r8 --case WorkSelectionTests.test_plan_analysis_caller_produces_selected_reports_and_retains_history --case WorkSelectionTests.test_plan_analysis_caller_refuses_map_mismatch_and_required_profile_drift --case WorkSelectionTests.test_plan_analysis_link_refuses_missing_persistence tests\integration\universal-work-lifecycle.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r9 tests\unit\intent-operation-boundary.sh tests\unit\orientator-mechanical-routing.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r10 --case WorkSelectionTests.test_plan_analysis_caller_produces_selected_reports_and_retains_history --case WorkSelectionTests.test_plan_analysis_caller_refuses_map_mismatch_and_required_profile_drift --case WorkSelectionTests.test_plan_analysis_link_refuses_missing_persistence tests\integration\universal-work-lifecycle.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r11 --case WorkSelectionTests.test_plan_analysis_caller_produces_selected_reports_and_retains_history --case WorkSelectionTests.test_plan_analysis_caller_refuses_map_mismatch_and_required_profile_drift --case WorkSelectionTests.test_plan_analysis_link_refuses_missing_persistence --case WorkSelectionTests.test_plan_analysis_caller_refuses_legacy_global_link tests\integration\universal-work-lifecycle.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r12 --case WorkSelectionTests.test_plan_analysis_caller_refuses_legacy_global_link tests\integration\universal-work-lifecycle.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r13 tests\unit\intent-operation-boundary.sh tests\unit\orientator-mechanical-routing.sh tests\integration\universal-work-lifecycle.sh
```

| Run | Actual outcome | Wrapper duration |
| --- | --- | --- |
| p8r7 | Exit 1; 72 assertions, seven failures on frozen routing | 13.89 s |
| p8r8 | Exit 1; three structural failures: unchanged PLAN Step 8 had zero expected Bash blocks | 24.69 s |
| p8r9 | Both wrappers exit 0; 153 routing assertions and all eight existing mechanical scenarios pass | 35.58 / 3.25 s |
| p8r10 | Exit 1; three tests, two failures in newly authored fixtures; missing-persistence refusal passes | 87.84 s |
| p8r11 | Exit 1; four tests, one failure in the new legacy-global guard's native-drive comparison | 215.95 s |
| p8r12 | Exit 0; corrected legacy-global method passes, including four present/absent and spelling subcases | 96.08 s |
| p8r13 | All three wrappers exit 0; 183 assertions, eight mechanical scenarios, **33 lifecycle tests OK** | 38.70 / 3.92 / 619.91 s |

p8r8 establishes the instruction/block inconsistency structurally; it is not an
observed global overwrite. p8r10's first fixture incorrectly requested P05 binding
on the DRAFT map; the accepted producer correctly rejected it. The other fixture
omitted required `voice.default_tier` and `navigation.default_workflow`; P07
correctly refused setup. Only the new fixtures were corrected: draft inspection
uses the accepted unbound context reader, and the strict pack uses existing
manifest prior art. No dependency assertion was relaxed.

p8r11 then exercised the intended three cases successfully and exposed the new
PLAN guard's lexical path-comparison defect. The correction is attributable to
this narrow PLAN change, not to the inherited checkpoint or Git failures.
p8r12/p8r13 verify the corrected guard without changing path defaults. p8r13's
unittest duration is **619.292 s**, with 33 tests, no failures, errors or skips;
the original 29 methods are retained plus four new methods. This is a three-wrapper
bounded aggregate, **not** a repository full-suite run or independent acceptance.

### Isolation, source identity and retained evidence

Before each wrapper, the exact fixture recorded effective HOME, USERPROFILE,
LINTEL_HOME, packs, pointer, profile, global/hook/resolver audit, jobs, both
registries, state, sessions, source and target. Each of the ten preflight records
has **16 contained effective paths**, `selectors: allowlist-only`, and no effective
path outside its own run fixture. Inherited selectors were removed before helper
resolution. Tests were serial within each run; running scripts were not edited.
Only synthetic repo/home/pack/state/log data was used. No real-home inspection,
restoration or rollback was performed.

Post-run report preparation compared all 16 recorded names/values against each
exact wrapper fixture: all ten records match 16/16. An initial read-only summary
command exited 1 because PowerShell rejected assignment to its reserved `$HOME`
variable; the corrected local name `$fixtureHome` produced those comparisons.
This bookkeeping failure invoked no test and changed no environment or file.

For final p8r13, source/target is `p8r13\s`, and each wrapper's HOME is its own
`p8r13\cN\h`; LINTEL_HOME is `h\.lintel`. All are beneath this worktree's
`.claude\runtime\`. The c3 effective lengths are:

| Effective path | Length |
| --- | --- |
| HOME / USERPROFILE | 141 / 141 |
| LINTEL_HOME / packs / pointer / profile | 149 / 155 / 167 / 162 |
| global_audit / hook_audit / resolver_audit | 155 / 160 / 155 |
| jobs / registry / canonical_registry | 159 / 165 / 165 |
| state / sessions / repo / source | 160 / 163 / 138 / 138 |

The driver and source-Git wrapper retain their previously recorded SHA-256 values
`5e6f6b786838efccbf9529128931d821ae61247b5d7cf770575fcc64b661441c`
and `07ed433d2a98370af95afc23cc03cc6f553508d2094a731540a3361aa43b14b9`.
All seven manifests contain 959 source files and HEAD `062ffc6...`, with WIP bytes
identified separately. Comparing p8r7 to p8r13 changes only the four owned paths.
The four product paths' final working bytes match p8r13's snapshot exactly; no
product edits occurred after that run. The commit uses Git's existing clean
conversion, not source normalization to silence EOL diagnostics:

| Owned path | Working EOL; index EOL | Tested working SHA-256 |
| --- | --- | --- |
| `lib\orientator-routing.sh` | LF; LF | `3b0fcc42ac5929b8283038dcfb20c33888031ce4481362f8f62747abcdfda092` |
| `skills\plan\SKILL.md` | CRLF; LF | `c0b4479e386d74eaaa0aed1eecfffea3bbeaa4d61da231a58a0a93934cbfeb5a` |
| `tests\integration\universal-work-lifecycle.py` | CRLF; LF | `b0e4db6790d6361402a9c6c7e01b3164b1a9c0e242865d4836262a9352b5f3f7` |
| `tests\unit\intent-operation-boundary.sh` | LF; LF | `b8629c7690b1a772c1acc8d9fc8c86c27d60121848329aff937402b37a6c60ca` |

The ignored run directories retain their source snapshots, commands, stdout,
stderr, preflights and exit results. Immutable SHA-256 references:

| Run | `source-manifest.json` | `results.json` |
| --- | --- | --- |
| p8r7 | `7eced74f5128146ee0d21f51963012f5ddcb685762db7aaea375b9805b3dca6a` | `7a47bb8da1411d79b82ffc3b01b73a933710c8dd77c4a1e91751db398d0909c8` |
| p8r8 | `93c83e4f80cf08d8461331321290845b7f2cf74fc8a5504341f5ffd9a43d134f` | `05c71b39252b633afb7d3c653f6888fa5216b0a36b9bd48854796fde32afe9e7` |
| p8r9 | `80de94619de0061fc939efde72af850f13fa56f6e442c96b98d6b1e00ca64d8b` | `6409b3ab186712b3fe762c5ed873d48b5bdffa983a0fd314680d867fa6ec5935` |
| p8r10 | `80de94619de0061fc939efde72af850f13fa56f6e442c96b98d6b1e00ca64d8b` | `0bb30ea40276e4ebee658aabf27c3481bb946a79e20c662b42efbe5fc28dab8e` |
| p8r11 | `6b79cf6c8bbab3df3c0a11818a81433a58b9677a8a3e5af96fa23bba045e2f4b` | `2dc861c775e2c4211f0cf3ec5f6ff89678445c4a677334393c131823c18a9f1f` |
| p8r12 | `0d43664d78f2c815cbbbcc56a006d18df67bc8e2f34d5fd045ea3ae79e903c5f` | `a80186be9e7e904b7cc830b3617a3154d4a6828513bdca1b3743114be5158520` |
| p8r13 | `e46076e2c0540d0e6472fe5f378b0ed149300b679f2383bd8cb415adf92c8da9` | `244857e52404d8007f9f96d0f2459877a012aff747a936f5d2a4c6e21a166c27` |

| Preflight | SHA-256 |
| --- | --- |
| p8r7/c1 | `fe6b32d1a58ea8c078c66c4741121234db32a7ae864aec7fb22ac1e77adaae2d` |
| p8r8/c1 | `84e1721eb1ae29f55b17409aa56e656d4a94f1e7c4b9b110a15a623db325d707` |
| p8r9/c1 | `e72e1048090ab2fd348ededb41ac72c5f0f591363f3c7abf4677c6ac4ee20e8a` |
| p8r9/c2 | `3080eb10c4d8f136dcdcea776acfd82b9fc654c58e1845bd3a6349f07a885fee` |
| p8r10/c1 | `b989841948404ff982e0f1e1a6c257e533da6e113e3c4cc4cd0dcf7545496755` |
| p8r11/c1 | `7a4fafc03348861ccb9b868833ba0b2bf185693b24c27d80aa551f02040b4158` |
| p8r12/c1 | `ef97501316e73c9c00080b85c5889213534d882c97e1bbd9645e8130ba851a71` |
| p8r13/c1 | `7d9eee7697aa0893241360104ad230c4e3b912940b50a4324f1e6df2c3de0b8a` |
| p8r13/c2 | `975fc51b0684a26c11c6f0dd89e9f31b3f9ca3b4242f36a0f64570c7458048e7` |
| p8r13/c3 | `677b302cbe5118071ad958b7a27cff0c1db66f5bc420c67956df19a4a9f9e771` |

### Per-leaf disposition and unchanged gates

These are implementer handoff dispositions against the existing IDs, not new plan
checkboxes or an independent verdict:

| Leaf | Current narrow handoff |
| --- | --- |
| A08.1 | F01/F02 repaired and bounded self-verification passes; independent SPEC re-review pending |
| A08.2.a / A08.2.b | No source delta; earlier mechanically verified coverage retained, not a new parent acceptance |
| A08.2.c | UNVERIFIED/BLOCKED preservation remains open |
| A08.3.a | No source delta; earlier bounded coverage retained |
| A08.3.b | UNVERIFIED preservation remains open |
| A08.3.c | F03 caller/persistence repair and new real-helper cases pass; semantic/model limitations above remain explicit; independent re-review pending |
| A08.4 | No delta; earlier bounded mechanical coverage only |
| A08.5 | BLOCKED inherited preservation is not closed by the new two-initiative case |
| A10.1 / A10.2 / A10.3 | No delta; remaining host/end-to-end acceptance remains UNVERIFIED |
| A10.4 | No delta; instruction-only dormancy, not a runtime settings reader |
| A13.3 | No delta; explicitly advisory, independently UNVERIFIED |
| A13.1 / A13.2 / A13.4 | DEFERRED/BLOCKED under the unreleased producer-interface gate |

The implementer self-pass found no additional unresolved defect in this four-path
repair after the recorded corrections; this is not a separate SPEC or QUALITY
review. The earlier independent **2 P1 + 1 P2** findings require the same reviewer,
`9dbf0a9b-750d-45c2-968c-41a5acb11c92`, to re-evaluate the new immutable
candidate. QUALITY remains not started until that SPEC disposition permits it.

**Historical evidence is unchanged:** shell27 remains INVALID with UNKNOWN
real-home effects; p8r2 remains 28 tests/seven errors; p8r5 remains FAILED at
14/18 wrappers, including context-safety 22/23; p8r6 still reproduces the four
failures on the unchanged dependency. The checkpoint-reservation and Git bisect
ref-lock groups remain distinct inherited blockers, not a proven common root
cause. Native unlink classification and preservation limits in the earlier
sections remain intact. No new passing run relabels those results or authorizes
global Git/OS settings, P03/P07 repair, actual-home access or rollback.

Generated catalog/wiki/README/native-kit reconciliation and P14's installed
canonical-shell bridge remain coordinator-owned. No new repository full-suite,
actual Python 3.9, live model/host-question, installed-client, shared-helper
migration or A13 producer-consumer acceptance is claimed. Missing proof stays
BLOCKED/UNVERIFIED.

The product was clean after its local feature commit; only this report was then
appended. Source/staged whitespace checks and the exact owned-path checks passed.
The final report-only commit and clean tracked state are verified in the handoff.
No push, PR, release, hook activation, credential/GitHub operation, nested agent
or external mutation occurred. P08 stops at this immutable partial checkpoint
pending the coordinator's concrete next scope.

## Governing-request and native-identity root correction

**PARTIAL, 2026-09-21: new builder checkpoint for the same reviewer.**
F01/F02/F03 remain independently OPEN (two P1 and one P2); QUALITY has not
started. This append records the authorized root correction and its bounded
self-verification, not acceptance of P08 or a waiver of earlier failures.

### Authority, frozen ancestry and source scope

Read the complete 362-line independent recheck
`af5c93999b5e83357ae337d92cfd4abde872e235`, preserved report-only by the
coordinator as `92502e6`. Its 21-case matrix has four failures despite the six
original examples passing; actual PLAN has two native-backslash refusal failures.
The design-only proposal preceded implementation. The selected correction is
authorized by exact coordinator
`679c6b8fb797607f3a712b2e196dcb9298d859b2`, appended P08 card and original
task refinements A08.1.r1/r2 and A08.3.c.r1/r2. The card and task rows were read
through isolated source Git, not imported or rewritten.

**Product: `171daa8d44f8e758bc1f6e98ff1c3a11a477ea18`.**
Its sole parent is report checkpoint
`8bde7005a1c6408f66c8656dc2e8a2923044b005`, whose parent remains the prior
product `44e2ef7d77ee1d9d68e466f2c7b11cd58be0d148`. No amendment,
dependency import, P03/P07 repair or source normalization occurred. The new product
contains exactly **five authorized paths, +563/-103**:

| Owned path | Delta | Product Git blob |
| --- | --- | --- |
| `lib\orientator-routing.sh` | +204/-92 | `f63e2cbf87f583c4d1c2236286a320245ffc5b3b` |
| `lib\workflow.sh` | +70/-1 | `27afa35ef9a826da57c8972b94d959bec60fbd9f` |
| `skills\plan\SKILL.md` | +12/-9 | `9597eba668bc7825429df6e39b25ff11926bf53f` |
| `tests\integration\universal-work-lifecycle.py` | +179/-1 | `e3548263ae9f31cb9a3538dfadaa36486299560b` |
| `tests\unit\intent-operation-boundary.sh` | +98/-0 | `923f4bbfc4414f73c70cfca7225fab0b555de5cf` |

Modes remain 100644 for the first four and 100755 for the routing test. The
workflow delta adds only the authorized internal guard plus its intent-review
date; existing workflow functions and signatures remain unchanged. This report
is committed separately, with its SHA supplied in the final handoff.

Source dependencies remain those already attributed in the earlier report.
The newly accepted P03 core/integration was coordination information, not imported
work. The existing private P07 fixture cleanup/unlink aliases remain unchanged.
Shared plan/map/todo/memory/ADRs, provider, P03/P07, P05 evidence/QA/SHIP, P10,
generated/distribution and gated audit/memory/promotion implementations are outside
this delta. No unreviewed `native_paths` helper is used.

### A08.1.r1/r2 - governing requests instead of token authority

The router now preserves clause, heading, colon and quotation structure before
recognizing a request head. Candidate operations come from supported imperative,
polite/emphatic or explicit request/assistance positions, never from scanning
arbitrary later words. Indirect information heads such as wanting to know or
needing advice retain their governing inquiry scope.

The complete clause is considered before emitting a recommendation. Prohibition,
conditional/ambiguous scope, quoted examples, nominal subjects and independent
requests cannot promote embedded action words into a write recommendation.
Colon prefixes retain a governing information request or a prohibition; a topic
prefix is not write authority. Separate requests are not collapsed merely because
they share the same public enum. Noun coordination remains distinct from an
independent action sequence.

The exact 21-case independent matrix, its permitted safe-result alternatives and
caller-IFS cases are retained. All original **183 assertions** remain; the
original 170-line test prefix is unchanged. Added structural contrasts independently
vary build/deploy/release against release-plan/build-script/review-checklist
subjects, with direct and polite controls, explicit request wrappers, indirect
assessment, subject prohibition, headings and quoted examples. Additional
contrasts cover colon scope, same-enum command sequences, conditional requests,
read complements, Swedish entry forms and direct IFS preservation.

The final focused result is **574 assertions, zero failures**, plus all eight
unchanged mechanical scenarios. No recommended workflow was executed.
`match_workflow`, risk/confidence interfaces, public enums, pack defaults and the
no-model-call behavior are unchanged.

**Limits:** this is a bounded request grammar, not full language understanding.
Unsupported wrappers, uncertain noun/verb readings and unresolved command or
constraint scope return unclear/low rather than granting authority. For example,
bare ambiguous release/build/ship nouns without an object marker or explicit
request wrapper are not affirmative operations. Symptom-only fix triage remains
available when no governing read/prohibition/ambiguous instruction supersedes it.
A recommendation is not execution permission.

### A08.3.c.r1/r2 - one native identity guard before selection

Actual PLAN preparation calls
`_workflow_guard_analyze_report_path(candidate, state-directory)` once, after
successful `workflow_resume` verifies the original map, P07 reference and required
policy, and before assigning the output report selection.

The guard uses the existing selected Python, native `Path` components, actual
`samefile` identity and accepted P03 `checked_root`/`safe_path` checks. Relative
input anchors to the explicit target. Only the declared target and state roots
authorize locations; a candidate parent never becomes its own authorized root.
There is no new drive parser, slash-only basename gate, directory creation,
stored-spelling rewrite, fallback on refusal or schema change.

Existing files are compared by actual filesystem identity. For absent leaves,
existing-parent identity is established first. The reserved history leaf is then
refused even when absent. A case-only leaf comparison is used solely to refuse
uncertainty when both files are absent under the same verified parent; it neither
establishes positive equality nor folds full/persisted paths or overrides distinct
existing-file identity. Known history returns exit 2 with the reconciliation
diagnostic; unsupported, missing-parent or denied identity is explicitly
INCOMPLETE, not assumed-different success.

Actual PLAN cases cover:

- Relative, drive-qualified forward-slash and native backslash history spellings,
  each absent and present, with no target/ledger/history changes on refusal.
- A distinct same-basename parent, absent and present in all three spellings;
  preparation runs from LINTEL_HOME rather than the target cwd and still preserves
  the exact selected spelling and correct target anchoring.
- An undeclared outside parent and a missing parent, both explicitly refused with
  no directory/report creation or ledger mutation.
- Existing native case-alias identity, uncertain absent case-only names, and an
  explicitly declared state root outside the target without permitting arbitrary
  outside candidate roots.
- The original two-initiative production, cold persistence/resume, selected
  consumer/P05 binding, wrong-map refusal, required-profile same-mtime drift and
  missing-persistence checks.

The full suite retains the previous 33 methods and adds six, for **39 methods**.
Report prose is still synthetic INCOMPLETE with no semantic legs checked;
selection, profile verification, report I/O and persistence are real. ADR-0004
advisory semantics and P05 mandatory/immutable evidence obligations are unchanged.

**Specific evidence limits:** this Windows fixture reported
`PLAN case-sensitive existing pair observed: False`. It verified the actual
existing alias/refusal branch, not the positive branch for two case-distinct
files on a case-sensitive filesystem. That host-specific positive execution
remains UNVERIFIED; no OS/filesystem case setting was changed. Denial was injected
as `PermissionError` at `os.stat` inside the process executing the actual guard;
other Python invocations passed through unchanged. This verifies error
propagation/no-write behavior, not an actual ACL denial. No live model analysis,
host question-channel interaction or semantic finding-carryforward is claimed.

### Commands and actual RED/intermediate/final outcomes

All entry commands below use the unchanged isolated driver and fresh evidence
directories. They are recorded commands, not instructions to overwrite those runs.

```powershell
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r14 tests\unit\intent-operation-boundary.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r15 --case WorkSelectionTests.test_plan_analysis_caller_refuses_legacy_global_link --case WorkSelectionTests.test_plan_analysis_identity_keeps_distinct_same_basename_parent --case WorkSelectionTests.test_plan_analysis_identity_refuses_unproven_or_undeclared_parent tests\integration\universal-work-lifecycle.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r16 tests\unit\intent-operation-boundary.sh tests\unit\orientator-mechanical-routing.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r17 --case WorkSelectionTests.test_plan_analysis_caller_refuses_legacy_global_link --case WorkSelectionTests.test_plan_analysis_identity_keeps_distinct_same_basename_parent --case WorkSelectionTests.test_plan_analysis_identity_refuses_unproven_or_undeclared_parent --case WorkSelectionTests.test_plan_analysis_identity_uses_existing_case_identity --case WorkSelectionTests.test_plan_analysis_identity_refuses_denied_probe --case WorkSelectionTests.test_plan_analysis_identity_honors_declared_state_root tests\integration\universal-work-lifecycle.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r18 tests\unit\intent-operation-boundary.sh tests\unit\orientator-mechanical-routing.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r19 --case WorkSelectionTests.test_plan_analysis_identity_keeps_distinct_same_basename_parent --case WorkSelectionTests.test_plan_analysis_identity_uses_existing_case_identity --case WorkSelectionTests.test_plan_analysis_identity_handles_absent_native_case_alias tests\integration\universal-work-lifecycle.sh
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8r20 tests\unit\intent-operation-boundary.sh tests\unit\orientator-mechanical-routing.sh tests\integration\universal-work-lifecycle.sh
```

| Run | Actual outcome | Wrapper seconds |
| --- | --- | --- |
| p8r14 | RED, exit 1: 553 assertions, 121 failures on unchanged product; retained 183 pass | 157.05 |
| p8r15 | RED, exit 1: three methods, four failure subtests; native history absent/present and undeclared/missing parent fail; distinct-parent controls pass | 298.47 |
| p8r16 | FAILED aggregate: routing exit 1, 553 assertions/one retained inquiry regression; mechanical exit 0/eight scenarios pass | 96.48 / 3.92 |
| p8r17 | Exit 0: six focused PLAN methods pass on the intermediate candidate | 422.94 |
| p8r18 | Both exits 0: 574 assertions and eight mechanical scenarios pass | 94.02 / 3.62 |
| p8r19 | Exit 0: three focused target-anchor/case-identity methods pass | 239.34 |
| p8r20 | Final bounded aggregate: all three exits 0; 574 assertions, eight mechanical scenarios, 39 lifecycle methods OK | 78.03 / 2.39 / 1134.14 |

p8r16's retained `should we build or release this?` failure came from treating
the alternative inside a governing inquiry as a separate operation. The fix
retains the inquiry's complement scope rather than exempting that input. Source
self-review also made colon-governed information/prohibition and same-enum
independent sequences explicit; their added contrasts pass in p8r18/p8r20.
p8r14/p8r15/p8r16 remain failed runs, not retrospective passes.

p8r20's unittest duration is **1133.705 seconds**, with 39 tests, no failures,
errors or skips. The aggregate is the three selected wrappers, **not a repository
full-suite pass**, another independent review or a rerun of inherited blockers.
No running test script or sealed source was edited.

### Isolation seals, source bytes and reproducibility

Every wrapper invocation recorded its actual effective-path preflight before
execution. The eleven preflights were compared field by field against their
exact synthetic run/slot: **16/16 values match in every record**,
`selectors: allowlist-only`. No effective path escapes its fixture. Synthetic
HOME/USERPROFILE/LINTEL_HOME, derived packs/profile/pointer/audit/registry locations
and explicit source/target roots were retained; inherited redirect selectors
were not imported. The driver and isolated source-Git wrapper retain the hashes
already recorded above. No real-home inspection, cleanup, restoration or rollback
occurred.

For p8r20, HOME/USERPROFILE lengths are 141, LINTEL_HOME 149, packs 155,
pointer 167, profile 162, global/resolver audit 155, hook audit 160, jobs 159,
both registry paths 165, state 160, sessions 163, and source/target 138.
Default paths were not shortened or stores relocated.

Each manifest contains 959 source files at base HEAD `8bde7005...` with the
exact subsequent working bytes recorded separately. After the final run, all
**959 original and 959 copied files** still matched the sealed manifest.
p8r18/p8r19/p8r20 share the exact source manifest. Product was committed only
after this preservation and the final aggregate passed.

| Owned source | Tested raw SHA-256 | Working EOL |
| --- | --- | --- |
| `lib\orientator-routing.sh` | `10be76f6744427b7e1a281d4ecfd168618629c00a3133e1117d8294e28f1152f` | LF, 384 lines |
| `lib\workflow.sh` | `629688fdfde96d279496bd22dfc173bc58fd7ab16b1cbd34824135aa4a707be9` | CRLF, 232 lines |
| `skills\plan\SKILL.md` | `3e54d4f088ee464cc67048feb7e63abc569c324ca934044eea762761cb1c9da0` | CRLF, 689 lines |
| `tests\integration\universal-work-lifecycle.py` | `fc2c53b98aad6f79baa4b6e3063ade98ff82fba80acdf18c696206a7285874ad` | CRLF, 955 lines |
| `tests\unit\intent-operation-boundary.sh` | `778c78d4386cbdd292f052d12650f42d81916b6c1061441e293d5ad2554047bd` | LF, 270 lines |

Git blobs use the existing LF clean conversion. Source/staged whitespace checks
exit zero. Git's CRLF-to-LF warning for `lib\workflow.sh` was retained; source was
not normalized to silence it. Actual copied execution bytes were checked, not
merely the pre-copy manifest. A read-only preflight-summary command initially
had a PowerShell `$name:` interpolation syntax error; it ran no test/helper
and wrote nothing. The corrected comparison produced the 16/16 results above.

| Run | Manifest SHA-256 | Results SHA-256 |
| --- | --- | --- |
| p8r14 | `4c9b2e69aa485377890a7e7162ecd407cce7a8984747d81f77fb1acf06682ac0` | `ffae522513c0331618d5727268cf0e51785a67c4d5880c203d084ed55e42cc0c` |
| p8r15 | `4c9b2e69aa485377890a7e7162ecd407cce7a8984747d81f77fb1acf06682ac0` | `8a2d88abe0a0cb97a433c5fc2a65a6974eb435497161c8558a91d9998098266e` |
| p8r16 | `e9fb2fb8243c372d41c01259e14136f1dc00c2da25992bafd29fb8c77889f6e3` | `9346d15e8d14cce0e758c7003aae38c7f0a477d154f5d2d8c9d2b462c80703e4` |
| p8r17 | `e9fb2fb8243c372d41c01259e14136f1dc00c2da25992bafd29fb8c77889f6e3` | `6979652f772fae4a7cad80ea0af05342b69885a9d71e14c8cb836284710e2cce` |
| p8r18 | `09c36ea65f8e972f7719f8c0942c663a4ac33fffea7a6c31622af10be1c97d0a` | `e73f84e292cc7f274f5e341620af7a6b44d41f7df95ca01134681ffd14190ac4` |
| p8r19 | `09c36ea65f8e972f7719f8c0942c663a4ac33fffea7a6c31622af10be1c97d0a` | `555f64ec56ee32eb0fda7faf1e65bb7c5292c510873493ac544d4383b8847ef7` |
| p8r20 | `09c36ea65f8e972f7719f8c0942c663a4ac33fffea7a6c31622af10be1c97d0a` | `176a22bfdc130ec33abd3be7b8f7734f52d1455039c1a7c30cd717a34a575723` |

| Preflight | SHA-256 |
| --- | --- |
| p8r14/c1 | `798cc55b178984ff9e2093562020adf590fd4a24bb8e7dbbee9e125f6f779b5d` |
| p8r15/c1 | `9e8f48616c574312cefcd061029e2d726edac356e8572c1311efb08121d456f8` |
| p8r16/c1 | `ee62f09e0d54f5a83e5385c2be324743a5bcac11da21cb6dfb12891347150c92` |
| p8r16/c2 | `398e29a7e847758be32173925e1b5bd7cd21f31014162772813a5854e439a56a` |
| p8r17/c1 | `6cd092959bc8582f02a83bdb30b0aa5c3a5eeb291936709a5a25958f40925798` |
| p8r18/c1 | `4984392e6294c100d2190a405137030bee305cd959ce691fc7bb95863c21a94f` |
| p8r18/c2 | `a468dca2ebe60c13ad366a3180118cc8125b94c3b18f290adee7bff8eca5255c` |
| p8r19/c1 | `12ed1cf17556a7caa5c2cc4fff0d04d0d2be01eff2fae0f3df7ce06feff4c1dc` |
| p8r20/c1 | `1fe7bbcd7612b825e4ad235506ab300cbb34a243d6cb6c6b65794bf683f4d1ab` |
| p8r20/c2 | `c70ddb33ae49c6e17fdee129c8d0e2b05f3ce431ce66e175949459f6bbe7e349` |
| p8r20/c3 | `59ad880aa85ed625dca2ad8b72a0693799eadebad118f9ad297ffd3ce1f78177` |

### Refinement disposition, lessons and unchanged gates

| Authorized refinement | Builder disposition |
| --- | --- |
| A08.1.r1 | Built: governing-request/clause recognition; no first-action-token scan or new public contract |
| A08.1.r2 | Locally verified by the retained matrix/assertions/mechanical scenarios and structural contrasts; independent F01/F02 closure pending |
| A08.3.c.r1 | Built: the sole internal identity guard and actual PLAN call; no other workflow implementation changed |
| A08.3.c.r2 | Available actual PLAN cases and all 39 lifecycle methods pass; case-sensitive positive-host and injected-denial limits above remain explicit; independent F03 closure pending |

**Dated correction lessons, retained here only:** on 2026-09-21 the independent
recheck demonstrated that passing six examples did not repair the action-token
authority class. Preserve structure and establish the governing request before
matching action vocabulary; vary subjects independently from action words in
regressions. Likewise, native path identity must precede report selection:
successful forward-slash controls do not prove backslash or absent-file behavior.
Shared lessons/ADRs remain coordinator-owned and were not edited.

Self-review found no further unresolved defect within this five-path correction
after the recorded changes, but this is not distinct SPEC or QUALITY review.
The same reviewer `9dbf0a9b-750d-45c2-968c-41a5acb11c92` must evaluate the
new product and these evidence limits. No local result closes original parents
A08.1/A08.3 or any other P08 leaf. All other per-leaf dispositions from `af5c9399`
remain unchanged: host/semantic/preservation gaps remain UNVERIFIED/BLOCKED.

**Historical evidence remains immutable:** `44e2ef7`/`8bde700` and `af5c9399`
are not rewritten or relabeled; shell27 remains INVALID with UNKNOWN real-home
effects; p8r2 remains 28 tests/seven errors; p8r5 remains FAILED 14/18 and
context-safety 22/23; p8r6 still records the unchanged-control failures. Checkpoint
reservation and Git ref-lock groups remain distinct inherited blockers.
All earlier RED/intermediate failures and native unlink/preservation limits remain
as recorded. No new full preservation run or shorter-path substitute occurred.

A13.1/.2/.4 and audit/memory/promotion remain gated and byte-unchanged. Advisory
A13.3 has no new execution/acceptance. The accepted core, shared-path fixture
migration, P10 and generated/installed P14 integration require separate exact
handoffs; none were imported or repaired here. Actual Python 3.9, other OSs,
live model/question/client behavior and semantic report carryforward remain
unobserved. QUALITY has not started; no whole-P08 or all-A01-A26 pass is claimed.

Product was clean after its local feature commit. Only this report was then
appended; the final report-only ancestry, unchanged earlier report prefix and
clean tracked/unignored state are verified in the handoff. Ignored diagnostics
remain preserved. No push, PR, network/credential operation, hook activation,
global setting, actual-home access/rollback, release or nested agent occurred.
The product/report are frozen pending a concrete coordinator instruction.

## Reviewed dependency join and split-fixture verification

**PARTIAL, 2026-09-21: combined product verified locally, not independently
accepted.** F01/F02/F03 remain OPEN for the same reviewer, and QUALITY has not
started. This append preserves every earlier report and failed/invalid run.

### Exact authority, attribution and unchanged product

Read the complete "Post-freeze reviewed dependency join" in coordinator
`733b28a8cc6285c33234f11ad860e60ecede33a5` and its A08.5.j1/j2 refinements.
After reporting the first joined-run blockers, read the entire "Joined
verification harness correction" in
`5efd5da534776a6e7d1043ed9319125db1a0fe1e`. That later authority changes only
private verification machinery and this report, not the product or accepted tests.

The combined product is **`2f4cc38077f547bc0ab006ecf9c90d6ba727db7a`**:

| Local commit | Sole parent / separately attributed origin | Scope |
| --- | --- | --- |
| `7a4fcdbac45ffd6f86de45ac982eb3be40800999` | Parent `723b067f1bbfe4f0cbd2b86614973b4c556f36c7`; exact cherry-pick `ce7415f6342220e49746880dd7338a6e7d9294ed` | Reviewed 16-path native-path core, +494/-118 |
| `1bb75560f685f9113b5f481e01db2d0f6a636fda` | Parent `7a4fcdb...`; exact cherry-pick `5fc657f14123daca9ec126cadc1e9d64ee60691c` | Reviewed P01 fixture follow-up, one path +17/-1 |
| `2f4cc38077f547bc0ab006ecf9c90d6ba727db7a` | Parent `1bb75560...` | P08 fixture consumer only, one path +5/-4 |

Both dependency imports were conflict-free and retain `-x` origin attribution.
Their independent scoped acceptance is
`062d0f20f41f33db437324d2d9094ab418338e2c`, integrated by the coordinator as
`1d44d2a`; neither review nor integration metadata was imported. All 16 final
dependency blobs and modes match `5fc657f` exactly. They have no intersection
with the preceding P08-authored path range.

The sole new P08-authored source delta is
`tests\integration\universal-work-lifecycle.py`: its cleanup and missing-profile
unlink use trusted `native_paths.native_io_path` instead of the private P07
alias. `ProfileConfig`, `context_path`, method bodies, assertions, cleanup/error
behavior and fixture roots remain otherwise unchanged. Its blob is
`d1ab38cedc7f3dad10b07a7a9f553e6d13bda766`, mode 100644. The focused p8j0
missing-profile/unlink/cleanup case passed before that separate fixture commit.

The four frozen root-correction blobs/modes remain exactly `171daa8`:
router `f63e2cbf87f583c4d1c2236286a320245ffc5b3b`, workflow
`27afa35ef9a826da57c8972b94d959bec60fbd9f`, PLAN
`9597eba668bc7825429df6e39b25ff11926bf53f`, routing assertions
`923f4bbfc4414f73c70cfca7225fab0b555de5cf`. Their raw working-byte hashes remain
those recorded in the preceding append. No root correction, P03/P07 logic,
accepted test method, P10, A13, generated output or shared ledger was edited.
The original 1,088-line report from `723b067f` remains the full unchanged prefix;
its working CRLF SHA-256 is
`bb856f2f8ff6f41493dd3965f4847d6e019e7146b671474c261a47a9471596ac`.

### Preserved interrupted run and concrete harness corrections

p8j1 is still **INCOMPLETE**: the first 18 wrappers had 16 exits zero and two
failures; c19 mechanical routing passed; c20 lifecycle was interrupted; c21
observation did not run. There is no successful p8j1 aggregate.

Its c4 checkpoint reservation succeeded at the original 268-character location,
then six preservation groups passed. The newly reached uninitialized plain-folder
case discovered the outer real checkout instead of returning `no-branch`.
Its c6 ran 28 methods with seven failure subtests in two new exact-size methods:
the deep fixture could not represent the required padding, including the
218-to-260 publication case's explicit parent-below-94 requirement. Those were
fixture representability assertions, not observed core I/O failures. The actual
283-character bisect method passed separately. Neither failure justified editing
the product, weakening an assertion or changing global Git/OS settings.

The original runner's unredirected outer output also caused the host tool to
report an automatic 25.7 KB AppData Temp capture. The attached runner was stopped.
That capture remains **uninspected host-managed output**: it is neither proof of
a test HOME escape nor proof of no external effect. It was not opened, removed
or rolled back. All in-repository p8j1 diagnostics and old private-driver bytes
remain preserved. A later read-only process check found no remaining owned
p8j0/p8j1 driver/test process before continuation; unrelated processes were not
changed.

Under `5efd5da`, the new private driver sets per-process
`GIT_CEILING_DIRECTORIES` to the exact verified synthetic run root. Actual
startup-free Bash/Git probes, not only environment strings, return exit 128 and
empty stdout for an uninitialized plain directory, then exit zero and the exact
expected root for an initialized child. Every command records the effective
ceiling, proof outputs, actual argv, source seal and exit. Source-checkout Git
verification uses its separate established wrapper and does not inherit these
fixture ceilings.

For the two specially authorized probes, the ceiling is an explicit two-root
list: the deep synthetic run and the owned short-temp child. It is never their
common personal-home ancestor. Both boundaries receive actual Git proofs.
The only authorized short children were `<this-session-files>\p8s1` for the
affected run and `<this-session-files>\p8s4` for the final run, each an unused
88-character native path, below the required 94-character limit. Creation checked
the exact assigned files directory, direct-parent identity, non-link/reparse
status, nonoverlap and exclusive ownership marker. No other personal-home or
configuration path was inspected.

The two unchanged methods alone use those short temporary parents. Their actual
tested logical destinations remain 259/260/268/278/277/290 and 218-to-260, with
unchanged assertions. After each run, only the expected owner marker and Git-proof
directory remained; the exact owned short child was removed after rechecking
ownership. Ordinary deep temporary roots were empty after their fixtures'
unchanged cleanup. Deep source/proof/log directories remain retained as evidence.

Outer stdout/stderr are redirected before launch to explicit in-repository
runtime logs. The actual controller exit is saved separately and returned without
a `runner | tail` pipeline. Only bounded summaries reach the host tool. No new
host output capture was reported during these corrected launches.

### Complete and disjoint context-safety split

The unchanged `tests\unit\context-safety.py` has 28 methods, raw SHA-256
`2ec19989e027700c459864fccc2af770571d1c2921489cfde2f3b9ba56aba26b`,
Git blob `67570146ec066eb036f0be2d021d06816133f981`. Its AST-derived inventory
is recorded before each run. The union is exactly 28 and the intersection empty:

```text
Deep c6: ContextSafetyTests.
  test_adr_and_related_recipes_preserve_scoped_selection
  test_budget_perf_and_cool_recipes_invoke_the_actual_reader
  test_capacity_unknown_estimated_and_observed_are_distinct
  test_cool_exclusions_have_a_real_consumer_and_no_reclamation
  test_cooling_exclusions_cover_native_case_aliases_without_hiding_distinct_files
  test_glob_exclusions_do_not_assume_every_windows_directory_is_case_insensitive
  test_glob_with_file_prefix_never_selects_the_nonmatching_prefix
  test_globs_are_scoped_and_missing_is_visible
  test_globstar_comparisons_are_bounded_by_distinct_states
  test_globstar_long_paths_and_rules_do_not_use_python_recursion
  test_globstar_normalization_preserves_whole_path_matching
  test_globstar_selector_finishes_with_subprocess_timeout_backstop
  test_isolated_bisect_recipe_preserves_dirty_source_on_success_and_error
  test_literal_spaces_and_execution_text_are_data
  test_markdown_yaml_and_unknown_adr_metadata_remain_visible
  test_matching_failure_is_not_reported_as_an_empty_selection
  test_native_long_link_escape_and_namespace_refusals_precede_temp_creation
  test_native_long_parents_read_select_cool_and_case_identity
  test_partial_migration_gate_does_not_auto_undo_unknown_states
  test_perf_flags_only_change_advice
  test_persisted_cooling_globstars_are_bounded_on_every_selection
  test_required_native_module_never_loads_a_target_or_pythonpath_substitute
  test_separated_globstar_states_are_compared_at_most_once
  test_shared_helpers_defer_annotations_for_python39_syntax
  test_traversal_aliases_and_links_are_refused
  test_warm_recipe_executes_selector_not_shell_input

Short c22: ContextSafetyTests.
  test_native_checkpoint_thresholds_keep_logical_paths_and_owned_bytes
  test_native_snapshot_atomic_publication_uses_218_to_260_without_shortening
```

Each group executes the original file with those complete unittest selectors and
the existing interpreter. The old whole c6 shell wrapper is **not** claimed green:
this is an explicitly authorized **split-fixture group, 26 + 2**, with no skips.
The Python 3.9 annotation/grammar case remains that kind of evidence, not an
actual Python 3.9 execution.

### Invocations and final joined outcomes

`$Jq` is the already approved read-only coordinator jq 1.8.2 executable, verified
against SHA-256
`a6fc67fedaf9128a3309a1e2ebb8b986aeccf70122ee46d2cb4849e423f0c627`.
All drivers/evidence below are under this worktree's `.claude\runtime\`.
The new verification driver preserves the original isolation driver's source
snapshot/environment machinery; its additions are private harness behavior only.

```powershell
python -I -S .\.claude\runtime\p08-isolated-run.py --run p8j0 --case WorkSelectionTests.test_cold_resume_does_not_recreate_missing_profile tests\integration\universal-work-lifecycle.sh
python -I -S -B .\.claude\runtime\p08-joined-run.py --run p8j1 --jq $Jq tests\unit\intent-operation-boundary.sh tests\unit\cycle-footer.sh tests\unit\cycle-continuity.sh tests\unit\context-repository-ownership.sh tests\unit\context-checkpoint-roundtrip.sh tests\unit\context-safety.sh tests\unit\jobs-steps.sh tests\unit\jobs-registry-concurrency.sh tests\unit\jobs-system-present.sh tests\unit\memory-v2.sh tests\unit\work-artifacts.sh tests\integration\enterprise-workflow-snippets.sh tests\unit\review-source-target.sh tests\integration\swarm-workflow.sh tests\shape\claude-home-paths.sh tests\shape\audit-writes-via-helper.sh tests\shape\handoff-cap-wired.sh tests\shape\discover-scans-all-agent-categories.sh tests\unit\orientator-mechanical-routing.sh tests\integration\universal-work-lifecycle.sh tests\integration\observation-learning.sh
python -I -S -B .\.claude\runtime\p08-joined-verified-run.py --run p8k1 --phase affected --short-child p8s1 --jq $Jq 1> .\.claude\runtime\p8k1-driver.stdout.log 2> .\.claude\runtime\p8k1-driver.stderr.log
python -I -S -B .\.claude\runtime\p08-joined-verified-run.py --run p8k2 --phase final --short-child p8s2 --jq $Jq 1> .\.claude\runtime\p8k2-driver.stdout.log 2> .\.claude\runtime\p8k2-driver.stderr.log
python -I -S -B .\.claude\runtime\p08-joined-verified-replay.py --run p8k3 --jq $Jq 1> .\.claude\runtime\p8k3-driver.stdout.log 2> .\.claude\runtime\p8k3-driver.stderr.log
python -I -S -B .\.claude\runtime\p08-joined-verified-replay.py --run p8k4 --jq $Jq 1> .\.claude\runtime\p8k4-driver.stdout.log 2> .\.claude\runtime\p8k4-driver.stderr.log
```

p8k1 first ran c4/c5/c6/c10 and c22: all five commands exit zero. p8k2's first
final attempt then **FAILED** at c2: c1 passed, but one original five-second
trailing-flag deadline assertion failed; remaining groups were not run and no
short p8s2 child was created. It is not a passing final run.

A single fresh p8k3 diagnostic ran the unchanged complete cycle-footer wrapper
with Bash xtrace. All five literal `timeout 5` cases (`--mode`, `--here`,
`--next`, `--state`, `--awaiting`) returned zero; no assertion/deadline/product
was changed. Its wrapper exit is zero, 54.141 seconds. The later final run also
passes c2. The p8k2 deadline failure remains recorded and its cause is not
assigned from these reruns; do not erase it as a presumed environment issue.

**p8k4 is the completed final joined run**, all 22 commands exit zero:

| Slot | Actual group under `tests\` | Outcome | Seconds |
| --- | --- | --- | --- |
| c1 | `unit\intent-operation-boundary.sh` | 574 assertions, zero failures; original 183 and exact 21-case matrix retained | 44.594 |
| c2 | `unit\cycle-footer.sh` | ALL PASS, 46 PASS lines; original deadlines unchanged | 38.860 |
| c3 | `unit\cycle-continuity.sh` | ALL PASS, 19 PASS lines in verified synthetic paths | 25.766 |
| c4 | `unit\context-repository-ownership.sh` | All eight preservation groups pass, including plain/unborn/detached cases | 57.000 |
| c5 | `unit\context-checkpoint-roundtrip.sh` | Seven PASS lines; real writes, latest and counts pass | 10.078 |
| c6 | `unit\context-safety.py`, listed deep 26 selectors | 26 tests OK, 13.731 unittest seconds | 14.141 |
| c7 | `unit\jobs-steps.sh` | ALL PASS, 28 PASS lines | 25.953 |
| c8 | `unit\jobs-registry-concurrency.sh` | Four preservation cases pass | 10.984 |
| c9 | `unit\jobs-system-present.sh` | All tests pass, 38 PASS lines | 12.109 |
| c10 | `unit\memory-v2.sh` | ALL PASS, 16 PASS lines, including actual checkpoint path/list | 16.890 |
| c11 | `unit\work-artifacts.sh` | Existing mappings and refusal cases pass | 0.469 |
| c12 | `integration\enterprise-workflow-snippets.sh` | Four cases pass | 14.906 |
| c13 | `unit\review-source-target.sh` | Three source/target/history cases pass | 8.797 |
| c14 | `integration\swarm-workflow.sh` | 56 Python tests and shell round trips pass | 83.047 |
| c15 | `shape\claude-home-paths.sh` | 19 structural assertions pass | 4.328 |
| c16 | `shape\audit-writes-via-helper.sh` | Two structural assertions pass; not A13 event acceptance | 12.047 |
| c17 | `shape\handoff-cap-wired.sh` | 17 structural assertions pass | 1.093 |
| c18 | `shape\discover-scans-all-agent-categories.sh` | Two structural assertions pass | 0.156 |
| c19 | `unit\orientator-mechanical-routing.sh` | All eight unchanged scenarios pass | 2.828 |
| c20 | `integration\universal-work-lifecycle.sh` | All 39 tests OK, 630.617 unittest seconds | 630.985 |
| c21 | `integration\observation-learning.sh` | Both tests OK, 3.535 unittest seconds; advisory scope only | 3.766 |
| c22 | `unit\context-safety.py`, listed short two selectors | Both tests OK, 0.215 unittest seconds | 0.625 |

Thus the original preservation selection is 17 unchanged wrapper invocations plus
the completed 26+2 split for context safety, not a passing invocation of all 18
original wrappers unchanged. c1 is the same recorded routing result used for the
final root/lifecycle selection; it is not run or counted twice. All groups share
one sealed unchanged `2f4cc38` source. There are no unittest skips, unresolved
cleanup errors or zero-run substitutions. This remains a targeted joined
selection, not the repository's full suite.

### Original dimensions, operation identities and isolation

For both affected p8k1 and final p8k4, every c4/c5/c6/c10 preflight field matches
both p8r5 and p8r6: **16/16 lengths and 16/16 values after only run-ID substitution**.
No existing root/default path was shortened or store relocated.

| Effective field | c4/c5/c6 | c10 |
| --- | --- | --- |
| HOME / USERPROFILE | 140 / 140 | 141 / 141 |
| LINTEL_HOME / packs / pointer / profile | 148 / 154 / 166 / 161 | 149 / 155 / 167 / 162 |
| global_audit / hook_audit / resolver_audit | 154 / 159 / 154 | 155 / 159 / 155 |
| jobs / registry / canonical_registry | 158 / 164 / 164 | 158 / 165 / 165 |
| state / sessions / repo / source | 159 / 162 / 137 / 137 | 159 / 162 / 137 / 137 |

Unchanged checkpoint wrappers c4/c5/c10 use diagnostic Bash `-x` to retain actual
producer paths. Conversion uses the platform `cygpath`, followed by accepted
pure `native_paths.path_identity`, not a custom drive parser or shorter alias.

| Actual operation | Logical native length | Observed extended spelling length | New outcome |
| --- | --- | --- | --- |
| c4 ownership checkpoint | 268 | 272 | Reservation and downstream ownership/history cases pass |
| c5 mywork checkpoint | 278 | 282 | Reservation, write, discovery and count pass |
| c5 later checkpoint | 277 | 281 | Reservation, write and newest selection pass |
| c10 memory checkpoint | 290 | 294 | Canonical path, read and merged list pass |
| c6 owned bisect ref-lock | 283, asserted by unchanged method | Not substituted for the logical assertion | Actual success/error recipe and caller HEAD/index/config/content/mode preservation pass |

The four checkpoint paths remain inside their original synthetic temporary
roots. The four-character Windows extended I/O prefix is same-location spelling,
not a changed logical identity. Earlier measurement diagnostics were retained:
direct Windows subprocess lookup did not find `cygpath`, and a naive containment
comparison rejected its extended namespace. The final measurement consumes the
accepted identity helper; it does not reinterpret those failed diagnostics as
product failures or change either location.

Final p8k4 has 22 sixteen-field effective preflights and 23 actual Git
plain-refusal/initialized-child proofs (two for c22). The actual Python home and
temp are separately recorded. All regular commands use only the deep synthetic
root; c22 explicitly names the deep and authorized short roots. Its effective
ceiling is `<worktree>\.claude\runtime\p8k4;<session-files>\p8s4` on Windows.
The exact roots, marker and nonoverlap proof are retained in c22's preflight;
only that owned child is cleaned. No common home ancestor is a ceiling or
authorized output root.

### Seals, EOL identities and retained evidence

Before report authoring, all **960 original and 960 copied source files** were
rechecked against the final manifest and matched. Each invocation also checks
both seals after its command. The product remained clean at `2f4cc38`; only this
report is subsequently appended. Working EOLs were not normalized.

| Joined source | Tested raw SHA-256 | Working EOL |
| --- | --- | --- |
| `tests\integration\universal-work-lifecycle.py` | `4ad0aec4c0835e7ebc19863469e76a11555ee2f5e3cc610a484df86181e9d129` | CRLF, 956 lines |
| `lib\context_safety.py` | `79d951a5de7bd283be4df196ae87b9c12b3ed2d8bdec3319095c1c396bd55bf8` | CRLF, 524 lines |
| `lib\native_paths.py` | `c0c464b4b3be82ec3436968ed2e30b974d714fc8dc5b642d28749cb77c5d2076` | CRLF, 58 lines |
| `lib\profile_context.py` | `b13fdfac4e1b65134c61f68ea31a799640672dbeffd334590f03182159669ee1` | CRLF, 1,124 lines |
| `bin\li-snapshot.py` | `7d5aaaca1a18777d7503b05d7900053c95ec41b0df378dc5b7b778694b564ebd` | LF, 481 lines |
| `tests\unit\context-safety.py` | `2ec19989e027700c459864fccc2af770571d1c2921489cfde2f3b9ba56aba26b` | CRLF, 665 lines |

The four root raw hashes/EOLs remain exactly those in the previous append.
Reviewed runtime Git blobs are context `b3d3f6e761ae0fa4562ccd992e1a5e7d3bc464c6`,
native `835098264c9228f74caae5c63f1ded1430dbd94d`, profile
`ce7ac27636b1d9f987408559b99f221414b01900`, snapshot
`9a4f5c67216e26eec4aa357b55a1ef33108fa906`. Git clean-conversion blobs and raw
CRLF working bytes remain distinct evidence, not a mistaken LF-versus-raw match.

The original and new private drivers are retained, not committed product files:

| Driver under `.claude\runtime\` | SHA-256 |
| --- | --- |
| `p08-isolated-run.py` (unchanged) | `5e6f6b786838efccbf9529128931d821ae61247b5d7cf770575fcc64b661441c` |
| `p08-joined-run.py` (unchanged) | `3e18fe5ffbc4e297a1e5378b82b1b9805ac514ef17939d50c1067b6715d38e70` |
| `p08-joined-verified-run.py` | `7d2f5f0c79a634510c00631c9ba990336ea1cd347261279679adc66f743a13de` |
| `p08-joined-verified-replay.py` | `ec46774157547c28862b2cd5ebdb1c69d113b3fbe144f3fe3208316cda36ce3c` |

p8j1 and all p8k runs share source-manifest SHA-256
`b7010a40835f7e8d3f7590082e5defd4b43922339370ba85fda67ee225324db6`,
identifying clean committed `2f4cc38` and the same 960 raw source files. This
equality does not turn the interrupted p8j1 into completed preservation evidence.
p8j0 predates the fixture commit and identifies `1bb75560` plus its exact fixture
WIP, manifest `2464a342c36628ae9cc7c357b839bfb1e4215cf4e6616e4a381c829b0148cbf2`.

| Run | Results SHA-256 | Controller / interpretation |
| --- | --- | --- |
| p8j0 | `a75d0ef79536de23cf311cf5d7146428d62f9a8a630058422a480db0c6a35645` | Exit 0, one focused fixture method |
| p8j1 | `a137bbc69ea2868df7f40ca4c3a329bd2a09425a8142943fffad765f9325beed` | Interrupted; first 18: 16 pass / two fail; c19 passed, c20 interrupted/c21 unrun |
| p8k1 | `00e72f766f7bd993c9d1ea1ebfdc908333e65a541f68bce4c028d0fd9216d4eb` | Saved controller exit 0; five affected commands |
| p8k2 | `5902588d823e413ae2831d3955096be80eef85be5d6d30b72d1b65809b4ed4e2` | Saved exit 1; final attempt stopped after c2 deadline failure |
| p8k3 | `8c0fcbbd6b6f3cd86f17b4985072c32cf8fbd9e248f372cca490921064c14fd6` | Saved exit 0; one unchanged deadline diagnostic |
| p8k4 | `4ca68d2fb330e7728fe1540e3c7aefc6e0e5111baa8f58aeb2e8f1ab8e84990b` | Saved exit 0; final 22-command joined selection |

All four corrected outer exits are retained in
`.claude\runtime\<run>-driver.exit.txt`; stdout/stderr are the adjacent explicitly
redirected logs. Per-command receipts include exact argv/env/preflight hash,
stdout/stderr hashes, durations, counts, source seals and cleanup observations.
Final `complete.json` SHA-256 is
`14f584411c53090309fc928b96578d76d833d485ab5871c7d81228f6add3faa9`;
the enumerated final partition is
`f45aae20254bb56ce76c2ddec814575083bebb12eea77b96aace0267f969e3e9`.

Ordered preflight-set hashes below bind UTF-8 lines `cN <preflight-sha256>`, sorted
by numeric N and LF-terminated, without altering individual records:

| Run | Count | Ordered set SHA-256 |
| --- | --- | --- |
| p8k1 | 5 | `2189dfcb1698e5458e5b5af8559af396705e56322c29fe3dd61c19b9a6ca3471` |
| p8k2 | 2 | `732ce488546461f66b6c5090fb684f64a979a4d738678b1a8c64b348a30df4ce` |
| p8k3 | 1 | `d7426166a6355e6e1bf355bd7684932956c5eb0eb2602d6741aa1b2dcff106bb` |
| p8k4 | 22 | `63fa813c73ea451cb05c5b7300fe16d532331e476db5af63d7a41bf12d4886be` |

### Handoff disposition and remaining limits

A08.5.j1 is built with exact dependency-versus-fixture attribution; A08.5.j2
has the final local joined evidence above. The earlier checkpoint-reservation
and Git-ref-lock operations now have passing same-dimension observations on the
combined tree, but only the independent reviewer may close the relevant
preservation/selected-subset requirements.

F01/F02/F03 remain OPEN until same reviewer
`9dbf0a9b-750d-45c2-968c-41a5acb11c92` rechecks the full classes and completes
selected A08/A10/advisory-A13.3 SPEC. QUALITY remains NOT STARTED. The lifecycle
case-sensitive existing pair again reported False: the case-distinct positive
host branch remains UNVERIFIED. Denied identity remains injected error evidence,
not a native ACL claim. Synthetic analysis prose remains INCOMPLETE; no live
model/question/client behavior, semantic identity/carryforward or actual Python
3.9 execution is established. Passing shape and observation fixtures do not
release A13.1/.2/.4 or prove a new producer interface.

shell27 stays INVALID with UNKNOWN real-home effects. p8r2 cleanup/unlink errors,
p8r5 FAILED 14/18 and context-safety 22/23, p8r6 control attribution, all earlier
root RED/failed runs, p8j1 interruption and p8k2 deadline failure retain their
original meaning. No successful rerun retroactively clears them. The uninspected
host-managed AppData capture remains untouched; no actual-home recovery is
claimed. The only session-files access was the specifically authorized owned
short synthetic children and their exact-root cleanup.

**Dated prevention, retained only here:** verify Git discovery boundaries as well
as filesystem containment for an uninitialized fixture inside a real checkout;
keep absolute-size probes separate from long-default-consumer tests when their
unchanged padding requirements cannot share the same parent; redirect outer
runner output before launch and retain the real exit independently. Shared
lessons/ADRs remain coordinator-owned.

No product change followed `2f4cc38`. Only this report is appended and committed,
with the full old prefix preserved. No reset/amend, P10 import, production/global
setting, network/GitHub operation, hook activation, new agent, generated
reconciliation, A13 learning/promotion change or unauthorized cleanup occurred.
The final report-only SHA, parent, old-prefix proof and clean tracked state are
verified in the coordinator handoff. The combined checkpoint is frozen for that
single same-reviewer selected-subset review, not whole-P08 or all-A01-A26 approval.

## F04/F05 request-boundary correction and remaining-proof protocol

### Authority, independent disposition and immutable source

This append implements only the correction authorized by
`39641b2427502271f7480b2f19a5bc3af7c959a4`, after reading its complete P08
request-boundary section and the complete 495-line independent report
`402fd80417c2aa08193a460045168d8bb562793d` (parent `8eb00b87`, preserved by the
coordinator as `26c80a0`). That report is selected SPEC FAIL: two P2 mechanisms,
F04/F05; QUALITY NOT STARTED. Its valid c20 closes the bounded F03
caller/history-spelling defect. It does not close the original semantic or
host-interaction requirements. Invalid q02 contributes no acceptance evidence.

Frozen product: `70469296f97e8292c6bcf7a0c2b324cc47fcfcf1`, sole parent
`8eb00b87a9a14da7eaf949d11f1d1b444e9ecd8d`; commit message
`fix(routing): preserve independent request boundaries`. The exact product diff
is three existing files, 346 insertions/57 deletions. The following modes are
unchanged. Raw working files and Git blobs have LF only, with zero CRLF lines:

| Owned path | Mode; bytes; LF lines | Git blob | Raw/LF SHA-256 |
|---|---|---|---|
| `lib/orientator-routing.sh` | 100644; 18853; 455 | `20776f2a69608acde1f99cfd95b34a153f964365` | `1dfcf1a5cc4909fcaa7fb81d9f56a53a37f50e3f1929938ecebb7ef274f6085f` |
| `tests/unit/intent-operation-boundary.sh` | 100755; 24087; 468 | `d492df61a03afc659eab4c9006d73c9e4b70fe4f` | `f408c1cf531a7f509b0479ff95335e64f527fe8ecd873eba770e4760e6eb0ad4` |
| `tests/unit/orientator-mechanical-routing.sh` | 100644; 7075; 132 | `b69156bcc7ce2572bf6ddf271d886ee282c088e9` | `6868c12127ec185cb70adb557b656e805ced5b0459b0a15112c4bb06dca3c6e3` |

The workflow guard, PLAN, lifecycle fixture and reviewed core were not edited:
`lib/workflow.sh` = `27afa35ef9a826da57c8972b94d959bec60fbd9f`;
`skills/plan/SKILL.md` = `9597eba668bc7825429df6e39b25ff11926bf53f`;
`tests/integration/universal-work-lifecycle.py` = `d1ab38cedc7f3dad10b07a7a9f553e6d13bda766`;
`lib/context_safety.py` = `b3d3f6e761ae0fa4562ccd992e1a5e7d3bc464c6`;
`lib/native_paths.py` = `835098264c9228f74caae5c63f1ded1430dbd94d`;
`lib/profile_context.py` = `ce7ac27636b1d9f987408559b99f221414b01900`;
`lib/markdown_source.py` = `0b3da55046358864fcd3075ba5bfb6c2348b1fec`;
`bin/li-snapshot.py` = `9a4f5c67216e26eec4aa357b55a1ef33108fa906`.
Byte/EOL checks confirm these remain equal to the starting tree. The complete
three-path diff also excludes P03/P07/P10/A13, generators, plan/work metadata,
shared ledgers, memory, promotion and the provider. No dependency was imported.

The report prefix before this append is the entire report at `8eb00b87`:
1,456 lines, Git-LF 98,842 bytes and raw-CRLF 100,298 bytes. Its Git blob is
`77fe107c05c025c90acc8954944e7d4ceaaeb1b9`; LF SHA-256
`470eba9b777f2f0ba585d0c9971cff374562fbca28a956e3004e01105cb56e09`;
raw SHA-256 `830874abf45cf888d2ea27a48dd74a1b0e805f617430c3b0fab806de1c907d2d`.
Both full-prefix representations are retained, not just the earlier 805 lines.
The report-only child and its complete hashes are returned separately so this
append does not make a self-referential commit/hash claim.

### Mechanism and retained contrasts

F04 came from a smaller independent-conjunction head list than the initial
request vocabulary; F05 came from erasing LF before resolving request scope.
The repair uses one maintained `_orientator_request_head` recognizer for initial
and independent positions. Its internal metadata describes the existing intent,
head position/width, explicit request wrapper, embedded scope, question scope and
bounded nominal use. Public enums/signatures are unchanged. Parsing that metadata
uses command-local IFS rather than changing the caller's IFS.

Conjunction resolution asks that recognizer whether a supported new governing
head exists. Positively evidenced shared-object coordination stays within the
first operation; an independent request, even one with the same intent enum,
remains ambiguous. Nominal metadata is owned by the same vocabulary, not another
shorter conjunction list. A write-head noun that could already be the completed
first object cannot alone establish shared-object scope. LF remains structural
until `_orientator_line_boundary` distinguishes independent requests from
wrappers, operands and read/prohibition complements. This is neither all-and/
all-newline rejection nor a growing list of exceptional input sentences.

The exact independent rows 22/24/26 now clarify:
`Build the service and edit its configuration.`,
`Deploy the release and change the deployment manifest.`,
`Build the service and provision the environment.` The actual LF in row 28,
`Deploy the release\nReview the release plan.`, remains an independent boundary.
The exact original 33-case matrix is retained in order with independently stated
expectations, including all original 21 inputs.

Positive controls retain `Implement audit and review logging.`,
`Build test and release fixtures.`, `Review build and release scripts.`,
`Please\nreview the fix\nwithout changing code`,
`Explain how to build and\ndeploy the service.` and quoted/code subjects.
Bare-object contrasts such as `Deploy release and review config.` clarify rather
than hiding the second head. Established direct/polite affirmative, read,
Swedish, symptom-triage, negation, assessment and IFS checks are retained.
The 61 explicitly declared supported-head fixtures exercise direct, and/or,
semicolon, LF/CRLF, wrapped, embedded-read/assessment and prohibition contexts.
Expected results are manually specified, not derived from router output.

The default routing producer returns the existing low-confidence unclear
recommendation for compounds. The real consumer test additionally verifies a
non-default pack route: `unclear|/team:delivery|low|yes`, where the last field
requires clarification. Wrapped review remains `review|/li:review`.
Neither producer nor consumer probes execute a returned workflow. Passing this
bounded structural matrix is not unrestricted natural-language understanding.

### Executed RED/repair/freeze sequence

All runs used the same immutable private driver
`.claude/runtime/p08-boundary-run.py`, SHA-256
`b7dbcdbd1e8efc5eedeb0eb2ff57a8b4cc8dde1a1110ba46dfc1333ca51979f6`.
It reuses unchanged private isolation helpers
`p08-joined-verified-run.py` =
`7d2f5f0c79a634510c00631c9ba990336ea1cd347261279679adc66f743a13de`
and `p08-isolated-run.py` =
`5e6f6b786838efccbf9529128931d821ae61247b5d7cf770575fcc64b661441c`.
Only the previously approved read-only jq tool was used, SHA-256
`a6fc67fedaf9128a3309a1e2ebb8b986aeccf70122ee46d2cb4849e423f0c627`.

| Evidence ID | c1 full assertion outcome | Exact 33-case matrix | c19 eight retained scenarios plus added consumers | c23 syntax | Actual outer exit |
|---|---|---|---|---|---|
| p8m1, unmodified router plus new tests | 3,757; 1,278 failures | 81 assertions; 10 failures | Original eight pass; added consumers fail | pass | 1 |
| p8m2, first shared-head/LF repair | 3,757; 0 failures | 81; 0 failures | pass | pass | 0 |
| p8m3, strengthened bare-object contrasts | 3,799; 6 failures | 81; 0 failures | pass | pass | 1 |
| p8m4, bounded nominal-scope correction | 3,811; 0 failures | 81; 0 failures | pass | pass | 0 |
| p8m5, exact clean committed 70469296 | 3,811; 0 failures | 81; 0 failures | pass | pass | 0 |

Every row retains passing original 183 and cumulative 574 assertions; these are
subsets, not extra counts to add to the final total. The p8m3 six failures are
intent/workflow/confidence for exactly `Deploy release and review config.` and
`Deploy release or review config.`; they remain failed intermediate evidence.
The final contrast expansion also covers Build/Implement bare first objects.
No source was edited during an active run. The ordered 960-file raw identity
arrays for p8m4/p8m5 are equal; their manifest hashes differ because the recorded
HEAD changed at the product commit. Final c1/c19/c23 durations were 337.64,
3.734 and 0.156 seconds. Process receipts before/after record zero remaining
owned Python/Bash/Git test processes.

Every c1/c19/c23 invocation has a separately recorded preflight before its test.
The 16 effective values are HOME, USERPROFILE, LINTEL_HOME, packs, pointer,
profile, global_audit, hook_audit, resolver_audit, jobs, registry,
canonical_registry, state, sessions, repo and source. They all resolve inside
that run's synthetic root. HOME/USERPROFILE, AppData/XDG/temp, Lintel/Gstack and
Python-effective home/temp are recorded along with argv, cwd and environment.
Trusted source/test copies are under `<run>\s`; c1/c19/c23 use their own `h` and
`t` children. Defaults were not shortened or relocated to evade a check.

Each fixture process uses exact `GIT_CEILING_DIRECTORIES=<workspace>\.claude\runtime\<run>`,
not a personal-home ancestor. Real Git rejects its plain uninitialized fixture
with exit 128 and recognizes its initialized child with exit 0 (init also 0).
Global/system config, hooks, signing, fsmonitor, credentials and templates remain
isolated/disabled as recorded. Source-checkout Git verification has no fixture
ceiling. All three groups in all five runs seal 960 original/copied files
unchanged and report empty temporary children; retained evidence/source copies
are intentional, not a claimed cleanup failure.

Exact final outer invocation, from this workspace, with output redirected before
launch and the actual exit saved independently:

```powershell
& 'C:\Users\jokerman\AppData\Local\Programs\Python\Python311\python.exe' -I -S -B .\.claude\runtime\p08-boundary-run.py --run p8m5 --expected-head 70469296f97e8292c6bcf7a0c2b324cc47fcfcf1 --jq 'C:\Users\jokerman\reference-repos\copilot-worktrees\jokerman-session-setup\jokerman-microsoft-fictional-broccoli\.claude\runtime\tools\jq-1.8.2\jq.exe' 1> .\.claude\runtime\p8m5-driver.stdout.log 2> .\.claude\runtime\p8m5-driver.stderr.log
$actualExit = $LASTEXITCODE
Set-Content -LiteralPath .\.claude\runtime\p8m5-driver.exit.txt -Value $actualExit -Encoding ascii
Get-Content -LiteralPath .\.claude\runtime\p8m5-driver.stdout.log -Tail 4
if ($actualExit -ne 0) {
    Get-Content -LiteralPath .\.claude\runtime\p8m5-driver.stderr.log -Tail 8
}
exit $actualExit
```

p8m1-p8m4 used that same invocation with their own run/log names and
`--expected-head 8eb00b87a9a14da7eaf949d11f1d1b444e9ecd8d`.
Child c1/c19 run `bash --noprofile --norc` on the respective unchanged-in-run
copied test; c23 uses Bash syntax checks on the three owned shell files.
Exact command/environment/preflight/result receipts and stdout/stderr are
retained under `.claude/runtime/<run>/c{1,19,23}/`. There is no runner-to-tail
pipeline and no unpreflighted outer-process product Python invocation.

| Run | Source manifest SHA-256 | Ordered preflight-set SHA-256 | Results SHA-256 |
|---|---|---|---|
| p8m1 | `cbe7c53d888bda2438d49df1c8357a3221d7208240c67cf96dcf3e7315ecad71` | `2324a5f75930ad47f16db50fbe467faba21f094408241dfa3090dad879dfa811` | `d4371e2d69d999a4e55c9b6699aa8f3abb33fcd2125c52da0d5506a2b8a72e25` |
| p8m2 | `c7d36353e8e0bb46cca7e19dfa85675ab9425f9de49952702b918bca34e615f5` | `5f9d29a6b077c777e0dca2979cb2adea98cbcdc0d3f884d21613b586c11c4568` | `88573891ea5ef5caedaea58bde40e17e6f2491a127c93e7213129202e5429998` |
| p8m3 | `09e36415e5cd63b6ec7aaf2c99bd0ebe46a6a10e974b23a8361f8b9e2c7b2e9b` | `309aba9f14c8749a9b7451dcc6ef4131baf34c2984202fbec5b3c812d2450611` | `55d30ef87342388e0925612b51155cd1c97bc5526d08bb3d8299d6b13defd3ad` |
| p8m4 | `7650d1183bc73b26ad49ea5a0c771b282222fcff922af253743505500741b677` | `0839484253ea1793d019d0e80fd80aa7c442a02a4543606b268e513bbefa65e1` | `f29987326c2204fa18c5b3a28fedf0cb2c4d0995aa542bfdb0f767854df0726f` |
| p8m5 | `e1c458ec06db5cce42b97c7e339cd2e36f8d148c4b325133d5ac5294144c9d59` | `63356d4c470c6c1fc7b5f8bce4a5df0d9c2774991579e54013fd3c758b5431eb` | `7abad3fdbbbb4592e40d944159ab9308153804f06ddf32200af42c00870c0c5b` |

Each preflight-set hash covers UTF-8 LF lines `c1 <hash>`, `c19 <hash>`,
`c23 <hash>` in that order with a trailing LF. It is not a hash of reformatted
JSON. The read-only stdlib/Git verifier
`.claude/runtime/p08-boundary-evidence.py` =
`e7ce806b0a61cb42f9ec3cdf36402ce3f9a4d62bafaa958542a4389ed05eab31`
checks receipt/log/preflight hashes, containment, Git discovery, source seals,
guarded blobs and the full report prefix without importing/calling product
Python. Its product evidence JSON SHA-256 is
`2a47bf5333e660b211651e9a8549915a3d62b1dc68df55228a77cfc763fec80c`.
This metadata verification is not another behavioral test or semantic result.

### Proposed bounded original-proof execution, not yet released or run

The following is an evidence protocol for the original open leaves in the
independent report, not a second plan, task schema, scheduler or acceptance
authority. No actor/fixture execution described here has started. MasterSession
must explicitly release the exact disposable root, actor roles, write scopes and
question handling first. Current F04/F05 authority does not release it.

**Native actors and isolation.** MasterSession is the native CYCLE controller.
It uses actual discovered `li-*` skill activation, or the Copilot adapter's
explicit canonical-file execution when no wrapper exists. Available native
`lintel-planner` performs fixture DEFINE/DISCOVER/PLAN and semantic ANALYZE;
`lintel-builder` performs actual fixture BUILD/CAPTURE. A fresh `general-purpose`
actor executes RESUME using persisted fixture context only. All actors are
dispatched sequentially by the controller, not nested writers/reviewers. Record
the actually selected agent/tool IDs, not an assumed role inventory. No factory,
forced model, external model CLI or paid benchmark is needed. The existing
independent reviewer
`9dbf0a9b-750d-45c2-968c-41a5acb11c92` remains the review gate; authors cannot
declare their own work independently reviewed.

Proposed write root is this worktree's new
`.claude\runtime\p08-semantic-01\`, subject to an unused-path/ownership check at
release. Under it, `seed`, `targets`, `homes`, `tmp` and `evidence` have explicit
nonoverlapping ownership. The frozen 70469296 resource copy is read-only.
Fixture actors may write only their selected target and own evidence; the
controller alone writes dispatch receipts. The semantic analyzer writes only
its selected report, never corrective source. The same reviewer writes only
its review evidence. Shared Lintel source, current plan/memory/ADRs and earlier
evidence directories remain outside all write scopes.

Before every actor's product/tool invocation, reuse recorded synthetic
environment/16-effective-path/Git-discovery checks, with exact per-target
ceilings and isolated Python/home/temp paths. This includes inline helpers and
fresh resume actors, not just an outer controller. Missing/denied capability is
recorded as BLOCKED/UNVERIFIED; no permission bypass or fabricated actor result.
Seal source, siblings and initial fixture bytes, save actual tool transcripts,
argv/cwd/environment/exits and final diffs, and clean only explicitly verified
owned temporary roots after evidence capture. No network, hook activation,
global/private config, actual-home inspection or q02 replay is included.

**Fixture and independent oracles.** Use a tiny local Python label-list CLI with
real code/tests, no external dependencies, and two initiatives whose authoritative
Spec Kit tasks live at `specs\alpha\tasks.md` and `specs\beta\tasks.md`. Alpha
retains original IDs T011/T027/T042; Beta deliberately reuses T011 for different
work so selection cannot rely on globally unique task numbers. Use only the
existing work-map schema and actual verified P07 references/policies. Seed a
LOCKED source-order constraint, a contradictory sorting plan, and an advisory
documentation coverage finding. Seed stale unlinked global analysis text and
newer sibling history, but no correct selected report pointer, completed phase,
accepted finding or completion checkbox. A human/same-reviewer oracle specifies
expected contradictions and permitted work before the actors run; passing tests
alone cannot determine a semantic verdict.

| Original leaf | Actual actor/caller exercise | Required observable result |
|---|---|---|
| A08.2.c | Native controller executes CYCLE with an explicit selected SENSE-to-CAPTURE range and SHIP explicitly skipped under local-only authority; planning/build roles perform real work. Then a separate approved PLAN hop-in and an interrupted BUILD are resumed without replaying startup phases. | Host activation/task/tool-call receipts correlate to one persisted cycle identity established before the first phase. Each selected phase runs once per attempt; explicit retry is a new recorded attempt, not hidden duplicate SENSE/SCOPE. Ledger-only or prewritten receipt evidence is insufficient. Skipped SHIP is never executed/DONE. |
| A08.3.c | Actual PLAN calls semantic ANALYZE on Alpha's real DEFINE/spec/plan/task contents, then BUILD and CAPTURE consume its selected reports. A later real analysis examines implemented code/task status. | The initial producer identifies the LOCKED source-order/sorting contradiction and advisory coverage gap with original IDs and citations. The authorized fixture owner corrects the contradiction, not the analyzer. A real operator acceptance of the advisory finding is persisted with date/identity, re-emitted on same-work reanalysis, and absent from Beta's unrelated report. Missing or contradictory build/task evidence is not a green verdict. |
| A08.5 | Implement and test T011/T027 in the original external Alpha task file; preserve deferred/blocked T042. Exercise Beta selection, explicit loop-back, CAPTURE and a fresh actor given only persisted paths/authority. Deliberately pause after a real BUILD STARTING before completion. | Actual task-file diffs retain original IDs/parser structure and change only proven statuses. Same selected map/report/cycle survives cold resume and authorized `state_phase_begin --retry`; sibling hashes remain unchanged except during explicitly selected Beta work. STARTING is not reported done. Stale/global/newest history cannot supply Alpha's selection or acceptance. No native duplicate backlog appears. |
| A10.1 | A fixture request leaves one material duplicate-label behavior unresolved. The actor asks through the available native `functions.ask_user`; the operator answers, and the actor persists that answer. Resume the same request and then an explicitly changed requirement. | Raw host question/answer receipt and resulting design/task diff show the question is relevant, prior authorization/answers are reused without another approval interview, and only the newly unresolved decision is asked. No seeded answer or scripted question transcript counts. |
| A10.2 | Run semantic intake on fully specified maintenance, disposable local-data migration and repository-only research requests; contrast an otherwise identical request explicitly opting into the venture lens. | Actual questions/artifacts show proportional maintenance/migration/research treatment and no inferred founder/venture interview. The opt-in lens remains useful and scoped rather than removed. Research performs no BUILD/SHIP or unintended task/source mutation. |
| A10.3 | Execute the finite paired entry cases below from equivalent sealed fixture inputs, using canonical callers and existing review/latest-reader semantics. | Compare original map/task IDs, selected artifact roles, applicable policy/approval and phase outcomes, not prose timestamps or a second alias verdict. Unavailable required review stays blocked. Different fixture roots have separately verified references; do not falsely equate their context hashes. |

For A10.1, the material fixture question is whether duplicate labels are retained
or deduplicated while preserving first-occurrence order. Both answers are in
local scope; the operator's actual response, not a preselected recommendation,
determines implementation/tests. If a native child cannot ask, it returns that
exact unresolved question and stops; the controller's real `functions.ask_user`
request/answer is recorded as an explicit host bridge and relayed unchanged.
If the bridge is denied/unavailable, leave the case blocked rather than using
another model, invented answer or ordinary progress text as question evidence.
Acceptance of the advisory finding also needs actual scoped operator authority;
it cannot waive mandatory P05 obligations or mark T042 done.

The finite A10.3 paired cases are:

| Alternate entry | Canonical comparison and scope-specific oracle |
|---|---|
| `autoplan --map <approved Alpha>` | Direct mapped PLAN: reuse existing design/approval, same original artifacts, no repeated DEFINE interview or duplicate PLAN review. |
| New-work `autoplan` / `office-hours` composition | DEFINE/DISCOVER/PLAN on the same maintenance input: office-hours remains DRAFT until canonical scoped approval; optional venture review occurs only on explicit opt-in. |
| `plan-and-build` | CYCLE PLAN-to-BUILD with the same approved map: no extra SENSE or alias state; actual code/tests/original-task edits and required package reviews are retained. |
| `review-and-ship` | CYCLE REVIEW-to-CAPTURE: use actual same-reviewer/P05 writer evidence and latest-reader/QA interpretation. Both stop identically at missing publication authority or a later rejecting review; no fake shipment or CAPTURE-after-failed-SHIP completion. |
| `fix` | Canonical bounded hotfix on the same local bug: actual minimal mapped original tasks, authorization and status; no new independent backlog or implied release. |
| `research` | Canonical research-dive on the same local question: substantive sourced answer, no implementation, no founder interview, and only scoped research artifacts. |
| Standalone `plan-eng-review`, `plan-devex-review`, opt-in `plan-ceo-review`, and `handoff-size-check` | Compare the applicable canonical PLAN lenses on the same artifact inputs: preserve useful methods, original IDs, real reports/budget result and PLAN-owned approval; no alias-created acceptance. No UI scope is invented. |

The controller may reuse an actual semantic report only when its exact selected
context and the existing latest-reader permit reuse; fixture filenames or
synthetic INCOMPLETE prose do not establish reuse. Wrong-map, policy drift or
missing-context negative checks compare no-write before/after seals and explicit
refusal. When a later real rejecting review is written, both delivery entries
must revoke readiness through the existing P05 reader, not a copied CLEAR string.

A complete explicitly selected nonpublication cycle is useful positive
orchestration evidence, not an all-nine-phase shipment. This protocol authorizes
no remote publication, deployment, credential or global change. Positive
post-publication behavior remains unverified if required; the truthful SHIP
gate cannot be bypassed merely to finish a fixture. The same reviewer decides
which original rows the actual receipts close. Stop the affected case on a new
product defect or unavailable host capability and return the exact finding for
scope; do not repair excluded source or create a new harness subsystem.

### Limits, prevention and frozen handoff

This correction has builder-only self-review and the actual mechanical evidence
above. F04/F05 are locally corrected candidates awaiting the same reviewer's
disposition, not self-closed independent findings. Original A08.2.c/.3.c/.5 and
A10.1-.3 semantic/host rows remain UNVERIFIED; selected SPEC is not complete and
QUALITY remains NOT STARTED. No proposed actor/protocol case is counted as run.
The earlier valid c20/26+2 split/preservation results remain usable only at their
recorded source/evidence level; this routing-only run does not newly execute them.

shell27 remains INVALID with UNKNOWN real-home effects; p8r2 cleanup/unlink
history, p8r5 FAILED 14/18 and context-safety 22/23, p8r6 attribution, previous
root RED results, incomplete p8j1 and p8k2 deadline failure remain unchanged.
q02 remains INVALID/UNKNOWN with zero acceptance evidence and was not replayed.
Existing case-sensitive native-pair execution, a real ACL denial and actual
Python 3.9 remain unobserved. A13.1/.2/.4 are still gated; A13.3 is advisory only.
No audit/memory/promotion interface, schema, dormant effect or enforcement was
activated.

During a large read-only repository-inspection output in this continuation,
the host reported an AppData capture ending
`1790069239364-copilot-tool-output-25384-0184cec4-7f90-4b71-99e9-a52ac992f56f.txt`.
It remains uninspected host-managed output, not a product/test invocation or
proof either of test HOME escape or of no external effect. No inspection,
cleanup or rollback of that capture or any actual-home state was performed.
Subsequent runner output was redirected in-repo and summaries were bounded.

**Prevention retained only in this owned report:** derive initial and independent
head recognition from one governing rule, preserve structural boundaries until
scope resolves, and require positive evidence before treating a second supported
head as an object modifier. Retain RED counterexamples and vary supported heads
and separators rather than adding phrase exceptions. Shared lessons remain
coordinator-owned.

The source is frozen at 70469296; only this append follows it. The complete
1,456-line old report prefix, product blobs/modes, report-only parent/scope and
clean final tree are checked for the immutable handoff. No reset/amend, new agent,
dependency import, hook activation, network/GitHub action, push/main mutation or
unreleased semantic execution occurred. The coordinator can send this one frozen
tree to the same reviewer and separately release the bounded original-proof
protocol; neither action is represented here as already accepted or executed.
