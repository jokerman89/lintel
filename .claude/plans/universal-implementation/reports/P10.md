# P10 implementation evidence

**Status:** the F04 canonical-redirect correction and focused evidence are frozen.
Latest independent report `131d3db4b61cdbb1792169913710522a15ecfeb6` is **SPEC FAIL**
with P2 F04; the first whole-P10 **QUALITY review is NOT STARTED**.
That report closed bounded F03 and retained F01/F02's independent closures.
The external Git limitation remains documented and compatibility experiments remain
**STOPPED**. No independent F04 closure, P10 acceptance, integration or A13 release
is claimed. New verification below is builder evidence.

**Authoritative work:** `../work.json` selects `../plan.md`; original A12.1.a-d,
A12.2.a-b, A12.3.a-b and A12.4.a-b are retained. Requirement R01 and audited A12,
CP-02/04/12/16, RU-06/07/08/11 govern this package. ADR-0030 preserves native
installation without Python.

**Initial checkpoint:** `0df1042a0be45db932d319c9959addcca44e15ce`, verified clean.
**Original implementation commit:** `a73cf9ee38977501c201225d8269dac3668c1080`.
**Fixture-only follow-up:** `acf97f1ca47d115501613ca29e91d902300b9bf6`.
**Previous immutable product snapshot:** `a400c03d384be29879644ec11214f0eeadafe7df`,
including the three separately attributed coordinator repairs below.
**F01 product snapshot:** `4407827f563fecac2ac6bf04a6bd0fc9cdd05641`,
the two-path F01 repair, directly parented by rejected candidate
`62ffb9a072c3314b97e015e0f8f98b7f64459fc8`.
**Previous native-consumer snapshot:** `a31c5eb9fe5a910f2f3d27b6079c144313f80b6c`,
the five-path direct-consumer unit on reviewed dependency imports `bc0863a` / `8097ba2`.
**Previous Git-refusal snapshot:** `a559c9f5aefc62b3a2e6094e8c899d5f3ed55f70`,
the three-path Git refusal correction directly on report-only `f7d2151`; no new
product dependency is imported.
**F02 product snapshot:** `a797b2d8e357d319e549015f591bc85421535cf7`,
the four-path F02 correction directly on report-only `4c0519e3`. No new dependency
or reviewer product change is imported.
**F03 product snapshot:** `036e90330dd96fa27dd1b1574e7d39a12af2eaf2`,
the three-path F03 reader correction directly on report-only `7bf5a315`.
No coordinator recovery, dependency-preflight or other product files were imported.
**Current immutable product snapshot:** `6e1b1e0047befae8fc45fd0858d337b117e2ff97`,
the three-path F04 reader correction directly on report-only `50e654af`.
**Prior report-only checkpoints:** `1117b9b54a55105d7202f92141a670f7b0cff578`,
`62ffb9a072c3314b97e015e0f8f98b7f64459fc8` and
`c1de05c7f18770f34f950a8c4d4e194892da3291`, followed by
`f7d2151d9596377e1023c01ed99269b3cb0a999f` and
`4c0519e3ba3ba610d4db3f7f19e18a85531546d9` and
`7bf5a31526cc39951978945df86dea85796fd01b` and
`50e654afcf7fae0548e4d16a4bf2d52b725523bc`.
**Owned diff base:** `fb96f71342f7994c42bdfc7cd61e6d452e6acb54`.
**Branch:** `jokerman-microsoft-universal-installer-lifecycle`.
No other project worktree was modified. Product state was clean immediately after commit.

## Dependency attribution

These are separately attributed imports, not P10-authored product changes:

| Accepted source | Local import | Purpose |
|---|---|---|
| `98ad7edfaed7566848ecf26f2bbe563c0486e91d` | `0f29ff4490f564ce3e4d243963e895fc5fb05dd6` | Coordinator runtime preflight |
| `29ae5d9` | `56be89a` | Historical P05 rejection report only |
| `0ecdb520ddbfc628cc049860608484866986e3b9` | `7c0fedc` | Approved native/runtime split, ADR-0030 and package metadata |
| `ca280747b9fe8c16ea54598c4192f40567b8430e` | `1de01ef` | P03 postponed annotations, unchanged snapshot APIs/formats |
| `0eab731c381985e21ef5b5520d7f93f49d3a8216` | `d6a0732` | Owned Windows test-sandbox cleanup |
| `4d0014639204e5b5ad84838272a840c5283e28d0` | `f35f500acea35e23f168cf6896b1d9315acb6c11` | Independently accepted P07 Windows persistence |
| Accepted P05 history ending `33eac071a26f5eb931c8ca7ed220fee91ea4469b` | merge `fb96f71342f7994c42bdfc7cd61e6d452e6acb54` | Actual review contract/consumers |
| `d53dc38efa41a8ea562ca5078f36b0ce4010b712` | `dcf0448aa6bb3d5d94d958b56655eca5362d3c6e` | Coordinator P05 evidence CLI mode repair; content unchanged |
| `aa56a672b0db187db20201d969f990ab0093802a` | `7a8927788b31610a38263b8f83a295268806eb48` | Coordinator literal failure-diagnostic output and regression |
| `935b640c0038a28921f130b7e48c6262cf6ecfd5` | `a400c03d384be29879644ec11214f0eeadafe7df` | Coordinator actual welcome footer and ADR-0025 parity oracle |
| `ce7415f6342220e49746880dd7338a6e7d9294ed` | `bc0863a8c4d16bc07f2389ccc8599c583c004834` | Independently reviewed native-path core and assigned resource/fixture closure |
| `5fc657f14123daca9ec126cadc1e9d64ee60691c` | `8097ba2c7d45cdc09715abf57593512ea28d8199` | Reviewed P01 exact-root fixture cleanup follow-up only |

The shared Markdown provider and P05 bytes remain unchanged. P10 did not author any
P07 runtime/schema change: the later P07 representation wrappers arrive only through
the independently accepted core dependency. Its SPEC then QUALITY report is
`062d0f20f41f33db437324d2d9094ab418338e2c`; coordinator integration `1d44d2a` is
not imported as a wider product tree. No P08 WIP was imported. The common audit writer,
shared plans/memory, ADRs and generated reducers remain coordinator-owned.

Twelve complete dependency blobs equal the reviewed final `5fc657f` tree. Four
paths retain earlier P10 content plus the reviewed resource/cleanup additions:
`bin/li-copilot.py`, `tests/integration/copilot-kit.py`,
`tests/integration/pack-source-target-resolution.sh`, and
`tests/integration/universal-profile-context.py`. The only cherry-pick conflicts
were the first and third resource lists; both owners' entries were retained.
The exact F01 closure and all earlier refs/reports remain preserved.

## Immutable producer contracts

Hashes below are SHA-256 over the exact Git bytes, not working-tree line-ending
conversions, rechecked at `6e1b1e0047befae8fc45fd0858d337b117e2ff97`. Five of these
seven artifacts equal frozen `036e903` / `50e654af`; only the explicitly authorized
layout-observation function and its documentation change in F04. Their prior hashes were
`11bcaa9ac61a4352b4126a13c2f43c6f13de26121425341f4578141f783ad109` and
`1a460d4d93f4d1397c7d35ea4c5dde566fdbb1051d3ee0bd60426734b918ff34`.
The preceding native-consumer unit changed `lib/managed_transaction.py` from its F01
checkpoint hash `2594ec6a12a0e9c37affe80d4f8b60ecf8e45fe5a438e7096c5ed8cabea8f020`;
the current correction leaves that module and the F01 `migration_inventory` parser
unchanged. The parser's containing lifecycle file intentionally changes elsewhere.
Native receipts and runtime result schemas are unchanged. P08's A13 gate remains
closed; these artifacts identify the candidate, not acceptance or permission to invent
another interpretation.

| Artifact | SHA-256 |
|---|---|
| `docs/native-installation.md` | `e2cf6bf3417e10ad12112002e0719a581f1632b0db2881459cfaad20208e1ae0` |
| `docs/lifecycle.md` | `9f5b868728a03bb8ce7210701347af8871864eea6cca3cf63099d7f73b5b6070` |
| `install/directories.txt` | `c8e0e825586f309c43bbcb5dc6644eb270a233ee638c7244696193db8f52d7c3` |
| `install/native.sh` | `1425c05888b24b413a45ec33f70a98fe432f73774634197cae4cbdc9d1c9e187` |
| `install/native.ps1` | `57173b99e0c809d3fe45a6c9b894ddab0c07b3961a2d73b6fd61af25047483f7` |
| `lib/managed_transaction.py` | `acbf84ef94a9fc4afdb8f66766d376653549ee82f4cf063c6a5721b1fdc08cae` |
| `bin/li-lifecycle.py` | `99295ac651a07e6a8a59d03034534a731b8ff22fb9391e047e8794d6d2c621bf` |

The native contract is version-1 UTF-8/LF TSV: managed inventory; root-bound store
marker; immutable source/target/store/id/platform/inventory-before metadata; exact
before/after hash-size-mode plan; verified blobs; per-file progress; aggregate state.
Native installation never invokes Python or the shared audit writer. The Windows Bash
entry uses the explicitly selected native PowerShell performer, with no fallback after
denial. The POSIX performer remains Bash-compatible code over the same contract.

The runtime primitive receives an exact caller-owned file plan, not an inferred
inventory. It consumes P03 snapshot/result/restore formats unchanged. Lifecycle JSON
reports actual transaction identity, state, changed paths, store and the exact caller
profile reference; a new target's profile remains unbound. `operation-profile.json`
retains producer context beside a new runtime receipt. None of these artifacts grants
review clearance, corroborates an independent actor or proves host enforcement.

## Per-leaf evidence

| Original leaf | Implemented and preserved behavior | Discriminating evidence |
|---|---|---|
| A12.1.a | Scaffold/doctor/health dispatch to trusted helpers; explicit target is separate from source/data. Existing prose and foundation seeds survive. | `ScaffoldMigrationLifecycle.test_scaffold_preserves_user_prose_and_is_repeatable_in_separate_consumer`; late-link refusal; doctor smoke; actual bundled scaffold cases in the strict run |
| A12.1.b | Pack creation/list/validation/switch use accepted structured policy. Configured pointer changes the observed profile, with retained generations; required-policy errors do not become neutral activation. | `ProfileLifecycle.test_pack_switch_uses_configured_pointer_and_fresh_process_pin`, required/invalid refusal, missing/drifted pin recovery, default nonactivating validation and interruption cases |
| A12.1.c | Pack-relative role/persona roots, private read consent, full expert sections, atomic preference updates and reviewed-digest role writes. No private sync or persistent audience overlay. | Role metadata/body/privacy/update tests; exact comment/config preservation; persona source/no-write hashes |
| A12.1.d | Layout/scaffold/extension changes use owned transactions. Knowledge and nested/hidden runtime collisions fail before writes; marker publishes last; Git index is untouched. | Migration collision, hidden runtime, malformed settings, pointer-only repair and actual injected-interruption/recovery cases |
| A12.2.a | Actual fresh and repeated native installs plus repository adapters in separate fixtures; installed-source consumers work without the originating checkout. | Complete native matrix, existing `check-install.ps1`, actual adapter fresh-clone/idempotence and installed-consumer tests |
| A12.2.b | Local managed edits refuse the entire update. Unmanaged files inside managed directories, config, profiles, neutral customizations, roles, hooks and brand assets survive. Obsolete removal requires old ownership. | Native conflict/update/extra-file tests, runtime/brand slot assertions, existing Copilot and Universal protected-file tests |
| A12.3.a | Durable intent and incomplete state remain visible. No completion prose or automatic rollback after a failed write; incomplete operations block re-init. | Native Windows entry-point interruption cases; actual adapter interruption; migration interruption plus doctor diagnosis; terminal-label coherence controls |
| A12.3.b | Explicit restore validates root/store/plan/blobs/current bytes. User edits and foreign/corrupt receipts refuse before changes; restored permission cannot be replayed. | Native corruption/foreign/replay cases and `ManagedTransaction` exact create/update/delete, failure, foreign store, stale expected state and post-recovery postimage cases |
| A12.4.a | Host toggles are explicitly unsupported rather than marker-based. Hook links/cache files are not activation evidence. Uninstall is bounded file recovery, not whole-root deletion. | Unsupported-host no-write hashes; source verifier link-only claim regression; doctor cache/hook-byte cases; native/runtime contract limits |
| A12.4.b | Historical aliases, snapshots and redirect stubs remain accessible. Overdue rows stay visible; missing/malformed/unknown metadata is not completion. | Migration inventory tests listed below; unchanged historical aliases/backup refusal behavior; versioned recovery documentation |

### Caller versus target policy

`ScaffoldMigrationLifecycle` exercises neutral caller/new child, required caller,
missing/drifted caller pin, conflicting target requirement, matching verified policy,
same name/different source-content refusal, missing target-required policy, unchanged
parent/home bytes and no child generation. Required caller policy is an operation
constraint, not a render hint; no private pack or target policy declaration is copied.

### CP-16 migration assertions

`ProfileLifecycle.test_migrations_use_source_catalog_and_keep_overdue_unknown_states`
checks the actual installed-source catalog against a poisoned target catalog, retains
overdue rows, observes `needs_migration`, preserves unknown detectors, includes archived
rows only with `--all`, excludes fenced fake rows and verifies no-write hashes.
`test_missing_migration_catalog_is_unknown_not_no_pending_work` requires nonzero
`MIGRATION_CATALOG_MISSING`, no success output and unchanged bytes.
`test_malformed_migration_metadata_is_not_silently_dropped` rejects an invalid date and
a missing active table instead of dropping them into an empty result.
Those original controls missed truncated rows: independent F01 reproduced that gap.
The new `MigrationInventory` methods and exact RED/GREEN evidence below address it
without treating the original coverage as sufficient or the repair as accepted.

## Commands and actual outcomes

Commands were run locally with disposable HOME/USERPROFILE/data/store roots and explicit
trusted source. Windows native verification used the specifically authorized existing
PowerShell 7 executable via per-process `LINTEL_POWERSHELL`; no policy was changed.

| Command/scenario | Actual result |
|---|---|
| `bash tests/runner/run-all.sh --require-all` | **120/128 passed; 8 failed; 0 skipped; 0 partial.** One aggregate run only. Frozen during execution. |
| `bash tests/integration/universal-lifecycle.sh` after repairs | **46/46 passed**, 466.031 seconds; full owned matrix, not the whole repository suite |
| `python tests/unit/managed-transaction.py --root .` | **7/7 passed**, including committed-product rerun |
| `pwsh -NoProfile -File tests/runner/check-install.ps1` | Passed actual temporary-root install/reinstall, assets/customizations and linked-home refusal |
| `pwsh -NoProfile -File tests/unit/native-receipt.ps1` | Passed exact native record/payload replacement and stale-expected refusal |
| `bash tests/behavior/install-target-boundaries.sh` | Passed overlap/source alias/linked destination refusal |
| `bash tests/behavior/install-fail-closed.sh` | Passed actual injected publication failure and recovery/replay contract |
| `bash tests/integration/pack-source-target-resolution.sh` | Passed installed source versus target versus configured store and actual skill dispatch |
| `bash tests/integration/enterprise-workflow-snippets.sh` | Passed preserved scope/work-map/resume/build assertions and actual pack dispatcher |
| `bash tests/shape/extension-pack-contract.sh` | Passed retained extension fields, actual resolver validation and hostile-description JSON case |
| `bash tests/unit/li-doctor-smoke.sh` | Passed bounded exits/sections/unknown flag behavior in an isolated home |
| `bash tests/shape/claude-home-paths.sh` | Passed canonical path and legacy-detection contracts |
| `bash tests/unit/v37-closeout-additions-present.sh` | Passed after pointing the old inline-installer presence oracle at the shared directory-slot contract |
| `bash tests/shape/skill-descriptions-trigger.sh` | Passed after scaffold trigger-form correction |
| Three bounded native Bash performer cases (`--native-small --native-performer bash`) | Passed on available Git Bash; not evidence of stock Bash 3.2/POSIX execution |
| Actual `CopilotKit.test_interrupted_adapter_publication_requires_explicit_owned_recovery` | Passed, including the unchanged long consumer target after compact full-root-bound default-store repair |
| P05 source-closure/installed control cases and unchanged P07 installed-default-home case | **3/3 passed**, 224.775 seconds; mandatory fail/unverified controls refuse |
| Committed-product runtime unit + default validation/malformed migration/neutral caller-child selectors | **7 + 3 passed** |
| Python `ast.parse(..., feature_version=(3, 9))`, Bash parsing, `git diff --check` | Passed; grammar is explicitly not Python 3.9 execution |

The strict-run log is retained in this session's noncommitted `files/p10-full-suite.log`.
The runner's then-existing `printf %b` interpreted Windows backslashes in failure details;
focused raw reruns established the exact failures rather than treating truncated text
as evidence. P10 did not author a runner change. The later coordinator fix `aa56a67`
was imported separately and passed its literal-output regression; the old log and failed
aggregate remain historical evidence, not a rewritten successful run.

### Authorized fixture follow-up, 2026-09-21

Coordinator `9a4d177ace2529f87f9157ea651b21e77515fd5e` authorized exactly the
copied-source dependency list and
`ProfileLifecycle.test_validation_skill_executes_shared_contract_without_activation`
in `tests/integration/universal-profile-context.py`. Commit `acf97f1` changes only those
two spans. An AST comparison confirmed that every other method, including the actual
long-path regressions, and the module imports are unchanged.

**RED:** the unchanged fixture failed with `AssertionError: 1 != 3` against the real
one-block skill. **GREEN:** the repaired method executes that exact Bash block through
the copied source-owned `li-lifecycle` entry, not a mock or a replacement parser. It
parses the actual JSON effective values and schema/product compatibility result.
It verifies implicit/explicit selection, `host_activation: "not performed"`, no initial
pin or selected-reference creation, and unchanged source/target/profile/host-home hashes.
After a separate explicit binding through the real producer, it verifies the exact
existing reference, rejects same-mtime required-policy drift and missing pins, and
does not recreate or rebind them. Malformed required policy fails for implicit selection,
explicit `strict` and explicit `_default`; a supplied name cannot bypass policy.
No product defect surfaced and no product repair was made.

The actual method and these seven unchanged preservation methods passed in one runner:

```text
python tests\integration\universal-profile-context.py --root . --bash <git-bash>
  ProfileLifecycle.test_validation_skill_executes_shared_contract_without_activation
  ProfileLifecycle.test_neutral_first_use_and_unbound_boundary
  ProfileLifecycle.test_repository_requirement_cannot_be_replaced_by_global_or_explicit_selection
  ProfileLifecycle.test_missing_invalid_and_incompatible_required_pack_never_become_neutral
  ProfileLifecycle.test_same_mtime_manifest_parent_pointer_and_deleted_parent_are_drift
  ProfileLifecycle.test_resumed_reference_rejects_later_generation_and_missing_pin
  ProfileLifecycle.test_explicit_roots_and_target_data_are_preserved
  ProfileLifecycle.test_default_home_long_runtime_keeps_fresh_shells_history_rebind_and_drift
```

**8/8 passed, 67.084 seconds**, with isolated synthetic HOME/USERPROFILE and LINTEL
roots. The updated method itself asserts root containment. At committed snapshot
`a400c03`, the actual method passed again (**1/1, 12.438 seconds**) along with all five
focused dependency checks: `tests/unit/test-runner-contract.sh`,
`tests/unit/cohort6-fas3-skills-present.sh`, `tests/shape/cycle-footer-present.sh`,
`tests/unit/session-protocol-sync.sh`, and `tests/shape/bin-scripts-executable.sh`.
The P07 runtime, profile schemas and shared Markdown provider bytes were also verified
identical to the original product checkpoint.

These are targeted results only. No second full suite, native install, PowerShell
policy change, dependency installation or P08 import occurred in this follow-up.

### Authorized F01 repair, 2026-09-21

The complete immutable rejection
`1d871338dcdd1b6a535e4b546c2b2d0a7aea031b` was read before edits. Coordinator
`def5d8db848b64cb1a18b597103806f1a5b383ac` authorized only the existing
`migration_inventory` parser, focused lifecycle tests and this report. The starting
branch was clean at `62ffb9a072c3314b97e015e0f8f98b7f64459fc8`; no dependencies were
imported. Product commit `4407827f563fecac2ac6bf04a6bd0fc9cdd05641` changes exactly:

| Path | Exact delta |
|---|---|
| `bin/li-lifecycle.py` | 12 additions / 4 deletions, all inside `migration_inventory` |
| `tests/integration/universal-lifecycle.py` | 218 additions: focused `MigrationInventory` fixture and seven methods; existing methods unchanged |

**RED first:** both complete reviewer catalogs were copied byte-for-byte, with LF
and no BOM, and executed through the unchanged real migrations skill block and copied
trusted dispatcher. The valid 192-byte catalog
(`4ca4751d30a521d13c18f0608d255960563c28a0de770882e37dfccbb737b7b0`)
returned exit 0 and one overdue/unknown obligation. The truncated 161-byte catalog
(`35adc26d85f0221cfb57f7de68e9823d4fbc371ba6992fc6dfeb8144b4a431c7`)
incorrectly returned exit 0, empty `migrations` and empty stderr. The probe itself
returned **3**, not GREEN. Both invocations preserved exact before/after hashes and
complete path/mode maps.

The new seven-method group then ran against the unchanged parser: **25 failed
assertions/subtests, 0 errors, 44 actual subprocess invocations, 97.610 seconds,
exit 1**. This included malformed active/archive row shapes and archive outcome text
lost at embedded pipes. Legitimate placeholders, the current catalog and retained
missing-catalog/section/date failures already passed.

**Repair:** validate five nonempty active fields or four nonempty archived fields,
removing only the single outer delimiters so empty metadata cannot disappear. Ignore
only complete recognized headers, complete separator rows and canonical placeholders.
Split according to the applicable table width, preserving pipes in the final prose
field. Reject malformed applicable rows explicitly with `MIGRATION_CATALOG_INVALID`
through the existing exit-2 error path, before any success JSON. Unrequested archive
rows remain omitted; `--all` validates them. Date semantics, source selection, the
shared Markdown provider, detector behavior and result shape are unchanged.

**GREEN:** the seven new methods plus six unchanged preservation methods passed in
one focused invocation: **13/13, 159.812 seconds, 52 actual subprocess invocations,
exit 0**. Exact selectors, executed through the session-only safety runner:

```text
python -I -B <session-files>\p10_f01_checks.py --root <checkout> --bash <git-bash> -v
  MigrationInventory
  ProfileLifecycle.test_migrations_use_source_catalog_and_keep_overdue_unknown_states
  ProfileLifecycle.test_missing_migration_catalog_is_unknown_not_no_pending_work
  ProfileLifecycle.test_malformed_migration_metadata_is_not_silently_dropped
  ProfileLifecycle.test_shell_lifecycle_preserves_configured_paths_across_native_python_boundary
  ProfileLifecycle.test_doctor_reports_bytes_not_hook_counts_or_historic_host_activity
  ProfileLifecycle.test_unsupported_host_profile_is_not_a_marker_file_success
```

The exact two-catalog probe ran again through that same runner with
`--probe <session-files>\p10-f01-green-probe.json`: **2 real invocations, exit 0**.
The valid result remained overdue/unknown; the malformed catalog now returned
**exit 2, empty stdout**, and
`MIGRATION_CATALOG_INVALID: active migrations row requires 5 nonempty fields: 'overdue-work'`.
Both retained identical before/after file hashes and complete path/mode maps.

The new methods also preserve valid overdue/open/unknown records, actual legacy-layout
detection, `--all` archive selection, descriptions/outcomes containing pipes, aligned
separators, legitimate empty inventories, missing-catalog/active-section/date failures
and fenced/quoted fake-row exclusion. Poisoned caller/target catalogs cannot replace
the selected source. Every new real-skill invocation verifies source, target, caller,
installed-home and synthetic personal-home bytes and paths/modes unchanged, including
no profile/pin/runtime directory creation.

The runner clears ambient selectors and verifies per-process synthetic HOME,
USERPROFILE, application/config/temp roots, source/repo/data/packs/pointer/private-role/
recovery/audit/job roots and derived profile/context/runtime paths before invocation.
Its Python audit hook checks subprocess environments/cwds; it does not replace
dispatch, parser results or subprocess execution. Actual runtimes were Python 3.11.9
and existing Windows Git Bash. No native installer or PowerShell performer was invoked
for F01, and fixture subprocesses did not use a global home. Fixtures use ordinary
disposable cleanup; no D03 workaround was added.

AST comparison verified that only the authorized product function and new test class
differ from `62ffb9a`; all original methods/imports remain unchanged. Python compilation,
Python-3.9 grammar parsing and `git diff --check` passed. Grammar parsing is not
execution on Python 3.9. Committed-tree checks confirmed the exact parent, two-path
scope, clean state and six unchanged producer hashes above. The unchanged Markdown
provider blob is `0b3da55046358864fcd3075ba5bfb6c2348b1fec`; P07, P05, native,
transaction, dispatch, schema and coordinator-owned bytes were not changed.

Raw evidence remains in the noncommitted `files\` store of builder session
`5ea6c88c-68c1-4712-8f55-adecdfe0061f`. Existing failed logs were not overwritten:

| Artifact | SHA-256 |
|---|---|
| `p10-f01-red-probe.log` | `28bf7ecbfc8d05cae9988a47082489b73ae31f4a5f230edd1b2935c0e7cbcb5f` |
| `p10-f01-red-probe.json` | `a08554b64c0038a47b45dfdffaa61bf498af4b63f030a852fc24487e94017008` |
| `p10-f01-red-tests.log` | `67c43696219be4378de1935f12c07a134a060cf797ca1cefcedf1f9439f8251b` |
| `p10-f01-green-focused.log` | `d652b653f832d40a6320cb5cbb06e09f10bf48400104819c4be2f78419125a6d` |
| `p10-f01-green-probe.log` | `918c41f87ee9d8862752b8b5ef778fb2c12dd3d13bf93cec3679d0db9a44c66e` |
| `p10-f01-green-probe.json` | `659c15c6c1cc2607995eb0bf4a28daa87ddbe486260e4da8b9d55800ccb93116` |
| `p10_f01_checks.py` | `8f01e8f666bae327d5aba007ab84a458fe3760c107ae8316c394077e7688fdf5` |

**Limits:** these are builder checks for one P2 repair, not independent SPEC or QUALITY
clearance. D01 default-store failure and D03 cleanup ERROR remain distinct
blocked/unverified observations, untouched and not rerun under F01. No shorter default,
P03/P07 repair, long-path setting change, cleanup workaround, new dependency or shared
parser was introduced. The historical **120/128, 8 failed** aggregate remains failed;
no second full suite was run. The same reviewer must repeat complete SPEC before the
first whole-P10 QUALITY review. No integration, self-closure or A13 release follows.

### Reviewed native-path dependency and direct-consumer unit

**Authority:** complete P10 card at `2180b551c91705748885b3f3c452b2cf1b5803b6`,
operand-only completion `6407b00c4470a4dfdb65a958c2b0fffe1e7474c6`, and the
separately conditional Git diagnostics `c921dd104fec9fcaff8206c3e91d505971ed3f56`,
`f7eeb1758f5d20e6103603ee451ebf35fabd06a6`,
`e775f2006f87200024784a27e58e94d0d57a4c8e` and
`86c8df09c1fd950ae5b919da33861a174b4f0753`. The amended card and ADR-0031 were
read before each dependent action. These coordinator revisions supplied metadata,
not additional product imports. After the final failed Git diagnostic, MasterSession
explicitly stopped that seam and authorized freezing the independent consumer unit
with the open boundary.

The initial checkout was clean at `c1de05c7`. The attributed dependency chain is
`c1de05c7 -> bc0863a -> 8097ba2 -> a31c5eb`. No earlier commit was amended and no
other worktree, generated output, shared report or shared runtime was modified.

#### Exact owned delta and retained contracts

| Path in `8097ba2..a31c5eb` | Additions/deletions | Change |
|---|---:|---|
| `bin/li-copilot.py` | 67 / 39 | Trusted native helper loading; native I/O operands in the released adapter functions, with logical selection/diagnostics retained |
| `lib/managed_transaction.py` | 19 / 18 | Native metadata, iteration, directory/lock creation, deletion and recovery-presence operands only |
| `tests/integration/copilot-kit.py` | 560 / 0 | New exact-dimension/canonical/metadata controls and the explicitly open Git diagnostic fixtures; original methods unchanged |
| `tests/integration/universal-lifecycle.py` | 23 / 3 | Copy the real required native helper and register verified exact-root, same-location cleanup |
| `tests/unit/managed-transaction.py` | 109 / 2 | Two real deep native-I/O/refusal/recovery methods and exact-root cleanup; original seven method bodies unchanged |

AST comparison verifies that every original test method remains identical. Production
definitions changed only in the released function sets; default-store selection,
receipt validation, schema/inventory constants, navigation parsing and protocol
decisions were unchanged. No Git argument, option, environment redirect or runtime
retry/fallback was added in that native-consumer unit: only its existing `.git`
metadata operand gained native I/O. At `a31c5eb`, the helper still used the old generic
ignore-conflict error, and init still bypassed it when the literal rule was absent.
The later authorized correction below fixes those behaviors, not Git compatibility.

Native aliases are passed only at I/O boundaries. Iterated native names are projected
back onto the original logical root. Default stores, owner/source strings, profile
references, inventory keys, byte/mode hashes and recovery permission stay logical.
The native installer and its TSV contract remain byte-identical; Python is still
absent from its exercised install/check PATH.

| Exact source at native-consumer checkpoint `a31c5eb` | Git-byte SHA-256 |
|---|---|
| `bin/li-copilot.py` | `00bb708a0f573af57a94d72d725561b335d3b8f6f6edc5a45155403e5c07130e` |
| `lib/native_paths.py` | `b4b8f541c91c40563f9cabfab433d3216e3d58e1167b357141b008e4769a90ed` |
| `lib/context_safety.py` | `e80cf6ac5b8edd0e97f865c05486da3c8a39ddb56c83e42eed09c04dd7d7176e` |
| `bin/li-snapshot.py` | `7d5aaaca1a18777d7503b05d7900053c95ec41b0df378dc5b7b778694b564ebd` |
| `lib/profile_context.py` | `353ba79537736ab8d2acb8da06fbb7bd731c19d697b02fbdd5702a73712291a1` |
| `tests/integration/copilot-kit.py` | `ad37be608269e030274f6bb9d16c7dd88302cdf3c0f3542d8d26d45d8250f81b` |
| `tests/integration/universal-lifecycle.py` | `634d5bd4569ac5970caca7faa262e1d180c5ad41efea5ba94c3f515724f7deec` |
| `tests/unit/managed-transaction.py` | `5a16fddeb5499307ab065c4c6362199bcc54f20acba243795b2503dc25776f6a` |

The four core runtime hashes above equal the reviewed final `5fc657f` bytes, not
the earlier diagnostic snapshot overlay beginning `071767`. P10 made no further
P03/P07 edit. F01, the Markdown provider, profile/review schemas, audit writer and
native performers remain frozen.

#### Discriminating REDs and preserved failed runs

All runs used real dispatch/readers and sealed disposable source exports. No returned
subprocess result was substituted. Injected interruptions in retained tests still
perform the actual write before raising the explicitly labelled synthetic exception.

| Evidence label (`files\<label>.log/.json`) | Actual outcome before its correction |
|---|---|
| `p10-native-red-transaction` | 2 methods: 1 ERROR + 1 failure in the original long-store `mkdir`; exact deep roots retained |
| `p10-native-red-adapter-io` | Actual classic atomic replacement at temporary256/destination263 raises WinError 3; missing-native/hostile-PYTHONPATH refusal passes |
| `p10-native-red-lifecycle-resource` | Actual copied lifecycle dispatcher refuses its missing `lib/native_paths.py`; no substitute dependency is loaded |
| `p10-native-red-classic` | Final-core/P10 composition at TEMP79/base109/source116/target182: first two methods pass; installed/removal method fails five assertions because existing long source metadata is reported missing |
| `p10-native-red-metadata` | Four methods: lost long-root user prose, false missing generated guide, omitted public directory index and silently skipped negative ignore validation |
| `p10-native-green-installed` | Despite its filename, **FAILED**: 7 methods, 6 pass + 1 test-oracle ERROR, 975.537 seconds. Product wrote the exact263 destination; the new test then read it with ordinary `Path.read_bytes`. Only that same-path fixture I/O operand was corrected; length and byte assertions stay identical. |
| `p10-native-adapter-preservation` | **FAILED**: 15 methods, 14 pass + 1 overstrict new fixture assertion, 1090.742 seconds. The actual missing-pin bootstrap appended its existing failure audit, while the test incorrectly required every caller byte to stay unchanged. |

The final-core classic metadata failure is distinct from the independently observed
old core-only classic writer failure and from the direct atomic256->263 probe.
None of these historical failures is relabelled by later checks.

The canonical audit assertion now follows the unchanged accepted producer:
`pack-resolver.sh::_profile_cli` calls `_resolver_audit fail` for a failed bootstrap.
For missing/drifted pins only, the new fixture requires exactly one append-only
`pack_resolver_fail` record with `msg=operation=bootstrap profile-context-unresolved`.
Existing audit bytes/mode are preserved; every other caller file, the complete
default home/pin/history, source and synthetic personal home remain unchanged.
No product/audit/P07 code was changed to silence an error. All pre-existing test
assertions remain intact.

#### Actual default, canonical and preservation checks

Final commands use the session-only driver with `--git-bytes`, whose source is
exported from immutable `a31c5eb` blobs and SHA-verified before and after execution:

```text
python -I -B <files>\p10_native_checks.py --root <checkout> --git-bytes
  --label <new-evidence-label> --script tests\integration\copilot-kit.py
  --temp-length 79 -v <exact selectors>
```

The required seven-case aggregate uses these selectors, without changing their
fixture/target names or selecting a shorter store:

```text
CopilotKit.test_native_publication_keeps_the_original_263_character_destination
CopilotKit.test_missing_native_helper_cannot_fall_back_to_target_pythonpath
CopilotKit.test_swarm_wrapper_and_complete_source_resource_inventory
CopilotKit.test_joined_runtime_dependencies_refuse_incomplete_source_before_writes
CopilotKit.test_joined_installed_dependencies_cannot_be_hidden_by_inventory_removal
CopilotKit.test_original_default_113_128_paths_init_check_and_owned_recovery
CopilotKit.test_canonical_default_caller_child_keeps_verified_parent_and_unbound_child
```

Its final frozen rerun is recorded separately as `p10-native-frozen-seven`:
**7/7 PASS, 633.193 seconds, 29 real subprocesses, exit 0**, with unchanged source
seal and complete cleanup. The earlier seven-case failure remains FAILED.

| Other selected run | Actual outcome |
|---|---|
| `p10-native-frozen-transaction`, `tests/unit/managed-transaction.py -v` | **9/9**, 28.032 seconds, exit 0; all original seven plus deep create/update/delete, lock/incomplete/foreign/user-edit/replay and logical-receipt assertions |
| `p10-native-frozen-validation`, unchanged `ProfileLifecycle.test_validation_skill_executes_shared_contract_without_activation` | **1/1**, 12.925 seconds, 15 real subprocesses, exit 0 with complete cleanup |
| `p10-native-frozen-policy-metadata`, required canonical caller plus the three Python metadata methods | **4/4**, 160.348 seconds, 9 subprocesses, exit 0 |
| `p10-native-lifecycle-preservation`, `ProfileLifecycle ScaffoldMigrationLifecycle MigrationInventory -v` | **43/43**, 347.410 seconds, 143 subprocesses; same final lifecycle/managed/core code in checkout representation, before the unrelated adapter metadata completion |
| `p10-native-floor-preservation`, native no-Python install/check and corrupt/foreign-receipt methods | **2/2**, 129.531 seconds, 12 subprocesses; complete source, explicitly approved existing PS7, no reduced native fixture |
| `p10-native-required-caller`, corrected canonical required-policy method | **1/1**, 136.896 seconds; then rerun on raw committed bytes in the four-case run above |

The 14 passing methods in the failed 15-case preservation run include exact263
publication, the three Python metadata methods, managed-edit/protocol/collision
preservation, malformed/traversing inventory, real symlink refusal, actual interrupted
adapter publication with explicit recovery, CRLF/autocrlf clone and source update.
Their adapter code is byte-identical to final `a31c5eb`; other executed core/runtime
files are either exact final Git bytes or their verified CRLF checkout representation.
This is individual evidence, not a synthesized 15/15 aggregate.

The default consumer uses fixture94, target113 and the unmodified default sibling
store128, with no `--store` or recovery-store environment override. A read-only Python
audit hook observes the actual snapshot replacement operands, confirming218->260.
Init, installed-source check, repeat init, exact owned recovery and replay refusal
preserve the original seeds and unrelated bytes. Records keep the original logical
source/owner/store, not I/O aliases.

Canonical cases clear ambient selectors, source the actual bundled `copilot-env.sh`,
bootstrap the caller, and invoke the real bundled `li-scaffold --target child` with
its inherited verified reference. Default home and retained304-character history
remain unchanged; child profile is unbound with no generation, copied pack or implicit
policy declaration. Required caller, neutral target, matching verified configured
policy, conflicting target, missing pin and same-mtime required-policy drift are
exercised. The original repository-local/same-name-different-content and all other
policy controls also remain in the 43-method preservation run.

The added metadata cases exercise real long-root prose/EOL/hash handling, an actual
public directory-index read, real generated-link existence and missing/link-refusal
checks. They do not replace navigation parsing or the shared provider.

#### Stopped Git boundary: no conditional product variant was released

Actual executable: `C:\Program Files\Git\bin\git.EXE`, version
`2.55.0.windows.3`. The target remains256 and `.git` entry261. Logical roots are
retained in records; no global/local persistent setting, environment redirect,
`--git-dir`/`--work-tree`, shorter target or runtime fallback was used.

| Separate retained evidence | Actual result and boundary |
|---|---|
| `p10-native-git-boundary` | After Python `.git` metadata recognition, original read-only `check-ignore` runs but returns128 for both valid/negated rules: error reading long `.git`. This is not exit1 negation evidence. |
| `p10-native-git-option-diagnostic` | Sole per-command `core.longpaths=true` still gives128/128 at the same root. Short controls distinguish0/1; malformed HEAD gives128. HEAD/index/config and staged/unstaged/untracked bytes/modes are preserved. Conditional product-option gate fails. |
| `p10-native-git-operand-gate` | Ordinary linked-worktree setup fails128 on the long controller's derived admin directory before any native-C comparison. |
| `p10-native-git-owned-controller` | Separately authorized fresh controller118/admin221 layout preserves consumer256/.git261, but ordinary setup fails128 creating the consumer `.git`. No native-C comparison was executed in that run. |
| `p10-native-git-two-form-gate` | Actual directory filesystem samefile/dev+ino identity checks pass. Native-C `rev-parse` fails128, with and without the one permitted option, before Git-selected identities or0/1 can be verified. Independently attempted setup with the expressly authorized controller-only option fails128 (`$GIT_DIR too big`); controller config is unchanged. |

The first two setup failures and the third flagged setup failure are distinct
from both the directory diagnostic and the Python failures. The directory attempt
was not suppressed by linked setup failure. No native-C variant was inferred to
pass from a setup action or string normalization.

**Compatibility experiments remain STOPPED.** At `a31c5eb`,
`CopilotKit.test_long_git_metadata_runs_real_effective_ignore_validation` and
`CopilotKit.test_native_git_operand_directory_and_linked_worktree_gate` were
RED/open diagnostic evidence, not passing acceptance. Their failed sources and logs
remain immutable. No option/native-C change entered `runtime_ignore_errors`.
The later operator decision `34b84a8` replaces only those two new experimental oracles
with fail-closed contract tests; it does not authorize another compatibility or long
linked-worktree setup attempt. The unsupported long linked positive remains
**UNVERIFIED**. Complete SPEC, first whole QUALITY, A13 and integration remain open.

#### Source seals and evidence retention

Every invocation reconstructs an allowlisted synthetic environment. HOME/USERPROFILE,
application/XDG/cache/temp roots, source/consumer/data/packs/pointer/private roles,
audit/jobs/registries and derived profile/runtime paths are checked before real
subprocess execution. Startup-free Bash observes21 actual effective paths through
the frozen path/resolver/jobs readers; Python confirms its actual home/temp.
Preflight-created audit directories stay inside the fixture. Ambient profile/Git
redirects and shell startup injection are not adopted. The hostile-PYTHONPATH test
deliberately points only to its own disposable target.

Preliminary runs use sealed checkout-byte exports; their metadata retains both
the base `8097ba2`, the exact working-diff SHA, every exported file SHA and its
Git-mode/object identity. They are not claimed as executions of an unmodified
`8097ba2` tree.
The final frozen runs use raw Git blobs from `a31c5eb`, verified against Git object
IDs and sealed again after execution. No executing source was edited.

Raw logs and JSON records remain under the builder session
`5ea6c88c-68c1-4712-8f55-adecdfe0061f\files`. Each labelled run above has both
`.log` and `.json`; JSON records expanded argv, synthetic roots, source manifest,
exit and cleanup. `p10_native_checks_checkout.py` preserves the exact preliminary
driver (SHA-256 `b0a1ab519c62ecd926a9eb735f7d38a9a96bdfc3404ef8743eb506bdda69f79e`).
`p10_native_checks.py` is the final raw-Git-capable driver, SHA-256
`20fd93419905399213195f58c23d1e7f83701a37032cef263a18481bf1f9ab3d`.
Earlier logs were not overwritten, and every completed fixture's exact-root cleanup
returned successfully.

`p10-native-evidence-index.json` binds all22 completed runs, their actual exits,
commands, source-manifest digests and both log/JSON hashes. Its SHA-256 is
`3386d3c52b73231611f7662867706e6b9e1b4f5a7b81b447479d9911717ece8e`.
Key immutable metadata seals are:

| Evidence JSON | SHA-256 |
|---|---|
| `p10-native-red-classic.json` | `0fd336aa1c3899c3e1795d0f8f7cafb36e2d2f3734c0751a356733afdeddb83d` |
| `p10-native-green-installed.json` (failed seven-case run) | `54ad2ed4d0ec4ce986d6514562380208033d2e359f7d337ba9816f90bfd4abec` |
| `p10-native-adapter-preservation.json` (failed fifteen-case run) | `68a5149e9aae7fb14acdf55ec5d4d19d4d593ca86354796c4d620c36c0c77674` |
| `p10-native-frozen-seven.json` | `589c2d794c65a53b99f28a63add6ac1047fcb3ce113cd9ee3ec475ad1e982368` |
| `p10-native-frozen-transaction.json` | `8edbb8f830a1965ecc76a9a5fc6ac1e8088512d7a66a3bab12cdda6a220abf5c` |
| `p10-native-frozen-validation.json` | `7681b9ba0c2e8f4d71fc68beadb37208a86b1d61ff27a1255e4b52bc1d014187` |
| `p10-native-frozen-policy-metadata.json` | `2babeb84fd77b7907fadcbbdf0b3bf974740f693594fcba21efa0aa46d8422e0` |
| `p10-native-git-boundary.json` | `b9eab82cbdbc2b44293198455eaf6c655f636f8e9f95da5097fe9f1de1213a1f` |
| `p10-native-git-option-diagnostic.json` | `9eabf7f4f804345f9257c17dfea960453091c45fd9d1d8d500d285b58c01b397` |
| `p10-native-git-operand-gate.json` | `7bb850dfd6199baf5800473294ad6f872337119ccb26da2f21b886eeeef6105b` |
| `p10-native-git-owned-controller.json` | `5a70635512f74cb048ef233ece8fa38a0c73616ea9d4fe06639ee1d7a474026b` |
| `p10-native-git-two-form-gate.json` | `efda6c2a85c9dc645a76ed17e216667a55ad61a2adb164837c41964d076a71e2` |

This is scoped builder evidence only. Historical120/128 remains **FAILED**; no
second full suite, Git-boundary waiver, new dependency installation, P08 import,
policy mutation or host-support claim occurred.

For that checkpoint, builder self-review rechecked report hashes against all22 immutable run records,
the four raw-Git passing groups against `a31c5eb`, and unchanged final-core/F01/native
identities. Current Python compilation, Python-3.9 grammar and `git diff --check`
passed; grammar is not3.9 runtime execution. Report-only `f7d2151` froze that evidence
before the separately authorized correction below. No independent SPEC/QUALITY verdict
is supplied by that self-check.

### Operator-approved pre-write Git refusal

**Authority:** `34b84a880875870e4f9b4c4ffdb01acc3f9c1e42`, the complete P10 section
"Operator decision: refuse unverifiable Git operations" and ADR-0031 addendum,
read before the correction. The operator selected documenting the external limit,
refusing safely and continuing the remaining fixes. This is not a historical-pass
waiver. No new dependency, P03/P07/P08 change, store/schema/receipt/inventory change,
recovery-policy change, global setting or Git compatibility experiment was made.

Product `a559c9f5aefc62b3a2e6094e8c899d5f3ed55f70` is directly parented by
`f7d2151d9596377e1023c01ed99269b3cb0a999f`; both older checkpoints remain intact.

| Exact product delta | Additions/deletions | Scope |
|---|---:|---|
| `bin/li-copilot.py` | 23 / 13 | `runtime_ignore_errors` and its directly coupled init preflight only |
| `tests/integration/copilot-kit.py` | 246 / 174 | Replace the two new Git diagnostic methods and add four private fixture helpers |
| `docs/lifecycle.md` | 23 / 0 | Actual Git refusal semantics, observed operation limit and unverified long linked positive |

The helper now distinguishes Git exit1 non-ignore from unavailable verification,
launch errors and exits outside0/1. Failure diagnostics retain the logical target
and actual exit/error context. Init verifies Git even before repairing a missing
literal rule. A supported0/1 result still permits legitimate missing-rule repair;
check still reports a missing rule, and an existing effectively negated rule still
refuses. Plain folders without `.git` metadata still work without Git.
Inspect/recover keep their independent existing ownership checks.

The complete actual subprocess invocation is unchanged:

```text
<actual git> -c core.fsmonitor=false -C <logical target>
  check-ignore --no-index --quiet .claude/runtime/.lintel-ignore-check
```

There is no platform-specific option, native-C/cwd redirect, numeric cutoff, retry
chain or substitute command. AST comparison verifies the entire subprocess call,
all29 original `a73` test methods, every other `f7d2151` test/class/module member,
and the exact single authorized init-preflight edit outside the helper. The remaining
repository tree is unchanged. The four reviewed core runtime hashes above still
match the final accepted dependency; its12 complete blobs and four retained composed
resource/fixture paths have the same attribution as before this correction.

#### RED, actual producer outcomes and final source

The new RED ran the real tests against unchanged product before the fix:
**2 methods, 20 assertion failures, 1320.528 seconds, exit1**. Fresh init observed
Git128 at target256/`.git`261 yet returned0 and changed1086 paths. Other actual128
results were mislabeled ignore conflicts; missing Git also allowed installed
init/check false success. This RED is distinct from every prior Python, atomic,
Git-option/native-C, setup and failed-aggregate observation.

The new fixture independently runs the unchanged real Git command before comparing
the actual adapter. On Git `2.55.0.windows.3` at
`C:\Program Files\Git\bin\git.EXE`, the frozen run recorded34 operations:

| Actual observation | Corresponding adapter evidence |
|---|---|
| Eight long directory invocations at target256/`.git`261 returned128: absent rule, valid rule, negation and invalid HEAD, each with init/check | All refused nonzero with actual128/stderr context, no success stdout and exact before/after state equality |
| Supported directory118/`.git`123 and linked-worktree128/`.git`133/admin152 controls | Real Git-selected root/admin identities; ordinary unmodified worktree setup; valid0 and non-ignore1 semantics preserved |
| Four short invalid-HEAD invocations returned128 | Explicit verification error, not an ignore-conflict diagnosis |
| Eight required-Git invocations with a genuinely empty synthetic PATH | Explicit unavailable-Git refusal with and without a literal rule; no mocked process result |
| Two plain-folder operations with the same absent executable | Fresh init and installed-source check succeeded without requiring Git |

Across all34 operations, **26 refusals preserved every path, byte and mode and emitted
no success stdout**; eight supported initialization/check operations succeeded.
The observed producer counts were0:4, 1:8, 128:12 and unavailable:10. Complete state
hashes include source, caller, home, controller metadata, target, default store,
inventory/receipts, HEAD/index/config and staged/unstaged/untracked seeds. Valid
initialization changes are confined to the target and its original default store.
Later short controls dispatch the actual installed bundled adapter.

The test derives its expected result from the actual producer, not an assertion that
all Git versions fail at256. The short linked case is explicitly a supported control,
**not proof of the unsupported long linked form**. That positive remains **UNVERIFIED**;
no stopped long setup or compatibility variant was retried.

| New evidence label (`files\<label>.log/.json`) | Actual result |
|---|---|
| `p10-git-refusal-red` | **FAILED**, 2 methods / 20 assertion failures, 1320.528s, 81 subprocesses, exit1; sealed checkout-byte test-only delta on `f7d2151` |
| `p10-git-refusal-green` | **2/2**, 850.584s, 81 subprocesses, exit0; separate sealed checkout-byte corrected candidate |
| `p10-git-refusal-frozen` | **2/2**, 793.558s, 81 subprocesses, exit0; raw immutable `a559c9f` bytes |
| `p10-git-refusal-preservation` | **22/22**, 1736.440s, 93 subprocesses, exit0; the same raw immutable `a559c9f` bytes |

Every run passed synthetic preflight, retained an unchanged source seal and completed
exact-root cleanup at the same location. No success summary contains skips. The
two final manifests are identical, SHA-256
`9b29bb4c830f8a4ebebe293040771e30531eda925fa5e15bcb863ebddbdce84f`.
The driver is unchanged at SHA-256
`20fd93419905399213195f58c23d1e7f83701a37032cef263a18481bf1f9ab3d`.

Final commands retain the earlier driver command form with `--git-bytes`,
`--script tests\integration\copilot-kit.py`, `--temp-length 79` and `-v`.
The Git pair selects:

```text
CopilotKit.test_long_git_metadata_runs_real_effective_ignore_validation
CopilotKit.test_git_verification_directory_linked_and_plain_folder_controls
```

The preservation run selects the exact union of the prior seven-case, fifteen-case
and four-case groups, plus original missing-ignore repair, deduplicated to22:

```text
CopilotKit.test_canonical_default_caller_child_keeps_verified_parent_and_unbound_child
CopilotKit.test_canonical_required_caller_policy_refuses_missing_drifted_and_conflicting_context
CopilotKit.test_crlf_source_and_autocrlf_clone
CopilotKit.test_full_protocol_is_portable_and_project_prose_survives_update
CopilotKit.test_incomplete_protocol_marker_refuses_without_changes
CopilotKit.test_interrupted_adapter_publication_requires_explicit_owned_recovery
CopilotKit.test_invalid_inventory_root_types_are_clean_errors
CopilotKit.test_inventory_traversal_and_windows_paths_refused
CopilotKit.test_joined_installed_dependencies_cannot_be_hidden_by_inventory_removal
CopilotKit.test_joined_runtime_dependencies_refuse_incomplete_source_before_writes
CopilotKit.test_long_generated_link_metadata_checks_real_missing_and_linked_targets
CopilotKit.test_long_protocol_metadata_preserves_prose_eol_and_modified_block_refusal
CopilotKit.test_long_public_directory_index_is_selected_from_the_real_source
CopilotKit.test_missing_native_helper_cannot_fall_back_to_target_pythonpath
CopilotKit.test_missing_runtime_ignore_is_detected_and_repaired
CopilotKit.test_modified_managed_file_refuses_entire_update
CopilotKit.test_native_publication_keeps_the_original_263_character_destination
CopilotKit.test_original_default_113_128_paths_init_check_and_owned_recovery
CopilotKit.test_source_update_is_reviewable_and_deterministic
CopilotKit.test_swarm_wrapper_and_complete_source_resource_inventory
CopilotKit.test_symlink_parent_refused
CopilotKit.test_unmanaged_collision_refuses_before_foundation_writes
```

This re-executes classic base109/source116/target182 and atomic256->263, the actual
default target113/store128 and replacement218->260, canonical installed caller/child
with retained304-character history, required-policy refusals, managed/user-prose
protection, complete resource closure, actual interruption/explicit owned recovery,
inventory/link/collision refusals and CRLF/source update. No fixture root, default
store, target basename, assertion or recovery permission was shortened or relaxed.
It is a new focused22/22 result, not a relabelled historical15-case or strict aggregate.

#### Git-refusal checkpoint seals and retained evidence

| Artifact at `a559c9f` | Exact Git-byte SHA-256 |
|---|---|
| `bin/li-copilot.py` | `f43ac1d9ebe1a4f961754014d33fae2738d240d78bf35a02947597f7ef3e18fc` |
| `tests/integration/copilot-kit.py` | `23a78f320425c4b1f7ddec9481e05568f726390affbc432bfb73445f926701c0` |
| `docs/lifecycle.md` | `4f6771383b8ed17c663d95a473d76e29928223212ebf9db745516318767bfe13` |

All new evidence is in the same builder session `files` directory identified above.
The following hashes bind its raw metadata and logs, not newline-normalized text:

| Label | JSON SHA-256 | Log SHA-256 |
|---|---|---|
| `p10-git-refusal-red` | `b35845ba2ec33a46ae70eb43df54dc9ffefbfa5d6ef3003c087c66bc021e7597` | `f449d739707b7bf3b8207707b6786579fbf9e8538719c6e0562d052264c15cda` |
| `p10-git-refusal-green` | `81eef08efad965a1047986365b8185e7986015fec1ff1195e2532b64bd6476ba` | `d1b6a64218b93f10009ab3adc1add6a6c3a2b28758270ca2d002a99ce6611698` |
| `p10-git-refusal-frozen` | `cec401c3a1f511f8523b702673eaec84f72fc74e3c3a081a7bb2a1ad1d5ae009` | `3d695d127a9258f7e9b43ce9d4b8b99feec1d7fb1884f635f05d549c71b49853` |
| `p10-git-refusal-preservation` | `321a2d695bbdbadff83e9e24a17ad4bc0a310226fa740c266876c1879b3c2196` | `9d2b33319298bae05ceb9e67cf2e32410ea7049a3d104827f194e115f681d08a` |

`p10-git-refusal-scope.json`, SHA-256
`5a4fc2de2fdc259c077eab9010be2498d9a4084dee3ee7e6e73530f2e76bad67`,
records exact authority/producer/dependency seals, 27 synthetic read-only inspection
roots, preserved ASTs and the current-Python compilation/3.9 grammar checks.
`p10-git-refusal-evidence-index.json`, SHA-256
`e555e589e38271eb762ebc98b5a58779e7b2ea13e4eb54ad79fff6f8d42801e4`,
binds all four new runs, expanded argv, source manifests, actual producer/adapter
outcomes and cleanup. It also verifies that the prior22 indexed log/JSON pairs and
their original index hash are unchanged. The evidence indexer reads summaries with
universal-newline handling while hashing the untouched raw log bytes.

At `a559c9f`, builder self-check found no additional open finding in that bounded correction
(P1:0, P2:0, P3:0); this is not independent review of either the correction or P10.
The historical120/128 remains **FAILED** and no second full suite was run. Historical
D01/D03, all stopped Git diagnostics, F01's independent closure and the original
dependency attribution remain separate. Reviewer
`1578dfd8-f239-4eba-989b-3c4bde3e5792` must complete full SPEC before first eligible
whole-P10 QUALITY; no acceptance, integration or A13 release follows from these
builder results.

### F02 preserve producer ownership decisions

**Authority:** `79a36cc125a8e96b1cd1a1b4e2920fa8be1df8d4`, original
A12.1.a/.1.d, A12.2.b and its explicitly recorded repair leaves A12.2.b.r1/r2;
guard-only clarification `5d4651b5eb0e5be6bee44da13f52c4f5986cf028`.
Both complete appended card sections were read before their dependent changes.
The complete 364-line independent rejection
`c1a38a03e0efb54454e14bddee7cd07b4af6e1e3` was read and verified: sole parent
`4c0519e3`, sole review artifact, Git-byte SHA-256
`fee7f46cdc7c6aacfb60561de2693b3a297d939457a995961d9402e47778aa4c`.
Coordinator preservation `3649dc5` is report-only and was not imported as product.

The review's actual installed caller retained default304-character profile history.
Its short116 control preserved seeds; its plain256 child lost eight existing
files/fields while scaffold returned complete/0. It had no Git metadata. This is a
consumer ownership defect, not the stopped external Git limitation. Independent
runtime9/9 and lifecycle53/53 were separate positives, not complete SPEC clearance.

Product `a797b2d8e357d319e549015f591bc85421535cf7` directly follows `4c0519e3`:
four paths, 553 additions / 49 deletions. No new dependency is imported.

| Owned path | Additions/deletions | Bounded change |
|---|---:|---|
| `bin/li-lifecycle.py` | 81 / 49 | Native-aware planning and captured expected states in the six released functions/calls |
| `tests/integration/copilot-kit.py` | 270 / 0 | Three installed preservation/admission tests and four private fixture helpers |
| `tests/integration/universal-lifecycle.py` | 188 / 0 | Four focused long migration/pointer/extension tests, three fixture helpers and trusted native-path import |
| `docs/lifecycle.md` | 14 / 0 | Original write authorization and explicit non-atomic guard boundary |

`destination` changes only parent-type I/O operands. `migration_changes`/`move`
capture paired bytes/state using `read_owned`, use checked native enumeration with
logical relative names, and preserve hidden files, collisions and historical stubs.
`scaffold` preserves existing regular seeds; genuine absences carry create-only
`None` expectations. Combining migration and foundation plans does not replace
their captured expectations. The migrate branch forwards that plan, and
`extension_pack` keeps all four outputs create-only, including native same-location
directory operations needed to reach the original long target.

`runtime_publication` no longer samples current files to grant overwrite authority.
It requires equal write/expected sets, retains modes from those original states,
keeps guard-only inputs disjoint and delegates the original write expectations
unchanged to `apply_files`. A fresh observation can reject, not authorize replacement.
No no-op user-file entries are inserted. Existing v5 marker comments remain exact;
only the existing documented `autoMemoryDirectory` value is changed in local settings.
`json_field`, F01 `migration_inventory`, `operation_profiles`, the adapter branch,
Git predicate/command/error behavior and every other lifecycle function are unchanged.

#### Admission boundary and failed probe

The first complete late-change probe exposed a real distinction in the accepted API:
`apply_files` requires equal write/expected/mode key sets. An unchanged v5 marker
is not a write and cannot be smuggled into that plan to gain a guard. The producer
reported this exact constraint rather than editing the engine or rewriting user files.

`p10-f02-guard-seam` remains **FAILED**: 1 method / 1 failure, 129.502s, exit1.
Its ten producer-entry interventions and actual-writer late creation refused, but
a marker edit at actual `apply_files` entry returned complete/0. Metadata SHA-256
`9c7b4ac27714a2b2c17f8eb0c653393821c0fc3ae6efaf6803d53931d0db8a6e`,
log SHA-256 `00d04640a75bd0df1e27d087f32b5e946ea50753f242d24a8e2d7db0a11cfd14`.
That sealed source/output was not changed while running or relabelled afterward.

The coordinator's committed clarification selected **consumer admission**, not a new
transaction contract. Captured unchanged inputs are checked immediately before
delegation. Their edits before that check refuse without target/store mutation.
Late write-set creation/edit/deletion is still protected by the existing writer;
a marker actually being migrated is a write and receives that protection too.

An unchanged marker edited **after** its final admission check is explicitly outside
the cross-file guarantee. The separately labelled observation returns complete/0,
retains the intervening marker and proves it is absent from the write plan/receipt.
That is an observed limitation, not a successful refusal or proof of atomicity.
No shared schema, transaction implementation, optional callback or permission
expansion was added. The earlier failed probe remains failed history.

#### Actual regressions and preserved cases

All new subprocesses use the existing synthetic driver/preflight and actual trusted
or installed source. No shipped source is patched to inject a result. Test-only
in-memory interception makes a real fixture edit, then delegates to the real
publication function or `apply_files`; it never supplies a mocked successful write.
Every intervention remains at its case's same 256-character target from planning
through publication, with no shortened path/store or Git metadata.

The installed seed scenario reproduces the independent literal seeds: CRLF AGENTS,
CLAUDE, lessons, memory index and rules; commented v5 layout; custom ignore prose;
custom settings plus the memory pointer; and unrelated-file control. It verifies
short116 and long256, all eight fields/files and their modes, exact settings
surroundings, genuine absent seeds, mixed existing/missing files, repeat initialization,
`check`, dry-run and `--no-memory-pointer`. The caller remains target113/store128,
with its actual installed source and304-character history; source, caller/home/pin,
synthetic personal home and unbound-child conditions are retained.

The clarified race matrix contains **21 required refusals**: eleven before final
consumer admission and ten at the actual writer entry. These cover absent seed and
migration destination creation, edit/delete of ignore/settings/legacy inputs,
new-marker creation and an existing marker genuinely scheduled for migration.
The unchanged v5 marker is guarded before admission, not after. Each required refusal
asserts nonzero/empty-success output, exact intervening target bytes/modes/directories,
no recovery-store creation, unchanged source/caller/home and no child binding.

Separate lifecycle cases verify nested hidden knowledge/runtime moves and retained
redirects, complete collision/stranded-redirect/non-file/invalid-marker/malformed-JSON
refusal, pointer-only repair, BOM/CRLF/custom-field preservation and pointer opt-out.
All four extension outputs are tested both existing and late-created at plain256:
the consumer bytes survive and no transaction store is created. Original migration
interruption/recovery, caller policy, explicit child and F01 catalog tests are retained.

| Evidence label (`files\<label>.log/.json`) | Actual outcome |
|---|---|
| `p10-f02-red-installed` | **FAILED**, 2 methods / 14 failures, 317.039s, 24 subprocesses, exit1; original product with test-only delta |
| `p10-f02-red-eight-seeds` | **FAILED**, 1 method / 28 assertions, 152.901s, 16 subprocesses, exit1; every original seed's before/after state recorded |
| `p10-f02-red-lifecycle` | **FAILED**, 4 methods / 11 failures / 1 missing-moved-file error, 23.743s, 24 subprocesses, exit1 |
| `p10-f02-lifecycle-focused` | **18/18**, 58.169s, 68 subprocesses, exit0; preliminary owned consumer |
| `p10-f02-guard-seam` | **FAILED**, 1 method / 1 failure, 129.502s, 15 subprocesses, exit1; admitted-marker window described above |
| `p10-f02-seeds-focused` | **1/1**, 282.416s, 16 subprocesses, exit0; preliminary installed seed check, not an aggregate rewrite |
| `p10-f02-green-installed` | **FAILED**, 3 methods / 1 failure, 742.705s, 40 subprocesses, exit1; first two methods passed, third setup failed before its observation |
| `p10-f02-green-lifecycle` | **25/25**, 239.634s, 112 subprocesses, exit0; corrected lifecycle plus unchanged F01 |
| `p10-f02-post-admission-observation` | **1/1**, 130.343s, 4 subprocesses, exit0; new unchanged-dimensional fixture observes the declared limit |
| `p10-f02-frozen-copilot` | **27/27**, 5092.210s, 214 subprocesses, exit0; one actual selected run on raw `a797b2d` |
| `p10-f02-frozen-lifecycle` | **25/25**, 146.999s, 112 subprocesses, exit0; raw `a797b2d` |
| `p10-f02-frozen-runtime` | **9/9**, 25.741s, 1 observed subprocess, exit0; unchanged runtime with raw `a797b2d` |
| `p10-f02-frozen-profile` | **1/1**, 16.203s, 15 subprocesses, exit0; real copied validation and complete cleanup on raw `a797b2d` |

The three-method candidate failure was actual default adapter setup:
`WinError 5` replacing a transaction's `journal.json`, before the third method reached
its guard observation. Its failed aggregate, raw error and source seal are retained.
No shared helper, permission, retry, global setting or root was changed to make the
separate one-method observation succeed. The cause of that access denial is not
claimed diagnosed or fixed by F02.

The final Copilot selection is the unchanged prior22 preservation selectors plus
the two approved Git-refusal methods and these three F02 methods. It retains the
classic109/116/182 and256->263, default113/128 and218->260, actual canonical caller/
child, required-policy, managed-file/prose/source-closure, interruption/owned recovery,
Git0/1/error and installed-source controls. It is not a second full repository suite.
The final lifecycle selection is `ScaffoldMigrationLifecycle MigrationInventory`;
runtime runs the unchanged nine-method file, and the profile selector is
`ProfileLifecycle.test_validation_skill_executes_shared_contract_without_activation`.
The actual27-case run completed on its original attached process and sealed source;
it was not restarted or assembled from earlier partial results. All final groups have
zero skips. The final Git methods independently retained34 actual producer/adapter
operations and26 zero-write refusals, separate from the21 F02 refusal cases.

#### F02 source and evidence seals

| Exact `a797b2d` product path | Git-byte SHA-256 |
|---|---|
| `bin/li-lifecycle.py` | `a4148ba69f4c2b752486d44b299a00449311af62b3de2f71404279c5a8d72203` |
| `docs/lifecycle.md` | `c723ea622f370237797712d316be67626a70ce41e292a40927d08bdfa136525e` |
| `tests/integration/copilot-kit.py` | `21cef63b23c7ffe81d6a30f3154e01f96f748367974f425c2cfcfb1d02f93e42` |
| `tests/integration/universal-lifecycle.py` | `e63ce62bab65b6d03417d951b9fbab1ba8d239de170fe36f3d4e97531c07f296` |

`p10-f02-frozen-scope.json`, SHA-256
`f13a91c6c560e39ae0b2900c3b85654b00f08c29fdea8d93a336625fcdbd03bc`,
binds both authority revisions, the rejection and direct product parent. It proves
the exact six-function scope, migrate-only main change, unchanged F01/JSON/profile
and Git/adapter ASTs, all39 prior Copilot and53 prior lifecycle test ASTs, and14
unchanged contract/core/adapter/provider/fixture hashes. The original29 Copilot
methods remain within those preserved methods and the preceding frozen scope proof.
Python3.9 grammar remains syntax evidence, not3.9 execution.

All four final manifests are identical, with digest
`fa1d4000ac15c8683b735d6ea889cf9b240267f4563b1c0f5a0fc02522ddb15f`
over sorted compact JSON. All13 new runs retained their source seals and
completed exact-root cleanup at the same location. The unchanged driver hash is
`20fd93419905399213195f58c23d1e7f83701a37032cef263a18481bf1f9ab3d`.

| Final evidence label | Metadata JSON SHA-256 | Raw log SHA-256 |
|---|---|---|
| `p10-f02-frozen-copilot` | `d9997312371aad1c29e13cbbe99146de15d6eb585f1b850a234de30c293ac1a4` | `726907b43b158f4da827ce9d518409d37d5a0941a8fa5deb0108e5f38b99e5ea` |
| `p10-f02-frozen-lifecycle` | `19b305b84784bc2149fb2cf5d2193b47e4b8354624b820abfd8a814f8ee1be88` | `0ba73d0cfcf57cefbfdd254eb0f9724cc49fc32d5498d025a060c8d70d09758f` |
| `p10-f02-frozen-runtime` | `09f4467ccf2e53d17ca7818071f1705a880b50d05a8cb159ccb3e7a7f9662633` | `3cf84116dc4c652d0f9e142697d477d4ae537c8235ac0efaab3280cd304b1a2e` |
| `p10-f02-frozen-profile` | `a1c6f5b8136a6c1fbd86464c0a66c2f6e9290ee017e4cda2ebc780b87ad37ac5` | `7aacffca53f8ca4b6c78eb7fad226ed2cb73cd43c937b1d941c003cd9eb1e45f` |

In the same session `files` directory, `p10-f02-evidence-index.json`, SHA-256
`7028e15f384b50c986975b744e39eb16ac3544b17c05918abaa2607c68ba431b`,
binds all13 new log/JSON pairs, exact expanded selections, source identities,
the21 actual refusal records, protected seed states and the explicit post-admission
limit. It reverified all26 earlier indexed log/JSON pairs unchanged, including their
failed records and both prior index seals. No historical aggregate was rewritten.

At the F02 handoff, builder self-check found no additional open implementation finding
in that correction (P1:0 / P2:0 / P3:0). It was not an independent review stage; the
then-latest independent report still had P1:1 (F02), P2:0 / P3:0.
The access-denied setup observation remains a disclosed, unrepaired shared-runtime
observation; a subsequent successful fixture is not proof of its cause.

F02 remained independently open at `7bf5a315`; subsequent independent `c1f6bad1`
closed that bounded repair and retained F01, then stopped on F03 below.
All failed historical aggregates, stopped Git experiments, D01/D03 observations and
prior attribution remain distinct. No closure of whole SPEC, A13 release, integration,
catalog regeneration, remote action or new host support claim follows.

### F03 truthful native layout observations

**Authority:** `0ad183a7464288e016febc0523d34e57f8e88ea3`, complete appended P10
section "F03 truthful native layout observations", original A12.4.b/R01. Recovery
coordinator is session `88aecc43-40f9-41d4-8947-6c2fb0a55481`; the original coordinator
worktree was not changed. Authority was read through immutable Git, not imported.

The complete 479-line independent report
`c1f6bad1a366bcb9088c118e2118a5ea3686a8b8` has sole parent `7bf5a315` and is
byte-identical to coordinator preservation `5f3d885`. Report SHA-256:
`91196f856572e4f3261a6702ff8827516178ee112edb1532b7ea2e9bb281a0de`.
It independently closes F02's bounded producer contract and retains F01. Its
latest complete gate is **SPEC FAIL**, P1:0 / P2:1 (F03) / P3:0, QUALITY NOT STARTED.
Its failed/stopped remaining-spec runs and journal `WinError 5` remain history.

**Product:** `036e90330dd96fa27dd1b1574e7d39a12af2eaf2`, direct parent
`7bf5a31526cc39951978945df86dea85796fd01b`. Exactly three paths changed:

| Path | Additions/deletions | Scope |
|---|---:|---|
| `bin/li-lifecycle.py` | 37 / 13 | Only `layout_observation`, including its local metadata helper |
| `tests/integration/universal-lifecycle.py` | 198 / 0 | Four new `MigrationInventory` methods and four private fixture helpers |
| `docs/lifecycle.md` | 5 / 0 | Truthful native observation and explicit inspection errors |

The reader now uses the already accepted `native_io_path` after `safe_path` for
marker/legacy metadata, file types, directory entries and redirect destinations.
Only `FileNotFoundError` denotes absence; other observation errors propagate.
Checked `iterdir` traversal retains logical relative names and exposes enumeration/
disappearing-entry errors rather than silently dropping entries. `read_owned` still
reads the marker and relevant legacy headers. Observation keys, version rules,
schedule versus target-state separation and retained redirect meanings are unchanged.
This is read-only diagnosis, not a migration, installation or completion receipt.

The F01 catalog parser, F02 producer/admission functions, outer doctor, `json_field`,
profile/context/path/snapshot/transaction implementations, native installers and Git
calls remain unchanged. No retry, compatibility variant, shared API, dependency,
source preflight constant or coordinator A23.4.g3 WIP was imported or edited.

#### Exact RED and verified behavior

The test-only RED retained the independent literal seeds and their exact hashes:

| Seed | Bytes | SHA-256 |
|---|---:|---|
| `.claude/lintel-layout.yaml` | 66 | `b6ff0ad8776fa67e263c5c694a9f44c3f41f1a62399863862fa20c4c117e99ee` |
| `tasks/lessons.md` | 63 | `34c67a8e8ed5b15d967750f15c1b5a628bd0f714501a1ce85b1f1144f0dad7c3` |

The same actual skill block and copied trusted dispatcher ran from an unrelated
caller against plain short153/marker180/lessons170 and long256/marker283/lessons273.
No Git metadata, alternate store, shortened default home, removed pin or mocked
helper result was used. On unchanged product, short returned correct
`incomplete`/version5/`["tasks/lessons.md"]`, while long returned exit0 with
`not_applicable`/null/empty lists. Both preserved state; the error was false diagnosis.
After repair, both return the correct incomplete observation with unchanged schedule
and row schema.

The four new methods perform32 actual skill calls:18 successful observations and14
explicit error cases on each GREEN source. Coverage includes real absence,
needs-migration, incomplete/current, existing future-version semantics, nested hidden
legacy knowledge/runtime files, retained redirects and a missing redirect destination.
Invalid/duplicate/non-UTF8 markers, non-file metadata and an invalid legacy-directory
type refuse. Actual Windows exclusive read handles produce real permission-denied
errors at both lengths; they are released after the call, with no ACL or global
setting change. The long unreadable marker falsely passed on RED.

Each call verifies complete before/after maps of source, target, unrelated caller,
synthetic home and derived roots: bytes, modes, sizes, attributes and directories.
All32 maps are equal on RED and GREEN. No audit, profile, pin, registry, receipt,
snapshot, transaction or recovery store is created. The immutable source is rechecked
by the unchanged driver, and each newly created exact fixture is cleaned only at its
own location. No retained reviewer or older incident fixture was touched.

| Evidence label | Actual result |
|---|---|
| `p10-f03-red-observation` | **FAILED**, 4 methods / 13 assertions, 21.575s, 33 subprocesses, exit1; only tests differed from `7bf5a315` |
| `p10-f03-green-observation` | **13/13**, 60.444s, 80 subprocesses, exit0; sealed corrected working source |
| `p10-f03-frozen-observation` | **13/13**, 51.330s, 80 subprocesses, exit0; raw immutable `036e903` bytes |

The final selection was:

```text
MigrationInventory
ProfileLifecycle.test_migrations_use_source_catalog_and_keep_overdue_unknown_states
ProfileLifecycle.test_doctor_reports_bytes_not_hook_counts_or_historic_host_activity
```

This combines the four new methods, all seven unchanged F01 methods and two existing
reader/caller controls. It ran through the same synthetic driver with `--git-bytes`,
`--script tests\integration\universal-lifecycle.py`, `--temp-length 79` and `-v`.
Every preflight, source seal and exact-root cleanup passed, with no skipped method.
These13 methods are not a new full repository/P10 aggregate.

#### F03 immutable seals and remaining gate

| Exact `036e903` product path | Git-byte SHA-256 |
|---|---|
| `bin/li-lifecycle.py` | `11bcaa9ac61a4352b4126a13c2f43c6f13de26121425341f4578141f783ad109` |
| `docs/lifecycle.md` | `1a460d4d93f4d1397c7d35ea4c5dde566fdbb1051d3ee0bd60426734b918ff34` |
| `tests/integration/universal-lifecycle.py` | `cf9ba0383897eb5ced5b25e441c360f19e5da2dd3a41d9b5369ef126b7d709df` |

The unchanged real skill, dispatcher and catalog retain respective Git-byte SHA-256:
`736eae4f2e30154be86a55223ff7f9133b013a0c7f7ab8483fc94d70b015adba`,
`8075bb67aad321701235b44f570521b96d1aa6b086a7f981b159f63b18beeb7f`,
`8c7cf772687d272b1bd9ab8dbc3b6ab088a1ce6ce81c402bbc9f11660af1f273`.
The final source-manifest digest is
`0f7e35899bcea67dfacfc34938a6bdba10443d7258af074586b942f732304e55`
over sorted compact JSON. Driver SHA-256 remains
`20fd93419905399213195f58c23d1e7f83701a37032cef263a18481bf1f9ab3d`.

| Label | Metadata JSON SHA-256 | Raw log SHA-256 |
|---|---|---|
| `p10-f03-red-observation` | `e43ef3b54b868779095adb49422b43606766db8cf1e82f80c3eefeb0ed465ff2` | `adaead60c623bf535cf2a0edf01eb552ed8c3b079db307224730361fad7db3e1` |
| `p10-f03-green-observation` | `ac6e1f11010cfe635ec59df93257666645a485cb779bdcdf934f2cb2f846601d` | `1047f5a771dcad3acf5fe0a9161a9b75166e9abd73ecb3a38d4f975bfbd424fd` |
| `p10-f03-frozen-observation` | `43764a5d1a488b22c4cfcf96270a62216562e9ead310940a6868ff49e4a7c4da` | `460ebdc9df689623c647d017b55bce480d44427f8e2b17d2a1aab6e6bd86b735` |

In the existing builder session `files` directory, `p10-f03-frozen-scope.json`,
SHA-256 `2632ae4b69734207bd7c623073cb4f380ccd2f159d569c87258938cf60ec97f1`,
proves the one-function AST scope, all57 prior lifecycle test ASTs and19 unchanged
contract/core/caller identities, including the entire prior Copilot test file.
`p10-f03-evidence-index.json`, SHA-256
`7ced8590c96fe01461b09433ad9c7463f67a61f0f7ded02619805badebb1c4d4`,
binds all three new log/JSON pairs, exact real-call results and full-map equality.
It reverified all39 previously indexed log/JSON pairs and three earlier indexes
unchanged. Source/dependency attribution and all failed/stopped records are retained.

At the F03 handoff, builder self-check found no additional open finding in that repair
(P1:0 / P2:0 / P3:0); independent F03 was still open. Prior F02/core/default/canonical/
native evidence is retained at its actual revision where applicable, not rerun or
relabeled as fresh complete-P10 acceptance. The unresolved journal access denial,
D01/D03, historical120/128 **FAILED**, stopped Git variants and unverified long linked
positive remain distinct. No retry or broad suite was used to erase any failure.

Subsequent independent `131d3db4` closed F03's native-observation counterexample,
then found the distinct canonical-redirect semantic gap below. Its missing-destination
coverage exposed that F03's unresolved redirect case was nested, while the fixed
canonical-prefix samples all had valid destinations. No complete redirect-class pass
is inferred from the earlier13 methods.
Final fan-in/reducers belong to the recovery coordinator. A13, integration, release,
remote publication, real-home actions and additional host-support claims remain closed.

### F04 canonical redirect visibility

**Authority:** `b8f0cf7b3e827a90f5afc0c2eacc3a809adbd804`, complete appended P10
section "F04 canonical redirect visibility", original A12.4.b/R01. All291 lines
of independent `131d3db4b61cdbb1792169913710522a15ecfeb6` were read; sole parent
`50e654af`, report SHA-256
`14c126cb0545212c7d4888fc288c3b34400fd04576e6f717d00ee89dbc15725d`.
Its coordinator-preserved `4b5f592` bytes are identical. It closes F03 and retains
F01/F02, but full SPEC still fails on P2 F04; whole QUALITY has not started.

**Product:** `6e1b1e0047befae8fc45fd0858d337b117e2ff97`, direct parent
`50e654afcf7fae0548e4d16a4bf2d52b725523bc`; three paths, 165 additions / 11 deletions.

| Path | Additions/deletions | Scope |
|---|---:|---|
| `bin/li-lifecycle.py` | 20 / 6 | Only `layout_observation`, with one local mapped-destination classifier |
| `tests/integration/universal-lifecycle.py` | 139 / 5 | Four new `MigrationInventory` tests, three private helpers and two existing fixture-helper extensions |
| `docs/lifecycle.md` | 6 / 0 | Canonical stub verification, missing destinations and explicit errors |

The reader consumes unchanged `KNOWLEDGE_MOVES` and the existing ADR relative
mapping. One local `retained_redirect` check verifies the exact expected prefix
and mapped destination through `safe_path`, native metadata and `read_owned` for
files. Missing destinations remain legacy; v5 yields `incomplete` and no marker
yields `needs_migration`. Correct-kind, readable destinations alone establish
retained stubs. Wrong kinds, links/reparse paths and inaccessible observations are
errors. Historical README-to-directory recognition precedes the generated
README-to-file form, fixing the earlier false legacy classification of a valid file
redirect. Near-match prose remains ordinary legacy content.

No arbitrary destination is extracted from consumer text. The reader does not
compare destination content against an invented original image, write or repair
stubs, or alter producer behavior. `migration_changes`, its mappings/guards,
F01 parsing, F02 publication, outer doctor, core/native/profile/transaction/Git
implementation and coordinator dependency-preflight constants remain unchanged.

#### Discriminating producer/reader evidence

The exact reviewer three-call fixture was reproduced before implementation:
two independent plain short153 targets with the canonical18-byte marker and117-byte
lessons redirect, both at marker180/redirect170/destination179. Only the positive
control contained the retained destination; the stranded fixture never contained it.
The unchanged real migrations skill was called for both, then the actual
`li-migrate-claude-home --dry-run --repo` for the stranded target.
On RED both reader calls reported `current`, while the producer refused with
`Stranded redirect without its destination: tasks/lessons.md`, exit2/empty stdout.
The corrected reader keeps the positive current and reports the stranded one as
incomplete/legacy lessons. The producer remains unchanged and refuses identically.

The finite matrix covers all four knowledge mappings, historical ADR README-to-directory,
generated README-to-file, an ordinary root ADR and a nested hidden ADR. Every
class is exercised at the same short153 and plain256 dimensions with valid, missing,
unmarked-missing and wrong-kind destinations. Reader and actual migration preview
are compared on the same fixture. Additional cases cover near-match prose, actual
symlink destinations and Windows exclusive read handles for every file-destination
mapping, with no permission/global-policy changes.

Both GREEN runs have223 actual F04 calls:128 matrix calls,32 near-match calls,
60 link/unreadable calls and the exact3-call contrast. Of these,112 are reader
calls and111 are actual producer previews. Reader results are66 valid observations
and46 explicit errors; producer results are32 previews and79 explicit refusals.
All before/after maps are equal: source, caller, target, synthetic home/derived paths,
bytes, modes, sizes, attributes and directories. No receipt, profile, transaction,
recovery store or other side effect is created. No helper/subprocess success is mocked.

| Evidence label | Actual outcome |
|---|---|
| `p10-f04-red-redirects` | **FAILED**, 4 methods / 65 assertions, 199.879s, 182 subprocesses, exit1; original product with test-only delta |
| `p10-f04-green-redirects` | **17/17**, 463.159s, 303 subprocesses, exit0; corrected sealed working source |
| `p10-f04-frozen-redirects` | **17/17**, 469.282s, 303 subprocesses, exit0; raw immutable `6e1b1e0` bytes |

RED retained181 actual F04 calls; failing per-case assertions stop their remaining
paired assertion work, never silently count it as verified. The65 failures include
the missing canonical destinations, valid generated README misclassification and
unverified wrong-kind/link/unreadable destinations. Near-match controls passed.
This RED is distinct from every prior F01/F02/F03 failure and aggregate.

The focused final selection is the existing `MigrationInventory` class plus
`ProfileLifecycle.test_migrations_use_source_catalog_and_keep_overdue_unknown_states`
and `ProfileLifecycle.test_doctor_reports_bytes_not_hook_counts_or_historic_host_activity`.
Its17 methods retain all seven F01 and four F03 tests plus the four new F04 methods
and two caller controls. Original native/core/default/canonical/adapter evidence stays
at its actual revision; no unrelated suite is restarted. The independent reviewer
may continue separately authorized, deduplicated adapter/profile SPEC on its unchanged
source. That concurrent work is not this builder's result and was not stopped or edited.

#### F04 seals and limits

| Exact `6e1b1e0` product path | Git-byte SHA-256 |
|---|---|
| `bin/li-lifecycle.py` | `99295ac651a07e6a8a59d03034534a731b8ff22fb9391e047e8794d6d2c621bf` |
| `docs/lifecycle.md` | `9f5b868728a03bb8ce7210701347af8871864eea6cca3cf63099d7f73b5b6070` |
| `tests/integration/universal-lifecycle.py` | `c32c6a97a85491638a71a7dc3fd976675b9cc7e179956300145cd50ecc4d2c79` |

`p10-f04-frozen-scope.json`, SHA-256
`c9952c032e45cc119b543dcc3957b9b52569d9494acb6e0806df6469511befc7`,
binds authority/rejection/parentage, unchanged mappings and every function except
`layout_observation`, all61 prior lifecycle test ASTs and20 unchanged core/contract/
caller identities. Only `_layout_targets` and `_layout_inventory` gained private
fixture options; their old default behavior/assertions and all F03 test methods remain.
No shared schema, new module, provider or distribution/preflight constant changed.
The frozen selection completed on its original process with no skips, source changes
or cleanup error. It includes255 full-map real observations/previews:223 F04 calls
and32 retained F03 calls. The final manifest digest is
`4af65a4db58d01f43d8cfb1bf6c055ecde03591e3cb4d42aa1d521a3e0149cc3`
over sorted compact JSON. Driver SHA-256 remains
`20fd93419905399213195f58c23d1e7f83701a37032cef263a18481bf1f9ab3d`.

| Label | Metadata JSON SHA-256 | Raw log SHA-256 |
|---|---|---|
| `p10-f04-red-redirects` | `87b9792ed79c0f99bd2ba4df030df6a75467ef65accb61a4e626978181dd718a` | `0520778cd10124b690265faae1938eeede50688ebbe5d45dc5e4a14eb5d59e1f` |
| `p10-f04-green-redirects` | `13e541eb60efe55770e773e1075c5609ddfbda72dbaa706391a65e7f8f5eac40` | `77213742b60234e20f9c55499c1e09bc379a7b2d5dd8c7a3b8f3e49f316527fd` |
| `p10-f04-frozen-redirects` | `cd93198e218f04df286d60dd61fe107ac669f4e364f26752b1e821055edd5182` | `6081d5c03aca14df621c9e0e92b9c61a571a3a39f8fa34d38e9da19b6f48b759` |

`p10-f04-evidence-index.json`, SHA-256
`460bc566ef47c262c5d9840a97300d57c0cbb295367decbc7fe2481a08cb3a73`,
in the same builder session `files` directory binds the three new real runs, expanded
commands, actual three-call contrast, case/exit counts and exact no-write/source/
cleanup evidence. It reverified all42 previously indexed run pairs and four earlier
indexes unchanged, including every failed/stopped history represented there.

Builder self-check found no additional open finding in this bounded change
(P1:0 / P2:0 / P3:0); this is not an independent review stage. The latest independent
verdict remains P1:0 / P2:1 (F04) / P3:0 and full SPEC FAIL until the same reviewer
rechecks the new product. No concurrent reviewer result is assumed or claimed.

F01/F02/F03 scoped closures remain retained; independent F04 remains open.
Historical120/128 **FAILED**, journal `WinError 5`, stopped reviewer aggregates,
D01/D03 and unsupported long linked-Git positive remain explicitly unresolved history.
No compatibility attempt, automatic retry, source import, broad suite or waiver
was used. Same-reviewer F04 recheck, remaining original SPEC and then first eligible
whole QUALITY still precede acceptance. A13, integration and external delivery stay gated.

### Strict-run failure disposition

| Script | Attribution and disposition |
|---|---|
| `cohort6-fas3-skills-present.sh` | Historical old string-oracle failure; coordinator repair `935b640` imported separately, focused check passed |
| `v37-closeout-additions-present.sh` | P10 repair: retain all runtime/brand slots, shared native declaration and updated actual-delegation oracle; focused pass |
| `universal-lifecycle.sh` | P10 fixture repair: derive native no-Python PATH from actual Git/Bash locations, including `usr/bin`; complete 46-case rerun passed |
| `universal-profile-context.sh` | Historical three-block/PASS-prose fixture failure; narrow authorized follow-up `acf97f1` and eight targeted cases passed. No P07 runtime/schema/other method change and no whole-script rerun claimed. |
| `bin-scripts-executable.sh` | Historical P05 dependency mode 100644; coordinator mode-only repair `d53dc38` imported separately, all executable checks passed with unchanged CLI content |
| `catalog-regenerates-clean.sh` | Generated catalog drift; coordinator owns regeneration, not P10 |
| `cycle-footer-present.sh` | Historical missing welcome invocation; coordinator `935b640` adds the actual trusted footer call, focused footer/protocol checks passed |
| `skill-descriptions-trigger.sh` | P10 scaffold description corrected; focused pass |

Targeted passes do not manufacture a new aggregate pass. The remaining joined gates are
explicit; this report does not claim the entire repository is green.

## RED evidence and self-review

Before repairs, real native fixtures reproduced overwritten managed edits, deleted
operator extensions under shared directories, stale obsolete files, and completion prose
after failed validation. Scaffold/migration fixtures reproduced late-link writes, runtime
collision overwrite and unavailable pointer repair. Additional RED cases covered absent
structured helper operations, false host toggles, lost inline comments, source-closure
gaps, unowned store adoption and inconsistent terminal evidence.

Windows raw-drive ancestor handling initially stalled a native Bash fixture; that run
was stopped and discarded, not reported green. A subsequent all-file Bash run was too
slow on this host. Windows now uses the single native PowerShell performer, explicitly
selected and authorized. A default PowerShell 5.1 invocation was denied as Restricted;
no alternate runtime was used until the operator explicitly authorized existing
PowerShell 7 for disposable verification. Native null-string replacement and transient
sharing errors were corrected with exact-byte, bounded retry checks, not forced deletion
or permission changes.

The original self-review reported no known open P1 and repairs for the then-identified
P2 preservation, false-success and receipt-coherence problems. Independent SPEC later
found P2 F01, then independently closed that exact parser repair in `00a0bef`.
Neither the native-path unit, Git refusal correction, F02, F03 nor F04 reopens or modifies the
F01 parser. The observed external Git limit remains documented, and unsupported
long linked positive operation remains unverified; the authorized consumer now
refuses unavailable/failed verification before writes. This is not a self-assigned
review clearance. Independent `c1a38a03` found P1 F02, then `c1f6bad1` closed its
bounded repair and found P2 F03. Independent `131d3db4` then closed F03 and found
P2 F04; that canonical-redirect correction awaits independent recheck.
Generated catalog drift and final integrated
verification remain open. Latest complete independent SPEC is **FAIL**, and
whole-package QUALITY is **NOT STARTED**.

## Owned paths

The original implementation's exact changed-path list is `git diff --name-only
fb96f71342f7994c42bdfc7cd61e6d452e6acb54
a73cf9ee38977501c201225d8269dac3668c1080` (48 files):

- `bin/li-copilot.py`, `bin/li-doctor`, `bin/li-lifecycle`, `bin/li-lifecycle.py`,
  `bin/li-managed-transaction.py`, `bin/li-migrate-claude-home`, `bin/li-pack-scaffold`,
  `bin/li-scaffold`; `lib/managed_transaction.py`.
- `install/install.sh`, `install/install.ps1`, `install/native.sh`, `install/native.ps1`,
  `install/directories.txt`, `install/verify.sh`.
- `docs/client-adapters.md`, `docs/copilot.md`, `docs/lifecycle.md`,
  `docs/native-installation.md`, `docs/migrations/_INDEX.md`.
- `skills/{doctor,health,migrations,pack-create,pack-list,pack-switch,pack-validate,
  personas-rotate,profile-switch,role-new,role,roles-list,scaffold-internal-tool,
  scaffold-mvp,scaffold,v4-migrate}/SKILL.md`.
- `tests/behavior/{install-fail-closed,install-target-boundaries}.sh`;
  `tests/integration/copilot-kit.py`, `enterprise-workflow-snippets.sh`,
  `pack-source-target-resolution.sh`, `universal-lifecycle.py`, `universal-lifecycle.sh`;
  `tests/shape/extension-pack-contract.sh`;
  `tests/unit/managed-transaction.py`, `managed-transaction.sh`, `native-receipt.ps1`,
  `v37-closeout-additions-present.sh`.

The separately authorized fixture follow-up adds only
`tests/integration/universal-profile-context.py`, at the copied dependency list and named
validation method. F01 then changes only the existing lifecycle parser and focused
lifecycle tests listed above. The native-path unit changes only the five owned paths
enumerated in its evidence section. The latest Git refusal correction changes only
`bin/li-copilot.py`, the two new Git methods/private support in
`tests/integration/copilot-kit.py`, and `docs/lifecycle.md`. F02 then changes only its
four explicitly listed existing paths; F03 and F04 then change only the reader, their focused
lifecycle tests and observation documentation. This report is the only additional P10 artifact.
Coordinator dependency paths are attributed in their separate commits, not P10-authored
repairs.

## Limits and next gate

Actual execution: Windows, Python 3.11.9, GNU Bash 5.3.15, explicitly approved PowerShell
7.6.6 and read-only coordinator-provided jq 1.8.2 where needed. Python 3.9 execution,
stock Bash 3.2, Linux/macOS and denied PowerShell 5.1 remain unverified. No paid model,
real private pack, real global install/profile change, hook registration, private sync,
authenticated GitHub operation, network publication, push, PR, release or main mutation
occurred. Native mode projections are not ACL/xattr or universal power-loss guarantees.

P08's A13 reader gate remains closed pending the coordinator's exact interface decision
and actual producer-to-reader fixtures against an accepted checkpoint. No informal
field list or this unaccepted repair authorizes another interpretation or a second audit
schema. Historical D01 default-store failure and D03 cleanup ERROR remain failed
records; the new final-core builder checks are separate evidence for their independent
recheck, not self-closure. Git compatibility/setup experiments remain stopped; the
approved fail-closed consumer contract is implemented but not independently accepted.
The unsupported long linked positive remains UNVERIFIED.
The independently closed F02 guard-only contract is consumer admission, not protection
against edits after that read or a new cross-file transaction guarantee. F03's native
observation repair is independently closed. P2 F04 remains open pending review of this
exact canonical-redirect repair.
Final fan-in/reducers, catalog consistency, repeat independent specification **then**
first whole-P10 quality, and final integrated acceptance remain with the recovery coordinator.

**Cycle position:** BUILD implementation evidence -> independent specification review ->
first whole-P10 quality review. P10 is not self-approved or shipped.
