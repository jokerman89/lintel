# Synthetic inventory experiment

This is P14 test data, not the Universal implementation backlog. Its work.json
selects this fixture's original T001, T002 and T003. Keep those IDs.

Read spec.md, plan.md, prompt.md, and the verified effective profile and its
applicable source documents. Use the coordinator-provided trusted Lintel source;
do not discover helpers in this target or a personal home. This target contains
no customer, personal or credential data. Use only Python standard-library code,
local files and the explicitly supplied synthetic environment.

Native implementation is NOT released by fixture creation or a green preparation
test. The separate coordinator packet must authorize the actual actor and writes.
No network, install, hook activation, UI, external messages or publication.
