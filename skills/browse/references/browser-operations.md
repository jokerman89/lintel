# Browser resource compatibility

The canonical [browser operations and ownership](../../web-session/references/browser-operations.md)
now belong to `web-session`. This reference-only bridge keeps existing PDF and
other consumer links valid during integration; it is not a separate entrypoint.
